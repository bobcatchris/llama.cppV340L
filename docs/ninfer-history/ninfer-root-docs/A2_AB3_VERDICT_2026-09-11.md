# ab3_cellrun — FIX CONFIRMED AT PARITY + 6th surface (one-shot step skew) named; cell-B join-pending

Era bundle: tree `55dd8510`, src = 4045bfd4 (fuse fix), BIN md5
**e143847ca67257f920ce124c594b3eee** (card line 1/7 self-printed; GO = CORD seq-42
written stamp behind A1 seq-17 one-word GREEN on the real diff). Fresh guard at
claim in card line 6. Wrapper: p1ab3.sh (dual-cell design; cell-B never reached).

## HEADLINE: the pool-fuse fix produced PARITY-LEVEL acceptance
The request's own vpos frontier table (the true acceptance record; the request's
stats line never printed because of the wall below): round deltas 2,3,3,4,5,3,6,3,4,2
= a-values 1,2,2,3,4,2,5,2,3,1 -> ~23 accepted drafts in ~12 rounds ~= **.38-.42
acc_rate — the batched reference band (.42)**. The success bar ('ANY nonzero') was
met by warmup's clean stats line first (rounds=3 accepted=1, line 746, pre-listening
747 — WARMUP cell, answering CORD's question; warmup now COMPLETES, P1 re-confirmed:
zero 'Paged KV' strings in the entire log), then blown out by the request's frontier
arithmetic. .42-parity, the declared 'next datum', arrived in the same boot.

## 6th surface: rank-1 SILENT CLEAN EXIT at round-12 tail; rank-0 driver-spin in verify-13
- Capture: FREEZE-SOP fired at 90 s exactly; bt_all.txt (401 lines) captured via the
  exec-gdb ancestor path BEFORE kill — first real-world use, worked as designed.
- Frame classes: rank-0 (thread 16) = CUDA-DRIVER-SPIN (libcuda poll frames) beneath
  gqa_kv_append_kvarn_and_commit <- kvarn_attend_text <- attn_mix_tp <-
  target_verify_batch_impl<DFlashFeatureSink> <- run_tp2_request_dflash2. rank-1 =
  ABSENT from the 34-LWP dump entirely (all 34 threads mapped and accounted); NO
  [D2-SS-RANK1-ERR], no throw, no error line = the thread RETURNED. SYNC prints
  symmetric through round 12 (15/15 argmax+bar, 16/16 chain-vocab both ranks); the
  lone step-13 input trace is rank-0's.
- Instrumentation rent, third collection: capture-hunk proved rank-1 didn't throw;
  SYNC-pair parity proves it exited CLEAN; the drafts-identical prints prove the
  divergence is INVISIBLE to content prints — it lives in the lic/accept channel.

## Verdicts on the candidate tree (all in the intercom record, seq-44/45)
- C1 D2H ORDERING: REFUTED — accept enqueued on s, lic/acc D2H on s (:3855-3858),
  streamSync (:3859) precedes every host read.
- C2 budget/room arithmetic: REFUTED (35 vs 8192; 11662+6 vs 16384).
- C4 (NAMED, standing): THE TWIN NEVER CALLS reset_one_shot_step. Batched routes do,
  per request, both counters (tp_group.cpp:288-293; rationale comment :2303 'an
  aborted request leaves them skewed; the reset zeroes BOTH'). The twin's only
  one-shot consumer is the verify allreduce_argmax; skew there makes GLOBAL target
  tokens rank-divergent, lic_h divergent, hit_stop rank-1-only -> exactly the
  observed silent clean exit. Drafts stay identical because the chain uses plain
  allgather + replicated pool — untouched by one-shot state. 248046x2 in round 12's
  drafts = stop-adjacent content as the trigger (datum, per CORD seq-45(3)).
- Fix shape (specified, NOT landed pending gate): per-rank
  backend.group().reset_one_shot_step(rank) after seed-prefill/first-chain, before
  the round loop (twin equivalent of batched :1893); A1 gate question: twin consumes
  only one_shot_argmax (grep of group calls), so zeroing both counters is parity,
  not behavior.

## Cell-B status
NOT RUN (CORD hold: no advance on my own). The d2mb cross-route hash join remains
JOIN-PENDING-NOT-LOST: instrument + script + ab3_join.py all banked; cell-B rides
the C4-fix verification boot (T-cell again + B-cell, one server, one BIN).

## Teardown release row
Killed 811413/811489 (own PIDs only) post-bt; both cards 15 MiB / 0% verified;
no foreign process touched. serve.log 5570 lines frozen at capture (complete for
T-cell purposes); bt_all.txt + .banked copy; card carries the freeze rows.
