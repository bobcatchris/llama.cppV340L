# AB2 VERDICT — verify .cu GDN write-target (the 'ring-vs-linear' hole): SHAPE-CORRECT on every statically-testable surface; hunt converges on pool-contents-at-depth

Method: pure static read (no boot), tree 9a76439e. The named hole from A1's map
('verify .cu GDN write-target semantics, ring-vs-linear at N=1 — THAT read is A2's')
came back SOUND on all four sub-questions; one claimed object doesn't exist; what
remains is what AB3 boots measure. This is the 'inconclusive-as-to-mechanism,
shape-correct' branch of CORD's seq-34 fork: AB3 runs as the DIAGNOSTIC boot.

## Sub-check 1 — WHICH .cu, WHICH semantics (the fork that made the hole ambiguous)
Two GDN mix implementations exist: `gdn_mix` (text_context_impl.h:2283+) has the
GdnStateAction::RecordForReplay branch (RECORDS inputs, no per-column state writes);
`gdn_mix_tp` (:2050-2282) is UNCONDITIONALLY snapshot-semantics (column j state lands
at base+j; initial read from initial[b]) — no record branch at all. The TP2 twin
verifies through the TP variant, so the speculative schedule's
`set_gdn_state_action(RecordForReplay)` (speculative_target_impl.h:15) is INERT for
TP2 routes: state selection genuinely happens at commit, exactly as the twin assumes.
The record-vs-snapshot ambiguity A1's naming worried about does not bite this route.

## Sub-check 2 — slot arithmetic twin-vs-batched (consistent both, deliberately offset)
Twin (N=1): slt2=[0, ring0] with ring0=st.lanes=2 (:3565-3566, :3738-3739); commit
copy_slot(ring0+a -> 0) at :3864 — column j=0..valid-1 wrote ring0+j; valid=vc=
min(T,ext+1); a<=ext so ring0+a is ALWAYS a written column's slot (:2197-2200 + the
valid-gated snapshot kernels in ops/linear_attention/gated_delta_net/recurrent.cu
Batched/Masked variants). Batched (N=2): initial[b]=b, base=ring0+b*T (ring0=4 there
per the capacity note at :4760: slot_count=2*lanes+(2k+1)=15 covers lane rings 4..9 /
10..15; lane COMMIT slots 0,1; 2,3 unused slack). Different bases across routes are
internally consistent per-route; no overlap, no capacity violation (twin ring 2..7 <15).
CONV side mirrors identically (causal_conv1d_silu_snapshot_split3, same initial/base
pair). => write-target, select-at-commit, gating, capacity: all four clean.

## Sub-check 3 — the DRAFTER has no GDN state to corrupt
dflash2_round.cu contains ZERO recurrent/linear-state machinery (grep: only lattice
slots and the kRowUsed candidate table). The chain is block-local: it consumes fused
target taps + the KV pool window. So a 'GDN state goes wrong in the chain' variant of
the hole does not exist — any state corruption can only enter the chain via its two
INPUT surfaces: the fusion feed (hidden-feature taps) and the pool window contents.

## Convergence (what AB2 leaves for the boot)
Chains are byte-identical cross-route at rounds 1-2 (closed f89f5db1-era) and
acceptance is zero from the first COMMITTED-CONTEXT round onward, while the verify
state machinery is shape-correct => the prime suspect is now structurally narrowed
to what the per-round republish puts in the POOL (coverage hook: d2h_fused feed ->
context_append -> kvarn_rewind_lane ordering at N=1) — A1's QUEUED pool-contents-
at-depth read — with the fusion FEED CONTENTS (tap capture window at N=1: which
hidden states does dflash2_batch_feature_sink stage when the attend is non-batched?)
as its sub-branch. Both are exactly what AB3's cross-route [DBG-GDN] at MATCHED
frontier + the first-round-pair (step 2->3) reading discriminate: state-correct-
but-window-wrong shows up as GDN EQUAL with chain drift at round 3, while a bad
feed shows up in the hidden-tap hashes before any state question.

## Gate-match precondition for AB3 (the self-imposed veto, checked)
Twin dumps: :3952 (step<=4, tags d2ss-r1-committed / d2ss-r1-ring, consumed=cur_F).
Batched dumps: :5877 gate = NINFER_MB_RINGDBG && rank==0 && STEP>=70 (tags
mbg-r<step>-l<lane>-{cur,base}, consumed=F_h[j]). VETO FIRES ON READING: different
env, rank-0-only, and a step floor of 70 chosen for a corruption hunt, not for
frontier matching at step<=4. So AB3 CANNOT run boot-only: it needs the small
instrument delta — under NINFER_D2_GDNHASH, fire the batched dumps at step<=4,
BOTH ranks, consumed-matched tags (one shared printf shape across routes so the
comparison is grep-able by (consumed, slot-role)). That is code: implement ->
build (BIN moves) -> fire-check re-green -> A1 one-word (instrument-only, decode
path untouched, same argument as increment-4) -> CORD stamp -> AB3 as the
FIX-verification or diagnostic boot per seq-34's fork.

## Boot-4 (AB3) card pre-registration (carries forward)
P5: state-equal + chain-drift-at-3 => pool-window contents (hook feed) named;
P6: tap-hash divergence at step 2 => fusion FEED window named (attend-naming branch);
P7: everything equal through step 4 + acc still 0 => accept-kernel-inputs branch
(the vc/ext mirror re-enters with clean data) -> A1 wakes for the queued read WITH
his two discriminators pre-met. Any new shape: 6th-surface report, no freeform boot 5.
