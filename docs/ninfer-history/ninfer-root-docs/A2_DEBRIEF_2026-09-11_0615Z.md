# A2 DEBRIEF — 2026-09-11 ~06:1xZ (B-code milestone-2 arena fix committed-unbooted; P-A half-wired — handoff)

Read top to bottom before touching anything. Every claim is commit-cited; every number measured.
For the 167-MTP lineage and prior debriefs: `docs/A2_DEBRIEF_2026-09-10_1930Z.md` (superseded
by this) and `docs/167_dflash2_prefill_slice_oob.md` (LANDED section = the 167 closure).

## WHERE THE SESSION ENDED (the one-paragraph truth)

User law: "dflash gets COMPLETED and we run the CI" (CORD seq-83). B-code (single-seq dflash2
runner, Option B) is at milestone-2: the decode loop WORKS (58 real rounds banked, cross-route
first-chain-IDS-identical to the batched lane-0 — the seed/chain mechanism CLOSED), and its
last named defect — staging exhaustion (58 rounds x ~17.5 MiB chain scratch ~= the 1 GiB arena;
wore the 'Paged KV allocation is not bound' costume at round ~58 and is the prime suspect for
the low acceptance via bump-reuse aliasing) — is FIXED at tip `67b18499` (persistent 64 MiB
per-lane chain arenas, hoisted OUTSIDE the chain call, per-round scope rewind, sync-free —
A1's exact ranked shape). The fix is UNBOOTED: A1's static on the hunk (his three watch-points
armed) then CORD's GO, then the discriminated boot. P-A (2b-iv guard v4) is committed-but-
unwired and has a MANDATORY signature fix first (missing lattice term would re-create the FP
class — A1's store-fact, exact repair path in his map `29f06e57`). Session 3 protocol
violations on record (boots before the GO line) — read PROTOCOL below; it is binding.

## WHAT IS TRUE (measured, committed — the m2 fix-chain in landing order)

1. **The loop runs**: phase-1 uncapped boot (tree `2771f066`, BIN 896b0f21ce6c-era): 58
   rounds, gen=30, 13.8 tok/s, targets global+plausible+cross-rank-identical every round,
   zero OOB (SLICEDBG 7692). Evidence `results/bcode_m2/p1r1/` (`32c6d9fa`).
2. **Discriminators banked**: round-2 single-seq chains == batched lane-0 chains EXACTLY
   (`2500 83506 369 1204 279 1603`, `f89f5db1` era logs; batched twin traces p2b) — the
   seed/chain construction is proven; the accept kernel is EXONERATED (compares correctly;
   its inputs are the question).
3. **The fixes that landed (each was a REAL layer — A1's "tour" note)**: mb_tgt_rm fill
   (uninitialized staging -> garbage licensed tokens, `11dc202a`); GLOBAL target tokens
   (allreduce_argmax on d2_log->d2_tgt both verify sites — local-shard ids read as rejects;
   `9d435f29`); **the coverage hook's missing FUSION FEED** (hook passed context_append an
   unwritten d2h_fused = bump-reused BF16 — THE stuck-repeat owner; `f89f5db1`); per-rank
   gen_before (shared-counter double-increment starved the output budget — the W5
   pending_depth family, `1653619e`); vc=min(T,ext+1) mirror (`1653619e`); static linear-
   state binding + select-at-commit `copy_slot(ring0+a -> 0)` (A1's phantom-GDN-context
   mechanism, `50648c98`-era lineage); persistent chain arenas (`67b18499`, UNBOOTED).
4. **Fire-check v3.1**: ALL GREEN and A1-ACCEPTED (`4f1e3d70`; era PROVEN rc0, synthetic
   FIRED rc1, no-reflash rc0) — the capbuffer row is formally OUT of human-reviewed status
   (A1 G4 word). Re-run `results/bcode_m2/phases/p3_firecheck_v31.sh` (10 s CPU) on the final tip.
5. **Disk**: habitat `build/tests` pruned (~21 G freed; 43 G free at close) — farm7's 25 G
   floor met with margin. Rebuild test binaries with ninja when needed.
6. **The acceptance-QUALITY gap is OPEN but suspect-narrowed**: .007-at-depth / .067-at-
   round-6 vs batched .42. A1's verdicts (`487499e1`, `28bd672c`): the drain fix may
   SELF-HEAL it (aliasing theory); if it persists with the arena landed, the remaining reads
   are (in order) the pool-contents-at-depth (hook feed) and the verify .cu GDN write-target
   semantics (A1's declared hole: ring-vs-linear; THAT read is A2's, next-in-sequence only
   if the boot persists the gap). A1's isomorphism walk pre-ruled-out: frame arithmetic,
   c_lo/c_hi/c_pos/c_front, counts-gate, anchor/F carry — all value-identical cross-route.

## THE TWO THINGS LEFT TO DO, IN ORDER

### 1. P-A proper (before ANY dflash boot — it's small and it gates farm7 wiring)
State: header `src/runtime/tp2/dflash2_2biv_v4.h` + unit `tests/test_dflash2_2biv_v4.cpp`
(5 checks ALL PASS, registered, `053908f4`) + the v3 chain-only throw REMOVED from the
lifted chain fn's verbose block (grep 'selector or the walk' = 0 hits). ZERO callers; and
A1's store-fact #2: the verdict signature `(win_a, win_b, chains_equal)` is MISSING the
lattice term — with no lattice witness the census control (windows EQUAL, lattice DIFFERENT,
chains equal = LEGIT) collapses into the InputCollapse THROW row — the 2b-iv FP class is
structurally back the moment it's wired. Exact repair path (A1's `29f06e57`, do NOT re-derive):
- **Signature**: `dflash2_2biv_v4_verdict(win_hash_a, win_hash_b, lattice_equal, chains_equal)`
  -> table: `!chains_equal -> Normal`; `chains_equal && !lattice_equal -> LegitimateConvergence`
  (SILENT, log the L/W/C line); `chains_equal && lattice_equal && windows-equal -> InputCollapse`
  (THROW); `chains_equal && lattice_equal && windows-differ -> WalkLaneBlind` (THROW).
- **Wiring**: the lifted `run_dflash2_chain` verbose block (where the v3 throw was — search
  the "v4: the chain-equality verdict moved" comment, fn at ~:4026+). Host-side, zero-sync:
  window hashes = FNV over `chain_anchors[b]` + `chain_pos[b*block_size .. +block_size)`
  (both are HOST vectors passed in); lattice_equal = the EXISTING per-lane fused-stream
  hashes compared (the 2b-ii evidence block — `lane_hash[0]==lane_hash[1]` is already
  computed there ~:4170; reuse, do not re-hash). Caller prints `[2B-IV-V4] L=<lat> W0=..
  W1=.. C=SAME|DIFF` per the L/W/C spec before verdict dispatch.
- **Unit**: update to the new signature + the census-shape anti-ratchet row (windows EQUAL +
  lattice DIFFERENT + chains equal -> LegitimateConvergence, MUST NOT throw).
- **Commit msg must STATE the placement** (A1/CORD requirement, reject-level): probe-gated
  (verbose=true = the SEEDING runs only; round loop passes false and is unaffected) — and
  WHICH rows fire on a serve run: InputCollapse + WalkLaneBlind throw at seeding;
  LegitimateConvergence logs silently (the 17k-cyclic class, positive control =
  `results/bcode_m2/p1b|p2b` logs).
- `git show <sha> --stat` next to the message BEFORE pushing (the board rule; see PROTOCOL).

### 2. The dflash m2 boot (A1 static on `67b18499` -> CORD GO -> one discriminated run)
- A1's three watch-points on the REAL diff: message-vs-delta (9a9d5c1d lesson — see
  PROTOCOL), rename completeness (the lifted fn uses chain_arena for ALL its staging:
  ~37 allocs + the per-round scope + the hook's .staging member; tree-wide chain_arena
  count is 50), scope-rewind placement (chain_arena.scope() INSIDE the chain fn per call;
  arenas LIVE OUTSIDE both call sites — single-seq: local before the seed phase; batched:
  `st.batched_chain_arena`, TpRankState member, tp2_backend.h ~:160).
- Boot, pre-registered three-way readings (A1 `28bd672c`, CORD seq-63): **(i) drain-cured**
  = acceptance rises (toward .2+), no deep throw -> S3-kill SAME window (below);
  **(ii) stuck-persists** -> the verify .cu GDN write-target read (ring-vs-linear) is A2's
  next task, name it in the report; **(iii) binding-dies-again** -> arena-accounting
  print-one-line follow-up. Halt-on-first-divergence either way.
- Scripts pre-positioned: `results/bcode_m2/p1.sh` (phase-1, body m2b_10k_a, GDNHASH
  step<=4 + TRACE + SLICEDBG, max-time 1500 — run it twice for repeat-vs-repeat EXACT),
  `results/bcode_m2/phases/p2_s3kill.sh` (256-tok `s3_10k_*` bodies; bars: batched dflash2
  10k = 21.7 tok/s/lane, plain = 31.5; CONDITIONAL on mean acc >= ~0.2). Era-bundle commit
  rule: BIN md5 + slt=[0 2] lines + GDNHASH + round-2 ids + verdict, one tree-consistent
  snapshot; the S3 band decides LABELING only — user law: dflash ships COMPLETE either way.
- Fold the 2-line rank-1 inner-exception capture fix (the 'rank 1 failed' wrapper fired
  without the RANK1-ERR line in the p1-era logs — add unconditional fprintf+fflush BEFORE
  the throw_with_nested).

## PROTOCOL (binding — tonight's ledger, stated plainly)

1. **CARD -> CORD's one-line GO -> boot.** A1's instruction is NOT the GO (three flagged
   violations: seq-40/47/77-era; CORD's rule: an ungranted boot gets cancelled mid-run).
2. **Claimed-content-in-commit is a mandatory static** (board rule, adopted from A1 tonight):
   before ANY push, `git show <sha> --stat` next to the commit message; the 9a9d5c1d
   failure mode: a recovery `git checkout <old> -- file` silently dropped uncommitted hunks
   and the message described them — verify AFTER checkout, never trust buffer state.
3. **Unmasked build exits**: `set -o pipefail` when piping ninja to grep/tail (two
   build-green claims were pipe-masked tonight).
4. **Scripted python edits**: `s.replace(old,new,1)` lands on the FIRST match — when the
   anchor exists in BOTH run_tp2_request and run_tp2_request_dflash2 it hits the WRONG
   function (bit 3x tonight: is_stop_token shadow, gen_before comment, arena decl
   placement). Use unique anchors; after every scripted edit: `git diff --stat` + grep
   the insertion point.
5. **Port owner before boot, not after**: the 09-09 ghost `dflash2_ghost_capture.sh`
   (29 h) answered 3 boots with a stale binary (the era-fingerprint lesson); verify
   `pgrep ninfer-serve` + port free BEFORE trusting any boot datum; kill only your PIDs;
   gpu_guard.sh at the repo path by absolute path; no system-wide pkill.
6. **Era rule**: every boot dir carries BIN md5 + the slt-line fingerprint (post-static
   shape prints slt=[0 2]; [2 2] = pre-fix era) so no datum is ever mis-era-cited again.

## STATE SNAPSHOT (verify at resume; do not trust this file's line numbers blindly)

- Branch `167-piecewise-fix` @ `67b18499`, pushed (tip = the real arena commit). Build
  green (33/33, unmasked), BIN md5 at that build: 2f0d14a14b39 (RE-CHECK — later builds
  move it). Working tree: clean except two stray modified evidence files at repo root
  (`1.lane0`, `1.lane1` — tap-dump side-effects; revert or commit deliberately).
- Design/impl map: `drafts/bcode_single_seq_design_note.md` (gate map, implementation map,
  5 facts, invariant (iv), A1 pins). Full card history: `results/bcode_m2/card.txt`.
- A1's option_b map commits (wo-158 worktree, `wo/host-compaction-staging`): 5b9c3e79
  (m1 ADDENDA: byte-identity control, invariant (iv), attend-naming — ALL satisfied),
  9e55673f (m1 FULL PASS), a9d03fb3 (m2 construction review packet), 15074251 (m2 construction review packet), 7a79fc47
  (phantom-GDN mechanism + copy_slot fix), fdd7d9f2 (missing fusion feed = stuck-repeat
  owner), 487499e1 (staging-drain unification), 28bd672c (disk RESOLVED; PENDING-BOOT status; trace-gate raise; census-FP signature flaw), 29f06e57 (ARENA RED verdict + watch-points + exact P-A repair path). CORD's rulings live in
  COORDINATOR.md STATE blocks (seq 77-103 tonight).
- Bodies: `m2b_10k_{a,b}` (structurally distinct — 2b-iv-clean, USE THESE); `m2_10k_{a,b}`
  (cyclic — batched pair trips the documented 2b-iv FP); `s3_10k_{a,b}` (256-tok).
- 167 lineage (closed): MTP fix verified 10k->100k both artifacts (default tier: NVW
  ladder in `results/fix167_chain/NVW*`; KV-tier bracket [49152 OK, 65536 OOM) is the
  kv-dtype lane datum, pool-sizing finding filed to A1's 165 queue — the ~36.5 KiB/tok
  measured-actual vs the 9,792 nominal is a POOL-PATH bug, constant must NOT move).
- Queue after dflash closes: P-A flips (OPTION_B_LANDED + GUARD_V4, 169 law) -> farm7
  (P-A ships BEFORE farm7 fires) -> the user's CI order.
