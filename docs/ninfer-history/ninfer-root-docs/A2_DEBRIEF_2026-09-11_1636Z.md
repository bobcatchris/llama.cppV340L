# A2 DEBRIEF — 2026-09-11 ~16:36Z (dflash2 twin: completes at parity, ~27% step-13 hang survives all four theories; lane DARK pending user convene)

**Audience: the next agent on this lane (or A1/CORD/user picking it up).** Everything here is
commit-cited or grep-verifiable; §8 is a 60-second cookbook to re-prove the headline claims
yourself. Read §0-§2 for posture, §3-§4 for the science, §5-§7 before touching anything.

## 0. SIXTY-SECOND VERSION
- **You are A2's lane.** Worktree `/home/intel/ninfer/worktrees/wo-dflash2-w5-habitat`,
  branch `167-piecewise-fix`, remote **`github`** (there is NO `origin` — pushes name
  `github 167-piecewise-fix`). Tip `c867b7c0`, pushed, tree clean except the usual
  untracked CI-dump strays (§5).
- **Intercom naming trap:** my pi-session registers as **`agent1` (id 01a08f32)** but the
  LANE is A2. The real A1 (static-gate keeper) is **`01a08ce2`**. When relaying "from/to
  agent1", check the ID.
- **State:** dflash2 single-seq ("twin", Option B) **completes requests at batched-parity
  acceptance** for the first time EVER (8/11 clean twin boots tonight, .369 acc, 37.4
  tok/s decode) but a **~27-50% hang at decode step ~13** survives. Four causes were found
  and fixed/retired; the hang persists; class **(B) "accept kernel's per-device output is
  nondeterministic" is the sole surviving theory** — pre-registered destination is a
  **USER CONVENE** (package owned by coordinator, my table is its raw material).
- **Posture: DARK. No boots, no GPU (cards belong to agent3's q3 window per user order),
  no src changes** until coordinator re-wakes this lane with exactly one bounded ask.
- **Gates you must respect (binding, AGENTS.md + lived tonight):** written GPU grant from
  **coordinator** only; boot-on-GREEN-of-A1-static only; VRAM LAW (no estimated-VRAM
  refusal ever; anti-resurrection check `git diff main -- src/runtime/tp2/tp_engine.cpp
  src/runtime/tp2/tp2_budget.h` content-wise clean before merges); never system-wide
  pkill; gpu_guard at `/home/intel/ninfer/repo/tools/smoke/diag/gpu_guard.sh` (library
  form — silent rc0 = clear; the real check is `pgrep -x ninfer-serve` +
  `nvidia-smi --query-compute-apps` empty).

## 1. WHAT THE TWIN IS + WHERE IT STOOD BEFORE TONIGHT
`run_tp2_request_dflash2` (tp2_backend.cpp ~:3354+) = single-sequence dflash2 speculative
decode on the tp2 two-GPU box: seed prefill -> chain proposal (block-local lattice drafter,
`dflash2_round.cu`, NO recurrent state) -> per-round: target verify (T=k+1=6 cols) ->
one-shot `allreduce_argmax` -> accept kernel -> commit (copy_slot ring0+a→0) -> pool
coverage hook -> re-propose. Launched by the engine ONLY for batch-of-one (`tp_engine.cpp`
deliver lambda ~:348). Its big sibling (batched dflash2, 2 lanes) works at .42 acceptance
and is the reference. Before tonight the twin had NEVER returned a client response: every
era ended in an error or a stall.

## 2. THE FIVE-LAYER CAUSAL CHAIN (peeled tonight, each layer commit-cited)
1. **ENGINE FALL-THROUGH** (`9a76439e`): d9a4689c replaced the §22.6 batch-of-one REFUSAL
   THROW with the twin route but never added `return;` → every request ran the twin to
   natural stop, then re-ran the SAME request through the forbidden MTP-shaped batched
   runner, whose start block died on twin state (`Paged KV allocation is not bound`) and
   replaced the client's 200 with an error JSON. Explained m3_boot1's spin, m3_boot2's
   throw, p1r1's "round-58 drain", the warmup bleed — **"one routing bug, four costumes"**.
   VERIFIED-FIXED: m3_boot3 P-scorecard 4/4 (docs/A2_GREENSUMMARY_m3_boot3.md).
2. **STATS DOUBLE-COUNT** (`dc92bddf`): `acc_rounds/acc_accepted` incremented on BOTH rank
   threads → all historical "rounds" were 2x. p1r1's famous 58 rounds = ~29 real steps;
   the 1 GiB arena-drain arithmetic was built on that phantom. Rates (num+den both x2)
   were valid; DEPTHS were not. Now rank-0-only; proven in vivo (rounds==steps==31).
3. **MISSING FUSION FEED, SECOND LANDING** (`4045bfd4`): twin's coverage hook fed
   `context_append` an UNWRITTEN `d2h_fused` — f89f5db1's original fix had been silently
   dropped by the arena refactor (the message-vs-delta disease in code form). After this:
   FIRST NONZERO acceptance ever, at **.369 ≈ batched-parity band** (docs/A2_AB3_VERDICT,
   battery rows).
4. **ONE-SHOT RESET PARITY** (`fcd045ce`): twin never called `reset_one_shot_step` (every
   other route does; rationale comment tp2_backend.cpp:2303). KEPT as hygiene — but
   boot-7/8 PROVED IT WASN'T THE HANG'S CAUSE either.
5. **THE HANG, post-all-fixes**: see §3.

## 3. THE HANG — EXACT STATE FOR WHOEVER PICKS IT UP
- **Signature (ZERO VARIANCE across ~10 reproductions, all eras post-fuse):** both ranks
  SYNC-print symmetric through round ~12 (drafts byte-identical incl. the suspicious
  `248046 248046 198 8839 220` round-12 set), rank-1 makes a **silent CLEAN EXIT** at
  the round-12/13 seam (absent from the 34-LWP gdb dump — thread RETURNED, not threw:
  the `[D2-SS-RANK1-ERR][immediate]` print never fires), rank-0 then blocks forever in
  verify-13: user thread `cuMemcpyDtoHAsync` under `gqa_kv_append_kvarn_and_commit`
  (stream-ordered behind work whose participant is gone), GPU0 100% / GPU1 0%.
- **Rates (the complete paired table, user-ordered untruncated):** armed(poison-ON) 4H/8
  (50%) vs OFF-control 1H/3 (33%) vs historical 3/11 (27%); Fisher p~.6 — **indistinguishable;
  small-n, treat as order-of-magnitude only.** "Poison may RAISE rate" = hypothesis-grade
  ONLY (p~.42), do not promote it.
- **What's DEAD as causes (each with its instrument's evidence):** engine fall-through
  (fixed; hang persists); arena/drain (phantom denominator, §2.2); one-shot **family**
  argmax+allreduce (EXONERATED AS A CLASS: 6 hangs x ~1000 collective observations,
  pairing CLEAN every time, 0 observed>epoch on argmax K-lines, 0 REJECT-candidates on
  KAR lines — the gen-stamp v2 probe never once saw a flag pass without a commit stamp);
  uninit-envelope class (A) — **RETIRED WITH EVIDENCE by boot-8**: the 0x5A poison was
  provably armed (arm-proof=4) on every hung flip and did not suppress the hang.
- **What SURVIVES:** **(B) device-nondeterminism (or per-device input skew you haven't
  found) inside `ops::speculative_accept_greedy_drafts`** — the last component computing
  rank-critical values INDEPENDENTLY on two GPUs. The exhaustive input enumeration
  (docs/A2_III_STATIC_VERDICT.md table) says every INPUT is replicated or
  complementary-consistent; the OUTPUT is not compared across ranks anywhere.
- **The mechanical next-look (first candidate if woken):** D2SS-TAIL per-rank print of
  `(step, a, lic[0..a], gen_before)` at the existing post-accept streamSync (:3859) —
  DIFFERENT `a` at same step = (B) named without interpretation; IDENTICAL `a` everywhere
  + hang = the whole exit-via-done theory is wrong and the hunt re-opens at thread
  lifecycle (unlikely but say it). Second candidate: read the accept kernel's greedy
  branch for order-dependent fp/reduction paths or per-device scratch reuse.
- **Do NOT re-run poison sweeps or re-exonerate closed doors** — §3's dead-list is the
  budget; the user's convene decision picks the next bounded ask.

## 4. BOOT/RIG TOOLBOX (what exists, what each is for)
- `results/bcode_m2/p1bt.sh <dir>` — single twin-cell boot (body `m2b_10k_a`, override
  `P1_BODY=`), card self-prints BIN/tree/guard/df + era rows, **FREEZE-SOP built in:
  90 s log-static -> parent-shell `exec gdb` full bt (yama=1 FORBIDS sibling attach —
  exec-from-ancestor is the only legal tracer, PROVEN; wrapper pid is gone by design,
  kill via kill_list.txt PIDs ONLY)**, four pre-registered-reading lines.
- `results/bcode_m2/p1ab3.sh <dir>` — dual-cell (twin + batched-pair same BIN) cross-route
  audits; `SKIP_CELLT=1` cell-B-only; `SKIP_CELLB=1` T-only; env must be exported at
  invocation to reach the server (`NINFER_MB_ARGMAX_TRACE=1 bash ...` — the b9/b9b
  lesson: an UNARMED run's zero is NOT a datum).
- `results/bcode_m2/battery.sh <tag> <N>` — N-run sweep over p1bt.sh; forces A1TRACE;
  `NINFER_D2_POISON=1` for the (A) probe; **`BATTERY_NO_HALT=1` for RATE probes** (hang
  = datum, not halt; CORD's own correction); per-run verdict PASS/HANG + staleness
  counters; `results/bcode_m2/b9_poison_README.md` = the armed-sweep runbook incl.
  **proof-of-arm invariant: grep -c D2SS-POISON per run must be >=2, even, nonzero
  (==4 is correct: warmup+request x 2 ranks)** — a 0-arm run is unarmed, re-run it.
- `results/bcode_m2/pairing_report.py <serve.log>` — THE authoritative pairing tool.
  **battery.sh's inline `pairing=` row is a known-broken artifact** (compares rank field
  into the tuple → always "DIVERGE at call#1"; disclosed; do not cite it).
- Bodies: `m2b_10k_{a,b}` (2b-iv-clean, NO max_tokens field — server default 8192,
  natural stop ~gen30; the old "max_tokens=128" card text was fiction, cards now
  derive everything from the actual files); `s3_10k_{a,b}` carry 256; `m2_*` are the
  CYCLIC pair (documented 2b-iv FP, trips LegitimateConvergence-SILENT since GUARD v4).
- Staged bins: `results/staged_bins/{a804d598e817,509300c7a654}_serve` (236MB > GitHub
  limit — DISK-ONLY persistence + md5-in-commit; re-copy to a saved path BEFORE any
  future prune of build/). Era BIN chain tonight (cite ONLY these):
  2f0d14a14b39 stalled -> a4c9ac9c992e capture-era -> 68f000ab0b56 inc4 ->
  e74560a03a65 P-A/boot1boot2-era -> cf0bcd8b9db7 counter-era -> 387150c10eb0
  fall-through-fix era (m3_boot3) -> **e143847ca672 fuse-fix era (m6_boot1, ab3b,
  bb_fixbin battery)** -> 4009899313a1 argmax-K-era (staged, unbooted) ->
  d94965428744 KAR-v1->v2 transition (b8_hunt, b9b) -> a804d598e817 boot-7 BIN
  (b7_karv2, b9_off) -> **509300c7a654 boot-8 poison BIN (b9_poison, b9c_cont) =
  CURRENT live build == saved**. NEVER cite `9cffb9fc2662` (never built-live).

## 5. STATE LEDGER (verify, don't trust)
- `git status`: clean except untracked `results/2026*_CI_*/` dumps (prior CI strays,
  deliberate era-bundle fodder — don't scatter-delete; ask coord) + `results/serve.log`
  + a few misc. Two `git stash` entries predate this session (tmp; HOOKDUMP) — untouched.
- Disk: was 21G free at boot-8; **now 30G** (other lanes/prunes moved it — always
  `df -h /` fresh; farm7 floor is 25G; build/tests already pruned with CORD approval,
  measured cold-relink cost ~4 min for tests, note: `ctest` in PATH is a BROKEN python
  shim, use `/usr/bin/ctest` or run test binaries directly).
- GPU: belongs to **agent3** (user-ordered q3 window, rule since the A3 race incident:
  claim cards ONLY on A2's RELEASE ROW or coord re-confirm — my release row landed at
  ~c867b7c0 push time and was coord-verified clean).
- Queue (NOT now, in CORD-stated order): user convene decision (mine/CORD's package) ->
  if next cycle authorized: bounded ask likely = D2SS-TAIL §3; -> S3-kill perf phase was
  NEVER RUN (its acc-gate ≥~0.2 is now MET by .369 — script `phases/p2_s3kill.sh` exists,
  bars: batched-dflash2 21.7 tok/s/lane, plain 31.5; twin decode_tps ALREADY showed 37.4
  un-officially — a formal pass is post-hang-decision); -> P-A flips (OPTION_B_LANDED +
  GUARD_V4, 169-law) + farm7 package assembly (keep-list must carry: staged_bins
  copy-first, nvfp4 18G in incoming/ untouchable, "four costumes" exhibit, arena
  no-revert-with-teeth row, sibling one_shot_allreduce review-listing).

## 6. PROTOCOL INBOX (learned the hard way; violations cost hours)
1. CARD -> A1 static (on the REAL diff) -> **coordinator's written GO** -> boot. A1's GO
   word is NOT the stamp; "clear to claim" is NOT a grant.
2. Rule-8 EVERY commit: `git show --stat HEAD` next to the message BEFORE claiming;
   **run every grep/command you cite BEFORE typing its output** (three hallucinated-
   verification near-misses tonight; the reflex that works: post-push `git show` audit).
3. Unmasked builds: `set -o pipefail` with ninja-piped-to-tail (twice bit pre-pipefail).
4. Anchored edits: scripted `replace` can SILENTLY no-op (anchor drift) — assert counts,
   check `git diff --stat` non-empty; double-racing edit calls at one site DO apply twice
   (seen) — re-grep after multi-edits.
5. Era rule: every boot dir self-carries BIN-full-md5 + guard + df + tree; fingerprint
   rows on the card, not in chat. Arms rule: prove the env reached the server (KAR/POISON
   greps) before reading any zero as evidence.
6. Kill only your PIDs (kill_list.txt pattern). Diagnostics leave-live-then-reap by PID
   (the exec-gdb SOP deliberately strands server+curl for exactly that).
7. Don't self-assign doc numbers (COORDINATOR.md seq/16x/17x numbering = coord only).
8. If you hit a 6th distinct surface on anything: STOP, report, don't freeform boot N+1.

## 7. WHERE THE TRUTH LIVES (docs map, newest-first per topic)
- Hang science: `docs/A2_III_STATIC_VERDICT.md` (input table + accepted residuals) ->
  `docs/A2_AB3B_P10_VERDICT.md` (determinism-refutation + door-iv closure + WITHDRAWAL
  section on the S1 over-claim) -> `docs/A2_QUALITY_AB_PLAN_2026-09-11.md` (AB1
  no-regression math — the '.067 era' was ~zero all along, vpos arithmetic).
- Fixes: commit messages are self-contained (`9a76439e` return; `dc92bddf` counters
  + falsifiable-edges; `4045bfd4` fuse-feed shape-audit/provenance; `fcd045ce` reset;
  `0662a874` S1-hardening-LABELLED-NOT-THE-FIX; `f2da5e8a` KAR-v2; `1abfc7ca`+`8e9ee344`
  +`c867b7c0` poison rig + rate table + release).
- Boot evidence: `results/bcode_m2/{m3_boot1,m3_boot2,m3_boot3,m6_boot1,ab3_cellrun,
  ab3b_cellrun,battery_bb_fixbin,battery_b7_karv2,battery_b8_hunt,battery_b9_poison,
  battery_b9c_cont,battery_b9_off,b9_cellb,b9b_cellb_armed}/` each with card.txt era rows;
  summaries: `battery_*/summary.txt`. CORD's user-facing index:
  `/home/intel/ninfer/drafts/170_user_datapack_twin_wall_2026-09-11.md` (his pen, mine
  cited).
- Prior-era context: AGENTS.md (VRAM law etc.), COORDINATOR.md STATE blocks (coord),
  `docs/A2_SESSION_NOTE_2026-09-11_edt_night.md` (running handoff — superseded in parts
  by THIS doc; where they disagree, THIS doc + the commits win).

## 8. 60-SECOND VERIFICATION COOKBOOK (trust-but-verify, all read-only)
```bash
cd /home/intel/ninfer/worktrees/wo-dflash2-w5-habitat
git log --oneline -1                      # expect c867b7c0 (or later A2 doc commits)
md5sum build/apps/ninfer-serve results/staged_bins/509300c7a654_serve  # match = era intact
grep -m1 'req 1. done' results/bcode_m2/m6_boot1/serve.log   # first complete twin request ever
grep -E 'D2-SS-STATS' results/bcode_m2/m6_boot1/serve.log | tail -1  # rounds=13 accepted=24 acc=.369
for s in battery_b9_poison battery_b9c_cont battery_b9_off; do grep -c 'verdict=HANG' $s/summary.txt; done
# armed-hangs total = (b9_poison 1)+(b9c_cont 3); OFF = 1. Rate table §3.
python3 results/bcode_m2/pairing_report.py results/bcode_m2/battery_b9c_cont/run1/serve.log
# expect: pairing CLEAN, observed>epoch 0, REJECT-candidates 0 even in a HANG run -> (iv) closed
pgrep -x ninfer-serve; nvidia-smi --query-compute-apps=pid,used_memory --format=csv,noheader
# both empty = lane-safe; if not, DO NOT claim cards — coord first.
```
If any line above disagrees with this doc, the FILES win — fix the doc, tell coord, and
only then the narrative.

## 9. PEOPLE/STATE AT SEAL TIME (16:36Z ~ 12:36 EDT, late night local)
User: asleep-last-seen, ordered "take care of business", option-A authorized the boot
cycle, gave A3 the post-release window. CORD: writing the convene package (mine = §3/§8
raw material). A1: gate-keeper, resting, briefings self-serve via §7. A3: live GPU window.
This lane: dark; wake only for the bounded ask, and re-run §8 before believing §0.
