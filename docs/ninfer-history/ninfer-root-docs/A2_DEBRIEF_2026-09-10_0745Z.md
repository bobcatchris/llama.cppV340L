# A2 DEBRIEF — 2026-09-10 ~07:4xZ (replaced mid-FIX-D verification; handoff)

Read this top to bottom before touching anything. Every claim is commit-cited.

## WHERE THE NIGHT ENDED (the one-paragraph truth)

The DFlash2 GHOST IS DEAD and the fix is PUSHED. On habitat tip the drafter runs at
acceptance 0.29/0.35 per lane, tok/round 2.53/2.82 (was 0.002-0.003 for ~12 hours),
decode 6.3s → 2.88s per 48-token gen, bitwise-reproducible. Verdict commit 6af9633e.
Root cause was TWO-layer: (i) a §22.57-class null-stream race at the selector-walk
D2H (fixed c8cd0af2, proof eae596e2); (ii) M2 — the drafter had NO target-feature
conditioning: pools held only self-generated K/V (FIX-C a6a564f2 slot0=anchor-emb;
FIX-D = A1's context-append fn 7dd0c85f/c55db6b8 + MY wiring c049fc9f: accept-hook
re-fuses pending once, counts=acc+1 per live lane, positions host-formula
F_r=cur_F_postcommit−(acc+1), blocker-H own-KV persist REMOVED). Two handoff-seam
defects in that wiring were caught and fixed in-cell (f373090a rmsnorm view
orientation; 0adb13b6 per-lane-vs-total width) — documented for your review pass.

## WHAT IS BROKEN RIGHT NOW (the reason you exist)

The VRAM PREFLIGHT on habitat was stale — A1's audit found FOUR silent reverts of
main's budget fixes (conc>=2 reserve gate, ledger relief, nvfp4 exemption term,
NINFER_PREFLIGHT_DISABLE hatch). bf16@80k was refusing at required 17,347 MiB —
phantom, not capacity. Per user order I restored main's FINAL preflight math
VERBATIM (commit 68282df3 pushed): tp2_budget.h == main byte-for-byte (incl. nvfp4
tier + types.h enum/parse/name plumbing lifted as its dependency); tp_engine's gate
region == main (measured-floor basis = claimed − 1200 MiB relief per
docs/VRAM_LEDGER.md, dual-figure print, DISABLE hatch). Binary 0d8345fc82fa, build
rc=0, tree clean at 68282df3.

## LIVE DATA FROM THE RESTORED-FORMULA BINARY (exact, not paraphrase)

bf16@80k, conc=2, k=3, kv-cap 80256, prefix 16384:
  [preflight] required 17347 MiB / usable 16310 (fixed 14363, context 2984,
              prefix cap 16384, ws 1024, slack −1037)
  [preflight] measured-floor gate basis 16147 MiB (legacy claimed 17347 − 1200 relief)
  → basis 16,147 < 16,310 usable ⇒ gate PASSES under main's math,
  and the server then DIED AT REAL LOAD with:
  `open /home/intel/ninfer/models/qwen3_8_27b.ninfer: No such file or directory`
  That path is WRONG — it never existed (my cell script's bug; earlier healthy cells
  used decode_guard.sh which resolves its own model path). THE VERY NEXT STEP:
  fix the MODEL path in tools/smoke/diag/a2_phase1_bf16_80k.sh (find the real
  .ninfer path the working cells use — check decode_guard.sh's MODEL/SERVE_BIN
  resolution), rerun, and get the ONE datum everyone is waiting for: actual
  nvidia-smi peak vs the 16,147 basis at 80k bf16. Sample every 2s (script does).
  If peak ≤ ~16.3G: the formula is vindicated AND 80k bf16 works — user gets the
  number he asked for twice. If peak > usable: the ledger needs a bf16@80k row and
  the honest ceiling is whatever it is.

## USER'S STANDING DIRECTIVES (do not re-litigate)
- "THE FORMULA NEVER CHANGES" — main's budget math is final; merges must never
  overwrite it (it got silently reverted 4x in merges — that's why habitat refused).
- No override-as-fix: overrides are for MEASUREMENT only (ledger precedent:
  int8@200k), never to ship around the guard.
- No CI re-run until the failing SECTION passes standalone; two failures same
  problem = no third attempt.
- Frozen-script discipline: never edit files a running farm touches.
- English only in user-facing work.

## OPEN THREADS (in order)
1. bf16@80k measured peak (see above — model path fix, rerun cell, 10 min).
2. 120k bf16 ladder rung + D1 nvfp4 ladder (nvfp4 exemption now in place via the
   transplant; nvfp4 tier plumbing in types.h included in 68282df3).
3. D2 W5-chain-share observational cell (10k/40k) — dflash now HEALTHY so this is
   finally measurable; W5-for-DFlash2 flip memo can be written with real numbers
   (doc number from coordinator only).
4. farm7 — but ONLY after per-cell isolation proofs per user's law, and on the
   post-merge habitat' tree per coordinator seq-111 (stale base never merges;
   A1 audits remaining ~80 files R/X/U).
5. HOOKDUMP patch LOST (self-review correction, verified: stash@{0} is an unrelated
   commit_data leftover, and NO commit carries the patch — `git log -S
   NINFER_DFLASH2_HOOKDUMP --all` is empty). It was a 36-line env-gated hook-scope
   fused dump (NINFER_DFLASH2_HOOKDUMP) for the kctx byte-twin. Rebuild from the
   accept-hook source in tp2_backend.cpp (~30 min, morning WO). Loss cause: stash /
   checkout churn during the dirty-probe at ~06:1xZ.
6. Morning hygiene WOs (already scoped, do not expand): cold-start prompt-context
   catch-up (prefill sink bound nowhere — prefill-path surgery); kctx VALUE-byte
   twin (needs HOOKDUMP); single shared predicate for pg-negative selftest;
   :225 pkill → PID-scoped (A1's drafts/ patch, post-farm); run_ci
   ambiguous-branch empty-log explainer is IN (89f54fed).

## COMMIT MAP (habitat wo/dflash2-w5-habitat, all pushed unless said)
68282df3 budget restore (VERBATIM main preflight math) ← YOU START HERE
3fd0c429 dtype-aware decode-guard ladder ([ceiling] derivation line; coverage gate
         untouched; bf16 derives to [10000] ON THE STALE BUDGET — recompute on
         fixed budget, the [10000] number is void, the MECHANISM is proven)
8e4d1df8 pg-negative verdict-contract fix + 4-scenario selftest (fossil #10)
89f54fed run_ci empty-log explainer
9f7b4154 commit_data.sh awk fix (success-only-death: grep -v exits 1 on CLEAN trees
         under set -euo pipefail — farm4's empty-log abort; run_verify_tests same fix)
61d7cab7 untrack results/latest_report.log (self-poisoning attestation loop)
c049fc9f FIX-D wiring + f373090a/0adb13b6 halt-fixes + 6af9633e VERDICT
a6a564f2 FIX-C (A1) / 7dd0c85f+c55db6b8 FIX-D item-1 (A1) / efec3993 DoD 4x500
c8cd0af2 RACE FIX (layer 1)
eae596e2 race proof / 22935431+ stage0+capture scripts / 23ac8b3a tap-fix (A1)

## CELL RECIPES THAT WORK
- decode-guard/serve cells: tools/smoke/diag/a2_racefix_run.sh & a2_fixd_cell.sh
  (envs: FUSEDUMP/STAGEDRAW/BLK0DUMP/LOGDUMP/SELDUMP/POOLDUMP(+MAX)/TRACE,
  BATCH_WINDOW_MS=2500, kvarn_k4v2, -c 2, --max-context 8192 --kv-capacity 16384 —
  that last pair matters: auto-KV OOM'd the first launch).
- bf16@80k cell: tools/smoke/diag/a2_phase1_bf16_80k.sh (FIX MODEL PATH FIRST).
- stage-0/block0 replay: tools/diag/stage0_fuse_reference.py, block0_replay.py
  (layouts named in-file; conv-base needs the BINDER PERMUTATION; rope is the
  DflashText1D table, pairs (j,j+64)).

## KNOWN TRAPS (each cost real time tonight)
- Working-tree-relative model paths in diag scripts — use absolute, verify with ls.
  CONCRETE INSTANCE (the reason the first proof cell never loaded): my bf16@80k
  cell pointed at /home/intel/ninfer/models/… (nonexistent); the real artifact is
  /home/intel/models/qwen3_8_27b.ninfer (18.2 GB). Fixed in
  tools/smoke/diag/a2_phase1_bf16_80k.sh (uncommitted at handoff — commit it).
- `git diff main HEAD` (commit-to-commit) does NOT show your uncommitted working
  tree — after transplant, diff `git diff main -- <file>` (worktree).
- python heredocs writing bash with brackets: my first HOOKDUMP edit escaped
  mangled and my revert-then-reapply lost a patch once (probe did git checkout --).
  Always re-grep the file after programmatic edits.
- setsid+background inside the bash tool got aborted mid-command twice; use
  `(setsid cmd &)` subshell form or foreground with explicit timeouts.
- Test binaries ~200MB each because static CUDA; strip saves only ~6MB. Disk 12-13G
  is workable for --no-build farms ONLY (run_ci has --no-build; --full alone
  rebuilds and will not fit).

## CARDS/ENV AT HANDOFF
Cards 15/15, 0%, no compute apps, no serve. Disk 12G. Watcher log
/tmp/a2_farm_diskwatch.log. Farm logs /tmp/a2_farm_run{1..6}.log. Evidence dirs all
committed (fc blobs omitted by design, sha in FC_OMITTED_SHA.txt per dir).
Coordinator has the merge ask pending farm7-on-habitat'; user wants bf16@80k/120k
numbers. Good luck — the hard part is done and it's committed.
