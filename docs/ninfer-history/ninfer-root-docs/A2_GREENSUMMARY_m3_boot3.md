# m3_boot3 — GREEN CLOSE: P-scorecard 4/4, fall-through wall retired, decode route COMPLETE

Era bundle (wrapper self-printed + coord-verified): tree `9a76439e`, BIN md5
**387150c10eb022f2ed8a40ffdf9bf001**, body m2b_10k_a (no cap fields; natural stop),
GO = CORD seq-28 written stamp (behind A1 seq-11/12 one-word static GREEN), fresh
guard at claim (no foreign serve, 0 GPU apps, both 15 MiB), ANTI-RESURRECTION rows
in the fix commit (tp2_budget.h 0-diff vs main; zero gate-region hunks), fire-check
v3.1 GREEN, build 30/30 unmasked. wall 20.0 s, curl rc=0, clean script-driven
teardown, no freeze (bt never armed), GPUs verified 0 apps post.

## P-scorecard (CORD seq-31 independent verification in parentheses)
- P1 warmup `not bound` bleed: **0 occurrences** (was 3 era-lines in every boot since
  milestone-1). [coord-checked]
- P2 client response: **200 + coherent twin text**, finish=stop, usage
  30 completion / 11626 prompt — FIRST EVER clean 200 on the twin route. [coord-checked]
- P3 counter fix in vivo: **rounds=31, gen=31, tok/round=1.00 EXACTLY** (warmup
  rounds=4 consistent); the 2x phantom is gone; `[req 1] done finish=stop_token` —
  a DONE line, not an error line. [coord-checked]
- P4' lane-0 fall-through canary: **0 hits** (return taken). [coord-checked]

## Retired stories (one line each — the unification ledger)
- "drain at 58 rounds" -> ~29 real steps + fall-through death after a CLEAN twin stop.
- m3_boot1 "fourth shape" spin -> fall-through with pre-capture-era collective timing.
- m3_boot2 "completion-path binding wall" -> same fall-through, symmetric-exit costume.
- "(iii) binding-throw at depth" -> never existed as a mid-generation wall.
- arena commit (67b18499): cured a phantom-denominator disease; KEEP as hygiene
  (per-lane persistent arenas are sound), disposition one-liner rides the 169/farm7
  package per CORD seq-26(6).
- warmup `not bound (continuing)`: was the fall-through eating warmup's own response;
  the 'zero_slot/+4251 costume' readings were this routing bug's shadow.

## Counter-arith caveat for the retirement notes (A2, pre-quality-read)
acc_rate = acc_accepted/(acc_rounds*k) with BOTH numerator and denominator
double-incremented -> the shared-inflation CANCELS in the RATE; the .007 (p1r1) and
.067 (27f066-era) rates were not phantom-denominator artifacts in the ratio itself —
what was phantom was DEPTH (rounds) and tok/round only. The honest statement: rates
were comparable, depths were halved-truth. So '.067 -> 0.000' remains a REAL quality
delta to explain (or a real era-compare), not a counting ghost — this sharpens
CORD's 'never measured clean' option toward the regression branch: something between
27f066 and 9a76439e's decode path lost acceptance. Named next: the .cu/semantics
A/B read plan (no boot 4 until on paper + stamped).

## Instrument status (self-corrected pre-commit — my grep tag was the error)
GDNHASH WORKS: prints carry tag `[DBG-GDN]` (not 'GDNHASH' — my first grep pattern
was wrong and nearly filed a phantom instrument-gap; corrected the same pass).
32 prints in boot3 = step<=4 gate x {committed,ring} x both ranks, matching the
card env row exactly. First-clean-era datum noticed while verifying, recorded
WITHOUT interpretation: at identical slot/consumed, r0 vs r1 committed-state
hashes DIFFER (rec=90ee.. vs 5431..) — the pool is TP-SHARDED so cross-rank
inequality may be by construction; the designed comparison is CROSS-ROUTE (twin
vs batched at the same frontier, 21:2x-era discriminator). Feeds the quality A/B
plan below, not a wall.

## Files
serve.log (8408 lines complete), respA.json (the clean 200 — era artifact), card.txt
(P1-P4' pre-registration self-printed), wall/kill_list/seeddraw, wrapper log.
