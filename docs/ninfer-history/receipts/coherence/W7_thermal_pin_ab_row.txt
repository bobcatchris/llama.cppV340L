W5 THERMAL ROW — pinned-high clocks are COUNTERPRODUCTIVE for sustained prefill at the 110W cap: auto wins 1.8x, 2026-09-18 (post-iommu=pt reboot, serving bin 0572d410, NINFER_MTP_TAIL_ASYNC=1)
A/B (2k client walls, identical probe, back-to-back requests, max_tokens=64, parity BLUE/stop/52 every leg):
  PINNED high:   burst 59.9 -> 54.3 -> 51.0 tok/s (decaying; junctions 75-86C)
  AUTO:          burst 109.7 -> 90.8 -> 74.0 tok/s (decaying slower; junctions 58-72C)
mechanism: fixed 110W cap + forced-max clocks = power-hungry corner -> firmware power-throttles harder under
  sustained GEMM load; auto DVFS finds the energy-efficient operating point and sustains ~1.3-1.8x more prefill.
  (The banked 1.3-1.75x pinning figure applied to HOT-machine rows vs an already-drooped base — the correct
  policy is workload-shaped: auto for sustained prefill; pin only for latency-critical short bursts on a cool box.)
ALSO: client walls today (18.9-40.7s at 2k) vs chunk-wall arithmetic (~1030ms/chunk = 114 tok/s) diverge — the
  client wall includes stem warm (one-time), decode tail, and thermal state. Clean post-cure auto burst 109.7 is
  today's best honest client number; sustained decays with soak (the W5 thermal wall, capped by 110W firmware).
POLICY: serving stays AUTO. The pinned-high experiment is CLOSED for sustained prefill (revisit only for
  short-burst latency work on a cool box). GEMM wall unchanged (770ms) — the mad-mix lever (see REV2 update) is
  the in-flight attack on it, measured-needing only CPU compile + same-band bench.
