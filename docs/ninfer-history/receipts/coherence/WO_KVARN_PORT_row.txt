# WO_KVARN_PORT ROW — KVarN KV tier ported to the HIP lane: kvarn_k4v4 BOOTS and SERVES on gfx900 — 2026-09-19 — desk amd/wo-kvarnport
# Host: chris-MS-7D28, 4x gfx900 (V340/MI25), ROCm 6.2. Clocks at probes: mclk lvl3 945 MHz, sclk idle 300 MHz (boot-behavior evidence; no perf claims).
# Lane: worktree /home/chris/worktrees/amd-wo-kvarnport, branch amd/wo-kvarnport (base amd/main @ b27a587ae). Bin: /home/chris/artifacts_bin/ninfer-serve_f3312f25c8201da0.bin (FINAL: adds the coordinator-audited slab-ring OOB fix — stride-window ring, structurally wrap-free; serving sanity 391 PASS post-fix). Measurement bin: 18993c160d07bf7c (identical outputs at kv_local=1 — the ring hazard cannot fire there). Red-row bins 73eecb33, 3af434bb, 63a3d37e, 18993c16 also banked.

== VERDICT: PORT GREEN. `--kv-dtype kvarn_k4v4` boots and serves on gfx900 at TP4. Quality 5/5 GREEN, byte-identical answers to the bf16-KV reference on every probe. VRAM funder MEASURED: kvarn KV 386 MiB/rank @36352 tok vs bf16 1207 MiB/rank = 821 MiB/die freed. MTP on kvarn: acceptance 0.99, tok/round 2.99. Chunk 256 + ws 256 MiB serves clean (no bad_alloc). ==

-- (1) B1/B2 CLOSED --
 B1: G-AMD-31 parse gate (serve_options.cpp:437-462) DELETED per its own scheduled (a)-resolution (port landed).
 B2: gqa_attention_kvarn.cu JOINED the HIP roster (HipSources.cmake, next to the A3-wave gqa pair); the THREE perf_stub_die arms (cached_launch/_cached_batched_launch/_capacity_bytes) RETIRED same-commit from hip_link_stub_arms_perf.cpp (atomic retirement b0f8b1a9 grammar). nm-verified both sides on bin 73eecb33: all three symbols = strong T definitions from the real TU; perf_stub_die remains only for the bidirectional arms.

-- (2) THREE DEFECTS BEHIND B2, FOUND AND FIXED (RED->GREEN, all deterministic) --
 D1 (capability constant): kvarn_workspace.cpp require_kv_input refused heads!=2&&!=4 (CUDA-line TP2-era constant). This model at world=4 is kv_local=1 (Gqa27Tp4Geometry, pair 24:4); tp2_backend.cpp ALSO bound every kvarn workspace with hardcoded kv_heads=2 (6 sites). RED: first boot ever (73eecb33) died at warmup "gqa_kv_append_kvarn: unsupported KV head count" on all 4 ranks. FIX: gate relaxed to heads<=0 (device path is head-generic — scatter grid.y=heads; kvarn_quantize_heads_launch has an explicit heads==1 fast path) + binds now TABLE-DERIVED via head_geom_for_config(24,4,tp_w) (same-table class law as decoder_spec). GREEN: warmup PASS.
 D2 (gfx900 LDS limit): the kvarn tile-quant/dequant kernel requested 69,380 B dynamic smem (TileShared carried the Sinkhorn scalars) — gfx900 MaxSharedMemoryPerBlock = 65536 B (MEASURED, /tmp/hip_smem_probe). Every quantize/dequantize launch died "invalid argument" (3af434bb row: "kvarn tile quantize_k: invalid argument" x4 ranks). FIX: TileShared keeps ONLY the 64 KB X matrix (exactly at the limit); the scalar side arrays + reduction scratch moved to a per-device global slab (64 slots x 3,844 B, lazily hipMalloc'd, monotonic slot counter; collision needs 64 launches' worth of quantize blocks in flight — in-flight <= 8 lanes x 2 blocks). Bit-identical numerics: ninfer_kvarn_tile_cuda_test ALL CHECKS PASSED on gfx900 post-fix.
 D3 (V-plane dtype contract — THE AMNESIA): the HIP-lane bf16-GQA cache contract is K=bf16 bits + V=fp16 BITS (T3 adoption: gqa_attention_decode.cu:156 and the prefill SIMT flash both cast cache_v.data to __half; production append writes matching fp16 bits). The kvarn materialize kernel wrote its V temp as BF16 (CUDA-line contract) — the flash read bf16 bits as fp16 = garbage V = prompt-blind attention. RED: serve booted green but the model could not read the prompt (P1-P5 all empty/failed; "The user hasn't actually asked me a question yet" while bf16 reference read it fine; unit test_kvarn_gqa q-block rel_l2 0.69 vs fp32 CPU). FIX: bf16->fp16 conversion of the materialized V temp in the kvarn launcher (HIP-guarded; CUDA line untouched — there the bf16 V contract is correct). GREEN: q-block 0.0025, T=1 0.0046; serve P1-P5 GREEN.

-- (3) NOT-BUGS RULED OUT WITH EVIDENCE --
 gfx900 shuffle semantics (probed, /tmp/shfl_probe*.hip): __shfl_xor butterflies are self-partitioning on wave64 (exact); __shfl broadcast DOES honor width=32 (raw form crosses half-waves — width added to the kvarn-family broadcasts/reductions as hardening; xor down-chains proven exact). The materialize kernel itself is GREEN: kvarn_materialize_oracle_test PASS (all three regions, max_err 1.9e-6). test_kvarn_gqa's bf16-REFERENCE legs (0.488/0.506/0.496 residual) are a TEST-input artifact: the test writes the reference V plane as bf16 bits, which the lane's kernels read as fp16 — the reference is garbage, the kvarn side matches fp32 CPU truth. Test needs a lane-contract audit (own follow-up; the two residual failures sit in the staged-shadow (dead in serve, stage_pages=0) and packed-decode (non-default A/B route) checks.

-- (4) MEASUREMENTS (this box, this boot set) --
 PRE-FLIGHT (mine, bin 18993c16): 8192 ctx: kv(11152 B/t x 8192) = 87 MiB/rank; 36352 ctx: kv(11152 B/t x 36352 + mtp 2) = 386 MiB/rank, fixed 6947, required 7339 / usable 8160 (slack 820). BF16 comparison (W7 row + serve_fast.log): 34816 B/t -> 1207 MiB/rank @36352.
 VRAM FUNDER: 1207 - 386 = 821 MiB/die FREED at the served ceiling (36352). 200704-token projection on measured anchors: 11152 B/t x 200704 = ~2,135 MiB/rank KV — needs the full workspace math re-run at boot (allocator is the gate).
 CHUNK 256: kvarn @36352, --prefill-chunk 256 + NINFER_WORKSPACE_MIB=256: BOOTS AND SERVES, no bad_alloc, zero worker errors; needle@6k PASS (fixed 7107 MiB, slack ~1050). Perf note: 18.7 tok/s prefill at chunk 256 vs 91-125 at chunk 128 — chunk ladder should keep 128 as default, 256 fits but is not faster on this geometry.
 MTP on kvarn (mt=600 counting prompt @36352): acceptance=0.99 tok/round=2.99 rounds=201 gen=600, decode 23.41 s (~25.6 tok/s window); content = clean 1..164 counting (600-token cap), finish=length. MTP is FUNCTIONAL on the kvarn KV tier on HIP.
 PARITY (400-token budget, same bin, same 8192 geometry): kvarn_k4v4 5/5 GREEN (BLUE / ORANGE / 391 / ZEBRA-48213 / ZEBRA-73942) — byte-identical answers to the bf16-KV boot 5/5 GREEN on all five probes. Battery rows: WO_KVARN_A1_probes_kvarn_budget400.json, WO_KVARN_B_probes_bf16.json (+ the 64-token-budget variants).

-- (5) PHASE 0 ESTIMATE VS ACTUAL --
 Phase 0 estimate: "if the probe is clean ~half a desk-day; if gfx900 codegen breaks 1-2 desk-days". ACTUAL: the hipcc probe was clean (TU compiles) — the REAL port surface was three runtime defects behind B2 (head-geometry constants, the 64 KB LDS limit, the fp16-V plane contract), found only by boot+unit falsification. Total ~1.5 desk-days. Phase 0's portability guess ("int-code math likely portable") held for the codec; the flash/lane contracts were the gap — recommend future Phase 0s include a RUNTIME-contract inventory (cache-plane dtypes, shape-table geometry, LDS ceilings), not just compile probes.

-- FILES BANKED / EVIDENCE --
 bins: ninfer-serve_73eecb3370b6334e.bin (RED D1), ninfer-serve_3af434bbb0823eeb.bin (RED D2), ninfer-serve_63a3d37e42b97833.bin (RED D3), ninfer-serve_18993c160d07bf7c.bin (GREEN — boots, serves, 5/5).
 logs: /tmp/kvarn_boot_a1.log (D1 RED), /tmp/kvarn_boot_a1b.log (D2 RED), /tmp/kvarn_boot_a1c.log + kvarn_boot_a1d.log (D3 RED->GREEN), /tmp/kvarn_boot_c36k.log (36352+MTP), /tmp/kvarn_boot_chunk256.log (chunk 256), /tmp/kvarn_boot_b2_bf16.log (bf16 ref).
 rows: WO_KVARN_A1_probes_kvarn.json (pre-fix RED battery), WO_KVARN_A1_probes_kvarn_FIXED.json (3/5 @64tok), WO_KVARN_A1_probes_kvarn_budget400.json (5/5), WO_KVARN_B_probes_bf16.json (5/5), WO_KVARN_EXP1_probes_k4v2.json (k4v2 RED row pre-D3).
 unit: kvarn_tile_cuda_test ALL PASS; kvarn_write_path_test ALL PASS; kvarn_materialize_oracle_test PASS; test_kvarn_gqa q-block 0.0025/T=1 0.0046 PASS (residual bf16-ref-leg failures = test artifact, see (3)).
