# ACCEPT v2 review under SD-1 — agent5, plan owner (2026-09-15, relaunch seat)

**Inputs:** `NVFP4_ACCEPT_v2_and_RUNG_GRAMMAR_2026-09-15.md` @ e335edad + the chair's SD-1 amendment
(95749104: A4/@w4 elevated, A3/w2 OPTIONAL-CONTROL, annotate-not-delete). Review seat per
`SPRINT_STATE_RESUME_2026-09-15.md` desk row. Zero card, zero grant, one host-only measurement added
(probe below). All cited bytes re-resolved at this seat, not recalled.

## RULING 1 — the table SHIPS with three amendments; no row deleted

SD-1 re-prioritization does not change any row's CONTRACT, only its rank. Amendments, all annotate-form:

- **AM-1 (A3 annotation).** `A3 (nvfp4 @w2): OPTIONAL CONTROL — post-SD-1. Its only load-bearing use
  is as a one-variable-different leg against A4 (world is the variable; format is shared). Never
  quotable as the goal; never gates another row.`
- **AM-2 (A4 elevation — see RULING 2 for its full operational expansion).** `A4 (nvfp4 @w4):
  THE ACCEPT ROW under SD-1. NEW MINT, artifact-conditional, gated by honest-first boot at the SAME
  (bin, artifact) pair and by G1/G7 triage rows already landed (§8s/§8t of the plan).`
- **AM-3 (digest-formula single home — FINDING, not preference).** The table's Digest column says
  `sha256-16(text)` of `.reasoning_content`; the confirmation-fire input kit (chair 4bb2b02c) names
  the RAW RULING-4 grammar: `sha256-16(reason+content concat)`, wrapper-bug lesson applied. Two
  formula dialects on mint rows is exactly what the four-part-contract law exists to kill — a digest
  without ITS formula is a claim without its contract. **Before any A-row fires, the A4 row must
  print ONE formula verbatim and the runner must name which one it computed.** My ruling as plan
  owner: RAW RULING-4 concat form is the home (it was born from the wrapper-bug, which is the
  demonstrated failure mode here); the table's `.reasoning_content` column is annotated to that
  reading, not deleted. agent1 (grader semantics) holds veto per the re-home — if agent1's r25
  signed profile graded reason-only, the PROFILE precedent wins and the concat column is the
  annotated one; what may NOT happen is a mint digested both ways.

## RULING 2 — what A4's "NEW MINT" operationally requires when the ports land

A4 is the project's goal-line under SD-1 (NVFP4 fully operational at TP4). "NEW MINT" is not a
placeholder; it is a statement that **no oracle exists yet for this cell**, which imposes six
conditions, each owned and checkable:

1. **Prerequisite pair, not prerequisite green.** The honest-first boot (§8t: BOOT-0 cold gate +
   BOOT-A pre-port refusal + BOOT-B inverse) must run at the SAME (bin, artifact) pair A4 will
   serve, BEFORE A4. Per SD-1's cheap-falsification ordering: BOOT-A/B is one boot and zero new
   code; A4 without it means the first surprise lands on the graded row instead of the ungraded one.
   BOOT-0 additionally asserts the trap the row grammar carries: the env is `NINFER_ALLOW_NVFP4_TP2`
   while the artifact rides world=4 — every refusal row names WHICH guard fired, never which env was set.
2. **Determinism 25/25 @w4 (C4), same-boot.** 25 reps of 'Hi.' at max_tokens=32, world=4, temp=0,
   ONE digest across all 25 — the G2 bar applied at the goal geometry. The pair-law rides: the
   determinism pair is same-boot; a 25/25 assembled across boots is not C4. Finish_reason uniform
   (whatever it is) — that's A2's profile form inherited: within-world identical, golden-prefix
   open, ≤1 NAMED near-tie fork, uniform finish. Expect-profile, not expect-bits.
3. **The mint is (artifact-sha, bin-sha)-conditional.** A4's future golden digest is a property of
   the PAIR, not of the format. The row prints artifact `eaf8ad12…56d2` / 18,324,067,840 B AND the
   bin bank-sha (bank = filename = stamp) AND tree sha AND grant id. When either component moves —
   a re-export, a cured bin — the old digest is annotate-kept and a NEW row mints. That is what
   prevents tomorrow's "A4 FAIL" on a moved artifact, and it is also why C5's blessing below is
   stamped to the artifact pair rather than to "the format".
4. **C5 — BLESSED, at the strongest grade, by measurement made for this review.** The aging caveat
   ("assumes shared tokenizer") was measured at this seat, both artifacts' OWN in-band bytes:
   **all six `frontend/*` resources are byte-identical** between q3.ninfer and nvfp4.ninfer —
   tokenizer.json (12,809,320 B, full-payload sha256 `0997f410c57a1f4e53b09e4be8f4a172d90edd9564368fb0847030937229b9f3`
   on BOTH), chat_template.jinja, tokenizer_config.json, generation_config.json, preprocessor +
   video_preprocessor configs; union datum identical on both (248,044 + 33 added, extent 248,077,
   holes 0). Instrument: `tools/v340l/nvfp4/c5_tokenizer_identity_probe.py` (this commit; rc=0,
   0.8 s, host-only; doc-35 trap row honored — the union, never len(vocab)). **So: A3≠A1 (and
   A4≠A2, the world-4 generalization) genuinely convicts weights-not-loaded — for the artifact PAIR
   OF RECORD, as of 2026-09-15.** Blessing is NOT a standing license: the identity is a pair check,
   and the D2 law ("declarations age, bytes don't") makes it re-run-able — **falsifier condition
   stamped on the row: any new artifact (re-export or the convert-lane real-decode file) re-runs the
   probe before C5 may convict anything at that artifact.** That is doc-35's blessing route executed
   with doc-35's numbers, extended to the pair the caveat actually worries about.
5. **Coherence is the HUMAN conjunct and the mint makes that structural, not ceremonial.** A4's
   expected digest does not exist, so no machine arm can certify A4's output is *meaningful* — only
   self-consistent (which C4 already covers). C6 stays exactly as patterned: marker-in-file or
   PENDING, never self-signed, and for A4 the sign-off names the digest it read (a coherence
   signature predating its digest is grading vapor). Sequence when ports land: 25/25 → capture the
   mint digest → human reads the 32 reasoning tokens + the natural-stop text (A6' leg at w4 if the
   budget wants it) → C6-signed with date+sha. Until then A4's max state is MEASURED-PENDING-HUMAN.
6. **Artifact reality (RULING 3 below) is stated on the row** — A4 as written serves the QAT-exported
   file, and that sentence has to be ON the row, because the row's meaning is "the shipped packed
   bytes decode at w4", not "the converter chain is blessed" (that's the convert-lane's separate row).

## RULING 3 — the 18.3 GB artifact reality check, answered from bytes

**The mount file exercises the REAL decode planes end-to-end, not a fake-quant round-trip.** The
distinctions that matter, each already measured at this desk:

- *Stored form:* all 247 NVFP4 tensors carry genuine packed 4-bit code planes + block-scale planes
  in `blockscale-k16-m128x4-v1` layout; the reader's per-tensor `stored == encoded` check
  (reader.cpp:138-142) duel-closes this BY CONSTRUCTION at parse, and A5's full byte sweep
  (988 arms: entire codes plane + every scale-plane (row,group) through the swizzle law,
  re-graded 988/0 GREEN on the merged tree at §8u) graded the actual bytes. There is no
  dequantized-bf16 masquerade in the file.
- *Runtime form:* the route that serves those planes is the A16 route (bf16 activations x real NVFP4
  weight decode — G7 triage: host route table + SIMT `.cu` decode/small_t kernels). "Fake-quant" is
  a property of the CONVERTER's training-time math, not of any runtime path on this box — once
  loaded, the planes are decoded as stored. There is no runtime fake-quant round-trip to fear.
- *What "QAT-exported" honestly marks:* the artifact's PROVENANCE — sidecar names converter
  `ninfer@90059874`, torch 2.13.0+cu130, RTX 5090. Its numbers came out of a QAT pipeline; its
  bytes are real packed planes. A4 therefore means: *QAT-exported packed bytes decode deterministically
  at w4* — a strong claim, and the right one for SD-1.
- *The row the deploy ledger still lacks* (as the chair put it): a **fresh convert** through the
  shipped converter + the G1 export-golden admission chain would produce a DIFFERENT object —
  the "real-decode codec artifact" — and it does not exist yet. My triage (§8s) wrote its row; it
  belongs in the deploy ledger verbatim:
  `real-decode codec artifact: NOT PRODUCED — owner = convert lane (tools/convert/qwen3_8_27b/convert_nvfp4.py
  + the design-era inventory banner-marked stale at e94e5940); admission gate = G1's shepherded
  export-golden consumer (143650e4, needs one torch-host run — host blocker: torch/numpy/pytest
  re-measured absent); expected-sha-at-landing = TBD at production, not at request (phantom-pin law).`
  **Sequencing ruling under SD-1: the A4 mint must NOT wait on that row.** The goal-line is
  NVFP4-operational-at-TP4; the of-record artifact today CAN and DOES exercise the decode planes.
  When the convert lane ships, A4 re-mints as A4' per condition 3 (annotate-not-delete), and C5's
  probe falsifier fires on the new file. Gating A4 on the convert would be gating the goal on a
  provenance nicety — the inverted SD-1.
- One ledger-truth note: the "NVFP4 DEPLOY LEDGER" (Gemini assignment, chair fb313831/4bb2b02c)
  has NO durable file yet — the author went offline mid-era (e335edad) and no `docs/amd/*DEPLOY*`
  exists at any tip I can resolve. This review's Rulings 2-3 ARE its A-rows, codec row and
  gate-state; the chair should land them into the ledger at first merge so the synthesis has one
  home rather than four memories.

## RULING 4 — triage receipts re-affirmed against plan REV 3.20 (§8s, landed pre-stop at 1cb467c5, lane branch — NOT yet in amd/main)

- **G1 codec goldens: STRUCK as new work, with two owner-tagged CLAIMs.** Three of four legs shipped
  and re-verified from landed bytes (generator 41d5e020, corpus a024627b, pack-golden duel 81aced51);
  the fourth (export-golden consumer 143650e4) is HOST-blocked, not task-blocked (torch/numpy absent,
  re-measured). CLAIM: agent3 shepherds 143650e4 + af31aecd + ec45cfde into amd/main in one commit
  (agent2's namespace row closes with it); + one torch-host run whenever a torch box exists. The
  head-of-chain statement holds: the device-port grading (G2-G4 capability gates at NVFP4 geometry)
  compiles against these goldens — but per RULING 3 the pack-golden leg (shipped) already covers the
  BOOT chain; only the CONVERT-side admission needs the fourth leg. The queue is honest when it says so.
- **G7 A16Only-bind: STRUCK as new work (already covered), CLAIMed as two grammar rows.**
  The A16Only policy branch is host-side and already driven CUDA-free by
  `tests/nvfp4_dispatch_table_test.cpp:126-127`; the only CUDA-family-bound NVFP4 surface is the
  TMA/W4A4 sm120 archive (src/CMakeLists.txt:72-80). The ACCEPT runtime column must name which
  guard governs (the CC=12.0 pin-cell trap, one organ over — the chair's own warning, honored).
  The mintable by-product: A4's pre-port refusal is a host throw with pinned text
  (`nvfp4 linear: unsupported shape`, nvfp4_dispatch.cpp:19-21/:60-62) — the honest-first boot's
  A/B pair greps it exactly, both directions, zero new instrument (§8t).

## Not claimed by this review
No boot ran; no digest predicted; C6 stays human; A4's mint value remains unknowable until ports
land — that is what NEW MINT MEANS and this review makes it operable, not fake-guessable. The
c_capture map-consumption rows (law-40 form) stay behind this on the desk queue per the relaunch
order; decode-ladder numbers are background-collected, not graded-as-front.

— agent5, plan owner. Companion commits: probe tool + plan §8v row + this doc, on `amd-wo-nvfp4`.
