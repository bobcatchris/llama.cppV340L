# WO-MTP-1 (DRAFT, agent4) — SPECULATIVE DECODE (MTP) ON TP4: named frontier, pre-registered expectations

## 0. STATUS + WHY IT'S A WO AND NOT A PARKING LOT NOTE
User-surfaced gap, verified at bytes this beat: the q3 artifact **carries 14 mtp/draft-weighted
objects** (`text/draft_head`, `text/draft_head_token_ids`, `mtp/input_projection`,
`mtp/{embedding_norm,hidden_norm,layer/input_norm,...}`) — k=0 in every boot line is a PLAN
feature, not absent weights. MTP has therefore never run: on four cards, on two cards, on this
artifact, on any traced or untraced leg this era. The verify path is world-derived and
statically graded (n_prop_vocab = draft_vocab_size/tp_w at tp2_backend:1188-class), but
speculative decode lives in the SAME gather/geometry organ class that just produced the soup
(GdnConv /2 literals) — an untested path in a recently-diseased neighborhood is a frontier with
a named risk, not an absence. Drafted here so the next session inherits a plan, not a guess.

## 1. ACCEPTANCE SCOPE (what must be TRUE before any MTP leg is believed)
- **A1 format acceptance**: mtp/* objects' shipped formats (NVFP4 vs W8G32 endpoints vs BF16)
  must be read from the manifest and matched against the W4/W2 load roles — `classify_tp` skips
  `mtp/*` when include_mtp=false (tp_load.cpp:450); the ENABLED pass's roles are UNGRADED by
  every cell in this repo (the skip was always taken). First act: one host pass listing the 14
  objects' format/shape + role map, throw-by-name on any role that falls to Replicate by
  default (silent replication of a sharded head = the H3 class reborn).
- **A2 divisibility**: draft_vocab_ids.size() % 4 — the world-derived n_prop_vocab line exists
  but has NEVER executed on this artifact; require_parts_divisible-style loud throws are the
  expected outcome if it fails (loud = accepted, silent-wrong = WO fails).

## 2. PRE-REGISTERED EXPECTATIONS (organ law: every one is falsifiable on banked channels)
- **E1 (oracle-identity, the strongest and cheapest)**: at temp=0 greedy, speculative decoding
  is EQUIV-ANCE-PRESERVING — MTP-ON on a world must emit the BYTE-IDENTICAL stream to MTP-OFF
  on the SAME bin, SAME world, SAME prompt. The banked censuses ARE the goldens: w4 →
  84dbd7c7d3d50008, w2 → 348e77a1222dea7f. ANY divergence is a new defect with a named
  (world, leg, index) — no interpretation needed, the same self-anchoring structure that made
  the soup unfakeable. This oracle works BEFORE any per-knob geometry is trusted.
- **E2 (materialization law)**: per-rank placement==materialized at MTP-on (the STEP-0/F
  cross-check the era owns) — the draft head adds bytes; the equality law must hold or the
  loader pair-math class is alive again.
- **E3 (verify-phase taps)**: r23's tap machinery already gates LA/LE on Prefill; extend the
  gate to Verify (phase-letter GV-prefix to be agreed) AND the RecordForReplay rail — the
  verify path IS where that rail lives (text_context :2321/:2367, read whole this session) —
  first-ever cross-world VERIFY ladders, w2-vs-w4, same-map discipline as the plane verdict.
- **E4 (T-shape law)**: the dossier's Q2-era asymmetry (T from ne[1] vs fixed-1 template)
  lives in the verify call-sites. Pre-declared reading: with k>0, verify T = width*batch columns;
  R1's T-handling and the ring's kMaxTokens arm must DISAGREE-LOUDLY anywhere they diverge
  (require_r1_shape exists; the expectation is it FIRES on any k-value it wasn't minted for,
  not passes silently).
- **E5 (conf-reassembly)**: speculative_accept_greedy_drafts at /4 reads verify_logits_full
  with the 2*nv stride (tp2_backend :2913 area — the D-21/D-22 comment family, world-derived
  now) — the GATE-3 capacity refusal must fire LOUD if any per-world product exceeds the
  buffer, and the census law applies: a draft ACCEPTED differently cross-world is E1-failure
  with a cause, grade by the accept-mask, not the tokens alone.

## 3. FIRE PLAN (when it runs, one boot at a time, card discipline)
LEG-0 (host, zero-card): A1/A2 pass — manifest + role-map audit, no boot.
LEG-1 (w4, --spec mtp --draft-tokens 2, untraced, prompt 'Hi.', GEN=64): E1 identity vs the
  banked 84dbd7c7; E2 from its own step-0 rows. PASS = speculative decode is REAL on four cards.
LEG-2 (same + C_LAYERS/planes verify-extension): E3/E4 — first verify ladders.
LEG-3 (w2 control, same bin): E1 vs 348e + side-by-side decode timing (chapter-two item 1
  numbers ride this leg — wall-clock per leg UNtraced, first real decode-rate measurement pair).
Fail-of-any = named arm, NOT a re-roll; every leg's green feeds the deploy ledger.

## 4. WHAT THIS WO DOES NOT CLAIM
It is not an order (chair seq-159 item 3 named it a judgment call); it does not pre-assume k=2
is the right knob (draft-tokens is a plan parameter, 1 is the conservative first); it does not
touch NVFP4-accept sequencing (deploy queue owns order; E1 makes MTP-on w4 provable against
banked bytes whatever the geometry story says — the safest first boot the queue can hold).

— agent4, 09:5xZ, drafted between the 59-token run's legs (item 1 fired first, this is the
zero-card work that filled its load windows).
