# TP2 CLOSE-OUT PLAN — bug-free gate on the clock (chair, 2026-09-13 18:2xZ)

**Why this doc exists:** user order — TP2 is behind schedule; closing = **bug-free**, defined as: the determinism cell below is GREEN + every checklist row banked + the two instrument fixes landed. Rebooted sessions: read THIS, then your lane handoff row inside it; act immediately, no relay archaeology. Reference control group: `/tmp/ninfer-gfx906` (same engine family, MI50-line port, deterministic at fixed config — proof per-config determinism is achievable here; our per-request fork is a delta from a tree that doesn't have it).

## GATE (definition of done — nothing else closes TP2)
**G-CELL (determinism cell):** one server, greedy, SAME prompt ×5, `cmp==0` on text → **5/5 identical**. RED is already banked (T3i7r1 5/5-distinct `1d0ff3c6` + G17x 3-identical/2-distinct `82b9d60f`). Green requires the mechanism named AND fixed. Permanent suite member thereafter.

## Lane assignments (parallel; zero conflicts)
- **agent4 (hunt owner):**
  1. ANSWER the chair's KVarN re-derivation (17:4xZ): pass-3 excluded rewind-no-zero by PATH-NAME ('paths the corpus never enters') but `kvarn reset inflight` prints 6× in every serve log — either restate the exclusion with the RESET-PATH predicate, or §3b re-opens as leading suspect. One line first, bytes after.
  2. ARTAG **raise-path one-liner** (:249 atomicOr→dead channel; volatile-RMW form your 30a proved crosses 5/5) — rides the same queued fix commit; then 30b green-leg (2 s) inside next pair grant.
  3. **Owns G-AMD-31 spec** (below) — boot is yours.
  4. Then: A-4 completion (paired-fire arms already in, 52 checks green), B4 remaining, tp1-control re-ask to agent2 stays on dev1.
- **agent5 (rebooting — differential seat #1):** `docs/amd/GFX906_DIFFERENTIAL_agent5.md`, ordered 18:0xZ: transport architecture diff (they have NO `src/core/multi_gpu/one_shot*` — their TP2 AR is a different design; our host-pinned ring family is ours alone ⇒ prime suspect-delta), known-bad construct census BOTH trees (they refused fp32-atomic K-split in writing, PASS2-DESIGN.md:78 — grep-census our serving closure for constructs on their refusal list), what their precedent licenses for parity bars (cross-config near-ties: accepted there AND here; in-process fork: they never had it — that asymmetry is the report's headline).
- **agent3 (rebooting — decisive-instrument seat):** read `T3_VOIDED_INFERENCE_INVENTORY` (eb4846df, yours) + this doc, then draft the **G-AMD-31 margin-census spec**: one boot, same request ×5, per-step dump argmax winner + top-2 margin + which KV/conv slots read (existing env instruments + at most one env-gated print arm from agent4's file). FIRST DIVERGING TOKEN decides the tree: margins ≈ 0 AND logits differ → upstream state (feeds agent4's KVarN answer); identical logits, different winner → comm/ring re-indicted (slot-ring wrap from pass-1 §3 becomes prime). This is the hunt's last decisive experiment — design to kill, one pre-declared branch table.
- **agent1:** determinism-test inventory (ordered 18:1xZ): every byte-identity assertion in their docs/tests/tools/parity mapped present/absent/degraded here; port candidates for G-CELL.
- **agent2:** liveness → tp1-control re-fire on dev1 (grant stands); host-suite four-count at merged tip; death-row triage note for the ledger.
- **Gemini:** G-CELL as gate member the moment agent1's port lands (red-capture already exists — this one's triple is pre-staged); no new work until then.
- **chair (me):** grants/pair schedule (next pair grant = G-AMD-31 immediately after agent4's KVarN line, 30b folds inside), merges with receipts, disk watch (15 G: agent4's queued build is the only one permitted until window prep), this doc kept current.

## Sequenced critical path (target: G-CELL green tonight)
1. agent4 KVarN line (minutes) → decides whether G-AMD-31's boot tree also carries a §3b re-open.
2. G-AMD-31 spec (agent3+agent4, ~30 min paper) → chair grant → **one ~7-min boot**.
3. Mechanism named → fix (candidate shapes already banked: reset-path zeroing arm / slot-ring depth / volatile raise) → **G-CELL green run, same night**.
4. Parallel and never blocking: transport diff (a5), test inventory (a1), ARTAG raise fix + 30b (a4), tp1/host-suite (a2).
5. TP2 STATE closeout + 4-card window announcement (TP4 is gated behind G-CELL green only for parity bars; first-light shape-math already done).

## Known-bad already cleared (do NOT re-hunt)
Sampler/split-k ordering (statics+ARTAG), GDN slot line (census §3a), D2 class (measurement, positive-control named), publish WORD integrity (G-AMD-30, arming-proof passes), RNG/argv drift (greedy, identical flags). Dead channels: host-mapped atomicOr (30a — anything reading via it is control-flow-unsafe, see eb4846df §3 interim rule: `[ARTAG]` HIT = reported, NOT stopped).

## Rules in force
- **RESIDENCE BEFORE SPAWN (standing clause, agent2 WARM_CACHE row b06ce67b):** run `results/amd/p3/residence_probe.py` before any boot window — it says whether the boot pays disk. Cold road is ~6 min (43 MB/s device floor); a served boot self-warms ~93%; post-pressure re-warm is one line (`dd if=/media/chris/EMTEC256/qwen3_8_27b_q3.ninfer of=/dev/null bs=8M`, ~1 s if only a slice evicted). Page cache holds 90-100% across build-free gaps; expect less after GPU-pinning boots or builds.
- RED→GREEN closure law; absence claims name arming proof; GRANT-ACK-in-arguments (exit-7/77, kit v7); boot pinned bins from `/home/chris/artifacts_bin/` only; one build at a time (disk 13 G); directs only — no channel posts (hub-enforced).

## Bin queue, post-flip (all single-delta, all authored, all witnesses live)
- **(i) epoch-wrap alignment** — DESIGN HANDED OFF by agent3 direct to agent4 (message IS the design doc; host-arg step+1 into the flag, retire the :83 `*dev_epoch` read and the :389-392 change-gated upload; ARM-2's :344 legacy memset dies in the same delta — one commit, two named defects, REDs pre-banked: G17x:755-764 + A2Q3 via the poison predicate). Bin (i) build = agent4's hands.
- **(ii) ×25 census** on the census-capable bin — reading table armed: first causal count-change (warmup expiries → zero expected), paired-vs-lone MC31-H separates worlds, fork table + poison grep on any served region; agent1's verdict script (four states: served-clean / lone-strand / FAILLOUT / instrument-error) is the row's mechanical half; agent3 holds interpretation.
- **(iii) conviction bin** — Z-arm, MC31 margins, ring depth, or (b)-crossing redo (both-skip/one-skip cells), whichever (ii) names; (b) rides behind the census per agent4's review, capacity-aware crossing is the design note.
- Agent2 seats held: host-suite four-count at current tip NOW; tp1-control stays binder-dead per death row pending product ruling; agent5's AR-parity + wrap-fix design docs queue behind (ii)'s rows; Gemini's wiring triggers armed for green-or-tracked-red at the release row.
