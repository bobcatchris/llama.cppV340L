# PLAN 2026-09-13 — finish TP2, then TP4 (coord cold-start doc for all restarted sessions)

**State at 01:0xZ (cite-by-name; verify tips at use, never from this doc):** amd/main = 0a953192; t3-wip = 0b2130a8; serve branch = ddea37df (has q4/q5 whitelist rows + Stages=2 + verify-arm + instruments — SANCTIONED repro base for the 0x8000 fault; its Stages=2 arm is load-bearing: main ships 0,4 which cannot launch on this box). Registration package CLOSED: 28-entry array, 11 files, three-lane attested, retraction-complete. Parity CLOSED both families at the ruled bars (cos+role-count; 2-ULP guard retired to ratio<0.85 outlier class): decode 12/12 min 0.999757, prefill 8/8x12/12 min 0.999702, K1/K2 bit-exact, B1/B2/B3 PASS. The ONLY live fault: device memory-access 0x8000 at first gating step (cols=51 unsplit, ldmatrix/mma SIMT staging at real geometry — wave64 assumption = the named unvalidated risk); exclusions banked in serve-branch G17b_ledger.md sec G17d (nullptr-partial & normalized_x = constexpr-guarded dead, Stages-line self-consistent, capacity/link/routing/launch-config ALL eliminated). Eight instruments found tonight, all product kernels exonerated.

## PHASE 0 — the token (IN PROGRESS: DIAGNOSED 01:00Z)
**DIAGNOSED by agent3, mechanism coord-verified at bytes: (A) landed a 64-bit token contract on 3 files; ~7+ consumer sites (q4/q5 gemm .cuh x2 each, swiglu, small_t, grouped, w8 pair/rowsplit) still hand smem_addr()'s `unsigned` — implicit-widening into ldm_addr_t = parse-green, device-dead; truncated generic pointer zero-extends into the 0x8000 class EXACTLY (AMD LDS-window base arithmetic). Deleted-overload guard makes every un-migrated site REFUSE TO COMPILE — proven live (q5 .cuh:360 error). The root-cause law this teaches: a contract change needs a compile-refusing mechanism, not a remembered list. FIX AUTHORIZED (coord, 01:0xZ): agent3 authors GUARD + MACRO SWEEP + acceptance-extensions (FRAGCELL 512-thread multi-warp leg + deleted-overload negative cell) as ONE atomic commit on amd/t3-wip; guard-compile enumerates the true site-set (CUDA-only-guarded sites reveal themselves); coord merges; restarted agent4 re-verifies their 4 residuals' ancestry vs new main BEFORE touching anything (their own debrief first-move), then re-fires under 17e terms. Fix lands (three-line precedent class); **17e = pre-authorized, terms agreed in STATE 00:5xZ** (dev0,1, ws=96, NINFER_GATING_TRACE mandatory arm-taken-per-step, throw-site attribution, sub-NONE — template floors at T~51, condition law: state tokens-including-template; 1200s/launch, own-pid, cold-CIFS ~6 min). One boot per TRUTH; truths changed = new stamp, that's how to spend the night's remaining device budget.

## PHASE 1 — TP2 properly done (the completion checklist, ~one more session)
1. 17e green: first coherent q3 tokens, margin-stable argmax per the coherence law (G17c_coherence_rules.md on serve branch).
2. Arm-routing measured: agent4's dladdr map (arm_routing_measured.md, serve branch) — confirm which arms a REAL boot executes at real T; this is the evidence that 'pre-authorized safe-stub' claims were about dead routes, and it feeds gemini's tier-3 reachability design.
3. Host CPU-layer suite: agent4's (a)+(b) wiring is ON MAIN (10e4a7cf lineage) — verify generate rc=0 at the merged tree (their earlier 72-error generate predates main's own tests-door; reconcile the two doors, cite both), then RUN the 9 host-only serve tests, four-count reporting (passed/skipped77/failed/not-reached).
4. tp1 CONTROL serve on the ref artifact (fresh stamp; NOTE-branch = expected, ref has no Q3G64; =129 arms-proof only for q3 ART) — the TP2 parity table gemini's TG1 consumes.
5. B4 window (one normal dev2 stamp — 'redesign' basis retracted, cost = one window) + warp.cuh:297-303 comment fix (attr accepted-and-inert) + G-AMD-25 if the Phase-0 read needs device confirmation.
6. Then G3 = closed if 1-5 all green; STATE + PROGRESS entries; sprint-goal sentence gets written.

## PHASE 2 — TP4: the DELTA MAP FIRST (zero-device, pattern = tonight's TB2 map that killed a plan-phase for a paragraph)
HEADLINE LAW: engine is world==2 BY CONSTRUCTION (tp_engine.cpp:862 comment). TP4 = product port. BEFORE any port work, one audit doc, every claim measured, no estimates:
- **Blocker-question #1 (may outrank everything): is the q3 ARTIFACT TP2-shaped?** The manifest carries 'per-rank K-halves 3072/8704 whitelisted' (94086bbd promote history) and 1124 objects; if the export itself is 2-way, TP4 needs an EXPORT or re-shard-on-load decision that crosses Team Green's promote machinery — that's a USER/Green question first, an engineering question second. Answer it in the map's first hour.
- world==2 inventory (agent5's -E -M closure machinery is the exact tool): every hard-2 assumption — tp_place_capacity/manifest-measured placement, tp2_budget.h (the NAME is the design), one_shot AR epoch/step math (advance_epoch/reset_step rank arithmetic), allreduce_argmax conf fusion (pairwise?), warmup cols arithmetic (template floor 51 at 2 ranks — what at 4?), Q3G64_F16S=129 TP-degree dependence (verify, do not infer from tonight's constancy), GDN 2:1 qkvz row skew (tp_load.cpp:214-226 — designed-in, 48 heads/4=12 is clean but the fused 16384-row range math must be DERIVED, not divided).
- transport design: staged no-P2P AR at 4 ranks (3.13 GiB/s measured, eager-first law, cross-device graphs dead — no hope there), AR-count-budget at 4, rccl bring-up vs one-shot extension: NAME the options, measure cost per token from tonight's TP2 numbers before choosing.
- VRAM: the upside ledger (measured): ~3416 MiB/card weights even-ish + tonight's per-rank floor anatomy (dec state 224/96-pair ckpts/160 const...) x4 -> per-card headroom ~4+ GiB -> context becomes a REAL question: put the 120k-class as a measured goal for TP4, the thing TP2 could never be.
- scheduling collision to plan: TP4 bring-up needs ALL FOUR cards as one unit — dev2 as window-card and the dev0/1 serve pair BOTH die. Phase 1 items 5/6 (B4, G-AMD-25, control serve) must be CONSUMED or explicitly deferred before TP4 boot windows start. Sequence, don't assume it resolves.
- gates: tonight's entire discipline inherits (single-writer HipSources, check-(a) registration per landed file, bare-include/codegen rows, three-class repair contracts, content-hash-no-numbers, name-your-baseline). TP4 surfaces will need their own exception pairs from day one — gemini lane grows before the code does.

## PHASE 3 — TP4 bring-up (only after the map is signed off): work orders split by the map's answers (engine port, AR choice, artifact/export decision path, TP2-then-TP4 parity tables, permanence gates), each with its own first-stamp. Expect the map to RANK these and possibly kill some — that's what it's for.

## LAWS (the night's, condensed — every restarted session operates under these)
verify-before-rule / read matched lines not totals / claimed != proven (freshness by sha; identity-bound receipts that NAME their artifact) / measure the real file over reproducing it; assert test-artifact existence before believing output / re-derive at the point of use, THROUGH THE EDGE not the constant / no-numbers in package specs (refs + content-hashes; digits = instrument output tagged by tree-sha) / name the BASELINE of every check / three-state exit always; green-that-did-not-run is the enemy class / bounded listings prove nothing about absence (use predicates) / retraction names the scripts that still emit it / plumbing for read-only checks, never move a cited worktree / VRAM LAW (allocator is the gate; measured anchors or the number doesn't exist) / written grant before device, one boot per truth, release rows / own-pid kills only, never shared-process pkills / df -h before >5 GB builds (25 G on / at 01:00Z; 20 G of it is the ref artifact) / artifacts: q3 canonical = /media/chris/EMTEC256, ref = repo artifacts/, BOTH VERIFIED no re-hash (CIFS readers banned; watcher guards) / cross-line: merge main INTO amd/main, never push main; all docs under docs/amd/ / STOP-ask > silent workaround, always / surface-to-user for silent lanes (wait, never reassign) / coordinates-namespace law: identity claims cite joined form (hub+pi+claim-file).

## SCHEDULING (cards at 01:00Z): all idle, zero KFD, dev0/1 = boot pair, dev2 = window card, dev3 spare. Disk 25 G /, /media 208 G. Serve pair reserved for 17e whenever the fix lands — nothing else may take it.
