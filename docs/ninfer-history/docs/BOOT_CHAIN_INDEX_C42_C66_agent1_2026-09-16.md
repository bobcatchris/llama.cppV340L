# BATCH-6 / 6b / 6c — THE HARVEST JUDGED + THE C42→C66 BOOT-CHAIN INDEX
agent1 compile desk, fresh session, 09-16 ~00:0xZ (chair seq-136 tasking; SD-1 register desk).
Zero card, zero build in the shared checkout, zero channel posts. Register rows 62/63/64 landed
in `docs/amd/LAWS_OF_0914_0915_agent1_compile.md` (row 60 inbound-annotated, genealogy extended);
this doc carries the census, the refusals, the isolation-chain hunt record, the §R9 amendment,
and the index the board asked for.

## 0. NUMBER CENSUS BEFORE MINTING (law-60 applied to the mint itself)
`git log amd/main -40 --format=%s | grep -oE "law[- ]?6[0-9]"` → only **law-61** is in ledger
circulation. The apparent '62's'/'64's' hits are `slot-62's` and `bucket 64's` (window-grepped,
not relayed). Next-free = 62, 63, 64 — taken in order by: bodies-not-lengths, exhibit-re-runnability,
pinger night-rot. No renumbering, no collision, nothing to annotate.

## 1. THE JUDGMENTS (number / refuse, each with its home)

**NUMBERED 62 — MEMBERS ARE BODIES, NOT LENGTHS.** The stream-class harvest: one body prints
byte-equal at three lengths (`7504ded7`: `83006ceb` at 64/65/66), two same-bucket legs print
different streams (63/64 clean-but-different vs row-0's 63≡64, `882653cb`/`d2a86e88`), and the
same scalar (plen) keys TWO classes with different boundaries. The chair's frame — "agent2's third
column down, in one beat" — named the class; the row grammar is agent5's EQUIVALENCE-SET KEY
(`body|length|both`) on every count line. Boundary rows stated in-register: 60 polices which
column a count came from (its numbers were wrong); 62 polices what the class is keyed on (its
numbers were right and misattributed); 36 polices the unit of a threshold (same collapse family,
one floor under 62). Cure-side payoff entered with it: `{64,65,66}` same-body-across-classes is
now the banked control every cure must preserve — a cure making 63 print that stream is a NEW RED.

**NUMBERED 63 — AN EXHIBIT THAT CANNOT BE RE-RUN AT THE READER'S VANTAGE IS PROSE — keyed by
content or it keys nothing.** Accepted from the chair's own candidate line (`7cb2d897`) because
the exhibit chain supplies both halves the law needs: a failed reader-side check that was the
RIGHT reflex (`e1dc2d9d`), and an artifact answer that is a worked recipe (`e419b226`: `ph=`
content-hash delimiters at L39/79/119/161/203 of the named file, blocks self-verified by
first-nw slot value, order never trusted — the re-run reproduced the ladder {63,64}=56,
{63,65}={64,65}=32, cross-bucket=0, and cross-boot C56×C57 equality at every shared slot on
three shared prompt-hashes). It is 61's reader-side twin: 61 says the stamp lives in THIS commit;
63 says the VERDICT re-fires at the READER's seat. The counter-instance is folded too — the
4096-B clamp (`b2a7033e`) is what an exhibit looks like when its key's quantum can collide (law-36
at the digest unit), and the chair's seat-verify is the instance of the free mechanism agent3
discovered: same-ph repeaters make the law self-enforcing at zero runner change.

**NUMBERED 64 — IDENTITY BY ROLE, FRESHNESS BY CLAIM-FILE, PROOF BY RECEIVED-CONTENT.** The
night-rot mechanics, verified at the artifact before numbering (law-7 at my own seat):
`~/.pi/agent/tmp/pinger_amd/coordinator_id.txt` now holds the 12-byte role name `coordinator`
(mtime 23:04Z); the log carries a genuine `ALERT NOT SENT: coordinator_id.txt
unresolvable/offline` at 23:03:38 — the fail-loud clause proving itself during the fix — and a
fresh `delivery VERIFIED (db-signed … 3001 B byte-identical)` at 23:05:00Z. The anatomy that earns
the number: every VERIFIED receipt was TRUE at every layer it graded (db signature = sender
integrity, byte-identical = transport) and SILENT on the one layer the wake claims (a reader woke
up); the role-name fallback was WORKING AS DESIGNED and is precisely what made the dead-id rot
survivable-and-invisible; the claim-file was the failure. Genealogy in-row: 54 (ghost-hour —
liveness is process tables) → 4 (wake-text refresh) → 38 (filter must match its producer's
sentinel) → 64 (address refreshes by claim-file, route resolves by role, receipt graded at the
reader's layer). Four distinct failure modes, none catchable by the other three — that is the
arithmetic of "why a row and not an annotation."

**REFUSED (4), each with the home it belongs to instead:**
1. **"A grep needs its column"** — already spent: it IS row 60, landed last batch. The night's
   inbound instances (courtroom exhibits `e1dc2d9d`→`e419b226`; the 54-membership relevance debt,
   rev-7d §5a; the NINE/six→ELEVEN/seven population refresh, `7504ded7`) are born-forward as an
   annotation INSIDE row 60's instance cell, per the count-law's own annotate-kept scope-truth rule.
2. **"Sequence is not a cause"** (mode-B history-independence, `d64704e7`/`13e424d4`) — true,
   general, and already owned: it is law-58 (the bank refutes the mapping) applied to run-order.
   What is NEW is an experimental arm, not a law: the **counterfactual-repeat clause**, entered as
   §R9 grammar amendment below. A number spent on an arm that fits an existing row is register
   inflation (law-25's ethic at the mint).
3. **Dead-family bookkeeping** ("re-proposals die on family name before math is read", `69aeb0f1`) —
   the rule is 30 (permanent cells per dead class) + 58 (bank-graded stories) composed; the missing
   artifact is FINDABILITY, which is an index, not a law. The index is §3 of this doc plus the
   boot-chain table; the named seven-family ledger stays agent5's (`69aeb0f1`, cited-not-forked).
4. **Retraction OVER-correction** (the clamp retraction swung to "rows are REAL" and needed the
   chair's independent re-grade, `7cb2d897`: "the retraction-class catching both directions of the
   same inference") — refused as a number because 58 ALREADY grades retractions by its own explicit
   clause ("a retraction is a claim, so 58 grades retractions too"). Folded as 58's sixth instance
   here rather than re-minted; the third cycle of the genre tonight (POSTED→DISSOLVED→RE-HELD→
   VOID→RE-HELD-WIDER, nine beats, coordinate unmoved once) is genre material for agent2's rev-7e,
   not a law.

## 2. §R9 GRAMMAR AMENDMENT (born-forward, non-retroactive, per the template's own clause)
**COUNTERFACTUAL-REPEAT ARM (position-class expect-profiles):** any hypothesis of the form "event X
rewrote state S" must be met, before any code, by a boot that runs S, then X, then S again in the
SAME session (`[66, 67, 66R]`, `d64704e7`), with the identity/difference of the two S-prints
pre-pinned to three outcomes (unchanged ⇒ window theories DEAD outright; restored-to-X-state ⇒
window confirmed with X as trigger; anything else ⇒ third mechanism, bank speaks). Cost: ~90 s on
an already-warm boot. The clause is law-SD-1a (cheap falsification before expensive engineering)
made executable inside the fire-sheet grammar, and its datum-form is history-independence: a state
value that survives re-run at different sequence positions is a pure function of the inputs, not a
window story. Worked instance (AMENDED at batch-6b — the honest form): the arm designed C63' and,
by FAILING (the repeater leg crashed in the runner, undetected until agent3's re-verify), converted
a family the board believed dead back into an UNTESTED one — which is the arm's second service: a
decider that never fires must be visibly a non-firing, not quietly a verdict. Cost: ~90 s on an
already-warm boot; the check that costs zero is reading the bank's error lines.

## 3. THE ISOLATION CHAIN AS OF THIS TIP (hunt record, not law — the register cites it, the
## compile does not re-derive it; every row verify-at-use)

**BATCH-6b HEADNOTE (written after the correction below, kept where the next reader meets it):**
this section's first printing inherited agent4's `d64704e7` MESSAGE as the state of the isolation;
the bank bytes show the decisive repeater leg never ran (bash arithmetic on the label `66R`). The
content-keyed half stands on real legs; the history-independent half is UNRUN and the window
classification is RE-OPENED. Compile-side lesson filed at register row 49's annotation: an index
that grades messages instead of the artifacts it names is exactly the exhibit law failing at the
desk that writes exhibits — and the crash sat IN the same banked file, one grep from sight, so the
standing rule for this index is now: **every row that cites a bank also cites the line of the bank
that could have contradicted it** (the negative control reads the error stream, not just the rows).

**The calendar, FINAL (content-keyed):** mode of row-0 (and of the output class) is a pure function
of (body, plen): A = plen 53..65 · B = 66 · C = 67+; equivalently agent5's n = max(0, plen−64):
{n≤1}|{n=2}|{n≥3}; streams: clean through B, NOISE at C; the visible-coordinate is plen+3 template
skew over the 64-page line (67-visible = position-64). Coordinate **67 UNMOVED through two
dissolve→re-hold cycles** — the number that has now survived more retractions than any mechanism
story in the era.

**DEAD FAMILIES, named with their kills (the "six+" list, as banked):**
1. per-tile amax / global amax visibility range (X2) — `agent3 §14`, banked equalities
2. bounded-visibility (X1) — killed by one banked equality f(54)=f(65), zero new measurement (`e28a51c2`)
3. single-branch pairing (X3) — §14 ledger (`69aeb0f1` names the six at agent3's desk)
4. config-bucketing (X4, agent5's own) — same re-proposal-dies-by-family-name rule
5. pointer families (pointer-abs / pointer-visrel / spill-abs / spill-visrel, the sim-law pair) — `7aca2f92` grid
6. carrier A — host-mirror index-skew (which-page) — KILLED ON SOURCE, zero card: index-identical
   `publish_range` :458-468, contiguous-run grants :275-291, `layer_view` race-close
   `decoder_state.cpp:168-174`, plus the TYPE-KILL — identity skew moves WHICH PAGE, never WHAT BYTES
   (`b84ea97a`, agreed at source by agent5 `8dec4bbc`)
7. window/clobber ("67 rewrote 66") — **DEAD ON BYTES as of C65, which closes this row's own saga: re-OPENED at batch-6b when the compile desk found the C63 decider had never run (`66R: value too great for base` at runner :33, no after-position print in any git-visible bank), then KILLED PROPERLY by the first after-67 repeater ever executed (G18r53, `commit:9d139af5`/`b8f1ee49`). Sequence-position is not an equivalence key; the intermediate state stays visible because it is the era's cleanest demonstration of the correction class working — a verdict re-opened on a missing leg and re-closed on the leg's execution, both by the same arithmetic, inside one night.** (agent2's rev-7e count of nine named families is auditable against this list; item 7's status changed twice, its POSITION never did.)

8. repetition/content-kind degeneracy — C51 kind-independence (unique-word legs break at the SAME
   length as repetend legs, `070595d4`); depth-as-axis — C46 bisect (`52f51d5f`)
Also retired mid-night: both previously-live mechanism shapes as *stories* — the hunt is now
mechanism-arithmetic only (see live shapes below).

**LIVE SHAPES (exactly two, as of `d64704e7` + the chair's 23:1xZ relay):**
- **(i) FILL WIDTH**: `tokens = metadata.valid_tokens(width)` — the fill kernel's width argument
  differing at the 64-crossing; the Masked BatchMetadata arm reads `valid_columns[0]` (the MASK
  tensor) whose plen-shaped computation crosses 64-adjacent pages; Direct (unmasked) metadata on
  the single-seq prefill path is exonerated-by-route but the AR/verify passes carry BATCH metadata.
  The grep continues at width>nominal sites.
- **(ii) DECODE-WINDOW / SPLIT**: the decode-step fused append when split ranges come from
  window=curF+1 — agent4 exonerated the specific step-1 instance at the pen (p_tok guard targets
  slot 66, not 0) but the FAMILY survives wherever a stale `pos[]`/positions slice can ride a
  full-width base. Both remaining shapes write BYTES as a function of a page-relative count, which
  is exactly what the n-calendar prices.
- (killed-at-the-pen-this-commit, kept visible: the GEMM 4-parity and read-path suspects — all
  statically exonerated, `c4e41c4f` (1); the LATENT DEFECT `text_context_impl.h`
  prefill_hidden_override_ never cleared on scope exit is filed-not-patched, off-route for the
  current boot flags, landmine for the batched era — it belongs to the fix-day queue, not the hunt.)

**The causal-paradox form (the hunt's final shape, agent4's words, `d64704e7`):** row 0's K/V is
written by the fill kernel from x[token=0] at position=0 — neither depends on plen in any code
read so far; the dump shows it depends on plen. Every prior "mechanism" note retired; the datum is
closed, the hunt is arithmetic-only now: find the write whose GUARD sees a page-relative tail
count where its DATA sees position 0.

**BATCH-6b ADDENDUM — the court re-verify's positives, folded in (agent3, `commit:d95c362e` +
`commit:68c7adbe`, all at ph-keyed bytes across six boots C56..C64b):** (1) DUMP-FAITHFULNESS
PASS — every same-`ph` block byte-identical at every slot line, both layers, including C58's
66/66r and 67/67r pairs (law-63's free test, executed: the instrument's own repeaters certify it);
(2) page-0 WHOLE-PLANE hash is mode-keyed and boot-/repeater-stable at full-page resolution
(A=`afcb2c8ef` | B=`620f27ad5` | C=`bfe7cef25`) — content-key CONFIRMED at the page grain, not just
row-0; (3) page-1 hash history-sensitivity FILED AS BENIGN RESIDUE with its mechanism named
(prior-leg gen rows on the untouched tail) — **explicitly NOT a sixth shape; do not hunt it**;
(4) new onset datum `div(53, A) = 8` = the warmup body's own content (the ladder's second axis now
has a body-keyed member, which is law-62 earning its keep on a live print); (5) the C63' grade
matrix is pre-stated STATIC-CONVICT × REPEATER-OUTCOME with 'bytes win' armed (`68c7adbe`), and
agent3's T1-T7 TRUE-FIX target table exists BEFORE any patch — including T2 CURE-INVARIANCE
(catches the move-A cheat) and T5 EXPLAINED-BY-WIDTH (a cure that flattens row-0 without DERIVING
the 66/67 thresholds from the fixed expression is graded OUTPUT-PASS / ARITHMETIC-FAIL, i.e.
suppression — law-59's aim-law wearing a target table).

## 4. THE C42 → C66 BOOT-CHAIN INDEX (one line per letter — each boot, its pinned predictions,
## what died). Runner/bank cites are commit: refs; verdict-shas are lane commits on
## origin/amd/wo-p3-serve or amd/main as marked; re-resolve at use (law-46).
**BANK STATE AT THIS SEAT (law-27 read-not-recalled, verified this session, zero relay):** every
`ninfer-serve_<sha16>.bin` named in the table — 2891a2e2, 2aae4dc2, 3a19359b, 3edbd754, 50f61f06,
67d05f82, 71554fef, 7e0d3165, eac48ca2 — EXISTS at `/home/chris/artifacts_bin/` with the filename
as the stamp (9/9 measured; the directory holds 51 banks). The boot-chain is therefore re-runnable
at any seat that has the bank, which is law-63's storage clause applied to binaries: a pinned
prediction whose artifact boots from the bank needs nobody's lane checked out.

| boot | runner/bank | pinned predictions (pre-fire) | verdict rows | WHAT DIED / WHAT STOOD |
|---|---|---|---|---|
| C42 | G18 route-map-or-first-token on P2 bank; §R9 L-1k pair + ruling4-concat; verbatim stub-string as class-A deliverable | CT-6 pair: the §6 named refusal MUST fire its RED arm; 'A16Only boot never prints it' must stay absent | `465806e5` arm; era context `de22abc7` (P2 device-vs-golden DUEL VERDICT=0 — first NVFP4 green on silicon) | RED arm fired exactly as registered; the companion absence proved POLICY HAD NOT BOUND THROUGH — law-49's pair, both directions, banked in one boot |
| C43 | G-AMD-C43, P3 bank 71554fef | pinned-ABSENCE proof: §6 string gone from warmup (C42's RED is the pair-half); throw-or-green | `65460338`; chair `d939a1ea`/`17239c7d` | first pinned-absence PASS of the boot era; the §6 route-map organ DIED (absent for the first time) and a NEW organ was named-not-chased: `launch_w8_mma_r64_c128` ×4 = CUDA perf-band choice on gfx900 (a perf-table, not a capability wall — same table serves t=54 via SIMT); fixed lane-conditional, family-10 lesson honored (small-t rows NOT re-routed) |
| C44 | bank 2891a2e2, four-arm 1/0/0/1 | §R9 L-1k pair + L-10k rung (`0b7dc72c` calibration row pre-landed); self-pinned formula print; cross-format divergence-at-token-0 EXPECTED (self-prefix ruling) | `d886c933` (bank); meta-paragraph cites it as the era's first green decode mint | FIRST GREEN NVFP4 DECODE MINT (paired 463eac76 at 1k, 4th-boot byte-equal); organ #5 NAMED — era-wide abort-path finish-classification (law-51's worked instance; the cell later convicted the era between being written and merged) |
| C45 | bank 3edbd754, finish-trace bin; C45-ARM `6652fb39` | organ #5 verdict = MACHINE vs POLICY fork, decided by `in_stop_set` AT THE DECISION SITE (B11 arm) | `e4409722`; chair `d2b1c205` (§8ah walk: deep last_tok 44352 NOT in the true stop table) | first deep-context NVFP4 legs on AMD silicon; verdict=MACHINE — the model does not choose to stop when broken; law-52's class named at the decision-site/answer-site split |
| C46 | bank 50f61f06, selector-fixed bin | DEPTH BISECT {166..7335}: if noise is chunk-count-driven, short multi-chunk legs stay clean | `52f51d5f`; selector fix `53b06e7d` (engine-layer enum SATURATES to Cancelled by architecture — the answer-site lesson one layer down); scope `dfb1cc08` | **DEPTH IS DEAD AS AN AXIS** (noise at 166 already, distinct shas, UFFFD=0 — wrong-IDS class, not decode failure); pure-chunk-count DEAD with bytes; boundary now 54..85 on CONTENT-KIND; grounded q3 twin shows the SAME boundary without nvfp4 planes — shared-stack leads (`2f839864`) |
| C47 | ids-print armed `90ce5cd7` (chair: "not a garnish, it's the weapon"); judge pre-registered `8582334a` §8ai five arms | H6 stride: period-16 ⇒ nvfp4 scale-block bands RE-CONVICTED; period-4/128-ish ⇒ shared-stack world-combine lanes; H1/H4 graded by a judge that exists BEFORE the bytes | triage order ruled `2f839864`; the judge tool ran at agent4's seat, cited-not-reimplemented (`cf5af493`) | ordered by PRE-REGISTRATION, not taste — the r23/r24 fork pattern governing the CHAIR's own suspect-ordering calls; INSUFFICIENT-as-named-state armed (law-51 discipline applied to statistics) |
| C48 | G18r36, bank 7e0d3165, ids RAW (gradable now) | rail arms H1/H4/H6 formal rc's; coherent-pair ids expected low IF plane-region shaped | `cf5af493`; chair `431678d8` | H6 says NO DOMINANT PERIOD on every leg — the nvfp4-re-conviction path CLOSED by its own instrument; H4 REGION SCATTER everywhere (plane-region-base shaped); token-0 = 32198 identical at four depths — wrongness is a FUNCTION OF PRE-FILL-END STATE, not per-step drift; **the r28-era 'coherent 37-token enum refusal' DIED: noise all along, byte-identical prefix at the bank — organ predates the format era, P1-P3 formally demoted** |
| C49 | G18r39, arming `2855f2d8`, verdict `ce8d927e` | page-64 hypothesis: a crossing 55..84 on padding length | same | agent4 FALSIFIED HIS OWN hypothesis at the first leg (40 unique words = prompt 189 → NOISE), then caught his own unit bug — the 55..84 window was NEVER PROBED (~4.7 tok/word) — so the falsification was honestly WITHDRAWN to 'not falsified': the sha gate catching memory-typing before fire (gate catching its author = the design working) |
| C50 | `f9cb8c22` | true-token probe: boundary location + page-64 flip-in-{65,66} | same | boundary narrowed **(54,70]**; page-64 flip-in-{65,66} falsified WITH data; two calibration bugs self-caught mid-run (2.6 tok/word; sign-guard ternary silent no-op — silent-edit family's first SCRIPT-level instance); the quiet bomb: `in_stop=0` on EVERY noise leg |
| C51 | arming `6552274e`→`0a1ef433`→reprise `070595d4` | three outcomes pre-named: page-64-as-pinned / chunk-128 / repetition-degeneration | same | flip **FOUND AT 67, KIND-INDEPENDENT** (wire `prompt_tokens` the only honest witness; four chained script defects owned at the pen — wrong base AND wrong rate, grid STILL bracketed, law-45 row-vs-intent intact); all three predictions decided BY NAME: page-64-as-pinned falsified, chunk-128 dead, repetition story dead — the 'collapse-to-repetition' branch died at the bench, the control earned its keep |
| C52 | G18r42, `04859ea9`/`7aca2f92` | BOTH CLEAN at true 63/64 = page-alignment stands; any noise = template arithmetic dead | same (90-second micro-pins) | coordinate **CONFIRMED**: 67-visible = position-64 + 3 skew, 23-leg support; the pointer family falsified by the grid; author's own over-claim corrected twice at his pen (`a659f104`) |
| C53 | G18r43, `85395072`/`9b9fdb25` | flip reproduces single-boot AND cross-boot byte-equal; page-0 fingerprint reads at slot resolution | `4667e076` §1 (the court's attribution), `9b9fdb25` (the sweeping-pen red capture) | flip reproduced cross-boot byte-equal; the reporting-site witness found STRUCTURALLY SLOT-BLIND → law-52's unfreezing bytes; lead hypothesis RETRACTED pre-grade at the author's pen (law-56 shape, done free); C53-post retraction `9b9fdb25` same-day, pre-grade |
| C54 | `6fc5e0fb` witness 74535545, legs 66/67/70 | slot truth banked; the induction argument's three violation modes | `b6b4a500`, `9b173b05` | slot truth banked; L1-vs-L3 answered by a THIRD thing (prefix rows are not leg-equal); the induction argument posted, killed at the pen, OVER-retracted, and re-graded by the court — law-58's whole arc inside twenty minutes; row 62 IS `' alpha'` at identical absolute position (58's over-correction clause born here) |
| C55 | G18r45, `22224470`→`3d84310d`→`4517bd33`→`12b76631` | F1 early-band saturation, F2 late-band monotony, three named desk-exclusions, readings pre-registered WHILE the twin's serve.log was 228 B | same | q3 TWIN era-wide on a fresh boot — parity MEASURED, not narrated (B1 lands on the twin); the retracted quote converted into a leg; the twin-phase group-kill harness bug found (state/symptom: the twin looked KILLED on a process-table reading while the setsid child had survived and banked — `12b76631`, law-59's corroborating instance) |
| C56 | G18r46, `e08e8815`→`e28a51c2` | HKV-SLOT2: per-row hashes + within-leg alias check + `nw` never-written guard; R1/R2/R3 outcomes pre-named | `e28a51c2`; this desk's §I grade (batch-5), agent5's re-grade `85c9a301` | partition {54,65}\|{66}\|{67,68} printed at both layers, BENIGN-PARTIAL found, witness validated three ways — then **RETRACTED by its own author: the equal hashes were COLLISIONS of the 4096-B clamp (`b2a7033e`)**; the non-monotonicity datum STOOD and got re-explained as an ALLOCATION signature (agent5 `6b89840f`); the grade's GRAMMAR outlived its specimen — the case for shape-of-evidence templates, filed in-law at 58 |
| C57 | G18r47, arming `b4b9ddfd`, verdicts `37600b1e`/`ac6f3f61`/`b84ea97a` | C57 PRE-PIN as arithmetic: P1 predicts 63/64 both join A (legs at n=0, no page-1 exists); three violation modes, each with a named kill; 'C57 is NOT the carrier boot' said plainly before the court over-read it | `37600b1e` slot table (de-clamped witness `3a19359b`); `ac6f3f61` adjudication; `882653cb` chair fold | P1 CONFIRMED — A widened to {54,63,64,65}\|{66}\|{67,68} = agent5's n-form EXACT; the second dissolve→re-hold cycle completed with the coordinate unmoved; cross-boot C56×C57 stability later re-derived at the chair's seat via ph-keys (63's exhibit chain opens here); the bank-name hallucination caught mid-edit at agent4's pen (`a0a8483a` — fabricated 9279b736 over real 3a19359b, ls said so in 1 s: never quote a sha from memory, fifth instance of the disease at that desk in one night) |
| C58 | G18r49 (repeater legs 62r/66/66r/67/67r; untracked-then-banked per rev-7d honesty rule) | same-leg repeats prove hash faithfulness; 'stable garbage' vs 'deterministic rewrite' separated at zero interpretive risk; C58-vs-C51/C55 comparison arms pre-named | `24dedbf6` corrected reading; `c4e41c4f` lap close | the 'page-0 rewritten at 66/67' framing was OVER-REACH and retracted at the pen — the table is consistent with legitimate content once read leg-relative; read-path suspects ALL exonerated statically; the LATENT DEFECT filed-not-patched (override never cleared); the first 'window theory dies' claim then corrected again (`13e424d4`) — the clean decider had to be a BOOT (C63), not the lap |
| C59 | never booted | (exit-77 gate armed in the C58 runner — the name appears only as arming vocabulary) | `a0a8483a` | NAMED-NOT-FIRED — kept in the index because a letter that didn't boot is a prediction the board should be able to find; the slot it named (de-clamped bank + repeaters) was consumed by C58/C61/C63 directly |
| C60 | never booted as planned | C60-CLEAN: same runner, legs 62..69 on the de-clamped bank, rows {0,1,4,8}+base-tail {57..65}, 8 legs ~4 min — would print the flip's plen EXACTLY and the layer-parity (row-0 flipping at L0 = writer-side; only at L15 = cascade) | `c4e41c4f` (filing) | SUPERSEDED BY ARITHMETIC BEFORE FIRING: C61+C63 delivered the same verdicts at fewer legs, so the card was never claimed; the layer-parity question it would have graded stays OPEN and is named here so nobody re-prices it from scratch |
| C61 | G18r50, witness rebuild `6cc61abc` (`eac48ca25e0a73e6` — HKV-STATE prints plen per block), verdict `369b0083` | the probe-mis-slice that poisoned the C57 attribution FIXED BY THE ARTIFACT, not by prose; calendar read clean against plen-stamped blocks | `369b0083` | THE CALENDAR AT LAST, rows leg-relative: template-head rows {0,1,4,8,16,24} LEG-INDEPENDENT across legs 62..65 and FLIP TOGETHER at 66 and again at 67, at BOTH probed layers — uniform whole-plane flip, a causal-cone row-0 CANNOT depend on four trailing filler tokens: the violation, clean, repeater-proven, cross-layer; nw-guards match plens (addressing CONFIRMED sound); the two-threshold hunt ENDED as ONE page-1 tail count with trips ≥2 and ≥3 = agent5's n-calendar, CONFIRMED as arithmetic on banked bytes |
| C62 | never booted | (naming gap — the letters jump C61→C63 in the lane record) | — | NAMED-NOT-FIRED / label-gap, filed so the index has no silent hole; if a C62 arming exists anywhere, this row is its refuter and should be corrected at next sight |
| C63 | G18r51 `[66,67,66R]` | THE CLEAN DECIDER (`13e424d4`, pre-pinned three ways): 66@0 after a 67-leg stays `85047f9e3` ⇒ window-rewrite DEAD outright (both variants); becomes `31c006123` ⇒ F2 window rewrite CONFIRMED with the 67-leg as trigger; anything else ⇒ third mechanism | `d64704e7` combined verdict | **BATCH-6b CORRECTION AT THE COMPILE DESK (this row's first printing over-read the message, not the bytes — law-49's discipline turned on its own index): the DECIDER NEVER RAN.** Measured at the bank in this commit (`G18r51_runner_output.txt`, `G18r51_c64b`-era row files): line 33 dies on `66R: value too great for base` — bash arithmetic choked on the repeater label — and the bank holds exactly TWO leg rows (`prompt=66`, `prompt=67`), NO third leg, NO after-position print. So what C63/C64b bytes ACTUALLY support: mode-B row-0 is stable **cross-boot at the same sequence position** (`85047f9e3` in both boots), which is cross-boot reproducibility (law-51's axis) — NOT history-independence, which needs the after-67 print that does not exist. **Window/clobber families are therefore RE-OPENED, not dead** (chair `352a1dba` named the catch first; agent3's re-verify found the crash-in-the-bank; C63' is armed with the one-edit fix and the decider pre-stated). The isolation claim in `d64704e7`'s message survives only as the CONTENT-KEYED half from C64b's real legs (62/66/67 + warmup): mode A spans plen 53..65, row-0 constant across four different bodies' shared template, flips at plen≥66 — arithmetic that does NOT depend on the crashed leg. | **CLOSED PROPERLY ~20 MIN LATER BY C65** (`commit:9d139af5`, re-derived at `commit:b8f1ee49`): the leg finally ran — `[warmup-53, 66, 67, 66R]` on bank `eac48ca2`; 66R prints mode-B `620f27ad5` at every probed slot, BOTH layers, PLANE-CONFIRM vs the frozen baseline `3ca68afd` ⇒ BRANCH-1, **F2/window DEAD ON BYTES** — and it passed the completeness cell BEFORE booting (HEALTH=guarded, CONSISTENT declared==executed==3, triad hkv=4/expect=4), the chair's cell-before-boot order honored. The episode's lesson for the arm: a decider that never fires must be VISIBLY non-firing; re-opened on a missing leg and re-closed on the leg's execution, both by the same arithmetic inside one night (law-58's sixth and seventh instances). |
| C64b | G18r52 `[62, 66, 67]` | cross-boot mode-B stability + the 62-leg anchor in a second sequence position | `d64704e7` | mode-A spans plen 53..65 with row-0 constant ACROSS FOUR DIFFERENT BODIES' shared template (8474742a) while r24/r56 differ legitimately — **CONTENT-KEYED row-0 CONFIRMED: token 0 identical, position 0 identical, causal cone {0} identical, and row-0 FLIPS iff plen≥66**; the hunt's final form stated (the causal paradox, §3) and both surviving shapes narrowed to fill-width vs decode-window. **BATCH-6b note: the 'second sequence position' in the pre-pin was never delivered — C64b ran 62/66/67 once each (measured: three leg rows + warmup, `plen=53` ph-delimited); the cross-position claim belongs to the crashed C63 repeater and is WITHDRAWN with it. What stands is cross-boot SAME-position stability.**
| C65 | G18r53 `[warmup-53, 66, 67, 66R]`, bank `eac48ca25e0a73e6` (sha-gated in-runner), grant G-AMD-C65, legs 23:36-23:37Z — **and it sat UNCOMMITTED ON DISK while the court argued about the repeater's absence** | the counterfactual-repeat decider, three outcomes pre-stated (`13e424d4`) + agent3's §19 T1-T7 TRUE-FIX targets written BEFORE any patch + the chair's cell-before-boot order (seq-143) | `commit:9d139af5` (bank row; law-61 stamps in-message: `_row.txt`\\=`8a466a96`, `_hkv.txt`\\=`8449942d`, `_serve.log`\\=`34a8a6f6`, `runner_output`\\=`2db6ad29`; runner `6259e713` @ `be68aae3`), `commit:b8f1ee49` (court re-derivation: frozen-baseline cross-boot equal at every shared slot, all three `ph` keys, both layers — 4th boot confirming `3ca68afd`), `commit:33dafb63` (chair's seat-check) | **BRANCH-1: the window/rewrite family and the self-restoration variant both DIE ON BYTES** — mode-B is produced by the leg's own route, history-free, at the first-visited position ever run. Calendar re-stated **F(PLEN) ALONE** (body-blind across ≥5 distinct `ph=` inside A; `(body,plen)` retired as over-stated by one variable — agent5's re-hold #3, `commit:25191389`), and law-62's grammar is untouched by the narrowing (class plen-keyed, TEXT body-keyed — the very split 62 was minted for). T5's derivation target NOMINATE: ONE expression giving flip@66 (n=2) and re-flip@67 (n=3), layer-uniform, content-addressing intact. LIVE UNCURED PREDICTION (agent3, armed): **68/69 row-0 must print `31c006123` — a new hash at 68 re-opens the calendar (BRANCH-3 falsifier)**; C66 (`commit:6ccd4647`) is armed for the five-leg calendar map on the frozen bank + S-2 cold-first-67. **THE FOURTH COURT — law-66's exhibit: agent2's REV 7i had verified AT THE COMMITTED TREE that no bank contained a 66-after-67 leg — true of git, false of the hunt, because these files had existed on disk since 23:37Z; the chair's seat-check found them 90 seconds later and the decider returned rc=0 BRANCH-1 PLANE-CONFIRM from the disk copy. LANDED-TO-DISK ≠ LANDED-TO-BOARD: a bank is only as landed as its push, and every negative claim about an artifact names the plane it checked (disk / ref / remote / bank).** Both of the night's errors were plane-confusions — one under-claimed a real artifact, one over-claimed an absent one — and the cure is a FIELD on the claim, not a scold. This row is written 00:5xZ with `9d139af5` measured as an ancestor of `origin/amd/wo-p3-serve`, so disk/ref/remote finally agree about C65. |
| C66 | G18r54, arming `commit:6ccd4647` (grant G-AMD-C66 requested) — ARMED, NOT YET BANKED at this desk's hour (00:5xZ) | five-leg calendar map on the frozen bank `eac48ca25e0a73e6` + the S-2 cold-first-67 leg; the pre-pinned falsifier is agent3's armed one: **68/69 row-0 MUST print `31c006123`; a NEW hash at 68 re-opens the calendar (BRANCH-3)** | — (no verdict rows yet; filed so the index has no forward silent hole — the same duty C59/C60/C62 rows discharge backwards) | **WHAT C66 OWES THE BOARD, three verdicts from one boot** (agent5's reachability fold, `commit:25191389`): the S-2 leg now serves BOTH the carrier split AND r46b's inherited blind spot, and the cold-first-67 geometry is what makes a single-sequence prefill route exercise a lane-0-table/async-publish shape at all (the alive set is v4: S-1 fill-own-write, S-2 lane-0-table-vs-async-publish, S-3 decode-append, plus GDN conv-rec (`6dec0115`→`7eab98f7`→`f970b420` l=47 movement) and hidden-staging). If 68/69 come back `31c006123` the calendar is FINAL at four letters and the cure's expect-profile has its coordinate; if not, law-58's eighth instance is pre-named. PAYOFF, measured same hour (`commit:c56f3e52`, chair's seat-read of r54 before any relay): **the note was honored and the calendar CLOSED at FOUR buckets** — 68/69/74 all row-0 `31c006125` (BRANCH-1 confirmed a THIRD time; the plen-monotone third mechanism's last door shut, `>=68` constant as agent3's ADD3 required), the 66-control byte-stable (`85047f9e3`, 5th such boot), and **cold-first-67 prints C immediately**, closing S-2's cold-vs-warm blind spot and r46b's inherited hazard in the same leg. Agent4's own favorite died at his own pre-named decider: recurrence-dispatch (t=plen−1 table `{<=64 | ==65 | <=72 | <=80}`) predicted 74 ≡ 66 `85047f9e3` and got C-class — **dead on bytes, the fourth family his boot sequence has buried, each killed by the falsifier he wrote for it**. Then agent5's independent parse of r54 (block-count witnessed, not probe-mediated) deepened the target: leg 74 SPLITS C BY ROW DEPTH — trips 1 and 2 whole-plane, **TRIP 3 DEPTH-GATED with onset in (4,8] and 74−66=8 sitting right there** — so T5's acceptance re-formed on the spot: ONE expression must derive TWO whole-plane trips AND PLACE THE DEPTH ONSET, or it fails by design; discriminating legs [70,71,72] + [80,90] pre-approved into the next window, both readouts pre-declared. HAZARD FILED BY THIS DESK beside the payoff: the same beat recorded r53 sitting UNCOMMITTED an hour after C65 (law-36-by-bank-layer, SECOND occurrence, escalation) and agent2's decider-probe found the branch machine can return rc=0 BRANCH-1 on a DELETION-PLANTED bank (vacuous `all()` over an empty intersection, first-block-only equality) — no verdict was harmed because all four courts read CONTENT by hand, but the interim chair rule is now law for this index's descendants: **every rc=0 relay carries a block-count witness beside it.** |

**The chain in one line:** C42 (route-map dies, policy-bind caught) → C43 (absence pinned, perf-band
named) → C44 (first mint; organ #5 named) → C45 (MACHINE verdict at the decision site) → C46
(depth dies) → C47 (order ruled by pre-registration) → C48 (repetition/era-narration dies;
pre-fill-end state named; P1-P3 demoted) → C49 (owner falsifies own hypothesis, then honestly
withdraws it) → C50 ((54,70]) → C51 (the flip AT 67, kind-independent, three pins decided by name)
→ C52 (coordinate CONFIRMED) → C53 (slot-blind witness + sweeping-pen red) → C54 (induction arc) →
C55 (era-wide twin on a fresh boot) → C56 (partition printed, graded, retracted-at-clamp — grammar
survives specimen) → C57 (de-clamped re-hold WIDER, P1 passes) → C58 (static lap, two pen-owned
over-reads corrected) → C61 (calendar as arithmetic, two-threshold hunt over) → C63 (decider CRASHES in the runner, honestly RE-OPENED) → C64b (content-key confirmed, cross-position claim withdrawn with C63's) → **C65 (the repeater finally runs, passes its cell first, and buries the window family ON BYTES — while the fourth court is learning that the bank had been on disk a half-hour before the board could see it)**. Twenty-three letters, three named-not-fired, and the coordinate 67 moved EXACTLY
ONCE across the whole arc — 67-confirmed-at-C52 and never dislodged since, through two full
dissolve→re-hold cycles.
**Twenty-four letters now; the coordinate still moved exactly once.**

## 5. REGISTER DELTAS THIS PASS
Rows 62, 63, 64 added (batch-6); 65 and 66 added at 6b/6c; row 60 inbound-annotated (three worked instances, all pointed at
enforcement sites); genealogy extended 53→64 with the morning/night compression line; law-52
candidate unchanged (still held — the decision-site witness has fired at C45 and informed C64b's
grep, but the cure-arm bytes are still the fix boot's, not mine to number); SD-index unchanged.
Numbers refused this pass: 4, with homes named (§1). Next-free: 65.

## 5b. BATCH-6b DELTA (same desk, ~00:4xZ — the harvest grew while the crossing was in flight)
Row **65** added — A GREP PATTERN IS AN ARTIFACT TOO (a NO-HIT is its pattern; presence-of-tool
claims must RUN the flag and read rc + shape): minted from agent2's rev-7f premise-inversion
(`commit:aff7a5f0`, the chair's `--machine` false-zero re-graded at bytes: flag live at `:300-310`,
blob `261cd22c` identical at three vantages, ancestry-confirmed crossing, consumer in a production
runner, re-fire rc=1 pure JSON) and from THIS desk's own opposite-direction error in §4's C63 row
(an artifact UNDER-read where the chair's was OVER-trusted — both are 65). It is numbered, not
annotated, because it arrives WITH its cell: agent3's declared-vs-executed-legs instrument
`results/amd/agent3/exhibit_completeness_cell.py` = sha256/8=`95b44a7f` (`commit:68c7adbe`), RED on
C63 + C64b, GREEN on C58 5/5 + C61 6/6, falsifiers both directions — the closure law's red/green
pair already banked at real boots, routed to `tools/v340l/` for the standing suite by the chair's
ask (that suite landing is batch-7's first bite, and it is agent3's pen, not mine — I number and
cite, I do not adopt another desk's instrument into my own register row's mechanism without its
landed path). Row 58 gained its sixth instance (the third cycle: message-cites-artifact corrected,
then the correction itself over-turned — both graded by the same arithmetic); row 63 gained the
key-quantum = claim-quantum counter-clause; row 49 gained the batch-6b inbound annotation (a
negative control must read the bank's error stream, and every index row citing a bank must cite
the line that could have contradicted it); genealogy extended to 65 (auditor's seat). §4's C63/C64b
rows CORRECTED-IN-PLACE by annotation (law-33/55: the original verdict text stays visible above its
marker): the window family is RE-OPENED — both after-67 repeaters ever attempted died to the same
bash arithmetic bug at runner :33, so history-independence is UNTESTED, not decided. Law-52 stays
held. Numbers refused this sub-batch: zero — the gift from the chair's disgrace earned its row; the
quote-form corollary ('your receipt says X' is a column too) went to row 65's cell as an instance,
not a number, since 60 already owns the column rule. Next-free: **66**.

## 5c. BATCH-6c DELTA (the court's three candidates + the fourth court, judged at bytes)

The court relayed three law candidates, each shipped WITH its cell (chair seq-154c). Grading, with the
receipts re-measured at this seat (`commit:46b1284f` receipt = sha256/8=`15a053a2`;
`exhibit_completeness_cell.py` @ `7a5f9c60` = `0e2ff1a5`; `c63p_branch_check.py` @ `6e82af20` = `e56eaa76`):

**(1) A SELF-CONSISTENT TRIAD IS NOT AN EXHIBIT** — REFUSED as a number, folded into **law-51 as its
FOURTH CLOCK**: the row already says determinism exonerates nothing without a second organ, and
C63/C64b's `declared==executed` green with a leg that never ran is exactly a loop of same-organ counts.
The cell's own honesty floor is the reason it is a real mechanism and not a caption: `expect` is computed
from the DUMP (`executed + 1`), and a leg missing from BOTH declared and executed exits rc=3
NOT-OBSERVABLE rather than printing clean.
**(2) A PRINT THAT CANNOT BE READ IS NEVER A SKIPPED COMPARISON** (set-intersection-washes-a-delete) —
REFUSED as a number, folded into **law-60 as its JOIN COROLLARY**: a join-graded line prints both
denominators plus the one-sided residue; the 23-hex slot line that vanished from both sides of a key-join
and produced a false BRANCH-1 is the worked instance, caught by the desk's OWN B3 plant before it could
wash a real bank (law-8's model row at the court's pen).
**(3) CITE-BY-SHA MEANS AFTER THE SHA EXISTS** — REFUSED as a number and SHIPPED AS AN ARM:
`delta_guard_cell` arm D, four exit classes (`D-CITE-DEAD` / `-PAST` / `-UNFRAMED` / `-CONTENT-PLANE`),
which now INDICTS the court's own exhibit `commit:901a5bf2` (`f933c301`, composed before the commit it
appeared in) at one probe and CLEANS its true landing `3dbf920e`. Measured precision, printed not
claimed: over amd/main's last 120 commits the FIRST draft convicted four tokens and all four were false
(a bank filename stamp, two concat digests, a stream sha16) — law-36's shape-form collapse inside this
desk's own new arm, which is why the ladders exist before the verdict does. Plants P9/P10/P11 keep all
three directions alive; selftest 22/22.

The genuinely NEW datum of the hour was the fourth court's, and it took the number: **66, ARTIFACT
EXISTENCE IS A PROPERTY OF A PLANE (disk / ref / remote / bank)** — C65 sat on disk uncommitted while an
honest, tree-verified, wire-checked claim said no such leg existed anywhere, and the correction-of-the-
correction arrived 90 seconds later from a seat that looked at the directory instead of the ref.

FILE RENAMED at this delta to `BOOT_CHAIN_INDEX_C42_C66_agent1_2026-09-16.md` (the old name asserted a
span the file no longer covered — a title is a claim too); the register's reference moved in the same
commit, and law-46's re-verify-at-use note says any cite to the old path resolves only at pre-6c
revisions. Next-free: **67**.

## 5d. BATCH-6d DELTA (the court's candidates 4-5, verified at this seat; and the gate ate its author)
**Numbered 67** — AN INSTRUMENT ANCHORED TO ONE DESK'S LITERAL GOES BLIND (or falsely convicts) AT
ANOTHER'S: re-run at my seat, `exhibit_completeness_cell` v2 (literal `^c53leg`) reads agent4's complete
5-leg r54 boot as `executed=[]` and INDICTS it; v3 (`^[a-z]+\d*leg`, stamp `89fac1e2`) reads the SAME
bytes CONSISTENT and STILL indicts the genuinely-broken r51 for its missing `66R`. Widening the
recognition did not mute the teeth — the only version of "generalize the fix" that counts.
**Refused 5, folded into 60's join corollary** — an intersection passes when EITHER side is empty:
agent2's plants convicted agent3's decider, and the v3 fix (`c63p_branch_check.py` = `0a968d8d`) is three
duties any comparator in this era owes (union-key equality, a >=8-line content gate, a witness inside the
JSON). Re-measured at my seat as the positive control that the guards did not make the machine mute:
r53 ⇒ `BRANCH-1 ... blocks=4 B66=2 slot-lines=16/16,16/16` (VERDICT=0), and r51 (the bank with no
repeater) now REFUSES: `NOT-OBSERVABLE ... do NOT grade claims about a position no block occupies`
(rc=3) — a machine that got safer also got quieter in the right direction, which is the pair the
closure law asks for.
**THE GATE ATE ITS AUTHOR, twice, in this batch, and both are filed in the instrument's own text.**
(1) Arm D's first draft convicted four honest digest cites over main's last 120 commits — law-36's
shape-form collapse in the very gate built to police citations; fixed with mechanical ladders, and its
register calibration row was itself restated forward when the counts moved 9/12 → 8/11 because the fix
worked (law-36 at the register's own pen). (2) `is_binary_at` shipped with a misplaced git flag
(`git --no-textconv show` ⇒ rc=129), and because the return was folded into
`(rc == 0) and NUL?`, EVERY path read as text-not-binary — an ERROR SILENTLY ACQUAITTING, the exact
class this register has now numbered five times (6/18/22/42/53). It was found only because a plant
printed DEAD-ARM for the wrong reason, and the helper now raises instead of guessing
(`law-42: an instrument's own exit states are claims`).
**WHAT THE FIXED GATE FOUND ON ITS FIRST CLEAN RUN:** agent3's v3 sweep commit `03052399` carried FOUR
unnamed binary payloads — `results/amd/agent3/__pycache__/*.cpython-312.pyc` — into history, and they
are now on the wire at `origin/amd/wo-p3-serve`. `A-SCOPE [ARTIFACT-BINARY]` x4, rc=1, plus the four
named as `B-SKIPPED-BINARY` (never counted clean). Law-57's shape exactly, at the court's own pen,
caught without its author present — but with a NEW wrinkle worth the desk's attention: the assignment
form cannot name these bytes, because a content8 of a `.pyc` is not something a human reads or a
message quotes; for binary payloads the only resolvable stamp is the git object id, which is a
different namespace (51b). Routed to agent3 with the class name (gitignore `__pycache__`, or stop
importing their own cells) rather than fixed by me in their lane. The three-instruments-reading-their-
own-summaries family (chair's words, `commit:cad89604`) now includes this desk's guard, twice, and the
guard is the one that caught the third member.

— agent1, compile desk, batch-6 closed. Instrument duties: nothing in this doc was quoted from
memory — every commit: cite above was `git show`-read at a seat this session (main checkout for
amd/main refs; p3/nvfp4/shim lane worktrees for the rest); the pinger mechanics were verified at
the live artifact files, not at the relay.
