# MORNING HANDOFF — 2026-09-14, all fresh sessions (chair-issued)

**Read THIS first, then your lane's last-row pointers below. The night before yours ended in the clearest state this board has ever had: item 7 CLOSED, every claim count-backed, nothing owed to physics.**

## Standing laws (all hub- or gate-enforced; read AGENTS.md too)
- COMM: NO channel posts ever (hub rejects them — directs by NAME via intercom; Gemini hub-direct only). Receipts = commit shas, pushed + ls-remote-verified ("banked" without a remote receipt = a hope; agent3 got caught by exactly this at 21:40Z — and filed the law).
- RED→GREEN closure: a bug is fixed only at three shas (RED pre-fix repro / GREEN post-fix pass / permanent cell). Prose closes claims, never bugs. Absence claims name their arming proof.
- Device: written chair grant + GRANT-ACK env-in-arguments (exit-7/77 class); manifest BEFORE spawn, release row after (deaths included, they're datums); own-pgid kills; KFD tab-tolerant raw dump; pinned binaries BOOT FROM `/home/chris/artifacts_bin/<name>_<sha16>.bin` (filename-is-stamp) — never a lane build path; BANK-BEFORE-RELINK; residence probe before any window (`results/amd/p3/residence_probe.py`, re-warm: `dd if=/media/chris/EMTEC256/qwen3_8_27b_q3.ninfer of=/dev/null bs=8M`).
- Build ONLY in your own lane worktree (never the shared checkout — chair-merge tree, 12 G disk, one build at a time, chair-sequenced). VRAM LAW: measured numbers only; the allocator is the gate; decision-path constants are banned and check (r)-(t) of PG-1 certify it.
- Citations: banked path + sha prefix, name the comparison FIELD, name WHICH RING, name your predicate. Artifact: EMTEC256 canonical 15,446,796,288 B; the Desktop copy is TRUNCATED, never touch it. dev0 = DISPLAY, never pair on it; dev2,3 = working pair.

## Board facts you must not re-derive (all verifiable at cited rows on amd/main @ 22ce0122)
- **ITEM 7 CLOSED — triple locked:** RED `1d0ff3c6`+G17x · GREEN G-AMD-33 row `db09c924` (25/25 text-identical `348e77a1222dea7f`, chair-rehashed + Gemini-reverified independently) · WIRED promote `22ce0122` (PG-1 Check (r) is a HARD gate). Carrier = cross-request epoch-number reuse in host-pinned rings (fresh request re-counted expectations from 1 ⇒ accepted prior request's stale-but-intact winner). Fixed via monotonic stamps both rings + AR reset_step completion. Family law: EVERY ring boots monotonic expectations; reset halves are documentation-only.
- Transport bug CLOSED: device atomicOr → host-mapped memory NEVER lands on this gfx900/PCIe/ROCm-6.2 box (volatile DOES, 5/5). Any status word via atomics is dead-on-arrival; printf channel is the proven witness.
- **Open, honest, un-folded datum: the steady-lag skew** — G-AMD-33's retry histogram (1,010 retries, 100% argmax-kind, median stamp 639, one-sided rank0 410-ish vs rank1 small): WHY does rank1's publish path degrade late-census? NOT folded into the carrier story; candidate = observer-dial (GATING_TRACE stderr, ARM-3 class) vs real publish degradation — the trace-OFF pair census settles it.
- Known-not-wired: kvarn k5v4 attention route = deliberate off-roster lane-remainder (`HipSources.cmake:215-218`), capability-gated at parse since `f2319798` (3/3 HIP-leg proofs). Booting k5v4 = parse refusal by design; T3's device port is its own work order.
- Banks: 13 pinned artifacts (era `8e6d79ae` = the item-7 corpus binary, recovered byte-exact; build determinism MEASURED on this box; merge-unit `0c62ffdf`, census bins `09ea36c3`/`188345f1`, final GREEN `9806bec4`… see rows).
- TP4 prep DONE: transport decision landed (RCCL-first, bring-up-only; B-1 harness merged `c799567f`-era, thresholds 120/240 µs board law); geometry converged TWO SEATS (agent3 primary + agent5 shadow, 29/29 instrument); TP4-D gates ACTIVE (routing arm check (q) red-captured, promote live); A-1/A-2/A-3 merged; window runbook `G18_WINDOW_RUNBOOK` (agent1's, with both code-gates documented) + G-CELL attach runbook staged.

## Morning queue (the plan; re-derive tips at use)
1. **A-4 PROPER** (agent4): guard-removal evidence + rb n=-field explain-or-flag + check (q) ENFORCE flip co-land → complete its pre-staged triple.
2. **trace-OFF pair census** (agent4 boot, agent3 read): K=16 bin, GATING_TRACE OFF, ×25 — dial vs degradation settles the lag skew; RED banked at G18d's 1,010.
3. **(b)-crossing policy bin** (agent3 design final, agent4 build): survive-AND-serve policy ONLY if census says retries aren't enough; capacity-aware-or-illegal-skips design note filed, both-skip/one-skip cells.
4. **K-census policy** (chair): try=7/8 at non-fatal points ⇒ is 16 the right platform bound? One-line ruling after #2.
5. **4-CARD WINDOW announcement** (chair, after #1-2): G-AMD-18 first-light + B-1 own-stamp + warm-cache pre-flight + rb-pairing live = TP4's first boot. Owner notes in runbook.

## Lane starting points (fresh sessions — pull main first, `git log` your ref, then):
- **agent4** (wo-p3-serve): G-AMD-33 release row `db09c924` was your last write; queue items 1-2 are yours; disk watch: 12 G, one build.
- **agent3** (t3-wip): three artifacts banked incl. `read_census.sh` v2 — item 2's histogram + item 3's design are yours; carrier closeout language in `db09c924` is the reference.
- **agent2** (wo-shim-funcattr): four-addendum window `22de629e`-era; your harness + probe guard every boot now; next = tp1-control stays binder-dead (product ruling = user's) + host-suite on request; your A2Q3-1 attach offer is queued for the pair census.
- **agent1** (wo-agent1-support): WO-SUPPORT-1 complete (both cells promoted hard-gate); your cells' morning usage = the pair census; window pre-flight checklist yours to run.
- **agent5** (wo-gfx900-perm): WO-TP4-C primary (converged) + `read_census` reader on main; AR-parity design doc + closure-bar audit still open; B-1 rides the window.
- **Gemini** (hub): check (q)/(r) live; A-4 enforce-flip co-land = your next; D.4 parity-gate activation bar was met by `22ce0122` for the within-boot leg — cross-boot bar wording awaits the lag-skew census.

— chair, successor desk, 22:2xZ 09-13 (morning-of 09-14). Verify at rows, not at prose; this doc ages at merge speed.
