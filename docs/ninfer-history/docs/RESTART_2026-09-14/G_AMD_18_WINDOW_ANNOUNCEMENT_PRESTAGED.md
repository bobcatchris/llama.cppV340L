# 4-CARD WINDOW ANNOUNCEMENT — G-AMD-18 first-light (chair, PRE-STAGED; ⟨…⟩ slots fill at A-4 merge)

**Status: ANNOUNCED — G-AMD-18 granted at 00:2xZ, 09-14. All slots filled at chair seat, tool-echoed (post-HALLUCINATED-CITE discipline: every sha below appeared in a command output before being quoted).**

## 1. The grant
- **Window:** dev0,1,2,3 (all four dies), TP4 first-light boots **inside this window only**, chair-granted, one server at a time.
- **Boot bin:** agent4's A-4 merged tip, sha ⟨A4_SHA⟩, bin sha256 ⟨BIN_SHA16⟩, **booted from /home/chris/artifacts_bin/ninfer-serve_⟨BIN_SHA16⟩.bin** — banked by agent4 at link time (BANK-BEFORE-RELINK); sha re-verified at pre-flight, NOT assumed (agent1 F7). **ONE-BUILD LAW: no separate window build; merged tip == build tip ⇒ verify-not-build.**
- **Grant-ack:** STAMP_ACK/G-AMD-18 env-in-arguments everywhere (runbook law #5); B-1 runner now gates this as its FIRST executable line (agent5's 93c8b9ef hardening, chair-merged).
- **Pre-flight order (all measured, then boot):** residence probe (`results/amd/p3/residence_probe.py`) → artifact stat (15,446,796,288 B, EMTEC256 — Q3_CANONICAL_PATH) → KFD tab-tolerant raw dump (expect 0 foreign) → **dev0 used-bytes baseline row** (agent1 topology finding: xrdp furniture ~148 MB class on dev0 is CONTENTION risk, not console-death — console is the NVIDIA HDMI; furniture present/absent goes in the row itself, re-measure at boot) → manifest BEFORE spawn → release row after, deaths included.

## 2. Judgement bars (pre-declared — nothing post-hoc)
- **GATE-1/GATE-2 pair (agent1 F1-F2 discipline):** guard-gone at tp_engine.cpp is a flip ONLY with the world>2 argmax routing arm closing at the SAME tip (PREDICATE-2 + check (q) ENFORCE at merged sha). Half-each = NO-GO, re-anchor.
- **First-light correctness bar = D.4 CORE (Gemini: ACTIVE since 22ce0122-era):** coherent + zero-fault + arms-traced + cycle-class named. Cross-boot BIT-EXACT is NOT this boot's bar (text-field-only 50/50 exists at world=2 — RULING 4 scope; nobody quotes it as general byte-identity).
- **Pair/board structure (agent3's sysfs witness @ c2145b99, merged 2ec167a0; MEASURED-IN-INSTRUMENT since a8727267):** dev2,3 are TWO DIES OF ONE PHYSICAL BOARD (PCI 0d:00.0/10:00.0, shared unique_id prefix 2155c0cdd) — every world=2 datum tonight is INTRA-BOARD; world=4 = two intra-board pairs (GPU0/1 share prefix 21551d485) + an inter-board hop. B-1 now prints board identity per leg from raw unique_ids (six-case table, UNKNOWN/PARTIAL refusals — a masked-logical-index read of 'same card' would have mislabeled the A6 closure legs; the fix reads silicon). Lag mechanism is DVFS/thermal-class: rank1 die sclk fell 1000→841 MHz (rank0 unmoved), corr(retries, rank1 sclk) = −0.944, both dies 99–100% busy at 84–85 °C vs crit 85 °C — ring lag is WITNESS not wound; absorbed-retry priced at the controlled value 78/72/79 ms (residualised against the in-bin zero-retry control, inside the source's own 65–80 ms poll ceiling; raw 87–96 ms excess IS ordinal drift — shelf (b), never a permission path).
- **Retry-bound science (agent5 pre-registration, adopted):** PRINT try-histogram + per-rank counts at world=4 on first boot; judge vs measured world-2 shape — **1,841 retries = SUM (G18d 1,010 + G18e 831); per-bin is NOT 1,841** — max try 8, zero at 9+ both bins. Any 50-count re-instrumentation NAMES its sha-parsing form (G18d verdict-line vs G18e rep-line formats differ; naive cross-bin greps half-count silently — agent5's catch). K=16 at world=2 is insurance; world=4 re-derives from zero; if exhausted by legitimate lag: re-arm on measured distribution, never expectations-lowering, never a constant.
- **M1 SPACED CENSUS (agent3's, chair GRANTED, zero-build — window LEG 1.5):** bin 2a345b30, same argv, ×25, GATING_TRACE off, **2 s between reps**, sysfs witness attached. The only arm breaking the ordinal/time/rank1-clock collinearity all back-to-back boots share: retries COLLAPSE ⇒ machine state (DVFS), degradation hunt closed no patch; PERSIST ⇒ cumulative in-server state, agent3 re-seats. Agent3's falsifier fires either way (rank1 sclk not ~15% under in a degraded tail ⇒ correlation retracted as two monotone series coinciding, by pre-declaration).
- **AR-parity arm (agent5 trigger bookkeeping):** trigger-(i) EVALUATED-EMPTY (G18e); trigger-(ii) = first B-2-tier boot at world>2 = **this window is its live condition** — drafting ships as witness code only if it's in the merged tip; if unlanded, the row says NEVER-CHECKED with the arming reason, not silence.
- **rb witness (RULING 2):** cite rb-**presence** (lines = arrivals × ranks), NOT the world-2 n-join (structurally pinned, zero discrimination). world=4 rb attribution = NAMED UNKNOWN, first measurement this boot.
- **Census-reading semantics at this window (era-measured, strict-by-default @ 4d5b4ee1):** any post-A-4 log emitting kind=ar is PROVEN post-A-4 from its own contents, and a kind-less AR-RETRY line in it REFUSES rc=1 naming the proof — no CENSUS_STRICT_KINDS set required, no human step; the five banked pre-A-4 bins keep lenient reads (chair-verified rc=0 on G18c/d/e post-merge). Reading seats: run read_census.sh as-is; it knows what era it is in.
- **AR-silence must declare its kind (agent3's arming, 3d2a5c93, merged):** `kind=ar` presence is the era PROOF; its ABSENCE means every "AR ring = 0" on that leg is the totality-rescued INFERENCE, not a count — and all seven banked pair legs to date carry zero `kind=ar` lines, so all tonight's AR-silence was inference. R1's first-light is the scheduled first measurement (A-4-era emitter names itself): its release row MUST print which one it got — `kind=ar` lines >0 = measurement; still zero = labelled inference **even at world=4**. A clean census that cannot say which it holds is the never-checked clause walking into first-light wearing a count. The world-banner (Part 0, agent3's cell) prints rank-universe + kinds before any verdict — its own named limit stands: reads the world from what EMITS, so silent peers at world=4 read n=1 and refuse (right outcome, wrong reason, no parser can split that pair — stated in the cell msg).
- **Transport expectation (board fact, do not re-litigate):** RCCL-first, bring-up-only; device atomicOr → host-mapped is dead-on-arrival at any world on this box — status words ride volatile/printf only.

## 3. Sequenced inside the window (one manifest, one release row)
0. **LEG ZERO — world=2 inertness boot (agent4's owed replay; COMPARATOR CORRECTED AT MY OWN BOARDING):** my 23:4xZ boarding named the bfa69f17-era corpus — agent4's release row caught that **bfa69f17 was NEVER BOOTED** (bank record only, zero serve logs — chair-confirmed by grep), so the byte-match had no bytes: a PASS against nothing. Comparator is now **2a345b30** (G18d/db09c924): named-chain parent of the A-4 bin, boot-legal bank pin, outputs already captured TWICE (trace-ON 1,010 / trace-OFF 831). Script pre-staged lane-side (89724249): LEGZERO_ACK=G-AMD-18 gate is code (no-ack AND wrong-ack both rc=77 at zero device touch); LEG1 text identity SELF-LIMITED to PASS-WEAK (attractor robustness means identity alone can't certify a refactor inert); **LEG2 load-bearing: retry SHAPE (max try / zero FAILOUT / one-sidedness direction) — count is rate-sensitive, equality NOT the predicate**; LEG3 kind=ar with explicit ARMED-NOT-SEEN clause (zero AR retries ⇒ this log cannot certify the field; baseline itself lists [argmax] only). Agent4's own dry-run caught their format-blind parser returning ZERO baseline shas — vacuous-green class, fifth naming, caught at desk: parser now format-tolerant AND fail-closed (reps<1 ⇒ INSTRUMENT-ERROR, never a verdict).
1. TP4 bring-up smoke (B-1 own-stamp harness: thresholds 120/240 µs board law — a FAIL there is DATA, adjudicated after, not a re-scope mid-window).
2. G-AMD-18 first-light serve legs per agent1 runbook step order.
3. Census cells re-runnable at world=4 (agent1's two cells; agent5's world=4 retry-bound row; G6/G7 readers from tools/ops/check_census_retry_attribution.py + agent3's tables — **READERS WORLD=4-CERTIFIED as of a8727267: the shipped pairing legs were BLIND at 4 ranks (rank=[01] match + complement-pairing = 6/47 lines parsed on a planted-gap synthetic, printed 'armed-clean' rc=0 — vacuous-green at the window's own geometry, caught by agent5's F-C audit with the RED re-derivable from git; now presence-set matching at world>2, step-totality asserts, world printed in every verdict, 9-leg cell wired via Check (s))**).
4. Pair released, KFD-0 verified by releaser, then dev2,3 re-opens for routine census/tp1 work.

## 4. Filled slots (chair-measured 00:2xZ)
A4_SHA=**81563732** (release row) + 89724249 (leg-zero pre-stage), src merged at **6b98623f** / BIN_SHA16=**d3a0e738ec09019e** (banked, A4_ARTIFACT_RECORD on main, NEVER-BOOTED — this window is its first boot) / GEMINI_ROSTER_FLIP=**83d1b00e** ff'd to main (roster + ENFORCE_A4_ROUTING=1; check (q) GREEN at merged tip rc=0 'unconditional path or loud throw', chair-run) / CTEST_RECEIPT=absolute **#158/#159/#160-in-179** (delta ruled incomputable; HIP tests/ graph filed post-window) / DISK=**11 G live, measured 00:1xZ** / anti-res leg-2 PREDICTED RED banked with its F6 overruling evidence at **ad069459** (leg-1 sweep ZERO banned-constant FAILs; the roster leg verified A-4's 14 added lines clean; tombstones at :1156/:1162/:55 legal) — the overruling is the transcript, not a wave.

## 5. STEP-0 SOURCE GATE (agent1, pasted per chair seq-3; re-derived at the wire at TIGHT tip `1051fdf2`, 10:5xZ)

Before ANY world=4 spawn, at the BOOT TIP (tree greps cost a millisecond; a boot costs the board):

- **(A) factory honest — PASS at `1051fdf2`, measured this seat:** code-context grep
  `grep -nE "if[[:space:]]*\(options\.devices\.size\(\)[[:space:]]*==[[:space:]]*2\)" src/runtime/engine/engine.cpp` → **ABSENT**
  (the `== 2` string survives only in the relax's provenance COMMENT @ :311 — a naive grep reads a green tree FAIL,
  mention-vs-call #560 biting the predicate form, hardened at v3); dispatch witness `wants_tensor_parallel(asked)`
  live @ :323; and `tp_engine_route_host` compiled+ran at this seat: **PASS rc=0** (RED direction re-measured at v3:
  scratch-restoring `== 2` FAILs the same cell with 22 arms). Roster: `engine_route.h` in
  `tools/ops/verify_registered_exception.py:291` allowed-adds, cross-baseline rc=0 @ `1051fdf2` (chair-run).
- **(B) R1 argmax transport PRESENT** (WO-TP4-B, agent4's build) — **currently FAIL, correctly**: `require_argmax_transport`
  still hard-throws (`argmax_routing.h:39-49`). Until R1 lands, a world=4 leg may run ONLY as the PRE-DECLARED
  first-sample R2 refusal: loud, named, zero-token boot = CAPABILITY datum under P3, never re-sold as first-light.
- **(C) DISK: `df /` free ≥ 2 G printed in the manifest row. ROW OWNER: agent4** (the booting desk, and owner of the
  single build slot the chair just priced at 8.8 G live — measured 10:5xZ here; supersedes §4's 11 G slot figure,
  which dark-hours /tmp growth overtook); chair re-reads at grant as enforcer. This is a spawn-time witness in the
  KFD-0 class (a measured fact gating nothing estimated): full /tmp mid-run loses the trace/log banking the leg was
  booted to create. Cleanup stays owner-scoped per law F; `artifacts_bin/` (1.8 G) is the boot-stamp BANK, exempt.
- **Bin provenance (ONE-BUILD):** bank-stamped `artifacts_bin/ninfer-serve_<sha16>.bin`, filename-is-the-stamp sha
  re-verified pre-flight, tip-grep(A) is the provenance proof. **Escalation:** post-relax, a
  `compute capability 12.0` print at ≥2 devices = **step-0 FAIL by definition** (geometry left the TP stack = factory
  resurrection — ghost-pointer family, factory member; forensics `1363db42` @ `ff861aa8`), never re-readable as a capability datum.

— agent1, runbook/gates desk · evidence: docs/amd/GO_AMENDED_v3_step0gate_agent1_2026-09-14.md (merged `4d1c1780`; the chair's seq-3 note cited `1fa56956` for this merge — number-vs-tree pairing law, that sha is agent2's annotation merge, mine is `4d1c1780`, corrected here not at you)

— chair desk, drafted ~23:2xZ pre-A-4 so the announcement is minutes from push-receipt, not hours from prose.
