# REBOOT KIT — agent4 (P3 serve lane). Fresh session, 01:0xZ. Your persistent debrief: results/amd/p3/agent4_DEBRIEF_2026-09-13-01Z.md on amd/wo-p3-serve @ e143e49f — READ THAT FIRST, this kit is the delta since it was written.
## STATE DELTA (new facts since your debrief):
- agent3's 0x8000 diagnosis IN (yours was the trace; theirs the mechanism: un-migrated smem_addr tokens under the (A) contract, silent widening, LDS-base zero-extend — coord-verified at bytes). Their sweep = one atomic t3-wip commit; guard makes every un-migrated site REFUSE compile. When it lands I merge; YOUR first move becomes: re-verify your 4 residuals' ancestry vs new main (your debrief's own first-move rule — kernels.cu Stages-line is load-bearing, verify it survived the merge), then 17e re-fire (PRE-AGREED terms, no re-ask; per-truth budget; your recipe: ws=96, trace envs, ctx64/kv64/chunk128/no-prefix/prefixcap256/no-cuda-graph/greedy; capacity physics: ws<=112 hard).
- PLAN committed 94ba02c2: TP2-finish checklist (Phase 1: token -> dladdr routing map arm_routing_measured.md -> host-9-tests GENERATE re-check (the two tests-doors: your 10e4a7cf + whatever merge state — reconcile before running; four-count reporting incl SKIP-77) -> tp1-control on ref (NOTE-branch expected, fresh stamp from me) -> B4) THEN TP4 delta-map (Phase 2 — your serve-lane interest: the AR/transport bring-up numbers are yours, staged no-P2P at 3.13 GiB/s).
- Registration: closed; your 4 unregistered residuals stay boot-green-then-merge BY DESIGN, warrants named in their commits (step-0 flags them on purpose — the tp_engine one is MY adjudicated carry, name it in your next merge msg, never 'fix' it toward NVIDIA main).
- Agent5 retracted the worktree-move cause story (frozen refs protected BY GIT; canonical-enforcement fix = gate:324 FORWARDING, not a default) — affects nothing you do; the w8 LDS numbers (66560=1 row, 67584=2 rows, 3 splitk ceiling-free) still yours for the post-token wave.
## FIRST FIVE MOVES: (1) your debrief file; (2) fetch — wait for/verify agent3's sweep merge; (3) residuals-ancestry check; (4) 17e under agreed terms (cards: dev0,1, pre-check zero-KFD, own-pid, G17e rows); (5) whatever 17e says, release row + report — the coherence reading law is yours (margin-stable argmax = coherent).

## LAWS (all lanes; tonight's distilled — operate under these)
verify-before-rule; read matched lines not totals; claimed!=proven (freshness by sha; receipts must NAME their artifact); measure the real file over reproducing it; assert test-artifact existence before believing output; NEVER discard a mutation's stderr — assert the state you intended to CAUSE; re-verification must vary the INSTRUMENT (self-consistency is not independence); re-derive at point of use THROUGH THE EDGE not the constant; no line-numbers in specs (refs + content-hashes; digits = instrument output tagged by tree-sha); name the BASELINE of every gate check; three-state exit 0/4/5, never 0-on-blindness; bounded listings prove nothing about absence (use predicates); retraction names the scripts that still emit it; frozen refs protected by git — plumbing for read-only cross-ref checks; numbers ship with counting-rule, claims with testable-contract, labels with referent; widening scope IS the test; VRAM LAW (allocator is the gate; measured anchors only); written grant before device, ONE BOOT PER TRUTH, release rows; own-pid kills only; df -h before big builds (25 G on /, ~20 G is the ref artifact — do not delete, do not re-hash); artifacts: q3 = /media/chris/EMTEC256 (canonical), ref = repo artifacts/, both VERIFIED; merge main INTO amd/main one-directionally; docs under docs/amd/; STOP-ask > silent workaround; identity claims cite joined form (hub+pi+claim-file); a contract change needs a compile-refusing mechanism, never a remembered list.
Cards: dev0,1 = boot pair; dev2 = window card; dev3 spare; all idle 01:0xZ, zero KFD.
