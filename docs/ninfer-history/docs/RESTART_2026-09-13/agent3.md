# RESTART KIT — agent3 (T3: kernels, parity, capture, oracles). Fresh session, 01:0xZ.
## MISSION NOW (the night's last sentence): the (A)-contract sweep.
Your 0x8000 diagnosis COORD-VERIFIED at bytes (smem_addr->unsigned at memory.cuh:32; ldm_addr_t at mma.cuh:16; q5 .cuh:360 passes one to the other; LDS-base zero-extend = the 0x8000 class EXACTLY) and AUTHORIZED: author GUARD (deleted unsigned-overloads in mma.cuh) + NINFER_LDM_ADDR sweep at every un-migrated site + FRAGCELL extensions (512-thread multi-warp leg; deleted-overload NEGATIVE cell) as ONE atomic commit on amd/t3-wip. The guard-compile IS your enumerator — let it list the truth (CUDA-only-guarded sites reveal themselves; your worklist + the compile, never memory). Report: per-file diff-stat, guard-compile full error sweep pasted, deletion-count ZERO outside your files (single-writer respected — if the sweep wants to touch kernels.cu/plan.cpp, STOP and route to agent4's lane; your residuals-check said those are theirs).
## LANDED FACTS (do NOT re-derive — refs given)
- Parity CLOSED both families at ruled bars: decode 12/12 min 0.999757; prefill 8/8x12/12 min 0.999702; outliers 19/24576 = razor-edge class (G-AMD-24 merged-tree re-run: digit-for-digit, 0b2130a8/698360be). B1/B2/B3 PASS, K1/K2 bit-exact, (A) closed-for-gating; bars: cos+role-count, 2-ULP guard retired to ratio<0.85 outlier detector.
- Eight instruments found tonight, ALL yours-adjacent, ALL closed: readback shift, canary byte-vs-slot, observer-erase, input-prep f32-as-bf16, out_f shadow, fopen-crash regen, grep-c link short-circuit... the stride-4 ghost was INPUT PREP — every product kernel was innocent from the start. Your debrief §5 addenda 1-5 tell the full chain.
- B4: NOT a redesign (your own retraction, 5254d1d5) — one normal dev2 window when scheduled; NOT NOW (chunked unserved at current caps). FP32-acc-plane: named, unscheduled, STOP-ASK class.
- Registration package CLOSED (28-entry array incl your 4 + re-pinned mma; caveat retracted at bytes — the relocation hazard was inert; content-hash 86dcd694 at 4d32956a).
- b2dd680c lineage (agent4 tree) is the sanctioned repro base: main ships Stages 0,4 which CANNOT launch on this box — your sweep lands t3-wip->main, then their branch re-merge + 17e re-fire produces the token attempt, not you.
## FIRST FIVE MOVES: (1) read your §5 addenda + this kit; (2) guard-compile enumerate at current t3-wip tip (fetch, merge main in — 94ba02c2+ carries the PLAN doc); (3) sweep+extensions atomic commit; (4) report with the three receipts above; (5) offer the deleted-overload negative-cell result to gemini's follow-up queue as a PERMANENT gate class (contract-drift tripwire — their design call).

## LAWS (all lanes; tonight's distilled — operate under these)
verify-before-rule; read matched lines not totals; claimed!=proven (freshness by sha; receipts must NAME their artifact); measure the real file over reproducing it; assert test-artifact existence before believing output; NEVER discard a mutation's stderr — assert the state you intended to CAUSE; re-verification must vary the INSTRUMENT (self-consistency is not independence); re-derive at point of use THROUGH THE EDGE not the constant; no line-numbers in specs (refs + content-hashes; digits = instrument output tagged by tree-sha); name the BASELINE of every gate check; three-state exit 0/4/5, never 0-on-blindness; bounded listings prove nothing about absence (use predicates); retraction names the scripts that still emit it; frozen refs protected by git — plumbing for read-only cross-ref checks; numbers ship with counting-rule, claims with testable-contract, labels with referent; widening scope IS the test; VRAM LAW (allocator is the gate; measured anchors only); written grant before device, ONE BOOT PER TRUTH, release rows; own-pid kills only; df -h before big builds (25 G on /, ~20 G is the ref artifact — do not delete, do not re-hash); artifacts: q3 = /media/chris/EMTEC256 (canonical), ref = repo artifacts/, both VERIFIED; merge main INTO amd/main one-directionally; docs under docs/amd/; STOP-ask > silent workaround; identity claims cite joined form (hub+pi+claim-file); a contract change needs a compile-refusing mechanism, never a remembered list.
Cards: dev0,1 = boot pair; dev2 = window card; dev3 spare; all idle 01:0xZ, zero KFD.
