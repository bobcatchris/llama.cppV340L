# RESTART KIT — agent5 (permanence lane). Fresh session, 01:0xZ. Your in-channel debrief (tip 44985a9f) + its two retractions (§20/§21) ARE the handoff — the retraction pair is the part most likely to be mis-cited: 13-vs-tip masking claim was YOUR stale script-copy's number (self-consistency != independent confirmation), and the worktree-move story was impossible (git blocks it; frozen refs protected by git, not care; replacement law: never discard mutation-stderr, assert the caused state).
## SINCE YOUR DEBRIEF: agent3's 0x8000 sweep is landing on t3-wip (the contract-drift guard is your §19 thesis made permanent — a compile-refusing mechanism where a remembered list failed); PLAN committed (94ba02c2): TP2-finish then TP4-delta-map. YOUR HOOKS, ranked: (1) the deleted-overload NEGATIVE cell as a permanent gate class for gemini (contract-drift tripwire — they'll ask for a spec when the sweep lands); (2) TP4 Phase-2: the world==2 inventory IS your -E -M closure machinery (blocker-question #1, artifact-shape, tp_place_capacity, AR rank-arithmetic, 129-constant TP-degree check — every one a call-graph reachability question, which is your tool); (3) store-width-vs-declared-dtype analyzer (post-wave #4 — the 0x8000 saga just made it two findings more urgent); (4) seed corpus for gemini's Stage-0.5 (one word away); (5) your tier-3 fix for check-(j) reachability blindness.
## CITE-REFRESH BEFORE USE (tonight's law, applied to this kit): every sha above moves; the PLAN doc names what was true at 01:0xZ, the gate says what is true now.

## LAWS (all lanes; tonight's distilled — operate under these)
verify-before-rule; read matched lines not totals; claimed!=proven (freshness by sha; receipts must NAME their artifact); measure the real file over reproducing it; assert test-artifact existence before believing output; NEVER discard a mutation's stderr — assert the state you intended to CAUSE; re-verification must vary the INSTRUMENT (self-consistency is not independence); re-derive at point of use THROUGH THE EDGE not the constant; no line-numbers in specs (refs + content-hashes; digits = instrument output tagged by tree-sha); name the BASELINE of every gate check; three-state exit 0/4/5, never 0-on-blindness; bounded listings prove nothing about absence (use predicates); retraction names the scripts that still emit it; frozen refs protected by git — plumbing for read-only cross-ref checks; numbers ship with counting-rule, claims with testable-contract, labels with referent; widening scope IS the test; VRAM LAW (allocator is the gate; measured anchors only); written grant before device, ONE BOOT PER TRUTH, release rows; own-pid kills only; df -h before big builds (25 G on /, ~20 G is the ref artifact — do not delete, do not re-hash); artifacts: q3 = /media/chris/EMTEC256 (canonical), ref = repo artifacts/, both VERIFIED; merge main INTO amd/main one-directionally; docs under docs/amd/; STOP-ask > silent workaround; identity claims cite joined form (hub+pi+claim-file); a contract change needs a compile-refusing mechanism, never a remembered list.
Cards: dev0,1 = boot pair; dev2 = window card; dev3 spare; all idle 01:0xZ, zero KFD.
