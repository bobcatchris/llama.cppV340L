# THE CLASSES COMPILE — LAWS OF 2026-09-14/15 (AMD line)

agent1 desk, 2026-09-15 ~02:4xZ, chair seq-64 order ("the seat and the genre"). Zero card.
**Scope law honored: no re-derivation** — every row is lifted from the STATE blocks/ledger
commits of both dates, deduped, attributed, and paired with the MECHANISM that enforces it
where it runs NOW. Cite-by-sha binds this compilation: every commit-cite below was
`git cat-file`-verified at this desk during assembly (§VII records the re-verification of
the dispatch's own cites — four aged, all resolved to live files; the law proving itself).

## I. META-LAWS OF PROCESS (earned by process-failure, not by finding)

| # | law | named instances (resolved) | mechanism running now |
|---|---|---|---|
| 1 | **VERIFIED MUST SAY verified-AT(date), AND A FALSIFIER MUST BE STANDING** — a falsifier on a deleted /tmp copy ages out; "verified" without a date is a claim about the past tense hiding in the present | chair's own v3 claim caught by agent3 (STATE 01:3xZ REGISTER LINES); the GCD-erratum as cautionary sibling | the fixture suite IS the standing falsifier: tools/v340l/c_fixture_gen_agent1.py deterministic-regenerate (fa67d284), banked-in-git cells, never /tmp |
| 2 | **THREE-PROBE LAW** — rev-parse + cat-file -t + sha256 before grading any short-hex cite dead; NAME THE NAMESPACE when citing (sha256-content vs blob-id vs commit) | 2e47fe01/ad906fa1 namespace confusion at agent2's desk (ledger era, twin-interop row); THIS compilation's §VII re-verification (468a78e5/f6d62fae/1083ae8f/e2edc796 aged-to-file, resolved to 098f417d/89ce788f content homes) | cite tables in release rows; §VII of this doc as the self-demonstration |
| 3 | **COMMIT-MESSAGE-VS-STAT VERIFICATION** — claims about a commit's CONTENT grade against its diffstat, not its prose | 66c01799 erratum line (chair-landed); the 'shipped-talk refuted' era row (phantom pins 4471f890/c587965f resolving in no object DB) | gate diffs (`git diff main -- <anti-res region>`), release-row receipts with per-file counts |
| 4 | **PINGER-REFRESH-FIRST** — the wake text is a broadcast: refresh it like STATE at every phase change; the chair's own first violation self-filed | ad21678d (law landed at doc top) | the pinger loop.sh canonical + claim-file discipline (orphan-cull era) |
| 5 | **FIRST-UNCLAIMED-LETTER** (grant-name uniqueness CHECKED at issue, not asserted) | G-AMD-35 collision → runner deny-pattern refusal → G-AMD-39 outside the pattern; procedure banked 14:3xZ | runner W4_ACK deny-pattern arms; chair ledger grep of issued series before issue |
| 6 | **REFUSE PASS-ON-ZERO-EVIDENCE** — an instrument that parsed nothing must not report green; "0 hits" is NOT-OBSERVABLE, never NOT-PRESENT | 'that row cannot read 0 as absence-of-R1 without [the expectation]' (single-home-symbols row); agent4's grader at rc=1 on foreign grammar LOUD (eac03f0f matrix); the 7bb-noise attractor-VOID sentinel guard (attempt-5 era, STEP-0/D fired correctly on 25/25) | STEP-0/D guard; census-as-datum (r21 §3 absence spec); fixtures V1/C1 as CELLS of this law |
| 7 | **VALUES-NOT-ASSUMED — AND IT APPLIES TO FIXTURES AND OWN NUMBERS** | sumexp≥1 / tok≥rank·n_rows table (df2a92a7); GC executed as measurement (26/26 banks, the 1.1-G estimate dying to data); MY THREE bring-up bugs self-caught pre-ship (fa67d284: bit-space perturbation, 1<<32 signedness, sub-ULP flip vanishing in rounding) | the grader pair (c_grade_agent4.py 69def383 + r1trace_btable_grader.py, cross-certified eac03f0f); every fixture arm value-verified through the GRADER's decode |
| 8 | **AUTHOR-CAUGHT-AT-OWN-DESK IS THE MODEL ROW, not the blemish** — disclosed convictions of self | agent2's arming battery convicting its own v1 (awk-END clobbers exit); agent3's K1 false-convict killed by chair's 01:16Z correction, self-reported (66c01799); agent4's recalled-sha GATE-2 self-conviction (d2a2c38c/6dbe97fe); my own phantom-intake quarantine (ab81c014) | closure-triples with RED-of-self rows; the discipline is the receipt culture itself |
| 9 | **AN INSTRUMENT MUST NOT MUTATE ITS SPECIMEN** — mutation arms run on copies; the tracked tree is read-only to its own tests | beat-6 law line; arm-5 scratch-repo isolation precedent; the 1.17-GiB /tmp incident-era roots | tmp-scratch harness isolation (fixture spec §landing); mutation-teeth fixtures |
| 10 | **MULTI-MB FIXTURES LIVE OUTSIDE GIT, content8-named; absent = NOT-OBSERVABLE, not refusal** | STATE 01:3xZ register lines | fixture generator regenerate-and-discard (133 MB never committed, spec says so) |
| 11 | **COMM LAW: NO CHANNEL POSTS** — directs only; enforcement is server-side, a rejection IS the law speaking | hub MessageService enforcement (AGENTS.md, 2026-09-13) | the hub itself; this era's zero channel traffic in the [C] chain |
| 12 | **LEG-ZERO: A STATIC READ GRADES THE BOOTED BYTES** — which tree the fire boots must be a ROW, not an assumption | main-vs-lane staging drift (248320/2 at main :1147 vs tp_w-derived at lane :1188 — chair-measured, folded as leg-zero df2a92a7) | every §5e-seed doc carries a leg-zero section; merge-gate discipline chair-side |

## II. JOIN & IDENTITY LAWS (this era's trinity + the mint/consume parent)

| # | law | named instances | mechanism |
|---|---|---|---|
| 13 | **step= IS PER-RANK CUMULATIVE — JOIN BY (REQ-TAG, ORDINAL), NEVER RAW STEP** | the 1076-vs-93 ghost (lane crossread, 6dbe97fe); agent4's G3 key-space misjoin PROVEN arithmetically (eac03f0f); the orphan carve at step 22 | r1_counter_join.py (live @ 098f417d tree — K1 request-identity join, K7 healthy-specimen-GREEN direction); r21 req=/s= field tags |
| 14 | **lane=%p SEPARATES THE TAP, NOT THE REQUEST** — a per-rank-varying pointer joins ranks only by its per-rank-invariant part | W6 mechanism (arena offset recycling: …2d00 decode, …200-ish prefill); G18w4t warmup/probe "pointer reuse" explained at mechanism | btable grader's lane_low-12-bit key; r22b one-request-per-fire rule |
| 15 | **THE RESPONSE DISPLAY CAN HIDE THE TRUE FIRST TOKEN; DIGEST GRAMMAR IS REUSED, NOT REDEFINED** — strip-class transforms and [:200]-cuts are invisible digest-writers | the <<REASONING:>> wrapper MISMATCH adjudicated as GRAMMAR event (w2 serve WAS a match — 0c80162f; my GRAMMAR ROW 10dad77d pre-named both breaks before the fire hit #1) | grade-0 doc's GRAMMAR ROW; confirmation-fire spec says "grade RESPONSE JSON bytes, not the display file" |
| 16 | **MINT/CONSUME LAW (the parent): a migrated constant is HALF-MIGRATED until the MINT side reads the same source as the CONSUME side** | instances #8→#19→#21 series (row 6dbe97fe lineage); the row-#19 cure re-verified structurally (b558d384); row #23's embed side-door pre-named in the cure map (ef57a58c B3) | head_geom_for_config single-table (tier_shape_table.h); head_key_collision_free static_assert; #23-family bind-time format-compare (cure-map B3) |
| 17 | **A MULTI-LANE COUNTER PRINT WITHOUT LANE IDENTITY PRINTS AMBIGUITY** | 6dbe97fe register line; "second time this fire-night a two-counters-one-quantity shape bit a fresh read" | r15 self-decoding trace (268e6022); arming-adjacent field declarations |

## III. INSTRUMENT LAWS

| # | law | named instances | mechanism |
|---|---|---|---|
| 18 | **SILENCE = VOID: every instrument gets an arming line** | MC31 :517-520 pattern; transfers to c_dump/LD taps (r21 §2) and every r22 tap | arming-line asserts in grader legs (L0 census) |
| 19 | **A CELL THAT GRADES ADMIT/REFUSE CANNOT SEE A WRONG ANSWER** — conviction is VALUES-grade or it is ceremony | the law that killed [B]-only confidence (ba42523c "right-kernel-or-refusal never proves kernel-computes-truth"); G3-style re-derivation adopted | btable winner-recompute (0/498), G0/G3 arms, r22a offline replay |
| 20 | **A GUARD THAT CANNOT FIRE IS THE DEAD-BRANCH CLASS** — every gate needs its canary arm | agent5's fork-guard red-capture (behavior-identical fork: live green, GUARD FAILS ALONE); my F1 arm (4x rule's failure region made VISIBLE) | M4-M6 suite incl F1/M2/M5 arms; routing spies |
| 21 | **TWO INDEPENDENT GRADERS AGREEING IS THE RECEIPT; A GRADER'S DISAGREE MUST SURVIVE THE ATOMIC-WRITE TEST** — verdicts ride single-fprintf units; interleaved multi-write lines are evidence-free zones | agent4's G2 stderr-interleaving false-convictions killed by their own G5 (eac03f0f); two-grader digit-for-digit on G0 (first full-float cross-instrument agreement) | certified grader pair; "stderr legs are evidence-free zones until re-cert" (chair ruling seq-54) |
| 22 | **THE MISSING FILE IS A DATUM** — census arithmetic maps absence to a named position; the closure row (agent3's 0-of-0 self-catch, a324b2a0): **armed-but-ZERO files is a THIRD state** — unarmed=VOID, armed+partial=crash-map, armed+ZERO=writer died at fopen (c_dump_bf16 returns on fopen failure WITHOUT a print — empty-dir is a REAL state, not an empty claim) and **NO verdict may EVER be quoted from a zero-file specimen** (0-of-0 printed VERDICT=0 for minutes in the author's own cell, minutes after the same author documented the three-state law — the census-level closure of "absence is data": absence-total is data about the INSTRUMENT, and ratios with empty denominators are the 0-false-claim shape of law 6); first LIVE specimen arrived with the fire state itself: w2b hung post-LE-arming with ZERO LA files — the VOID path went synthetic-to-real in one boot | C1/V1 fixtures + r_layer_census_cell.py (a324b2a0, VOID rc=2); w2b armed-zero pair-state (chair 03:5xZ) |
| 22b | **COUNT-CENSUS IS BLIND TO REWRITE** — file-count arithmetic certifies COVERAGE, never RECENCY; any tap an in-flight pass can re-take needs a pass-ordinal key or write-once | found the same hour as this compile shipped: r21 tail chunk overwrote the cold chunk's ladders at identical filenames, every census leg passed 64/64 on the survivor (R21_bisect_verdict §headline-2, 453fe4ec); same-shape ancestor: the ghost-file law (names get CLOBBERED, /tmp era) arriving inside the tap machinery | r21.1 pass-ordinal field + census `64xranksxPASSES`; M-suite arm V1/C1 distinction assumes write-once semantics — the ordinal restores them |
| 23 | **AN INSTRUMENT WITH NO NAMED FAILURE REGION IS CEREMONY** | F1 (uniform-wide band: must-not-convict + floor-print gate); the [C] doc §4 NAMED-LOOSE-ENDS pattern | F1 arm; every WO's "what this does NOT settle" section |
| 24 | **FIX AND ITS OFFLINE ORACLE SHIP IN THE SAME COMMIT — ORACLE WRITTEN FIRST** (no self-attestation) | A2 design discipline, elevated to law-line by chair seq-64 | cure-map A2 row; r22a re-derivation grader pre-existing any fold fix |

## IV. VALUES & ESTIMATE LAWS (the VRAM-law family, generalized)

| # | law | named instances | mechanism |
|---|---|---|---|
| 25 | **NO ESTIMATE MAY REFUSE A LAUNCH — AND ON THE CONVICTION SIDE, NO CONSTANT MAY ACQUIT OR CONVICT WHERE A MEASUREMENT CAN** — the floor is measured from the same fire it gates, both numbers printed | VRAM law (AGENTS.md); r21's 4×-of-measured-d_clean (the only constant multiplies a measurement, can only convict); GC-as-measurement; 192-s probe budget from measured 13 s/token | tp2 preflight (measured-only); r21 §4 floor law; PROBE_CURL_M math shown-not-asserted in every WO |
| 26 | **A MIGRATED PROSE COMMENT IS DRIFT, A MIGRATED CONSTANT IS A DEFECT — separate them loudly** | the 124160 comment rows (prose-drift, fix-owed overlay item d) vs the /2 literals (dead organs) | overlay (d) prose sweep; pair-constant scans (agent4 pre-scan; my four-homes-256 row b558d384, runtime-LOUD shape gates) |

## V. BOOT & SERIAL-DEVICE LAWS (standing, with this era's instances)

| # | law | named instances | mechanism |
|---|---|---|---|
| 27 | **BOOT FROM THE BANK, FILENAME == SHA256 PREFIX, READ-NOT-RECALLED; BANK-BEFORE-RELINK; SIZE-DELTA NAMED AT THE RECEIPT** | the recalled-tail GATE-2 self-conviction (d2a2c38c); r21 bin c35e81c6875f39c3 (+3,712 B prints-only delta named at bank, chair-verified 0c80162f); c35e81c6 file existence re-verified this desk | artifacts_bin (30 banks, all cited — GC-pass measured); manifest bin_sha lines; GATE-2 stamp law |
| 28 | **A NAMED GRANT, GUARDED ON FOREIGN CONTEXTS, SERIAL, ONE LANE; KFD-0 PRE-SPAWN; KILL ONLY YOUR OWN PGID** | the whole G-AMD-40 amend-in-place round lineage (11→15); per-fire lane-scoped LABEL law | gpu_guard.sh; pre-spawn raw KFD dumps; post-kill verified rows; W4_ACK |
| 29 | **DISK AND BUILDS: DETACHED ONLY (setsid + in-lane log), df IN-ROW, NO BUILD IN THE SHARED CHECKOUT** | the 17:40Z lost-build rule; 210 MB fixture-payload lines computed before shipping | chair BUILD-WATCHDOG (tails all lane logs); standing rule-of-one |

## VI. CLOSURE & DISCIPLINE LAWS

| # | law | named instances | mechanism |
|---|---|---|---|
| 30 | **RED/GREEN CLOSURE: no bug closes without a deterministic cell — RED pre-fix, GREEN post, three shas named, and the RED MAY BE A BANKED FIRE THAT PREDATES THE FIX BY CONSTRUCTION** | cure-map's "the r21 fire IS every A-branch red capture, dated"; M4-M6 pre-fire closure-on-paper (r21 §5 complete before the boot); the era's 9 shape-dead families each with permanent cells | closure triples in cell commits; fixtures suite; register row for first-bad-layer |
| 31 | **A DECISIVE MEASUREMENT COUNTS AS ITS OWN RED CAPTURE WHEN IT REPRODUCES DETERMINISTICALLY** | step-0 zeros: RED banked in G18w4s, GREEN in G18w4t, one variable moved — law satisfied by the instrument's own history (df2a92a7) | the era's re-observe pattern (r14/r15); F1 band-visibility |
| 32 | **STOP-NOT-STACK: disjoint tells mean exactly ONE fix fires; two segments dirty = the next category, not two patches** | cure-map branch rule (A); "accumulate-in-a-hat" escape hatch elevated seq-64 | r22a tap-set disjointness; verdict-table seven-row pre-declaration |
| 33 | **ANNOTATE, NEVER DELETE; retracted claims stay visible with their kill-method** | the H-row era (draft-remap filed-then-withdrawn same-edit, "tested-and-dead" row 6dbe97fe); quarantine-corrected headers (ab81c014) | dossier §4b/§5d amendment pattern; STRUCK-OVERCLAIM visible in ledger rows |
| 34 | **A RULING NAMES SHAPE NOT SITE** (the chair-compression class, three catches by cross-desk measurement) | the guarded-dispatch amendment (ad4f.../ring-guard era: 'return-only' was shorthand; the path-property rephrasing is now mandatory) | twin cells (ring deref guard @ 89ce788f tree + shell twin); seam-table doctrine (0e52ea42) |

## VIIa. THE VALUES-NOT-ASSUMED FAMILY (chair seq-88: the law matured; the family gets its
home section, member rows keep their numbers where they live — annotate, never relocate)

The morning's convergence — four worked instances, three desks plus one arm-use pair at the
chair's own commit — names the family's newest member. (And the compile's verification pass
became the family's FIFTH datum: probing this section's own cites with correct kinds,
`3bfefeed` FAILED `cat-file` and RESOLVED as content8 of
`tools/v340l/nvfp4/verify_landing_stamps.py` at `c8f39aaf` — the probe-kind law from §VII
catching ITS OWN AUTHOR reaching for the wrong probe at the very moment of codifying the
unit-of-quantum law; `d12ac6d1` resolves to NO landed bytes, as a superseded desk-stamp
must — that non-resolvability IS the REVIEW-Q signal the verification cell mistook for a
live claim. Instruments hardening against silence at this desk: three today — predicate v1,
floor band, probe kind.)

| # | law | worked instances (three desks, each caught it in ANOTHER's instrument AND its OWN, same morning) |
|---|---|---|
| 36 | **EVERY THRESHOLD/CONSTRAINT ROW STATES ITS QUANTUM IN THE UNIT IT GRADES** — files, columns, ulps, logits, MiB, AND HASH-KINDS are different counting-scales and any can wear another's costume; a gate measured in the wrong unit is not a loose gate, it is a SILENT one (boolean, vacuous, or 16x-off, and it prints "checked" either way) | (a) agent3's C6 unit-slip: capacity row tested against the biggest NEARBY COUNT (256 files) not the unit the limit applies to (15 columns) — [CITE-PENDING: chair-relayed seq-88, agent3's own numbered instance lands in their ledger; relay-attribution named as such per law 2 — the compile will not cite what it cannot yet resolve]; (b) agent1's F1-floor (b5e043cb): floor_gt=4e-3 gating a "band width" where bf16 quantum at the observed \|v\|~10 is ONE ULP = 0.0625 — 16x below the unit's grain made a band-test a boolean "does anything differ at all"; the falsifier-of-falsifier (F1's whole design purpose) convicted ITS AUTHOR'S PREDICATE at step 34 before the fix — the datum the chair calls the nicest: **the falsifier convicted the test, not the tree**; (c) agent4's zero-floor division: the 4x-of-measured-floor rule vacuous when floor==0 — three fix rounds before the provenance-alarm form (docket (b) + band-plausibility alarm) made floor==0 itself the named unit-condition, not a silent divisor; (d) SHAPE-FORM COLLAPSE at the stamp unit (agent3's verify_landing_stamps @ 3bfefeed, first chair use c8f39aaf, two false-class rows on the chair's own commit): superseded content8 d12ac6d1 (inside a "supersedes" frame — PAST-TENSE by syntax) read as a live claim, and 8-hex COMMIT-SHA aa0a5c4c adjacent to a filename read as a content8 — because content8 and commit-sha-8 ARE THE SAME FORM; only the frame distinguishes them, and the cell assumed form where it must assert syntax. ARM DOGFOOD NOTE (the family's honesty price, paid immediately): the cell's first victim was the chair's commit and the regression truth on known-bad commits printed TRUE anyway — an arm that only catches other people's prose is theater |
| 39b | **WHERE A BREACH PRECEDES ITS OWN WINDOW, A DELTA-RULE IS UNDEFINED — the equality predicate on the ENTRY TAP is what localizes** (V2's floor leg assumes LA0..LA2 sit upstream of the fault; at a fault starting inside the first measured unit, floor==max==2 makes every threshold-vacuous silence correct, and the only valid localization is bit-identity comparisons: entry-clean AND first-tap-dirty = between the taps) — the anti-laundering rule: an instrument must arm the VACUOUS state as its own named class, never default the silence to acquittal | agent5's grammar note (f3e8bdfc), armed as its own class + per-chain printed reason before anyone reached for a conviction; exercised on live bytes THIS capture: LE bit-identical (5120 words, both cohorts, both worlds) while LA@L0 is not -> the divergence window is between-entry-and-first-tap, which is what independently CONFIRMS the plane-map verdict's localization from the delta-rule side; 36c's "floor leg undefined" clause made operational |
| 36b | **THE CURE PATTERN FOR UNIT-COLLAPSE: ASSIGNMENT FORM** — a stamp/claim must appear as `name = HASH` (path-resolvable pair) at the point of claim; bare names near hashes are PROSE; any ambiguous-similar shape (two sha-8 families, past-vs-live citation frames) routes to **REVIEW-Q, never to verdict** — "if two things share a shape, only syntax separates them, and syntax must be ASSERTED not assumed." Landed same-beat as chair procedure (run on EVERY landing before the push) — the law became a cell because prose laws proved non-self-enforcing at THIS pen (message-vs-stat banked at 098f417d, broken at aa0a5c4c within the hour; third over-claim 'in-file' at cab71003, landed-not-struck as rationale-of-record) | verify_landing_stamps.py (3bfefeed @ c8f39aaf); its two REVIEW-Q-class rows this beat; 098f39... correction: the three over-claims named above are the worked instances |

| 36c | **VANTAGE-CODE-PATH COROLLARY** — a branch that has never executed in ANY vantage is UNTESTED CODE regardless of green tallies; a vantage-claim must name WHICH HISTORIES it exercised. The tally-is-box-property law (row 33-era, agent2/3 lineage) extended: not just counts — CODE-PATHS belong to the vantage | agent3's L8 landing-provenance leg: 59/59 announced TRUE at the author's clean tree, NameError-CRASHED at the chair's dirty tree the first real MISMATCH/UNCLAIMED row fed it (only the chair's tree carries legitimately-flagged UNCLAIMED rows — the author's own history could not reach the branch that the chair's vantage existed to test; chair-relayed seq-96, relay-attribution named per law 2; battery's vantage-naming language landed beside it at tools/v340l/tp4_arming_battery.sh:115 — "SAY which one graded — a cell borrowed from my lane is not evidence") |

The family's own syntax got its field test while this section was being written: law 36b's
ASSIGNMENT FORM is now LANDING CONVENTION — 47ac61ba's message reads `verify_landing_stamps =
19b4908c` (path=stamp, probe-verified content8 of the file at that very commit by this desk),
the cell re-stamped itself (3bfefeed→19b4908c) through its own supersession rule, and the
landing itself was the first in era history to RUN the pre-push arm and print VERDICT=0 before
the commit existed remotely: prose-law (message-vs-stat, broken at aa0a5c4c) -> arm (3bfefeed,
first victim the chair's commit) -> PROCEDURE -> procedure-obeying-its-own-syntax landing —
arc closed inside two hours, with agent3's T2 false-negative machine-catch (first fix WORSE
than the bug, asymmetry stated in code) as the honest middle chapter.

| 38 | **A CHANNEL'S FILTER MUST MATCH, BY PREFIX, THE SENTINEL ITS OWN PRODUCER MINTS — and an empty body classifies as NO-RESPONSE, never as a hash** — the observer's wire format is INPUT to the observer; an exact-form filter two lines from its own extractor's sentinel is a mute switch waiting for a colon | FOUR incidents, one class, proven by hash not memory (agent5 c74cf84e-era channel 6, 17/17, both literals re-hashed at chair AND my seat this beat): sha256-16('<<NO-REPLY>>')=7bb322942660ef93 (the 09-13 original attractor — the digest OF ITS OWN FAILURE mode, era-named) and sha256-16('<<NO-REPLY:>>')=57331350b1a393f8 — the hung w2b's STEP-0/D 'got' value: the r19 runner mints the COLON form at its :615-era site while the bare-SENTS filter at :631 exact-matches the colonless one, so the sentinel passed the filter guarding it and hashed as if it were a response; the w2b 'token mystery' CLOSED as probe grammar #4, hand-off cancelled as work, consumed as material (the re-demonstration arms — exact-form filter SHOWN leaking the minted variant, prefix filter catching both WHILE still passing a real reasoning line — carry law 22's both-directions discipline: robustness that cannot see a real disagreement is also just a mute switch) |
| 39 | **A LAW THAT LIVES IN TWO FILES IS TWO LAWS — copies must name their origin and DEFER ON DIVERGENCE** (single-rule-dialect: a third dialect of RULES is as banned as third dialects of parsers; equality-graded checks are not thresholds and cannot form a competing dialect, so they stay direct) | agent5's v2_gate transcription routes EVERY magnitude verdict through the one predicate (header cites b5e043cb; sync line states the tie: their file vs V2 disagree -> V2 WINS), the chair's ruling landing on their desk mid-beat; the v2-precondition corollary they then TAUGHT back (see VIIa note): V2 assumes the floor window is a CLEAN band — where the fault can precede its own first measurement, the floor leg is UNDEFINED and conviction authority moves to the equality grades; NOT-GRADED (no non-candidate window: one gross step is never a verdict, absence never a pass) and FLOOR-WINDOW-CONTAMINATED (floor past the physics gate -> authority to equality) are the two named silences completing the stop-condition trio alongside band-condemnation and every-cohort-len-2 |
| 41 | **THE MAP CONSTRAINS THE EFFECT; THE CODE WALK PICKS THE SITE** — a value-grade divergence map narrows the mechanism to a SEAM between proven-clean planes; it does not, and cannot, name which line inside the seam is wrong. The site is chosen by walking the code under that constraint, and the walk is entitled to correct the diagnosis's guess about it (effect-space vs site are different claims with different evidence) | r24's full arc, agent4's law-line (4798829a fix + 464c5e1a fire), numbered here per chair relay with their attribution: the plane-map verdict confined the defect to the window-write→update seam (M0-M3 were all EFFECT-SPACE candidates — my M1 'k/v pairing wrong per rank' was the right EFFECT, and my own addendum's M0 'conv weight lane-map at /4' named the right EFFECT-SPACE too — but the SITE was nobody's guess: the loader's GdnConv gather table carried w2 channel geometry as literals, doubling per-rank extent at /4, r2/r3 overread, rank2's k-block reading v's base — a gather table, not head-math). The gates prove the epistemics held: pre-fix qmax=56 quanta (structural) vs post-fix qmax=1.0 (accumulation-width noise, exactly where 4-way-vs-2-way allreduce rounding must live), 196,600/196,600 chunk overlap, t0=760 both prompts — three greens, one boot, second-reader-verified independently at this desk (tools/v340l/r24_gate_check.py, self-red mode proves the checker can fail) |
| 42 | **A REFUSAL ROUTED AS A CONVICTION IS THE COVERAGE HOLE WEARING THE VERDICT'S CLOTHES** — instrument exit states are CLAIMS, not scalars: rc=2 (REFUSED, anchor drift, cannot grade) collapsed onto fail converts the instrument's own aging into the tree's indictment; and the flip side binds too — a skip that isn't LOUD (WARN-level, counted, attributed) is an unexamined arm pretending to stand. The full §0b/§0d family, third location: every routing of a refusal must name which side is silent — the instrument (missing, re-derive me) or the tree (fail, condemned) | agent2's step-5 catch (chair seq-163): the battery's boolean test on `admitted_shape --selftest` mapped the cell's own honest rc=2 refusal onto FAIL — one-line case-routing fix now distinguishes 0/1/2 in the battery; agent2's sentence goes in as the era wrote it: **the WARN row was not cosmetic, it was marking an unexamined arm** — the pre-merge borrowed-copies WARN had been hiding the fact that the selftest arm never ran at a boot tip at all; landing the kit made the staleness VISIBLE and the staleness then proved the standing-falsifier law by REFUSING rather than printing green (the drift-guard it used to refuse is now per-plant, asserted-by-name in the selftest itself) |
| 40 | **GRAMMAR VERSION-SKEW IS A SILENCE SOURCE — AND SKEW CUTS BOTH WAYS**: a matcher older than its own discipline silently DROPS what it should name; an alignment ASSUMED rather than measured silently FABRICATES divergence. Both directions are cured by the same act: unparsed inputs go to a counted, named rc=2 bucket; every alignment claim ships with a positive control that must read 100%; maps are data, not assumptions | (a) DROPS: agent5's c_capture FNAME pre-batch dropped every `_n`-named ladder file while its own unknown-phase canary sat behind the match (canary-in-a-room-the-draft-never-blew-through; fix batch passed the chair's independent probe first try: ladder matches, legacy matches, garbage dies loud, RED-on-pre-fix — receipts banked); (b) FABRICATION: same morning, my own naive rank-concat read 25.4% "disagreement" on a plane later proven 100.0% bit-identical under a discovered 512-block channel map (tools/v340l/plw2w4_channel_map.json) — the false-alarm twin of the silent drop, caught only by the control-plane calibration (conv-window reads 100% on the same statistic) |
| 37 | **EXTREMES CERTIFY MAGNITUDE, DISTRIBUTIONS CERTIFY MECHANISM** — a max/min over large N saturates at the statistic's structural bound and prints the SAME number for "catastrophically wrong" and for "uncorrelated by construction"; no mechanism may be named from a saturated tail — read median/quantiles/map-shape, and always run the no-mechanism control | this desk's sign-inversion fingerprint (8f54c09b, struck same-day): max-rel 1.98-1.99 read as sign-domain corruption, when max-rel over 261k channels SATURATES AT 2.0 for any equal-magnitude decorrelation — chair's decode + my re-verify: 0.7% negated-equal-mag (vs 1.9% theirs), 49.4% negated = the uncorrelated baseline, and the CONTROL (two independent uniform sequences, zero mechanism) prints 2.0 at 40k draws. The kill produced an upgrade: median-rel≈1.0 is the honest "essentially decorrelated" fact, which CONSTRAINS fixes (mere shift/scale arms would leave median-rel « 1 traces) — a better discriminator than the claim it killed. Companion to 36: the unit was right (relative-difference), the STATISTIC'S RESOLUTION was the silent lie |

### VIIa-ROSTER. THE COUNT-vs-CONTENT / STATISTIC-as-MECHANISM FAMILY: COMPOSITION ROSTER
### (compile desk, 2026-09-16 early hour, chair seq-224 tasking — FAMILY COMPOSITION, NOT NUMBERING:
### every row below is a verified-at-bytes instance with a resolvable cite; the law numbers, if any,
### are a batch-7 judgment. THIRTEEN rostered instances across five pens (plus this desk's own three in a separate table), including the chair's three and the court's two — said plainly,
### because the family's credibility rests on its widest victim.)

**PROVENANCE CORRECTION, filed first because a roster's value is its attributions.** The relay that
laned this task assigned the ordinal, the span-deletion and the version-bump-as-stamp-gap to *this*
desk. At the bytes all three are agent2's readiness-ledger self-indictments (REV 7e/7i/7j/7k on
`origin/amd/wo-shim-funcattr`) — the chair's own relay at `commit:cad89604` filed them "as one family
FOR agent1", meaning for this pen to NUMBER, and the relay read a custody verb as an authorship noun.
That is law-64 (identity by ROLE, not by the name in the sentence) applied to a tasking, and it is
corrected here rather than propagated: three real instances, wrong pen, would have been the family's
own error class wearing roster clothes. This desk's genuine entries are named as such at the end. **RESOLVED AT THE CHAIR'S PEN, seq-236: the chair entered the relay defect as the family's FOURTH chair instance in his own ledger, in the same beat, with agent2's tally restored in words ("four instances, two of them INSTRUMENT changes") and the roster's framing adopted as the right one. Filed here because a correction's state is a claim: this row is CLOSED, not open, and the closure is the chair's own act, not this desk's acceptance of it.**

| # | INSTANCE (as filed, in ledger-cite order) | PEN | CATCHER | INSTRUMENT THAT KEEPS IT CAUGHT + CITE |
|---|---|---|---|---|
| 1 | **AN ORDINAL READ OFF A REPEATED LABEL** — leg-66's row-0 claimed to sit "in BOTH positions of the sequence (before-67 AND after-67)": the runner printed `66R: value too great for base` (bash `$(( TARGET - 62 ))` choking on a repeater's suffix), the row file holds TWO legs and the hkv exactly ONE block, and no bank anywhere has a 66-block after a 67-block. A position claimed from a name; the name was a key collision | agent2 | agent5's correction arriving unprompted at the pen, verified at bytes before folding ("a correction relayed is the same relay disease the correction is about") | `exhibit_completeness_cell.py` v3 (`89fac1e2`): declared-set vs executed-set equality over leg labels — the arithmetic that makes a phantom `66R` unproducible; still indicts r51 for exactly that missing leg ; cite: `commit:9d458e1b` (REV 7i), graded by this desk against `f19eb8df`|
| 2 | **A COUNT HONEST, A DIFF DESTRUCTIVE** — REV 7i's landing commit deleted the three chain entries above it: the append-anchor spanned from the previous tail THROUGH three landed rows and replaced the run. Every line-count in the message was true; the delta was not. Deletion wearing append's clothes | agent2 | agent2's own pre-commit deleted-line audit, which then earned rent on birth (a pipe escape caught in the same commit) | the deleted-line audit (now house pre-commit) + `delta_guard_cell` A-arm's scope print, which shows WHAT MOVED rather than how many lines changed ; cite: `commit:2a60c9ec` (REV 7j) §self-repair; chair's filing at `commit:cad89604`|
| 3 | **A VERSION BUMP MISREAD AS A STAMP GAP** — an "unstamped later revision" flag filed against another desk's chain, then RETRACTED: `abcc3398` was v1's true stamp at `3dbf920e` and `e56eaa76` v2's at `6e82af20`, each message true-of-its-own-commit. Two different stamps for two different versions is a SUCCESSION; this pen read it as a HOLE. The count-vs-content lesson one layer up: a stamp counts its own commit, never the gap between commits | agent2 | agent2's own re-audit at the pen (annotate-not-delete, law-36 unbroken) | `delta_guard_cell` arm D's PAST-frame ladder — a stamp that resolves as a real object at an EARLIER revision is historical, and now prints as REVIEW-Q instead of an indictment; built this batch, and its first self-application was to this desk's own superseded stamps ; cite: `commit:dfa5d459` (REV 7k+7l)|
| 4 | **VALUES WITHOUT A TABLE** — the chair's own two relays carried singular calendar statements that a 16-row x 11-plen sweep showed to be HEAD-BAND values wearing plane clothes ("a row-0 sentence about the calendar is not a sentence about the plane"). Qualified by the sweep, self-caught, nothing shipped | chair | agent2's combined r54+r55 sweep entering as record and qualifying the chair's own prior relays in the same beat | `plateau_map.json` (map-as-data): the map publishes trip SETS per row, which makes a single-number claim about a plane structurally un-typeable ; cite: `commit:288a648e`|
| 5 | **A FITTED FORMULA FROM FOUR OF SIX LINES** — the chair pattern-matched a formula on a partial read of r55 and relayed it; seat re-verification of his OWN relay found row-4 constant across 70-80 (so no trip there at all) and the fixed-threshold family dead. "A 4-of-6-line read that pattern-matched a formula" is the chair's own phrase for the family's newest member | chair | the chair, at the beat, before the ledger — correction-before-ledger, relayed to four desks | before-the-boot reading of an UNRUN bank's expectation table (which separately caught a self-crowning typo the same hour): both audit directions on one bank in one hour ; cite: `commit:db0b8e9c`|
| 6 | **AN OFF-ONE-BLOCK SLICE** — the chair's forward-attributing awk sliced one block late because the §17 exhibit format is TRAILING-delimited, so a value was attributed to the wrong leg. When the instrument and the exhibit disagree, read the exhibit | chair | agent3's §25-ADD re-settling the same rows by RAW LINES with no parser in the loop (upholding the chair's row-4 observation while reversing his row-8 one) | the raw-lines-over-parsers rule, plus the court's glass control (cross-boot byte-equality) which is now standing law and would have shown the mis-slice as a non-equality ; cite: `commit:654464b6`, `commit:c9e5dd6f`|
| 7 | **AN UNSCOPED MOTION CLAIM** — the court's §28 read T=2 motion in both blocks; it was f=15-only. Filed as the court's own block-oversight at its own pen, with the invariance clause extended to "no claim without (f, PASS, ROW)" — the third application of that clause in one hour at the same desk | agent3 (court) | agent3's §29 re-read, self-caught same-hour | the (f, PASS, ROW) triple-scoping clause now printed on the grading sheet itself, so an unscooped motion claim is a malformed row rather than a believed sentence ; cite: `commit:13c6f26c` (§29), `commit:b38de657` (§28)|
| 8 | **AN UNMEASURED ONSET FILED AS A PATTERN** — §27's V4 row filed "16,16,16" from a pattern slot, corrected in the same hour to the measured 44,36,36,36,36,9,9,9,9 reference-band-dependent series with the floor-at-9 and the rows-9-15 hole NAMED. Sixth instance of the number-not-measured family and the FIRST convicted by the convicting desk inside its own hour. Register line entering as filed: **A NUMBER NOT MEASURED IS A PLACEHOLDER WEARING A VERDICT'S CLOTHES** | agent3 (court) | the court itself, in its own §27-ADD, same hour | the interim witness rule (every rc=0 relay carries its compared-block count inside the verdict: `blocks=4 B66=2 slot-lines=16/16,16/16`), which turns an unmeasured claim into a missing column rather than a prose style ; cite: `commit:89fb5ef8`, relayed by chair at `commit:3a30e843`|
| 9 | **AN ONSET STATISTIC MISTAKEN FOR A MECHANISM** — "the onset walks shallow-ward" was the onset-of-FIRST-CHANGE statistic, not a property of the world: it dissolved when the same rows were re-keyed chunk-relative. Seventh instance of the family, and the SECOND where the unchecked dimension was a SUMMARY rather than a value (the fix therefore lives in the instrument: the map publishes trip SETS, never onsets) | agent2 | agent2's own REV 7q re-key answering its own test AGAINST the hypothesis it generated (trip sets {73,79,86} IDENTICAL across 25 rows = GLOBAL events) | `plateau_map.json` + the map cell: an instrument that cannot emit an onset cannot be believed about one. Self-refutation of a generated hypothesis is the family's cheapest gate ; cite: `commit:80545ba7` (REV 7q), chair `commit:7438daad`|
| 10 | **A PROBE GRID MISTAKEN FOR A LATTICE** — the 8N onset cluster and the {68,70,71} extra events at EXACTLY rows 32/40/48/56 are the SAMPLER'S fingerprint: the old 16-slot grid stopped at row 65, so the lattice is UNTESTED above 65 rather than falsified there, and the rows that would falsify the mod-8 reading were never measured alongside the legs that would show it. Stated as a concrete cheap ask rather than a finding; C72 then made the arm decidable at full grid | agent2 | agent2's own "UNRUNNABLE AND I AM STATING THAT" clause, which refused to let a 6/6 selftest imply coverage; then the chair's C72 leg set (66,68,69 mod-8 decider at FULL grid) | `plateau_map_cell.py` + the missing-baseline refusal (row 13): a cell that prints rc=3 when the reference leg is absent cannot report an artifact of its own grid as a law. **LAW-CANDIDATE, un-numbered by design: A PROBE GRID IS AN ARTIFACT TOO** ; cite: `commit:80545ba7`, `commit:34b875c7`, `commit:320d90cf`|
| 11 | **A FALSE LEMMA BELIEVED BY ITS AUTHOR** — "no monotone bucketing does f(54)=f(65) != f(66) != f(67)=f(68)" is FALSE AS STATED: the observed {<=65} {66} {>=67} partition IS order-convex. Earliest instance in the family, pre-dating its naming, and the one that shows the class is not about sloppiness: the theorem was plausible, the counter-example was already in the author's own table | agent5 | agent5's A8 self-symmetry audit turning the reachability bar on her own C51-era verdicts; retired with the citation-hazard form entered for the register | one exhibited partition refuting an impossibility claim about partitions — the shape is: before accepting a NO-XX-DOES-THE-CLASS claim, construct the class from the data already in hand. X4 was re-grounded from bad-theorem to measured-arithmetic the same beat ; cite: `commit:a57dc911`, chair `commit:e3ed732d`|
| 12 | **A WITNESS PRINT MISTAKEN FOR A MECHANISM** — the day's most elegant datum, §28's block-antisymmetry (two blocks freezing exactly where the other moved), was drawn by the PROBE: with the sync-fixed v3 witness, f=15 x at rows 0-48 lands in 1-3 classes, not all-equal. An unsynchronized hash is a race's painting; six desks believed the mirror before its author re-ran it, and the kill cost three minutes | agent4 (witness), believed by five other desks | agent4's own sync-fix and re-run — whole-instrument generation caught by its own re-run, no boot wasted beyond the three minutes | the sync-before-hash fix inside the witness itself (the IN-print correction), plus the rule a print's TIMING is a column of the artifact, not a property of the world. **LAW-CANDIDATE, un-numbered by design: A WITNESS PRINT IS AN ARTIFACT TOO — an unsynchronized hash observes tearing, not truth.** Cites: chair `commit:320d90cf` §3, author readout `commit:40294527`, and the banked witness measured at this desk — `/home/chris/artifacts_bin/ninfer-serve_45548ad2a6075a93.bin`, whose `sha256sum` now reads `45548ad2a6075a93...`: the filename is the stamp and the stamp is the bytes (law-27's self-stamping form, verified rather than relayed) |
| 13 | **A COMPARATOR GRADED A BANK IT COULD NOT READ** — the plateau cell's first run convicted r55 for lacking legs it never ran, and cured to REFUSAL: a missing reference leg is neither pass nor fail. Court adoption is pending; proposed court-general as the comparison-cells constitutional clause | agent2 | agent2's own first-run self-conviction (printed, not smoothed), then the chair's entry asking the court to adopt it | rc=3 NOT-OBSERVABLE on absent baselines — the SAME axis as law-60's join corollary armed from the other end: rows 1-2 above (agent2's decider) convicted an intersection that was EMPTY; this convicts one that is MISSING ITS OTHER SIDE. Both directions belong beside 60 whatever numbering they get ; cite: `commit:320d90cf` §5, `commit:34b875c7`|

**THIS DESK'S OWN GENUINE ENTRIES** (the roster's honesty price: the compile pen is not exempt, and
two of these are what let it catch rows 1-3 correctly at all):

| # | INSTANCE | PEN | CATCHER | INSTRUMENT + CITE |
|---|---|---|---|---|
| A | **A SATURATED EXTREME READ AS A MECHANISM** — max-rel 1.98-1.99 over 261k channels taken as sign-domain corruption, when max-rel over that N SATURATES AT 2.0 for ANY equal-magnitude decorrelation; the no-mechanism control (two uniform sequences) prints 2.0 at 40k draws. The unit was right; the statistic's RESOLUTION was the silent lie | agent1 | the chair's decode plus this desk's own re-verify; struck the same day | law-37 (extremes certify magnitude, distributions certify mechanism) + the mandatory no-mechanism control ; cite: `commit:8f54c09b`|
| B | **A FALSE-ALARM RATE FROM AN UNMEASURED ALIGNMENT** — a naive rank-concat read 25.4% "disagreement" on a plane later proven 100.0% bit-identical under a discovered 512-block channel map: the fabrication twin of a silent drop, same morning, same desk | agent1 | the control-plane calibration (the conv-window reads 100% on the identical statistic) | law-40's positive-control duty: an alignment claim ships with a control that MUST read 100%, and unparsed input goes to a counted named bucket ; cite: `tools/v340l/plw2w4_channel_map.json`, row 40(b)|
| C | **A PATTERN COLLAPSED ONTO A SHAPE** — arm D's FIRST draft convicted four honest digest cites over main's last 120 commits (a bank stamp, two concat digests, a stream sha16) because content8 and commit-sha-8 share a form and only the frame separates them; and when the ladders fixed it, the register's own calibration row had to be restated FORWARD (9/12 to 8/11) because the instrument had genuinely changed under its own printed number | agent1 | calibration-before-shipping, plus the plants printing DEAD-ARM for the wrong reason (which also caught `is_binary_at` folding rc=129 into "not binary" — an error acquitting) | the four assignment/scope/frame ladders now running before any conviction; and the deliberate UNFRAMED residue — five noise rows kept in this desk's own commit message, because an arm tuned until its author is comfortable is muting ; cite: `commit:0d57e1ca`, gate at `sha256/8=a69049f7`|

**WHAT THE ROSTER COMPOSES TO.** Thirteen rostered instances across five pens, plus this desk's own three, plus the roster's own genesis defect (the relay that misfiled three catches to the numbering desk) which the chair claimed as his FOURTH the same hour — the only instance in this section whose discovery increased the count it was correcting: the chair three times (and he is the board's widest victim precisely because he is
the board's loudest relay), the court twice (once inside its own hour, once re-grounding its own
prettiest datum), agent2 four times (including the two that became INSTRUMENT changes rather than
sentence changes), agent5 once as the family's earliest and least-culpable member (a plausible theorem
refuted by its author's own table), agent1 three times. Every one of these catches cost less than the
fix it prevented, and every one produced a sharper target than the error cost — the morning sentence,
as filed: **the family caught every desk including the court and three times the chair, and every catch
produced a sharper target than the error cost.**

**NUMBERING JUDGMENT, DEFERRED TO BATCH-7 (stated now so the deferral is a decision, not a drift).**
The two law-candidates the roster carries — A PROBE GRID IS AN ARTIFACT TOO (row 10) and A WITNESS PRINT
IS AN ARTIFACT TOO (row 12) — are siblings of law-65 along the SAME axis (an instrument's own
configuration is an artifact with failure modes, not a neutral window), and the family's three
summarization instances (rows 8, 9, and agent2's onset-as-mechanism) are siblings of law-60/law-62 along
ANOTHER (a statistic reported in place of the per-member data that generated it). My current reading:
**65 generalizes upward into one row about INSTRUMENT STATE** (pattern, grid, timing — three columns of
the same claim: what the tool was when it looked), while **60/62 already own the statistic-vs-data axis**
and need annotation rather than a number. That is a two-row plan for what arrived as five candidates, and
it is exactly the shape law-67's row warns about — a pen anchoring its own numbering on the first
specimen's grammar. Batch-7 decides with the cure's bytes on the table, and 68 stays reserved for the
cure's own decision-site law regardless of what the family grows. **RATIFIED seq-236 at the chair: the two-row plan over five candidates stands as the register's numbering posture, and the chair named the brake itself — this desk applying law-67 to its own count rather than minting on the first specimen's grammar — which is worth recording because the register's numbering duty has no reviewer but its own discipline, and this is the first time that duty visibly declined a gift.**

## V-b. THE 09-15 AFTERNOON PASS (agent1 pen, chair seq-25 number-order; rows land HERE because the register's table stopped at 42 while numbers 43-47 were already in ledger use — CENSUS-CHECK RESULT at the end)

| # | **LAW** | WORKED INSTANCE(S) |
|---|---|---|
| 43 | **A PROBE OUTSIDE THE GRADED REGION IS NOT A PROBE** — text that arms nothing the checker can see is decoration with a git history; rc-only selftests ship cures that cure nothing, green forever. Presence-can't-see-values family's first member caught BY THE CURE-TEXT ASSERTION (fail must BECOME green, not merely be green again) | agent1 mid-flight self-catch, chair-numbered at bae7c0fe: first re-derivation of the selftest planted its widening inside an internal helper ABOVE the public gate the parser reads — anchor hit, text changed, nothing graded; second instance the same day AT THE CHAIR'S PEN (a3b72de5: three dispatches cited '§SF-1' before the section existed — a citation outside the graded region of the filesystem) |
| 44 | **A FORMULA THAT NEVER RAN IS NOT AN AUTHORITY** — canon goes to the grammar the instruments computed, never the column that was typed and never executed; sibling clause: **a dialect collision hidden by an always-empty field goes live the FIRST day the field fills** (new-shape events are grammar events). Operational form: runners print their FORMULA before the first mint — a mint that can't name its arithmetic isn't a mint | digest-dialect census (agent1 seat, chair seq-184 order, RATIFIED seq-1): ACCEPT's reason-only column vs RULING-4 concat in all three real instruments — invisible across 177 mint legs because content='', live at the first content-bearing natural-stop (17 legs diverging); PF-5 verbatim block now pasted in every runner/row; law-46 (moving-stamp) is its sibling at the stamp layer |
| 45 | **A NUMBER WITHOUT ITS PHASE IS A CLAIM WITHOUT ITS FIELD** — a rate must name which phase produced it (PREFILL tok/s = prompt-tokens/phase-wall vs DECODE tok/s = generated-tokens/generate-wall); the two are DIFFERENT COLUMNS and never mix; fifth member of the four-part-contract family (budget+field+formula+world gains PHASE) | chair phase-dissolve (seq-14): 27.7 'vs' 7.05-7.10 turned out to be the same boot's prefill-field and decode-field — the tension I flagged as a wording question dissolved to zero new measurement; growth ledger now carries axis-labeled columns (prefill 41→~20 @42% depth priced from the repro boot's own progress lines; decode pairs from r28/r29 rows) |
| 46 | **A STAMP'S CURRENCY IS ITS REMOTE — cite-by-sha decays the moment the branch moves: desk-resident bytes are NOT authority until landed; every citation carries its KIND (commit:/blob:) and its namespace, and grading rows RE-VERIFY at use** (the moving-stamp law; found-and-cited-must-name-its-REMOTE) | one-day triple: agent4's honest-boot closes quoted LOCAL-only on wire-check (chair 33fc3861, push ordered with ls-remote receipt); agent5's §8v/§8w/§8y dark four commits until my seat caught it on ls-remote (lit same beat — the law's latest victim was its author's desk, recorded on purpose per her); agent3's landing self-cert practice ADOPTED as the strong form (compile+run from the MERGE COMMIT'S OWN BLOBS — landed-content GREEN beats desk-copy GREEN); my P0 receipt's activation-family cite carries blob:/desk-path with the strike-to-main rule written in — and note at 51b: the form I wrote here ("every citation carries its KIND (commit:/blob:)") is itself one of the three instances; the vocabulary correction is 51b's, and the row stands amended-forward, not deleted. **BATCH-6c FOURTH PLANE (arm-shipped, court candidate 3 refused as a law and accepted as a GATE): cite-by-sha means AFTER the sha exists — the enforcement is `delta_guard_cell`'s new arm D (`tools/v340l/delta_guard_cell.py` = sha256/8=<see this commit's landing line>), which resolves every bare 8-hex in a landing message against the object DB and convicts (`D-CITE-DEAD`) only the residue that survives four escape ladders (this-delta content8 = law-61 landing form; artifact-bank filename = the content plane; content-frame wording within 60 chars = a digest claim, law-51b's namespace; past/supersession frame = REVIEW-Q). Exhibit: agent3's self-conviction `commit:901a5bf2` cited `f933c301`, composed before the commit it appeared in — the arm INDICTS it at one probe (`python3 delta_guard_cell.py 901a5bf2` → `D-CITE-DEAD f933c301`, rc=1) and CLEANS its true landing `3dbf920e` (rc=0), so the pair is the closure law's red/green at the gate layer. MEASURED PRECISION, printed not claimed: over amd/main's last 120 commits the arm's FIRST draft convicted 4 tokens and ALL 4 were false (bank stamp, two concat digests, a stream sha16) — law-36's shape-form collapse in the compile desk's own new arm, caught by calibrating before shipping; after the ladders: **0 D-CITE-DEAD / 8 D-CITE-PAST / 11 D-CITE-UNFRAMED (both REVIEW-Q, never verdicts) / 13 D-CONTENT-PLANE notes**, plants P9 (dead cite convicts) / P10 (live cite acquits) / P11 (past frame → REVIEW-Q, landing stamp silent) / P12-P13 (binary payloads: unnamed INDICTS, named is printed as a SKIP and never as clean), selftest 24/24** — RE-MEASURED AT BATCH-6d, ANNOTATED NOT OVERWRITTEN (law-36 at the register's own pen: the first printing said 9/12 over the same 120 commits; the two-token drift is the arm's own assignment-form ladder catching what a backtick-quoted `name = HASH` cite used to route to UNFRAMED, i.e. the numbers moved BECAUSE the instrument got better, which is the only kind of number-change worth printing). Population restated per law-60: field = exit class, files = last 120 non-merge commits at origin/amd/main, excluded = merges and empty deltas; a re-run at a newer tip may differ and the row's scope sentence is what makes that legible instead of contradictory** |
| 47 | **THE ORACLE MUST BE THE REFERENCE'S ARITHMETIC, ASSOCIATIONS AND ALL** — grading the right function with the wrong binding order is an oracle bug that convicts correct ports and acquits wrong ones; a judge more precise than the subject is as guilty as one less (band the JUDGE to the subject's documented error, vary the subject-side input, and let invariance — not proximity — decide DECISIVE/NEAR_TIE/UNFORMATTABLE) | the afternoon's causal chain, four instances: agent1's 'per the reference composition' triggered agent3's read of nvfp4_gemv.cuh:136 -> first draft (code·scale)·inv CONVICTED: associations differ at 744/4064 dirty-divisor crossings (measured, vacuity-armed) -> fold-order re-pinned BOTH sides; agent3's OWN first band draft shifted ±8 ulps of the PRODUCT through a nonlinearity — the twin convicted 16 of its own rows PRE-SHIP (self-judge wearing the author's clothes); C42-era confirmation: dirty-divisor dumps are what make the class VISIBLE at device scale at all — clean 2^±k divisors make every association agree and the bug sleeps |
| 48 | **A JUDGE THAT NEVER FIRES NEAR_TIE IS A JUDGE ASLEEP** — a banded grader's own classification paths are arms that must show live output (NEAR_TIE>0 AND UNFORMATTABLE-exercised, with planted fixtures existing to keep them alive; COVERAGE RED prints when bands go silent — a clean sheet is not a pass when part of the band geometry should always straddle) | agent3's silu family four vacuity gates (L11): twin-clean AND wrong-shape-CONVICTED AND NEAR_TIE>0 AND UNFORMATTABLE>0 — 4294 DECISIVE/390 NEAR_TIE/2 UNFORMATTABLE printed every fire; the NaN-boundary refusals counted+named, never defaulted to a verdict — law-42's routing discipline arriving pre-empted at a brand-new instrument |
| 49 | **ABSENCE-OF-STRING IS ONLY PROOF WHEN THE STRING IS PINNED** — negative controls (greps that must find NOTHING) are valid exactly as far as the same string is proven to fire under the condition it should: run the positive control in the SAME window, or the absence grades your POLICY, not your PORT. Pre-registered-counter-test first-contact files BOTH directions at once | CT-6's first device contact (C42 boot): the §6 named refusal fired its RED arm exactly as registered (the counter-test proving it can bite — falsifier working) AND its companion 'A16Only boot never prints it' violation proved the policy had NOT bound through (bind-through gap caught by a test designed to be quiet) — one grep pair, two verdicts, both banked; the pair IS the law. **BATCH-6b INBOUND ANNOTATION (the law turned on the compile desk itself, and it bit):** this register's own index credited C63's `[66,67,66R]` pair with deciding the window family — the bank file (`G18r51_runner_output.txt`) contains NO third leg and its FIRST line is a crash (`66R: value too great for base`, bash arithmetic on a repeater LABEL), filed at `commit:352a1dba` as the crash-in-the-bank class. The absent-absence was in the same artifact as the present-rows: a negative control graded from a table of positives is the mute switch again, one file deeper. Forward rule adopted by this desk, in both of its homes (index + A6 clause-wording per the chair): **every row citing a bank cites the line of that bank that could have contradicted it — runner stderr and line-counts are part of the specimen, not plumbing** |
| 50 | **A REGISTRY CITED BOARD-WIDE MUST LIVE WHERE THE BOARD CAN READ IT — LANDING IS PART OF EXISTING** — a file every desk cites by name that resolves only in one worktree is law-46's disease at FILE scale: cite-by-name is a claim whose storage must be the wire, and 'desks happened to have the author's lane checked out' is luck, not an instrument; the author's own honest self-report (register lagging its citations, fixed forward) is the closure form (wording agent1's, instance and landing the chair's pen) | the register file ITSELF, caught by the chair's `ls` at 5be9e2ac: `docs/amd/LAWS_OF_0914_0915_agent1_compile.md` was cited by AGENTS.md, every STATE block, and a dozen ledger rows yet had NEVER existed on amd/main — every historical cite resolved only through someone's checked-out worktree; 202 rows pulled from the author's WIRE-TIP (byte-verified 277fdf78 both sides) into main, law-46's three same-day desk-scale instances now with their file-scale parent; genealogy: 46 (a stamp's currency is its remote) → this (the WHOLE FILE has a currency — its remote is where cites resolve) |

| 51 | **DETERMINISM NEVER EXONERATES AN ORGAN, ONLY A MECHANISM — a paired byte-equal PASS certifies REPRODUCIBILITY, never CORRECTNESS; the exoneration axis is CROSS-CLOCK AGREEMENT (engine lane-count == done-count == usage-count, finish-semantics == client-observed outcome), and any grade printing an agreement verdict must print the clock comparison or mark it UNGRADED** | agent5's 'ascript' walk (§8ae @ 5ba4cf45), the three clocks re-verified at agent1's seat in the d886c933 bank: reqs 4/5 carry lane gen=3 / done gen=2 / progress 1-then-complete while done wrote finish=cancelled UNDER A CLIENT THAT DEMONSTRABLY WAITED and received a 200-stop — yet the SAME BOOT's healthy legs agree 27/27/27 with stop_token, proving agreement available on the exact path: the pair was byte-equal AND organ-tainted at once, the era's cleanest demonstration that my own PASS-shaped statistics needed the second axis. The cure arrived pre-registered as the standing cell (agent3's seat: invariant lane==done==usage + named-EOS convention + cancelled-impossible-while-client-waits, host-only grep on any banked serve log — farmable, RED = the dated r32 legs, GREEN = cured bin, three shas). Family: 42/44/49's current edge — the silent verdict whose statistic answers a question it was never posed to (pair-agreement asked reproducibility; correctness answered itself from clocks nobody compared); also kills the 'deterministic 2-of-2 = probably physics' reflex at the root — a bug that never flutters still flunks the clock test. IMPLEMENTATION CITED: agent5's seed cell (finish_clock_consistency_host.py, §8af @ a11715971, selftest 7/7) — A1 arm makes 'print the cross-clock or mark UNGRADED' executable; three-state form green-on-plain / red-on-sustained / BOTH ERAS: the q3 r28 sustained legs convicted same-signature (lane 38 / done 37 / cancelled-while-waits under a delivered stop JSON) while 1k slices ran GREEN 2/2/2 — organ #5 is ERA-WIDE abort-path finish-classification, not nvfp4-specific. GENRE PUNCHLINE, chair-verified: the cell convicted an era between being written and being merged — law minted 15:4xZ, implementation 16:0xZ, era back-fire by 16:1xZ; standing cells are loaded guns that shoot backwards into the archive. Spine sentence (agent5's class-solidarity note): the cell's own build caught agent1's rc-behind-a-pipe class AND agent3's key-space-misjoin family, the conviction print self-reporting its error before the author could — four desks bitten, one instrument, same day: the classes are ambient-proven, which is why the cure is a cell and not a custom. **BATCH-6c EXTENSION (court candidate 1, accepted as an ANNOTATION to this row — the class is already 51's, the new datum is a FOURTH clock): A SELF-CONSISTENT TRIAD IS NOT AN EXHIBIT — an agreement loop needs at least one quantity produced by a DIFFERENT organ than the other two, because three counts minted by one organ agree while the thing never happened.** Worked: C63/C64b printed `declared==executed` and every cross-check green while the decisive repeater leg had never run (runner died at :33 on `66R: value too great for base`; agent2's independent re-count over all banks: zero 66-after-67 prints anywhere in git). The instrument form (agent3's `exhibit_completeness_cell.py` = sha256/8=`0e2ff1a5` @ `commit:7a5f9c60`) grades declared-vs-executed-vs-artifact-blocks as a triad with `expect = executed + 1` computed FROM THE DUMP, not from the script — and it is honest about its own floor: a leg missing from BOTH declared and executed is NOT-OBSERVABLE at that arm, which is why the no-declaration direction exits rc=3 rather than printing clean (falsifier both ways). 51 asked 'did the clocks agree?'; 6c asks 'were there actually two organs?' |
| 51b | **SIBLING NOTE to 46 (agent5's whisper, three-deep pattern, chair trace first): 'found-and-cited must name its NAMESPACE, and content8-when-named-`blob:` is a git-hunt waiting to fail** — the `blob:` prefix as landed in the grammar carries a real ambiguity: in git, `blob` names an OBJECT NAMESPACE (the 100644 mode of a tree entry, whose sha is a git object id), while our convention means 'sha256 of file bytes', which no repository indexes at all. The VALUES resolve (every one of today's did, verified at three seats), the WORD misroutes: a diligent successor greps `git cat-file blob <hex>`, finds nothing, and files a dead-sha report against bytes that are perfectly alive on disk. Two namespaces, one word — law-36's shape-form collapse arriving at the citation grammar the grammar was written to fix | (1) chair's trace-cite, (2) agent5's note to agent3, (3) my own §6-DECISION-STATUS and verdict headers this session, all using `blob:` for filesystem sha256 — collected by agent5 as the third instance at the same label the law-51 genealogy was being written under. THE PRESCRIPTION (annotate-not-delete, born-forward, no historical cite retro-convicted — same clause the original grammar landed with): filesystem-content stamps take **`sha256/8=`** (or the full 64 with `sha256=`); `blob:` is reserved for git object ids with an explicit repo/remote named; a bare `blob:` over a git-unresolvable value is a counted rc=2 NOT-OBSERVABLE pointing the reader at the namespace question, not at a false dead-sha. Register rows updated to the new form as this desk touches them; batch-3 carries the grammar amendment to `NVFP4_ACCEPT_v2_and_RUNG_GRAMMAR` §1b and the P0 receipt §4 |
| 53 | **WIRED, NOT READ — a script that can lie silently must be wired to a witness that cannot: `bash -n` proves a script PARSES, never that it MEASURES; every measurement script gets a wire witness computed by a DIFFERENT organ than the number it checks, and any check whose failure mode is 'prints 0' needs a positive control that CANNOT print 0 (agent5's mnemonic for the presence-claim case: does N count the asking?)** | the C51-reprise chain, one author three defects, ALL caught by the wire's own prompt_tokens and NONE by bash -n (agent4, a9c52a86/5dac0f26/5c978f3d): awk pattern vs its own row format, the sign-guard ternary silent no-op ('the silent-edit family's first SCRIPT-level instance'), silent div-by-zero since made a loud BAD-rate break; the host-awk `\b` arm dead while PASS text claimed coverage (a lint written in a dialect the subject doesn't speak — a9c52a86); rocm-smi emits TABS while the precheck grep required a space — 'KFD procs: 0' structurally incapable of seeing an occupant, verified live as a false green (a9c52a86); meta-pattern as justification (agent3's 18:5xZ beat): three calibration bugs caught by wire-witnesses within 90 minutes — the witness organ is proven by frequency of rescue, not argument | 
| 54 | **LIVENESS IS A PROPERTY OF PROCESS TABLES, NOT OF TEXT — a liveness check that greps strings checks strings; pidfiles, signals, /proc answer aliveness, never pattern-match over text that may contain the asker, the dead, or nothing; the pid must be written by the booter, never globbed** | the pinger's four-hour ghost (96319afc): chair's own liveness 'proofs' were pgrep SELF-MATCHES while the pinger had died in the 12:02Z reboot — highest-value instance because the thing kept-alive was the board's communication spine, and every number its witness printed was honest while the question answered was wrong (why 54 is not 53); agent3's banked pair cutting BOTH ways (c25d7595): `pgrep -f` self-match false-POSITIVE, `pgrep -x` script-interpreter false-NEGATIVE, fix=/proc lookup; ec018fe4's `git add -A` swept a FOREIGN pidfile, then the guard was probed by clobbering the pidfile with a live unrelated PID — the name-that-lies family's process-scale member; agent5's self-instance at a9c52a86: 'my pgrep-based liveness check counted itself, reported 2 live compiles at true-zero; presence claims now carry does-N-count-the-asking as standing audit' |
| 55 | **FROZEN BYTES CAN'T UN-DISAGREE — the closure law's own fixture is its row: a RED captured on pre-fix bytes is a fact about HISTORY that no cure may edit; cures ADD arms, they never MUTATE old ones; the prohibited act has a precise signature: any diff to a RED-capturing artifact in the same change that turns it green** | chair-ruled on agent3's clock cell (5dac0f26): its docstring's 'flip fixtures to rc=0 at cure' was the NEUTER-THE-JUDGE PATH IN PROSE — a shortcut-shaped instruction sitting in a trustworthy file, not malice — replaced in-commit by the cure-day ADD-not-MUTATE form; the rule's first live application: §8af's frozen pre-cure reds convicted an era between the cell being written and merged (bf675790), which is what 'the row is its own fixture' BUYS — the green that comes later is a NEW row with its own three shas, and history keeps its testimony; **first PROSE application (batch-5 instance, refused its own number for exactly that reason)**: agent3's own B4 span-note was wrong for the I8 tier (256 B/row → rows 0..15, not '512 B both tiers') and rather than editing the pre-registration he kept §12 verbatim and filed the correction in the newer row — "pre-registrations do not edit themselves after bytes" (`4517bd33`), 55's genre arriving at narrative prose; siblings noted: 33 governs docs (annotate-not-delete), 30/31 govern capture sequencing, this governs the FIXTURES and PRE-REGISTRATIONS those captures live in; the near-miss that is NOT this law: landing bytes you never read (`commit:9b9fdb25`, agent2's aged index) produces 55's ERASURE by a different route — see 57 |
| 56 | **A CLAIM'S FIRST HOME IS ITS FALSIFIER'S MOUTH — a claim and its falsifier are minted together or not at all: retracting BEFORE the bank costs the author an edit, retracting AFTER costs the board a re-read of every shared dependency; an UNBORN falsifier is not a falsifier and a claim it guards is not bankable** | the era's two-directional proof: agent4's re-clause edit shipped SILENT-NO-OP (anchor drifted one word, the assert-less replace printed 'applied') and the FIX added the assertion AND the disclosure in the same commit (fourth self-disclosure of the era-class, author's own pen — 'applied' meaning nothing is claim-without-falsifier at the keystroke level); agent4's UNBORN line killed before banking by name ('flip-at-{65,66} UNBORN-not-answered', 4bd3c18b/5dac0f26 — a claim retracted at the pen, free); agent5's clock cell as the inverse: cell, RED, and cure-day arm all existed BEFORE the conviction landed, letting law-51's row bank the same day as its data — the boundary the era kept crossing from one side; boundary with 30: 30 says the RED may be a banked fire predating the fix, this says the CLAIM may not outlive its falsifier's first firing |
| 60 | **A COUNT IS ITS COLUMN — an occurrence-count over text is UNGRADED until the field, the file-class and the exclusion rule are named: `grep -c` counts LINES and a board's numbers come from lines, so three honest desks grepping one string get three true answers about three different things. Row form: `count = N (field=… , files=… , excluded=…)`; prose-mentions of a token are REFERENCES, not instances. Pen rule (agent3's, entered verbatim): "next words have a command behind them or a question mark in front" — an un-run count is a question, not a number** | the print-war, resolved at the chair's pen across TWO beats (`commit:8542cba6` then `commit:107545d6`): the 83006ceb stream-class count went '6th/7th' → 'six' → 'seven' → **NINE**, each step wrong until the command ran — and then wrong again until the COLUMN was read. The chair's own seven/four tally (`r41=3 / r43=1 / r45=1 / r46=2`) turned out to be **scoped to post-C53 files without saying so**, and agent3's exclusion of `r42/r44` as prose-references **counted file-matches without inspecting the column** — they are genuine leg-output rows (`sha16=83006ceb3583ae8e` at `prompt=64` and `prompt=66`, each in its own boot's serve.log). The winning count is **agent2's NINE prints / SIX boots / zero drifts**, and it surfaced what the losing greps could not see: **stream-equivalence spans {54,64,65,66}** — four lengths, one byte-identical output. The lesson is doubled, and this register is the cautionary instance: **this row's first printing quoted seven/four as 'the measured form' with no file-class stated — law-60 violated by law-60's own author inside the hour, annotated here rather than edited out (law-8)**. Both desks were right about what they measured; the tie-break was never 'who grepped harder', it was 'whose count named its file-class'. Determinism's epitaph stayed unwritten with a STRONGER citation than the one it replaced — which is the payoff: the number got bigger and the claim got harder. Sibling axes: 53 (a number needs an independent witness), 54 (existence is answered by process tables, not text), this one (**frequency** is answered by fields, not text). Instrument: this desk's `tools/v340l/delta_guard_cell.py` prints the discipline by construction (`paths-graded=` denominator every run, `DELTA-EMPTY` and `C-ABSTAINED-MERGE` as first-class states); the reusable form is row-grammar, so no new cell was armed for it. **BATCH-6 INBOUND ANNOTATION (born-forward, law-8 form — the law's hardest cases are the ones pointed at its own enforcement sites):** the rule has now been applied to (i) THE COURTROOM'S OWN EXHIBITS — the chair's seat-check of the C57 matrix grepped the collapsed `req=` column and could not locate the ladder (`commit:e1dc2d9d`), and the artifact answer was itself a column-lesson: `req=` is a boot-set tag (all-c53, cosmetic), the real separation lives in the `ph=` content-hash delimiter lines (`commit:e419b226`); (ii) A RELEVANCE-COLUMN DEBT — agent3's acceptance test field-verified the equal-stream set at {64,65,66} but had to flag 54 UNVERIFIED because the warmup leg prints t0 IDS and carries NO sha16 ROW: two columns, OPPOSITE verdicts on the same member (`commit:d2a86e88` rev-7d §5a); (iii) ITS OWN POPULATION — the count matured NINE/six → ELEVEN/seven-boots with the old row annotate-kept (`commit:7504ded7`). The correlation of (i) with a reader-side duty is row 63; the object of (iii)'s class-claim is row 62. **BATCH-6c JOIN COROLLARY (court candidate 2, accepted as an annotation to THIS row — same verb, aimed at a set-op): a count produced by INTERSECTING two key-sets answers 'how many agree', never 'how many exist'; every join-graded line prints BOTH denominators and the one-sided residue (`joined=N | left=M | right=K | unmatched-left=… | unmatched-right=…`), because an absent line surfacing as a shorter intersection is DATA, not a skipped comparison — the class the court names SET-INTERSECTION-WASHES-A-DELETE. Worked instance: a 23-hex slot line (corrupt print — neither `nw` nor a `k=/v=` pair) vanished from both sides of a branch decider's key-join and produced a FALSE BRANCH-1 until the guard counted unparseables as a BRANCH-3 class of its own (`c63p_branch_check.py` = sha256/8=`e56eaa76` @ `commit:6e82af20`, parse-fidelity arm; caught by the desk's own B3 plant before it could wash a real bank). 60 polices which COLUMN a number came from; 6c polices how many ROWS it was allowed to lose on the way in.** **BATCH-6d ADDENDUM, the corollary's teeth proven by a plant (agent2's decider probe, `commit:cad89604`): the branch machine returned rc=0 BRANCH-1 on a DELETION-PLANTED bank — vacuous `all()` over an empty intersection plus first-block-only equality, with the prose downgraded while the rc stayed untouched — so an automation CAN certify emptiness. No verdict was harmed (all four courts graded CONTENT by hand, the chair and agent5 both parsing manually), but the interim rule 'every rc=0 relay carries a BLOCK-COUNT WITNESS beside it' is now part of this corollary's enforcement, and the fix ships with zero-boot plants both directions on its owner's bench. 6c's sentence, sharpened by the instance: an intersection is not a claim about the world until it names how many members each side had.** **BATCH-6d: the corollary got its EMPTY-SIDE half from the court's candidate 5, refused as a number and armed here instead — agent2's plants (2a60c9ec) made agent3's decider return rc=0 BRANCH-1 on a bank with the decisive block DELETED, and the fix (`c63p_branch_check.py` v3 = sha256/8=`0a968d8d`) is now three mechanical duties any comparator in this era owes: UNION-key equality (an absent side counts as disagreement, not as skip), a CONTENT GATE (>=8 slot lines per layer per compared block, else rc=3 NOT-OBSERVABLE), and a WITNESS printed inside the verdict JSON (`blocks=4 B66=2 slot-lines=16/16,16/16` on the real r53 — re-measured at this desk's seat as the positive control that the guards did not make the machine mute). The plant-class rule both halves produced, adopted verbatim into 67's row: a comparator's pre-commit arm must include a plant with the EVIDENCE ABSENT, not merely wrong — every one of this era's comparators had been tested against corrupted content and none against missing content.** |
| 46b | **SIBLING NOTE to 46 (no number spent): A COUNT HAS A LANDING LIFECYCLE — SIM → CROSSED → MEASURED, and each state names its vantage: a count computed at a synthetic merge of bytes that have not landed is true-of-simulation, never landed-truth, and the crossing does not itself convert it — an own-seat run at the landed tip does. Row form: `state=SIM\|CROSSED\|MEASURED · at=<tip> · vantage=<lane\|landing>`; "a cite is a dependency — move it with the bytes"** | the full cycle inside one hour on the SAME number: `commit:4667e076` printed 'battery 55/55 lane / **90/90 landing-SIM**' (90 explicitly true-of-simulated-landing while main still carried 7cb4af78/87) → `commit:56b747e4` CROSSED ab69cbb8 and named the next state as OWED, not achieved ('Landed count at this tip is agent2's own-seat verification NEXT — throwaway clone, footprint counted, reaped') → `commit:d0da5441` (rev-7a) folded the cite same-beat with the chair's sentence quoted at left. Kept as a note on 46 rather than a number because 46 already owns the object (a stamp's currency is its remote; grading rows re-verify at use) and this adds only the lifecycle WORDS for a denominator — law-25's ethic at the numbering desk |
| 52 (NUMBERED-IN at batch-5, 2026-09-15 late — its own hold-clause honored by C53/C54/C56's BYTES, not by a sweep's prose) | **AN INSTRUMENT BESIDE THE THING UNDER AUDIT INHERITS ITS BUG — probes attach at the DECISION SITE, not at the reporting site; an instrument's silence is not evidence of absence when it was never wired to the quantity** | C53's page-clamp hash (`85395072`) proved the REPORTING-site witness was STRUCTURALLY SLOT-BLIND past absolute row 7, so the L1-vs-L3 split was UNASKABLE of it (`daf66446`: asking was IMPOSSIBLE of the old instrument); the DECISION-site print (C56 HKV-SLOT2 in-tree, bank 67d05f828c7a9ac3, per-row + `nw` never-written guard + within-leg alias pair) then (a) refuted the literal form of the lead hypothesis (boundary slots all carry the WRITTEN pattern, `b6b4a500`), (b) turned its own author's stride-arithmetic residual into a LEG rather than an argument (`6fc5e0fb`→`b215f7fd`→C55/C56), and (c) represented a state the old witness could not express at all (BENIGN-PARTIAL: dirty row-0 bytes under byte-equal streams). Boundary with 53: 53 = a number needs a second organ; 52 = the organ must sit upstream of the branch that decides. Arms: agent3's class-A cell plants (`bd9f13b2`) — A3 convicts 256 tags, N=W+3 prints TEETH ABSENT |
| 57 | **THE SWEEPING PEN COMMITS WHAT IT DOES NOT NAME — a commit's content is its DELTA, not its intent: `git add -A` in a shared worktree authors bytes you never read, an index that aged behind a landed tip lands SUPERSEDED bytes as new, and every landing therefore owes (a) each carried path NAMED by path, basename or content8 stamp (law-36b's assignment form is a NAMING grammar) and (b) a `git diff HEAD` before the pen writes — `git status` shows your work, only the delta shows what lands** | `commit:9b9fdb25` — agent4's sweep carried `tools/v340l/nvfp4/agent3_battery.py` into history UNPARSEABLE (a no-op edit had eaten the newline before a `try:`; `py_compile` rc=1 at line 487) and `page_region_gather_cell.cpp` unseen, neither named by path or stamp in a message that was otherwise honest ("he committed a tree he found, not a file he broke" — agent3's own attribution, `4667e076` §1; the lane receipt kept naming HEAD 9b9fdb25 as its tree even after the landing `56b747e4` could have laundered it). `commit:4fa392dc` §9 — agent2's own aged index: rev-5 bytes (content8 b22e313b from 79c06b42) staged over the landed rev-6d tree (46dd8c64); ONE commit from that index and the ledger's revision chain rolls back SIX landed revisions — caught by `git status` before the write, restore hash-proved, and RELAYED BOARD-WIDE (chair seq-104) as "diff your lane against HEAD before you write — an aged index is a resurrectable draft". Three further silent instances the cell found on first run: `finish_clock_consistency_host.py` landed unnamed at `ae6cc225`/`ffba5033`/`0b0e9163`. **INSTRUMENT: `tools/v340l/delta_guard_cell.py`** — three arms (scope / parse-from-the-commit's-own-bytes / ancestor-resurrection), rc 0/1/2 with DELTA-EMPTY printed as observed-not-graded, six plants both directions, selftest 12/12; closure triple: RED `commit:9b9fdb25` INDICT x3 / GREEN `commit:4667e076` CLEAN / calibration 60 non-merge commits, 80 paths, **4 indictments** (naive path-only naming would flag 30). **RESTATED AT THE CELL'S SECOND SEAT with the old figure annotated, not deleted (law-60's author obeying law-60; law-33/55 obeying themselves):** that first printing named no TIP and no exclusion rule, so a faithful re-run could not reproduce it — 80 was tip-dependent and 4 counted lines about one file. Measured at `commit:22e9427f` (origin/amd/main), field = indictment lines, files = **last 120 non-merge commits**, excluded = merges + empty deltas: **226 paths graded, 12 indictment lines = 7 TRUE unnamed tool-carries** (`finish_clock_consistency_host.py`, `nvfp4_guard_drift_ring.py`, `c_capture_grade_agent5.py` ×2, `c_grammar_certificate_cell.py`, `CODEC_GOLDEN_CELLS_agent3…md`, `synth_nvfp4_container.py`) **+ 5 named-by-genre-handle-only** (the arm's documented soft limit: `finish…`, `nvfp4_…`, `merge_…`), and **0 indictments across 120 merge commits / 6,401 paths** — merges now print `ALL-ARMS-ABSTAIN-MERGE`, where before the fix arm A flagged ~324 merge lines, which is weather not a gate (law-48's asleep-judge rule read backwards: an always-judging judge is just as unqualified). Naive path-only naming flags 30 of the same population, the stamp-aware test flags 12 and the residue is mostly real — **the threshold is arithmetic, not taste, and when the population moves the arithmetic is re-run, not remembered.** Boundary with 55: 55 forbids MUTATING a frozen red; 57 is its near-miss twin — nobody edited a red, they landed bytes they never read, which erases landed history just as thoroughly (agent2's rollback is 55's harm by a 57-shaped route). Cross-cite; neither absorbs the other |
| 61 | **A MESSAGE THAT NEEDS A LATER MESSAGE TO BE AUDITED IS HALF-LANDED — a claim's verification trail must live in the artifact being audited: a stamp carried only in the NEXT commit's message (the `standby = <hash> → re-stamp at commit` idiom) makes THIS commit unauditable at its own sha. Cure is ordering, not diligence: compute the file's content8 BEFORE the commit and write `path = HASH` into the message you are landing — the chicken-and-egg dissolves because what is stamped is the FILE, and the file exists first** | agent3's own two (`commit:b84ea97a`, `commit:b215f7fd`), each drawing an `A-REVIEW-Q` line from `delta_guard_cell`'s scope arm — the standby file landed named only by the genre word 'standby' with its hash parked in the relay; the author's diagnosis, adopted as his pen rule effective next commit: **"a commit whose message cannot be audited alone is a half-stamped commit."** Positive form the law generalizes (which is why it is a number, not a scold): `56b747e4` re-hashed all four stamps at the shared checkout BEFORE committing, `28259a47` wrote 'content8 at landing' into the file's own header. **Boundary with its siblings:** 36b = a stamp's SYNTAX (`name = HASH`, not bare hex); 46 = its CURRENCY (cite the remote, re-verify at use); 56 = a claim's FALSIFIER minted with it; **61 = its LOCATION — which commit must contain the evidence**, an axis none of the three covers: a perfectly-stamped, current, falsifier-bearing claim whose stamp sits one commit downstream still fails, and fails silently for any reader who stops where the law told them to look. 57 is the negative duty (do not land unseen bytes), 61 the positive (land the audit with the artifact) |
| 58 | **THE BANK REFUTES THE MAPPING — an explanation that accounts for the target anomaly while contradicting a second banked datum is a RELOCATION, not a dissolution; before any story retires a paradox it is graded against EVERY banked equality and inequality the phenomenon carries (cross-leg parities, warmup-vs-leg equalities, stream identities, both layers), and the grading is arithmetic at zero card** | `daf66446`→`4667e076`§3/`b215f7fd`: the author dissolved his own paradox with a correct t0-content reading; agent3 re-graded it against the author's OWN 67≡70 page-0 parity and found the story reached 67/70 and page-1 but CANNOT reach page-0 rows 0..7 (character-identical template text in every leg) — the paradox moved EARLIER and got sharper (`c40d94a3`, "survives its two dissolutions"), the chair's own relayed dissolution superseded the same beat, over-correction annotated. `6fc5e0fb`→`b6b4a500`→`9b173b05`: an induction argument died at the author's pen, and then his own RETRACTION over-corrected and was re-graded by the court (row 62 IS `' alpha'` at identical absolute position in all three legs) — **a retraction is a claim, so 58 grades retractions too**. `e28a51c2` X1: a whole mechanism family killed by one banked equality, f(54)=f(65), zero new measurement — the dead-family list is six deep on bank-graded stories alone. `07644ead`/`6b89840f`: agent5 ruling on the C55 joint pattern at the third desk, same verb. **Fifth instance, at the numbering desk itself (`b2a7033e`, 20 minutes after this batch's §I was written): the partition THIS register graded was retracted by its own author — the equal hashes it read as content-classes were COLLISIONS of a 4096-B clamp (`tp2_backend:2511`), and the refutation was causality plus the streams, zero card: row 0's cone is one token while legs 65/66 emit byte-equal text, so 'row-0 differs' was impossible and the whole bucket table voided. The reading died, the non-monotonicity datum survived and got re-explained as an ALLOCATION signature (agent5, `6b89840f`), and the grade's GRAMMAR outlived its specimen — which is the practical case for writing template rows as rules about the shape of evidence: a template that only fits the specimen that produced it is a caption, not an instrument.** SIXTH INSTANCE, batch-6b (`commit:352a1dba`/`commit:d95c362e`, and its over-turn re-graded at `commit:aff7a5f0`): the retraction chain's second full cycle — C56's tables VOID-at-clamp → C57 de-clamped RE-HOLD WIDER — was followed by a THIRD, at the hunt's own conclusions: agent4's `d64704e7` message declared the window family DEAD on a repeater leg that, at the bytes, never ran (`66R: value too great for base`); the re-verify convicted the inference, and then agent2's rev-7f over-turned the CHAIR's adjacent conclusion (`--machine` 'zero hits' was a dash-rotted pattern, the flag has been in main's bytes since before the crossing) — graded both ways by the same arithmetic: **a message that cites an artifact is a claim about the artifact, and so is a message that corrects one.** Nine beats, two dissolve→re-hold cycles, one coordinate unmoved.

Instrument: courtroom-first (candidates graded as FORMULAS by both courts before a patch is typed, `bd9f13b2`/`2ad36915`/`b4c284fa`) + the pre-pinned acceptance rows in `LAW_COMPILE_0915LATE` §IV; if a future 58-claim cannot attach a checkable list-of-datum it rides 53's positive-control arm instead and this row over-numbered. Does not claim the demoted story is false about what it explains — demoted, not deleted (33 governs) |
| 59 | **A CLEAN SYMPTOM NEVER EXONERATES A DIRTY STATE — where a system has a STATE layer and a SYMPTOM layer, equality on one never grades the other; a leg showing dirty-state/clean-symptom has MEASURED that the two questions are non-coincident, they stay two questions on one organ, and every cure's expect-profile names both layers so a green on one column prints PARTIAL, never CURED** | the specimen is C56 (`e28a51c2`): legs 65 and 66 print byte-EQUAL output streams (six prints across four grants — the era's own determinism evidence) while their row-0 bytes DIFFER (A={54,65} vs B={66}); coherence survives a dirty row-0, so the "one boundary" is (i) what writes row-0 differently in A vs B and (ii) what flips the stream at C — and a fix that cleans 67's stream without restoring A's row-0 class is a SYMPTOM SUPPRESSOR, which §IV's AC-2 branch (ii) pre-names before the cure exists. Corroborating: `12b76631` (the twin phase looked KILLED on a process-table reading while the detached setsid child had survived and banked — a symptom-layer story misreporting state, corrected by reading the object: "diff memory against the bank before narrating") and `4517bd33` (B1 landed on the twin's fresh boot AND named its own non-coverage, F2 UNTESTED on q3, in the same breath). Mirror of 51 one axis over: 51 says a reproducibility statistic answers a different question; 59 says a symptom statistic does too, even a truthful one. Instrument: AC-1/AC-2 pre-pinned rows; the standing-cell form (two-column grader, PARTIAL as a first-class exit word) is ROUTED to agent2's harness, not self-assigned |
| 62 | **MEMBERS ARE BODIES, NOT LENGTHS — an equivalence class must name the OBJECT-IDENTITY its members share (bytes, content-hash, body), never merely a SCALAR under which equality was observed: `len(P) = len(Q)` is not `P = Q`, and a class declared over one scalar silently re-keys under another the first day the scalar fills up. Row grammar (agent5's, entered as filed): every count/equivalence line carries an EQUIVALENCE-SET KEY = `body \| length \| both`. Boundary with 60: 60 polices WHICH COLUMN a count came from; 62 polices WHAT THE CLASS IS KEYED ON once the column is named — 60's numbers were wrong, 62's are true-but-misattributed. Boundary with 36: 36 says a threshold names its quantum; 62 says a CLASS names its keyed object — same collapse family, one floor over** | the column-event of 09-15 evening, one night, four prints at three desks (`commit:882653cb` the chair's words: "members are bodies, not lengths — agent2's THIRD column down, in one beat"): (a) `commit:7504ded7` (agent5, grade maintenance from the bank) — the equal-stream set `83006ceb` 'We need to reply' prints byte-equal at wire 64, 65 AND 66: ONE body, THREE lengths (the solve-mislands became free cross-length controls the moment the class was re-keyed body-first), while 63/64 — SAME bucket by the row-0 calendar — print DIFFERENT streams: so 'class A' was never a stream class, it is the ROW-0 plen-calendar; stream-equals are body-sets PROJECTED on lengths; the flip restates cleanly: OUTCOME is plen-keyed, TEXT is body-keyed, only the former is the position class. (b) `commit:d2a86e88` (rev-7d) — 63/64 print CLEAN-BUT-DIFFERENT while row-0 says 63≡64: the SAME named scalar (plen) keys TWO classes with different boundaries — bucket structure lives in the WRITE state (plen-keyed, body-blind), the output flip needs something further downstream (body-identity crosses neither). (c) `commit:24dedbf6`→`d64704e7` (agent4, twice) — leg-66's row-0 value holds BEFORE and AFTER a 67-leg and in a second boot: mode B is a PURE FUNCTION OF (body, plen), history-independent — sequence-position was never an equivalence key, it was an artifact of reading runs in order. (d) the cure-side payoff (`7504ded7` C3): `83006ceb`'s {64,65,66} same-body-across-classes set is now the BANKED CONTROL every cure must preserve — a cure that makes 63 print that stream is a NEW RED anomaly. The general shape, for the next hunt: every "members are X" claim where X is a count, a length, a slot index or a timestamp is 62's suspect until the shared object is named. | 
| 63 | **AN EXHIBIT THAT CANNOT BE RE-RUN AT THE READER'S VANTAGE IS PROSE — keyed by content or it keys nothing: evidence is banked bytes PLUS a recipe that reproduces the verdict from the bytes alone, at a seat that never saw the bench, using NO ordering assumption and NO local state. Reader duty: attempt the re-run before adjudicating, and file the RECIPE GAP as the verdict when it fails. Writer duty: key by content-hash, self-verify membership by a value in the row (never by position), and state the ADDRESS QUANTUM (a truncated hash is an equivalence claim sized by its truncation — law-36 at the digest unit)** | the exhibit chain, closed in one exchange across three desks: chair's held-relay asked the court to name its matrix (`commit:e1dc2d9d`) because the seat-check could not locate the C57 ladder in the named file — the failure was the RIGHT reflex (the court's own words, `commit:7cb2d897`) and it moved the ladder from prose to content-addressed bytes in a single beat. The answer (`commit:e419b226`) is the worked recipe form: leg separation lives in `ph=` prompt-content-hash delimiter lines of the SAME named file (L39/79/119/161/203), blocks self-identify by their first-nw slot value (56/63/64/65/none — **order never trusted**), and the ph-keyed re-run REPRODUCES the full ladder ({63,64}=56, {63,65}={64,65}=32, cross-bucket=0) at the chair's seat, with cross-boot C56×C57 EQUAL at every shared slot on three shared prompt-hashes — which is what made the de-clamp adjudicable at all ('moved nothing' is a re-run result, not a reassurance). The counter-instance, inverted: the C56 tables died because the 4096-B clamp made DISTINCT objects print EQUAL hashes (`commit:b2a7033e`) — an exhibit keyed by a hash whose quantum can collide is prose wearing a digest's clothes (53's 'instrument cannot see what it was built to see', at the address layer); a retraction that then over-corrects the OTHER way (rows declared REAL because a second instrument agrees, `7cb2d897`) is the same inference graded twice, and only the re-run settles it either direction. 61 is the writer-side audit axis (the stamp lives in THIS commit); 63 is its reader-side twin (the VERDICT re-fires at the reader's seat) — a perfectly law-61-compliant message whose evidence can't be re-derived still fails, and fails silently for exactly the reader the chain is meant to serve. Free-mechanism note (the era's cheapest arm): where repeater legs already exist, the law self-enforces at zero runner change — same `ph` ⇒ byte-identical every slot is the faithfulness test, and a disagreement is itself the datum (`e419b226`, ratified `7cb2d897`). **BATCH-6b COUNTER-CLAUSE (key-quantum = claim-quantum, agent2's phrasing, `commit:aff7a5f0`):** content-addressing keys the ARTIFACT, not the CITATION — a content8 survives a dash-corrupted transcription of the message that carries it, a GREP PATTERN does not; the chair's false-zero audit (`--machine` 'absent from landed bytes') was produced by exactly the rot that content-addressing was invented to defeat, and it sent two desks on a bounded detour before the re-run found the flag at `:300-310` in main's own bytes since before the crossing. A law-63-compliant exhibit therefore keys BOTH its bytes and its reader's re-derivation command: a pattern is an artifact too (row 65), and quoting a pattern from memory rather than from the file is law-27's read-not-recalled at the tool layer |
| 64 | **IDENTITY BY ROLE, FRESHNESS BY CLAIM-FILE, PROOF BY RECEIVED-CONTENT — a router addresses a ROLE resolved through a live claim, never a cached instance id (ids die with reboots; roles survive restarts); the claim-file is the FRESHNESS authority and its loss must fail LOUD (suppressed alert = named state, never silence); and a delivery VERIFIED one layer below the reader proves the PIPE, not the WAKE — every receipt names the layer it graded, and the only receipt that reaches the claim is content arriving at the vantage the claim is about** | the pinger's night-rot, same organ as law-54's ghost-hour wearing accounting clothes (`commit:ff1a45a6`, mechanism read at the artifact `~/.pi/agent/tmp/pinger_amd/`): the claim-file held a DEAD pi-session id (106391, killed by the day's reboots) while `wake.mjs`'s role-name fallback kept delivering to a live non-reader (4032c47e) with VERIFIED receipts — measured at the log, the 'delivery VERIFIED (db-signed 8fdc0ac6 -> 4032c47e, 3306 B byte-identical)' rows were TRUE at every layer they graded (db signature: sender integrity, law-54's own cure; byte-identical payload: transport) and SILENT on the one layer the wake claims (a reader woke up). The fallback was WORKING AS DESIGNED — it is what made the rot survivable-and-invisible; the claim-file was the failure. Fix measured at this seat: `coordinator_id.txt` re-pinned to the role name the resolver actually uses (bytes 'coordinator', mtime 23:04Z), a fresh 3001-B payload VERIFIED at 23:05:00Z, and — the loudness clause proving itself — a real 'ALERT NOT SENT: coordinator_id.txt unresolvable/offline' line at 23:03:38 during the re-pin window: the suppression is a NAMED STATE in the log, not a quiet zero (law-42's refusal-routed-as-conviction, arriving at the transport). Genealogy: 54 (liveness is process tables, not text — the ghost-hour) → 4 (wake TEXT refreshes at every phase change — content freshness) → 38 (a filter must match the sentinel its own producer mints — form at the wire) → **64: the ADDRESS refreshes by claim-file, the ROUTE resolves by role, and the RECEIPT is graded at the reader's layer** — each of the four has a distinct failure mode none of the others catches, which is why this is a row and not an annotation. Second, older instance in the same commit's confession: the restart protocol's full-board dispatch failed twice because the chair treated ITS OWN stale context as the board's state — a desk's self-model is subject to the same claim-file law as any router's (identity by name, freshness by claim, and quiet lanes are NOT proof of busy ones — law-6/22's absence rule at the coordination layer). |
| 65 | **A GREP PATTERN IS AN ARTIFACT TOO — a passing grep can NEVER prove a non-existence: the pattern is a byte-string with its own failure modes (dash/unicode rot, dialect, tabs-vs-spaces, the collapsed column), so every presence-of-tool or absence-of-thing claim must be produced by RUNNING the thing and reading rc + output-shape (or by a git-side find-object that walks history for the blob), and the negative control is the RUN, not the string** | the chair's false-zero, re-graded at agent2's seat (`commit:aff7a5f0` rev-7f): the dispatch 'the `--machine` clock cell does NOT EXIST in any landed bytes' was reproducible as a FALSE ZERO from any dash-corrupted transcription (`---machine`, `—-machine`, `' -machine'` all return 0 where `grep -F machine` returns 3), while the flag sits in `tools/v340l/nvfp4/finish_clock_consistency_host.py:300-310` at `56b747e4`, at `ff1a45a6` and at agent5's lane tip — ONE blob `261cd22c` across all three vantages, an ANCESTRY-confirmed crossing (`merge-base --is-ancestor` YES), a LIVE CONSUMER (agent4's production runner `G18r41_pinbisect_agent4.sh:217` fires it; agent3's battery wraps `--machine` legs WITH an impurity falsifier), and a RE-FIRE: main@ff1a45a6's own bytes × r41's committed serve.log ⇒ rc=1, pure one-line JSON, convictions=8 — the bank is reproducible, not merely valid-shaped. Sibling instances of the same half-second: the dispatch's quoted 'rev-7c receipt' sentence resolved to NOWHERE in the landed ledger (zero hits, grep at seat) — **the citation FORM ('your receipt says X') is a column too, and a quote that resolves to no landed artifact is law-50's bare-cite class wearing a mail costume**; and this desk's own batch-6 index credited C63 with killing a family on a leg the bank shows never ran (row 49's batch-6b annotation — same law, opposite error direction: there the artifact was UNDER-read, here the pattern was OVER-trusted). Instrument: agent3's **declared-vs-executed-legs cell** `results/amd/agent3/exhibit_completeness_cell.py` = sha256/8=`95b44a7f` (law-61 form, `commit:68c7adbe`) — RED C63 (missing 66R) + RED C64b (missing 66X, the SAME bash bug at runner :33 — both after-67 repeaters ever attempted were silently lost), GREEN C58 5/5 + C61 6/6, falsifiers both directions (no-decl rc=3; truncated-green indicts); zero-GPU, host-only, ROUTED to `tools/v340l/` for the standing suite by the chair's own ask — which is why this row is a NUMBER and not an annotation: the class now has its cell with both-direction teeth and a red pair banked at real boots. Boundary: 49 (absence-of-string is proof only when the string is pinned SOMEWHERE upstream — 65 adds that the pin must be the RUN, not the transcription), 60 (a count is its column — 65 is the zero-count's twin: a NO-HIT is its pattern), 53 (wired-not-read — 65 is wired-not-read applied to the auditor's own probe), 27 (read-not-recalled — applied to command text). **BATCH-6c STAMP-DRIFT NOTE (law-7's sibling, STAMPED-FILE-DRIFT-THROUGH-APPLY, caught by re-probing my own cite an hour after minting it): the cell named above was `95b44a7f` at `68c7adbe` and has since moved twice at the court's own pen — v2 with the HEALTH line = sha256/8=`0e2ff1a5` @ `commit:7a5f9c60`, standing-battery receipt bank `15a053a2` @ `commit:46b1284f` (four rows measured at my seat: C58 case-mapped GREEN / C61 numeric GREEN / C63+C64b CRASH-PRONE@:33 INDICTED). The rule the §VII candidate has been waiting for, now with a live instance: a landing patch that rewrites a file OTHER rows cite by content8 must re-announce that file's stamp in its own commit — agent3's v2 message DID (`exhibit_completeness_cell.py = 0e2ff1a5` in-message, law-61 form), so the drift was honest and detectable in the same breath; what failed was only my own re-verification interval, annotated here rather than edited out (law-8).** **BATCH-7 PRE-ANNOTATION (roster §VIIa-ROSTER rows 10/12, candidates NOT yet numbered): 65's axis has two more specimens waiting — A PROBE GRID IS AN ARTIFACT TOO (agent2's 8N onset cluster = the 16-slot sampler's own fingerprint, `commit:80545ba7`/`commit:34b875c7`) and A WITNESS PRINT IS AN ARTIFACT TOO (agent4's sync-fix re-grounding §28's block-mirror: an unsynchronized hash observes tearing, `commit:320d90cf`/`commit:40294527`). Held for the numbering pass rather than folded now, per law-67's warning against anchoring a count on the first specimen's grammar.** |
| 66 | **ARTIFACT EXISTENCE IS A PROPERTY OF A PLANE, NOT OF A FACT — a thing's presence answers a FOUR-part question (`disk` in the lane, `git` at a ref, `remote` on the wire, `bank` for content-addressed binaries) and every claim about it must name the plane it checked; `lanDED-TO-DISK ≠ LANDED-TO-BOARD`, so a bank that exists only in a worktree is INVISIBLE to the board and a search that did not sweep the disk plane cannot say 'zero'. Negative claims about artifacts owe the stronger form: check the plane where the claim could have been false, or state which plane was graded and let the answer be NOT-OBSERVABLE there** | the fourth court (`commit:33dafb63`, chair's seat-check 90 seconds after an honest correction): agent2's REV 7i verified at the COMMITTED TREE that no bank contained a 66-after-67 repeater — true of git, and WRONG of the hunt, because `G18r53_c65_*` had been on disk since 23:37Z uncommitted (the first after-67 repeater ever executed, ph-sequence warmup-53/66/67/66R at lines 39/81/123/165; the chair's own re-run of agent3's decider returned rc=0 BRANCH-1 F2-DEAD PLANE-CONFIRM there). Same beat, opposite direction: the court's earlier 'window family dead' claim (`d64704e7`) had rested on a leg that never ran anywhere, disk included — so BOTH errors were plane-confusions, one under-claiming a real artifact, one over-claiming an absent one, and the fix is the same row-grammar: name the plane. Process corollaries the chair filed with it, adopted: commit-before-claim at the sender (a bank's own law-61 stamp makes the disk plane and the git plane the same instant), plane-disclosure at the checker (`verified at <plane>` in the verdict line, exactly as law-36c makes vantage a field). **SECOND OCCURRENCE WITHIN THE HOUR, which is what escalates it from lesson to law** (`commit:c56f3e52`): r53 sat uncommitted AGAIN for ~60 minutes after C65's adjudication, and the chair ordered COMMIT-BEFORE-CLAIM at the sender before any non-trivial claim cites a bank, with the court instructed to record a bank-commit-sha in its frozen-baseline keys once one exists — the plane field is being migrated from courtesy into a required column, which is the difference between a rule and a gate. Genealogy: 36 (a number names its unit) -> 50 (a FILE's authority is where the board can read it) -> 46 (a stamp's currency is its remote) -> 22b (count-census is blind to rewrite) -> **66 (existence itself is per-plane: the census that says 'absent' has to know which of four shelves it looked at)**. Instrument: none armed beyond this row's grammar — the predicate is a field on a claim, not a computation over a tree, and arming a fake one would be 65-style ceremony; the mechanical teeth that DO exist are agent3's completeness cell (declared-vs-executed-vs-artifact-blocks, `commit:7a5f9c60`) plus this desk's arm D, which together catch the git-plane and artifact-plane halves. |
| 67 | **AN INSTRUMENT ANCHORED TO ONE DESK'S LITERAL GOES BLIND (or falsely CONVICTS) AT ANOTHER'S — a parse pattern, prefix, or key form copied from the specimen that manufactured it is a SITE-GRAMMAR, not a class-grammar: the first new vantage that renames a field turns a green cell into a verdict machine that grades its own memory. Row form: an instrument reading OTHER desks' artifacts must state the GRAMMAR FAMILY it accepts (a regex over the shared namespace, not a literal), and its plant set must include a RENAMED-SPECIMEN arm — same content, different tag — in both directions. Boundary with 13/14: those police how you JOIN identity across ranks (join by tag+ordinal, never raw step; a lane pointer separates the tap, not the request); 67 polices how you PARSE identity across DESKS, which is the same collapse one layer up in the tool chain. Boundary with 65: 65 says a pattern is an artifact that can rot at transcription; 67 says a pattern is an artifact that was BORN attached to one desk's bytes — the failure is at mint, not at copy** | measured at the compile seat, both directions, on the court's own artifacts (`commit:03052399`): v2 of `exhibit_completeness_cell.py` matched legs with the literal `^c53leg` — agent4's C66 boot (r54, `commit:f19eb8df`) prints `c66leg` and is 5/5 COMPLETE, so v2 read `executed=[]` and INDICTED a whole boot (a false conviction is the worse failure: it costs a good bank its verdict). Re-runs at this desk: **v2 on the complete r54 specimen ⇒ VERDICT=2 INDICT, executed=[] , triad hkv=6 expect=1 MISMATCH**; **v3 (stamp `89fac1e2`, generalized `^[a-z]+\d*leg`) on the SAME bytes ⇒ VERDICT=0 CONSISTENT declared==executed==5 legs, triad hkv=6 expect=6 OK**; and v3 KEEPS its teeth — on the genuinely broken C63 bank (r51) it prints `INDICT missing=['66R']`, the red row surviving the widening. That last pair is the closure law's shape satisfied at the desk that shipped the bug: widening the recognition did not mute the instrument, which is the only version of 'generalize the fix' that counts. The generalization agent3 filed with it, adopted verbatim as the plant-class duty (also recorded at 60's join corollary): **every comparator's pre-commit arm must include a plant with the EVIDENCE ABSENT, not merely wrong** — the era's comparators have all been tested against corrupted content and none against missing content, which is exactly how `all()` over an empty intersection kept printing GREEN

**BATCH-6 NUMBER CENSUS (law-60's own hygiene applied before minting):** `git log amd/main -40 | grep -oE "law[- ]?6[0-9]"` returns only law-61 usage; the strings '62's'/'64's' in ledger prose are 'slot-62's' and 'bucket 64's' (verified by window-grep, not relayed) — numbers 62/63/64 are unspent and the three rows above take them in order. Refusals this batch (refusal-as-verdict, four, with their homes): **AMENDED AT BATCH-6b — the census's own premise moved:** by the time these rows crossed, the harvest had produced a fifth numbered row (65, the grep-pattern-is-an-artifact law — armed with its cell and both-direction falsifiers, so it EARNED the number where the four refusals below did not) and the index's C63/C64b rows needed their correction (the window family is RE-OPENED, not dead: the decider leg never ran — law-58's sixth instance and law-49's batch-6b annotation carry the episode). Next-free is **66**, not 65. **AMENDED AGAIN AT BATCH-6c (00:5xZ, the court's own three-candidate relay + the chair's fourth-court gift): 65 was spent above, so 66 was taken by ARTIFACT EXISTENCE IS A PROPERTY OF A PLANE (disk/git/remote/bank) — the lesson of the fourth court, `commit:33dafb63`. The court's other two candidates were REFUSED as numbers and folded where they live: candidate 1 (a self-consistent triad is not an exhibit) is law-51's FOURTH CLOCK and landed as its extension; candidate 2 (a print that cannot be read is never a skipped comparison — set-intersection-washes-a-delete) is law-60's JOIN COROLLARY and landed there with its cell named. Candidate 3 (cite-by-sha means after the sha exists) became the fourth ARM of this desk's own gate, `delta_guard_cell` arm D, not a law — the class was already 46/61's, the missing thing was a mechanism, and an arm is the honest answer to a promise no constant had kept (its first calibration convicted four honest digest cites and taught law-36 a lesson at my pen: see 46's batch-6c cell for the measured precision numbers). Next-free after this pass: **67**. **BATCH-6d (court candidates 4-5, `commit:03052399`): 67 taken by AN INSTRUMENT ANCHORED TO ONE DESK'S LITERAL (numbered because it arrived with a RED row and a GREEN row measured at THIS seat, not relayed: v2 falsely indicts the complete r54 boot, v3 grades it CONSISTENT while still indicting the broken r51 for its missing 66R — the widening did not mute the teeth). Candidate 5 (an intersection passes when EITHER side is empty) was REFUSED a number: it is law-60's JOIN COROLLARY with its empty-side armed, and it landed there with agent2's deletion plant and the v3 fix's own numbers. Next-free: 68.**** (1) 'sequence is not a cause' → NOT a number — it is law-58 applied to run-order, and its reusable form is a fire-sheet arm, so it lands as §R9 grammar amendment (counterfactual-repeat clause) in the batch-6 compile doc; (2) the dead-family bookkeeping rule ('re-proposals die on family name before math is read') → 30+58 already own it, the missing piece is FINDABILITY, which is an index, not a law — the index lands at docs/amd/BOOT_CHAIN_INDEX_C42_C66_agent1_2026-09-16.md; (3) retraction OVER-correction (the clamp retraction swung to 'rows are REAL' and needed the chair's re-grade) → refused as a number, 58 ALREADY grades retractions by its own explicit clause — folded as 58's sixth instance in the compile doc rather than re-minted; (4) 'a grep needs its column' → already spent as row 60; the night's exhibit-chain instances are born-forward as row 60's inbound annotation, not a new number.

**DECISION-LAW FAMILY INDEX (SD-series — user-sourced, dated, FINDABLE ten sprints out; these are DECISIONS about direction, not catches about defects; drift signature ships with each):**

| # | **DECISION-LAW** | ANTI-DRIFT CLAUSE |
|---|---|---|
| SD-1 | **THE NORTH STAR (user, 2026-09-15 11:1xZ): the project's goal is NVFP4 FULLY OPERATIONAL ON THIS HARDWARE AT TP4. Not q3. Not w2.** q3 ladder + w2 controls are RISK-REDUCTION FOR the goal, never the mission; ACCEPT A4/@w4 is the row that matters (definition-of-done = new mint, 25/25 determinism, human coherence, ruling4-concat formula verbatim) | any sprint polishing q3 throughput/parity/breadth while NVFP4-at-TP4 waits IS the drift — re-read the banner box (SPRINT_STATE_RESUME header / COORDINATOR STATE 11:2xZ / ledger 95749104 — three durable homes) |
| SD-1a | **CHEAP FALSIFICATION BEFORE EXPENSIVE ENGINEERING (the reusable half, banked beside SD-1):** every untested assumption that a one-boot/one-grep/one-read answer prices must run BEFORE the code that assumes it (the honest-first boot preceded the SIMT ports by one boot and zero new code; it returned 'loader NOT the risk, route-table IS' and re-priced the queue) | if engineering is hours deep while a zero-cost discriminator for its central assumption sits unrun, that's SD-1a drift |
| SD-1b | **FIT-LAW (hardware necessity under SD-1, banked 13:1xZ):** NVFP4 needs all 4 dies — 16.4 GB planes don't fit a 2-die budget — so **TP4 is the MINIMUM VIABLE WORLD for the mission**, not an optimization target; and the terminology pin: user's 'tp2-boards' == our world=4 (two dual-die boards), written once so the vocabulary never forks a roadmap |

**CENSUS-CHECK of the chair's numbers (seq-25 ask, registry truth):** the compile's table stopped at 42 — numbers 43-47 existed ONLY in ledger prose (bae7c0fe '43', 511c2b6b 'PHASE-AXIS RULE' and '46'/'47' usage from 2a8a82bb/d0d20f37/19754e17) with no register rows: the chair's 45 (phase) and 46 (moving-stamp) CONFIRMED as the numbers already in circulation, so rows land to match the ledger — no renumbering, no mis-quote to annotate: the ONLY gap was the register's own lag (law-43-class, an instrument outside its graded region: rows not written when numbers were cited — this pass closes it). 'Law-47' as cited in three merges = the oracle-associations law (row 47 above), lands correctly. 35 and 43 are never-tabled numbers this pass repairs; 48/49 bank the two candidates from the chair's seq-25 with their worked instances. [COMPILE-PEN NOTE: rows 43-49 written from agent1's desk under chair seq-25 commission; every cite above re-resolves at origin/amd/main tips — verify-at-use, law 46 applied to this very row.]

Family genealogy in one line each: row 7 (values-not-assumed: no remembered numbers) ->
row 22b (count-census blind to rewrite: coverage units are not recency units) -> rows 20/23
(band alarms: named failure regions; a threshold with no stated quantum has an unnamed one) ->
row 36 (the unit itself: every number in a gate row carries its quantum or the gate lies).
The maturation signature the chair named: each desk hit the class twice this morning, once in
someone else's instrument and once in its own — which is what a LAW, as opposed to a habit,
looks like from the inside.

**BATCH-6 EXTENSION of that genealogy (the evening's line, rows 53→65, one clause each):**
53 (wired, not read: a script needs a witness from another organ) -> 54 (liveness is process
tables, not text) -> 55 (frozen bytes can't un-disagree) -> 56 (claim and falsifier minted
together) -> 57 (the delta, not the intent) -> 58 (the bank refutes the mapping — and it grades
retractions too) -> 59 (a clean symptom never exonerates a dirty state) -> 60 (a count is its
column) -> 61 (the audit lives in THIS commit) -> 62 (members are bodies, not lengths — 60's
object-keyed sibling) -> 63 (an exhibit must re-run at the READER's vantage — 61's reader-side
twin) -> 64 (identity by role, freshness by claim-file, proof by received-content — 54's ghost
arriving inside the machinery that keeps the board awake) -> 65 (a grep pattern is an artifact
too — a NO-HIT is its pattern, and the re-run an exhibit owes must be a RUN, not a transcription).
The compression, one floor under the
morning's: the morning learned no single LAYER can be trusted; the night learned no single
VANTAGE can be trusted — every 6x row is a duty assigned to a seat (writer 61, reader 63,
grader 60/62, router 64, auditor 65) — 66 adds that the ARTIFACT's existence is a plane, not a fact (disk/git/remote/bank), which is the seat the auditor stands at, and 67 adds that the GRAMMAR an instrument reads by is another desk's dialect, which is the seat the writer sits in. Not to a file.

**META-PARAGRAPH (chair seq-33 suggestion taken): the era's arc in one pair of timestamps — the register acquired a board-readable home at 15:2xZ (5be9e2ac, law-50's worked instance) and the FIRST GREEN NVFP4 DECODE MINT was graded at 15:3xZ (C44, d886c933: paired 463eac76 at 1k, ruling4-concat printed pre-mint on its first content-bearing child, cross-format divergence at token 0 per the expect-profile grade DUEL_GRADING_C44). Four hours apart because the laws were banked before the event they describe — a compile that lands the day its subject's first data arrives is the register doing its job, and the board should note which order the artifacts came in: authority, then home, then silicon.**

## VII. RE-VERIFICATION RECORD OF THIS DOCUMENT'S OWN DISPATCH (AMENDED SAME-DAY,
chair computation @ seq-67 — the amendment carries the compile's own error, law 8 form)

ORIGINAL FINDING (this desk, ~02:4xZ): the seq-64 order's four artifact-cites — 468a78e5,
f6d62fae (+1083ae8f/e2edc796 inside 66c01799's prose) — failed `git cat-file -t` in three
object DBs; I resolved all four by file-traversal to live homes and filed them uniformly as
"aged shas, content live." **THAT UNIFORM FILING WAS WRONG IN TWO WAYS, both caught by the
chair computing at main tip with the CORRECT probe kind:**

1. **PROBE-KIND LAW (the mechanism restated, this is the durable content):** a content8 stamp
   is sha256-of-FILE-CONTENT truncated to 8 hex — `git cat-file` CANNOT resolve it BY
   DEFINITION (it is not an object id; it names bytes, not a tree entry). The three-probe law
   therefore begins with a probe-KIND identification: commit/blob cites -> cat-file/rev-parse;
   content8 stamps -> `git show <ref>:<path> | sha256sum | cut -c1-8` at the NAMED path+ref.
   Measured with the right kind: 468a78e5 RESOLVES (battery, tools/v340l/tp4_arming_battery.sh)
   and e2edc796 RESOLVES (pad arm) — my "all four aged" over-claim aged two LIVE stamps by
   probing them with the wrong instrument. Same failure shape as the numbers-the-row-reads-
   wrong class, aimed at the verifier this time; named in my own compilation, no editing of
   the original error out — the row stands with its correction beside it.
2. **TWO DISTINCT STALE CLASSES (not one):**
   - **STALE-BY-DESIGN-SUPERSESSION** — 1083ae8f (counter-join content8 at 66c01799's prose):
     superseded BEFORE landing by the grammar fix; main bytes are 2527c93b, and 098f417d's
     landing message NAMED the true stamp — the supersession was honest and recorded; the
     prose row that kept the old number is the erratum, not a phantom. Cure: announce-at-landing
     names the FINAL bytes (done right here by the mechanism, the defect is only in the older
     row's residue).
   - **STALE-BY-PATCH-EVOLUTION** — f6d62fae (ring cell): f6d62fae WAS the true 13-arm beat-1
     cell at its cite-date (9837c576's cite was TRUE then); the beat-3 LANDING PATCH REWROTE THE
     CITED FILE (181 lines, F1-F5 arms) and main bytes are NOW 334b3cec. New law candidate,
     row-27's family, name it **STAMPED-FILE-DRIFT-THROUGH-APPLY: a landing patch that rewrites
     a file other rows cite by content8 MUST RE-ANNOUNCE that file's stamp IN ITS OWN COMMIT**
     (the bank-law's filename==sha discipline, applied to content8-cited source files — the
     mechanism today is prose-discipline, the cell-candidate is a CI arm: any commit touching a
     file listed in a cited-stamps registry (the register row of content8s: gate family,
     battery, ring, cells) fails review without an updated registry row in the same commit).
   **RUNTIME DAMAGE: ZERO, re-verified and unchanged** — no cell or battery pins these stamps
   in expectations; batteries run BYTES, not hashes. The damage surface is prose, and the
   compile caught it before propagation — the document proving its own thesis inside its first
   hour, then being corrected on its own first two claims inside its second. Both halves are the
   row; the era's law is that both halves get PRINTED.

CURRENT-OF-RECORD (main tip, sha256-of-content first-8, computed at chair seat 02:3xZ,
file-path-of-record named with each):
| artifact | content8 NOW | path |
|---|---|---|
| arming battery | 468a78e5 | tools/v340l/tp4_arming_battery.sh |
| ring-guard cell | **334b3cec** | tools/ops/check_ring_deref_guarded.py |
| pad-region arm | e2edc796 | tools/v340l/nvfp4/pad_region_census_arm.py |
| counter-join cell | 2527c93b | tools/v340l/nvfp4/r1_counter_join.py |

(Related same-era correction to this section's first printing: I wrote that the stale-sha
pattern was "consistent with rebase/merge churn" — for f6d62fae the churn is the landing patch
rewriting the file (mechanism named above); for 1083ae8f it is pre-landing supersession; the
generic-churn sentence was the lazy fifth member of the plausible-structure family and is
struck with its name attached.)

-- file: docs/amd/LAWS_OF_0914_0915_agent1_compile.md, amended same day as written; the
   STAMPED-FILE-DRIFT-THROUGH-APPLY CI-arm candidate is handed to agent3's cell queue, NOT
   self-assigned to the fire path.
