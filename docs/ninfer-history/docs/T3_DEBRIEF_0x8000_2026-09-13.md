# T3 debrief — the 0x8000 boot fault: complete diagnosis + fix plan (session #N handoff, 2026-09-13 ~01:0xZ)

**Audience:** the fresh session resuming T3. Entry chain: this file → `docs/amd/T3_HANDOFF_DEBRIEF.md` §5 addenda (esp. 4 and 5) → agent4's `results/amd/p3/G17d_serve.log` + their three-delta note (intercom seq-24, content-anchored).
**Status at handoff:** G-AMD-17d is ONE MECHANICAL SWEEP from first-token. Diagnosis is COMPLETE at mechanism level and PROVEN at parse level; the fix is authored-in-draft (`results/amd/T3_ldmatrix_guard.draft.patch`, uncommitted BY DESIGN — committing the guard alone would red-build every lane's unconverted files). The open decision is custody of the sweep files, asked of the coordinator in my seq-76-adjacent message; if unanswered, that ask is this session's first relay.

## The defect in one paragraph
G-AMD-22 ruling (A) changed the HIP ldmatrix emulations' token from `unsigned` (smem_addr's truncated generic pointer — undereferenceable on this box, ROCm 6.2 has no CVTA) to a full 64-bit generic pointer, sweeping the then-live consumers: `mma.cuh` itself, gating `gemm_mma.cuh` ×3, chunked `output.cuh` ×2, `prepare_wy_wu.cuh` ×2. It did NOT sweep the rest of the fleet — and agent4's b2dd680c whitelist wave just made q4/q5 GEMMs HIP-live. `smem_addr()` returns `unsigned`; an **unsigned → 64-bit implicit widening is silent at parse** and reconstructs the truncated pointer. AMD's generic LDS window is based at `0x6000_0000_0000`, so a truncated dynamic-smem pointer zero-extends to exactly the `0x8000`-class address G-AMD-17d faults on. The boot's last traces (`[gating] unsplit@cols=51 ×2`) are HOST-side launch prints; the gating kernel itself is (A)-clean (see evidence) — the faulting kernel is the NEXT one in the queue: the newly-live q4/q5 GEMM (async launch; first sync reports the earlier death).

## Proof chain (all bytes, re-runnable)
1. **b2dd680c carries (A) for my files:** `git show b2dd680c:src/ops/common/mma.cuh | sha1sum` = `86dcd694…`; `…/bf16_gdn_gating_proj_gemm_mma.cuh` = `57623881…` — both equal the registered post-(A) pins. Gating tokens are correct 64-bit.
2. **Unconverted sites still feed `smem_addr`:** e.g. `b2dd680c:src/ops/linear/q5/q5_rowsplit_gemm_mma.cuh:360` and `:367`, `q4_rowsplit_gemm_mma.cuh:316/:323` — printed verbatim in-thread.
3. **The guard catches it at parse:** applying `T3_ldmatrix_guard.draft.patch` (deleted `unsigned`-token overloads in the HIP section of mma.cuh) and compiling q5 → `error: call to deleted function 'ldmatrix_x4'` at :360. Implicit widening blocked ⇒ every un-migrated site becomes a named compile error. (My q4 compile attempt hit agent2's cuda_pipeline bare-chain instead — already fixed on main; compile at b2dd680c/main base, not at my 5254d1d5 worktree, to keep chains clean.)
4. **Not the wave64-mask theory:** the shim's 3-arg `__shfl_sync` pins width=32 logical-warp semantics (hip_shim/cuda_runtime.h:505-512, exhaustively verified per its own comment) and FRAGCELL K2 ran the real 64-bit-token shuffle green. The emulation is sound; the TOKEN TYPE is the seam.
5. **Not G17f redux:** cols=51 Stages=2 launches fine (40,960 B ≤ 65,536 readback; agent4's verify-arm silent-pass confirmed the arm math).

## Worklist (measured at b2dd680c; `ldmatrix_*(` calls whose token flows through `smem_addr`)
- b2dd680c-live (q4/q5 wave): `q4_rowsplit_gemm_mma.cuh` ×2 (:316,:323), `q5_rowsplit_gemm_mma.cuh` ×2 (:360,:367), `q4_linear_swiglu_gemm_mma.cuh` ×2 (:180,:186), `q4_small_t_mma.cuh` ×1 (:165), `rowsplit_grouped_mma.cuh` ×2 (:300,:306), `w8_rowsplit_gemm_mma.cuh` ×2, `w8_pair_gemm_mma.cuh` ×2, `w8_small_t_mma.cuh` (via `w8_gdn_input_gemm_splitk.cu:184`), plus q6/fp8/bf16_gemm/nvfp4-TMA/swiglu-nvfp4 families (2-5 each).
- CUDA-only-guarded (verify, likely inert on HIP): the gqa TC/kvarn/i8/nvfp4 decode+prefill bodies, bidirectional, vision, sparse_moe (~20 sites in one .cu).
- **The guard makes this list self-maintaining:** land guard + sweep atomically; any future un-converted site refuses to compile instead of faulting at 0x8000. That is the (A)-seam lesson generalizing: *contract changes need compile-refusal mechanisms, not remembered lists.*

## Fix plan (atomic commit, ~30 min of work, all mechanical)
1. Guard: apply `T3_ldmatrix_guard.draft.patch` to mma.cuh HIP section (my custody; keep the comment naming G-AMD-17d as the motivating fault).
2. Sweep: wrap each live token `smem_addr(X)` → `NINFER_LDM_ADDR(X)` at every site the guard flags (macro defined in mma.cuh namespace body; CUDA expands to the verbatim `smem_addr(p)`, so mainline text semantics are preserved by construction; add the macro-visibility include check per file — all consumers already include the mma family).
3. Custody: ask the coordinator to assign the q4/q5/swiglu/w8 files (asked in seq-76 window; they are product/mainline files, NOT in T3's roster — author-by-patch-for-agent4 was my offer).
4. Acceptance, widened: (a) guard-compile the FULL serve set at the swept tip — zero deleted-call errors = fleet-complete; (b) FRAGCELL gains a `blockDim=512` multi-warp leg (kills the remaining "cells ran single-warp" gap between K2 and production geometry) + a negative cell asserting the deleted overload fires (mutation-tested, agent2's discipline); (c) agent4 re-fires 17d with their three deltas (Stages-2 launcher / ws-warrant override / q4q5 whitelist — all already in b2dd680c; repro kit command preserved in `G17d_serve.log` head, ART EMTEC256, ws96/ctx64/kv64/chunk128/no-prefix/prefixcap256/no-cuda-graph). Expect (b) needs a dev2 stamp (~3 min, G-AMD-25 ask pattern: window law, zero-KFD precheck, own-pid, stamped header with artifact shas + ref).

## Lane state at handoff (everything else CLOSED)
- Tree `/home/chris/worktrees/amd-wo-q3hip` @ `5254d1d5`+`0b2130a8` (log row, ff-merged into main as 698360be); worktree CLEAN (guard reverted to patch file); all five T3 refs ancestors of `amd/main`.
- Parity: decode 12/12, prefill 8/8×12/12, transferability re-measured on merged tree (G-AMD-24, digit-for-digit). K1 bit-exact, K2 BIT-IDENTICAL, B1/B2/B3 PASS. Registration package 11 files DONE and landed (gemini d5774709 (verified: feat(gate) registration commit)); my caveat retraction + agent2's comment-genealogy retraction both filed — thread closed.
- Standing parked asks: B4 chunked parity = one plain window (no redesign — smem table in addendum-5); FP32-acc-plane product ruling (STOP-ASK, unscheduled); warp.cuh direct-hip-include fix (post-merge, coordinator dispatches, exception-row-deletion = receipt); agent5-sweep dispatch targets on my six gdn/gating files (answered by custody).
- Night's laws the resumer should NOT renegotiate: cos+role-count is the parity bar (guards = outlier detectors only); cite refs not numbers, method+scope with counts; re-derive at point of use (including my numbers here — they're months-old at most by tomorrow, but the pattern held); pre-launch re-hash caught a stale binary TWICE tonight; STOP-ASK before product edits without a ruling; never branch around a wavefull instruction (agent2's shim doc).

— agent3's held lane; the fingerprints the coordinator mentioned are in §5's addenda, and the last one is this file.
