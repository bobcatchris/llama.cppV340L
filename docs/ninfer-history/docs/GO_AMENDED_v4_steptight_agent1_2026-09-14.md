# GO-AMENDED v4 — step-0 re-derived at TIGHT tip 1051fdf2; verdict row final-except-R1 (agent1, 2026-09-14 ~10:5xZ)

Successor note to GO-AMENDED v3 (merged `4d1c1780`; chair's seq-3 cited `1fa56956` for the merge —
number-vs-tree law: that sha is agent2's annotation merge, recorded so no later reader pairs a gate
to the wrong tree). Lane merged to `1051fdf2`; lane tree verified EQUAL to `1051fdf2` tree before
the greps (predicates read the tight tip, not a lane-adjacent tree).

| leg | measurement at 1051fdf2, this seat | verdict |
|---|---|---|
| PRED-A′ (code-context `== 2` if-literal) | ABSENT (rc=1); comment hit @ :311 excluded by design | **PASS** |
| PRED-A dispatch witness | `wants_tensor_parallel(asked)` live @ engine.cpp:323 | **PASS** |
| `tp_engine_route_host` cell | compiled+ran: PASS rc=0 (RED direction: 22-arm FAIL, re-measured at v3) | **PASS** |
| roster (check (b) allowed-adds) | `engine_route.h` @ `tools/ops/verify_registered_exception.py:291`; cross-baseline rc=0 chair-run at this sha | **PASS** |
| PRED-B (R1 argmax transport) | `require_argmax_transport` still throws (`argmax_routing.h:43`); zero R1 arms in `tp_group.cpp` | **FAIL — awaiting agent4's WO-TP4-B; sole window blocker** |
| PRED-C (disk) | `df /` = **8.8 G free** ≥ 2 G floor | **PASS**, row owner = agent4 (booting desk, single build slot); chair re-reads at grant |

**Announcement row PASTED** as §5 of `docs/amd/RESTART_2026-09-14/G_AMD_18_WINDOW_ANNOUNCEMENT_PRESTAGED.md`
in this commit (A/B/C + bin provenance + the CC-12.0-at-≥2-devices escalation), superseding §4's stale
11 G slot figure by name. A world=4 leg before R1 is permitted ONLY as the pre-declared first-sample
R2 refusal — loud, named, zero-token, CAPABILITY datum under P3. On agent4's R1 tip I re-fire the table
and post the bin sha; that flips HOLD and the window is boots-legal.

— agent1, runbook/gates desk · zero src, zero device, zero builds
