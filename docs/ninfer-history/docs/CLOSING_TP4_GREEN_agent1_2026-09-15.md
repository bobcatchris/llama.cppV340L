# CLOSING — TP4-GREEN: the era's acceptance, in its own evidence

agent1 ([C] design authority), 2026-09-15 ~08:5xZ. Co-sign sent to chair per protocol;
C6 is the chair's pen: **C6: chair-signed, ledger ref f4603ae3 (pivot bank; the conjuncts
sheet is the release row this table counts).**

## THE SENTENCE

**TP4-GREEN, mechanism-consistent form:** the organ is cured (r24 gates green under two
independent readers — GP/window/carry bit-exact, state qmax 56.1→1.0 exactly-one-quantum,
t0 760 both prompts); the stream is cured (w4: 26/26 deterministic one-digest English
opening at the golden's own words, one named near-tie fork AT the physics floor — agent4's
phrase stands as written: "the difference is the world, exactly and only as the
accumulation-width theory quantifies it" — the era's cleanest negative control); the oracle
is invariant (w2 boot on the fixed binary: 26/26 legs digest EXACTLY 348e77a1222dea7f —
the (b) trajectory bar, physics-barred at w4, MET verbatim at w2: the bar was never wrong,
it was world-mismatched, and the pair of its w4-FAIL + w2-PASS is what proves the physics
claim instead of asserting it).

## THE EVIDENCE CHAIN, END TO END (one line each; every line banked)

1. Soup deterministic across 4 fresh processes → transport was always the suspect class to eliminate first.
2. [B]/r15: exchange exonerated at full population (498/498 joins, band 0 violations, winner-recompute 0).
3. step-0 zeros: closed by TUPLE ARITHMETIC (the triple (0,0,0) is not in the fold's output space) — print-path race, cured before the verdict needed it.
4. §4b write-order: closed statically (total writes, one stream) AND empirically — the read-before-write family retired with its signature table.
5. Prefill KV geometry: closed per leg, with row #19's /2 excavated from the exact organ — the hypothesis had already fired there once and been cured structurally.
6. Grade-0 reference: w2 opens 'The' 50/50, 1354 NEVER — the indictment pre-armed from banked bytes before the fire.
7. r21→r21b→r23: cold ladders recovered (past the rewrite trap), leg-A 3640/3640 clean, tail-dirty = propagation not E2; LE bit-clean + L0-dirty = entry-localized.
8. The plane map: GP 100% bit-exact under a DISCOVERED 512-block channel map (concat banned by measurement), window content+roll exact, carry 16/16 — and the recurrent state BIT-DISJOINT with a calibrated control reading 100% on the clean plane.
9. The site: the map constrained the effect; agent4's walk found the loader gather table carrying w2 literals — fix 4798829a, gates green 464c5e1a, second-reader verified qmax-correct independently.
10. The acceptance re-band: C1/C2 visible, physics-noted, role-based replacement graded on real banked bytes BOTH ways (w4 profile: MECHANISM-GREEN; w2 pivot: 26/26 EXACT) — the six conjuncts close with C6 the chair's.

## NAMED RESIDUE (post-green, out of gate, not forgotten)

- **Near-tie audit**: per-fork top-2 logit gap ≤ ULP-reach, from extended CL dumps — proves the 129-fork IS the near-tie the theory predicted, not merely consistent with one.
- **59-token completion run**: GEN past the mint's truncation — the full-stream golden as the stronger forward bar for everything after this era.
- **Bulk-collective residue** (§5e Q7 footnote): champion-wire verified at 13/13, leg-A at 64 layers; the widened-transport witness rides any future package-topology change.
- **The law rows the day left in the registry**: 36/36b (unit quantum + assignment form), 37 (extremes vs distributions), 38 (sentinel-prefix), 39 (single-rule dialect), 40 (skew both jaws), 41 (map-effect, walk-site), 22b/22c/39b — each with its self-caught instance; and the closing grader's own three bugs, self-reported with the row-IDs-are-the-contract capstone, all on the SUCCESS path, which is where the discipline earns its keep.

## THE LAST LINE, AS THE BOARD WILL QUOTE IT

The six-conjunct table **failed loudly where it was wrong** instead of passing quietly where
it was ambiguous — and when the wrongness turned out to be physics rather than pathology, the
table said so in its own NOTE rows, kept the failed bar visible, and let the oracle-side
verbatim PASS do the proving. That is what an acceptance decided by physics-verification,
not goalpost-moving, looks like in print: qmax 56→1 is its evidence; 26/26 on both sides of
the same digest is its certificate. — TP4: **GREEN, with residue named and the register holding.**

— agent1, pi 01a0a25e-66c9 · closing doc for the chair's co-sign and the merge wave · out, at last, on a green.
