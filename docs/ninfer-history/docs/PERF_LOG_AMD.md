# PERF LOG — AMD line performance record (APPEND-ONLY, HASH-CHAINED)

**Law:** append-only like BUGTRACK — corrections are NEW entries citing the old one; nothing
below is ever edited or deleted. **Phase-axis law:** every rate names PREFILL or DECODE;
columns never mix. Each entry carries: UTC-ish timestamp, phase(s), one-line config tag,
metrics (measured only — no estimates, VRAM law), provenance (row file or doc:line), and
`bin:` the sha16 of the serve binary measured. `hash:` = first 16 hex of
sha256(prev_hash + '\0' + entry-text) — a tamper chain; verify with
`python3 tools/perflog_verify.py` (to be added; until then recompute by hand).

### PLOG-001 [2026-09-14 ~M1 window] phase=DECODE — w4-era GAMD35 read
- metrics: decode drift 4.80 -> 2.0/2.3 tok/s (-52/-58%) with elapsed time; prefill 14.9 -> 9.8 across dial states
- provenance: docs/amd/M1_GAMD35_READ_agent3_2026-09-14.md:34
- bin: n/a (banked read)
- hash: df219cf3b7cf271d (prev=GENESIS)
### PLOG-002 [2026-09-15] phase=PREFILL — A4 40k leg curve
- metrics: prefill 41 tok/s at 4% depth -> 21.0-21.7 tok/s through the 9-10k band (decay law, cause unnamed)
- provenance: docs/amd/A4_RUNBOOK_SD1_agent5_2026-09-15.md:300
- bin: n/a (banked read)
- hash: 6296774bd6de847a (prev=df219cf3b7cf271d)
### PLOG-003 [2026-09-15] phase=PREFILL+DECODE — NVFP4 plan sheet
- metrics: nvfp4 shallow prefill 23.4-25.8 tok/s @ plen 54/85 = q3 PARITY; 1k decode window 8.2 tok/s; L-10k deep row pre-designed, never fired
- provenance: docs/amd/NVFP4_AMD_PLAN_agent5.md:862-863
- bin: n/a (banked read)
- hash: 9f5424740c18bee8 (prev=6296774bd6de847a)
### PLOG-004 [2026-09-15] phase=DECODE — w4 sprint state
- metrics: w4 decode 7.07-7.10 tok/s @1k ctx
- provenance: docs/amd/SPRINT_STATE_RESUME_2026-09-15.md:21-26
- bin: n/a (banked read)
- hash: 156dbc4cb3f44b44 (prev=9f5424740c18bee8)
### PLOG-005 [standing] phase=AR-PHYSICS — coordinator measured facts
- metrics: host-staged allreduce RTT 98-101 us FLAT with size; ~130 AR/step unbatched = ~13 ms/step dead; 2-4-layer batching -> 3.3-6.5 ms; collective COUNT is the budget
- provenance: docs/amd/COORDINATOR.md measured-facts block
- bin: n/a (standing)
- hash: 925d8d01645af99a (prev=156dbc4cb3f44b44)
### PLOG-006 [2026-09-16 17:5x CDT] phase=PREFILL+DECODE — E-16 north star, NON-MTP graphs-OFF
- metrics: plen=10000: prefill 1029.78 s = 9.7 tok/s mean (14.5 early -> ~9 deep); decode 26.87 s / 69 tok = 2.57 tok/s; grade PASS
- provenance: results/amd/coherence/E16_10k_row.txt
- bin: b864220912aec151
- hash: 5145e84cfb909358 (prev=925d8d01645af99a)
### PLOG-007 [2026-09-16 19:0x CDT] phase=DECODE — MTP1: MTP k=2 graphs-OFF, first MTP boot ever
- metrics: count600 plen67: decode 99.50 s = 6.03 tok/s, acc=0.97 tok/round=2.94 (487 ms/round); fast ladder 14/14 PASS; real-prompt acc 0.53-0.64 (t/r 2.1-2.4)
- provenance: results/amd/coherence/MTP1_row.txt + MTP1_serve.log
- bin: b864220912aec151
- hash: 0bf5f0998a1dc04a (prev=5145e84cfb909358)
### PLOG-008 [2026-09-16 19:1x CDT] phase=DECODE — MTP2: MTP k=2 GRAPHS-ON (drop --no-cuda-graph)
- metrics: same request: decode 58.35 s = 10.28 tok/s (286 ms/round) = 1.70x vs graphs-OFF; spot BLUE@66 exact, water coherent
- provenance: results/amd/coherence/MTP2_row.txt
- bin: b864220912aec151
- hash: b5a98ddffa6eb5d3 (prev=0bf5f0998a1dc04a)
### PLOG-009 [2026-09-16 19:1x CDT] phase=DECODE — BASE2: non-MTP GRAPHS-ON, same-shape baseline
- metrics: same request: decode 180.32 s = 3.33 tok/s; MTP k=2 + graphs = 3.09x over non-MTP + graphs
- provenance: results/amd/coherence/BASE2_row.txt
- bin: b864220912aec151
- hash: 8d7cf1e1ad7d9934 (prev=b5a98ddffa6eb5d3)
### PLOG-010 [2026-09-16 19:38 CDT] phase=PREFILL+DECODE — MTP3: 10k NORTH STAR on MTP k=2 GRAPHS-ON
- metrics: plen=10000: prefill 939.25 s = 10.65 tok/s mean (1.10x vs E-16); decode 17.88 s / 63 tok = 3.52 tok/s @10k ctx (1.37x vs E-16 2.57); acc=0.69 t/r=2.42; grade PASS (stop, 'BLUE', zero mojibake)
- provenance: results/amd/coherence/MTP3_10k_row.txt
- bin: b864220912aec151
- hash: cd46ba108e06f3c2 (prev=8d7cf1e1ad7d9934)
### PLOG-011 [2026-09-17 ~01:00 UTC] phase=DECODE — TIMING1: B1 8-phase round breakdown, count600, MTP k=2 graphs-ON
- metrics: 211.95 ms/round (mean 203 rounds): Target Verify 195.34 ms (92.2%) OWNS the round; AR Draft Chain 7.61 ms (3.6%, ~76-78 collects @98-101us law), Alignment Fwd 8.56 ms (4.0%), bookkeeping phases 2+3+4+6+7 sum 0.45 ms (0.2%); decode 43.26 s / 600 tok = 13.87 tok/s, acc=0.97 t/r=2.94; BLUE@66 exact PASS; same-shape MTP2 anchor 286 ms/round — variance noted, not averaged
- provenance: results/amd/coherence/TIMING1_row.txt + TIMING1_serve.log:111-124
- bin: 07ad7eccc0b97cc0
- hash: 2eb27282f9d4276b (prev=cd46ba108e06f3c2)
### PLOG-012 [2026-09-17 ~01:2x UTC] phase=DECODE — TIMINGK1: k-sweep leg k=1, count600, MTP graphs-ON
- metrics: 169.70 ms/round (mean 301): Verify(T=2) 160.97 ms (94.9%), AR Draft Chain 0.01 ms; decode 51.30 s / 600 tok = 11.70 tok/s, acc=0.98 t/r=1.99; BLUE exact PASS. k-table: 85.3 ms/tok vs k=2 72.1 — k=2 wins; verify T-sweep slope ~34.4 ms/position, intercept ~126.5 @T=1; OPEN: non-MTP step 300 ms vs T=2 verify 161 ms unexplained
- provenance: results/amd/coherence/TIMINGK1_row.txt + TIMINGK1_serve.log:123-138
- bin: 07ad7eccc0b97cc0
- hash: 87b25ca741589c59 (prev=2eb27282f9d4276b)
### PLOG-013 [2026-09-17 ~01:5x UTC] phase=DECODE — TIMINGK3: k-sweep leg k=3, count600, MTP graphs-ON — k-table complete
- metrics: 262.10 ms/round (mean 153): Verify(T=4) 237.43 ms (90.6%), AR Draft Chain 15.49 ms (5.9%); decode 40.37 s / 600 tok = 14.86 tok/s, acc=0.96 t/r=3.90, p_full=0.942; BLUE exact PASS. k-table: k1 169.70/1.99/11.70, k2 211.95/2.94/13.87, k3 262.10/3.90/14.86 (ms/round, t/r, tok/s); marginal 22.5 -> 19.1 tok/s — k=3 still pays on synthetic; verify T-sweep slope grows 34.4 -> 42.1 ms/position beyond T=3, intercept ~126.5 @T=1
- provenance: results/amd/coherence/TIMINGK3_row.txt + TIMINGK3_serve.log:104-117
- bin: 07ad7eccc0b97cc0
- hash: 636535bb0732a447 (prev=87b25ca741589c59)
### PLOG-014 [2026-09-17 ~01:5x UTC] phase=DECODE — REALPROMPT: real acceptance datum on best-k (k=3), "why is the sky blue" mt=300
- metrics: finish=stop, 186 tok, decode 18.86 s = 9.86 tok/s @ k=3; acc=0.54 t/r=2.62 p_full=0.366 (counting shape was 0.96/3.90/0.942) — the expected 0.5-0.7 band confirmed; prose clean, zero mojibake; counting-shape k-table does not transfer 1:1 to prose (draft-head acceptance collapses on real text)
- provenance: results/amd/coherence/TIMINGK3_row.txt + TIMINGK3_serve.log:178-191
- bin: 07ad7eccc0b97cc0
- hash: 1a25fff89802b9b6 (prev=636535bb0732a447)
### PLOG-015 [2026-09-17 ~02:1x UTC] phase=PREFILL — PREFILL-DECAY: per-chunk curve extracted from MTP3 10k log, two regimes + req-2 contradiction
- metrics: req 1 (plen 10000, chunk 128): PLATEAU 24.0 tok/s flat through tok 1152 -> KNEE 17.7->11.5 across tok 1280-2176 (~10 s wall) -> SLOW settles 12 -> 9.3 by 9984 (slope ~-0.4 tok/s per 1k); overall 10.65 (PLOG-010). REQ-2 (zombie dup, solo, 13 s after req 1, kvarn reset between): SAME shallow depths at 9.7 mean (8.9-10.2), no plateau -> depth alone does NOT set prefill rate; candidate states: thermal/clocks, KV-pool state post-10k, page dirtiness. Falsifier designed: cold boot -> immediate ~1.2k prefill -> immediate re-fire + rocm-smi sideband
- provenance: results/amd/coherence/PREFILL_DECAY_MTP3.txt + MTP3_serve.log:48-166
- bin: b864220912aec151
- hash: ac3d93bdb2f81017 (prev=1a25fff89802b9b6)
### PLOG-016 [2026-09-17 ~02:4x UTC] phase=PREFILL+DECODE — TIMINGPF: decay falsifier DECISIVE — regime is load-duration/thermal STATE, not KV depth
- metrics: plen=1279 x2 back-to-back, cold boot: run A flat 24.5->23.9 tok/s all 10 chunks (prefill 52.73 s, decode 12.0 tok/s); run B same depths: flat 24 to tok 640 then knee 16.2->13.6 (prefill 70.03 s, decode 5.5 tok/s); sideband edge 32-40C cold / 67-77C post-A / 84-85C post-B; MTP3 knee (~60 s in) consistent. Depth does not set the fast regime; ~45-60 s sustained TP4 load trips progressive slowdown; DECODE also thermal-sensitive (12.0 vs 5.5 same request) — deep-ctx decode crisis confounded by thermal. Next: during-load clock loop, cool-down re-fire, forced-clock A/B
- provenance: results/amd/coherence/TIMINGPF_row.txt + TIMINGPF_serve.log
- bin: 07ad7eccc0b97cc0
- hash: ebacf2a2ba7b082e (prev=ac3d93bdb2f81017)
### PLOG-017 [2026-09-17 ~03:0x UTC] phase=PREFILL+DECODE — TIMINGPF2: thermal throttling NAMED as the decay mechanism (during-load clocks)
- metrics: 2 s rocm-smi sampler during load: run C (cool start) flat 24 tok/s with sclk levels 3-7 (991-1500 MHz, no GPU below 991); run D (62-68C start) knee at tok 640 EXACTLY when GPU0 sclk collapses 1500->560-775 MHz (20:24:25-27), throttle spreads to GPU1/GPU3 by 20:24:54 (rate bottoms 11.2); 60 s idle did NOT recover fast regime (edge only 84->62-68C). Depth effect second-order (flat 24 through tok 1152 whenever clocks hold). Proposal: future perf rows MUST carry clocks+temps sideband; next: 5-min idle re-fire, forced-clock A/B
- provenance: results/amd/coherence/TIMINGPF2_row.txt + TIMINGPF2_clocks.log + TIMINGPF2_serve.log
- bin: 07ad7eccc0b97cc0
- hash: 97f249706702df46 (prev=ebacf2a2ba7b082e)
### PLOG-018 [2026-09-17 ~03:5x UTC] phase=DECODE — THARM1 ARM-HIGH: cold-start count600, clock-verified thermal A/B (arm 1 of 2)
- metrics: decode 43.52 s / 600 tok = 13.79 tok/s, 213.25 ms/round (Verify 196.64 = 92.2%, AR chain 7.62 = 3.6%), acc 0.97 t/r 2.94; reproduces cold TIMING1 211.95; BLUE PASS. Forced-clock pinning IMPOSSIBLE on this box (rocm-smi set verbs root-gated, no NOPASSWD sudo; docs/amd/v340l/01:38-41) — arms are clock-VERIFIED via sysfs sampler, not pinned
- provenance: results/amd/coherence/THARM1_row.txt + THARM1_serve.log + THARM1_clocks2.log
- bin: 07ad7eccc0b97cc0
- hash: 69bc561abce2f25d (prev=97f249706702df46)
### PLOG-019 [2026-09-17 ~03:5x UTC] phase=DECODE — THARM1 ARM-THROTTLED + recovery: sustained-load integrator named, edge temp exonerated
- metrics: after 5.5 min continuous load (84C edge): decode 122.70 s = 4.89 tok/s, 606.44 ms/round = 2.82x cold arm (Verify 538.18, AR chain 33.01 — both swell); BUT after ~3 min idle at the SAME 84C edge: 222.62 ms/round, 13.28 tok/s, sclk 991-1500 MHz (sysfs) — FULL RECOVERY. Edge temp is NOT the throttle control; the trigger is a minutes-scale integrator of sustained load duration (junction/hotspot or power-cap accounting), recovery ~2-3 min idle. Operating rule: benchmark legs need idle-gap logging; <60 s bursts or ~2-3 min gaps hold the fast regime
- provenance: results/amd/coherence/THARM1_row.txt + THARM1_serve.log + THARM1_clocks2.log
- bin: 07ad7eccc0b97cc0
- hash: 52531d37131c1b9e (prev=69bc561abce2f25d)
### PLOG-020 [2026-09-17 ~04:2x UTC] phase=PREFILL — COOL10K: cool-start 10k prefill curve with clocks sideband, plateau extended
- metrics: plen=10000 MTP k=2 graphs-ON, edge 36-42C start: PLATEAU 24.5->23.7 tok/s FLAT through tok 1664 (~68 s, longest tonight vs PF-A 53 s / MTP3 ~60 s — cooler start buys ~10 s more plateau, fits the load-duration integrator) -> KNEE 18.0/15.6/14.4 (tok 1792-2304, ~13 s) -> SLOW 12.5 by 2560, deep ~9.x; total 885.49 s = 11.30 tok/s mean (1.06x vs MTP3 10.65); grade PASS (BLUE stop)
- provenance: results/amd/coherence/COOL10K_row.txt + COOL10K_serve.log + COOL10K_clocks.log
- bin: 07ad7eccc0b97cc0
- hash: 982583565b5dfe36 (prev=52531d37131c1b9e)
### PLOG-021 [2026-09-17 ~04:2x UTC] phase=DECODE — COOL10K: 10k decode 3.65 tok/s, still thermal-confounded — honest cell needs privileged clocks
- metrics: decode 17.27 s / 63 tok = 3.65 tok/s engine-side (billed 3.5), acc=0.69 t/r=2.42 @10k ctx, grade PASS; sideband shows decode-window sclk unstable 300-1269 MHz at edge 84-85C (decode follows the 885 s prefill soak and cannot precede it) — the 10k decode crisis number remains a soaked-machine number; brackets with THARM1 12.0-vs-5.5 @1.3k; the clean split (depth vs thermal) needs the forced-clock cell (root-gated, next window)
- provenance: results/amd/coherence/COOL10K_row.txt + COOL10K_clocks.log
- bin: 07ad7eccc0b97cc0
- hash: 99452bec70b94475 (prev=982583565b5dfe36)
### PLOG-022 [2026-09-17 ~04:5x UTC] phase=DECODE-CORRECTNESS — E1 byte-identity oracle: RED (MTP-arm-specific), deterministic control banked, NOT fixed
- metrics: graphs-OFF count600 temp=0: MTP k=2 arm [ids] vs non-MTP arm [ids] DIVERGE (~token 30: 486 vs 1472+220); control: non-MTP same boot repeat fire BYTE-IDENTICAL (268 B) -> divergence is MTP-arm-specific, not engine nondeterminism; both completions coherent, finish=length, no mojibake. Two hypotheses open: verify-batch geometry numerics (T=3 vs T=1 argmax flips at near-ties) vs genuine mis-acceptance. WO_MTP_1's byte-identity bar FAILED on 07ad7eccc0b97cc0; falsifiers named (cross-boot non-MTP, k=1 geometry probe, logit-dump, target-argmax replay); NO fix attempted per banking law
- provenance: results/amd/coherence/E1ORACLE_row.txt + E1A_ids.txt + E1B_ids.txt + E1B2_ids.txt
- bin: 07ad7eccc0b97cc0
- hash: 0a28bff8cfc2240f (prev=99452bec70b94475)
### PLOG-023 [2026-09-17 ~05:2x UTC] phase=DECODE — B3HARDEN: draft-vocab path hardening landed, all three resolution arms proven, token stream unchanged
- metrics: new bin fcef463d655e8150 (banked): count600 acc=0.97 t/r=2.94 rounds=204, phase structure identical (Verify 90.1/AR 4.7 warm); cooled re-probe 244.27 ms/round (11.97 tok/s) in thermal band; [ids] BYTE-IDENTICAL to pre-hardening TIMING1 (265 B); positive boot: no warning + remap loads; /tmp boot: WARNING fires verbatim, fallback functional and slow (7.2 vs ~11 tok/s BLUE); env boot: NINFER_DRAFT_VOCAB honored, remap loads from env path; guard G5 narrative updated (selftest 21 mutations PASS), battery step 0c sideband fixed (set -e sampler death on card1 grep miss) and validated (56 samples). E1 RED corroborated: same-config streams identical across binaries -> divergence is config-geometry-tied
- provenance: results/amd/coherence/B3HARDEN_row.txt + B3HARDEN_serve.log + B3NEG_serve.log + B3ENV_serve.log + B3HARDEN_ids.txt + BOOT_BATTERY_20260916_213102_clocks.log
- bin: fcef463d655e8150
- hash: c1b2f18e5ce298a0 (prev=0a28bff8cfc2240f)
### PLOG-024 [2026-09-17 ~05:5x UTC] phase=DECODE-CORRECTNESS — C1: E1 RED classified — geometry numerics, accept EXONERATED (audit tap 0/204), bar re-scoped (BUGTRACK E-17)
- metrics: k-sweep falsifier: divergence point MOVES with k (k2 flips gen-pos ~2 '486' vs '1472 220'; k1 flips gen-pos ~15 '874 4799 1414' vs '1534 1132 1103'; k1+k2 concur at second site); cross-boot non-MTP byte-identical; accept-audit tap (bin c6361bde8bc0dcc1, NINFER_ACCEPT_AUDIT=1): 206 rounds, 0 retained-mismatches, tap passive (ids == E1A). Verdict: T=k+1 verify-forward argmax flips at near-tie numerals vs T=1 path — LCP 29/42, ratios 0.923/0.954, no derailment. E1 bar re-scoped: same-geometry identity + acceptance<=argmax, both hold. Margin print named as follow-up. VOID row logged: audit attempt 1 spawned at KFD=1 (hygiene miss), clean-fail on 816 MiB, re-run clean
- provenance: results/amd/coherence/E1K1_row.txt + E1K1A/B/B2_ids.txt + E1AUDIT_serve.log + docs/amd/BUGTRACK_NVFP4_TP4_INCOHERENCE.md E-17
- bin: c6361bde8bc0dcc1 (+07ad7eccc0b97cc0 for the k-sweep arms)
- hash: 13b82ccbac1376cc (prev=c1b2f18e5ce298a0)
### PLOG-025 [2026-09-17 ~06:3x UTC] phase=DECODE — REALK2: real-prompt k=2 arm of the head-to-head (sky-blue mt=300, first-probe-on-boot)
- metrics: acc=0.65 t/r=2.31 rounds=87 gen=201, decode 18.64 s = 10.79 tok/s, finish=stop clean prose; edge 45-46C start; sideband REALK2_clocks.log
- provenance: results/amd/coherence/REALK23_row.txt + REALK2_serve.log
- bin: 07ad7eccc0b97cc0
- hash: 12d676525ea4eda2 (prev=13b82ccbac1376cc)
### PLOG-026 [2026-09-17 ~06:3x UTC] phase=DECODE — REALK3: real-prompt k=3 arm — k=2 WINS on prose, counting k-table does not transfer (closes SPEED_PLAN §6-2)
- metrics: acc=0.51 t/r=2.55 rounds=73 gen=186, decode 19.07 s = 9.75 tok/s vs k=2's 10.79 (+10.7% for k=2); finish=stop clean; edge 54-56C start (booted right after k2 leg, asymmetry noted), sideband banked. Counting-shape k-table (k3 best 14.86) does NOT transfer at real acceptance ~0.5-0.65; recommended serving k = 2 (shipped default stands)
- provenance: results/amd/coherence/REALK23_row.txt + REALK3_serve.log + REALK3_clocks.log
- bin: 07ad7eccc0b97cc0
- hash: 0ba8159766b67742 (prev=12d676525ea4eda2)
### PLOG-027 [2026-09-17 ~07:1x UTC] phase=PREFILL+DECODE — INTEG: integrator-vs-instantaneous DECIDED — accumulating integrator WITH saturation under fixed duty
- metrics: 5x [1279-tok probe + 60 s idle], same prompt: knee@ 768/640/512/512/512 tok; prefill 53.11/72.34/86.36/91.65/92.81 s; mean rate 18.6/16.0/14.8/14.8/14.8 tok/s; decode 4.25->11.15 s. Deficit ACCUMULATES over 2-3 cycles (60 s idle never fully drains) then LOCKS at a periodic steady state (c4==c5) — classic first-order thermal RC. Serving implication: sustained duty stabilizes at predictable ~1.75x-degraded prefill / ~2.6x decode; full-rate bursts need >=2-3 min idle (THARM1). Sampler-orphan hygiene misses logged (REALK2 clocks contains post-window samples, bounded by serve-log timestamps)
- provenance: results/amd/coherence/INTEG_row.txt + INTEG_serve.log + INTEG_clocks.log
- bin: 07ad7eccc0b97cc0
- hash: cbb233e8b9217334 (prev=0ba8159766b67742)
### PLOG-028 [2026-09-17 ~08:0x UTC] phase=DECODE-CORRECTNESS — E1MARGIN: E-17 follow-up CLOSED via existing NINFER_D22_PDBG hook (no new plumbing) — flips are bf16 near-ties/degenerate
- metrics: margin arm [ids] byte-identical to E1A (hook read-only); 126 columns: flip-class p_am 0.1727-0.5540 (committed argmax holds 17-55% of mass), recurring numeral-boundary column local_am=16 vs global_am=486 at p_am=1.0000 (top-2 BIT-IDENTICAL at bf16 after gather — tiebreak order decides), gathered-bf16 argmax differs from committed allreduce argmax at several columns; p_am distribution 49x 1.0000 with tail to 0.1727. Absolute logit-gap plumbing re-queued as NOT NEEDED. Handoff doc NIGHT_HANDOFF_2026-09-16_mtp_thermal.md banked
- provenance: results/amd/coherence/E1MARGIN_row.txt + E1MARGIN_serve.log
- bin: 07ad7eccc0b97cc0
- hash: d0dc22535523b7de (prev=cbb233e8b9217334)
### PLOG-029 [2026-09-17 ~04:2xZ] phase=BOOT-GATE — N7 executed: --allow-nvfp4-weights flag + value-parsed env + both-numbers refusal
- metrics: arm A refusal loud (profile+world=4 named), arm B falsy-env refused, arm C flag-only boot battery GREEN (guard 5/5, 2 device cells PASS, ladder 14/14); no throughput columns this entry (gate work, not perf)
- provenance: results/amd/coherence/N7_row.txt + BOOT_BATTERY_20260917_041518.row
- bin: 6c1fe9366f0ef83d
- hash: 922fd8b315248377 (prev=d0dc22535523b7de)
### PLOG-030 [2026-09-17 ~06:4xZ] phase=DECODE-KERNEL+BOOT-GATE — GEMV-TUNE: NVFP4 decode GEMV 1.68-1.88x, bit-equal, landed; TP4 window + driver-state incident banked
- metrics: kernel (isolation bench, dev0, read-ceiling ref): AttnInput 155.4->282.0 GB/s (40.7->73.2-74.2%), GdnInput 153.7->289.5 (75-76%), MlpGateUp 159.8->290.2-295.1 (75-77%), Resid6144 125.9->211.8 (55%) — bar >=70% on MlpGateUp+AttnInput MET; winning techniques: e2m1 pair table in LDS (the ISA showed 16 dynamic-index GLOBAL table loads/group + 64-bit addr math = ~110/160 slots), e4m3 LDS table, 8 warps x 2 rows/warp, u64 code loads, 2x uint4 x loads, scale quad loads — ALL order-contract-preserving; bit-equality: permanent cell nvfp4_gemv_bitcheck.cu, 15 problems x fin/raw passes, BIT-EQUAL YES at every iteration vs pre-edit reference; end-to-end (sky-blue mt300, server window accounting, TP4 mtp k2): decode 11.2 tok/s / prefill 25.9 vs prior-window 10.79 (+~4%, within cross-window thermal noise; Part C predicted ~1.1x); BOOT_BATTERY_20260917_055635 GREEN (guard 5/5 + 2 device cells + ladder). INCIDENT: from 06:10Z every TP4 serve warmup faults on node-3 (Memory access fault, layout-dependent addr), 6/6 across BOTH binaries (pre-tuning 6c1fe9366f0ef83d is byte-reproducible from 9751436f6, 4/4 incl --no-cuda-graph; tuned e341a2072c6f5078 2/2; tuned had booted GREEN 05:55) — driver-state degradation after ~6 rapid kill/boot cycles, NOT a kernel regression (bitcheck+bench at full ceiling throughout, KFD=0); TP4 windows need a root driver reset/reboot; single-die work unaffected. Baseline A/B boot blocked by the incident; prior-row baseline cited instead
- provenance: results/amd/coherence/GEMV_TUNING_row.txt + BOOT_BATTERY_20260917_055635.row + docs/amd/EXTRACTING_NUMBERS_and_50TPS_budget.md B.1/B.2; bin e341a2072c6f5078 (banked, rebuild-reproducible)
- bin: e341a2072c6f5078
- hash: 927898f51c211a7b (prev=922fd8b315248377)
### PLOG-031 [2026-09-17 ~07:1xZ] phase=BOOT-GATE/INCIDENT — TP4FAULT: node-3 warmup fault class durably documented; GEMV-TUNE end-state safe on amd/main
- metrics: 6/6 TP4 serve warmup aborts on GPU node-3 ("Memory access fault ... Page not present or supervisor privilege", layout-varying addresses, always at the same warmup point) across BOTH binaries — pre-tuning 6c1fe9366f0ef83d (byte-reproducible rebuild from 9751436f6; the 04:15Z N7 binary) 4/4 incl --no-cuda-graph, tuned e341a2072c6f5078 2/2 (that binary booted GREEN 05:55 with battery GREEN, GEMV-TUNE numbers stand) — machine/driver state degradation after ~6 rapid kill/boot cycles, NOT a kernel regression; KFD=0 + VRAM clean + single-die bench at FULL ceiling (copy 369.3/read 385.1 GB/s) throughout; remediation: ROOT driver reset / reboot before next TP4 window, no windows re-trying, single-die work OK
- provenance: results/amd/coherence/TP4FAULT_row.txt (6 verbatim fault lines + binary provenance + cleanliness probes) + WARNING block at top of docs/amd/NIGHT_HANDOFF_2026-09-16_mtp_thermal.md + GEMV_TUNING_row.txt incident note
- bin: n/a (machine-level; certified tuned binary e341a2072c6f5078 banked, amd/main head 4f5c0e2b0 carries the GEMV-TUNE end-state — tuned kernel + bitcheck cell + docs are merged and pushed; nothing on the branch depends on the faulting state)
- hash: 0fc261647b9ec752 (prev=927898f51c211a7b)
### PLOG-032 [2026-09-17 ~11:5xZ] phase=DECODE-DECOMPOSITION — HOLE1: verify interior measured per-layer/per-rank; H1-H4 resolved; draft-vocab remap CWD trap banked (+142 ms/round)
- metrics: TP4 mtp k=2 count600 remap-ON: round 233.40 ms — verify 215.03 (92.1%) [layer loop 138.9 = AR 14.5 (128 x ~114us, RTT law) + nonAR 124.4 (~1.94 ms/layer UNIFORM, FULL layers only +0.15)] + outside-loop ~64 (incl full-vocab lm_head) + ar 8.83 + align 8.92 + propose 0.34 + book 0.29; per-rank skew 0.017-0.019 ms (H1 skew ACQUITTED: 4 ranks identical <0.1 ms); implied weight stream ~33 GB/s vs 265 isolated ceiling (8x kernel-exec gap); clocks at load: power pinned 110 W cap, SCLK 1269-1350, MCLK 945; remap-OFF trap: propose 0.34->59.5, ar 8.8->66.4, round 233->376, acceptance 0.97->0.55 (certified e341 A/B shows same without new arms — instrument exonerated); TP2/TP4 round loop is EAGER (audit s1 'one captured graph' premise wrong for this path)
- provenance: results/amd/coherence/HOLE1_row.txt + HOLE1_ltrace_table.txt + HOLE1_serve.log (boot1, remap broken) + HOLE1AB_e341_serve.log (A/B) + HOLE1REMAP_serve.log (remap on) + HOLE1REMAP_clocks_t12.txt + docs/amd/VERIFY_LAUNCH_AUDIT_2026-09-17.md s0 MEASURED; analyzer tools/optrace_analyze.py (ebad7e1c7); instrument NINFER_TP2_OPTRACE + NINFER_VERIFY_LAYER_TRACE landed env-gated (tp2_backend.cpp PhaseTimer made per-rank, text_context_impl.h VerifyLayerTrace)
- bin: 5735aaf3e9ddd410 (banked) + e341a2072c6f5078 (A/B arm)
- hash: cb1b8b4e1b64037a (prev=0fc261647b9ec752)
### PLOG-033 [2026-09-17 ~12:0x-13:0xZ] phase=DECODE-KERNEL+BOOT-GATE — SMALLT: verify-width kernel identified + tuned 3.2x, bit-equal, landed; round -39.6%, decode +66%
- metrics: PHASE1 kernel identity PROVEN (code chain + serving trace): at verify T=3 on TP4 ALL layer projections execute nvfp4_small_t_hip_kernel (nvfp4_small_t_hip.cu) at the five W4 geometries — GdnInputW4 4096x5120, AttnInputW4 3584x5120, MlpGateUpW4 8704x5120, Residual6144W4 5120x1536, Residual17408W4 5120x4352 — via SIMT-lane A16Only binding (HipSources.cmake:330) -> tp_gemv (tp_kernel.cu:77) -> nvfp4_dispatch -> launch_a16 -> launch_nvfp4_small_t; the tuned decode GEMV never runs at T>=2. BASE isolated T=3: 52.0-56.7 GB/s = 14-15% read ceiling. TUNED (S5: LDS e2m1 pair table + e4m3 table from the codec fns, u64 codes, 2x uint4 x, scale quad loads, 8 warps x 2 rows, TOKEN-SHARING warp = 1 row x all T so weights stream once): T=3 147.5-192.4 GB/s = 39-50% ceiling = 3.2x, bit-equal vs pre-edit ref (5 W4 problems x 2 passes, sha b6318b4d...); T=2 48-57%, T=4 33-41%; legacy kernel (T=5..32 prefill chunks) keeps S1+S2. Mission 60% bar NOT met at T=3 — honesty row in SMALLT_row.txt (3x ALU per weight byte moves the wall to issue; S6/S7/S8 regress; split-K/extra chains banned by codec contract). SERVING same-boot A/B (count600 counting class acc 0.99 t/r 2.99): round 212.68 -> 128.43 ms (-39.6%), verify 194.01 -> 111.95 (-42.3%), decode 14.0 -> 23.2 tok/s (+66%), prefill 22.4 -> 37.3 (+66%); E-17 re-scoped byte-identity GREEN (count600 completion byte-equal across binaries, temp 0); sky-blue mt300 18.0 tok/s decode / 46.4 prefill / 2.33 MTP (vs e341 hot anchors 10.1-11.2 / 25.3-25.9); BOOT_BATTERY_20260917_123502 OVERALL GREEN; prefill sibling bench (coordinator ask): TILED-GEMM-1 3.5x PROD GF/s at M=128 (3080 vs 889) relL2 PASS — tiled GEMM greenlit as prefill follow-on
- provenance: results/amd/coherence/SMALLT_row.txt (manifest, proof lines, tuning table, honesty) + SMALLT_serve.log (boot1 pre-tune) + SMALLT2_serve.log (boot2 landed) + nvfp4_smallt_roofline_bench.cu + nvfp4_smallt_bitcheck.cu + nvfp4_prefill_bench.cu + BOOT_BATTERY_20260917_123502.row; chain HOLE1_row.txt -> this row
- bin: be4c9dd763cd649f (pre-tune boot1) + 1e5745f9f8febcc3 (LANDED, banked) 
- hash: a3e87a9527a559ac
### PLOG-034 [2026-09-17 ~19:45-20:01Z] phase=PREFILL-KERNEL — T1 TILED-PREFILL-DATUM: tiled GEMM prefill dispatch route-proven serving-side; plen=1996 prefill 75.7 vs smallt 42.1/41.6 tok/s = 1.80-1.82x serving (diluted from the 3.5x kernel-only bench by non-GEMM prefill work)
- metrics: bin 409450b83ffa4be8, tiled dispatch default-ON (M in 128/256 -> nvfp4_tiled_gemm; NINFER_TILED_TRACE=1 first-seen lines name ALL FIVE W4 problems at M=128: n=4096 k=5120 / n=5120 k=1536 / n=8704 k=5120 / n=5120 k=4352 / n=3584 k=5120). (a) sky-blue mt300 (prompt 58 tok, M<64 -> small_t chunk path as designed): prefill 46.5 tok/s (anchor 46.4 SMALLT2 — CONSISTENT), decode 17.4, MTP 2.23 t/r, B1 92r round=127.69 AR 7.48. (b) plen~2000 probe (BLUE body + 1938 space-separated alphas, measured plen=1996, mt 64, temp 0, finish=output_limit): TILED ttft 26.36 s = PREFILL 75.7 tok/s; NINFER_A16_FORCE_SMALLT=1 arm 42.1 (hot window 68-80C) and 41.6 (cool-state re-probe, same boot, 150 s idle) = smallt thermally FLAT, ratio 1.80-1.82x robust in direction in BOTH thermal states. vs the 11.3 mean@10k long-prompt anchor: tiled 6.7x, tuned-smallt 3.7x (the SMALLT S1/S2 legacy-kernel tune already lifted the smallt prefill arm). Short-prompt posture unchanged (M>=64 threshold intact). Sidebands banked, no sclk collapse; KFD=0 pre/mid/post; anchored kills.
- provenance: results/amd/coherence/TILED_DATUM_row.txt + TILED1_serve.log (tiled arm) + TILED2_serve.log (smallt arm) + TILED_probe_a/b/b2/b3_clocks.tsv; chain SMALLT_row -> this row. CHAIN RECIPE NOTE: this and following entries hash = sha256(prev_hash + entry_text_excluding_hash_line)[:16] (prior recipe unrecoverable from file; recipe now documented)
- bin: 409450b83ffa4be8 (both boots)
- hash: 415287360024faa9 (prev=a3e87a9527a559ac)
### PLOG-035 [2026-09-17 ~20:04-20:36Z] phase=AR-TRANSPORT — T2 W4-ONESHOT-AR: NINFER_TP_ONESHOT_AR=1 WEDGES the TP4 boot DETERMINISTICALLY (RED capture 2/2 boots) — ring stays the transport; one-shot NOT adoptable in 409450b83ffa4be8; ring anchor AR 7.63 ms/round (5.9% of round)
- metrics: arm A ring (default construction): count600 decode 23.0 tok/s, prefill 36.9, acc 0.99 t/r 2.99 201 rounds, B1 200r round=129.38 ms — VERIFY 112.82, align 8.48, AR-draft-chain 7.63 ms (the tasking's 14.5 anchor was a pre-SMALLT-boot number; SMALLT halved every phase incl AR); [ids]+completion banked as the identity reference for any future GREEN leg. Arm B (NINFER_TP_ONESHOT_AR=1): model loads 19.6 s then "warming up..." NEVER completes (17+ min observed vs ~2 s normal): process 100% CPU single-thread spin (21 thr), 3 of 4 rank GPUs at 100% use (stranded rank VARIES: rank3 boot B, rank1 boot B2), NO AR-FAILOUT, NO REJECT-candidate, NO soft-fail declaration — the R2 bounded-wait law (2x(world-1) poll budgets x16 retries ~= 7 s to declared death) NEVER FIRES; repro boot B2 identical = 2/2 deterministic. Default-OFF env-gate discipline VINDICATED (all default boots healthy, bit-identical anchors). BUG CLASS: world>2 collective lockstep/bounded-wait violation (ONESHOT_W4_notes R2/R3) — design-level repair with its own RED/GREEN cycle, coordinator-confirmed NOT this window.
- provenance: results/amd/coherence/ONESHOT_AR_row.txt + ONESHOT_A_serve.log + ONESHOT_B_serve.log + ONESHOT_B2_serve.log + ONESHOT_B_wedge_evidence.txt + ONESHOT_B2_wedge_evidence.txt; chain ONESHOT_W4_notes.md s3 -> this row
- bin: 409450b83ffa4be8 (all three boots)
- hash: 46e05ea247fed453 (prev=415287360024faa9)
### PLOG-036 [2026-09-17 ~20:35-21:0xZ] phase=CONCURRENCY-PREFLIGHT — T3 mc=4 KV findings: default(auto) KV at mc=4 REFUSES at preflight on 8GB dies (fixed 8483 MiB > 8160 free incl the 1536 MiB bf16 mc>=2 reserve); int8 routes to I8_KV = DEFERRED on the HIP lane (half-init cache, 100% request failures); workable posture = EXPLICIT --kv-dtype bf16 + explicit kv-capacity; concurrency_bench.sh env(1) boot bug fixed in place
- metrics: boot1 mc=4 default KV: "[tp2] kv-capacity auto: no feasible capacity — free VRAM 8160 MiB/rank (min both), placement basis measured-manifest 5131 MiB, fixed 8483 MiB total" = clean preflight refusal; the tp2_runtime_reserve(bf16,conc>=2) 1536 MiB constant (tp_engine.cpp:1147-1149 / tp2_budget.h:89 per CONCURRENCY_BENCH.md s1.5) is a VRAM-LAW-CLASS refuse-constant candidate banked loud for the owner desk (estimate-constant refusing a launch; NOT patched this window). boot2 mc=4 --kv-dtype int8 (auto cap): died IN cudaMalloc hipErrorOutOfMemory (allocator gate, clean, real time). boot3 int8 + explicit --kv-capacity 8192: BOOTED (preflight line proves tp2_runtime_reserve(bf16,conc>=2) 0 — the reserve IS bf16-KV-specific) but the I8_KV cache is DEFERRED on the HIP lane -> warmup half-initialized, every request fails instantly (meltdown volleys FAIL=4 at 1.0 s walls; the N=1 leg also failed at 0.4 s — NO numbers from that attempt). STAND-DOWN per coordinator mid-attempt: coordinator took over the meltdown boot with EXPLICIT --kv-dtype bf16 + explicit capacity (their MELTDOWN2 artifacts, their row). Script fix: `env --allow-nvfp4-weights NINFER_WORKSPACE_MIB=96 ...` passed the CLI flag as an env(1) ARGUMENT -> env exits RC=125 "unrecognized option", boot died instantly; flag moved to CLI args, NINFER_ALLOW_NVFP4_TP2=1 + NINFER_DRAFT_VOCAB mandated env added, mc=4 KV reality documented in-script. NO N-arm M-wall numbers this window — next window reruns arms N=1/2/4 per the fixed script on the coordinator's verified bf16-explicit posture.
- provenance: results/amd/coherence/concurrency_bench.sh (fix + reality comments) + MELTDOWN_row.txt (boot1/2/3 findings, pre-stand-down) + MELTDOWN_serve.log; chain CONCURRENCY_BENCH.md -> this row
- bin: 409450b83ffa4be8 (all boots)
- hash: 5f780ae37c7a265c (prev=46e05ea247fed453)
### PLOG-037 [2026-09-17 ~21:5xZ] phase=MELTDOWN-10K — 10K GRADE PASS: coherent 10k serve is banked fact
- context: the durable detached launch script /home/chris/serve_10k.sh (setsid+disown so agent shell cancellations CANNOT kill the boot; anchored `^/home/chris/artifacts_bin/ninfer-serve` kill pattern per the pgrep-self-kill law) fired the known-answer 10k request at the sustained TP4 boot (bin 409450b83ffa4be8, MTP k=2, N7 ON, tiled dispatch ON, NINFER_DRAFT_VOCAB set, explicit bf16 KV posture)
- result: `10K GRADE: finish=stop content='BLUE' prompt=10000 completion=61 -> PASS`; reasoning_content fully coherent (correct instruction extraction out of a 10k 'alpha'-filler prompt); response banked /tmp/resp10k.json; boot SUSTAINED post-grade (still serving on :8100; GPU0 69C edge / 72C mem under load, sclk level 7 1500 MHz, mclk level 3 945 MHz = full clocks held)
- meaning: E-13/E-14/E-15 cure holds at max geometry; the launch pipeline is now ONE command any operator can run; closes the user-flagged '50 attempts to launch a server' loop
- convention fix: this entry is the FIRST appended via tools/guards/plog_append.py — chain formula now stated in-script (sha256(prev+"\0"+entry-no-hashline)[:16]); PLOG-001..036 were hand-hashed with frame drift and are NOT byte-reproducible (verify mode reports DRIFT for them); continuity rides the prev-pointer
- provenance: /home/chris/serve_10k.sh + /home/chris/serve_10k_session.log + /tmp/resp10k.json; chain PLOG-036 -> this
- bin: 409450b83ffa4be8
- hash: ed74440917f31333 (prev=5f780ae37c7a265c)
### PLOG-038 [2026-09-17 ~22:0xZ] phase=CHAIN-AUDIT — correction to PLOG-037's convention note
- fact: with the byte-framed verify walk (separator newline handled), 29/34 links DO byte-reproduce; only 5 hand-hashed entries drifted (edited post-hash). PLOG-037's '001..036 NOT byte-reproducible' was overstated — the chain was largely sound; the helper makes every link from 037 on exactly reproducible. head=ed74440917f31333
- provenance: tools/guards/plog_append.py verify output this commit; chain PLOG-037 -> this
- hash: e3e1fe013ad91b64 (prev=ed74440917f31333)
### PLOG-039 [2026-09-17 ~22:0xZ] phase=TILED-SWEEP-MEASURE — T1 sweep FIRED: V0 (shipped tile) wins ALL FOUR geometries by 15-41%; prediction V4>V3>V0>V1>V2 FALSIFIED; falsifier (a) fired => V0 STAYS, no promotion, no rebuild owed
- context: GPU-OWNER window (mission = written grant), meltdown boot 125438 retired first (TERM->dead 1 s, 0 KFD contexts after); bench compiled on THIS box with the exact -O3 LAW line (ONESHOT_AR_notes s5 form: nvfp4_prefill_bench.cu + src/core/device.cu, RC=0, 143 warnings = the desk's own count); --sweep mode: V0-V4 at M=128 x AttnInput 14336x5120 / GdnInput 16384x5120 / MlpGateUp 34816x5120 / Residual6144 5120x6144; 5 s mclk hammer inside bench BEFORE timing
- result: relL2 vs REFERENCE = 0.00e+00 on ALL 20 rows (bit-identical across ids as designed, NO RED row, no consistency STOP). GF/s (%nom vs 10.75 TF/s kNominalF32Peak): V0 3083/2756/3103/2366 (28.7/25.6/28.9/22.0%) — WINNER ON ALL FOUR; V1 2276/2119/2220/2146; V2 2302/1810/2242/1700; V3 1921/1786/1876/1391 (12.9-17.9%); V4 2064/1908/2008/1491. Ceilings this run: copy 354.4 / read 368.7 GB/s (achievable anchor); M=128 lives past the M-wall = compute-bound, GF/s decisive. mclk verified level 3 (945 MHz TOP) through the window, zero 167 MHz samples; thermal 64->68 C edge on the bench die, no throttle
- meaning: the shipped V0 tile is the measured optimum of the shape family — barrier-halving (V3 BK128) and double-buffering (V4) both LOSE 33-41% to the 35 KB-LDS occupancy cost, and the falsifier-(a) follow-up knob (SMALLER x-tile, V1) also loses 16-26% => shape-sweeping this kernel is CLOSED, next prefill work is NOT tile shapes. 50-TPS arithmetic: prefill stays 75.7 tok/s (V0 kept), no serving change
- provenance: results/amd/coherence/SWEEP_row.txt + SWEEP_run_20260917.log (raw); chain PLOG-038 -> this
- bin: 0ed849be3c526487 (sweep bench, /tmp/gpuowner_build/nvfp4_prefill_bench_sweep; sha256 0ed849be3c526487ba9f32f7effe4cde702f1d63dbbf40092c29740fcd43b060)
- hash: 2c04974bdf81ac5e (prev=e3e1fe013ad91b64)
### PLOG-040 [2026-09-17 ~22:0xZ] phase=HFMA2-PK-MEASURE — T2 HFMA2 packed-fp16 arm: numerics GREEN (rel-L2 1.1-1.3e-3, 8x under bar), perf RED vs adoption bar (mean 1.07x, 1.02x on MlpGateUpW4) => PK NOT promoted, default stays S5
- context: same window, same discipline (bench + src/core/device.cu at the -O3 LAW line, RC=0); SMALLT-PK A/B at T=3 on the five W4 geometries, fixed-seed production-bounded data |x| in [2^-9,32], NaN-masked scales, dirty divisor 1.177; bench's own 5 s hammer then timing; mclk level 3 verified top
- result: GATES ALL PASS (rc 0): AttnInputW4 162.7->148.8 GB/s 1.09x rel-L2 1.14e-03 | GdnInputW4 142.9->139.3 1.03x 1.20e-03 | MlpGateUpW4 150.3->146.7 1.02x 1.26e-03 | Resid6144W4 145.6->129.6 1.12x 1.22e-03 | Resid17408W4 151.5->138.1 1.10x 1.24e-03. Ceilings copy 358.8 / read 368.3 GB/s
- meaning: desk prediction was 1.15-1.6x IF still issue-bound; measured mean 1.07x with 1.02x on the largest geometry = the v_cvt tax ate the packed-FMA win — the notes' own "cvt-bound/~flat" branch. ADOPTION RULE (>= ~1.1x AND gates PASS) FAILS on ratio => NO promotion: default stays S5 (byte-identical boot), NINFER_SMALLT_PK=1 stays a non-default env arm, launch_nvfp4_small_t_pk stays parked, NO server rebuild, NO BOOT_BATTERY, NO serving A/B owed (gated on promotion); 128.43 ms count600 anchor NOT challenged. Lever DEAD as built; named follow-up (LDS-activation staging, convert x once per block) is a different commit. S5 context from same run: T=2 183-215 GB/s (serving width), T=3 143-163, T=4 118-155
- provenance: results/amd/coherence/PK_row.txt + PK_run_20260917.log (raw) + HFMA2_notes.md; chain PLOG-039 -> this
- bin: 2a52722227a671284 (smallt bench, /tmp/gpuowner_build/nvfp4_smallt_roofline_bench; sha256 2a52722227a67128445f2f477ca60312ff1d911dd121096d1c46ced94c8ec1ca)
- hash: c595bca78e3a3453 (prev=2c04974bdf81ac5e)
### PLOG-041 [2026-09-17 ~22:2xZ] phase=VERIFY-DECOMP-M1 — M1 FIRED (coordinator add): verify 117.41 decomposed — loop 62.20 (body 52.60 + in-loop AR 9.60), OUTSIDE-TAIL 55.21 ms/round UNCHANGED from pre-tune = the named #1 owner; tail is post-layer-64 (lm_head/logits/sampling/D2H), NOT layer bodies
- context: coordinator mid-window addition (VERIFY-DECOMP desk 6cfb3cf2 s4 M1, the decisive cell); SAME window as PLOG-039/040, inserted before banking; banked bin 409450b83ffa4be8 booted with strings-verified trace arms (TIMING/OPTRACE/VERIFY_LAYER_TRACE/SMALLT_TRACE all IN the binary, zero new code); serve_10k posture minus the 10k body, NINFER_DRAFT_VOCAB loaded (40960 ids line verified), conc=1; count600-class leg (105-tok counting prompt, mt600 temp0 greedy): 204 rounds, t/r 2.94 acc 0.98 = in anchor class; anchored kill, KFD=0
- result: B1 TOTAL 133.80 ms/round traces-on (no-trace anchor 128.43; +5.4 = trace overhead + THARM1 hot band, mclk/sclk TOP 113/113 sideband samples, edge 63->82C): Verify 117.37 (87.7%) | align 8.03 | AR draft chain 7.85 | rest ~0.6. OPTRACE: all 4 ranks burn verify identically (skew 0.010 ms; unaccounted between-phases 0.028 ms => COMPUTE verdict, no gap, no laggard). LTRACE ring (3 windows x 4 ranks x 191 passes): layer bodies 52.60 ms/pass (GDN ~765 us, FULL ~946, heavy-FULL 1038-1198 us, rank-uniform) + in-loop AR 9.60 => LOOP-RESIDENT 62.20; OUTSIDE-LAYER-LOOP = 117.41-62.20 = **55.21 ms/round**. HOLE1 cross-check: pre-tune outside was 54.3 (195.34-141.0) — the SMALLT tune shrank the loop 141->62 (-79 ms) and the tail DID NOT MOVE
- meaning: H1's ~50-60 ms unattributed owner is now NAMED and MEASURED: the post-layer-64 verify TAIL — final norm + full-vocab lm_head/logits at T=3 + sampling/logits post-processing + D2H + tail collectives — one single lever target BIGGER than the entire 64-layer body (52.60). Next desk lever per the doc's own ladder: M2 per-op probe at tpv_probe sites (one GDN + one FULL + TAIL ops) to split lm_head-GEMV vs sampling vs sync inside the 55.21; fusion/graph-capture of the tail is the lever class. In-loop AR 9.60 ms also now precisely owned (64 x ~150 us) on top of the 7.85 ms draft-chain AR (parked behind R2/R3 repair, PLOG-035). 50-TPS arithmetic unchanged THIS window (23.2 decode / 128.43 round / 75.7 prefill anchors stand) but the 46-ms-round path now has its decisive map
- provenance: results/amd/coherence/VERIFY_DECOMP_row.txt + M1_serve.log (820 OPTRACE rounds, 1548 LTRACE lines, B1 blocks) + M1_clocks_sideband.log + M1_count600_resp.json + tools/optrace_analyze.py output (in row); chain PLOG-040 -> this
- bin: 409450b83ffa4be8 (banked, unchanged — no rebuild this window)
- hash: 7ffd8eb3ea861eed (prev=c595bca78e3a3453)
### PLOG-042 [2026-09-17 ~22:3xZ] phase=WINDOW-CLOSE — GPU-OWNER window closed: two promote-branches measured dead, M1 decomposition banked, 50-TPS arithmetic honestly UNMOVED (23.2 tok/s decode / 128.43 ms round / 75.7 tok/s prefill anchors stand), user service restored
- context: mission scope law CONCURRENCY-1 ONLY honored (no mc>=2 arm ran); only kill was the granted PID 125438 meltdown boot (cmdline-verified, TERM->1 s) plus this seat's own M1 boot; every bench/boot hammer-first + mclk-top sidebanded; rows + raw logs banked before close
- result: PLOG-039 sweep -> V0 stays (falsifier (a) fired; no tile promotion); PLOG-040 PK -> S5 stays (gates PASS, ratio 1.07x < 1.1x bar; no PK promotion); PLOG-041 M1 -> the 55.21 ms verify tail named (coordinator mid-window addition). No new server binary built or booted => no BOOT_BATTERY owed (no code change to gate; both promote-branches gated on wins that did not materialize). 10k posture re-fired via serve_10k.sh at close, SERVING confirmed on :8100
- meaning: the window's product is three clean NEGATIVE-or-NAMING answers instead of open questions: tile-shape family CLOSED (V0 optimal), HFMA2 PK dead-as-built (follow-up named: LDS-activation staging), and the round's #1 owner NAMED (55.21 ms verify tail -> M2 per-op probe). Next 46-ms-round work: M2 tail split, W4 one-shot AR (parked, R2/R3 repair), layer-body non-linears — NOT tile shapes, NOT PK
- provenance: results/amd/coherence/SWEEP_row.txt + PK_row.txt + VERIFY_DECOMP_row.txt + SWEEP_run_20260917.log + PK_run_20260917.log + M1_serve.log + docs/amd/PLAN_50TPS_master.md updates; chain PLOG-041 -> this
- bin: none banked (no server binary change; benches 0ed849be3c526487 + 2a52722227a671284 per PLOG-039/040)
- hash: db6ec93ac8bf8ce2 (prev=7ffd8eb3ea861eed)
### PLOG-043 [2026-09-17 ~18:4xCST] phase=WINDOW-RESULT — M2 verify-tail split DELIVERED: the 55.21 ms tail IS lm_head (61.9 of ~67 ms clean-band device tail, ~90%; #2 lgather 4.9 = ranks1-3 waiting on rank0's late lm_head); fix target NAMED (tiled/split-K lm_head at skinny T + finalnorm fold); round total moved loud (+28.7% mean / +9.4% p50) — cause BOX CLOCK DROOP (new-today instant-onset burst droop, sideband-proven), not the instrument (clean-band fields land ON M1's B1 anchors within 3%)
- bin: NINFER-serve_248630d1b110c2bf banked (/home/chris/artifacts_bin/ninfer-serve_248630d1b110c2bf.bin, HEAD = M2 tracer commit; strings-verified pre-boot); BOOT_BATTERY 20260917_180806 OVERALL GREEN (guard + both device cells + ladder on the traced server)
- posture: serve_10k line minus 10k body + NINFER_VERIFY_TAIL_TRACE=1 + M1 trace family; "loaded 40960 draft vocabulary IDs" verified; count600 conc=1 greedy x2 legs (204 rounds / acc 0.97 / t/r 2.94 both — determinism fingerprints IDENTICAL to M1 family)
- result: clean-band (OPTRACE verify<=125ms, 278/727 rank-rounds) per-op tail: lm_head 61.93 (rank0 66.0 > ranks1-3 59.7-61.6) >> lgather 4.92 (ranks1-3 wait on rank0 root = rank0's lm_head excess) > ar_argmax 0.20 > everything else <0.5 ms combined; draft-side align 8.28 / chain_fwd 7.90 / propose 0.32 / sum 16.84 vs M1 B1 8.03 / 7.85 / 0.31 / ~16.44 (+3% — instrument cross-check PASS); host sync 117.46 rank-uniform == whole verify phase (host sleeps at the drain; sync OVERLAPS, not additive); d2h 0.026 us-class (code-read confirmed: no verify-logits D2H)
- lm_head COMPUTE-BOUND proof: leg1 deep-droop 153.8 vs leg2 clean 66.0 = 2.33x == clock ratio (wait-class ops don't scale with clocks); CONCURRENCY-1 honored, no mc>=2 anything
- LOUD (as ordered): round total MOVED vs 117.41 — mean 151.06 (+28.7%) / p50 130.29 (+9.4%); sideband shows sclk 7 -> 2-5 within 2-6 s of ANY decode burst start today (instant recovery on idle; prefill 35.3 tok/s unaffected vs M1 34.3) — THARM1's 5.5-min/3-min integrator rule NO LONGER DESCRIBES this box; follow-up owed on the droop's control variable; absolute-ms rows today need clean-band treatment
- meaning: M1 named the tail; M2 split it: the 50-tps path's #1 fix target is the lm_head GEMV at skinny T (fp4 weights + bf16 act, draft-vocab 40960 slice) — tiled/split-K + finalnorm fold (M1's "lm_head fusion/tiled lever" now has its number); secondary lgather root asymmetry ~5 ms; parked W4 AR (in-loop 9.6 + chain 7.9) unchanged
- provenance: results/amd/coherence/VERIFY_TAIL_row.txt + M2_serve.log + M2_clocks_sideband.log + BOOT_BATTERY_20260917_180806.row(+_clocks.log) + M2_count600_resp{,_leg2}.json + docs/amd/PLAN_50TPS_master.md MISSING-list update; chain PLOG-042 -> this
- hash: ea2d4c1f975c37e7 (prev=db6ec93ac8bf8ce2)
### PLOG-044 [2026-09-17 ~21:3xCST] phase=WINDOW-RESULT — window 3 DELIVERED (lm_head route flip GREEN end-to-end + P1 prefill decomposition): flip leg round 134.5 -> 60.0 ms/round (2.24x, decode arithmetic 49.5 tok/s — the 50-TPS goal reached to within 1%) with fingerprint/greedy-text IDENTICAL; P1 names the prefill residual: gap is only 122.7 ms/chunk (NOT the ~900 phantom) — the unnamed time is the IN-LAYER NON-GEMM BODY 645 ms/chunk (39%) + a 270 ms GEMM sketch-underestimate, chunk graph capture DEMOTED to the 7.5% gap class
- bin: NINFER-serve_6c8ae21399516750 banked (/home/chris/artifacts_bin/ninfer-serve_6c8ae21399516750.bin, HEAD 61c1ecc0b = LMHEAD route flip + P1 OPTRACE; strings 5/5 gates pre-boot); BOOT_BATTERY 20260917_201153 OVERALL GREEN (guard + both device cells + ladder, default-off boot)
- roofline (W8_LMHEAD_roofline_run_20260917_2023.log, arms DIRECT, bench sha BIT-IDENTICAL to desk verify ba8ae5a9...): T=3 primary A small_t 60.1-61.3 ms @5.5-5.6 GB/s vs B simt_r8_c4 2.47-2.48 ms @136.5-137.0 GB/s = B/A 24.4-24.7x (bar >=10x PASS); C mma_r64x16 151-158 ms = 0.39-0.40x A (does not win, as predicted); T=1 B/A 40.5-42.6x, T=4 20.6-21.9x; rel-L2 all PASS; DRAFT-FC [5120,10240] T=1 A 6.0 -> B 0.25 ms (23.7-24.2x) prices the fc share; ANCHOR [10240,5120] T=3 0.405 ms @137.7 GB/s; SELECTOR: default -> small_t both problems T=1/3/4, =simt rerun -> simt BOTH CELLS, T=33 stays small_t and T=40 stays the (pre-existing) mma-lane simt fallback in BOTH runs — flip scoped to verify widths exactly
- control leg (ARM unset, M2 trace family): 202 rounds / acc 0.985 / t-r 2.97 IN the M1/M2 family; greedy text byte-identical to both M2 legs; clean-band rank0 lm_head 65.8 (M2 66.0), sync 117.7 (M1 B1 117.4), round ~134.5 (M1 133.8) — posture/instrument CONFIRMED before flip
- flip leg (NINFER_LMHEAD_ARM=simt + NINFER_LMHEAD_TRACE=1): [LMHEAD] route proof first-seen simt gate=simt at BOTH sites (62080,5120 T=1/3 AND 5120,10240 T=1/2/3); MERGE GATE PASS — 202 rounds / 0.985 / 2.97 IDENTICAL to control + text byte-identical (accumulation-order risk did not materialize); PERF GATE PASS — lm_head 2.526 ms mean / 2.466 p50 / 3.46 max (<=4 bar; RED >=10 not approached; 26x vs control clean-band), lgather 0.673 (<=1), align 8.33 -> 2.16 and chain_fwd 7.83 -> 1.39 = EXACTLY the DRAFT-FC-priced fc share (seam topology confirmed; residual = the named tp_gemv-arm retune follow-up)
- ROUND ARITHMETIC NOW: verify 55.7 + align 2.16 + chain_fwd 1.39 + propose 0.34 + chain_head 0.33 + book ~0.1 = ~60.0 ms/round (2.24x vs the 134.5 control / 133.8 M1 anchor) = 2.97 tok / 60.0 ms = 49.5 tok/s decode arithmetic (goal 50 = REACHED within 1%; 46 ms bar remains 14 ms away: tp_gemv draft-arm retune ~10 ms + W4 one-shot AR in-loop 9.6); client end-to-end 14.63 s / 600 tok = 41.0 tok/s vs control 33.51 s = 17.9; flip leg ran FULLY CLEAN (all 201 rounds sync<=65.3 — 60 ms rounds are too short to trip the instant droop; no clean-band filter needed); spec's conservative 85-90 ms prediction BEATEN because removing rank0's lm_head also collapsed the ranks1-3 lgather rank-wait
- P1 prefill (PREFILL_OPTRACE_row.txt, ONE plen-2000 cool-window probe, boot D): [PREFILL-SUM] n=16 4-rank means wall 1642.3 = gemm 798.8 + ar 70.7 + body 645.1 + gap 122.7 ms/chunk (ngemm 192, gemm_over 0, rank-uniform); THE GAP NUMBER: 122.7 — NOT the ~900 unnamed residual the check chased; falsifiers applied verbatim: gemm NOT >1100 (798.8 warm), ar NOT >250 (70.7), body NOT <300 (645.1) -> letter of the tree = ELSE/G, but the same boot's gap column REFUTES G's mechanism (gap 7.5% is not large); WINNER NAMED: the in-layer non-GEMM/non-AR BODY 645.1 ms/chunk (39.3%, GDN mixer/conv/norm/activation class at T=128) + a ~270 ms GEMM underestimate in the doc sketch (predicted 530, measured 798.8 warm); chunk graph capture (Fix A) DEMOTED to the 122.7 ms gap class it can actually touch (~+7.5%, NOT 100-130 tok/s); prefill rate 75.5 tok/s ON the doc's 75.7 anchor (sideband: sclk 4-7 partial band, mclk 3 throughout)
- scope: CONCURRENCY-1 ONLY honored (no mc>=2 anything); kills = granted PID 310132 (cmdline-anchored, TERM) + this window's own boots A/B/C/D 523541/565312/576190/587686 (each anchored-verified before TERM, KFD=0 after each); every leg hammer/sideband covered; promote-to-default (unset -> simt) remains a coordinator ORDER — this window delivers the gated arm + its proof, boots stay byte-identical default until ordered
- meaning: the 50-TPS scoreboard's #1 lever LANDED and parity-held; next decode levers are the tp_gemv draft-arm retune (align/chain_fwd residual 2.16+1.39) and W4 one-shot AR; prefill's next lever is NOT graph capture — it is the non-GEMM in-layer op class (body 645), with GEMM's 799 warm as the second column
- provenance: results/amd/coherence/W8_LMHEAD_row.txt + LMHEAD_flip_row.txt + PREFILL_OPTRACE_row.txt + W8_LMHEAD_roofline_run_20260917_2023.log + W3_boot{A,B,C,D}_*_serve.log + W3_{control,flip,p1}_clocks.log + W3_{control,flip}_count600_resp.json + W3_p1_prefill_resp.json + BOOT_BATTERY_20260917_201153.row(+_clocks.log) + docs/amd/PLAN_50TPS_master.md updates; chain PLOG-043 -> this
- hash: 867ef25ac76933ee (prev=ea2d4c1f975c37e7)
### PLOG-045 [2026-09-17 ~21:5xEST] phase=WINDOW-RESULT — LMHEAD ROUTE PROMOTED TO DEFAULT (user order executed): unset now serves simt at BOTH cells, confirming leg GREEN (route proof 5/5 simt + ZERO small_t launches, fingerprint 204/0.971/2.94 in family, lm_head 2.45 ms <= 4 bar, greedy content byte-identical to all four banked legs) — the 50-TPS-class decode round (~60 ms, ~49 tok/s arithmetic) is now the DEFAULT route, legacy arm stays on =smallt for A/B
- commit 8bf74c44d (amd/tp4-cure): gate default flip in src/ops/linear/w8/w8_dispatch.cpp ONLY (one commit, cites the three banked gates W8_LMHEAD_row + LMHEAD_flip_row + BOOT_BATTERY 20260917_201153); N7 value-parse law unchanged (=0/=off/"" still refuse loud; =smallt still forces the legacy small_t arm; both cells case 62080 t<=4 + case 10240/n=5120 t<=4 stay on the one gate)
- bin: NINFER-serve_c46e352d8c2e8837 banked (/home/chris/artifacts_bin/ninfer-serve_c46e352d8c2e8837.bin, HEAD 8bf74c44d, -O3, whitelist parity 220/220; bank-before-relink verified: pre-relink build-tree binary bit-identical to banked 6c8ae21399516750); strings gate 3/3 (NINFER_LMHEAD_ARM + NINFER_VERIFY_TAIL_TRACE + new "default simt since the 2026-09-17 promotion" refusal text)
- BOOT_BATTERY 20260917_213446 OVERALL GREEN on the promotion binary (guard + both device cells + ladder fast arm; row + 11-sample sideband banked); boot healthy ~10 s, "loaded 40960 draft vocabulary IDs" x1, zero refusals; standing user server PID 605170 (serve_10k posture, bin 409450b83ffa4be8 — cmdline+sha verified before TERM; its 10k request had finished 21:01:33) retired clean, KFD=0, port freed
- ROUTE PROOF (default boot, env UNSET): [LMHEAD] first-seen 5/5 arm=launch_w8_simt_r8_c4 gate=simt at BOTH sites (62080x5120 T=1/3 + 5120x10240 T=1/2/3); grep arm=launch_w8_small_t across the WHOLE boot = 0 — the promoted default serves simt exclusively
- CONFIRMING LEG (count600 conc=1 greedy, warmup + 150 s cool gap + measured leg, 2 s sideband PROMOTE_clocks.log): measured HTTP 200 wall 14.61 s = 41.1 tok/s e2e; [TAIL-SUM] rank0 n=204 -> t/r 2.94, acc 0.971 — IN the M1/M2 family (201-204/0.97-0.99/2.94-2.99); lm_head 2.446 mean / 2.388 p50 / 3.461 max <= 4 PASS (== flip leg 2.526/2.466/3.46); lgather 0.674 <= 1; sync 55.076 (flip 55.713); align 2.122 / chain_fwd 1.355 = fc share held; round ~60 ms = 49 tok/s decode arithmetic — DEFAULT REPRODUCES THE FLIP LEG
- GREEDY TEXT: content byte-identical to ALL FOUR banked legs (W3_flip + W3_control + both M2 legs); reasoning_content matches both M2 legs byte-exactly and diverges from the W3 legs exactly as the W3 legs already diverged from M2 (pre-existing cross-binary think-stream class; the gate's object — visible content — unaffected in every comparison run); sideband: one instant-droop sample at leg start (mclk 1/sclk 2) recovered to sclk 5-7 in 4 s, steady decode clean-band (sync p50 54.5)
- scope: CONCURRENCY-1 ONLY honored; kills = granted PID 605170 (anchored, TERM) only; this window's own boot 652401 anchored-TERM'd after the leg (KFD=0); W4AR device leg follows in the same window under its own posture
- meaning: the promotion closes window-3's named remainder ("promote-to-default remains a coordinator order") — the fastest known route is now the default with the A/B escape hatch preserved; next levers unchanged (tp_gemv draft-arm retune ~10 ms + W4 one-shot AR in-loop 9.6 + prefill non-GEMM body 645)
- provenance: results/amd/coherence/PROMOTE_row.txt + PROMOTE_serve.log + PROMOTE_clocks.log + PROMOTE_count600_resp.json + BOOT_BATTERY_20260917_213446.row(+_clocks.log); gates cited: W8_LMHEAD_row + LMHEAD_flip_row + BOOT_BATTERY_20260917_201153 (+ BOOTBAT_20260917_201153_row.{json,txt} now tracked); chain PLOG-044 -> this
- hash: 105a0a1dbae05088 (prev=867ef25ac76933ee)
### PLOG-045 [2026-09-18 ~00:0xZ] phase=PREFILL-PK-VERDICT — V0-PK HFMA2 arm DEAD by pre-registered floor; 500-tok/s gemm front reopened as design question
- context: window-4 agent died (budget) AFTER banking PROMOTE GREEN (bin c46e352d8c2e8837 default-flip confirmed + battery rows committed); coordinator ran the decisive --pk bench directly (no agents, budget law). V0PK bench RC=0 at HEAD 22e42a796 on the free box.
- result: ALL PK arms 0.21-0.36x V0 across the five W4 shard geometries (V0 1989-2444 GF/s; PK best 710 @AttnInput-W16, worst 503 @Resid-W64); relL2(PK,V0)=0.00e+00 all rows (numerics exact, perf-only failure); pre-registered DEAD branch (<1.3x) fired exactly as designed
- meaning: HFMA2 packed-fp16 is falsified for M=128 prefill GEMM — convert/stage bill dominates MAC-heavy shapes (matches small_t-PK 1.07x parked decode datum); the 2.4-3.3x prediction band is DEAD, arm closed with its receipt. Gemm front alternatives: leaner issued-ops/MAC rewrite (census 2.4 ops/MAC, theory 4.5-6 TF/s) or accept F32-SIMT wall (~240-330 tok/s class). Decode unaffected (lm_head route flip = different seam, GREEN banked)
- provenance: results/amd/coherence/V0PK_row.txt + V0PK_bench_run.log + V0PK_NOTES.md; chain PLOG-044 -> this
- bin: none banked (bench-only session; server restore follows)
- hash: 5f71d6d591375e78 (prev=105a0a1dbae05088)
### PLOG-046 [2026-09-18 ~01:4xZ] phase=W4AR-REPRO — wedge cure verdict: silent hang -> loud crash (class guarded); one-shot world=4 still non-functional
- context: coordinator-run device cell after window-4 budget death (bin aec8ac56765458af = oneshot-ar cure build 7dd270662, NINFER_TP_ONESHOT_AR=1, bounded 100 s, 4-rank TP4 warmup)
- result: LOUD DEATH at ~20 s warmup — GPU node-3 memory access fault (Page not present), core dump; zero [AR-*] diagnostic lines (B2/B3 deadlines never reached); OLD failure was 2/2 17-min silent spin
- meaning: the liveness cure ACHIEVES its contract (no unbounded silent failure — the box can no longer hang on this path) but the one-shot world=4 path has a deeper memory bug that previously manifested as the wedge race and now faults on access; B1/B2/B3 never got to arbitrate. Follow-up desk: fault-find the world=4 one-shot access (compute-sanitizer arm) before re-adoption; ring stays transport everywhere (already default); decode/promotion unaffected (default boots never arm one-shot)
- provenance: results/amd/coherence/W4AR_repro_row.txt + W4AR_repro_serve.log; chain PLOG-045 -> this
- bin: aec8ac56765458af (cure build)
- hash: e034b016657edb47 (prev=5f71d6d591375e78)
### PLOG-047 [2026-09-18 ~02:1xZ] phase=BOX-RECOVERY — per-die GPU reset clears poisoned VM state; certified boot clean
- context: after the W4AR node-3 fault (PLOG-046) EVERY TP4 boot faulted at warmup (certified 409450b8 too, varying node, same 'Page not present' class); drive healthy (10.5 GB/s), GPUs idle 0% / 0 VRAM, no stray KFD holders => poisoned GPU VM state, reset-only fix
- result: sudo rocm-smi --gpureset -d N for N=0..3 (ONE die per call — multi-die call refuses 'one GPU per call'; syntax is --gpureset -d, NOT --gpu-reset/--gpuselect): all four 'Successfully reset' with ZERO hangs (GPUs idle — the historical reset hang was wedged-context contention, now avoided by retiring boots first); serve_10k.sh restore: clean warmup, 0 faults, SERVING :8100, 10k grade firing
- meaning: recovery playbook for any future device-fault poisoning: retire all boots -> per-die --gpureset -> serve_10k.sh; requires root (user-provided). W4AR fault-find desk must run its repro boots on a RESET box and clean up after itself (retire + reset own die) to avoid re-poisoning the serving boot
- provenance: /tmp/gpureset_[0-3].logs; session log; chain PLOG-046 -> this
- bin: 409450b83ffa4be8 (serving)
- hash: 1fa6b8c296b06d28 (prev=e034b016657edb47)
### PLOG-048 [2026-09-18 ~02:5xZ] phase=P2-BODY-SPLIT — the 645 ms prefill body NAMED: delta-net trio 73% (510 ms), gate 80, gqa 56; FIX desks staffed in order
- context: P2 tracer (74219298c4f3d4a4, 7 incremental commits, mode 2 = 12 classes + 5 depth-3) run by coordinator on the reset box; sanity BLUE finish=stop; probe plen-2016 n=16 chunks
- result: [PREFILL-BODY-SUM] 4-rank mean: dn 501.6-513.7 / gate 78.1-82.1 / gqa 54.7-57.4 / rest ~15 / unbr 57.9-91.3; body 677.5-698.3; rank-uniform 2% (compute class, the standing conviction law); prefill-class rate ~70.7 tok/s incl decode tail = on-anchor
- meaning: the body design desk's three suspects CONFIRMED in descending order; FIX-A (gfx900-native delta-net chunked rewrite + W dedupe, 510 ms owner) staffed now; FIX-B gate proj route fix + FIX-C GQA split-K follow. Body 690 -> ~75-90 target = the largest single step on the prefill road (75.5 -> ~140 class with gap+AR)
- provenance: results/amd/coherence/PREFILL_BODY_row.txt + /tmp/p2boot.log (banked as P2_boot.log) + P2_BODY_NOTES.md; chain PLOG-047 -> this
- bin: 74219298c4f3d4a4 (measurement boot, retired; serving restored on 409450b8)
- hash: 083439ccc9e263b9 (prev=1fa6b8c296b06d28)
### PLOG-049 [2026-09-18 ~03:0xZ] phase=W4AR-REVERDICT — cure GREEN at world=4 (wedge byte-match + serving, confirmed twice); PLOG-046 'memory bug' CORRECTED to box poisoning
- context: window-4 agent's uncommitted artifacts recovered (drills + W2 + wedge logs — it measured EVERYTHING before the budget death, committed nothing); coordinator re-ran wedge-repro on the post-reset clean box
- result: wedge-repro GREEN — [ids] 760 1156 1018 328 byte-match + serving (agent run 21:56 + clean-box run 02:50); drills ALL fired as designed: stall->[AR-WEDGE-WATCHDOG] exit=72, ahead_k200->[AR-DESYNC]+[AR-STALEPASS] exit=73, repeat->[AR-FAILOUT]+45x[AR-RETRY] exit=70, ahead_k2 census-counted alive (under window 64, correct), all kfd-clean; W2 legs 'NOT HEALTHY' incl CERTIFIED binary = box was already poisoned by drill-sequence abrupt deaths (many _exit with live contexts) — poisoning timeline explains PLOG-046's fault (misattributed)
- meaning: the W4-LOCKSTEP-LIVENESS class is CLOSED at the device level: no silent wedge possible (bounds fire loud, kfd clean), one-shot world=4 FUNCTIONAL. Adoption: default stays OFF (perf wash at decode sizes 7.6-12 vs 7.6-9.6 per ARTAIL; size-dead at prefill 1.31MB) — the flip's value is the liveness net, available under NINFER_TP_ONESHOT_AR=1. Ops law: cure-boot sequences must end with per-die reset (PLOG-047 playbook) before serving boots — abrupt _exit deaths poison the box
- provenance: W4AR_drills_verdicts.txt + W4AR_wedge_serve.log + W4AR_w2_verdicts.txt + W4AR_repeat_verdicts.txt + W4AR_cleanbox_row.txt + W4AR_cleanbox_repro.log (all banked this commit); chain PLOG-048 -> this
- bin: aec8ac56765458af (cure build)
- hash: bc26275a7bce919c (prev=083439ccc9e263b9)
### PLOG-050 [2026-09-18 ~07:4xZ] phase=W6-BODYFIX — gate fix PROMOTED default-ON (80.4->11.31 ms); gqa AMBER 29.3; dn fix RED BY WEDGE; window terminated by die-1 reset-proof thermal state
- context: window-6 GPU-owner (mission = written grant, CONCURRENCY-1): measure the three body fixes (FIX-A dn / FIX-B gate / FIX-C gqa) at HEAD 9fe491f8f, bin a43312bb9634bef9 (strings 4/4, battery BOOT_BATTERY_20260918_061052 GREEN); plen-2016 conc=1 first+only probes, OPTRACE=2, 120 s cool + sysfs sidebands every leg
- result: BASELINE reproduced PLOG-048 on the fix-build binary (dn 512.6 / gate 80.4 / gqa 57.2 / body 710.0, wall 1674.7). ARM-B GGATE_SIMT=1 GREEN: gate 80.4 -> 11.31 (x7.1, acceptance <12 PASS), dn/gqa columns unchanged, body 710 -> 626.5, wall -> 1579.0, client 29.34 -> 27.20 s (~76.4 tok/s prefill-class), parity byte-identical -> PROMOTED: gate default flipped (unset/'1'=SimtColTile cols>=9, '0'/garbage=mma rollback, trace-once every first consult), cell matrix updated + host cell ALL GREEN, bin2 93515c1f5854b346 banked; re-leg route proof via DEFAULT (<unset> -> col_tile) + parity PASS. ARM-C GQA_SPLITK=1 AMBER: gqa 57.2 -> 29.26 (x1.95; <20 acceptance MISSED, >35 RED not hit), route proofs exact (splits=4/96ctas@128, auto 2/3/4 by width), parity clean; pre-registered S=2 A/B WORSE (31.35) -> S=4 wins, stays env-gated. ARM-A GDN_SIMT=1 RED BY DEVICE WEDGE: T=51 single-chunk clean (dn 12.0), first T=128/chunks=2 trio call hangs dies 0/2/3 at 100% (300 s bounded, zero chunk completions), TERM ignored -> KILL, gpureset 0/2/3 receipts — 510 ms STILL ON the body. ARM-D combined: leg1 deep-droop band (disqualified), leg2 BOOT WEDGE at warmup (flaky 1-in-2, dies 1/2/3) — combined perf claim NOT banked
- meaning: the body's first real coupon landed (gate promoted, ~13 tok/s prefill-class gain); the dn trio wedge (deterministic at T=128) + a flaky combined-boot warmup wedge + the GQA device cell's NaN harness (29/29 comparisons -nan INCLUDING the shipped kernel vs oracle — cell authored zero-GPU, harness defect) are FILED for the desks with full evidence. Promotion acceptance did not require a new clean perf leg (identical route+kernel to arm B; default changes selection only — proven by the re-leg's default route proof + gate column)
- HARDWARE STOP: after armD2's wedge, die 1 entered a reset-proof stuck state — 79-82 W at IDLE (baseline 24-28), junction 91-92C, KFD empty, fan controller responding (76%), survives per-die AND all-die gpureset, kernel log clean — firmware class, HOST REBOOT required. Serving restore NOT fired (booting die 1 into load at 92C junction idle exceeds the MELTDOWN manifest's own 92C abort edge); serve_10k.sh is one command after the host reboots (certified 409450b8, or promoted 93515c1f after a fresh battery). Recovery-playbook addendum: "Successfully reset" needs an idle-power postcondition (~25-30 W/die) before recovery is declared
- provenance: results/amd/coherence/W6_row_baseline.txt + W6_row_armA_gdn_RED.txt + W6_row_armB_ggate_GREEN.txt + W6_row_armC_gqa_AMBER.txt + W6_row_armC2_gqaS2_AB.txt + W6_row_armD_combined.txt + W6_row_promote_ggate.txt + W6_THERMAL_INCIDENT_row.txt + W6_boot*_serve.log + W6_*_clocks.log (+ per-leg resp.json) + W6_gqa_cell_device_run.log + W6_ggate_cell_promote_receipt.log; commits 794848cea..42583d848 on amd/tp4-cure; chain PLOG-048 -> PLOG-049 (coordinator-staged entry, appended by window-6 to unblock the chain) -> this
- bin: a43312bb9634bef9 (measurement legs), 93515c1f5854b346 (promoted default, banked; serving NOT cut over — restore blocked)
- hash: 93fbf7dc3b21b00d (prev=bc26275a7bce919c)
### PLOG-051 [2026-09-18 ~19:3xZ] phase=RECOVERY-v3 + SESSION CLOSE — sysfs FLR clears what gpureset cannot; serving restored; PREFILL_1K plan written
- event: post-window-6 die 1 stuck 79-82 W / 91-92C junction at IDLE surviving 5x gpureset; during close BOTH dies 0+1 hot at idle (42-55 W / 84-90C). sysfs FLR on the two cards (echo 1 > /sys/class/drm/card0|card2/device/reset; GPU0=05:00.0 GPU1=08:00.0; card1 = NVIDIA 1050 Ti display — NEVER touched) brought all four dies to 24-30C / 3-8 W idle; 4 devices re-enumerated clean; serving restored :8100
- PLAYBOOK v3 (supersedes PLOG-047 v2): retire boots -> rocm-smi --gpureset -d N per die -> CHECK IDLE POWER (~3-8 W/die) -> if stuck hot: sysfs FLR the specific card -> recheck -> only then serve. "Successfully reset" alone proves nothing (v2 lesson) and may not clear firmware power state (v3 lesson). AMD cards only — card1 is the display adapter
- session close state: decode 41 e2e PROMOTED permanent; prefill 76.4 promoted-GGATE (93515c1f battery-re-verify owed; certified 409450b8 serving); FIX-A dn wedge RED filed (W1 of the new plan); FIX-C amber (harness fix owed); GEMM front closed with 4 falsifications (PK/LEANMAC/PIPE/CSHAPE — total cost ~2 windows, all receipts); PREFILL_1K_PLAN_2026-09-18.md written (physics: 1k = 54 TF/s agg = 54-63% pk16 peak — the only arithmetic class; realistic band 400-700; W1-W5 workstreams with kill-switch-first discipline)
- provenance: PREFILL_1K_PLAN_2026-09-18.md + W6_row_*.txt + this entry; chain PLOG-050 -> this
- bin: 409450b83ffa4be8 (serving)
- hash: 9c970e3f94f14442 (prev=93fbf7dc3b21b00d)
### PLOG-052 [2026-09-18 ~12:0xZ] phase=W7-BODY-PROMO — gqa PROMOTED default-ON (cell RED->GREEN + default-route leg + battery GREEN); dn wedge ROOT-CAUSED, OOB class cured at cell level; W3 pk16 sketch KILLED at zero GPU

- context: window 7 on lane amd/wo-w7-body (branched amd/main 7245244e6; GPU grant = user-direct in session); three pre-registered desks from the handoff: (1) W1 dn T=128 wedge root-cause, (2) GQA harness fix + arm-C promotion, (3) W3 pk16 sketch census (kill-switch first, zero-GPU step-0)
- result GQA: the cell's all-{-nan} device defect root-caused to the CELL's own Rng — `next() % 20001 - 10000` in UNSIGNED arithmetic wraps ~half of all draws to ~1.8e19 -> "uniform" ~1.8e15 inputs -> softmax inf/nan; production kernels NEVER wrong. RED captured twice (W6 banked log + fresh healthy-box run, deterministic 29/29, bin 5cd11fae257efc01) -> one-line signed-cast fix -> GREEN ALL PASS (8f19b93a0c0d66cd): shipped kernel vs fp64 oracle relL2 1.6e-3, splitk parity <=2.9e-3, comparator falsifier trips 1.6e-1. Promotion flipped unset->AUTO ('0'/garbage rollback, GGATE shape); default-route leg on banked a943225ad6da661e: [GQA] splitk=on splits=4 ctas=4x6x4=96 width=128 FROM DEFAULT (no env), probe plen-2075 parity BLUE/completion=41, BOOT_BATTERY_20260918_112525 OVERALL GREEN (host guard + ladder fast arm + sideband). PERF note (band-uncontrolled, not comparable to W6 n=16 columns): wall mean 1521.8 ms/chunk at n=17 -> 80.1 tok/s prefill class. SERVING CUTOVER: a943225ad IS the :8100 server; serve_10k.sh flipped; certified 409450b8 = one-line rollback
- result W1: wedge root-caused from source (agent desk, HIGH confidence) — h_chunk base used the TOKEN offset where the CHUNK index belongs in state_passing_simt.cuh + output_simt.cuh (64x OOB at chunk 1, ~24 MiB past a 768 KiB tensor); prepare Phase-G v_row double-offset (wrong numerics all shapes + ~190 KiB OOB reads); premise correction: T=51 never ran the trio (T<64 = recurrent path; the trio's first device execution ever was the wedging call). Device cell (tools/v340l/dn_simt_chunk_cell.cu: poison canaries + 10 s watchdog, mma-vs-simt A/B): pre-fix 20 FAILURES with the chunk-1 workspace stomp at EXACTLY the desk-predicted byte (h_chunk + (64*12+0)*16384 elts); post-fix all stomps gone, chunk handoff self-consistent (2xT=64 chained == T=128), state vs mma 3.3e-8/8.9e-7. REMAINING: prepare W relL2 ~1.2 + U ~0.13 vs mma (attn ~0.14) — Phase-G residual desk in flight (W1_PHASEG_DELTA_desk.md); NINFER_GDN_SIMT stays default-OFF, no promotion until cell relL2 < 1e-2 + serve-leg parity
- result W3: SKETCH CENSUS KILLED IN-DOC at zero GPU (3 design compiles = the 2-3 budget): resourcing cured vs PK (102 VGPR, 0 B scratch, native v_pk_fma_f16 x128/body) but LDS batch cannot fund — 44 waits/body vs <=20 acceptance, group-flush tax +50% MAC count shape-invariant, A=32 spills 136-140 B, measured issue model 39-49% of pk16 peak < the 54% bar at ZERO stall; v_dot2_f32_f16 absent on gfx900 (dot10-insts). Reopen conditions named: compiler wait-emission at <=112 VGPR, or gfx906+ hardware class. LEDGER MEANING: the 1k arithmetic family is CLOSED; the honest program band stands at 400-700 and 1k needs a named reopener (the plan's own framing, now with the W3 receipt)
- ops: two leg-script pgrep lessons filed in W7_row_promote_gqa.txt — pgrep -x 'ninfer-serve' matches NOTHING (comm truncated to 15 chars) so a retire can silently no-op; pgrep -f without a ^ anchor self-matches the caller's args. Safe form: pgrep -f '^/home/chris/artifacts_bin/ninfer-serve'. Leg-1 of the promotion booted into 828 MiB free against the still-running old server and its probe hit the OLD server (invalid claim, discarded before banking)
- provenance: W7_gqa_cell_red_green_row.txt + W7_gqa_cell_{prefix_RED,postfix_GREEN}_run.log + W7_row_promote_gqa.txt + W7_boot_promote_gqa_serve.log + W7_promote_gqa_resp.json + BOOT_BATTERY_20260918_112525.row(+_clocks.log) + W7_dn_cell_prefix_RED_run.log + W7_dn_cell_postfix_run3.log + W1_DN_WEDGE_ROOTCAUSE_desk.md + W3_SKETCH_CENSUS_step0.md + W3_sketch_isa_v1.txt + PREFILL_1K_PLAN ledger rows (this commit); chain PLOG-051 -> this
- bin: a943225ad6da661e (serving, promoted ggate+gqa), 409450b83ffa4be8 (certified fallback); cell bins 5cd11fae257efc01 (pre-fix RED) / 8f19b93a0c0d66cd (post-fix GREEN)
- hash: 42d8d4b8786b0f13 (prev=9c970e3f94f14442)
### PLOG-053 [2026-09-18 ~12:3xZ] phase=W7-DN-PROMO — NINFER_GDN_SIMT PROMOTED default-ON: dn 480 -> ~30 ms/chunk (x16), prefill class 76 -> ~116-121 (+~55%); G4 ladder measured (chunk256: +15%, GEMM flat); payoff order re-issued

- context: continuation of window 7 (same lane, same grant). User order: focus the largest windows, shortest ordered list, biggest payoffs first. The two desks in flight (Phase-G residual, W2 gap capture) + the cheap decisive G4 ladder probe
- result PHASE-G: the W/U residual was TWO defects, isolated by a float64 re-simulation that reproduced every cell number (W1_PHASEG_DELTA_desk.md): DELTA-1 W product read K column d2 instead of d_off+d2 (one line; made all four 32-col W blocks identical); DELTA-2 Phase-E block-Schur missing both unit-diagonal block factors (two-pass rewrite, +1 KiB smem). Post-fix cell = ALL PASS at the desk's predicted ~1e-6 (W 2.0e-11/7.7e-7, U 3.2e-8/1.3e-6, attn 0.0/1.9e-6, T=64 and T=128, canaries clean, handoff consistent)
- result DN PROMOTION: serve leg env-armed at the EXACT former wedge geometry (T=128/chunks=2) COMPLETED — 17 chunks, 87 body traces, no wedge, no fault; dn column 480 -> 28.9/29.6/30.9/32.1 ms (x16), wall 990-1050 ms/chunk ~ 120 tok/s class. Default-route leg on 1e03e5af5fb438b9 (env UNSET): [GDN] <unset> -> SIMT route proof from default, probe BLUE/stop (completion 41->52 = documented summation-order trajectory drift, content unchanged). BOOT_BATTERY_20260918_120531 OVERALL GREEN. SERVING CUTOVER: 1e03e5af5fb438b9 IS :8100 (ggate+gqa+dn); serve_10k.sh flipped; rollback NINFER_GDN_SIMT=0 (byte-identical mma). Parity honesty: SIMT is a different summation ORDER (cell 1.9e-6), bit-identity not claimed and not holds
- result G4 LADDER (chunk-256 probe, 9 chunks, plen-2075): wall ~2770 ms/chunk = 92.4 tok/s class (+15% vs 128); gemm 1748-1822 ms/256tok => GEMM TF/s DID NOT RISE (~2.0-2.1 TF/s/die) — the per-thread-resource closure binds at any M; dn is launch-bound (2.15 ms/token vs 3.76 — the dn cure amortizes even better on the ladder); gap is token-proportional (~460 ms/256-chunk, fatter than the plan's 122.7-era fixed-cost model). Ladder compounds with W1/W2 but unlocks nothing in GEMM
- WALL ACCOUNTING after dn promotion (per 128-chunk): gemm ~735 (73%) | gap ~92-152 | ar ~49-78 | unbr ~49-67 | gqa 34 | dn 30 | gate 11. Remaining software levers: W2 gap capture (desk in flight) ~-90..-110, AR floor ~-25, unbr (same capture medicine) ~-30 => honest stack ~140 tok/s class; the ceiling with the GEMM front closed (5 ledger kills + this window's M=256 confirmation) is ~163 tok/s even at zero non-GEMM cost. Bigger requires a G1 reopener (gfx906 dot10 hardware, compiler wait-emission at <=112 VGPR, or a different arithmetic class)
- provenance: W7_row_promote_dn.txt + W7_dn_simt_serve.log + W7_dn_default_serve.log + W7_dn_default_resp.json + BOOT_BATTERY_20260918_120531.row + W7_row_ladder_chunk256.txt + W7_ladder_chunk256_serve.log + W1_PHASEG_DELTA_desk.md + commits 31861a311 (DELTA-1/2) + promotion commit on amd/wo-w7-body; chain PLOG-052 -> this
- bin: 1e03e5af5fb438b9 (serving: ggate+gqa+dn promoted), 7a1af98f65fbad8c (dn env-leg), a943225ad6da661e (gqa-era), 409450b83ffa4be8 (certified fallback)
- hash: 5139c1da91dd89ad (prev=42d8d4b8786b0f13)
### PLOG-054 [2026-09-18 ~13:2xZ] phase=W7-PK16-BENCH — GEMM pk16 REOPEN bench: MEASURED KILL 1.34x V0 mean (pre-registered <1.5x); GEMM front now closed with a measured floor; REV2 additive path is the road

- context: user plan-review order ("think through where we can multiply") -> REV2 written (goal change 700-class, payoff-ordered attack); desk #1 = the W3 sketch as a REAL bench arm; two agent launches hit box concurrency limits -> owner executed inline
- setup: tools/v340l/w7_pk16_bench.cu = the SWEEP bench verbatim (validated: V0 reproduced SWEEP_row within ~2%: 3157/2830/3166/2396 GF/s vs 3083/2756/3103/2366) + the w3 sketch included as an arm + STILES=96 instantiation. Census-first kill-switch PASSED for the benched arm: A16/TN64 = 102 VGPR, 0 B scratch, v_pk_fma_f16 x128/body
- result: MEAN pk16/V0 = 1.34x (AttnInput 1.29 / GdnInput 1.34 / MlpGateUp 1.28 / Residual 1.43) => KILL under the pre-registered <1.5x floor. pk16 sustains ~4.0 TF/s/die = 38% fp32-nom / ~19% pk16 peak — the census's 44 waits/body do NOT hide (REV2's ceiling-not-floor caveat fired). Numerics FAIL (relL2 ~146 vs REF) recorded as by-design-undebugged: the sketch was a resource census artifact; a numerics fix only adds work and cannot change the verdict
- meaning: the pk16 GEMM class is killed THREE times at three bars (HFMA2-PK resourcing 1.07x -> W3 census 54% 1k-bar -> W7 bench 1.34x under the 700-class 2x bar). The GEMM front on this hardware/compiler is CLOSED WITH A MEASURED FLOOR (~3.1-3.2 TF/s/die V0 = the wall's 75% is not software-addressable). REV2 projection reverts to additive: finalize anomaly (1393 ms/request, staircase measured, code region named) + AR floor + capture => ~140-160 tok/s class; 300+ requires gfx906 dot10 hardware or a ROCm wait-emission change
- provenance: W7_pk16_bench_row.txt + W7_pk16_bench_run1.log + PREFILL_PLAN_REV2_2026-09-18.md (verdict note) + ledger reopen row CLOSED; commits 31861a311..eb89efb8a on amd/wo-w7-body; chain PLOG-053 -> this
- bin: 1e03e5af5fb438b9 (serving, unchanged by this desk)
- hash: 83e118555cdd6139 (prev=5139c1da91dd89ad)
### PLOG-055 [2026-09-18 ~15:0xZ] phase=W7-FINALIZE-CURE — finalize-chunk anomaly NAMED AND CURED: host-side MTP-tail stall (pageable H2D staging class + sync/D2H), NINFER_MTP_TAIL_ASYNC=1 device recipe; chunk-17 prep 922-1602 -> ~14 ms x3 ranks; parity GREEN every leg; promoted 0572d4108317ee85

- context: finalize desk continuation on the FINAL platform (iommu=pt live, clocks pinned high, 4x gfx900); mission = name and cure the ~1394 ms chunk-17 (tok=27) gap with the rotating 330 ms-quantized rank staircase; prime suspect was the recurrent-dn T<64 tail
- exonerations (measured): recurrent-dn tail (dn 7.2-8.3 ms at tok=27, body bracket); MTP finalize/AR path (mtp_proposal_extent_==0, no LMHEAD in window, no ar-steps — never executes in serve); sync+D2H alone (v1 cure removed both, gap persisted 654-1328); clock band (same staircase pre-pin 12:03/12:08 and post-pin)
- bracket chain (v3 fin marks -> FINHOST host stamps -> FINMTP stem stamps, all env-gated, zero cost unset): prep bracket owns the whole gap (922/943/1272/1602 vs gap 925/946/1275/1605); FINHOST split: copy= (pageable H2D of mtp_ids, a 108-byte staging size class NEW to the process) = 659.6 ms on ALL FOUR ranks +-0.4 ms, chunk= (mtp_prefill_chunk host section) 669-1333 (~330 ms quanta, process-serialized); warmup final chunk (T=51, same calls) is 0.6-49 ms => state/width-dependent, not region-intrinsic
- CURE (NINFER_MTP_TAIL_ASYNC=1): compose mtp_ids entirely on device for the text path — prompt columns = the chunk's own ids shifted by one (D2D from ids_device, already resident), final column = io_.token (D2D, same-stream after the is_last allreduce_argmax, same champion every rank); guard shifted_embedding_begin == text_prefill->begin + 1 && multimodal == nullptr (v5 leg caught the recipe-bail: text_prefill is NEVER null on the serve text path); unset = byte-identical legacy path
- before/after (same probe plen-2075 mt64 temp0, pinned+pt): copy= 659.6 -> 0.007-0.008 ms; chunk-17 gap 925-1605 -> 16.6/16.7/17.3 ms on 3/4 ranks (steady-chunk level ~17-20) + one 320-336 straggler; chunk-17 wall max 2266 -> ~1360; request mean unbracketed gap/chunk 126-167 -> 73-92 (REV2 §5 projected -82 amortized; measured -53..-75); steady chunks unchanged; parity 'BLUE'/stop/52 on every leg
- residual named NOT cured (one-time per PROCESS): first mtp_forward_stem call at a NEW width class costs ~330 ms host (T=27: 328-676; T=2/51/128 warm from first call: 0.015-1.2) — width-class-specific first-use, paid in parallel, exposes on-stream only on the rank with the smallest layer backlog; proposal: warm a ~27-token prefill during serve boot warmup (one boot-path edit + verification leg, next window)
- promotion: BOOT_BATTERY_20260918_145747 OVERALL GREEN (port 8100, clocks sideband banked); serve_10k.sh flipped to bin 0572d4108317ee85 + NINFER_MTP_TAIL_ASYNC=1; that bin left SERVING on :8100
- provenance: W7_finbracket_row_v2.txt (full A/B ledger + owner chain + 6 bins) + W7_fintail_{base_v3,async_b_leg,finhost_leg,v5_recipebail,v6_recipe_leg}_serve.log + W7_fintail_async_serve.log (FINMTP leg) + W7_finbracket_v2_1341_serve.log + PREFILL_PLAN_REV2 §3 verdict rewrite + resp jsons; commits 2c253b19b, eaa958bbd + this; chain PLOG-054 -> this
- bin: 0572d4108317ee85 (SERVING, promoted), d986ea7a187b87c5 (cure, identical modulo FINMTP prints), 6a684e7f2988adf7 (baseline brackets), 1e03e5af5fb438b9 (prior serving)
- hash: acab21aa7120af0f (prev=83e118555cdd6139)
### PLOG-056 [2026-09-18 ~21:1xZ] phase=W7-VPERM-FALSIFY — co-residency hypothesis FALSIFIED by two independent desks (V3c/V0 1.016x/1.023x at measured 3 blocks/CU); v_perm ~1.1x is issue-side, its ceiling at every register budget; side-product: 2 latent V-family bugs RED->GREEN closed; F1a draft-vocab coverage 97.6% banked

- context: night-shift continuous run (user order: 12h, no breaks). vperm-3blocks desk (push V arm 113 VGPR -> <=96 so 3 blocks/CU) DOUBLE-DISPATCHED by accident — pre-break agent had survived the context break and a re-dispatch ran in parallel; both executed independently and reached the SAME verdict (companion rows, no overwrites) — a replication, not a conflict. Decode desk (agent a7b1b499) alive throughout, mid-F4a; unbr attribution desk launched at entry time
- result V3c (desk A, commit 38af51c90 + b182e2c6f): survivor variant c <32,2,64,32,256,1> = 71 VGPR / 0 B scratch / 8 KB LDS, measured 3 blocks/CU; V3c/V0 mean over 4 geometries x 3 back-to-back same-window runs = 1.016x (spread +-3.3%), per-geometry profile TRACKS V instead of scaling with occupancy. Pre-registered gate <1.15 => FALSIFIED: co-residency is NOT the limiter; the v_perm win is issue-side. Census law held: e-forcing probe = 84 VGPR + 104 B spill (honest 3-block bar is <=84, not 96); variant f 161 VGPR killed at census pre-bench; kS=2 stripe alone lands 90-92 and does NOT unlock the third block (KTPR=2 rows-per-thread rebalance is what does)
- result V3c (desk B, commit de6a33eed): INDEPENDENT replication — V3c mean 1.023x (V arm reproduced its banked 1.102x within band; V0 anchor reproduced banked within 6.5% cross-window); run1 band-dead (server burst mid-run, 0.59x on a bit-identical-layout kernel) self-rejected and kept as receipt. Same verdict, same census table
- result RED->GREEN x2 (both desks, independently — the desk's real product): two latent V-family bugs invisible on the uniform data that gated the banked repack row: (1) positional-x stage read x at block-local token instead of blockIdx.y*TN+t under grid.y M-tiling => exactly half the output columns computed from wrong tokens on real requests (fraction-arithmetic RED capture); (2) restage word order fed 0,1,2,3 where the decoder emits (k0,k4),(k2,k6),(k1,k5),(k3,k7) — declared order 0,4,2,6,1,5,3,7 => quarter of products wrong (relL2 RED 0.546). RED shas a4de0b567 (relL2 4.760e-01) / 67eb41674 (1.089e-01) -> GREEN e7985fadf (1.285e-03, fp16-window class), bin 70b89f70587f38eb, permanent guard: --v3parity patterned cell. Production V0 immune by construction (never tiles M). Banked V/PV TIMINGS stand (fixes value-invariant on uniform data) but any V integration MUST carry both fixes (landed in w7_repack_bench.cu + w7_vperm3_bench.cu)
- result F1a (decode desk, legs banked): draft-vocab COVERAGE = 97.597% (18,239/18,688 output tokens re-tokenized, 28 prose generations off the LIVE 7c11c3ac server, SE +-0.112 pct-pts; content 98.95% / reasoning 96.28%). The 40,960-id slice is NOT the acceptance suppressor (k=2 ceiling at c=0.976 is 2.93 vs measured acceptance 0.65); slice widening REFUSED by the banked decision rule (would buy <=2.4% acceptance mass for +130 MiB/rank). Recommendation of record: next prose-serving boot sets NINFER_VOCAB_COUNT_DIR to re-measure on the true emitted stream (zero code, counter in-tree)
- meaning: with this row the GEMM-front falsification set is COMPLETE: mad-mix 0.48x (resourcing), pk16 sketch 1.34x (wait-bound floor), K-split V0 (no co-residency headroom at 128 VGPR), and now explicit 3-blocks/CU (occupancy raised by measurement, no time moved). The wall is access-pattern/issue-side at ANY achievable occupancy on gfx900/ROCm-6.2. Reopen-if: a compiler that changes VGPR allocation or wait emission, or gfx906-class dot hardware. Remaining software levers are the additive set: unbr 53 ms/chunk (desk now in flight), AR floor ~25, gap capture, decode-side F4a draft-arm GEMV (50x software gap) — decode/pre-dequant/q4-KV class work per REV2
- provenance: W7_vperm3_row.txt (desk A) + W7_vperm3_coresidency_row.txt (desk B) + W7_vperm3_parity_{RED,GREEN}*.log + W7_vperm3_runs.log + W7_VPERM3_desk.md + W7_REPACK_RESUME.md amendments + results/amd/w7_f1a/ (raw JSONL + coverage_report.md) + W7_DECODE_RETUNE_desk.md; commits 38af51c90, b182e2c6f, de6a33eed on amd/wo-w7-body; chain PLOG-055 -> this
- bin: 7c11c3ac07863366 (SERVING, unchanged), 70b89f70587f38eb (parity GREEN cell), 76a98ca6f3838f06 / ba364d711a04f023 (RED captures)
- hash: 189da87cc3cc5a44 (prev=acab21aa7120af0f)
### PLOG-057 [2026-09-18 ~21:1xZ] phase=W7-DECODE-RETUNE-CLOSE — decode retune desk CLOSED: F4a is a PREMISE FALSIFICATION (the "50x draft-arm GEMV gap" never existed in serving — head already at 1.15x of the memory floor on the routed kernel); F1a coverage 98.4-99.4% on real prose, widening NOT triggered; serving recipe hardened (BIN flip-back footgun + vocab counter)

- context: night-run desk closure (decode retune, agent a7b1b499, ~113 min); commits 69db827f3 + 09cc0ce20 on amd/wo-w7-body. The desk was dispatched to retune the draft-arm GEMV under the banked premise "~10 ms/round vs 0.19 ms floor (~50x software gap)"; it killed the premise instead — exactly what pre-registration is for
- result F4a NO-GO (measured): all six draft-round GEMVs ALREADY ride launch_w8_simt_r8_c4 at T=1 (verified in code, not assumed). Isolated roofline (die 2, 300 iters/arm, 2 runs <=1.5% apart): proposal head [10240,5120] 0.232 ms @240 GB/s = 1.15x of the 265-280 GB/s floor; fc [5120,10240] 0.246 @227; attn-qkv 0.090 @218; gate_up 0.203 @233; down 0.135 @175. The legacy "54-56 GB/s" claim on [14336..34816,5120] FALSIFIED — those shapes measure 242-246 GB/s. A/B: r8_c4 best on EVERY shape; r8_c8 loses 3-8% (relL2 0.0, bit-identical); small_t loses 10-72x where registered (relL2 <=3.5e-7). No candidate >=1.5x exists => nothing to flip; serving already routes the winner. Zero kernel edits => census exempt; wave64 checklist verified clean in code
- re-pricing (decode-side wall): forward kernel sum 2.007/2.014 ms/round vs banked 3.549 ms tp_gemv serving residual => ~1.5 ms/round is NON-GEMV. The "~10 ms/round" of the master plan was the pre-flip small_t class — already harvested at PLOG-044. Re-priced fire order: F2 W4AR (unchanged, the big lever) > align/embedding machinery ~1.15 ms > per-forward non-GEMV ~0.8 ms
- result F1a CONFIRMED on real prose: 334/336 = 99.40% (SE +-0.4%, 21 varied prose prompts through the live lane boot, engine-printed ids; single miss id 68111 x2) + concurrent foreign stream 98.75%; REALK sky-blue anchor 63/64 = 98.4%; counting class 98.0% (matches the 0.97-0.99 acceptance anchor); healthy probe corpus 83.7% (prompt-dependent; 6 ids = 41% of misses); corruption-era logs 73.1% => SIDE-FINDING: slice coverage doubles as a cheap corruption tripwire. Widening NOT triggered; acceptance suppressor remains drafter quality/quant-flip class (F1b conditional per desk doc). Serve-leg recipe banked (zero code): NINFER_VOCAB_COUNT_DIR=<dir> on any prose boot; flush(shutdown) reports coverage at any run length
- ops (coordinator, same beat): serve_10k.sh BIN line was STALE (0572d410) while the live server is 7c11c3ac07863366 — any runbook restore would have silently dropped the stem-warmup cure; BIN flipped to 7c11c3ac with a history comment. NINFER_VOCAB_COUNT_DIR=/home/chris/vocab_counts added to the boot env (F1a free rider: the next window's boot counts true-stream coverage at shutdown, zero cost)
- provenance: results/amd/coherence/W7_decoderetune_row.txt + W7_draftarm_run{1,2}.log + tools/v340l/w7_draft_arm_bench.cu (cell bin sha fd764d9fa134a552) + results/amd/w7_f1a/ (raw JSONL + validators) + W7_DECODE_DESK.md + W7_DECODE_RETUNE_desk.md + W7_DECODERETUNE_RESUME.md; commits 69db827f3, 09cc0ce20; chain PLOG-056 -> this
- bin: 7c11c3ac07863366 (SERVING + now the runbook BIN), fd764d9fa134a552 (roofline cell)
- hash: 59ac06b1527ea66a (prev=189da87cc3cc5a44)
### PLOG-058 [2026-09-18 ~22:4xZ] phase=W7-UNBR-NAMED — the prefill "unbr ~53 ms/chunk" is NAMED (two mechanisms, both measured): ar-bracket re-record drops the mixer AR (~59-60 ms, accounting) + per-chunk ~995 ms pageable-H2D enqueue stall (real waste, ~14.3 ms device idle); pinned-vs-pageable cell PASS x3; serving restored GREEN

- context: W7 unbr-attribution desk (lane amd/wo-w7-body, serving-window grant). Target: REV2 §1 body column "unbr 53" — time inside layer bodies owned by no named op. Constraints honored: no cmake builds (disk 96%), knob-in-bin verification first, EXACT anchored retire only, restore-to-canonical at window close.
- instrument: the banked serving bin 0572d4108317ee85 already carries NINFER_PREFILL_OPTRACE (strings 5/5 gates) — zero tree edits for the trace legs. Leg A: OPTRACE=3 boot, 2k probe (plen-2075 mt64: 'BLUE'/stop/52, wall 19.5 s) + 10k probe ('BLUE'/stop, 102.5 s). Leg B: SAME bin under rocprofv3 (hip-runtime + kernel + memory-copy traces, csv, ONE 2k probe, parity GREEN, +7.7% wall trace overhead). Timeline stamps share CLOCK_MONOTONIC with OPTRACE t= (verified 3948/3952 tiled GEMMs inside the log-derived window).
- measurement (chunk-5 device timeline, GPU0, single queue, ZERO overlap, 98.5% busy): gemm kernels 821.4 (nvfp4 tiled 714.2 + W8-SIMT 107.2 — full-attn qkv/o-proj run w8_rowsplit at ~5.4-6.4 ms/launch, all inside the gemm column) | nccl AR busy 119.5 (125 ARs, mean 956 us, p50 742, max 11.1 ms straggler) | named-12 ops ~73 | unnamed kernels ~2.3 | IDLE 15.2 (618 intervals: 14.3 sync-class, 0.9 launch-gap) | in-body H2D device ~0.02. OPTRACE unbr bucket CLOSES as: ~59-60 ar-undercount + ~14.3 stall-idle + ~0.9 gaps + ~0.3-2 unnamed + ~0 H2D; E≈0 within band slop.
- FINDING 1 (accounting): OPTRACE ar column 60.3 = HALF the nccl device time (119.5). VerifyLayerTrace owns ONE ar event pair per layer; BOTH per-layer ARs (mixer :2603/:3013, mlp :3219) call ar_begin and the mlp site RE-RECORDS the begin event, so only the MLP AR is timed; the ~64 mixer ARs/chunk (~59-60 ms) fall out of ar into body -> unbr. Fix = instrumentation (per-AR slots), zero wall ms. CONSEQUENCE: TRUE AR device cost = ~120 ms/chunk (2x the plan's 60) — the AR floor desk's premise doubles; the §4 "capture medicine (unbr 53)" premise is retracted (consistent with the §8 capture kill).
- FINDING 2 (real waste): per chunk EACH rank thread blocks ~995 ms inside ONE ~512-B pageable hipMemcpyAsync (device transfer 3.7 us executing only at chunk end on the in-order stream; ZERO other API calls overlap the block; 4/4 ranks identical, 16 giants each per prefill) + a medium 27.5-us H2D blocking 51-60 ms. Class = the CURED finalize bug's staging class (PLOG-055), still live on the steady-chunk path (copy_i32 pageable wrapper; chunk-ids/worker-redo sites :3598/:3883 region; TAIL_ASYNC cured only the finalize tail). Launches land in one ~65 ms burst; the queue runs 1 chunk deep and thin -> 618 micro-idles + 15.5 ms host chunk-end sync.
- deliverable-3 cell (pre-registered gate IN the source header): w7_h2d_stall_cell — on a stream queued ~200 ms, 512-B pageable H2D must block >=100 ms while identical pinned returns <1 ms, parity byte-equal. PASS x3 (pageable 167.24/184.22/184.54 ms vs pinned 0.58-0.66 ms, parity OK) — cure mechanism MEASURED (~300x host-block collapse). Serve-leg A/B of the cure NOT run (needs bin rebuild; cmake banned this window per disk law).
- re-derivations of note: unbr is CONTEXT-LENGTH-INDEPENDENT (54.9 mean at 10k vs 52-56 steady at 2k; gqa grows 8->55->160 with context — REV2 §1's "droop rise" in body was partly context growth, not clocks); chunk2 unbr ~94-98 (first steady, 2x); finalize chunk still 342-399 unbr (PLOG-055 residual + this accounting).
- ranked fixes banked in results/amd/coherence/W7_unbr_row.txt: (1) pin/D2D the per-chunk ids H2D (~14.3 ms/chunk + robustness; cell-priced), (2) ar pair fix (attribution), (3) re-premise AR floor desk on 119.5 ms true AR (the #2 wall), (4) unnamed-kernel fusions below noise, (5) capture-medicine premise retracted.
- restore: performed by the COORDINATOR at 21:23 (the desk died mid-final-step in the platform usage-limit event ~21:45-21:55 that also killed the round-wall desk; no OOM, memory healthy): bash /home/chris/serve_10k.sh re-booted the runbook config with the BIN line first CORRECTED 0572d410 -> 7c11c3ac07863366 (the script was stale vs the live server and would have dropped the stem-warmup cure on every restore — footgun closed, history comment in-file) + NINFER_VOCAB_COUNT_DIR armed (F1a free rider, PLOG-057); /health ok; its 10k known-answer grade = the generation leg: PASS (finish=stop, content 'BLUE', prompt=10000, completion=69)
- provenance: results/amd/coherence/W7_unbr_row.txt (budget table + fixes + excerpts) + W7_unbr_trace_excerpts.log + W7_unbr_optrace3_serve.log + W7_unbr_tl_serve.log + w7_unbr_tl/*.csv + W7_unbr_clocks.log + W7_h2d_stall_cell{.cu,_run.log} + docs/amd/W7_UNBR_desk.md (checkpoint law, per-step) + docs/amd/PLOG_057_unbr_entry.md (renumbered 058 on append); chain PLOG-057 -> this
- bin: 7c11c3ac07863366 (SERVING, restored canonical), 0572d4108317ee85 (trace-leg bin — carried both OPTRACE gates; no rebuild by this desk)
- hash: 745a8f37a308c017 (prev=59ac06b1527ea66a)
### PLOG-059 [2026-09-18 ~23:5xZ] phase=W7-UNBR-CURE-PROMO — pageable-H2D enqueue stall CURED (NINFER_H2D_PINNED_STAGE=1, PinnedCopyRing), promoted to canonical runbook: 10k soak 70.7 tok/s prefill (62.5 banked first-soak era), 2k series FLAT vs control DECAYING; parity BLUE/stop/52 on all 13 requests; BOOT_BATTERY GREEN

- context: night run continued INLINE (platform usage limit killed all desk agents ~21:45-21:55; reset ~12:42 tomorrow; owner executed per PLOG-054 inline precedent). Cure = PLOG-058 FINDING 2 (the one REAL recoverable waste: ~512-B pageable chunk-ids H2D blocked each rank's enqueue thread ~995 ms/chunk because a pageable async copy cannot return until the in-order stream executes it at chunk end)
- implementation: PinnedCopyRing in text_context_impl.h copy_i32 — per-thread ring of 64 pinned slots, event-guarded reuse (a slot is handed out only after its completion event fires => a pending copy can NEVER observe a rewritten payload = the torn-payload trap closed by construction); exhausted-ring degrades to legacy-class blocking rather than corrupt; unset/"0" = legacy pageable call byte-identical; ANY pinned-path failure (alloc/event/query) falls back permanently in-process — no new refusal path anywhere (VRAM-law class respected). Shim +2 names (cudaEventQuery, cudaErrorNotReady) with provenance comments; both follow the shim's established alias pattern. Builds GREEN (1 header touch => qwen3_6 27b/35b_a3b variants + tp2_backend recompiled)
- cell chain: desk's w7_h2d_stall_cell PASS x3 (pageable 167-185 ms vs pinned 0.58-0.66 ms host block at identical queue depth, parity byte-equal — ~300x collapse) is the RED/GREEN cell for the CLASS (any pageable staged H2D, any stream depth); source-level fix committed before build; bin BANKED 07fcf7a5d98dea7d (BANK-BEFORE-RELINK)
- serve-leg A/B (2k probe plen-2075 mt64 temp0; parity BLUE/stop/52 on ALL 12 requests both arms): PINNED boot wall 18.7/18.9/18.8/18.8 s FLAT over 4 requests, unbr request-mean 69.2 -> 52.4 -> 45.6 -> 44.9 (plateau), body -> 133.6-135.0. UNSET control (SAME bin): wall 19.7 -> 23.0 -> 25.8 s RISING, unbr -> 83.8. First-request-vs-first-request IDENTICAL (69.2 vs 69.5, both = boot-A's 72.2 class) — the cure's effect emerges under sustained load, exactly the serving regime
- HONEST CONFOUND NOTE: cross-boot absolute deltas are thermally confounded (control boot started hot and decayed; pinned boot started cooler and held). The named reopen-if: paired same-window A/B + rocprof timeline on the cure bin, including the AR-OVERLAP hypothesis — with the queue no longer starved, the ~64 mixer-ARs/chunk may overlap compute (which would explain steady unbr landing BELOW the ~60 ms ar-undercount floor the stalled timeline implied)
- 10k soak (canonical cure boot, first big soak of fresh boot, PASS BLUE/stop/69): [tp2] acceptance=0.71 tok/round=2.45 rounds=29; prefill=141.47 s => 70.7 tok/s prefill (log: prefill=70.7t) vs 62.5 tok/s banked first-soak era = +13%. DIRECTION is mechanism-consistent; magnitude does NOT decompose cleanly to the cell-priced 14.3 ms/chunk (78 chunks x 14.3 ms = 1.1 s, observed delta 18.6 s) — cross-era thermal band differences are the named confounder; the paired-A/B reopen-if owns the decomposition. Decode phase 5.16 s/71 tok
- promotion: BOOT_BATTERY_20260918_225600 OVERALL GREEN (guard + ladder fast arm + 18-sample clocks sideband; device cells SKIP — not compiled this tree). Runbook flipped: BIN 07fcf7a5d98dea7d + NINFER_H2D_PINNED_STAGE=1 (TAIL_ASYNC promotion pattern — runbook env is the serving config of record; rollback = remove the line, byte-identical legacy). Canonical boot verified /health + its 10k grade = the generation leg
- provenance: results/amd/coherence/W7_pinned_serve.log + W7_pinned_unset_serve.log + resp2k_{pinned,unset}_{1,2,3}.json + BOOT_BATTERY_20260918_225600.row(+_clocks.log) + w7_h2d_stall_cell{.cu,_run.log} + W7_unbr_row.txt + docs/amd/W7_UNBR_desk.md (OWNER-INLINE CLOSURE section) + serve_10k.sh (BIN history + cure comment); commits on amd/wo-w7-body: cure 1-up, shim 1-up, serve-leg bank; chain PLOG-058 -> this
- bin: 07fcf7a5d98dea7d (SERVING, canonical + env-armed cure), 7c11c3ac07863366 (prior canonical, one-line rollback target), fd764d9fa134a552 (roofline cell), w7_h2d_stall_cell (class cell, /tmp-built by desk — rebank candidate)
- hash: d374f03650bb3951 (prev=745a8f37a308c017)
### PLOG-060 [2026-09-18 ~23:5xZ] phase=W7-PAIRED-AB — pinned-staging cure CONFIRMED by paired same-window A/B (same bin, ordinal-paired fresh boots): -18.6/-27.1/-32.6% per position, pinned FLAT vs unset RISING; the stall compounds thermally — this is why the unset arm "decays" and the cure arm does not

- context: reopen-if A/B half of PLOG-059, executed inline (night sweep refill; agents still dead on the platform limit). Design: THREE fresh boots total would be the full set; this row banks the decisive TWO (arm A pinned, arm B unset, same bin 07fcf7a5d98dea7d, identical life-story: boot -> auto warmup -> 3x 2k probes, ordinal-paired). Canonical restore + third 10k soak fired concurrently (result appends here when the grade returns)
- result: ARM A (pinned) 18.4/18.6/18.8 s FLAT (+-1%); ARM B (unset) 22.6/25.5/27.9 s RISING (+23% across the series). Paired deltas: ordinal1 -18.6%, ordinal2 -27.1%, ordinal3 -32.6% — pinned faster at EVERY position, >> the +-2% same-window band. Parity BLUE/stop 6/6
- mechanism (now closed): the ~995 ms/chunk pageable-H2D enqueue block does not just add ~14 ms device idle — it LENGTHENS each request's thermal load, so the minutes-scale clock integrator (THARM1/PF2 class) compounds it; shorter pinned requests + inter-request idle keep the integrator drained. One picture explains both earlier signals (10k soak +13%; 2k flat-vs-decay) AND why first-request-after-boot showed no delta (integrator not yet loaded)
- ar-bracket trace fix (PLOG-058 FINDING 1) SPEC banked in the row file for cold execution: per-(layer,pass) ar-subslot ring, ar_seq counter, recorded[] flags, report() sums subs; consumer ~:1398 area updated likewise; R6 verify = OPTRACE ar column ~120 ms/chunk (2x the misattributing 60.3) at wall +-2%. Deferred deliberately: surgery on the trace struct exceeds this session's safe context, and an unverifiable instrument edit is worse than a documented one
- provenance: results/amd/coherence/W7_paired_ab_row.txt (full row + fix spec) + resp_p{A,B}_{1,2,3}.json + w7_paired_{A,B}.log (boot logs); commits on amd/wo-w7-body; chain PLOG-059 -> this
- bin: 07fcf7a5d98dea7d (both arms AND canonical serving — unchanged)
- hash: 5de43850e43b70d6 (prev=d374f03650bb3951)
### PLOG-061 [2026-09-19 ~00:0xZ] phase=W7-SOAK-BAND — 10k soak is a THERMAL-HISTORY BAND on the cure config, not a point: third grade 63.1 tok/s (158.57 s) vs PLOG-059's 70.7 — same bin, same env; boot thermal history is the variable; the paired A/B (PLOG-060) remains the controlled receipt

- context: canonical restore after the paired A/B fired its standard 10k grade on the same bin/env (07fcf7a5 + PINNED_STAGE=1): PASS BLUE/stop/69, prefill=158.57 s => 63.1 tok/s, acceptance 0.71 tok/round 2.45 (identical to the 70.7 boot's decode side)
- the variable is thermal history: the 70.7 boot started on a box cooled by an idle gap; this boot started straight after the A/B's six hot 2k prefills (arm B's decaying series last). THARM1's minutes-scale integrator is the gate; the soak number samples it
- LEDGER CORRECTION: the cure-config 10k soak band is 63.1-70.7 tok/s (thermal-history-dependent); the 62.5 banked first-soak-era number is consistent with the low end of that band under a degraded thermal start — the cure's honest controlled claim is the paired A/B (-18.6/-27.1/-32.6% per ordinal), NOT any single soak point. PLOG-059's 70.7 stands as the observed best-case start; this row is its required companion
- provenance: /home/chris/serve_10k.log (third grade lines) + this row; chain PLOG-060 -> this
- bin: 07fcf7a5d98dea7d (SERVING, unchanged)
- hash: 4a17e9d67fffd919 (prev=5de43850e43b70d6)
### PLOG-062 [2026-09-18 ~23:5xZ] phase=W7-AR-BRACKET-FIX — the trace instrument is FIXED and R6-VERIFIED: ar column reads 123.0/123.3 ms/chunk (predicted ~120 = 2x the misattributing 60.3), body drops 152 -> 43-74 (mixer ARs moved into ar where they belong), wall unchanged (18.7 s, cure-band); promoted 2c8901d3d18adef1, battery GREEN

- context: PLOG-058 FINDING 1's fix, executed inline from the banked cold-exec spec (W7_paired_ab_row.txt): VerifyLayerTrace gains a per-layer kAr=4 AR subslot ring + ar_seq/active_ar counters (last-record-wins shared pair retired); drain (:begin_pass), per-chunk OPTRACE printer, and the ensure() event farm updated; ar_seq resets per layer_begin so each pass re-records the same subs and the existing mid-round-sync drain stays race-free. Trace-only: zero behavior when OPTRACE env unset
- R6 VERIFY (the pre-registered assertion): boot 2c8901d3d18adef1 + OPTRACE=3 + PINNED_STAGE=1, one 2k probe (plen-2075 mt64): [PREFILL-SUM] rank=0 ar=122.997/123.274 ms/chunk across n=17 — the pre-fix instrument read 60.3 while rocprof nccl busy measured 119.5; post-fix ar = 123 = INDEPENDENT GROUND TRUTH MATCH. body rebalanced 152.3 -> 43.5/74.0 (mixer ARs out of body), gemm 756-766 (was 791 — the cure's timeline), wall 978-992 ms/chunk / 18.7 s request = cure band +-0%
- promotion: runbook BIN flipped to 2c8901d3d18adef1 (superset of 07fcf7a5: same cure + honest instrument; the running server already WAS this bin — zero-downtime flip); BOOT_BATTERY_20260918_235238 OVERALL GREEN (ladder fast arm + 8-sample clocks sideband)
- meaning: every FUTURE attribution leg reads an honest ar column; the AR floor desk's true premise (TRUE AR ~120 ms/chunk = the #2 prefill wall item) is now visible in-serve without rocprof. The AR-overlap reopen-if keeps its rocprof half open (timeline overlap structure), but its cost question is answered in-serve: ar ~= 123 stands next to gemm ~760 in the same chunk
- provenance: results/amd/coherence/W7_arfix_serve.log + resp_arfix_1.json (/tmp bank pending) + BOOT_BATTERY_20260918_235238.row(+_clocks.log) + commit trail on amd/wo-w7-body; chain PLOG-061 -> this
- bin: 2c8901d3d18adef1 (SERVING + runbook BIN), 07fcf7a5d98dea7d (one-line rollback)
- hash: 31acb872fc4c47b6 (prev=4a17e9d67fffd919)
### PLOG-063 [2026-09-19 ~01:2xZ] phase=W7-CURE-TIMELINE — AR-overlap hypothesis FALSIFIED by rocprof on the cure bin: nccl ARs 0.0% overlapped with GEMM on ALL FOUR dies (serialized by construction, single in-order stream); paired-A/B gain fully attributed to thermal+enqueue mechanism; TRUE AR ~120 ms/chunk is structural -> the #2 prefill item needs fewer/cheaper collectives, not scheduling

- context: PLOG-060's reopen-if had two halves; the paired A/B closed the measurement half, this leg closes the structure half. Desk leg-B recipe replayed on the CURE bin (2c8901d3d18adef1 + PINNED_STAGE=1 + OPTRACE=1) via rocprofv3 (absolute path lesson: not on PATH, /opt/rocm-6.2.0/bin/); ONE 2k probe, parity BLUE/stop/52, wall 19.1 s (trace overhead small vs the stalled bin's +7.7% — consistent with the cure shrinking the trace-sensitive stall window)
- result (steady 1.0 s window per die): nccl busy 101.0/141.4/132.3/116.0 ms vs gemm busy 806.1/768.9/778.0/792.2 — OVERLAP 0.0 ms (0.0% of nccl) on every die. The ARs ride the same in-order stream as compute; overlap is impossible by construction, cure or no cure. Die-to-die AR spread (101-141) matches the desk's straggler observation (max 11.1 ms)
- attribution now CLOSED end-to-end: the paired-A/B's -18.6/-27.1/-32.6% is the thermal-integrator + enqueue-pattern mechanism (PLOG-060), NOT AR/compute overlap. Honest-instrument books close per window: gemm ~756-806 + ar ~101-141 + body + gap ~= OPTRACE wall (978-992 ms/chunk)
- consequence (the #2 prefill item's honest shape): TRUE prefill AR ~120 ms/chunk serialized = 12% of chunk wall, structurally un-hideable on this stream design. Reducing it is ALGORITHMIC: fewer ARs per layer (fuse mixer+mlp collectives), cheaper collectives (smaller messages/quantized AR), or stream-structure change (dedicated AR stream + event deps = a real engineering desk with graph-capture interactions). Parked as the next-window decision with this row as its receipt
- provenance: results/amd/coherence/W7_curetl_row.txt (full row + parse method) + w7_cure_tl/*.csv (190 MB, committed per desk precedent) + W7_cure_tl_serve.log + resp_curetl_1.json; chain PLOG-062 -> this
- bin: 2c8901d3d18adef1 (canonical, restored post-leg via runbook)
- hash: 5c927253aa22a07f (prev=31acb872fc4c47b6)
### PLOG-064 [2026-09-19 ~01:5xZ] phase=W7-SOAK-COLD-START — the soak "band" is a thermal STATE FUNCTION and its cold end is 104.0 tok/s: fourth 10k grade (96.18 s, PASS BLUE/stop/69, acceptance 0.71 tok/round 2.45, decode 3.03 s) on the SAME bin/env as the 63.1 and 70.7 soaks — the box was COLD (post-rocprof restore after idle)

- the four 10k grades on the cure config, all PASS, all identical decode-side: 70.7 (warm start), 63.1 (hot start, straight out of the paired A/B), 104.0 (COLD start, ~01:35 AM after idle). The spread is NOT noise and NOT config — it is the THARM1 integrator sampled at different start states, exactly as W7_therm_row predicted directionally but 3x LARGER in magnitude than the 2k-burst evidence suggested: a 10k is 78 sustained chunks (~96-160 s); a cold box holds boost clocks past the whole request, a warm box trips the throttle mid-soak
- LEDGER CORRECTION (supersedes PLOG-061's band statement): cure-config 10k prefill = 63-104 tok/s as a MONOTONE FUNCTION of start temperature (hot ~63 / warm ~71 / cold ~104). The banked 62.5-era number = hot-start sample. Single-soak claims are banned; every future soak row carries its start temp (edge C at request fire)
- serving rule of thumb (updated): ~3 min idle resets the local band (W7_therm_row); a FULLY cold box buys +47% on the next 10k vs a hot start. Scheduling lever, not just measurement hygiene: keep heavy-request gaps >=3 min where the workload allows
- the cure's contribution is unchanged (paired A/B remains the controlled receipt) — but the PLATFORM ceiling on this hardware is now measured at 104 tok/s 10k-prefill cold-start, 1.66x the 62.5 banked era
- provenance: /home/chris/serve_10k.log (grade lines: prefill=96.18 s => 104.0 tok/s, acceptance 0.71, decode 3.03 s) + restore log /tmp/restore_curetl.log; chain PLOG-063 -> this
- bin: 2c8901d3d18adef1 (canonical, unchanged)
- hash: cbfa9ba5d88b2dbe (prev=5c927253aa22a07f)
### PLOG-065 [2026-09-19 ~02:5xZ] phase=W7-RCCL-MATRIX — RCCL env matrix NULL RESULT (desk executed inline): 16 channels WORSE, Tree WORSE, LL128 wash; default Ring/SHM/2-channel is the achievable envelope — the ~99-123 ms/chunk AR is host-SHM-bandwidth-bound and configuration-immovable; #2 prefill item is ALGORITHMIC ONLY

- context: WO_RCCL_TUNING_desk.md's pre-registered matrix, executed inline (night run, no agents). 4 arms, ordinal-paired fresh boots x 3 probes, heat-equalized ~150 s cooldowns; box entered the matrix HOT after the PLOG-064 cold-start soak (absolutes warm-era; within-ordinal comparison per PLOG-060 design)
- data plane BANKED (arm 0 NCCL_DEBUG=INFO): "Could not enable P2P" every pair (matches the P2P canAccess=0 receipts) -> collectives ride SHM/direct on 2 channels; bootstrap via wlo1/tailscale0 is normal NCCL handshake, not the data plane. The ~990 us mean per 1.5 MB bf16 collective = host-SHM copy bandwidth bound (consistent with the 3.15 GiB/s host-staged transport measurement)
- results (ar column ordinals 1/2/3): baseline 120.9/99.1/98.9 | MIN_NCHANNELS=16 140.2/117.0/109.7 (WORSE — SHM copy contention) | ALGO=Tree 157.3/110.2/141.3 (WORSE + noisy) | PROTO=LL128 124.8/97.1/99.0 (WASH). Pre-registered gate (>=10% ar within-ordinal + >=3% wall): NOT MET by any arm
- closure: "RCCL env knobs do not improve the 4x-gfx900 SHM-ring allreduce at ~1.5 MB on ROCm 6.2" — the cheap class is CLOSED as designed (the desk doc's null-result branch). The #2 prefill item (~12% of chunk wall) now rests ENTIRELY on the algorithmic desks: (a) fuse the per-layer mixer+mlp collectives (halve AR count), (b) quantized AR messages (halve bytes), (c) dedicated AR stream + graph rework (PLOG-063 says overlap alone buys 0). Each is a real engineering desk; none is tonight's scope
- ops: canonical restore fired post-matrix (10k grade = 5th band datapoint, will append at synthesis); walls rose in EVERY arm's run-3 (box accumulated heat across the matrix) — the within-ordinal law absorbed it as designed
- provenance: results/amd/coherence/W7_rccl_matrix_row.txt (full table + transport receipt) + /tmp/rccl_arm{0,1,2,3}.log + resp_rc_a{0,1,2,3}_*.json + WO_RCCL_TUNING_desk.md; chain PLOG-064 -> this
- bin: 2c8901d3d18adef1 (all arms + canonical restore, unchanged)
- hash: 0d4c30c3272d3092 (prev=cbfa9ba5d88b2dbe)
### PLOG-066 [2026-09-19 ~07:0xZ] phase=W7-ROUNDWALL-CLOSE — the MTP decode round is FULLY NAMED (59.2 ms cold p50: 87% = 64-layer verify-loop bodies, host additive 0.21 ms) + NEW DOCTRINE: decode is a thermal state function (59 cold <-> 106 hot ms) + GATE-K verdicts (accept-D2H falsified as lever; Branch-B sized 3-4.5 ms/round) + INCIDENT: canonical bin boot regression

- context: round-wall desk (resumed from the usage-limit kill; 43 min banked carried over). Deliverable leg GATE-R: canonical-arg line on c618d356 (VPERM unset, byte-identical), VERIFY_TAIL_TRACE+TP2_OPTRACE on, counting prompt mt=600, 206 rounds, 4 dies 98-99% busy
- the budget (every ms named, one boot): verify-loop bodies ~52.6 (87%) + in-loop AR 6-9.6 + lm_head 2.36 + align fwd 2.03 + chain/propose/head 1.98 + lgather/ar_argmax/micro 1.05 + host additive 0.21 = round 59.2 ms p50 = 49.3 tok/s at acceptance 0.96, tok/round 2.91. The mission's "~56 ms unattributed" was the M1->M2->PLOG-044-era bodies; this leg collapsed the vintage spread to one boot (tail fields match flip era digit-for-digit)
- NEW DOCTRINE (PLOG-064's decode twin): the round is a thermal state function — 59 ms cold <-> 106 ms hot at mt=600; GATE-B's 27.0 t/s was a hot sample, this leg's 49.3 cold. GATE-R +-10% failed LOUD; absolutes quarantined, structure banked. Decode absolutes now carry start-temp like soaks
- GATE-K verdicts (pre-registered): (1) accept-D2H async accept NO-FIRE — host additive total 0.21 ms, the sync is pure overlap (host sleeps through the round); falsified as an additive lever, item closed. (2) In-loop AR — Branch-B spec DELIVERED for the W4AR desk (owns the one_shot_allreduce.cu:1033 change): GATE-T pipelined 0.47x @30 KiB -> ~3-4.5 ms/round saving cold-corrected; decisive cell = ordinal-paired serve-leg. (3) Loop bodies (87%) own the round; per-kernel work was proven optimal at PLOG-057 rooflines -> the remaining decode levers are ROUND-COUNT (acceptance 0.96, tok/round 2.91 measured; k=3 pricing now data-backed) and thermal state
- INCIDENT 1 (coordinator action owed): canonical bin 2c8901d3 cannot complete warmup this morning (0/6 boots) — deterministic page-fault at the T=27 tail-warm [GQA] width=53 kernel, RANDOM die, TCP read perm-fault; c618d356 boots 3/4. Random-die + both-bins-affected => PRIME SUSPECT IS BOX/FIRMWARE STATE (degradation after ~30 boots), not a single bin; but a real GQA OOB at width=53 is not excluded (old bins may have masked it by allocation luck). PLAN: at V-arm window close — capture kern.log FIRST (recovery playbook v3), sysfs FLR per playbook, retest both bins; if 2c8901d3 still faults, root-look desk on the width=53 GQA path
- INCIDENT 2 (process): window double-claim race (roundwall 05:26 claim vs V-arm stale read) + a 10k-grade collision killed and quarantined ~06:52. LESSON BANKED: re-check the rival desk file immediately before EACH boot act, not once per window
- provenance: results/amd/coherence/W7_roundwall_row.txt + docs/amd/W7_ROUNDWALL_desk.md + /home/chris/w7_roundwall_tail_serve.log + clocks sideband; chain PLOG-065 -> this
- bin: c618d356f0cdc401 (serving, V-arm desk's lawful window), 2c8901d3d18adef1 (canonical-in-question — boot regression under root-look)
- hash: 3b049e35e145a35d (prev=0d4c30c3272d3092)
### PLOG-067 [2026-09-19 ~09:1xZ] phase=W7-CONSOLIDATION — five completions banked since PLOG-066 + turnaround infrastructure: V-arm NO-GO close, boot-fault incident resolved (FLR+reboot), LEANMAC census kill, AR-fusion design (fusion falsified, quant chosen), artifact-yield re-derivation

- V-arm integration CLOSED NO-GO (paired A/B on bin c618d356, both arms, PLOG-060 design): bench 1.1x did NOT convert — V arm +5.7/+6.6% SLOWER within-pair, 10k wash -1.4%, parity GREEN 12/12 + patterned cell GREEN with both-directions falsifier RED 4.68e-01 (the cell has teeth), route proof confirmed. Arm stays in-tree behind NINFER_VPERM_ARM. Re-entry banked (W7_varm_ab_row.txt): gap-bimodality attribution FIRST — the +6% observed equals the unattributed 15-vs-69 ms mode's amplitude. The A-prime contaminated epoch (29.6-115.4 s) quarantined into the A7 evidence file
- INCIDENT 1 RESOLVED: canonical 2c8901d3 warmup fault (GQA width=53, random dies, 0/6) — kern.log captured pre-reset (kernlog_pre_flr_080919.log; dmesg = no-retry page faults across two boards), sysfs FLR x4 + full reboot -> clean boot, fast probe PASS 18.3 s cold. Root cause: accumulated driver/firmware state after ~30 boots. STANDING RULE: kern.log -> FLR/reboot at first sign; never retry through it
- LEANMAC family KILLED AT CENSUS (zero GPU): V0's shipped binary already contains the design — LLVM LICM hoists the coeff*e2m1 product (measured 1.52 ops/MAC vs LEANMAC's modeled 1.50; the 152-op premise was source-static, wrong about the binary); restructured arm trips resourcing (256 VGPR + 884 B scratch + 611 waits — madmix class). BONUS: V0's 0.28-0.34 gap-to-model attributed to the LDS-port/barrier/wait floor. Receipts: WO_LEANMAC_census_row.txt + V0 body extract (3462 lines)
- AR-fusion DESIGN DELIVERED (WO_AR_FUSION_design.md): count-halving FALSIFIED at design level (the two per-layer ARs sit either side of a nonlinear seam — rmsnorm + MoE GEMMs — no legal batching position; the earlier 'both sides win' framing is retracted). Chosen: prefill-only fp8e4m3 quantized AR via a second one-shot instance (+21 MB pinned/rank), central route tp_group.cpp:381. Projected AR column 123 -> ~50-53 ms (wall -5..-7.5%); decode NO-GO by arithmetic (latency-dominated 30 KiB collects). Gates Q0-Q3 pre-registered; composes with Branch-B; 10-12 desk-hours. KEY RECON: kMaxElements=65536 caps the one-shot path below prefill sizes — ALL prefill collectives ride the SHM ring today
- TURNAROUND INFRA (principal order): serve_fast.sh (40 s boots, no 10k-grade tax); model moved USB -> NVMe /home/chris/models/ (sha eaf8ad124256d0a0, both scripts flipped); disk cleanup 3.2 -> 22 GB (89 bins archived to USB, 5 stale worktrees removed with preservation, logs vacuumed); work-order dispatch model live (WO_LEANMAC/WO_DECODE_LANDING/WO_AR_FUSION_design/WO_F1B_DRAFTER_design) — desks execute, coordinator owns infrastructure
- ARTIFACT-YIELD re-derivation (principal re-ask): prefill GEMM is loads-dominant (pure-consume 0.85-0.95x) => weight bytes are the currency. q4 artifact = wash (4.25 vs 4.5 bits, same nibble class); q8/bf16 = slower (1.78x/2x load bytes vs <=15% decode-tax saving); q3 = the only bytes-win (potentially 15-25% faster both sides) at a 3-bit quality price + SD-1 mission conflict. Empirical settle available on request: one paired serve-leg with a baked q4 artifact (needs Team Green bake-pipeline recon)
- provenance: W7_varm_ab_row.txt + W7_varm_instrument_faults.log + WO_LEANMAC_census_row.txt + WO_AR_FUSION_design.md + kernlog_pre_flr_080919.log + serve_fast.sh + REMAINING_ITEMS.md (clean rewrite) + BACKLOG.md; commits on amd/wo-w7-body through 428e9255f + ledger fixes; chain PLOG-066 -> this
- bin: 2c8901d3d18adef1 (canonical, restored post-reboot), c618d356f0cdc401 (V-arm desk bin, arm unset = canonical-identical)
- hash: 73f260044917a102 (prev=3b049e35e145a35d)
### PLOG-068 [2026-09-19 ~11:3xZ] phase=W7-DECODE-LANDING-CLOSE — Branch-B banked-unpromoted (gate FAIL bracket-1 + bracket-2 untestable); k=3 closed LOSS (keep k=2); KEY FINDING: the ONE-SHOT AR ROUTE itself is -34% end-to-end decode vs ring WITH acceptance collapse 0.96->0.85 (ulp-drift greedy flips) — the AR program's foundation route needs its own root-look; box restored canonical, 10k PASS

- context: WO_DECODE_LANDING close (desk ran to its close criteria; 110 tool uses, 8 serve logs, full row). Incidents from this desk (oneshot warmup wedge 6/7 boots, both bins) already filed as the REMAINING_ITEMS blocker; this row records the close verdicts + the route-level finding
- Task 1 Branch-B (NINFER_AR_DEFER_STATUS=1, one env read + one early break, unset/'0' byte-identical, loud-failout consumption at :884): LANDED, BUILT, BANKED (183da007801e0fb6), parity GREEN (12/12 temp-0 + 3/3 mt600 byte-identical = equivalent-time) — paired A/B bracket-1 GATE FAIL (defer -31.8/-12.2/-9.9% within-pair, needed +3%), bracket-2 order-swapped hit 2 more warmup wedges => untestable today => CLOSED banked-unpromoted per close criteria
- Task 2 k=3: CLOSED LOSS — --draft-tokens is a runtime dial (serve_options.cpp:264; tp_engine refuses >3); tok/round 2.91->3.75 (+29%) but round 59.0->84.8 ms (+44%, breakeven <=76 ms), acceptance 0.96->0.92 => decode t/s -10.5%. KEEP k=2. k=2 control reproduced PLOG-066 exactly (2.91 / 0.96 / 59.0)
- KEY FINDING (routes to W4AR + AR-quant): oneshot arm vs canonical SAME bin SAME window: -34% decode t/s AND acceptance 0.96->0.85 with byte-different outputs — the one-shot AR's reduction order differs from the ring => ulp drift => greedy flips => acceptance collapse, ON TOP of the end-to-end slowness. GATE-T's per-collect microbench (0.31-0.47x ring) did NOT predict end-to-end (handshake/polling + acceptance effects dominate). CONSEQUENCE: any AR landing on the one-shot machinery (Branch-B, AR-quant's second instance) inherits BOTH the wedge blocker AND the ulp-drift acceptance risk; the llama.cpp-style copy-add option 0 (no one-shot machinery) gains priority; the AR-quant desk's acceptance gate must be re-barred for drift, not just precision
- infra: box restored canonical 2c8901d3 (10K GRADE PASS BLUE/stop/10000/69); dies idle-cold; no d1 carry in the bin (tp2_backend == HEAD at build — F1b containment clean)
- provenance: results/amd/coherence/WO_DECODE_LANDING_row.txt + w7_defer_landing/ (8 serve logs) + WO_DECODE_LANDING.md; commits c9b9c402b + e3d705374; chain PLOG-067 -> this
- bin: 2c8901d3d18adef1 (canonical, restored), 183da007801e0fb6 (Branch-B bin, banked-unpromoted)
- hash: 217b5b6d569f1295 (prev=73f260044917a102)
### PLOG-069 [2026-09-19 ~12:4xZ] phase=W7-PATHA-GATES — Path A gates fire: G-GRAPH FAIL (retry confirms kill, degradation documented), G-LADDER VRAM-BLOCKED (bad_alloc x3 at chunk 256; 17 MiB/die slack = the M=128 ceiling's root), copy-add GATE-Q0 NULL (same wire bytes as ring) — AR program narrows to fp8-on-ring; hosted-KV Phase 2 ELEVATED (VRAM funder)

- context: Path A measurement matrix (WO_PATHA_MATRIX.md, coordinator inline) + copy-add GATE-Q0 desk, both complete. Box restored canonical (fast probe PASS) post-matrix
- G-GRAPH FAIL: NINFER_PREFILL_GRAPH=1 retry on the cured config DEGRADES 20.13/24.17/26.94 s vs baseline 18.43/18.63/18.80 (128/off stable) — the old kill CONFIRMED with new evidence (capture-cache pathology per identical probe). Closed again
- G-LADDER VRAM-BLOCKED: chunk 256/512 boot but every request dies ~1.2-2.3 s std::bad_alloc on 3 ranks — the 96 MiB workspace arena is sized for chunk 128 AND CANNOT GROW: preflight composition (w7_pa_256_off.log) = weights 5131 + decoder_fixed 1360 + workspace 96 + staging 200 + padding 160 + hidden 5 + kv(36352) 1207 = 8159/8176 MiB/die = 17 MiB slack. THE M=128 CEILING'S ROOT CONSTRAINT IS THE VRAM BUDGET. llama.cpp layer-split dies run 3.8 GB/die FREE — why M=2048 works there and bad_allocs here
- copy-add GATE-Q0 NULL (desk, pre-registered): C at 1.31 MB = 2399 us = 6.7x over the 355 bar; R (copy ring) vs C within 2.3% — same 12 stage-hop wire bytes, byte-counting forbids <=0.55x; C3 (zero-copy root, family best) proves kernel-NT PCIe reads 4.3 GB/s vs 6.6 copy engines — a FIXED one-shot's ceiling at prefill sizes is ~the ring, not 2x under. Q (fp8 partials over copy-add) fails both halves (1.19x slower + maxabs 0.2812 >> 1e-2); scope note: fp8-ON-RING remains open (~half ring bytes => AR ~123 -> ~62 ms, -6% wall) with numerics gates
- AR PROGRAM RESHAPED: transport door fully closed by measurement (ring byte-optimal; one-shot wedge+slower+drift; copy-add null; SDMA 3.3-3.6). Remaining AR lever = fp8-on-ring (A5, re-spec'd) — and the fusion count-halving stays falsified (nonlinear seam). 
- VRAM BUDGET RESHAPES THE PROGRAM: M=128 ceiling root = 17 MiB/die slack. Funding paths ranked: (1) hosted-KV Phase 2 ELEVATED — KV VRAM -> DRAM frees the budget that funds a bigger workspace => chunk ladder + big-M; (2) KV cap trade (36352 -> lower) = product decision; (3) layer-split parked. Decode landing queue unchanged (F1b resume, Branch-B re-fire gated on the oneshot root-look, live)
- provenance: WO_PATHA_matrix_row.txt (verdicts section) + w7_pa_*.log (6 boots; 128/off clean, 256/512 bad_alloc receipts) + WO_COPYADD_row.txt + WO_COPYADD_cell.md + w7_copyadd_cell.cu (3 RED->GREEN cell bugs logged en route) + WO_ONESHOT_ROOTLOOK.md (live, STEP 2); chain PLOG-068 -> this
- bin: 2c8901d3d18adef1 (canonical, restored)
- hash: 521ced8718cb2dc3 (prev=217b5b6d569f1295)
### PLOG-070 [2026-09-19 ~13:4xZ] phase=W7-ONESHOT-ROOTLOOK-CLOSE — the "wedge" was TWO layers: L1 SOLVED (watchdog 2 s deadline false-killed legal 18.7 s cold-cache crossings — deadline knob landed, exit-72 class dead) + L2 OPEN (armed-only launch-submission stall surviving 4 cures, libhsa semaphore class); Branch-B re-fire = NO on one-shot today; copy-add/pinned lane elevated; box canonical healthy

- context: WO_ONESHOT_ROOTLOOK close (157 tool uses, R1-R8 repro logs + C1-C4 cure attempts + R7 frozen-wedge 23-thread gdb dump). The blocker filed at PLOG-068 is now SPLIT by measurement into two mechanisms
- L1 SOLVED (the exit-72 class): kArCallDeadlineNs = 2 s (ar_liveness.h) consumed by the B3 crossing watchdog fires on ANY legal >2 s crossing — cold-cache HIP first-use module loads serialize rank launch threads for 18.7 s (R8 ring control proved the crossings legal; ring-posture "4/4 healthy" was BLIND — no watchdog in ring posture). CURE LANDED default-identical: NINFER_AR_DEADLINE_MS single-source ar_deadline_ns() (eea46f67) — provably removes the exit-72 kills. Guard cell evolved: RED 5 findings pre-fix, GREEN post, selftest 27 mutations
- L2 OPEN (the armed-only stall): past the deadline, armed warmup crawls tail-warm chunks at 130 s+ (7-10x ring); R7 gdb dump = every rank's kernel-launch leg blocked in libamdhip64/libhsa semaphore paths (ncclAllReduce, one_shot_axpy, tp_gemv) with RCCL proxies idle; one ring crossing stuck >60 s too. Survives: mapped-staging reduction (C1 1044->20 ranges, identical wedge — mapped-pinned suspect DEMOTED), deadline widening, ctor-warmup reorder (C3 NO-GO reverted b7b20017), cache warmth (C4). NOT the defer edit (LKG bin affected), NOT the :661-665 recycling hole (kernel never entered, gen=0)
- CONSEQUENCE: Branch-B re-fire = NO on the one-shot route today (cure gate 0/5 — no armed boot reached listening). ELEVATED: the copy-add/pinned lane (needs only plain pinned staging, no device-visible host pages, no one-shot machinery) — PLOG-068's option 0. AR-quant-on-one-shot doubly blocked (wedge + ulp-drift acceptance collapse per PLOG-068)
- decode-landing ledger (same window close): Branch-B banked-unpromoted (bracket-1 gate FAIL + bracket-2 untestable), k=3 closed LOSS (keep k=2, control reproduced PLOG-066 exactly), d1-fix carry exonerated (bin built before the F1b edit)
- box at close: canonical 2c8901d3 UP, health ok, FAST PROBE PASS 18.3 s; KV/chunk ladder v2 (ctx 8192 sacrifice, ws 1024) running on the window — verdict appends next
- provenance: docs/amd/WO_ONESHOT_ROOTLOOK.md + results/amd/coherence/w7_oneshot_rootlook/ (R1-R8, C1-C4, R7 dump) + banks fd9198ae8/23707ada/fbd36d21/eea46f67/b7b20017/8127002e; commits fd9198ae8, 23707ada; chain PLOG-069 -> this
- bin: 2c8901d3d18adef1 (canonical), c618d356f0cdc401 (LKG), 183da007801e0fb6 (Branch-B, banked-unpromoted)
- hash: 74f40321209430b3 (prev=521ced8718cb2dc3)
## PLOG-071 — GAP BIMODALITY CLOSED AS EXTRACTION ARTIFACT; first-chunk per-request gap named+priced; OPTRACE depth-3 host instrument built
- what: request-segmented re-analysis of the banked arfix log (33 request segments, ranks 0-3) shows NO odd/even gap alternation: even 17.5-19.3 vs odd 17-22 ms flat, walls +-0.2%, gemm identical, all 6 multi-chunk requests x 2 ranks. The '2-chunk period / rising odd surcharge' (PLOG-070 era) was an extraction artifact of time-sorted reads across interleaved requests. REAL structure: (1) chunk 1 of EVERY request pays 305-329 ms hot / ~900 ms cold of gap (per-request init class, ~1.8% of wall at plen-2075, 0.4% at 10k); (2) thermal drift 987->1034 ms/chunk across back-to-back probes (known state function); (3) gqa grows +3.2 ms/chunk linearly (attention-vs-depth physics, not a defect); (4) one first-request-only 366 ms upk spike on a partial chunk (lazy first-touch, never repeats).
- staged-shadow suspect DEAD by code fact: tp2_backend.cpp:1065 pins stage_pages=0 (docs/83 wall) — has_staged never true on the serve path.
- instrument: NINFER_PREFILL_OPTRACE=3 [PREFILL-HOST] lines (host_span mono-domain, free_b/free_e cudaMemGetInfo, h2d_kb copy_i32 counter) — commit 5d95f60fb, bin 05628598c977e42a banked; + w7_gap_clock_sampler.sh / w7_gap_correlate.py (parser dry-run on banked log OK); boot protocol banked in WO_PATHA_matrix_row.txt.
- consequence: VALUE_QUEUE #3 replaced by 'first-chunk request-start cost' (priced small); V-arm re-entry key RESOLVED (measurements were never dirty; serving loss real); clean-measurement blocker for 5-8% arms lifted.
- clocks: analysis of banked logs (boot-time rows carried clocks sidebands per law); no new device time claimed.
- chain: PLOG-070 -> this
- hash: 85e5c909badb6c49 (prev=74f40321209430b3)
## PLOG-072 — FP8-ON-RING DESK CLOSED: G-FP8-0/1 FAIL on every arm; AR quant is NUMERICS-bound, not transport-bound
- what: fp8ar desk (agent, own worktree amd/wo-fp8ar, 9 commits HEAD 4e0e66908) executed WO_FP8_ON_RING gates G-FP8-0/1 on the 4-die ring. Verdicts (results/amd/coherence/WO_FP8_ring_row.txt): stage-R control 2522-2531us @1.31MB (reproduces copyadd floor); WO shape stage-FS transport CORRECT (crossrank=0) but 7.5x over band; RCCL 2.20.5 ncclFp8E4M3 ACCEPTS fp8 and sums correctly (banked fact) but is SLOWER than bf16 (1300-1394 vs 843-847us; wire 2.8-3.0 vs 18.6 GB/s); exact 6-bit packed-int ring (rig-I8P) 516-524us = -38.6% vs its own bf16 anchor yet FAILS the pre-registered band and relL2 0.0160 > 1e-2.
- class-level finding: byte-halving AR quantization is NUMERICS-bound — no 6-bit-class code meets 1e-2 relL2 on realistic partials; falsifiers caught both directions (rig-I8P torn-payload 4.11 vs clean 0.17; stage-FS torn-slot 2.14 vs 0.28).
- gates: G-FP8-2/3 NOT RUN (per WO law, gated behind 0/1). Promotion: NONE; serve arm 4a7533a7 built but never booted; k4v2 default untouched.
- re-attempt keys (banked in desk file): principal numerics-budget call (~2e-2 would pass I8P at kappa=1 with behavioral evidence) or upstream RCCL fp8 perf fix.
- infra: window raced cleanly via watcher (window dropped 16:18, zero contact with k4v4 legs, zero pkill); push of the branch is REJECTED by pre-receive on >100MB historical blobs — merge to amd/main must be LOCAL (coordinator, window close).
- clocks: cell rows carry the PLOG-060 reproduced-pair design; no serving numbers claimed.
- chain: PLOG-071 -> this
- hash: ff0d5066e854718a (prev=85e5c909badb6c49)
## PLOG-073 — MADMIX FAMILY COMPLETE (3 genuinely distinct attempts): attempt-1 mad-mix GREEN 1.119x cell; pk16 closed legitimately; consolidation ceiling explained
- attempt 1 (mad-mix on dequant-mul, C++ delivery): the W7 0.48x kill was the inline-asm DELIVERY (opaque chains -> 256 VGPR + spill), not the ISA. Plain C++ wpair on half operands forms v_mad_mix_f32 under -fgpu-flush-denormals-to-zero -ffp-contract=off (clang 18; no builtin; fmaf never forms it; V0 census byte-identical under both flag sets = anchor valid). Cells: [96,128] Residual6144 1.119x (2422 vs 2164 GF/s), [80,128] GdnInput 1.104x, 3-run spread <1%. Census 122 VGPR / no spill / v_mad_mix_f32 1024 / decode stream 164 vs V0 413. G-MM-1 relL2 1.65e-3, BIT-IDENTICAL to V0 at matched mode, falsifiers trip both directions.
- attempt 2 (pk16 numerics-correct, full geometries): relL2 1.69-1.83e-3 — the relL2~146 class is genuinely FIXED — but 0.237-0.262x vs the >=1.15x bar (256 VGPR + 820 B scratch). pk16 CLOSED on decisive measurement, all three legs.
- attempt 3 (LDS-LUT consolidation): LLVM canonicalizes constant ubfe back to lshl+and; conversion class only -15.7% (bar -30%), cell 1.007x; bit-exact. The -30% class exists only across the fp32/fp16 boundary — which is attempt 1's arm (decode stream -60%).
- harness REDs captured en route: fp64 golden must read the TILED scale plane (row-major read showed relL2 1.10 on V0 itself); 64 MiB hammer cells do not ramp mclk (256 MiB does).
- handoff: attempt-1 TU integration (2 declared flags) + G-MM-2 serving pairs = coordinator word; integration desk dispatched (amd/wo-madmix continues).
- clocks: same-run V0 ~-10% vs banked (clock-state class, uniform across arms; ratios same-band back-to-back) — noted per sideband law.
- rows: results/amd/coherence/WO_MADMIX_REATTEMPT_attempt{1_mm2,2_pk,3_consol}_row.txt (+census rows + logs) on amd/wo-madmix.
- chain: PLOG-072 -> this
- hash: 09629f2eced69ea2 (prev=ff0d5066e854718a)
## PLOG-074 — KVARn HIP PORT GREEN: k4v4 boots+serves at TP4; 821 MiB/die VRAM funder measured; chunk-256 re-test under funder = clean no-gain; MTP acceptance 0.99
- verdict: --kv-dtype kvarn_k4v4 BOOTS and SERVES on gfx900 at TP4 (commit 9d1692533 on amd/wo-kvarnport, green bin f3312f25c8201da0 banked; red-row bins 73eecb33/3af434bb/63a3d37e/18993c16 also banked). B1 parse gate deleted, B2 roster join + 3 stub arms retired (nm-verified).
- three runtime contract defects behind B2, all RED->GREEN with banked bins: (1) head-geometry constants (kv_local=1 at world=4 vs hardcoded 2) — table-derived binds; (2) gfx900 LDS ceiling (measured 65,536 B workgroup limit vs 69,380 B request) — 64 KB X-only smem + fp16-safe global scalars slab, the coordinator-audited OOB closed BY CONSTRUCTION; (3) THE V-PLANE DTYPE AMNESIA — HIP-lane bf16-GQA caches carry V as FP16 BITS (kernels cast cache_v to __half); kvarn materialize wrote bf16 V -> prompt-blind attention. HIP-guarded bf16->fp16 V-temp conversion; q-block rel_l2 0.69 -> 0.0025.
- parity: kvarn battery 5/5 GREEN, answers BYTE-IDENTICAL to the bf16 reference boot's 5/5 (BLUE/ORANGE/391/ZEBRA-48213/ZEBRA-73942); needle@6k PASS.
- VRAM FUNDER (measured): kvarn KV 386 MiB/rank vs bf16 1207 MiB/rank at 36352 tok = 821 MiB/die freed. Chunk 256 + ws 256 MiB @36352 serves clean, NO bad_alloc, needle@6k PASS — but prefill 18.7 tok/s vs 91-125 at chunk 128 => keep 128 default; the ladder's 'big-M does not pay in our kernel' close STANDS with the funder-landed receipt (re-attempt key consumed cleanly).
- MTP on kvarn: acceptance 0.99, tok/round 2.99, 600/600 on the mt=600 counting probe (~25.6 tok/s).
- process receipt: Phase-0 compile probe predicted half a day; the real surface was three RUNTIME contract defects (cache-plane dtypes, shape-table geometry, LDS ceilings) — actual 1.5 desk-days. Lesson banked in the WO: future Phase-0s need a runtime-contract inventory, not just compile probes.
- next: k4v4 Phase 2/3 quality gates (rank 0) now EXECUTABLE — tools staged (w7_needle_gen.py + w7_k4v4_gates.sh); 50k/150k needles need the raised ceiling the funder bought.
- chain: PLOG-073 -> this
- hash: f4a9be03c2047bf7 (prev=09629f2eced69ea2)
## PLOG-075 — INCIDENT+RECOVERY: port desk closed with the GREEN source UNCOMMITTED (silent-revert hazard on main); coordinator sweep caught it before cleanup
- what: the kvarnport desk's final commit 9d1692533 did NOT contain the as-built state — the working tree carried 273 lines across 11 files ON TOP (roster join body, launcher TU changes, head-geometry table binds, 64KB LDS scalars slab, V-plane fp16-bits conversion, serve_options gate deletion, WO final log). The GREEN bin f3312f25 was built from the WORKING TREE; a fresh build from main-as-merged-at-82ef3006a would have silently reverted the port (the d1/clobber incident class).
- caught by: the coordinator's disk-cleanup sweep — `git worktree remove` refused on modified files; diff triage showed the modifications WERE the fix.
- recovery (same sweep): working state committed as 4178169ef on amd/wo-kvarnport; merged to amd/main at 37be21917; source-level verification on main: scalars-slab code present in kvarn_tile_cuda.cu, kvarn_k4v4 accepted in serve_options, cuda_fp16.h + V-plane fp16-bits contract comment in the launcher TU. Worktrees kvarnport + madmix(cell) + redaudit + fp8ar then removed cleanly; disk 830MB-critical -> 4.1GB.
- LAW AMEND (proposed for AGENTS.md adoption at next chair pass): a desk CLOSE requires `git status --porcelain` CLEAN on its branch as part of the final report — a desk that built from its working tree must either commit everything or explicitly declare the uncommitted delta in its close-out. GREEN bins are provenance artifacts: the tree that built them is part of the evidence.
- no device claims this row (analysis + git recovery only).
- chain: PLOG-074 -> this
- hash: 144532f3622502b7 (prev=f4a9be03c2047bf7)
## PLOG-076 — k4v4 GATES: short-ctx ALL GREEN (parity/acceptance/soak), prefill pairs k4v4 FASTER (-13 to -36%, thermal-confounded); long-ctx BLOCKED on kvarn ~10.1k std::bad_alloc; device incident #2 cleared by v3 playbook (gpureset only)
- G-Q1 PASS both arms (5/5, no mojibake, no length collapse); G-Q3 PASS (k4v4 acceptance 0.86 / 2.73 tok-per-round vs k4v2 0.88/2.75, -2pp < 4pp gate); G-Q4 PASS (10/10 soak legs each, acceptance FLAT, edge 80-84C). Phase-3 decode wash (-1.2% < 2% law).
- Phase-3 prefill pairs (3x ~7.9k): k4v4 faster EVERY ordinal (-35.8/-33.4/-13.3% wall; 96.9/71.3/50.2 vs 62.2/47.5/43.6 tok/s) — CONFOUND banked (k4v2 arm hotter, mclk lvl0-167 vs lvl1-500): direction safe, magnitude needs a cold cold-pair before quoting as kernel fact.
- BLOCKER A (owner dispatched): kvarn long-prefill std::bad_alloc at ~10.0-10.2k REAL tokens, 4/4 deterministic, ALL 4 ranks, dies at 9984-10112 progress marks on both 19.7k and 49.3k prompts, RSS FLAT 3.0 GB with 55 GB host free (bf16 did 36k routinely). Hypothesis class: int32 size/index arithmetic crossing 2^31 in the kvarn host prefill path -> huge/negative single alloc -> bad_alloc without RSS growth. G-Q2 long legs unrun => promotion NOT met; nothing flipped; default stays bf16-KV.
- CEILING receipts (from the coordinator correction): auto-KV k4v4 113472 / k4v2 141056 tokens; 151552 refused both tiers (8564/8160 -404; 8249/8160 -89). Budget constant verified 0.16% vs measurement.
- BLOCKER B (coordinator-run, CLOSED): canonical restore boot faulted at GQA width=53 warmup — PLOG-066 incident class RECURRENCE #2 (kern.log captured first; no retry through the fault). PLAYBOOK v3 executed: gpureset die 1 (08:00.0) alone cleared it — all 4 dies idle 3-9 W, canonical 2c8901d3 restored, FAST PROBE PASS 18.6s, health OK. LESSON: recurrence #2 was MILDER than incident #1 (FLR+reboot then; gpureset-only now) — playbook step order works as designed.
- also: gates-desk tool defects found+fixed RED->GREEN en route (reasoning-model max_tokens artifact, model field, argv-length posting) — the AMBER empty-answer signature was a TOOL artifact, does not reproduce on the green bin (3k needles 3/3 finish=stop).
- provenance: results/amd/k4v4gates/W7_k4v4_phase2_row.txt + 4 serve logs + transcripts + RSS trace + kern.log copy on amd/wo-k4v4gates (8 commits b90b668a1..873594cbe); restore log /tmp/restore_after_flr.log; chain PLOG-075 -> this
- hash: f55a1a1dd9341cf7 (prev=144532f3622502b7)
## PLOG-077 — L2-STALL FAMILY CLOSED on decisive mechanism map (5th cure = platform-class conviction); Branch-B re-fire NO
- decisive new split: the faithful armed-route CELL cannot reproduce the stall at ANY world (ring-w4/armed-w4/armed-w2/ring-w2 all CLEAN; armed-w4 p99 4096 ns over 825k launches) while the SERVE reproduces on a CURED bin (E1: wedge at warmup, first-ever device-side face `HW Exception by GPU node-2: GPU Hang`; ring control healthy same minutes). The stall requires the full serve process context -> PLATFORM class: gfx900 / ROCm 6.2 / KFD interaction. Our-side hot path API-CLEAN (no per-call allocs/lookups/lazy inits; zero direct hsa_* in src/).
- R7 mystery ioctl NAMED: 3222817548 = 0xC0184B0C = AMDKFD_IOC_WAIT_EVENTS — the frozen wedge is the HSA event thread parked on a never-signaled GPU event, main thread parked in libhsa mid-launch, rank runners queued on submission semaphores.
- gates: G-L2-0/G-L2-1 both missed -> miss branch taken, family CLOSES with the map. Reopen ONLY on platform change (FLR+immediate-validation window, ROCm upgrade, or ROCm SR carrying R7+E1+cell matrix). Branch-B re-fire on one-shot: NO; copy-add/pinned lane remains the elevated decode path (already banked).
- infra note: E1's wedge was killed externally by the canonical restore's pkill during the coordinator's incident recovery — timelines overlapped lawfully (desk banked the state first).
- provenance: results/amd/l2stall/{API_AUDIT_row.md,PROBE_matrix_row.txt,E1_w4armed_repro_row.txt,E1_w4armed_serve.log} + bin c9075897caa4f027 on amd/wo-l2stall (4 commits, head 5da4a50fe); chain PLOG-076 -> this
- hash: 6d8d4a0b6e0ffcda (prev=f55a1a1dd9341cf7)
## PLOG-078 — COLD COLD-PAIR: PLOG-076's k4v4 prefill win DEAD at 2k (thermal); k5v4 BOOTS COHERENT — kvarn family COMPLETE on HIP with budget verified x3
- LEG A (cold cold-pair, k4v4 vs k4v2 @plen-2075 mt64, both arms cold-gated <45C edge (42.0/44.0 max), identical body, mclk lvl3-945 all dies every sample, temps within 2C): ttft-paired delta -0.03..-0.09% — k4v4 faster all 3 ordinals but NOISE; SIGN NOT FLIPPED, ADVANTAGE GONE. The PLOG-076 banked -13..-36% was THERMAL (k4v2 arm ran hotter/mclk-degraded), not kernel. The client-wall -3.7% was a decode-length artifact (35 vs 54 tok completions). 10k geometry stays UNQUOTABLE (cold 10k cell unmeasured; kvarn KV traffic scales with position — divergence not excluded, just not evidenced). Within-pair ttft ramp +2.18%/+2.17% monotonic IDENTICAL shape both arms (marginal vs the 2% gate; paired delta stable). Decode sideband: k4v2 +1-3% rate, acceptance 81.0% vs k4v4 78.6%.
- HONEST k4v4 VALUE PROPOSITION (supersedes the prefill-speed narrative): the 821 MiB/die VRAM funder + short-ctx quality parity + MTP 0.99 — NOT prefill speed at 2k. The gates' promotion question is unaffected (G-Q2 long legs still gated on the ovf fix).
- LEG B: k5v4 BOOTS COHERENT on HIP — first family completion. Preflight MEASURED kv(12240 B/t) vs budget prediction 12,234 = +0.049% — the kvarn width-generic budget constant is now VERIFIED x3 (k4v4 0.16%, k4v2 exact, k5v4 +0.049%). Family ladder: k4v2 8976 < k4v4 11152 < k5v4 12240 << bf16 34816 B/t/rank. Behavioral 5/5 stop/coherent/no-mojibake; 3k needle 3/3 EXACT; receipt 7048/8160 MiB, allocator-gated, zero estimated refusals, no stub-die. 2k prefill 82.7 tok/s vs k4v4 111-113 cold = -26% with LOUD thermal caveat (ran post-battery at 84-85C; matched-temp k5v4 perf = candidate follow-up if promoted).
- provenance: results/amd/k5v4/W7_k4v4_coldpair_row.txt + boot logs + sidebands on amd/wo-k5v4 (7 commits 122cd9e84..d16d27c43); canonical restored + FAST PROBE PASS 24.5s at desk close (no incident #3); chain PLOG-077 -> this
- hash: 327883db1bdf5e77 (prev=6d8d4a0b6e0ffcda)
## PLOG-079 — KVARn 10K OVERFLOW FIXED (G-OVF-0+1 MET): arena exhaustion, not overflow; in-place fp16 V-temp + ws=512 lever; standing cell joins battery
- defect (measured): DeviceArena::alloc_bytes (arena.cu:299) refuse at end=100,665,088 > cap_=100,663,296 (the 96 MiB NINFER_WORKSPACE_MIB chunk work arena — an AMD 2-rank-era override); caller gqa_attention_kvarn_cached_launch; failing alloc = the HIP port's THIRD O(committed_pages) temp v_temp_h alloc(FP16,{256,64,1,160}) at gqa_attention_kvarn.cu:396 — n_tiles=160 pages = 10,240 tok = the exact death band. The WO's int32 hypothesis FALSIFIED (requested size honest 5 MiB; arena rides its ceiling, base ~81 MiB at chunk 79).
- fix (8ac1ba93e on amd/wo-ovf): in-place bf16->fp16 V-temp conversion (same-width elementwise, race-free, bit-identical, byte-identical serving below the wall) — deletes the second O(pages) temp; + NINFER_WORKSPACE_MIB=512 env lever for kvarn long-context, live-preflight-verified 7755/8160 (slack 404 MiB). Structural cure (docs/120 B2 direct route) flagged as lane decision — parked with receipt.
- RED: bin f3312f25, 4/4 fires of the banked 19,709-tok body (wall 108-127 s, all ranks), full backtrace (red_gdb5.log). GREEN: bin 8ac1ba93eb7fbfad — SAME body COMPLETES finish=stop, exact ORCHID-TUNNEL, 2k parity BLUE, 9.5k needle exact; 3/3 passes even through the coordinator G-MM-2 collision (serve never retired, 0 worker errors). Standing cell (needle20k replay+grade) joins the kvarn boot battery per closure law.
- infra: one transient mis-commit of a window claim hit shared amd/main and was surgically reverted by the desk pre-close. G-MM-2 first attempt VOIDED by a window collision with the ovf desk's GREEN claim (coordinator launched without its own claim — process lesson: coordinator window slots claim in a WO file too); void outputs deleted, re-run pending.
- device: incident #3 of the GQA-warmup fault class at the post-ovf canonical restore (faulted die 0d:00.0 this time — random-die class; incident #2 was 08:00.0). gpureset-only did NOT clear (unlike #2); escalated per playbook: FLR all 4 V340 cards (PCI-verified mapping — card numbering shifted across reboots; NVIDIA=15:00.0=card2 today, untouched) -> serve still dead -> FULL REBOOT per the morning recipe. Coordinator executing.
- provenance: results/amd/ovf/{red_gdb5.log,GREEN_OVF_row.md,RED_OVF_row.md} + bins 8ac1ba93/f3312f25 on amd/wo-ovf (head 2aea195b1); chain PLOG-078 -> this
- hash: 0c4a91c20669ddc6 (prev=327883db1bdf5e77)
## PLOG-080 — MAD-MIX PROMOTED: G-MM-2 serving wall +8.83% (MM 41.73s vs V0 45.77s medians, n=9/arm) — 5.9x the 1.5% gate; serving default flipped to cc122cc0
- G-MM-2 executed to completion on the post-incident-#3 recovered box: 6 fresh boots alternating V0,MM / MM,V0 / V0,MM (drift cannot hide in arm identity), 18/18 probes BLUEPASS, within-pair spreads inside the +-2% law. V0 median 45.77s / MM median 41.73s => MM win +8.83% (PROMOTE per the pre-registered G-MM-2: >=1.5%).
- NOTE vs the cell prediction (~1.1x on 2 of the GEMM kernels = ~5% of chunk): the serving figure is larger; the paired alternating design is the registered control and the gate is met as written. Direction consistent; magnitude noted for the ledger.
- PROMOTION ACTIONS: amd/wo-madmix + amd/wo-madmix-int merged to amd/main (5305681f0); serve_10k.sh BIN -> cc122cc0ab616e4e (2c8901d3 = rollback; runtime NINFER_MADMIX=0 = V0); serve_fast inherits. First canonical boot on the promoted build = next restore (FAST PROBE + health will verify).
- tool receipt: the pairs script's verdict aggregation had a contiguous-regex bug (sideband lines between boot and probe lines broke every match -> "WALLS INCOMPLETE"); fixed split-based, re-run — official verdict banked in WO_MADMIX_GMM2_pairs_row.txt with all 18 walls.
- desk status: k4v4fin (G-Q2 113664 paired on the FIXED kvarn bin 8ac1ba93 + ws=512) and fuse (WO_BODY_FUSION bench-first) live as the 2 agents; both queued their window claims behind the pairs.
- provenance: WO_MADMIX_GMM2_pairs_row.txt (row + verdict) + tools/v340l/wo_madmix_gmm2_pairs.sh (fixed) on amd/main; chain PLOG-079 -> this
- hash: a50ebf0393dcc915 (prev=0c4a91c20669ddc6)
## PLOG-081 — G-BF-0 MET (BODY FUSION cell): SimtColTileKSplit 2.03x control-only (116.29 vs 236.43 µs/launch) — MmaUnsplit hypothesis CLOSED (stale framing), fusion seam exonerated (~3%), serving leg G-BF-1 armed (TIP bin 37bc9b25 banked, queued behind k4v4fin battery)
- PHASE-1 (what the ~11.6 ms/chunk `gate` column is): the WO's "MmaUnsplit 20x off floor" framing was STALE — FIX-B (SimtColTile) has been the PROMOTED DEFAULT since PLOG-050 (2026-09-18); MmaUnsplit measures 1.51-1.55 ms/launch (6.5x worse than served). The mass is the SimtColTile kernel itself at ~236 µs/launch: 384 single-warp CTAs (~6 warps/CU) moving ~47 MB at ~10 GB/s effective — LAUNCH-SHAPE-BOUND, not fusion-bound. rmsnorm seam = 244.0-236.4 = ~8 µs/launch (~3%) — the fusion premise is FALSIFIED as the lever; launch shape is the lever. Rows: results/amd/fuse/GATE_ROWS_20260920.md (P1a 243.98 composed / P1b 1507.31 rollback / P1c 236.43 / P1d 1554.88 / P1e decode 25.18 µs; bench = hand-linked gdn_gating_proj_bench_v2, --flush-mib 64, warmup 5, repeat 50, medians; window 08:58Z idle-sclk 34-40 C between G-MM-2 pairs).
- PHASE-2 (SimtColTileKSplit, NINFER_GGATE_GSPLIT layer, 8 warps/CTA partition K, 1 KiB smem cross-slice close): control-only 116.29 µs = **2.03x** vs served; composed norm+control 125.62 µs = 1.94x; decode T=1 route byte-identical (25.18 µs, GemvPairedRows untouched). Chunk-scale reconciliation STATED-ASSUMPTION: gate column reads as 4-rank-summed (12 launches/rank/chunk x 4 x ~242 µs ~= 11.6 ms) => ksplit saves ~-5.8 ms/chunk (~0.6% of the 990 ms chunk), halving the column; the serving wall effect is G-BF-1's to price, per-launch 2.0x stands regardless of census convention.
- G-BF-0 cell GREEN (under-load rerun 09:23:13Z, 82 C): numerics old 9.931e-08 / new 9.443e-08 vs fp64 oracle (bar 1e-2); falsifiers BOTH directions (corrupt-vs-clean trips at 1.255e-01; corrupt-vs-own-oracle stays 9.433e-08); perf old=230.5 / new=131.0 µs = **1.76x** (gate >=1.10x). RED capture of the cell's own crash bug (leg-B hipDeviceReset invalidated leg-C rig -> HIP FAIL invalid argument) banked + fixed before the green rerun. Census: 64->84 vgpr / 84->36 B scratch; unroll-4 alternative measured-rejected (59 vgpr but 144 B scratch, worse than the old kernel's own spill). Rollback layered: NINFER_GGATE_GSPLIT=0 -> pre-WO, NINFER_GGATE_SIMT=0 -> pre-FIX-B; zero-card cell joins the standing suite.
- G-BF-1 ARMED: desk agent #2 died platform-class (model request) mid-rebuild; coordinator inherited end-to-end — orphaned build adopted and completed (no-op check 100% RC=0), tip banked per BANK-BEFORE-RELINK as `ninfer-serve_37bc9b250bfada70.bin` (branch 80b510210 = mad-mix + ksplit, GSPLIT default-ON verified in source per R6). Runner wo_bf1_pairs.sh verified: BASE auto-pins promoted cc122cc0 (mad-mix in BOTH arms — isolates ksplit), 3x3 ordinal-paired alternating, +-2% within-pair, promote bar >=0.5% TIP median win, canonical restore at exit. QUEUED behind the k4v4fin long-needle battery (window law); anchor-leg client loss on arm A noted with server-side evidence (req 2 done finish=stop_token gen=74).
- desk status: k4v4fin battery self-driving (driver2: arm A k4v4@65536 needles -> arm B k4v2@65536 -> canonical restore; 113664 legs REFUSED live both tiers at ws512 — receipts banked, pool arithmetic + arena O(position) ceiling; KV-B3 registered-parked behind docs/120 B2); fuse = coordinator-hands until the window opens.
- provenance: results/amd/fuse/GATE_ROWS_20260920.md + docs/amd/WO_BODY_FUSION.md (desk close + coordinator inheritance, 144a6e0e0) on amd/wo-fuse; chain PLOG-080 -> this
- hash: 899e7da8698e8fd0 (prev=a50ebf0393dcc915)
## PLOG-082 — V-ARM RE-ENTRY CLOSED PRICED-NEGATIVE (desk varm, GPU-free): the REV2 "+5-8%" band inverted post-mad-mix; honest fold band -0.5..+1.0% vs the 1.5% bar; loss mechanism named (N2 = deleted latency-hiding material, cold-L2 serving exposure)
- AUTOPSY (receipts: results/amd/varm/VARM_REENTRY_DESIGN.md, branch amd/wo-varm c9eac0280, merged): the +5.7/+6.6% serving loss (W7_varm_ab_row.txt, PLOG-067) decomposes as — EXCLUDED: gap mode (PLOG-071 artifact), box drift (B2 band +-1.5% brackets leg A), numerics (parity 12/12 BLUEPASS, falsifier RED 4.681e-01), launch count (WO premise FALSIFIED BY SOURCE: V arm is a single-kernel route, same 192 launches/chunk — nvfp4_tiled_gemm_hip.cu:1166-1363), occupancy (113 VGPR = 2 CTAs/CU; V3c falsifier: 3 blocks/CU benched only 1.023x). N1 (byte-priced, +2..5 ms/chunk): TN=64 M-tiling doubles code/scale-plane streams (~189 MB/chunk/die re-read, L2-evicted twins) at measured 36-51 GB/s. N2 (dominant, ~60 ms/chunk class): DELETION OF LATENCY-HIDING MATERIAL (waits 212->32, body 2994->1359, 128 LDS reads/body) — the bench regime (hot-L2, issue-bound) priced the deletion as the 1.136x win; the serving regime (cold-L2, access-pattern-bound) exposes it as a loss.
- THE CONTROLLED CONTRAST (why this closes the class): mad-mix KEPT every load/launch/tile and won +8.83% serving (PLOG-080); V DELETED streams, changed grid and MAC, and lost +6%. Bench-vs-serving conversion is the gating question for ANY kernel arm: only reductions that preserve latency-hiding convert (mad-mix class); deletions price inverted (V class). This is the transferable lesson and it is now on the ledger.
- VARM-FOLD (the redesign, fully specced): fold the bit-exact v_perm decode atom into the PROMOTED kernel_mm at V0's exact tile/grid/MAC/consumption order (bit-identical to mm by construction, <=136 VGPR, NINFER_VPERM_FOLD value-parsed gate, TILED_PK mutual-exclusion kept). HONEST PRICE: -0.5..+1.0% (expected ~0) — attempt-3 dose-response (1.007x cell at -15.7% stream) prices the fold's -3.5% body cut at ~1.00-1.02x cell vs the 1.5% promote bar. PRICED-NEGATIVE => no window spent; family CLOSED. G-V1 census (zero GPU) -> G-V2 cell (bar >=1.05x, expected FAIL = the designed close) -> G-V3 pairs only on a cell pass, pre-registered in the design doc.
- HONESTY CAVEAT banked: PREFILL-SUM was down in the original window (receipts), so the +70 ms/chunk is wall-derived; N1 is byte-priced; N2 is mechanism inference from three independent receipts. Reopen key: a cold-L2-class bench harness (or OPTRACE-class in-serve column evidence) that can price latency-hiding deletions BEFORE a serving leg — the current bench cannot, and that gap (not any V-arm code) is what the family died of.
- desk status: varm DONE (branch merged, worktree released, 7.1G free); chunk256 staged + self-executing window watcher v2 (fires after the k4v4fin merge, retire+launch+restore unattended); k4v4fin battery arm A 4/4 legs PASS awaiting d0.75 grade + arm B k4v2 baseline (coordinator hands).
- provenance: results/amd/varm/VARM_REENTRY_DESIGN.md on amd/main; chain PLOG-081 -> this
- hash: 086e76269351da7b (prev=899e7da8698e8fd0)
## PLOG-083 — THERMAL STATE-FUNCTION MAP banked (desk thermalmap, GPU-free): prefill is a monotone function of accumulated heat in every era (1.2-2.8x per-request spread); recommended FREE ask = >=3-min idle policy (+52-65% interactive prefill, 2.7x interactive decode); power-cap A/B named as the privileged follow-up
- THE CURVE (per era, eras never mixed; receipts: results/amd/thermalmap/{THERMAL_MAP,MINED_THERMAL_TABLE}.md, branch amd/wo-thermalmap merged): cure-cfg 10k class 104.0 cold / 70.7 warm / 63.1 hot tok/s (1.65x); kvarn k4v4 10k ordinals one boot 96.9/71.3/50.2 with mclk 945->500->167 + sclk 1269->991->560 (1.93x); live 60k needle battery 35.1 cold-boot mean (instant start 91, max 120) -> 28.9-29.4 park (20.7 floor digit-stable); decode count600 13.79 cold / 13.28 hot+3-min-gap / 4.89 hot-no-gap (2.8x). Shape universal: fast band -> ~85 s sustained-load trip -> park band; ~90 s drain tau; ~3 min idle = full recovery.
- MECHANISM (measured twice, 09-17 + 09-19, now cross-era confirmed): a MINUTES-SCALE sustained-load integrator, NOT edge temp — 84 C runs 2.8x slow or full speed in the same boot depending only on idle gap. Forcing clocks high backfires 1.3-1.8x at the 110 W cap. Instantaneous trace values vs leg means reconciled (the battery's 40.8/20.7/28.0 are warm-sample/park-floor/mid-decay; means 35.1/29.4/28.9/28.9).
- THE LEVERS: (a) power-cap raise — cap verified 110 W/die = factory default (passive sysfs read during the live battery); HOLDING the fast band through a 10k = 63 -> 96-104 tok/s (+52-65%), 60k legs +19%; magnitude at raised values unpriced, root-gated, one privileged A/B cell named as follow-up. (b) cooling/ambient — duty-cycle lever: halving the 90 s tau shrinks the reset gap ~3 -> ~1.5 min and raises the park floor; multiplies (a) and (c). (c) RECOMMENDED (free, no root, measured across eras): >=3-min idle before heavy prefills = +52-65% per interactive 10k prefill and 2.7x interactive decode; bulk chains stay chained and expect the park band BY DESIGN. This is a principal/product decision — no serving config touched.
- desk status: thermalmap DONE (merged, worktree released); varm DONE (PLOG-082); chunk256 staged with self-executing watcher v2 (fires post-k4v4fin-merge); k4v4fin ARM A COMPLETE 5/5 PASS exact (019b598b2), ARM B k4v2@65536 booted green (preflight 7929/8160 slack 230) anchor running — promotion decision on its grades per the pre-staged rule.
- provenance: results/amd/thermalmap/THERMAL_MAP.md on amd/main; chain PLOG-082 -> this
- hash: 3498a29dd498aab6 (prev=086e76269351da7b)
## PLOG-084 — K4V4 PROMOTED (kvarn close, coordinator-executed end-to-end): G-Q battery 8/8 EXACT at the 65536 paired posture; serving default flipped to kvarn_k4v4@65536/ws512 on NEW UNION BIN 058a7b85859c5cd0; one lineage incident caught by flip-verify + one observation banked
- THE GATES (desk inherited by coordinator mid-arm after platform-class death; battery self-driving, kill-proof design held): arm A k4v4 4/4 PASS — anchor 49,276 tok + needles 59,935 (d0.25) / 59,830 (d0.5) / 59,984 (d0.75), ORCHID-TUNNEL / MARBLE-COMPASS / CINDER-HARBOR all EXACT substring, finish=stop, zero worker errors; arm B k4v2 4/4 PASS identical grading (fresh boot, same bodies, same ordinal order). STOP condition "k4v4 < k4v2 anywhere" never fired. G-Q1 behavioral + G-Q3 MTP (-2pp < 4pp) + G-Q4 soak PASS per the banked gates-desk rows; prefill-within-3% satisfied a fortiori (gates pairs: k4v4 faster every ordinal). PROMOTION MET per the pre-registered rule.
- THE DELIVERABLE: serving default bf16@36,352 -> kvarn_k4v4@65,536 tokens (+80% context at 11,152 vs 34,816 B/t/rank — 3.1x KV density); 113k-class pools remain parked behind docs/120 B2 (refusal receipts banked; KV-B3 registered). Rollback: --kv-dtype kvarn_k4v2 dial or bf16 revert (script comments).
- INCIDENT (lineage gap; caught by the flip's mandatory boot-verify): promoted mad-mix bin cc122cc0 (built PRE-ovf-merge) REFUSES kvarn at parse (cached-attention route = roster remainder in its tree) — the GEMM promotion (PLOG-080) and the KV capability rode different branches; no union bin existed. First flip attempt silently booted bf16@ws512 (serve_fast builds its own arg line; only BIN+env inherit — now flag-synced). FIX: union build from amd/main tip ece88d382 (roster/link files verified byte-identical to the battery bin's tree 8ac1ba93e) -> BANK-BEFORE-RELINK ninfer-serve_058a7b85859c5cd0.bin -> both scripts flipped. LIVE-VERIFIED: preflight kv(11152 B/t x 65536) slack 0, FAST PROBE PASS (18.8 s), healthy past 90 s. LESSON (extends the desk-close law): a promotion flip is not done until the EXACT flipped line boots AND serves AND survives +3 min — estimate-free, boot-verified.
- OBSERVATION (unnamed cause, not reproduced): the battery restore boot exited gracefully ~90 s post-probe (vocab flush(shutdown) = SIGTERM-class, not the law-form SIGKILL); coordinator-session boots persist. Close rule amended: liveness at health-OK AND +3 min.
- WINDOW HANDOFF: G-BF-1 (ksplit serving pairs, cc122cc0 vs 37bc9b25 — both bf16 legs, pair isolates ksplit; claim 44013dfff blocks the chunk256 watcher by design) executes NOW; chunk256 runner launches at its close; k4v4fin worktree released post-merge (d5ef4c876).
- provenance: results/amd/k4v4fin/ (8 grade rows + receipts) + docs/amd/WO_Q4KV_K4V4.md close entry + docs/amd/KVARN_BOOT_BATTERY.md on amd/main; chain PLOG-083 -> this
- hash: 1c82e85f0aa3e78d (prev=3498a29dd498aab6)
## PLOG-085 — G-BF-1 (ksplit) + chunk256 (M=256 re-leg) both CLOSED NO-PROMOTE: the conversion lesson generalizes — three cell wins this window (2.03x gate kernel, +15% chunk ladder, 1.1x V-arm bench) produced 0.0%/+0.17%/-6% serving; only request-critical-path reductions convert
- G-BF-1 (ksplit serving pairs, union BASE 058a7b85 vs TIP 37bc9b25): run-1 INVALID banked honestly (18/18 BLUEPASS but cold-start ramp + my misread of the alternation retracted — the loop was correct; the defect was NO HOT-START PRECONDITION). CLOSED on the saturated ordinal-3 subset: BASE med 45.16 vs TIP med 45.16 = 0.0% vs the 0.5% bar; within-pair noise +-1.3-1.5% EXCEEDS the bar so a re-run is uninformative (declared in the WO before spending the window). ksplit stays parked in-tree on amd/wo-fuse, NOT merged — the promoted union bin ships without it. REDPRE rows: the G-MM-2 killer class (prompt_tokens==2075 FAILed at 2134) recurred in the copied skeleton — fixed range-check e9dad7a66.
- chunk256 (REV2 W5 M=256 re-leg, the "+15% AFTER GEMM" scheduled lever): run-1 INVALID — (a) the same ==2075 class (third occurrence; ALL arms FAILed), (b) C256@ws96 is a LIVE-REFUSED posture (rank2 std::bad_alloc arena throw — the +15% receipt era ran the 1024 MiB default arena, before canonical pinned ws96). Run-2 (fixed: range check + ws512 BOTH arms = fair pair; read rule declared pre-read: ordinal-1 discarded): 9/9+9/9 BLUEPASS, ord 2-3 hot-band C128 med 45.11 vs C256 med 45.04 = **+0.17% vs the 5.0% bar. NO-PROMOTE.** The +15% chunk-level receipt amortized per-chunk fixed costs (finalize stall, pageable H2D) that REV2 §3 + PLOG-058/059 already cured — chunk count no longer sits on the request critical path. C256 unpromoted; branch merged 485a0280c.
- THE GENERALIZED LESSON (now 3-for-3 this window: V-arm PLOG-082, G-BF-1, chunk256): CELL WINS DO NOT CONVERT UNLESS THEY SHRINK THE REQUEST-CRITICAL PATH. Bench hot-L2 deletions, per-chunk amortizations, and kernel-level speedups on non-critical columns all died at the serving leg. Standing rule for every future arm: BEFORE a serving window, name the request-critical-path term it shrinks and its measured share; if the term is already cured or <1% of wall, the cell result stays a cell result. (The mad-mix counter-example that proves the rule: it attacked the GEMM column = 75% of wall — and converted +8.83%.)
- Remaining named lever in this family: the gate kernel's ~116 us/launch residual (16x weight / 24x activation re-reads) — needs a further tiling pass and must clear the ~1%-of-wall noise floor before earning a window.
- desk status: fuse branch parked unmerged (ksplit + runner fixes + G-BF-1 rows banked at its tip); firstchunk desk holds the last window (GRANT pending); thermal + power-cap levers remain with the principal (PLOG-083).
- provenance: docs/amd/WO_BODY_FUSION.md + docs/amd/WO_CHUNK256.md + results/amd/{fuse/gbf1,chunk256}/ on amd/main; chain PLOG-084 -> this
- hash: 101b337179b49987 (prev=1c82e85f0aa3e78d)
## PLOG-086 — FIRSTCHUNK CLOSED: the "per-request first-chunk gap" (VALUE_QUEUE #3) DOES NOT EXIST on the current stack — instrument leg 7/7 PASS refutes the premise; historical readings were the uncured pageable-H2D stall + extraction artifacts; lever dead, #3 retires
- THE LEG (desk executed in its granted window, coordinator checkpoint + acknowledgment; bin 05628598c977e42a era-faithful ws96 posture, NINFER_PREFILL_OPTRACE=3, 11 request segments, 7/7 behavioral PASS, retire law form, canonical restore + +3-min liveness): ttft minus prompt/steady-throughput = +1/-7/-5/+4/+8/+14/+6 ms across blue+p1..p6 — COLD AND HOT. Chunk-1 device wall is FASTER than each request's own steady mean on all six long requests.
- THE MECHANISM QUESTION ANSWERED BY EXONERATION: all four candidate mechanisms clean at hot chunk 1 — host_span 5.9-9.8 ms flat; Δfree = 0.000 MiB (no allocation); h2d 2-4 KiB (pinned ring holds, PLOG-058/059); no pool-restored re-alloc pattern. The 918.2/922.3 ms chunk-1 "gap" readings on the first two multi-chunk requests are a STALE-DRAIN SEGMENTATION ARTIFACT (ar field byte-equal to the preceding MTP-redo line; layers<gemm impossible; walls leave no room for real idle) — first flagged by the coordinator's transient reading, then sharpened by the desk to full artifact. Receipt: results/amd/firstchunk/MECHANISM_ROW.md + the full segment table.
- RESIDUALS PRICED (both harmless): ~208 MiB one-time pool growth frozen after request 1 (no per-request recurrence); p1's single 1037 ms finalize stall = legacy class, this instrument bin predates NINFER_MTP_TAIL_ASYNC consumption — the promoted union bin carries the cure, never recurs.
- THE CLOSE: VALUE_QUEUE #3 retires to the CLOSED SET as a refuted premise. The historical "305-329 ms hot / ~900 ms cold first-chunk gap" was the pre-cure pageable-H2D enqueue stall (cured by NINFER_H2D_PINNED_STAGE, PLOG-058/059) plus extraction artifacts — on the current stack there is no per-request first-chunk mechanism and no pre-touch fix to sell. The only named re-attempt: a control boot with NINFER_H2D_PINNED_STAGE unset (pricing first; expected to resurrect the cured stall and merely re-measure PLOG-058).
- BONUS RED banked for the w7 lane: w7_gap_clock_sampler.sh's own heredoc swallows the rocm-smi pipe — every banked clock_*.csv from that tool is header-only (the empty clock CSV in this leg's receipts is the live instance). One-line fix owed by the w7 lane; the correlator's units guard + dead-join auto-detect is what caught it.
- desk status: firstchunk DONE (merged amd/main), agent slot freed — 0 live agents, slots for principal-directed work (llamacpp probe + power-cap A/B both parked on cooling/storage per principal 2026-09-20).
- provenance: results/amd/firstchunk/ + docs/amd/WO_FIRSTCHUNK.md (branch amd/wo-firstchunk, merged) on amd/main; chain PLOG-085 -> this
- hash: 2064da91e7862d12 (prev=101b337179b49987)
## PLOG-087 — POST-PROMOTION CENSUS (coordinator hands): the promoted kvarn line runs ~183-194 tok/s steady-chunk at plen-2075 (~1.5x the REV2 class; request walls 18.7-19.7 s) — BUT the OPTRACE partition is stale on the kvarn route (~54% of wall untraced); no column-share claim valid until the tracer extends to the kvarn kernels
- THE MEASUREMENT: union bin 058a7b85859c5cd0, kvarn_k4v4@65536/ws512, NINFER_PREFILL_OPTRACE=3, 3x plen-2075 mt64 probes + fast probe (7 request segments, rank=0). Steady chunks 2+: wall 661.1/667.7/674.4/684.2/699.2 ms across requests; traced columns gemm 112.7-179.0 (16-27%), ar 142.0-145.8 (21%), body 23.9-41.9 (3-6%), gap 23.9-41.9 (4-6%). Client request walls 18.9/19.1/19.7 s (all finish=stop) corroborate: prefill ~11.2 s of each wall.
- FINDING 1 (speed): ~192 tok/s steady-chunk prefill class = the fastest measured serving state on this box (old class ~128 tok/s at ~990 ms/chunk). The promoted line is not just denser KV — it is FASTER per chunk than anything previously banked on this route geometry. (Thermal state: walls rose 661->699 across the probe series = warming ramp; the cold floor is likely faster still.)
- FINDING 2 (instrument): the OPTRACE partition does NOT describe the kvarn route — columns sum to ~305 ms of a ~670 ms wall; ~54% of wall is untraced device work (kvarn attention/dequant kernels outside the tracer's sections). The REV2 books (gemm 76% of wall, the basis of the additive ceiling) describe the bf16 route only. CONSEQUENCE per the PLOG-085 rule: no future arm earns a window against a column-share claim on this route until the tracer partition is extended to the kvarn kernels — that tracer desk (zero-GPU design + one instrumented boot) is the top executable item on the board.
- SEQUENCING ERROR STAMPED: the raw OPTRACE lines were lost — serve_fast truncates its log on every boot and the canonical restore ran BEFORE extraction; only the live-computed steady table survives (results/amd/census/CENSUS_ROW_union_kvarn_20260920.md). Rule candidate for the runbook: EXTRACT BEFORE RESTORE, every instrumented boot.
- ALSO THIS SWEEP: w7_gap_clock_sampler.sh RED->GREEN (3 defects: heredoc overrode pipe; rocm-smi JSON schema mismatch card*/verbose keys; values '(945Mhz)') — live-validated 84 rows/8 s, all dies; the tool had never worked end-to-end. Banked in the same commit as this census.
- desk status: 0 live agents; canonical serving healthy (union bin). Board: tracer-partition desk is the top executable; llamacpp + power-cap stay parked on principal (cooling/storage).
- provenance: results/amd/census/CENSUS_ROW_union_kvarn_20260920.md on amd/main; chain PLOG-086 -> this
- hash: 285613397b507b22 (prev=2064da91e7862d12)
## PLOG-088 — HISTORICAL CORRECTION (principal challenged the math; grep archaeology over results/): the largest bf16 TP4 boot on this box is 98,944 tokens (2026-09-15 era, placement 3,820 MiB/rank) — NOT 36,352; today's manifest is 1,311 MiB/rank bigger and ate the pool; + mc=10 VRAM experiment (reservation≠residency quantified)
- THE ARCHAEOLOGY (grep recipes handed to the principal): `grep -rhoE 'capacity=[0-9]+ tokens' results/ | sort | uniq -c` → 109x36352, 74x63936, 36x98944, singles 188096/186368/141056/113472. Tier/world verified per log:
  - **98,944 tokens = TP4 bf16, THIS BOX** (results/amd/p3/G18w4q_p3serve_serve.log et al., 36 boots, 2026-09-15): rank=0..3, free 8,160 MiB/rank, placement 3,820 MiB/rank measured-manifest (pre-drafter era), default tier (no kvarn flags existed). Pool math consistent: 98,944 x 34,816 B = 3,271 MiB of (8,160 - 3,820 - arena) MiB.
  - 63,936 = TP2-era boots (results/amd/coherence/BASE2-class, placement 4,983 MiB/rank).
  - 186,368 (bf16) / 188,096 (int8-KV) = **NOT THIS BOX** — NVIDIA-line logs (free 15,667/15,697 MiB/rank = 16 GB cards, nsys/int8 names, results/157_p0/, 2026-09-11) sitting in the shared results tree.
- WHY MY 36,352 CLAIM WAS WRONG: the weights manifest GREW 3,820 -> 5,131 MiB/rank (+1,311; drafter + emplacements) between 09-15 and today; the bf16 pool shrank 4,340 -> 1,208 MiB -> capacity fell 98,944 -> 36,352. The "36352 is the ceiling" framing was era-myopia — it is the ceiling OF TODAY'S MANIFEST, not of the hardware. Corrected ladder: 98,944 bf16 (09-15 manifest) -> 65,536 kvarn_k4v4 (today, promoted) -> 113k/141k pool-ceiling kvarn unlockable only via smaller fixed side or B2.
- MC=10 EXPERIMENT (principal-requested, same sweep): booted kvarn_k4v4@max-context 10 on the promoted line. Reservation vs residency QUANTIFIED: preflight reserves fixed 7,363 MiB (slack 796 paper) but rocm-smi shows only ~6,496 MiB resident -> ~867 MiB of workspace/staging/padding is RESERVED-NOT-RESIDENT; true free VRAM per die ~1,680 MiB (draft-2) / ~1,606 MiB (draft-3, +74 MiB for the third draft slot). Chat-API serving at mc=10 is impossible (template floor: "Say BLUE" = 54 prepared tokens; context_length_exceeded receipt /tmp/mc10_mtp3_boot.log). Canonical restored + probe PASS after.
- LESSON (the principal was right): era-myopia is the failure mode — current-config arithmetic is not history. Boot logs beat derivations; every ceiling claim must cite its manifest era.
- provenance: results/amd/p3/, results/157_p0/, results/amd/coherence/ on this repo; chain PLOG-087 -> this
- hash: 33f78cf1f3b66c7b (prev=285613397b507b22)
## PLOG-089 — PRINCIPAL-DIRECTED LONG-CONTEXT ATTEMPTS (300k/250k/200k kvarn_k4v4): allocator refused all live — the wall is the 8 GB die vs KV bytes themselves; full posture ladder banked; hosted-KV is the only unlock (parked by principal)
- RECEIPTS (live preflight, one line each, results/amd/k4v4fin/boot_refused_k4v4_300k_receipt.log + this entry): 300k@ws512 required 10,559 (slack -2,399) | 250k@ws512 10,027 (-1,867) | 200k@ws512 9,495 (-1,335) | 200k@ws96 9,079 (-919) | 131,072@ws1024 9,274 (-1,114) | 74,240@ws1024 8,670 (-510). At ws512 the pool maxes ~74,752 tok and the arena caps position at 65,536 — the promoted 65,536 IS the hardware-max viable kvarn_k4v4 context.
- THE UNREMOVABLE ARITHMETIC: 200k KV alone = 2,132 MiB/rank; the absolute zero-posture floor after weights(5,131)+decoder(1,360)+5 = 1,664 MiB -> theoretical max ~156k tok with NO workspace (prefill would die immediately). 200k is 29% beyond the physics; 300k is 81% beyond. No arena cure (B2), no posture lever, no code change moves it — the limitation is 8,160 MiB dies holding a 20.5 GB model.
- THE ONLY UNLOCK: hosted-KV Phase 2 (KV in host RAM; 200k needs ~8.5 GB host total — trivial on this box) — PARKED by principal order 2026-09-19, integration banked (WO_KVHOSTED_P2.md resume checklist). Unpark is a principal decision, not a boot flag.
- corrected-ladder context (PLOG-088): bf16 reached 98,944 in the 09-15 manifest era; today's manifest shrunk bf16 to 36,352; kvarn_k4v4 promoted 65,536. The 300k demand sits 4.6x above today's ceiling; 200k sits 3x above.
- provenance: results/amd/k4v4fin/boot_refused_k4v4_300k_receipt.log; chain PLOG-088 -> this
- hash: ac230f0c5e9946eb (prev=33f78cf1f3b66c7b)
## PLOG-090 — GOAL CHANGE (principal, 2026-09-20): moving to a SMALLER model — "nvfp4 does not have any benefit for us" — SD-1 (NVFP4@TP4 on 27B) superseded; the 27B-era queues close with the model
- PRINCIPAL STATEMENT: "we are moving to another model, a smaller one, nvfp4 does not have any benefit for us." Precedent: REV2 §0 recorded the last goal change the same way. IMPACT MAP (what carries / what dies):
  - DIES WITH THE 27B: the NVFP4 GEMM family (pk16, mad-mix — promoted +8.83% but its value was 27B-specific), the NVFP4 bake pipeline as a requirement, the 300k-on-8GB-die fight (PLOG-089 — a smaller model's weights shrink the fixed side and the KV budget opens; the whole ceiling ladder dissolves), the MTP draft assets (27B-specific draft vocab/heads).
  - CARRIES (stack-level, model-agnostic): ninfer TP serving stack, kvarn KV cache family (any GQA model; on a smaller model bf16 KV may suffice at long context — kvarn becomes optional, not the milestone), pinned-H2D cure, finalize-tail cure, the thermal state-function map + operator levers (PLOG-083), the conversion lesson (PLOG-085), the window/ledger/laws infrastructure, the corrected historical boot record (PLOG-088).
  - HELD PENDING THE NEW MODEL'S SHAPE: tracer-partition desk (its validation boot is moot until the stack shape is known — desk directed to close on Phase-1 design only), firstchunk tooling (carries), llamacpp probe (REVALUED: a smaller model on llama.cpp/ROCm is exactly its use case — the park on cooling/storage stands, but its rank rises if the new model runs outside ninfer).
- NEEDED FROM THE PRINCIPAL TO DISPATCH: (a) which model (name / weights / HF repo), (b) the target that matters (context? latency? throughput?), (c) ninfer or external stack (decides llama.cpp probe rank).
- desk status: tracer directed to close on design; all other slots held for the pivot.
- provenance: this entry is the pivot marker; chain PLOG-089 -> this
- hash: 43f7d560d517b57f (prev=ac230f0c5e9946eb)
## PLOG-091 — PIVOT INFRA: new 128GB SSD formatted+mounted+fstab'd (sda, old Windows install wiped per principal — peeked read-only first: stock Windows dirs, nothing personal); ALL MODELS migrated with sha verification; serving re-verified from the SSD
- THE SSD: /dev/sda (Samsung 128GB, was unmounted with a Windows install: NTFS sda3 118.5G + sda4 + EFI — read-only peek confirmed stock Windows/Program Files/etc, principal confirmed nothing to preserve). wipefs + mkfs.ext4 (-m 0), mounted /media/chris/ssd128, chown chris, fstab by UUID (4fa59f3b-1bae-436d-aa66-0ba93c7c469a, defaults,nofail). 117G free.
- THE MIGRATION: /home/chris/models (18G artifact qwen3_8_27b_nvfp4.ninfer) copied to /media/chris/ssd128/models, sha256 VERIFIED identical (eaf8ad12...c56d2 — matches the 09-19 NVMe-move receipt), original removed, /home/chris/models is now a SYMLINK to the SSD — every existing path (serve_10k/serve_fast ART lines, runbooks) keeps working unmodified. BOOT VERIFIED from the SSD: FAST PROBE PASS 18.5s (parity with NVMe). Root disk: 8.5G -> 26G free (93% -> 77%).
- THE LLAMA.CPP EXPERIMENT OPENS (principal-directed): target GGUF = byteshape/Qwen3.8-27B-IQ4_XS-3.84bpw.gguf (HF; ~14G) — this resolves the earlier "no source weights" blocker (the GGUF is published). Cheap-first ladder: upstream llama.cpp cloned to /media/chris/ssd128/llamacpp/, HIP build configuring on the HOST ROCm 6.2.0 (hipcc 6.2.0, clang 18, cmake 3.30.5) for GPU_TARGETS=gfx900 — NO container needed if the 6.2-era rocBLAS carries gfx900 payloads (the rocm-vega repo's premise was that 7.2.1 lacks them; 6.x had them). GGUF download in flight.
- provenance: chain PLOG-090 -> this
- hash: f74e268b7bb27130 (prev=43f7d560d517b57f)
## PLOG-092 — LLAMA.CPP ON V340 MEASURED (principal-directed): upstream llama.cpp HIP gfx900 (ROCm 6.2.0 host build, FP8-gate patch), Qwen3.8-27B IQ4_XS GGUF (12.17 GiB, 27.32B params), 10k ctx, q4_0 KV, FA on — TP4-layer pp 54.7 / tg 8.9 tok/s; TP2-layer 29.3 / 4.9; row split produced NO tests (both worlds); vs ninfer: 3.5x slower prefill
- THE BUILD: upstream ggml-org/llama.cpp (the rocm-vega repo is a setup guide, its llama.cpp is upstream), host HIP build on ROCm 6.2.0 (hipcc 6.2.0/clang 18, cmake 3.30.5), GPU_TARGETS=gfx900. ONE patch required: vendors/hip.h gated FP8_AVAILABLE to gfx942+ (__hip_fp8_e4m3 does not exist for gfx900 targets in ROCm 6.2 headers; the IQ4_XS model uses no FP8). Patch committed in the SSD tree (build2.log).
- THE MATRIX (llama-bench, -p 10000 -n 128 -ngl 99 -ctk q4_0 -ctv q4_0 -fa 1 -r 1; results/amd/llamacpp/matrix_results.txt):
  | world/split | pp10000 | tg128 |
  | TP2 layer | 29.29 tok/s | 4.91 tok/s |
  | TP2 row   | NO RESULT (row split produced no test on this stack — upstream's known partial support) |
  | TP4 layer | 54.68 tok/s | 8.86 tok/s |
  | TP4 row   | NO RESULT |
  MTP: NOT TESTABLE in llama.cpp (no Qwen3 MTP implementation; its spec-decode needs a separate draft model). The MTP cell exists only on the ninfer side.
- THE COMPARISON (same box, same 27B model): ninfer promoted line = ~183-194 tok/s steady prefill (PLOG-087 census) and 13.83 tok/s decode (short-ctx, MTP 2.59 tok/round) vs llama.cpp TP4-layer 54.68 pp / 8.86 tg. Prefill: **ninfer ~3.5x faster**. Decode: llama.cpp 8.86 tok/s single-token at 10k vs ninfer 14 tok/s WITH MTP at short ctx — the exact same-context ninfer 10k decode leg was interrupted (rerun on word) but even ninfer's raw 2k-ctx step rate (~5.3 fwd/s = 13.83/2.59) brackets the comparison: llama.cpp wins NOTHING on this hardware at these settings; the NVFP4-fused ninfer kernels + MTP dominate every measured cell.
- STRATEGIC READ for the pivot: the "nvfp4 has no benefit" premise is CONTRADICTED by this measurement — the NVFP4-fused stack is precisely what makes ninfer 3.5x faster on prefill than the best external alternative on identical silicon. A smaller model on ninfer keeps that advantage; llama.cpp's value here is only as a cross-check/comfort stack, not a performance path.
- NOTES: llama.cpp guessed the arch "qwen35 27B" (GGUF metadata; handles it). The rocm-vega repo's premise (needs ROCm 7.2.1 container) was FALSIFIED — the host 6.2.0 build works with one patch. GGUF + build + results all live on /media/chris/ssd128 (ssd128).
- provenance: results/amd/llamacpp/matrix_results.txt on amd/main; chain PLOG-091 -> this
- hash: 899702bc0b6d5842 (prev=f74e268b7bb27130)
## PLOG-093 — CORRECTION + MTP MEASURED: llama.cpp DOES have MTP (principal was right — --spec-type draft-mtp; the GGUF carries NextN tensors); TP4-layer 10k ctx with MTP: pp 69.1 / decode 9.96 tok/s (acceptance 0.65, mean 2.95 tok/step) vs 8.86 without — ninfer still leads every measured cell
- THE CORRECTION: PLOG-092's "no MTP in llama.cpp" claim was WRONG. Current upstream has a full spec-decode suite (--spec-type none,draft-simple,draft-eagle3,draft-mtp,draft-dflash,draft-dspark,ngram-simple,ngram-map-k,ngram-map-k4v,ngram-mod,ngram-cache) and llama-arch reads NextN/MTP tensors per-block (blk.%d.nextn.*). The byteshape GGUF carries them; llama-server created the MTP draft context against the target model on first boot.
- THE MTP NUMBERS (llama-server, TP4 layer-split, -c 10240, q4_0 KV, FA on, 7,857-tok prompt, n=63 stopped): pp 69.1 tok/s | decode 9.96 tok/s | draft acceptance 0.65079 (41 accepted / 63 generated), mean len 2.95. vs the no-MTP bench cell: decode 8.86 -> 9.96 = +12% from MTP.
- STANDING COMPARISON (same box): ninfer promoted line decode 13.83 tok/s at 2k ctx (acceptance 0.88, 2.59 tok/round) vs llama.cpp MTP 9.96 at 10k — ninfer leads the decode column everywhere measured; prefill ninfer ~190 @2k (10k leg pending — interrupted twice by principal interrupts; will run when wanted).
- HUMILITY NOTE (the principal's challenge pattern is productive): two of my claims this session were overturned by measurement/reading — era-myopia (PLOG-088) and the llama.cpp MTP miss (this row). The llama.cpp spec suite ALSO contains the ngram-map-k4v/ngram-mod variants — the same family as the parked d2 idea — upstream already ships them; relevant if the new-model stack choice lands external.
- provenance: /tmp/llama_mtp_tp4.log (server log w/ acceptance), /media/chris/ssd128/llamacpp/matrix_results.txt; chain PLOG-092 -> this
- hash: a3140dfc47cea1fa (prev=899702bc0b6d5842)
## PLOG-094 — MATRIX COMPLETE: TP2+MTP REFUSED live (compute buffer 151 MiB short at 10k ctx — "probably wont fit on tp2" CONFIRMED by the principal's own prediction); the full llamacpp-vs-ninfer matrix is closed
- TP2+MTP receipt (llama-server, HIP_VISIBLE_DEVICES=0,1, layer split, -c 10240, q4_0 KV, draft-mtp): "failed to allocate ROCm1 buffer of size 158564608" -> exit. Weights ~6.5 GB/die (13.1 GB GGUF over 2) + MTP nextn layers + KV + compute > 8 GB. Log: /tmp/llama_mtp_tp2.log.
- THE COMPLETED MATRIX (llama.cpp, Qwen3.8-27B IQ4_XS, 10k ctx, q4_0 KV, FA):
  | cell | pp | decode | notes |
  | TP2 layer no-MTP | 29.29 | 4.91 | |
  | TP2 layer +MTP | REFUSED | REFUSED | compute buffer short (this row) |
  | TP4 layer no-MTP | 54.68 | 8.86 | |
  | TP4 layer +MTP | 69.1 | 9.96 | acceptance 0.65, mean 2.95 tok/step |
  row/tensor split: no tests produced (upstream partial support), both worlds. MTP lifts TP4 decode +12%.
- NINFER (promoted, same box): ~190 tok/s steady prefill @2k geometry; 13.83 tok/s decode @2k (MTP accept 0.88). Same-context 10k ninfer leg remains interrupted-on-request; every measured cell favors ninfer.
- provenance: /tmp/llama_mtp_tp2.log, /media/chris/ssd128/llamacpp/matrix_results.txt; chain PLOG-093 -> this
- hash: 09c39cc3d07e51e1 (prev=a3140dfc47cea1fa)
## PLOG-095 — FAIR 2K COMPARISON COMPLETE (all configs, same geometry, same session) — REVERSAL: at matched geometry llama.cpp is COMPETITIVE; my PLOG-092 "3.5x faster" claim was geometry-mixed (ninfer 2k census vs llama 10k bench) and is RETRACTED; bf16-ninfer is the decode king (16.9 median, peaks 23.5); llama draft-mtp (11.93) BEATS ninfer k4v4-d2 (10.5)
- THE FAIR TABLE (plen-2075-class prompts, same hot-box window, ratios valid/absolutes are park-band):
  ninfer union bin (NVFP4 27B, TP4): bf16+d2 decode 16.3/23.5/16.9 (med 16.9), prefill 75.8-101.0, accept 73.5%, cap 23,808 | bf16+d3 decode 14.2-16.6, prefill 64.9-69.5, accept 61.4% | k4v4+d2 decode 10.4-10.6, prefill 64.3-67.5, accept 73.5%, cap 65,536 | k4v4+d3 decode 10.8-11.9, prefill 64.5-65.6, accept 61.1%.
  llama.cpp (27B IQ4_XS GGUF, TP4-layer, q4_0 KV): no-spec pp 90.1 / decode 8.98 | draft-mtp pp 87.6 / decode 11.93 (accept 0.65, mean 2.95 tok/step).
- WHAT THE FAIR TABLE SAYS: (1) llama.cpp pp 90.1 sits INSIDE ninfer's bf16 band (75.8-101) and ABOVE ninfer kvarn (64-67.5) — competitive, not 3.5x behind; (2) llama.cpp draft-mtp decode 11.93 BEATS ninfer k4v4-d2 (10.5) and ties k4v4-d3 (11.9); (3) only ninfer bf16-d2 clearly leads decode (16.9 med, peak 23.5) and prefill (101 peak); (4) kvarn costs ~35-40% decode vs bf16 (attention dequant overhead) — its value on the 27B is CAPACITY ONLY (65,536 vs 23,808 at ws512); (5) draft-3 loses on ninfer bf16 (acceptance 73.5->61.4%) and is a wash on k4v4 — draft-2 stays optimal on this stack.
- FOR THE PIVOT DECISION: with a smaller model (no capacity pressure), bf16-KV + draft-2 on ninfer is the speed-optimal config (matches NEW_MODEL_LANDING.md's default); llama.cpp is a legitimate fallback stack on this hardware — competitive at 2k, not the 3.5x strawman I banked an hour ago. The REAL ninfer advantages that survive fair measurement: MTP acceptance 0.88 vs 0.65, capacity per die (NVFP4), and the thermal/operator levers.
- HUMILITY LEDGER (session total): three claims overturned by measurement — era-myopia (PLOG-088), llama.cpp-has-no-MTP (PLOG-093), and the 3.5x claim (this row). Each was caught by the principal pushing on a number. The fair-geometry rule joins ERA-MYOPIA in the runbook candidates: NEVER compare across context geometries or thermal bands.
- provenance: results/amd/census/FAIR_MATRIX_2k_ninfer.txt + results/amd/llamacpp/FAIR_MATRIX_2k_llamacpp.txt on amd/main; chain PLOG-094 -> this
- hash: fe0de4423047f4c8 (prev=09c39cc3d07e51e1)
## PLOG-096 — LLAMA.CPP COMPLETE MATRIX @2k (all clean cells) + the draft-n knob + the bf16-sustained reconciliation: "40 t/s" was near-zero-context decode; at 2k context bf16+d2 sustains 16.9-23.5; llama decode ~8.9-11.9 everywhere and does not scale with world or KV width
- THE DRAFT KNOB (principal was right, found): --spec-draft-n-max / LLAMA_ARG_SPEC_DRAFT_N_MAX (arg.cpp:4128, params.speculative.draft.n_max). llama.cpp MTP depth is a knob, not model-structural — my "model-structural" claim retracted. Measured on TP4 bf16-KV + draft-mtp: n_max=2 -> decode 11.02 (accept 0.676, mean len 2.35); n_max=3 -> 11.5 (accept 0.633, mean 2.90); unbounded -> 11.93 (2.95). Draft-3 mildly better than 2 on llama.
- THE COMPLETE LLAMA TABLE @2k (TP4-layer except noted, clean cells only): TP2-q4_0-nospec pp 103.2 / decode 8.95 | TP2-bf16kv-nospec pp 100.9 / 8.67 | TP4-q4_0-nospec pp 90.1 / 8.98 | TP4-bf16kv-nospec pp 90.4 / 8.87 | TP4-q4_0+MTP(default) pp 87.6 / 11.93 | TP4-bf16kv+MTP-d2 86.4 / 11.02 | TP4-bf16kv+MTP-d3 86.4 / 11.5 | TP2+MTP REFUSED (compute buffer short at 10k ctx; at -c 4096 untested). ROW SPLIT: no tests ever produced (upstream), dropped from the matrix.
- LLAMA FINDINGS: (1) decode is ~8.9 flat no-spec across TP2/TP4 AND q4_0/bf16 KV — llama decode does not scale with world count or KV width here (not KV-bytes-bound; likely kernel/host-bound on gfx900); (2) bf16 KV == q4_0 KV for llama decode (wash); (3) TP2 prefill 103.2 > TP4 90.4 (layer-split prefill parallelism imperfect); (4) MTP is the only llama decode lever: +12-33%.
- THE BF16-SUSTAINED RECONCILIATION (ninfer bf16+d2, mt=600, short prompt): decode 36.3 tok/s SUSTAINED over ~600 generated (wall 17.78 s, accept 56.4%, 2.13 tok/round) vs the fair-matrix 16.9-23.5 at TRUE 2k context — decode scales down with context bytes as expected. The historical "40 t/s" = near-zero-context decode of that era; at true 2k the honest sustained band is 17-24 (bf16) — the "can't hold 20" observation is CONFIRMED for 2k context, and kvarn halves it again (10.4).
- NINFER vs LLAMA at 2k (final): decode — ninfer bf16+d2 16.9-23.5 BEATS llama best (11.9); kvarn loses to llama (10.4 vs 11.9). prefill — llama 87-103 vs ninfer 64-101 (comparable band; llama TP2 103.2 is the single best pp measured at 2k). The pivot question (stack choice for a smaller model) is now genuinely open on prefill; ninfer wins decode only with bf16 KV, and wins capacity with kvarn.
- provenance: results/amd/llamacpp/FAIR_MATRIX_2k_llamacpp.txt + results/amd/census/BF16_SUSTAINED_2k.txt on amd/main; chain PLOG-095 -> this
- hash: 1af174ad397c425f (prev=fe0de4423047f4c8)
## PLOG-097 — MMQ QUESTION CLOSED (principal's stop order vindicated): the DEV-DUFORD gfx900-mmq-tune branch's OWN comment says gfx900 LOSES MMQ for dense matrices ("Vega 10 lacks native dp4a, loses to dequant + hipBLAS; keep MMQ only for MoE") — our 27B is dense, so the fork's prompt-processing optimization was MoE-specific and offers the port nothing; the fork's master merge additionally does not link (broken merge: headers reference symbols whose implementations diverged)
- THE ARCHAEOLOGY: duford/gfx900-mmq-tune final state = 4 lines of gfx900 conditionals (mmq.cu: `if (cc == GGML_CUDA_CC_VEGA) return n_experts > 0;` with the loses-to-hipBLAS comment; mmq.cuh: 3-line tune) on a #24568-era base (no merge path to current master). The branch's own measured conclusion: for DENSE quants on gfx900, dequant+hipBLAS beats MMQ — which is exactly the path our upstream build already uses (the measured 90-103 tok/s prefill cells ARE the hipBLAS path).
- CONSEQUENCE: the port = upstream llama.cpp HEAD + the FP8 gate patch (committed, pushed: bobcatchris/llama.cppV340L amd/v340-port 760b1f5b3) + q4_0-via-hipBLAS (already the default fast path). No MMQ porting needed. The principal's stop call on the 40k/80k run + "no way it's better" was correct — closed without further GPU spend.
- STATE: port branch restored in the llama.cpp tree (amd/v340-port, 675f11cbe tip); server+bench validation rebuild RUNNING. ASCII-P1M GGUF (islamsidratul) downloading — to be tested at TP2+MTP when the build validates (the "might allow TP2 with MTP" artifact question remains OPEN).
- provenance: /media/chris/ssd128/llamacpp/build_duford*.log, the duford/gfx900-mmq-tune tree; chain PLOG-096 -> this
- hash: 36864826983649f0 (prev=1af174ad397c425f)
## PLOG-098 — WORLD x MTP MATRIX COMPLETE (ASCII-P1M artifact, duford port branch build, -c4096, q4_0 KV, 2075-tok prompt): MTP lifts decode +27% in every world; TP2+MTP REFUSED at request time (fit abort — principal prediction confirmed); ASCII artifact performance-identical to ByteShape
- THE MATRIX (results/amd/llamacpp/WORLD_MTP_MATRIX_2k.txt): TP2/none pp 96.3, decode 9.65 | TP2/draft-mtp REFUSED (ggml_abort "failed to fit params to free device memory" at first request — lazy compute-buffer alloc; boots healthy, dies on probe) | TP3/none 88.0 / 9.75 | TP3/draft-mtp 84.7 / **12.33** (accept 0.609, 2.83/step) | TP4/none 83.4 / 9.53 | TP4/draft-mtp 80.8 / **12.04** (accept 0.609).
- FINDINGS: (1) MTP lifts llama decode +27% in every world that fits; (2) TP3+MTP (12.33) is the llama decode sweet spot, beating TP4 (12.04) — AR overhead grows with world; (3) prefill DECLINES with world on llama (TP2 96.3 > TP3 88.0 > TP4 83.4) — TP2 is llama.cpp's natural shape on this box, matching the 09-09 NVIDIA reference's TP2 shape; (4) the ASCII-P1M artifact performs identical to ByteShape (within noise) but FITS where ByteShape refused — worth it for the fit alone; (5) placement-vs-quantization nuance for the fleet ledger: the ASCII artifact at 11.4 GiB splits 2 dies at 5.7 GiB/die and MTP's nextn layers push first-request allocation ~151+ MiB past the wall at 10k ctx (4096 ctx also aborts at request time).
- NINFER CONTEXT (the standing comparison at 2k): bf16+d2 16.9-23.5 decode (kvarn d2 10.4-10.6) — llama TP3+MTP 12.33 sits between ninfer kvarn and ninfer bf16. Prefill: llama TP2 96.3 vs ninfer bf16 75.8-101.0 hot-band — comparable.
- provenance: results/amd/llamacpp/WORLD_MTP_MATRIX_2k.txt on amd/main; retry receipt /home/chris/probe/tp2mtp_retry.log; chain PLOG-097 -> this
- hash: 907cd607894d4135 (prev=36864826983649f0)
## PLOG-099 — TENSOR SPLIT UNLOCKED + completed world matrix (v2 port pin = d9df110 + FP8 patch): TP3-tensor+MTP decode 17.78 tok/s = parity with ninfer bf16-d2 (16.9 med); tensor split beats layer split everywhere (decode +11-60%, prefill +11%); per-world batch tuning matters (TP2 bs256 vs TP3/4 bs512)
- THE REGRESSION + FIX (the session's core finding): upstream removed split-buffer support from the HIP backend after June 30 (the PR-merge state d9df110 had 26 split_buffer_type refs in ggml-cuda incl. the HIP export at :5627; current master + HEAD have ZERO outside SYCL -> "device does not support split buffers" at load). FIX: pinned amd/v340-port-v2 to d9df110 + re-applied the FP8 gfx942 gate (24e9e5dc4, pushed to bobcatchris/llama.cppV340L). Tensor split works again on all 4 V340 dies.
- COMPLETED TENSOR TABLE @2k (ASCII IQ4_XS, q4_0 KV, FA, draft-mtp, 2075-tok prompt):
  | world (ub128) | pp | decode | accept |
  | TP2 tensor bs256 +MTP | 81.8 | **16.7** | 0.646 (2.94/step) |
  | TP3 tensor bs512 +MTP | 96.9 | **17.78** | 0.708 (3.12/step) |
  | TP4 tensor bs512 +MTP | 97.7 | **15.2** | 0.630 (2.89/step) |
  layer-split comparison (same format): TP4+MTP 87.6/11.93 — tensor wins decode +27%, prefill +11%.
- PARITY STATEMENT: llama.cpp TP3-tensor+MTP (17.78) matches ninfer bf16-d2 (16.9 median, 16.3-23.5 band) at 2k decode, and beats ninfer kvarn-d2 (10.4) by 71%. Prefill: llama TP3/TP4 tensor 96.9-97.7 vs ninfer bf16 75.8-101.0 hot band — comparable. The stacks are at PARITY at 2k on this hardware; the remaining differentiators are capacity (GGUF 11.4 GiB/2-4 dies vs kvarn 65,536 tokens), MTP acceptance (ninfer 0.88 vs llama 0.61-0.71 on these probes), and operational simplicity (llama).
- RULE CONFIRMED: TP2 needs its own batch/ubatch tuning (bs256: 16.7 decode; bs512 untested at TP2-tensor) — per-world config, not one-size.
- provenance: results/amd/llamacpp/WORLD_MTP_MATRIX_2k.txt on amd/main; chain PLOG-098 -> this
- hash: 7d2370c3fc978bbc (prev=907cd607894d4135)
## PLOG-100 — BATCH-SIZE SETTLED for the port config + quant-ladder receipts: TP3 is batch-invariant (17.78 decode at bs256 AND bs512); TP4 bs256 trades prefill for nothing; quant conversions from IQ4_XS are disabled by llama-quantize (need original weights); d4 collapses — the port config is FINAL: TP3 tensor bs256/512 ub128 q4_0-KV MTP-d3
- BATCH MATRIX (TP3/TP4 tensor+MTP, ASCII IQ4_XS, -c4096): TP3 bs256/ub128 pp 96.5 decode 17.78 == TP3 bs512/ub128 pp 96.9 decode 17.78 (batch-invariant); TP4 bs256/ub128 pp 84.1 decode 15.53 vs TP4 bs512/ub128 pp 97.7 decode 15.2 (bs256 trades prefill for a sliver of decode; bs512 favors prefill). FINAL PORT CONFIG: **TP3, tensor split, batch 256-512 (wash), ubatch 128, q4_0 KV, FA, draft-mtp** -> decode ~17.8 / pp ~96.5 at 2k.
- DRAFT-N ON TP3-TENSOR (IQ4XS): d2 14.34 (accept 0.70) | default-d3 17.78 (accept 0.708) | d4 11.08 (accept 0.50 — collapse). d3 optimal, d4 killed by acceptance.
- QUANT-LADDER RECEIPT: llama-quantize refuses requantizing from iq4_xs/q6_K ("requantizing from type X is disabled") — Q4_1/Q8_0 variants of this model require the ORIGINAL weights (HF full-precision GGUF ~54 GB, not on disk). Parked: only worth it if a simpler-quant dequant path (Q4_1-style) shows prefill gains — the DUFORD fork's own data says Q4_1 pp 144 on this hardware vs our IQ4_XS 97-104; that delta is quant-dequant-cost, reachable only via a source-weight requant.
- GGUF METADATA (for the port docs): qwen35 arch, 65 blocks, native ctx 262,144, nextn_predict_layers=1 (MTP), HYBRID architecture (ssm conv_kernel/state/group/inner + full_attention_interval=4 — every 4th layer full attention, rest SSM) — explains why decode does not scale with KV-bytes the way pure-attention models do, and why llama.cpp decode was flat across worlds/KV-widths.
- provenance: results/amd/llamacpp/WORLD_MTP_MATRIX_2k.txt + QUANT_LADDER_TP3TENSOR.txt on amd/main; chain PLOG-099 -> this
- hash: a7d79c3c80039335 (prev=7d2370c3fc978bbc)
## PLOG-101 — THE PORT'S CAPABILITY MILESTONE: 200k-context q4_0 KV boot on TP3 tensor-split with MTP live — the boot ninfer refused (PLOG-089) works on llama.cpp; 10k-ctx decode 15.57 tok/s (accept 0.667, 3.00/step), pp 82.2
- THE BOOT (ASCII-P1M GGUF, HIP_VISIBLE_DEVICES=0,1,2, -ngl 999 -sm tensor -c 200000 --batch-size 512 --ubatch-size 128 -ctk q4_0 -ctv q4_0 -fa on --spec-type draft-mtp): health OK, 10k-prompt probe 200 (103.9 s total). VRAM during decode: 8,419 / 8,131 / 8,130 MiB used of 8,573 per die — free 154/442/443 MiB: die 0 nearly full (uneven shard + nextn placement); 200k is the practical ceiling at TP3+MTP on 8 GB dies.
- THE NUMBERS: pp 82.2 tok/s (7,857-prompt) | decode 15.57 tok/s at ~8-10k ctx | draft acceptance 0.667, mean 3.00 tok/step. Context-scaling is MILD on this model: 17.78 @2k -> 15.57 @8-10k (-12%) — the hybrid SSM architecture (only every 4th layer full attention) + q4_0 KV keep long-context decode cheap, unlike ninfer's pure-attention kvarn route.
- THE HEADLINE: PLOG-089's ladder (200k/250k/300k refused on ninfer) is answered — 200k BOOTS on the llama.cpp port at TP3 with MTP. The port is validated end-to-end: builds clean, boots at 200k, serves with MTP, tensor split, per-world batch configs.
- provenance: /home/chris/probe/tp3_200k.log + resp_tp3_200k.json; chain PLOG-100 -> this
- hash: 7a261d885f6aaa74 (prev=a7d79c3c80039335)
