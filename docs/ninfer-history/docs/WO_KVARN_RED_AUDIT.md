# WO_KVARN_RED_AUDIT — read-only falsifier desk for the kvarn-port RED (empty outputs)

- **Desk:** agent #2 (freed by fp8ar closure), read-only scope.
- **Worktree:** `/home/chris/worktrees/amd-wo-redaudit` (create: `git worktree add /home/chris/worktrees/amd-wo-redaudit amd/wo-kvarnport` then branch `amd/wo-redaudit`). Read-only: NO source edits, NO builds, NO boots, NO pkill. The serving window and the port debug belong to the kvarnport desk.
- **Mission:** the kvarnport desk's port BOOTS (finish=stop, no crash) but every probe returns EMPTY answers on BOTH kvarn tiers (A1 k4v4 RED and EXP1 k4v2 RED on bin 63a3d37e, `results/amd/coherence/WO_KVARN_A1_probes_kvarn.json` / `WO_KVARN_EXP1_probes_k4v2.json` in the kvarnport worktree). Both tiers failing together names a SHARED seam: launcher dispatch, KV append/commit, scale-table wiring, or host-side dial — NOT the k4v4-specific slice6 path. Your job: an evidence-first ranked root-cause list that shortens their debug.
- **Method (in order):**
  1. Read the port diff: `git diff amd/main...amd/wo-kvarnport -- src/` in your worktree (3 files expected: HipSources.cmake, hip_link_stub_arms_perf.cpp, serve_options.cpp) PLUS the desk's uncommitted edits (`git -C /home/chris/worktrees/amd-wo-kvarnport status --porcelain` + `git diff` there — read-only).
  2. Read the banked probe transcripts + any serve logs in the kvarnport worktree's results/ dir. Look for: boot preflight lines (kv bytes/token — does the boot PRINT kvarn-class KV sizing, i.e. did the dial actually route?), `[rank N] kvarn ...` boot lines, any 0-token or empty-content patterns.
  3. Trace the single-seq serving path with the dial ON: serve_options parse → tp_engine dispatch (tp_engine.cpp:190-196) → text_context kvarn_attend_text (text_context_impl.h:429-487) → `ops::gqa_kv_append_kvarn_and_commit` → attention launch. For each seam, name what would produce EMPTY output while finish=stop (e.g., attention reading never-committed pages → garbage → argmax collapses to EOS every step; scale tables zeroed; capacity_bytes returning 0 → zero-page pool).
  4. Rank hypotheses by (evidence strength × cheapest decisive probe). For each: the ONE probe that decides it (a log line to read, a 5-min check — NOT a new instrument).
- **Deliverable:** append `## PROGRESS LOG` (newest-first) to THIS file with the ranked list + evidence lines, and `git commit` it on your branch. Hand off by committing; the coordinator relays to the port desk.
- **Laws:** read-only (any edit = desk fails audit); no GPU touches; clocks irrelevant (no perf numbers); if you find the bug with certainty, STILL only write the report — the port desk owns the fix (RED→GREEN cell law applies to THEIR close).
- **Escalate:** if the kvarnport worktree shows no file activity for >30 min (stall), note it in your log — coordinator decides on resume.

## PROGRESS LOG (newest first)

- **2026-09-19 — agent #2 (read-only falsifier desk): ROOT-CAUSE HYPOTHESIS RANKING for the k4v4+k4v2 empty-answer RED, bin `63a3d37e42b97833`.** Read-only honored: no source edits, no builds, no boots, no GPU, no pkill. Evidence base: both RED probe JSONs (kvarnport worktree `results/amd/coherence/`), the port diff (`git diff amd/main...amd/wo-kvarnport` + the desk's uncommitted edits), `docs/amd/WO_KVARN_PORT.md`, and the leg serve logs in `/tmp` (`kvarn_boot_k4v2.log`, `kvarn_boot_b_bf16.log`, `kvarn_probes.py`).

**0. THE DECISIVE EVIDENCE WAS ALREADY IN THE BOOT LOG — the RED is a PREFILL-SCALE signature, not a tier bug and not a decode bug.**

`/tmp/kvarn_boot_k4v2.log` (same bin as both RED JSONs) partitions the requests exactly:

- prompt=68 tok → `[rank 0] kvarn reset inflight (re-prefill from 0)` → **gen=25, coherent deterministic ids** `760 1156 369 9859 728 310 5707 ...` (req 1 and req 4 byte-identical). So for EVERY one of the 16 full-attention layers: kvarn quantize→pack→commit (page 0 + tail tile of 4), materialize, the BF16 flash, slice4-unified decode, per-tile scale write/read at (layer,head=kv_local=1,page 0), and the lm_head are all CORRECT.
- every prompt ≥ 1063 tok → `[rank 0] kvarn reset inflight (long-prompt re-prefill)` → **gen=1, last_tok=248046 (EOS/im_end), decode=0.00s**. The first generated token t0 is produced by the FINALIZE chunk's prefill attend over the full context — it is already EOS. The decode-split kernels never executed at long context in these legs.
- canonical serve line uses `--prefill-chunk 128` (`/home/chris/serve_fast.sh:15`): prompts ≤ 128 tok = ONE `prefill_chunk` call; > 128 = multi-chunk at 128 tok = 2 packed pages per chunk.
- preflight `kv(8976 B/t x 8192 tok)` — kvarn-class sizing, the dial routed (bf16 is 34816 B/t).
- Both tiers share this route (width-generic materialize+flash prefill), which is why k4v4 and k4v2 die identically. k4v2 P3 (short arith prompt) emitting "2" is the lone survivor — consistent.

Consequence: everything the 68-token pass exercises is EXONERATED (it cannot be the bug). The bug lives ONLY in what grows beyond it: **packed pages ≥ 2, scale tiles ≥ 2, and/or the 2nd+ `prefill_chunk` call.** Ranked list:

**H1 (rank 1) — page ≥ 1 misaddressing in the kvarn packed-page write/read pair (codes or per-tile scales read at the wrong logical/physical slot once `block_table[1..]` is non-identity).**
- (a) Seams: read side `src/ops/kernel/gqa_attention_kvarn_flash.cuh:80` (`if (kb < packed_pages) { const int phys = block_table[kb]; ...}` + the `kvarn_scale_at` page argument in the same kernel) and `src/ops/launcher/gqa_attention_kvarn.cu:331-359` (materialize launch, grid `(n_tiles, kv_heads)`); write side `src/ops/kvarn/kvarn_workspace.cpp:614-619` (`gqa_kvarn_prepare_page_for_append` / `gqa_kvarn_commit_completed`) with the quantize/scatter launches of `src/ops/kvarn/kvarn_tile_cuda.cu`.
- (b) Mechanism: at 68 tok the attend reads exactly one packed page whose logical and physical slots coincide on a fresh sequence — any logical-vs-physical (or workspace-page-count vs pool-page-count) convention mismatch is invisible. From page 2 on, a mismatch feeds attention wrong scales/codes → dequant garbage → garbage finalize-chunk hidden → argmax = EOS every request, finish=stop, empty answers. This device pair had NEVER been exercised on gfx900 before this port (the route was gate+stub until today), so a hipcc-gfx900-only miscompile or a gfx900-only indexing assumption has had zero prior soak.
- (c) Decisive probe (NO boot, minutes): the desk has ALREADY drafted the right instrument — `/tmp/test_kvarn_write_path.cpp` (16:18): append a ≥3-page sequence on-device, read back pages 0..2 codes+scales, diff vs the CPU quantize reference (the F5 style that caught the mma FWHT). Pages ≥ 1 diverging convicts H1 and the diff names the side that misindexes. Fallback that rides any next mandatory boot: 3 curls at T=100 (1 chunk, tail 36), T=128 (1 chunk, 2 packed pages, no tail), T=132 (2 chunks) — see H2.

**H2 (rank 2) — chunk-boundary seam: host state that only matters on the 2nd+ `prefill_chunk` call.**
- (a) Seam: `src/runtime/tp2/tp2_backend.cpp:2411-2461` (long-prompt loop: `set_text_kv_base(cursor)` at :2415, `prefill_chunk(...)` at :2419) feeding `src/targets/qwen3_6/impl/runtime/text_context_impl.h:429-485` (`kvarn_attend_text`; `tail.packed_pages = layer.tile_page >= 0 ? layer.tile_page : layer.committed_pages` at :468, envelope at :484).
- (b) Mechanism: if chunk 2+'s envelope/tail misreports packed pages or visible keys relative to what chunk 1 committed (off-by-one page, pos base not advanced), the causal mask or the materialize range is wrong → garbage → EOS. Host code is CUDA-proven, so this needs the misvalue to INTERACT with a HIP-only divergence — lower prior than H1, but the same curl ladder decides it for free.
- (c) Decisive probe: the T=132 curl (128+4): if T=128 passes and T=132 fails, the chunk boundary is convicted and H1's page class is exonerated. One boot, three curls, no new code.

**H3 (rank 3, LATENT — same wave64 class the desk already convicted) — 12 bare shuffles remain in `src/ops/kernel/gqa_attention_kvarn.cuh`: :155, :161, :329, :335, :381, :382, :576, :578, :591, :592, :658, :664** (e.g. `kvarn_dot_reduce`, `kvarn_dot_reduce_qr`, `kvarn_score_raw_k`, and the tail-dot block at :651-664 with `__shfl_down_sync(0xffffffffu, s, 16)`).
- (b) Mechanism: the desk's own mma.cuh fix comment is the proof: gfx900 has wave64 with no wave32 option; a mask of `0xffffffff` (32 bits) leaves lanes 32-63 non-participating with undefined partner data — measured F5 rel L2 > 6e-3 before the width=32 fix. Every site above is the same hazard unfixed.
- Reachability caveat (5-min grep, done): the carrier kernel `gqa_attention_kvarn_kernel` (:207) has NO launch site anywhere in src/ (superseded by the 2-pass materialize+flash route and the slice4 unified decode), and the T2..6 packed-verify route is MTP-gated with MTP off in these legs — so these sites are LATENT, not the convicted cause of THIS red. They will bite the first time a T≤16/small-t arm or a packed-route A/B runs on gfx900.
- (c) Decisive probe: extend the existing kvarn_gqa F5 test to each named helper — minutes, no boot; fix is the same `, 32)` width argument the desk already applied in `gqa_attention_kvarn_mma.cuh:82/:125/:134`.

**H4 (rank 4, HAZARD — provable by inspection, must-fix regardless of H1) — `scalars_slab_for_device` in the working-tree edit of `src/ops/kvarn/kvarn_tile_cuda.cu` can write past its 64-slot slab.** `scal_base = slab + (fetch_add(ntiles) % 64)` then the kernel indexes `scal_base[blockIdx.x]` for `blockIdx.x < ntiles`: any `ntiles > 64`, or any counter position where `base + ntiles > 64`, is an out-of-bounds write past the `hipMalloc(sizeof(TileScalars)*64)` region into neighboring device allocations — silent corruption, no fault.
- Why it did NOT fire in the RED legs: `--prefill-chunk 128` ⇒ ntiles ∈ {1, 2, 8} per launch and 8 | 64 keeps the monotonic counter aligned so `base+ntiles ≤ 64` always holds. Any future geometry with ntiles ∤ 64 (bigger chunks, k5v4, hosted-KV legs) corrupts silently — this is the VRAM-law-adjacent bug class (corruption, not refusal).
- (c) Decisive probe: none needed — provable statically; RED→GREEN cell for the close: a unit case with ntiles=65 asserting no OOB (slab-bound assert in `launch_quantize_tile` or a device canary).

**H5 (rank 5, observation for the budget owner, not a cause) — preflight printed `kv(8976 B/t × 8192 tok)` (70 MiB) but the pool spec sums to ≈4224 B/t** (16 layers × [256·4/8 K + 256·2/8 V + 1152·4/64 scales] per `src/targets/qwen3_6/impl/state/decoder_state.cpp:97-98,135-143`). A 2.1× over-charge can only ever refuse a launch (VRAM-law reporting accuracy), never corrupt output — reconcile `tp2_budget.h:138`'s width-generic formula against the plane spec.

**Process notes**
- Desk NOT stalled (no escalation warranted): `/tmp` shows activity 16:18 (`test_kvarn_gqa.cpp`, `test_kvarn_write_path.cpp` drafted — exactly H1's decisive probe) and 16:30 (hipfb compile temps — a rebuild in flight).
- Leg B (bf16 reference) log `/tmp/kvarn_boot_b_bf16.log` is TRUNCATED at req 1 — the bf16 long-prompt control rows were never banked. Bank leg B fully next window so the kvarn legs have their coherence anchor per the serving-window manifest.
- Recommended order for the next window: (1) run the drafted write-path test (H1, no boot); (2) if clean, the 3-curl ladder T=100/128/132 on the next mandatory boot (H1-vs-H2 split); (3) F5-extend the .cuh shuffle helpers while the window is idle (H3). Free instrumentation already built in: `NINFER_MB_DBG=1` prints `[SEQ-TAIL] tail_count/committed_pages/tile_page` + first k/v floats per boot; `NINFER_WS_PEAK=1` prints `[WS-PEAK]` arena attribution per route.
