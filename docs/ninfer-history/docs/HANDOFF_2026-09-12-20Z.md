# ═══ HANDOFF — COORDINATOR COLD-START (AMD line, 2026-09-12 ~20:0xZ, coordinator 01a095b4 handing off at user request) ═══
**A fresh coordinator session reads THIS block first**, then AGENTS.md, then the STATE blocks above it (newest first). Do not act on the NVIDIA-line blocks or pre-2026-09-12 STATE as live state — those are another host/era.

## In one screen (T-18:0x on a user-set 24 h sprint: functional NON-MTP q3 TP2 serve on this 4×7.98 GiB gfx900 box)
The hard parts are DONE and hardware-verified: shuffle emulation fixed + G-AMD-13/15 device data; Q3 data path builds/links on HIP; 1,900+ LOC of gfx906-donor kernels adopted VERBATIM (re-hashed) incl. full attention family; T3 prefill **device-VALIDATED to the bf16-quantization scale**; gemini's full gate package merged (checks (a)-(j), spin-guard, macro-guard, D3-fingerprint, d=1152, q3_ci conditional MODE + ART-guarded =129 — all verified by me at 2e1a98e7 merge); **q3 artifact ON THIS BOX, sha256 7f26a0eb… MATCH, manifest parses, Q3G64_F16S=129 at rest** (agent2's receipt results/amd/q3_arrival_verification.md). What remains: (1) agent4 builds the `ninfer-serve` HIP LINK (first-real-errors pending; then MY stamped serve window = G-AMD-17+); (2) agent3's T3 decode zero-visible-keys block (handoff debrief @ eb2dad69 on t3-wip; my fresh-eyes note: prefill validated with the SAME append/cast ⇒ layout-break suspect weakened, walk/width-semantics the differential); (3) reference-artifact salvage: agent2 TRUNCATE GO given (prefix sha proven = registry, no resume — two-writer class); (4) agent5 permanence census (WO-07, just booted); (5) P3 bring-up + gemini's parity vs OUR tp1 control.

## Live branch map at handoff (verified ~20:0xZ — cite refs BY NAME, tip moves hourly)
| Ref | Tip | Means |
|---|---|---|
| `origin/amd/main` | **2e1a98e7-merged, pushed** | Integration branch = gate package + agent5 base refresh; golden GREEN, anti-res PASS, gate (a) 9 registered exceptions. |
| `origin/main` (Team Green) | moved 5× today; last sync `fc4da3ca` era fully absorbed | merge INTO amd/main only, §1 zero-overlap proof in msg, step-0 tp_engine/tp2_budget zero-diff after EVERY merge (mine to run); their q3 landing PASSED — TB2 settled (v340l/05). |
| `amd/wo-p3-serve` (agent4) | from f4cf9a82 | WO-06 serve bring-up; worktree `/home/chris/worktrees/amd-wo-p3-serve`. |
| `amd/wo-gfx900-perm` (agent5) | from b91d3bf0 | WO-07 permanence; worktree amd-wo-gfx900-perm. |
| `amd/t3-wip` (agent3) | eb2dad69 | T3 all: prefill validated, decode block open; NOT merged past 042c5604-era — merge gate = coord verify of their choice-point report. |
| `amd/wo-shim-funcattr` (agent2) | ecd7fd60 | hostalloc template-asymmetry fix + last 2 spine surfaces (28/0) — **NEEDS REBASE onto 2e1a98e7-merged main, re-run 12+4 cells, then I merge**. Their salvage + sha lanes in-flight. |
| `wo/v340l-phase-gate` (gemini) | 2e1a98e7 | MERGED. Back online 19:5xZ after silence — surface-to-user canceled. |
| dead/done | wo-q3hip (merged), wo-shim-width (merged), tp2-probes (merged), v340l-shflfix/hip/phase-gate (historical) | provenance only. |

## Roster & mesh (current)
| Lane | Who | Channel | Doing | Owns exclusively |
|---|---|---|---|---|
| Coord | **incoming session** — claim `coordinator_id.txt` FIRST | intercom + hub `coordinator` | merges, stamps, verify-by-rerun | this doc, grants, rulings |
| P3 serve | agent4 (new) | intercom | WO-06 G1 link now; serve stamp G2 after | apps/serve wiring, kvarn launcher lanes, HipSources appends for those |
| T3 | agent3 (01a095d8, glm-5.3, ~51% ctx) | intercom | decode block (handoff debrief eb2dad69 if handed off) | gqa_attention_*_gfx906*, oracle tables |
| Shim/artifacts | agent2 (01a09668) | intercom | truncate-GO executing; ecd7fd60 rebase; watcher daemon | hip_shim/cuda_runtime.h, v340l/06 discipline |
| Permanence | agent5 (new) | intercom | WO-07 S1 baseline | tools/v340l receipts/probes, v340l/07 |
| Gates/tests | gemini (b4791a54 HUB ONLY, intercom name does NOT resolve) | hub | fingerprint-cell upgrade (see queue), parity consumption at P3 | gate scripts, tests/** |
Pi lanes ↔ intercom; gemini ↔ hub ONLY. Never route pi↔pi via hub.

## Pinger/watchdog (yours to take over — DO NOT rebuild, it's tested)
`~/.pi/agent/tmp/pinger_amd/`: loop.sh (3600 s), watchdog_loop.sh (900 s, 2-tick rule, candidate-branch tips — lanes move branches, do NOT retarget per-lane-name), stop via `bash stop.sh` (own-pid discipline, refuses foreign), wake text = `ping_msg.txt` read FRESH every tick — **rewrite at every phase change** (stale wakes burned us 3× today). **`coordinator_id.txt` MUST be rewritten to the new session's hub name** or alerts suppress (fail-loud, by design — the loose-finder mis-routed to agent3 for 3 h; that fix is load-bearing). **DEFECT FOUND AT CLOSE, guard shipped, harden fully on your first wake:** the pinger relayed my own merge-message (#194) under its own name — forged-authorship class. wake.mjs now refuses state-file content claiming non-pinger authorship (guard + refusal-log cell, tested 20:07Z: fake 'coord-' content → DELIVERY REFUSED, real wake → delivered). Your hardening: verify sender integrity from the DB (who actually signed), keep the delivery log's size+target lines. #197/#198 (20:07/20:10Z) are the OLD stale successor-text on the real header — correct sender, stale content, since superseded by the 20:10:22Z text (1705 B); if you wake to a #197/#198-clone, check FAST-FACTS date vs `git log -1 --format=%ci amd/main` — mismatch = stale, reload the file. The lesson generalizes both ways: a pinger mail is only as good as its content's timestamp, and its header is only as good as the guard (mine at #194 signed YOUR prose; the regex guard now refuses coord-authored state-file content; DB-signature hardening stays yours). Alert mail reaches the claim-file session only; drain lag is normal, `created_at` discriminates echoes (the #74-#82/#133-#153 families were mine; all closed).

## In-flight promises the successor owns (act, don't rediscover)
1. **G-AMD-16/17**: agent4 asks for serve stamp when link green — written grant, dev pair 0,1 (flat topology, pair choice NOT load-bearing — v340l/05 §1), per-launch timeout, release row, freshness by sha; q3 file ready at `/media/chris/EMTEC256/qwen3_8_27b_q3.ninfer`.
2. **agent2: FULLY CLOSED, merged 2ae7631a (pushed)** — hostalloc/spine shim (28/0, param-1 companion mechanism — their correction to my flag-enum attribution is in the merge msg AND the file), REF SALVAGE done+sha-MATCH both artifacts verified, q3_ci truth table (=129 proven biting), addendum-delete list routed to agent3 (5 mappings byte-identical shim targets + their 2 compile cells as the deletion gate). Their watcher daemon (30-min) still running for successors. NOTE for agent4 wiring: reference artifact has NO Q3G64_F16S — =129 arms-proof applies ONLY when ART is the q3 file; first real MODE=tp2 serve takes the NOTE branch (tally emits only when non-empty, main.cpp:77-80) — that's expected, not failure.
2a. **AGENT3 G-AMD-16 REPORT (landed mid-close, verify before you act):** root cause = **runner readback bit-cast bug** (healthy partials m≈18/l≈5-10 now committed; device chain kernel→partials→reduce→out sound by their code-read; the 'all zeros' were 1e-41 denormals from bf16-into-low-half cast; fix runner-side, one shift at :246 — coord-verified in place; comparator upgraded to the C441 bars + raw-f32 dump for independent recheck). Evidence commit **0bbce5b8 NOT YET PUSHED as of handoff** — if absent when you wake, they were mid-push or handed to a fresh session; their ask = fresh window (G-AMD-17) to run the fixed comparator, and t3-wip rebase-then-merge stands. My open discriminator (run3 still-zeros → partials-path same-bug check) is now MOSTLY answered by their healthy-partials finding — your job is the byte-check, not the re-diagnosis.
2b. **G-AMD-16 GRANTED 20:0xZ (written = my intercom msg to agent3):** /tmp/t3_device_runner_g16 @ 0bbce5b8, dev0, ≤10 min, rebuild-freshness REQUIRED (binary ~40 min old at grant time), cos/role + ULP verdict + raw-f32 dump + release row. Root cause PRE-VERIFIED by me with one gap to watch: the readback diagnosis is real (runner :246 zero-extends bf16 into the f32 TOP half; a bottom-half bit-cast prints 1e-41≈0; healthy partials m≈18/l≈5-10 now committed; binary sha matched in place). BUT run3's committed log still printed 3072×0.00000000 after a claimed-fixed runner — pre-fix residue or the PARTIALS readback carries the same bug (:246 is the out path; fix may be partial). G-AMD-16 doubles as the discriminator: if out-cells pass and partial-cells read ~0 again → partial path needs the shift too; do NOT conclude kernel-product either way. Rebase onto 7d2f70c9-merged main ordered BEFORE further work (5 merges behind; their Check-(k) reads agent3's own files — expect PASS). t3-wip UNMERGED pending all this; successor re-runs the golden+gate on their merge.
3. **gemini: QUEUE FULLY CLOSED EARLY (7f09af8c, merged by me 7d2f70c9 after a throwaway-worktree gate PASS (a)-(k) rc=0 + golden + anti-res on my own runs)** — spelling-matrix blesses the 3 math.cuh bridge helpers and CATCHES the __hadd2 software-lowering class (my receipts became the probe corpus); Check (k) is the GQA geometry tripwire (never-launch-vs-wrote-neutral, the silent-fallback class behind both device anomalies); parity spec now banks P1 eager + fp16-V facts. Their remaining consumption work: TG1 parity tables at P3 data (agent4's serve window) + embed-dense sweep cell — both noted on their side, not owed-blocking.
4. **My sha re-launch** was running (~20:30Z ETA) — its result is now redundant vs agent2's double-witnessed match; kill it if still alive (own pid only), don't spawn a third reader on CIFS.
5. **STATE 20:0xZ commit** (this block + ref-salvage + gate merge) lands in my final push; if missing when you cold-start, `git log amd/main` and reconcile.

## Measured facts — cite, do not re-derive (added today)
4× gfx900 7.98 GiB, flat topology (all off-diag weights 40, 4 buses), no P2P (canAccess=0; memcpyPeerAsync RETURNS SUCCESS driver-staged — the stale expect-error note is corrected), staged 3.13 GiB/s / 14 µs small, **cross-device graphs DEAD on 6.2.0 (launch OOM + spliced-memcpy DATA LOSS; per-device-only graphs 11.26 µs/node; capture itself legal — donor 6.4.1's rejection did not reproduce)**; eager-first + AR-count-budget = measured law; donor artifact ≠ ours (18.21/20.44/15.45 GB — goldens never transfer); bf16 arith COMPILES+emulates (39-instr/6-instr-per-op; compiler-enforcement premise dead); hipcc __CUDA_ARCH__ undefined host-pass; funcattr mapping≠success (hipFuncSetAttribute NotSupported class); __syncwarp = true no-op on gfx900 (receipts script); artifact discipline: size+sha+magic+read-storm before ANY serve; **reference file over-length +367 MB pending truncate (prefix proven)**.

## Standing rules that held the sprint (keep them)
Verify-before-rule (4 static stories died tonight, TWO were mine incl a false 'undeclared' claim and 'ref 101%' from a stale cached size); claimed≠proven (freshness by content: marker+hashes in-message, rebuilt from committed HEAD); read the matched line not the total (three instrument errors today — mine included, all disclosed before being caught by others); no estimates refuse launches (VRAM LAW); §7.x (gemini test-lane exclusive, silent-lane → surface-and-wait NOT reassign — honored today: the 'escalate at next wake' was canceled by their return, zero blame); write grants BEFORE device time (G-AMD-14/15 both written in-file; grant-before-launch inversion at 15:08Z was process-noted, no harm); own-pid kills only; df -h before builds (27 G now — ref salvage frees nothing but the truncate lands the file; /media and / independent); cite refs by name; commit before report; STOP-ask > silent workaround (agent3's addendum restore, agent4's shim asks, agent5's base pull — all correct shapes).

**State at handoff:** amd/main = 2e1a98e7-merged (pushed), golden GREEN, anti-res PASS, gate 9-exception clean, cards No-KFD baseline, 5 lanes executing (agent3/4/5/agent2 + gemini back), q3 artifact VERIFIED, G-AMD-16 pending on agent4's link. — **C441**
