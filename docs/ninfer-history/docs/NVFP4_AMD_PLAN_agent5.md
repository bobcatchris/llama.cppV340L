# NVFP4-AMD execution plan — agent5, planner of record (2026-09-14; **rev 3.4** — §6 what-changed, §7 N-LATER queue, §8 ARTIFACT-DAY RUNBOOK (the operational section); rulings in chat rot at merge-speed, so they live here)

**Domain:** run the published mixed NVFP4/FP8 Qwen3.8-27B artifact on this gfx900 box,
software-dequant, v100-skinny's recipe in AMD style. Feasibility is GIVEN by chair ruling
(user: "known to be possible, no pushback") — this is an execution plan, not an adjudication.

**Recipe being executed** (v100-skinny claim, fetched by chair — cited-unverified here):
same model family on four 2017 V100s; NVFP4 regions via QPN2 W4A16, FP8 regions via QPN8
W8A16; **published quantization read UNCHANGED, no requant**; FP16 activations/KV; decode
PARITY with RTX 5090 at native MTP k=7; verification cost +0.383 ms/row vs drafter
+0.817 ms/step (both numbers are THEIRS until our battery re-measures ours — era/field
citation hygiene).

**Ground rules this plan inherits:** shared checkout = chair-merge only (this lane's work
runs from `worktrees/amd-wo-nvfp4`, branch `amd-wo-nvfp4` off `amd/main` @ ff8ff70e —
worktree created and announced to chair); TP4 finishes first, nothing here steals the
card pair while window legs run; artifact lands in hours, so every zero-card leg must be
executable TODAY against synthetic fixtures.

---

## §0 What I verified at my seat BEFORE planning (tool-echoed, tip ff8ff70e)

The chair's dispatch brief was written partly from the NVIDIA line's memory. Recon found
**more of the tree already exists than the brief assumed** — that reshapes the lanes. All
cites below were opened and echoed this session; anything I could not verify is marked
**[chair-cite, unverified-here]**.

1. **Artifact layer already knows both codecs.** `src/artifact/reader.h:31-32` declares
   `NumericFormat::NVFP4` and `FP8_E4M3FN_ROW_BF16S`; `:38-39` declare
   `StorageLayout::BlockScaleK16M128x4V1` and `RowScaleV1`.
   `src/artifact/storage_layouts.cpp:182-205`: NVFP4 geometry = code plane + swizzled E4M3
   scale plane at a tensor-aligned offset + a 4-byte FP32 **weight-divisor word**;
   `encoded_bytes` is exact-checked arithmetic (`checked_mul/checked_add` — refuses loud).
   The `quant_geometry` switch at `:35-55` (Q2 16+2 / Q3 24+2 rowsplit law) *throws* on
   NVFP4 by design — NVFP4 lives on the blockscale layout, not rowsplit. That throw is
   door-(iii) already working.
2. **The MIXED profile already resolves.** `package.cpp:109-111` **[PATH CORRECTED rev 2 — agent4's find, verified at my seat: there is no `src/artifact/package.cpp` on this line; resolution is `src/targets/qwen3_6_27b/export/ninfer/targets/qwen3_6_27b/package.h:36` + `impl/load/bindings.cpp:44,525` — see §6.0]**: `model_id==qwen3_8 &&
   weights_id=="nvfp4"` → `WeightsProfile::Qwen38Nvfp4`. `variant_kernels.cpp` has 24
   NVFP4/FP8 profile branches; `:656-660` sizes Qwen38Nvfp4 workspaces as
   `max(NVFP4, FP8_E4M3FN_ROW_BF16S)` per site — i.e. **the tree already encodes
   "some regions NVFP4, some FP8"** exactly as the recipe needs.
3. **Row_view law and bindings already exist.** `bindings.cpp:108` `nvfp4_row_view`
   (whole-128-row-blocks-only, per the `:106` review-fix comment), `:162` generic
   `row_view`; `:71-72` a *logic_error* refusal if an NVFP4 weight arrives without its
   paired divisor; `:86` binds format+layout jointly; `:152` `scale_dtype = FP8_E4M3FN`;
   `:395` `kFp8 = FP8_E4M3FN_ROW_BF16S`; `:438` comment: NVFP4 exports vary — fused
   `[96,5120]` or split — already a known admission hazard.
4. **TP2 shard law + a pure-host byte-exact golden ALREADY EXIST** (the chair's "wall-3"
   is real and green-able today): `tests/multi_gpu/nvfp4_shard_host.cpp` tests
   `nvfp4_shard_image()` from `tp_load.{h,cpp}`; its fixture generator
   `gen_nvfp4_shard_fixture.py` uses **the real exporter** `tools/artifact/layouts.py
   encode_nvfp4` as oracle (imported, not re-implemented), proving rank images
   reassemble byte-for-byte, divisor verbatim in every image. `layouts.py` also has
   `swizzle_nvfp4_scales/unswizzle` (`:486/:505`, formula pinned in tp_load.cpp) and
   `encode_nvfp4/decode_nvfp4_words` (`:544/:578`).
5. **The KV flag's twin situation — the chair's open question, answered:**
   `--kv-dtype nvfp4` parses → `KvCacheStorage::Nvfp4Group16`; the TP2 backend maps it
   (`tp2_backend.cpp:735-754`, group=16, `DType::NVFP4_G16`); `decoder_state.cpp:37-85`
   validates geometry (requires `quant_group==16`, `head_dim%16==0`) and cites the
   gzenz-codec layout from docs/155. **`decoder_state.cpp` IS HIP-whitelisted**
   (HipSources.cmake:156) so the host-side state math lives. The device-side
   quantize-append / attention-dequant kernels (`gqa_attention_kv_quant_nvfp4.cuh`,
   `gqa_decode_slice7_nvfp4.cuh`, `test_nvfp4_kv_append.cu`) are **NOT in the HIP
   whitelist — there is no HIP device twin today.** And note: the v100-skinny recipe runs
   **FP16 KV** — NVFP4-KV is *beyond* their recipe, so this is a stretch lane, not a
   critical path (§N4).
6. **The NVIDIA-side source list for "copy in AMD style" is exactly where the brief said:**
   `src/ops/linear/nvfp4/` (`nvfp4_gemv.cu`, `nvfp4_small_t.cu`, `nvfp4_w4a4.cu`,
   `nvfp4_codec.cuh`, `nvfp4_format.cpp`), `linear_add/nvfp4/`, `linear_swiglu/nvfp4/`,
   `attn_input_proj/nvfp4/`, `gdn_input_proj/nvfp4/`, and the FP8 twins under each.
   `nvfp4_codec.cuh:14-15` decodes E2M1 via `__nv_fp4x2_e2m1` and `:35-49` packs via PTX
   `cvt.rn.satfinite.e2m1x2` — **both PTX/tcgen05-era surfaces gfx900 lacks** (same class
   as Volta lacking fp4/fp8 mma; their answer was SIMT, ours is too). CPU oracle exists:
   `nvfp4_kv_codec_ref.h` ("identical semantics" per the KV kernel header).
7. **HIP lane state:** `src/HipSources.cmake` whitelists nvfp4/fp8 **host-side only**
   (plans/dispatch/formats, `:73-107`) plus one kernel (`nvfp4_gdn_snapshot_post.cu`
   `:256`). q2/q3 rowsplit device ports are live and green (`:208-212`), w8 SIMT family is
   live. The gap inventory `v340l/07` already classed `cuda_fp4.h`-dependents
   (`nvfp4_small_t.cu`, `nvfp4_w4a4.cu`) RED with the honest ruling: **not a shim hole —
   D1 include-trim + real port is the route.**
8. **The TP2 weights-gate:** `tp2_backend.cpp:1336` fails closed on NVFP4-weights artifacts
   behind `NINFER_ALLOW_NVFP4_TP2` env; its own comment says the untested surface is
   `make_rank / materialize_tp / prefill dispatch`, and flip to a real
   `--allow-nvfp4-weights` flag only after live-path + run_ci proof. That is our N7 gate,
   already legislated by the code.
9. **Not found at my seat:** `R4-35` appears nowhere in docs/ or src/ here
   **[chair-cite, unverified-here]** — N0's duel-close law stands on its own arithmetic;
   chair should supply the precedent text at routing or we name the class fresh.
   `CC-12.0` likewise appears only in the day-plan prose (agent1's world=4 forensics) —
   the dependency in §N5 is real but its mechanism I inherit from agent1's reports, not
   from a grep.
10. **Host python has no `pytest` module** (measured: `python3 -m pytest` fails here;
    cmake/ctest exist off-PATH at /home/chris/opt/cmake/bin per chair's RULING 3). Test
    lanes must run goldens via direct `python3 <script>` harnesses or a venv — gemini pins
    the runner choice as a gate, not a surprise.

## §1 The actual gap (one sentence, then the honest list)

The tree can already **admit, geometry-check, shard, and byte-exactly place** the mixed
artifact on the host; what does not exist is any **gfx900 device-side dequant path** for
NVFP4/FP8 weights. Everything else is registration, gates, and receipts.

| # | Gap | Class |
|---|---|---|
| G1 | E2M1→F16 and E4M3→F16 LUT decoders as `__device__` tables, replacing PTX/cuda_fp4 spellings | new codec member (tiny LUTs: 16 and 256 entries) |
| G2 | NVFP4 blockscale W4A16 SIMT GEMV (T=1) — one warp/row, reads swizzled scale planes directly | port in the q3_rowsplit_gemv dialect |
| G3 | NVFP4 W4A16 SIMT GEMM (T>1, chunked) | q3_rowsplit_gemm_simt dialect |
| G4 | FP8_E4M3FN_ROW_BF16S W8A16 GEMV/GEMM device path (HIP twin of fp8_gemv/fp8_small_t minus their PTX) | w8_rowsplit_gemm_simt dialect |
| G5 | Route registration: the linear/linear_add/linear_swiglu/attn_input/gdn nvfp4+fp8 **plans** are whitelisted host-side; their `.cu` decode/small_t arms are not | whitelist + per-arm compile cells |
| G6 | Dispatch refuses: `nvfp4_dispatch.cpp:21/62` "unsupported shape" throws must cover our three-presentation-sites inventory {4096,2048}×{12288,6144,3072} | **[rev 3.10 INVENTORY CORRECTION, agent3's READ-1 on real bytes: the REAL NVFP4|blockscale shape set is {14336,16384,34816}×5120 + 5120×{17408,6144} — ZERO overlap with the drafted {4096,2048}×{12288,…}; the draft's numbers were the OTHER export's geometry. The derive-not-literal law is why the cells still PASS on real bytes (247/247 legality N%128/K%64 measured by agent3's b1395914 and by my §8g census) — only the named inventory was wrong, and ONLY in prose: nothing literal was ever coded against it. Hour-1 dispatch-refusal arms re-aim at the five real shapes + their world-2/4 halves.]** | derive-not-literal (vindicated: the law is why the prose error cost nothing executable) |
| G7 | The A4 (W4A4) policy `kNvfp4TextPolicy = AllowA4` (variant_kernels.cpp:48) — A4 needs fp4 mma we don't have; the AMD profile must bind A16Only and the refusal for A4 must be loud, not a silent downgrade | exact-contract refusal |
| G8 | TP2 boot path behind `NINFER_ALLOW_NVFP4_TP2` on the HIP lane, plus the eventual real flag | N7 |
| G9 | KV nvfp4 device twin (append + attention dequant) | stretch, §N4; v1 runs FP16/BF16 KV per the recipe itself |

## §2 Lanes, owners, gates

Owner proposals are **proposals** — chair routes and grants; nobody boots without a
written grant. Every lane below is **zero-card** (CPU/host/compile) unless stated; card
work is deliberately deferred to a per-window boot battery so nothing of ours can collide
with agent1's TP4 window legs.

### N0 — Artifact admission (host-only). Owner proposal: agent4. Reviewer: agent5.
The tier-size identity duel-close **before ANY boot** — sealed by arithmetic.
- N0.1 Read container identity → assert `Qwen38Nvfp4` resolution (bindings.cpp:44,525 — path corrected rev 2, was a phantom `package.cpp:109`); any
  other resolution is a REFUSED boot, three-state exit D.
- N0.2 Per-tensor geometry check for every tensor: dispatch on (format, layout) pairs —
  NVFP4 → `block_scale_geometry`, Q2/Q3/Q4/Q5/W8 → `quant_geometry` rowsplit, FP8 →
  RowScaleV1; recompute `encoded_bytes` and duel against the container's manifest/offsets
  with **no slack, no estimation** (measured-only law: exact-arithmetic equality or
  cudaMalloc-time truth, never a budget constant).
- N0.3 Mixed-region census in the closed-tier style (Q4=135/Q5=70 precedent): a table
  {NVFP4-region rows, FP8-region rows, BF16 remainder, divisor-word presence per NVFP4
  tensor} — the artifact's answer must equal the manifest-derived expectation per-tensor,
  and the fused-vs-split `[96,5120]` hazard (bindings.cpp:438) is enumerated, not assumed.
- N0.4 World-2 projection: same identity replayed at per-rank rows = full/2 via
  `nvfp4_shard_image` — composition of rank images reassembles to the full image
  (extends the wall-3 mechanism to the REAL artifact shapes).
- **Gate N0-G:** RED→GREEN triple — a corrupted-divisor / off-by-one-offset synthetic
  fixture must RED the admission cell (positive control, law-C: seat that observed each
  direction named), the honest fixture GREENs, cell joins `run_ci_amd.sh`.

### N1 — Loader/codec registration, HIP lane. Owner proposal: agent2.
- N1.1 Verify the door-(iii) refusals fire on the HIP build, not just CUDA: missing
  divisor → logic_error (bindings.cpp:71); NVFP4 into rowsplit geometry → ArtifactError
  (storage_layouts.cpp:54); bad swizzle → host test.
- N1.2 Register the AMD dialect branch: row_view whole-128-block law at world=2 for
  NVFP4/FP8/rowsplit alike; no new per-format special case without a can-fail cell.
- N1.3 Loud refusal inventory: a table of every NVFP4/FP8 admission/refusal site with the
  exception text and its fixture.
- **Gate N1-G:** each refusal ships with a positive-control fixture where the defect IS
  PRESENT (review-bar addition, 1991b550); absence-bins alone are rejected.

### N2 — Linear tiers, SIMT device kernels (the heart). Owner proposal: agent3.
LUT dequant, no requant, no fp16 weight copies — weights stay on disk bytes as published.
- N2.1 `nvfp4_amd_codec.cuh`: `__constant__` F16 LUT[16] for E2M1 (incl. the two -0/+0
  and six magnitudes with the NaN slot handled by contract), F16 LUT[256] for E4M3 scale
  bytes (0x7F=NaN → refusal at admission, not at multiply). **Golden:** must equal
  `decode_nvfp4_words`/`nvfp4_kv_codec_ref.h` on exhaustive inputs (16×256×divisor
  enumeration — finite space, enumerate it).
- N2.2 T=1 GEMV in q3_rowsplit_gemv.cuh's dialect (one warp/row, correctness-first,
  mirrors the CPU test's arithmetic as its single source of truth): read 16-element
  nibble runs + one swizzled-plane scale, `out += lut[code] * lut_e4m3[scale] *
  divisor` at FP32 accumulate / BF16 out (our house activation dtype; v100-skinny's FP16
  is THEIRS — we cite it, we don't inherit it silently).
- N2.3 T>1 chunked SIMT GEMM in the q3_rowsplit_gemm_simt dialect; scale-index math for
  the m128x4 swizzle shared between N2.2/N2.3 (single home, era law).
- N2.4 FP8_E4M3FN_ROW_BF16S arms (G4): reuse N2.1's E4M3 LUT; row-scale is one bf16/row —
  w8_rowsplit_gemm_simt.cu shape.
- **Gate N2-G (per sub-lane):** RED→GREEN triples — mutate one nibble / one scale byte /
  one divisor bit in the fixture, cell must go RED naming the element; exhaustive host
  LUT golden is the positive control; SIMT-vs-CPU-oracle sampled compare at the real
  three-presentation-sites geometries {4096,2048}×{12288,6144,3072}×k=5120 AND their
  world=2 halves, derived (G6). `ctest -N` inventory line as first receipt (RULING 3).

### N3 — gdn/v/z + attention routes. Owner proposal: agent2 (same hands as N1).
- Wire the existing plan-side (already HIP-whitelisted) to N2's kernels:
  attn_input_proj, gdn_input_proj (+ snapshot post, already whitelisted), linear_swiglu,
  linear_pair; A16Only policy arms (G7: A4 refuses loudly — the refusal cell is a
  positive-control fixture, not prose).
- **Gate N3-G:** every route has a table-driven dispatch test at T=1 and T∈{4,16,33};
  a route without a device arm returns the named refusal.

### N4 — KV on HIP. Owner proposal: agent3, SEQUENCED LAST.
- v1 scope: **BF16 KV** — per the recipe, v100-skinny runs FP16 KV; NVFP4-KV is not on
  the critical path. State the scope so nobody "helpfully" turns on --kv-dtype nvfp4 and
  discovers G9 mid-window.
- N4-stretch (only after N0-N3 close): port `gqa_attention_kv_quant_nvfp4.cuh` append +
  decode-slice dequant to HIP against `nvfp4_kv_codec_ref.h` oracle (it is pure C++ —
  already enumerates the quantize semantics).
- **Gate N4-G:** append→readback bit-exactness on quantize-able values; lossy-equality
  tolerance claims are FORBIDDEN unless measured against the oracle per dtype — the
  closed-tier census style applies.

### N5 — MTP / verify depth k≤7. SEQUENCED BEHIND agent1's CC-12.0 world=4 forensics.
Honest dependency flag, chair asked for it: if CC-12.0 is a multi-token dispatch defect at
world=4, our world=2 boots inherit the same code path family; the day-plan's item 7
(post-TP4 MTP datum, draft-tokens [0,3] known, +2GB budgeted) is where our k-sweep joins.
`mtp_pack/mtp_round` are already HIP-whitelisted (HipSources :221-222) and MTP workspaces
are profile-agnostic (variant_kernels `mtp_post_mixer_workspace_capacity_bytes` — BF16
sized, no nvfp4 term to change).
- Gate: inherit agent1's CC-12.0 verdict before ANY k>0 boot; then k∈{0,1,3,7} ladder
  with per-k receipt rows; our verification-cost measurement replaces the v100-skinny
  +0.383/+0.817 citation at that moment (never cite their ms as ours).

### N6 — Tests lane = GEMINI (board standing; chair routes).
Already pre-routed in the day-plan (queue item 4: "nvfp4 codec host goldens + CC-gate pin
cell (RED leg = today's refusal)"). This plan adds the requirements:
- Quantize→reference→**enumeration** goldens (the E2M1×E4M3×divisor space is finite;
  sample-where-you-can't-enumerate is only allowed on the K-dimension of gemms).
- Can-fail both directions (law C: named observing seat per direction), three-state exit
  (D: pass/fail/refused-instrument-error before any text-matching), delta-against-this-run
  baseline (E), scoped cleanup + pre-run digest (F).
- Boot-receipt battery per per-window law: every card window ends by running its cells,
  results banked per-LABEL; pinned binaries boot from `/home/chris/artifacts_bin/` by sha
  stamp, never from a lane build path.
- CI wiring: `tools/ops/run_ci_amd.sh` joins every N0-N3 cell; anti-resurrection gate
  (`git diff main -- tp_engine.cpp tp2_budget.h` stale-region check) already guards the
  capacity law — our lanes touch that file only via the N7 flag flip.
- **Runner finding to pin (from §0.10):** no host pytest; the lane's cells choose direct
  python harnesses or a banked venv — gemini names it in the cell, chair approves.

### N7 — Un-gating the TP2 boot (`NINFER_ALLOW_NVFP4_TP2` → `--allow-nvfp4-weights`).
The code's own comment legislates this: flip only after live-path + run_ci proof.
Sequence: N0 (admission GREEN on real artifact) → N2/N3 (device arms GREEN on host
goldens) → single-card zero-TP smoke run under the env gate **with a written boot grant**
→ run_ci full → flag flip commit. Capacity: measured `cudaMemGetInfo` + actual-bytes
compare only; the preflight reports, the allocator refuses (VRAM law, ABSOLUTE).

### N8 — Artifact-wait strategy (cross-cutting, ACTIVE TODAY).
**IMPORT-HAZARD CORRECTION (agent3 verified finding, appendix 1fa3c219, 2026-09-14): tools/artifact/layouts.py uses RELATIVE imports (`from .container import …`) — a naive sys.path insert FAILS with `ImportError: attempted relative import with no known parent package` BEFORE any codec runs. Every fixture generator must import `tools.artifact.layouts` as a package with the REPO ROOT on sys.path (or run from repo root). A leg failing for an import reason must never read as a codec result — the whole boot-night lesson, pre-armed into §N0/§N8 execution.**
Every zero-card leg of N0-N3 is executable now against synthetic fixtures built by the
SAME exporter the real artifact will have passed through (`layouts.py encode_nvfp4`,
per the gen_nvfp4_shard_fixture.py precedent: oracle = real exporter, imported not
re-implemented) + the unsloth-derived published spec **[chair-cite — the spec doc is not
in this tree; chair to attach it when it lands]**. Target: when the artifact arrives, it
lands in a tree that is already GREEN on fixtures and can only go RED for a real
reason — a surprise becomes a signal, not a week.

## §3 Sequencing & card discipline

```
TODAY (zero-card):        N0.1-N0.4(synthetic) N1 N2.1 N2.2(host-golden prep) N6 goldens
                         N8 fixture factory
ON ARTIFACT (zero-card):  N0 real-artifact duel-close → N1.1 HIP-build refusals
                         N2.2/N2.3/N2.4 device compile + host-vs-device goldens (compile-only, no boot)
GRANTED WINDOW (pair):    N7 single-card smoke → TP2 world=2 boots → boot battery → N5 ladder (post CC-12.0)
STRETCH (later window):   N4 KV-nvfp4 twin
```
Nothing in TODAY/ON-ARTIFACT rows touches a GPU context. The pair stays agent1's while
window legs run; our device legs queue behind the TP4 close or into a fresh grant.

## §4 Law-family index (where each bites, so reviewers can check by name)

- **C direction observation** — every falsifier row in N0-G/N1-G/N2-G names the seat that
  saw GREEN-on-fixed and RED-on-broken.
- **D three-state exit** — admission (N0.1) and dispatch refusals (G7) are designed as
  distinct exit codes {pass,fail,refused}; checker scripts read rc before text.
- **E assert a delta** — N0's duel closes compare container-vs-geometry IN THE SAME RUN
  (never a remembered byte total); boot battery diffs against pre-run digests.
- **F scoped cleanup** — fixture generators write to per-cell temp roots, report their
  own collateral; leg-11 repo-cleanliness witness on every new cell.
- **Positive-control bar** — no guard ships in N1/N3/N7 without a fixture where the
  defect is PRESENT.
- **Measured-only capacity (VRAM law)** — N0.2/N7: exact arithmetic at admission, live
  cudaMemGetInfo at boot; no budget constant may refuse; anti-resurrection diff gate
  guards tp_engine/tp2_budget.
- **derive-not-literal** — N2-G world=2 geometries computed as full/2 from the same spec
  as the full rows.
- **cite-hygiene** — v100-skinny numbers are THEIRS until our battery prints OURS;
  [chair-cite, unverified-here] marks in §0 are the honest inventory of what I did not
  find (R4-35, CC-12.0 mechanism, unsloth spec doc).
- **RED→GREEN closure law** — each lane's gate row must show pre-fix sha RED, post-fix
  sha GREEN, suite entry, three shas. Prose closes claims, cells close bugs.
- **BANK-BEFORE-RELINK / boot-from-bank** — N6 battery boots `/home/chris/artifacts_bin/`
  stamped binaries.
- **Lane separation** — all execution from `worktrees/amd-wo-nvfp4` (or per-owner lane
  worktrees); no builds in the shared checkout; COMM LAW: directs only, no channel posts.

## §5 What I need from chair to route (not blockers — inputs)

1. R4-35 precedent text (absent from this tree — §0.9) and the unsloth-derived published
   spec doc the fixtures should match (§N8).
2. Confirmation that the artifact's exporter is our `tools/artifact/layouts.py` lineage
   (if it's a third-party exporter, N0.2's duel-close is the SAME check but the fixture
   oracle must be re-derived, not imported — one day of N6 work changes shape).
3. Owner routing for N0/N1/N2/N3 proposals (agent4/agent2/agent3/agent2 above) — say GO
   and I'll dispatch lane WOs directly.
4. When the artifact ships: its path + sha256 as echoed by a tool, not a filename.
4. When the artifact ships: its path + sha256 as echoed by a tool, not a filename.
5. **[rev 3.8 re-stated main-resident] the one-run unlock: the banked corpus a024627b is READ-free but REGENERATE-blocked by this host's torch/numpy absence; the cheapest unlock is ONE `python3 tools/v340l/nvfp4/gen_nvfp4_export_golden.py --out DIR` run on ANY torch-capable host from a plain checkout — the generator is now GIT-RESIDENT (amd/main@c218547f, blob content8 41d5e020 measured == the desk pin), so the ask carries no desk-path dependency anymore — consumer grades with `--fixtures DIR --expect-origin sha256(layouts.py)` and refuses rc=2 the moment the oracle moves. Contract loop closed on paper; one command closes it in fact.**
6. **[rev 3.8, formalized from agent3's notes §F.2 at its new main home — the kTensorAlignment hoist rider] hoist `kTensorAlignment=256` from `src/artifact/storage_layouts.cpp:8` into `reader.h` — ONE line, owner-line agent5 (their words: 'a one-line change in *agent5's* lane, deliberately not mine to make'); landing = the next legitimate `storage_layouts.cpp` touch in a live lane, NEVER standalone; anti-res winner named: storage_layouts.cpp carries no canonical-home claim (unlike tp_engine/tp2_budget.h), so first-legitimate-touch wins; the single-definition guard cell (RED on re-duplication, era/ring citation law) rides the same commit. NOT tonight — TP4 owns the trees.**

— agent5 (planner of record, AMD-NVFP4), lane worktree `amd-wo-nvfp4` @ ff8ff70e

## PARKED MICRO-FIX (chair rider 16:25Z, 1be9be6b): hoist kTensorAlignment=256 out of storage_layouts.cpp:8 into reader.h
(agent3's N0 cell duplicates it because the constexpr has no header home — one-line hoist, join in whichever commit next legitimately touches storage_layouts.cpp; DO NOT perturb it standalone; guard: the hoisted constant joins the era/ring citation law — cell asserts single definition at both consumer files, RED on re-duplication.)


## DECISION (agent5, 16:2xZ): TAKE agent4's tier→bytes identity cell (their retraction-thread survivor, 3eeab3b9 lineage).
Class: kv_bytes_for_tier's BF16 default (34*1024) is reached by ANY unlisted cache enum — optimistic-default guard, GATE-3's
out_capacity_elems==0 shape one file over. Cell (~10 lines, host, zero-card): enumerate every KvCacheStorage/DType cache member,
assert tier→bytes identity table EXHAUSTIVE (new enum without explicit branch = compile-time-testable RED naming the member).
Landing: next legitimate lane commit adjacent to tp2_budget.h — CELL ADDITION ONLY, no region edits (anti-resurrection law:
tp_engine/tp2_budget.h canonical home untouched; diff-gate will prove it). Credit: agent4's find, retracted-elsewhere-but-alive-here.
SHIPPED as agent4's `tools/ops/check_kv_tier_priced.py` @ 656aa7f6 — see §6.0 for my seat's verification receipt.

## §6 WHAT CHANGED TONIGHT (plan rev 2, agent5, 2026-09-14 ~18:1xZ — chair seq-87 assignment: fold agent3's appendix + agent4's cell and parks; every fold verified at MY seat, tool-echoed, not copied from the briefs)

### 6.0 Receipts for the folded artifacts (re-derived, not remembered)
- `tools/ops/check_kv_tier_priced.py` @ agent4 lane tip `amd/wo-p3-serve` = 656aa7f6: sha256-first-8 **e6db3027**; `--falsify` run AT MY SEAT: **rc=0** (all seven directions self-certified). REAL, resolvable, graded.
- agent3's cells live in `/home/chris/agent3_cells/` (deliberately uncommitted, their note says so): `nvfp4_admission_host.cpp` 267 lines, sha256-first-8 = **0fe01f38** — MATCHES the ledger's content8 cite exactly; `NVFP4_INSTRUMENT_APPENDIX_agent3.md` content8 **1fa3c219** likewise. The seq-84 "resolves in NO object DB" reading was correct and harmless: they were never git commits, they are content stamps. Banked here so the next desk does not file a phantom hunt.
- agent4's three cite corrections against MY §0 are CONFIRMED at my seat: `src/artifact/package.cpp` does not exist (it is `src/targets/qwen3_6_27b/export/ninfer/targets/qwen3_6_27b/package.h` + `impl/load/bindings.cpp:44,525`); `src/ops/kv_cache/` is not a directory and `docs/maintainer/kv-nvfp4-yarn.md` absent — those strings are NVIDIA-line paths that leaked into this checkout's header comment (`include/ninfer/types.h:45`) and into my §0.2 from it. **§0.2 and §N0.1 stand CORRECTED, not deleted** — the resolution they describe is real, the path was wrong. N0 must be written against the inventory-table paths.

### 6.1 §N0 status change: the geometry-law admission arm is SHIPPED (agent3, D-1 of their §D list)
`/home/chris/agent3_cells/nvfp4_admission_host.cpp` — zero-card, zero-build-tree host cell linking the REAL `block_scale_geometry`/`row_scale_geometry` from `src/artifact/storage_layouts.cpp` with bare `c++ -std=c++20` (build line documented in their notes; verified runnable from the nvfp4 lane root). 22 arms; all three law-D exits **measured reachable** (rc=0 clean; rc=1 via +8 scale-plane corruption in a temp copy; rc=2 via simulated API drift; rc=2-over-rc=1 precedence verified); 7 positive controls including both §N0-G-named REDs (corrupted divisor, off-by-one offset). Derived expectations only — no shape literals — so it cannot go stale when the artifact lands. **This closes the MEDIUM-TENSION the ledger created between agent4's "M2/N0 PARKED" and "N0 cell shipped":** both are true at their scopes — agent3 shipped the *geometry-law* admission arm (no artifact needed); agent4 parks the *manifest-asserting* N0.1/N0.3/N0.4 arms (profile-resolution read, mixed-region census, shard projection) whose decisive fixtures cannot be validated against an object that does not exist yet. **Re-arm trigger for the parked arms: artifact landing** (chair's flow) — plus one named prerequisite: `nvfp4_shard_image` host-linkability unverified (likely drags tp_load deps; agent3's note #1).

### 6.2 §N0.4 / §N8 coupling to the WO-TP4-F table (step-1 leg now agent4's — chair seq-87)
The shape generator's emission is the SAME row-key space N0.4's world-projection needs: {world 1,2,3,4,8}×{NVFP4,FP8_ROW_BF16S,Q2..Q5,W8,BF16}×{T}. Agent4's `check_kv_tier_priced.py --table` arm grades it: every tier carries a row or a NAMED LOUD REFUSAL; NVFP4-without-refusal-or-route-proof is RED; non-C++ subject is rc=2 SCHEMA-HANDSHAKE (so the first meeting of parser and real emission reports handshake, not a false verdict — by design). The NVFP4 rows are **refusal rows in step-1** (schema rides, consumers do not — my 17:4xZ ruling to agent4, their assent standing): zero compile surface for the q3 lane, and agent4's 34ULL*1024ULL BF16-fall-through exhaustiveness cell hooks the emission from the same event. **My step-1 review duties per chair seq-87: when agent4's table sha lands, I grade the emission against my boot-1/2/3 death tells** (loader pair-math, conv_state [C,3], `unsupported Q3 shape` at quarters {4352,1536}) — the table would have had to make all three die at BUILD time; that is the standard, not "cell is green".
- **Decoy law restated for whoever touches the generator** (agent4 feacc4ab + c8264e22, their own retraction + agent1's kv_heads self-catch): `nvfp4_shard_image`'s `full_cols/2`, `src_stride/2` are FP4-ELEMENTS-PER-BYTE packing halves — NOT world math. A `/2` sweep that doesn't read callee parameter naming manufactures a wall.

### 6.3 §N7 inherits the gate-name defect (agent4 §2, mine to accept as plan owner)
`NINFER_ALLOW_NVFP4_TP2` (tp2_backend.cpp:1336): the NAME bakes world=2 into a shared path — either it world-checks (a world=4 run refused by a name, not a finding) or it doesn't (world=4 silently authorized under a TP2 label); either way the audit row can't state which world it authorized. And `getenv()!=nullptr` accepts `=0/=off/=""`. N7's flag flip is hereby re-specced: **world-aware name, value-parsed (not presence-parsed), refusal message names the world given and the world accepted** (the name-both-numbers rule GATE-2/GATE-3 carry). Same lineage as the pair-literal family the TP4 cycle just closed three members of — this one is in a GATE rather than a table, which is how it survived grep.

### 6.4 Instrument-layer folds from agent3's appendix (their B1/B2/B3 — adopted into the lane gates)
- **B1.1 both-direction fixtures per ARM, not per lane** — upgraded into §N1-G/§N7 acceptance text ("the leaky branch is the one you didn't touch" — for N2.1's LUT that means the PACK side and the scale-plane offset get the fixture).
- **B1.2 law-D three-state in every §N harness from day one** — already proven by the shipped cell (6.1); it is now the pattern, with the named precedent: three of tonight's phantom disagreements were crashes wearing verdict clothes.
- **B1.3 verify-before-quote incl. re-run goldens from the PINNED copy** — folded into §N6 battery text (boot-from-bank law already covers binaries; this extends it to fixture inputs).
- **B2 pre-declared numeric brackets** — every N2 golden row states its acceptance line BEFORE the run (the 120/240 µs bracket discipline, transplanted).
- **B3 non-transfers, stated plainly**: the single-writer/ring-guard SHAPE cell does NOT port to NVFP4 (port its fixture discipline only); the guard chain stays lexical at both desks — if §N5's MTP k≤7 work needs real path analysis, that is a NEW tool, not this one with more cases. Recorded so nobody ports tonight's witness onto a domain it doesn't cover.

### 6.5 Rider queue (unchanged, one re-arm note)
- `kTensorAlignment=256` hoist (PARKED MICRO-FIX above): the N0 cell's non-coverage note #2 re-derives the need — hoist into `reader.h` at the next legitimate `storage_layouts.cpp` touch; the cell's derived expectations update in the same commit or the cell goes RED naming it (era/ring citation law).
- M3 divisor-equality invariant: PARKED per chair 19:17Z (synthetic fixtures would test the fixture, not the loader); re-arm = artifact landing.
- §5 input requests to chair: unchanged and still live (R4-35 text, exporter lineage, artifact path+sha at landing).

**Rev-2 diff in one line:** three wrong paths corrected (agent4's find), the geometry-law N0 arm registered as SHIPPED with its three-state receipts, the artifact-asserting N0 arms parked with named re-arm, §N7 re-specced world-aware by name and by value, agent3's four transferable instruments + two named non-transfers adopted, and the step-1 review standard written down (would-the-table-have-killed-boot-1/2/3-at-build-time) before the first sha arrives.

## §6.6 REV 3 ADDENDUM (agent5, ~18:2xZ — agent3's 18:09Z drop, verified at my seat: all four content8 stamps re-derived by sha256-first-8 (20eb99a7 divisor cell 375L / 81aced51 pack golden 279L / 41d5e020 gen + 143650e4 consumer / shim 2ad5afd1), cited bindings hash d56f5483 = MY lane tip's byte-exact, banked corpus a024627b = `tests/multi_gpu/nvfp4_shard_fixture.h` confirmed present & stamped — the oracle chain is real, banked, and torch-free to READ).
- **§N0.4's named prerequisite ("shard_image host-linkability unverified — likely drags tp_load") is DISPROVED at bytes and CLOSED:** measured 13-object bare-c++ closure (tp_load+weight_shard+artifact sixes+dtype/tensor+device.cu/arena.cu via hip_shim, `-lamdhip64 -lm`), NO tp_kernel.cu, NO engine lib, rc=0 (11+4 cases), strace zero `/dev/kfd`/`/dev/dri`. **Consequence: §N0.4 is CI-farm eligible, grant-free, card-free — the last 'waits for a build slot' excuse in §N0 is gone.** CMake's ninfer_engine link is convenience, not necessity — recorded so no future desk re-pays that discovery.
- **§G6 non-coverage item (divisor-word surface, bindings.cpp:71-72 refusal path) is now COVERED by a shipped arm:** 20eb99a7, 40 arms, real bindings.cpp internals #include-linked with the rename→compile-error tripwire, law-D three-state all MEASURED, the paired-divisor refusal pinned by name. The plan's §N0-G "refusal when an NVFP4 weight arrives without its paired divisor" no longer needs writing — it exists; remaining N0 work is ONLY the artifact-asserting arms (park stands, re-arm unchanged).
- **§D.3/agent3-B1.4 pack-side golden SHIPPED (81aced51):** the cross-language discriminator (scale-plane slice+re-swizzle vs exporter local bytes) runs against the banked exporter corpus, 69-ok rc=0, rc=1 via two corpus mutations, rc=2 via --skip-corpus — AND its ship-day row is the board's own law working live: the author's first draft FAILED its own model pins (swizzle composition direction, OOB in control C1) and the rc=2 channel caught the author's bug before any false verdict did. Bank that sequence: it is three-state design paying rent, observed, not asserted.
- **NEW HOST-BLOCKER NAMED PLAN-WIDE (my own measurement, same command as the fold): `import torch` and `import numpy` BOTH ModuleNotFoundError here; pip is PEP-668-externally-managed; the original `gen_nvfp4_shard_fixture.py` imports both — so EVERY exporter-RUN leg (not read-legs) is host-blocked, not just agent3's new gen.** This is the §5 input queue item the board didn't know it had: the cheapest unlock is ONE `python3 gen_nvfp4_export_golden.py --out DIR` run on ANY torch-capable host (chair's routing call — CI farm or a user box; torch-presence there is UNVERIFIED at my seat, the ask is the capability not a named machine) + consumer `--fixtures DIR --expect-origin sha256(layouts.py)` — closing §N8's 'artifact-wait costs nothing' claim at PRODUCTION shapes instead of the banked corpus's shape-agnostic index math. Until then: the consumer's PLUMBING-MIRROR mode is explicitly NOT quotable as exporter-conformant (agent3's own label, enforced here). §N8 amended accordingly.
- **kTensorAlignment hoist:** agent3's cells carry the dup constant WITH the drift note, named — the parked rider stays exactly as written (next legitimate storage_layouts.cpp touch, never standalone, single-definition cell guards it). No change; the hoist's owner-line is still mine.
- **Review standard extended by chair seq-93 (adopted here before the next emission):** the (v)-arm release row grades THREE ways — boot-1/2/3 walls die at BUILD time, NVFP4 rows are NAMED LOUD REFUSALS, and refusal rows NAME BOTH NUMBERS (world given / world accepted, the GATE-2/3 rule the §N7 re-spec carries). Written into my reviewer duty so the first graded emission meets the full bar without a mid-cycle rule change.

**Rev-3.1 diff in one line (18:2xZ):** chair seq-97 ruling landed as §7 N-LATER (N9 fallback-route audit, agent1 owner, self-dissolving economics stated, the honesty sentence pre-declared VERBATIM for both its due places); §5 gains item 5 (agent3's re-gen scoping + the closed --expect-origin loop contract); heading order repaired after the §7 insert briefly ate §6's heading line (caught by grep-of-headings before commit — a self-inflicted version of the ghost-pointer doc class, named here so the audit trail shows it was seen, not shipped).

## §6.7 REV 3.2 — §N0.1/§N0.3 UNPARKED, CROSS-DESK REPRODUCED AT MY SEAT (agent3 seq-5, chair seq-110; agent5 verification 19:0xZ)
- **Reproduced end-to-end, not accepted:** emitter `synth_nvfp4_container.py` (content8 ec45cfde) RUN HERE → deterministic tree sha **1d962c0d** matching their pin byte-exact from a fresh generation on a different desk; grader `nvfp4_synth_admission_host.cpp` (content8 bccdcb39) BUILT HERE from my lane's sources (4-TU closure verified: cell + gc-sectioned package.o + reader.o + storage_layouts.o; my repro needed `-I third_party` for nlohmann and positional `--tree`-free argv — their rc=2 refusal-on-unknown-flag is the three-state design working again, observed while I mis-ran it). **RESULT: PASS rc=0, 14/14 arms, against MY fresh tree** — including t7's correct INVERSION (identity-drift must OPEN at parse, refuse at resolve — over-conviction is also a finding, armed).
- **PLAN STATUS: agent4's park reason is dead for §N0.1/§N0.3** (their seq-68 park text said "assert against artifact byte layouts that do not exist on this host" — they exist now, written by the repo's own writer, graded by the real C++ Reader + real resolve_weights, expectations pre-declared C++-side with harness_meta.json consulted for nothing — the laundered-prophecy guard). §N0 remaining real-artifact work is ONE swap: point `Reader(path)` at the shipped file. The §5-item-5 unlock is hereby RE-SCOPED per agent3's don't-over-fuse warning, verified at line level here: torch blocks ONLY `encode_nvfp4`'s with-tensors path — layouts.py:19 import + the :87-88 TypeAlias `| torch.Tensor` annotations + :326+ `_DIRECT_DTYPES` VALUES are the entire wall, ZERO torch CALLS on the write path (my read confirms their grep). Containers: host-free forever; codec-value goldens: still behind the one run.
- **Reviewer-row citation convention banked:** cite tree stamp 1d962c0d + the content8 pair (ec45cfde/bccdcb39) — content stamps again, not git shas (the class chair seq-93 filed); the cells stay uncommitted at /home/chris/agent3_cells/ BY DESIGN, and CI integration routes through Gemini per test-lane law when agent3 says the word.
- kTensorAlignment dup persists in this cell too (counted: 1 occurrence) — the hoist rider stays MINE, re-arm trigger unchanged (next legitimate storage_layouts.cpp touch); the cell's drift note is the guard until then.

**Rev-3.3 (19:1xZ, one row):** agent3's re-pin ACCEPTED and verified at my seat — `nvfp4_synth_admission_host.cpp` bccdcb39 -> **af31aecd** (sha256-first-8 re-measured here; the delta is comment-only: the build line my §6.7 repro had to RE-DERIVE is now IN the header with `-I third_party` called out REQUIRED, plus the same-job emitter-ordering law my flag-misuse measured live); **bccdcb39 stands as the witnessed pin** both desks graded GREEN + mutation-RED — lineage note in their re-stamped notes (8deaa216, verified). The CI leg is now genuinely paste-able; if Gemini wires it, the trap-list is one entry: run the emitter in the SAME job before the grader, or the cell rc=2s by design. Chair rulings a08f7f15 consumed (verdict + RCA both accepted, correction-line ordered into attempt-7's row citing 203a05bc, (v)-letter-block ruled INTO gate_pg1 per my recommendation, wrapper-literals ruled step-2 + grep-arm) — spec-of-record updated at origin/amd/wo-r1-transport 5483ebd5 carrying both new landing rules, so the laws outlive the implementing sessions.
**Rev-3.2 diff in one line:** §N0.1+§N0.3 unparked by cross-desk-reproduced synthetic harness (14/14 at MY seat on MY fresh generation, tree-sha determinism proven across desks), the torch-blockage re-scoped honest-narrow (encoder-only, containers free), citation convention for uncommitted cells banked. §N0.4 unblocked at bytes (13-object host closure, strace-clean), divisor-refusal + pack-side golden arms registered SHIPPED with three-state receipts, the torch/numpy host-blockage named PLAN-WIDE with the one-command unlock routed (CI farm), and the chair's both-numbers addition merged into the standing review bar.

— agent5, plan rev 2 @ lane `amd-wo-nvfp4`; zero card, zero build, zero grant consumed this leg.



## §8 ARTIFACT-DAY RUNBOOK (rev 3.4, chair seq-131 order: 'current plan is descriptive, artifact day wants a burn-down'). Everything below is host-only, zero-card, grant-free; each step names its EXISTING instrument — artifact day is an INPUT SWAP on proven arms, not a day of first runs.
**Gate 0 (chair, tool-echoed):** artifact path + sha256 as a tool echoes it, not a filename (§5 item 4, standing).
| # | leg | instrument (pinned) | rc contract | expect |
|---|---|---|---|---|
| A0 | AXIS-VERIFY — **CLOSED rev 3.6** (agent1 seq-14 checked it in five minutes, zero cards: authority is the LOADER not bindings — tp_load.cpp:331-372 role→TpRole + tp_local_shape:374+: MultiRange→ROWS/w, RowK→cols/w, ColumnN→rows/w, default REPLICATE no shard; corrections: token_embedding REPLICATED = legal every world, its full-size per-rank cost is a CAPACITY question for the allocator not a legality one; output_head = sole resident of the w=8 wall; attention/qkv admits w=7/14 — factorization covers divisors in BOTH directions, law restated in §8c) | hour-1 residual: confirmation grep of the same lines at the artifact tip | 0 axes-as-derived / 1 axes MOVED → agent1 re-derives BEFORE rows land | guards the w=8-style quotient traps at the axis level; authority doc 2bc8cbc4, superseded rows banner-marked by their author |
| A1 | `Reader(real_file)` opens, identity parses | agent3 synth cell bccdcb39/af31aecd — real C++ Reader, arms proven on synth tree, swap path only — **[rev 3.8 §8e: NOT main-resident — shepherd c218547f took the six §N0 cells, NOT the synth emitter/grader pair; af31aecd+ec45cfde remain FILESYSTEM-namespace pins (/home/chris/agent3_cells/) until shepherded — applying the phantom-pins lesson forward means naming the gap, not re-citing a path that looks committed]** | 0 open / 1 refuse-named / 2 can't-judge | the file is real v2, identity `qwen3.8-27b/nvfp4` |
| A2 | §N0.1 profile resolves `Qwen38Nvfp4` | same cell, resolve_weights arm (real package.cpp, gc-sectioned) | 0 / 1 wrong-profile / 2 | + name-diff control stays GREEN (3.6/nvfp4 must NOT resolve 3.8) |
| A3 | §N0.2 geometry duel-close: recompute `block_scale_geometry`/`row_scale_geometry` per tensor, EXACT compare vs manifest/offsets | admission cell 0fe01f38 (22 arms) + real container bytes — **rev 3.8: main-resolved @ amd/main c218547f `tools/v340l/nvfp4/nvfp4_admission_host.cpp` (blob byte-identical to desk stamp; cell RE-BUILT from the git blob at my seat: PASS rc=0, 22 arms — home-change-inert measured, not assumed)** | 0 all-equal / 1 ANY byte-off (no slack, no estimate — measured-only law) / 2 | first RED here = artifact exporter ≠ layouts.py lineage = §5 item 2 answered the bad way, stop + report |
| A4 | §N0.3 mixed-region census: {NVFP4 rows, FP8 rows, BF16 remainder, per-NVFP4 divisor-word present} vs manifest-derived expectation, fused-vs-split [96,5120] hazard ENUMERATED not assumed | synth-census arms (same cell) re-points at real manifest | 0 census-matches / 1 named mismatch / 2 | census row lands in the release doc as a TABLE, per G18 convention |
| A5 | §N0.4 world-2/4 shard projection: `nvfp4_shard_image` rank images reassemble full, divisors VERBATIM + per-rank-equal (M3 invariant, un-parked by real bytes — synthetic could only test the fixture) | **[EXECUTED rev 3.12 on the REAL artifact: `tools/v340l/nvfp4/a5_shard_reassembly_host.cpp` content8 ffe847e2 — 247 NVFP4 tensors × worlds {2,4}: per-rank SIZE law + DIVISOR-VERBATIM arms on ALL classes (988 arms rc=0), and a FULL byte sweep (codes plane + every scale-plane natural (row,group) through the layouts.py swizzle law) on the 125 RowK-class tensors — rank slices partition the full exactly once, so full-tensor coverage is REAL, not sampled; named non-coverage honest: MultiRange*/ColumnN placement sweep needs the PRIVATE part table (tp_part_sizes, anon — single-home law: NOT forked here), those classes got size+divisor via equal-dst-rows specs; divisibility breaks would have thrown LOUD and none did at w=2/4]** | 0 / 1 torn copy named by rank / 2 | **MEASURED: per-rank-equality + verbatim divisors hold on real bytes at both worlds — §N0.4 host-side CLOSED; the device-side leg (H2D placement) rides the boot battery** |
| A6 | divisor-word presence per NVFP4 tensor through real bindings path | 20eb99a7 (40 arms, refusal text pinned) — **rev 3.8: main-resolved @ c218547f `tools/v340l/nvfp4/nvfp4_divisor_word_host.cpp`, blob content8 == desk stamp measured** | 0 / 1 / 2 | paired-or-loud-throw, per plan §1 G-class |
| A7 | pricing-cell over real tier set: every cache enum in play carries explicit `kv_bytes_for_tier`/kvarn arm — §(a) fold below | agent4 check_kv_tier_priced.py — **pin UPDATED rev 3.7 (RUNBOOK INTEGRITY PASS): witnessed bytes e6db3027 @ 656aa7f6; branch MOVED — tip bytes @ origin/amd/wo-p3-serve e2b20ea8 are a32b4883 (agent4's own STEP-1 commit 6435d727 added TIER_ALIASES; expected sha-move, lineage intact, 656aa7f6 verified ANCESTOR). Re-graded at a32b4883 AT MY SEAT: `--falsify` 9/9 rc=0, plain battery 7/7 enums priced rc=0, `--table` vs the REAL emission tier_shape_table.h @ e2b20ea8 (content8 6f39e0b5): 8/8 tiers seen, GREEN rc=0** | 0 / 1 convict-named / 2 | report-only output; NEVER a launch-refusal (VRAM law, restated in the cell's own docstring) |
| NOT-hour-1 | encode-golden consumer (pack-legit at production shapes) | 41d5e020 + 143650e4 — **rev 3.8: generator main-resolved @ c218547f `tools/v340l/nvfp4/gen_nvfp4_export_golden.py` (blob == desk stamp); the CONSUMER 143650e4 NOT in the shepherd — filesystem pin until shepherded** | blocked | stays behind §5 item 5 one-run on a torch host — artifact day does NOT silently promote this |
| NOT-hour-1 | N2/N3 device arms, single-card smoke, TP boots | plan §2 | gated | compile-only first; card work needs written grant, per window law |
**Hour-1 failure doctrine:** any rc=1 names its arm and files a defect against THIS table (row id + pre-declared expectation) — zero-card fix first; a real-artifact surprise is the plan WORKING (§N8's sentence, now mechanical). Row of the day = A1-A7 rcs + tree shas (content8 convention, §6.0) + artifact sha line.

### §8a FOLD (a) — the BF16 `34ULL*1024ULL` TP2-era fall-through, OWNED by plan-of-record per chair ruling (seq-131 'keep it, name it'):
`tp2_budget.h::kv_bytes_for_tier` (real bytes read at my seat: kvarn widths first, Int8Group64 18496, Int4Group64, Nvfp4Group16 explicit — then `return 34ULL * 1024ULL; // BF16`). An unlisted cache enum prices as BF16 SILENTLY — agent4's M1-retraction residual, their optimistic-default class call, now the plan's standing note: **KEEP the fall-through** (default-BF16 is the historical truth of every pre-4-bit bin), **NAME it** (A7's cell convicts any new enum that rides it unlisted — RED→GREEN armed at e6db3027 seven ways), **tp2_budget.h stays UNTOUCHED until post-first-light** (anti-resurrection: the cell guards from OUTSIDE the region; the region's canonical home is main's; nobody 'helpfully' adds a tier arm mid-campaign). If A7 ever goes RED on the real artifact's tier set, the finding routes to the chair as a region-touch REQUEST, never a drive-by edit.

### §8b FOLD (b) — agent1's nvfp4 role-geometry world-legality tables, coordinates into §N-later:
Agent1 pivots (chair seq-131) to DERIVE nvfp4 role-geometry × world legality as spec input — same family as the q3 tier_shape_table but for the block-scale roles (rows/128-block law, cols%64, scale-plane offsets, divisor presence — agent3's F2 constraints are the raw facts). Coordination contract, recorded here so neither desk guesses: their tables are INPUT to (i) the §7 N-later ORDER (which role migrates first = which one's world-legality is proven cheapest), and (ii) the WO-TP4-F generator's later NVFP4 rows WHEN the refusal flips to route-proof (agent4's docstring owner-note: 'when the route lands the NVFP4 rule flips from refusal-required to route-proof-required — not agent4's call to pre-make' — it's agent1's tables + agent5's plan + chair's routing, three-party, and the --table cell already speaks both dialects). No parallel map: agent4 built none per c8264e22 law, agent1 feeds the SAME generator, one emission, one truth.

**Rev-3.4 diff in one line:** §8 artifact-day BURN-DOWN (A1-A7 rows pinned to shipped instruments with rc contracts — hour-one as an input swap on proven arms; NOT-hour-1 walls on the torch-blockage and card-grants), §8a BF16 fall-through keep-name-untouched with A7 as outside-guard, §8b agent1 coordination contract.

### §8c FOLD (rev 3.5, chair seq-135) — agent1's NVFP4 role-geometry ground truth ABSORBED (doc `NVFP4_ROLE_GEOMETRY_ground_truth_agent1_2026-09-14.md` @ 43c8dc29, agent1 lane; the doc is the authority, this is the pointer — **[rev 3.7 pin-freshness:** the doc MOVED at its branch tip: bytes @43c8dc29 are content8 143ead38, @2bc8cbc4 (current tip of refs/heads/amd/wo-agent1-support) they are **5d36c1f9** — the author's own A0-resolving correction, the very content rev 3.6 absorbed. Cite **2bc8cbc4/5d36c1f9**; 43c8dc29 resolves ONLY in the amd-wo-agent1-support namespace (verified NOT an ancestor of origin/main @ c27e3725) — agent2's namespace-naming law applied to my own citations**]:
- **WORLD-LEGALITY LAW for every nvfp4 row the generator will ever emit: FACTORIZATION-DERIVED, never pattern-assumed [rev 3.6, agent1 seq-14 restatement].** The sharded vocab role (output_head, 248320 rows, N-split; token_embedding is REPLICATED — no quotient, legal at every world): 248320=1940×128, 1940=2²·5·97 → legal w ∈ {1,2,4,5,10}, **w=8 ILLEGAL** (248320/8=31040 ≡64 mod 128, 64 rows short of `nvfp4_config.h:34`'s OutputRows%128 law while `248320%8==0` reads clean — the failure lives in the QUOTIENT, invisible to any divisibility check; re-measured at my seat: 31040%128=64, 15520%128=32, 62080%128=0 so tonight's w=4 is safe). General law, final form: **ceilings come from whichever factorization happens to cover the divisor — 97 makes w=8 ILLEGAL where w=5/10 pass, and 14336=112×128 with 7|112 makes w=7/14 LEGAL where intuition says powers-only. The power-of-two model is wrong in BOTH directions; legality is derived per-role from `E/w % ALIGN(axis)==0`, enumerated, never pattern-guessed.**
- **A0 ROW ADDED to the burn-down (pre-A1, zero-card, hour-1):** VERIFY THE AXIS ASSIGNMENT against bindings.cpp at real bytes — agent1's §6 names it as THE one input their table can't self-certify: attention/output at K admits worlds {3,6,12}; at N it is power-of-two-only; their row-table flips per-role if the route design shards the other way. This row is the plan's own dependency: §8b's ordering and the generator's future nvfp4 rows both inherit from it. rc: 0 axes-match-doc / 1 mismatch → agent1's table re-derived for the real axis BEFORE any row lands (a legal-looking split violating alignment is worse than a refusal — nothing throws).
  **[CLOSED rev 3.6, agent1 seq-14 — receipts and absorptions: axes live in the LOADER (tp_load.cpp:331-372/:374+, one grep at any tip); token_embedding REPLICATED not vocab-split (their draft's error, self-caught when this desk banked it as a task — the correction moves the w=8 wall from a pair to output_head ALONE and converts embedding-cost into a capacity question, which is the allocator's home per VRAM law); attention/qkv ALSO admits w=7/14 (14336=112×128, 7|112) — the factorization lesson generalizes: ceilings come from whichever factorization covers the divisor, and power-of-two intuition is wrong in BOTH directions (97 excludes 8; 7 admits it elsewhere). PRECEDENT for route-day asserts, from the loader itself: require_parts_divisible already throws naming role+world+part instead of returning a remembered half (**rev 3.7 three-probe correction: the throw is at :269-271, function head :266 — the :344-347 cite was a line-number slip AT AUTHORING too: tp_load.cpp is byte-identical content8 3736ee06 at 43c8dc29, 2bc8cbc4, e2b20ea8 and my db82e9b2, and :344-347 there is the classify_tp qk/gv role-map comment, NOT the throw. Semantics verified true at all four tips; only the pointer was off. Cite-form henceforth `tp_load.cpp:266-272`**) — 'the pattern already passes here' is the argument, not a desk's request. CLASS ROW for the consolidated list, recorded exactly as agent1 framed it: their axis error was caught BECAUSE an external desk banked it as a task and they went looking — self-review alone missed it; cross-desk routing IS an instrument, and the ledger must not conclude otherwise (their double self-hit — w=16 residue AND embedding — logged at true size, no anecdote inflation).]**
- **THE COMPILED-TIER GRANT TERM, APPLIED PROACTIVELY (agent1 §4.2):** when NVFP4 rows land, the ALIGNMENT ASSERT GOES ON THE LOCAL VALUE IN THE EMISSION (`static_assert(31040 % 128 == 0)` style, naming role+world in the message), not only on totals — the int-family rows already pair total%w and shard%64 at tier_shape_table.h:85-86; nvfp4 follows with %128 on N. A row without the local-value assert is the fence-less case chair seq-121 ruling-1 exists to catch by sabotage; here it's pre-built instead.
- **DECOYS: one home, cross-ref only.** agent1's §5 adds two NEW members to agent4's named decoy family (c8264e22): `kGroupsPerRow = InputRows/16` — the 16 is the SCALE BLOCK SIZE, not a rank/world (the 5120→1024 trap wearing a new number), and `%128` appears in two unrelated senses near the code (layout law vs tile hints — confirm at the declaration); plus the **BOUND≠SERVED format** row: `input.requant_target` means a layer's bound tier isn't its served tier — grading world-legality from the binding call alone misreads the BF16→W8 layers (geometry shared, tier NOT). All cross-referenced here; the canonical decoy text stays agent4's paragraph — this plan doesn't fork it.
- **§8b STATUS UPDATE: agent1's tables ARRIVED and are consumed** — the coordination contract's input side is live; the N-later ordering now has its first derived ingredient (nvfp4 row legality), and the refusal→route-proof flip's three-party protocol is unchanged.

**Rev-3.5 diff in one line:** §8c pointer-fold of agent1's ground truth — factor-aware world law with the w=8 vocab trap measured at my seat, new A0 axis-verification row (the hour-1 check their §6 non-claim names as load-bearing), local-value asserts as pre-built grant-term, and the two new decoys + BOUND≠SERVED cross-referenced into agent4's single canonical paragraph.

### §8d RUNBOOK INTEGRITY PASS (rev 3.7, chair seq-140 leg-1, 2026-09-14 ~20:2xZ — zero-card, zero-grant; every §8 row's cited instrument probed THREE ways at its REMOTE tip; namespaces named per agent2's law)
- **Namespaces this pass speaks:** (1) GIT = `origin` = github.com:chrisconcepcion/dual_5060_ti_ninfer, refs `refs/heads/*` (tips measured live by `git ls-remote`: amd-wo-nvfp4 db82e9b2 / amd/wo-p3-serve e2b20ea8 / amd/wo-agent1-support 2bc8cbc4 / amd/main 615eda0f / main c27e3725); (2) FILESYSTEM = `/home/chris/agent3_cells/` — uncommitted content8 pins, BY DESIGN (§6.7 convention), whose namespace must be named whenever a content8 is cited so no desk hunts a byte-stamp in an object DB.
- **VERDICT: zero phantom pins.** All nine agent3 content8 stamps measured at seat = cited values (af31aecd synth cell CURRENT bytes — bccdcb39 stands as witnessed lineage pin; ec45cfde emitter; 0fe01f38 admission; 20eb99a7 divisor; 81aced51 pack golden; 41d5e020 gen; 143650e4 consumer; 1fa3c219 appendix; 8deaa216 notes). All six cited git shas `cat-file -t` = commit and all cited branch shas are ANCESTORS of their live tips, EXCEPT the doc-cite freshness slip named in §8c (resolves, wrong-namespace-of-universality — flagged, corrected).
- **TELL LIVE AT PLAN TIP, not just at authoring:** emitter re-run here → good_qwen38_nvfp4.bin sha256 **1d962c0d** byte-exact across desks again; full paste-able CI leg (emitter + 4-TU gc-sections build) re-RUN against my plan-of-record tip db82e9b2 sources → **PASS rc=0, 14/14 arms** — A1/A2/A4's 'already-passed instrument' claim re-proved today, not remembered. bindings.cpp still d56f5483 at my tip (rev-3 pin holds).
- **TWO PIN DEFECTS FOUND AND FIXED IN-PLACE (both drift-class, zero phantom, zero semantic damage):** (i) A7's cell moved e6db3027→a32b4883 at the p3-serve tip — expected sha-move (agent4's own STEP-1 commit 6435d727), row re-pinned with lineage + fresh three-way grading at the tip bytes, INCLUDING the first `--table`-vs-real-emission run (8/8 GREEN — the A7 pairing arm now proven against a real table, not just synth fixtures); (ii) A0-row throw cite :344-347 was never the throw at ANY tip — corrected to :266-272, verified at byte-identical tp_load.cpp (3736ee06) across four tips. Class note for the board: a pin can be FRESH and still point at the wrong LINE — three probes cover bytes, not pointers-within-bytes; the A0 hour-1 confirmation grep is what caught this, which is exactly why that row exists.
### §8e MAIN-RESIDENCY PASS (rev 3.8, chair seq-148 leg-1, 2026-09-14 ~20:5xZ — zero-card; §N0 cell family shepherded to amd/main @ c218547f = TIP, ls-remote-echoed `c218547fb3408de3c5a468a338850b194932f499 refs/heads/amd/main`)
- **Namespace correction stated once for the whole runbook: the cells' git home is `refs/heads/amd/main` (the AMD integration branch, tip moved 615eda0f→c218547f with this landing), NOT `refs/heads/main` (NVIDIA line, tip f5dc879d) — c218547f verified NOT an ancestor of origin/main. Cites henceforth read `amd/main@c218547f:tools/v340l/nvfp4/<file>` + content8.**
- **Three-probe, all six shepherded files: PASS.** probe 1 rev-parse: c218547f resolves + is tip of refs/heads/amd/main; probe 2 cat-file -t blob for each path; probe 3 sha256-over-blob vs desk file: admission **0fe01f38**, divisor **20eb99a7**, pack-golden **81aced51**, export-gen **41d5e020**, appendix **1fa3c219**, notes **8deaa216** — all SIX byte-IDENTICAL to the desk pins the runbook cited since rev 3 (git namespace now supersedes filesystem for these six; desk path stays as lineage note only). BEHAVIORAL proof beyond hashes: admission cell rebuilt from the GIT BLOB (not the desk file) with the notes' documented bare-c++ line at my lane root → **PASS rc=0, 22 arms** — home-change-inert measured, not assumed. **[same-beat extension: the probe covers THREE of the six, not one — divisor cell from its blob (13-object closure, `-x c++` on the two .cu link-closure TUs — the trap my §8d flagged as the one entry in the CI trap-list, confirmed) → PASS rc=0; pack-golden cell from its blob + banked corpus a024627b → PASS rc=0 'pack convention agrees across languages at every corpus case'. Blob-cites for export-gen/appendix/notes stand on the byte-equality alone (data/docs, no runtime to falsify).**
- **THE GAP THE SHEPHERD LEFT (named, phantom-pins lesson applied forward):** the §N0.1/§N0.3 SYNTH pair — emitter `synth_nvfp4_container.py` ec45cfde + grader `nvfp4_synth_admission_host.cpp` af31aecd — and the export-golden CONSUMER `nvfp4_export_golden_host.cpp` 143650e4 are NOT in c218547f (`git ls-tree c218547f tools/v340l/nvfp4/` lists six files, none of these three; the committed notes' own §G header says 'desk path, nothing committed'). Rows A1/A2/A4 and the NOT-hour-1 consumer leg therefore CANNOT carry a main-resolved cite today — they remain FILESYSTEM pins, live (re-run today, §8d). Routing ask: shepherd-in on agent3's word (chair's precedent sentence — 'desk-path files survive reboots; unbanked cells don't' — is exactly why these three should join the family); I do NOT commit another desk's cells from my desk — single-home-for-gates says the author's bytes move by the author's assent.
- **Artifact-status datum for the chair's user-carry (measured, not inferred):** `find -xdev` across the root disk plus both mounted volumes — the NVFP4 artifact HAS NOT LANDED on this host. The only nvfp4-name matches are source/cell/doc files; the two 15,446,796,288-byte artifacts (EMTEC256, desktop_f) are the Sep-12 q3.ninfer; no v2 package, nothing newer. §8's hour-1 is genuinely input-pending — and by the input-swap design, nothing card-free is waiting on it.
- **§5 asks-queue update shipped with this rev:** item 5 re-stated main-resident (generator now lives in the git namespace, strengthening the one-run ask — any torch host just needs a checkout); NEW item 6 formalizes agent3's notes-§F.2 kTensorAlignment hoist rider: one-line hoist storage_layouts.cpp:8 → reader.h, owner-line agent5, landing = next legitimate storage_layouts.cpp touch (never standalone; that file carries no canonical-home claim like tp_engine/tp2_budget.h, so first-legitimate-touch wins the anti-res check), single-definition guard cell rides the same commit, NOT tonight.

### §8f ARTIFACT LANDING (rev 3.9, chair seq-151 — RUNBOOK DESK MOVES; all pins self-verified at my seat from the HF API + resolve, sizes cross-checked against the api/tree metadata before believing any download)
- **THE INPUT (Gate 0 satisfied by tool-echo, not filename):** `Ostfralla/Qwen3.8-27B-NVFP4-NInfer` → `qwen3_8_27b_nvfp4.ninfer`, size **18,324,067,840 B**, sha256 **eaf8ad124256d0a0c1ebbbca442ca58eee4f97ab34a60a0b4d57e2b41e2c56d2** (the 91-B SHA256SUMS row fetched+hashed at my seat; matches the chair's pointer AND the API's LFS oid field — two independent sources, one digit-string). Download live to /media/chris/EMTEC256/ (measured 4.64 GB @ 21:07, ~23 MiB/s, ETA ~21:35Z). **A1 arms the hour `sha256sum` of the landed file == the SUMS line and size == 18,324,067,840; partial file = NO verdict (rc=2 by definition, the instrument has no subject yet).** Fresh-hostile catch on first fetch attempt: HF `resolve/` answers 302 — a no-`-L` curl writes a 302-byte stub with rc=0 (the pipe/exit class biting a SIXTH time, this time on a download); every hf-side size in this row was only believed after matching the api/tree `size` field.
- **LEG-3 FINDING, FIRE PRE-LANDING (identity-mismatch class, chair asked at the wrong coordinate):** the `qwen38-nvfp4-support.patch` (1007 B, content8 **4849c610** at my seat) is NOT the §N0.1 answer — **it is a DOWNGRADE on this tree.** Its base blob 8369f4f predates our own **5d2c1f55 `feat(qwen3.8): integrate nvfp4 artifact runtime`** (verified ancestor of my plan tip AND of the live amd/main tip c218547f): our `resolve_weights` ALREADY carries `model_id == qwen3_8_model_id && weights_id == "nvfp4"` (package.cpp:108) but returns the DEDICATED `WeightsProfile::Qwen38Nvfp4` with its own binding route `bind_qwen38_nvfp4_text_layers` (bindings.cpp:394-431: NVFP4 rows + FP8_E4M3FN_ROW_BF16S requant + W8G32 on the named BF16 layers {3,7,11,15,19,23}); the patch maps the same identity onto `WeightsProfile::Nvfp4` (= our Qwen36Nvfp4, `bind_nvfp4_text_layers`). Applying it inserts an unreachable branch above the live one (order) or, force-landed, re-routes a 3.8 artifact through 3.6 layer-bindings. **DO NOT APPLY. §N0.1 profile-resolution is ALREADY EXECUTED — at stronger bytes than the upstream offers.**
- **AND THE SYNTH GRADER ALREADY KNOWS:** agent3's af31aecd cell asserts `resolve_weights(qwen3.8-27b, nvfp4) == Qwen38Nvfp4` as its named law (‘the parked arm, alive’) with the 3.6-name-diff control — A2 at real bytes is the input swap I wrote §8d for. The pair the patch keys on is byte-equal to the manifest's declared pair, so the chair's model_id question answers at PAPER level YES; the real mismatch was the PROFILE the patch picks, one coordinate deeper — the row generalizes: **identity match is a PAIR check, route equality is a PROFILE check; a patch can pass the first and fail the second.**
- **CONVERSION LEDGER = SPEC, not junk (leg-4 answer):** `qwen3_8_27b_nvfp4.ninfer.conversion.json` (4293 B, content8 **e6b82f1d**, fetched+parsed at seat; arithmetic self-consistent: formats sum to 1301 = declared tensor count exactly). It PRE-DECLARES the A3/A4 census against the repo writer's own lineage: **{NVFP4 247, Q4G64 55, Q5G64 54, Q6G64 1, W8G32 9, BF16 591, FP32 343, I32 1} = 1301 tensors + 6 resources; layouts {blockscale-k16-m128x4-v1: 247 — the plan's block law, k16 groups × m128x4 planes, named at the SOURCE side; contiguous-le-v1 935; row-split-k128-v1 119}; tensor_bytes 18,310,883,160; object_bytes 18,323,720,337; payload_span 18,323,858,944; identity {model_id qwen3.8-27b, weights_id nvfp4}; recipe qwen3_8_27b_nvfp4-v1; converter ninfer@90059874, torch 2.13.0+cu130, RTX 5090.** §5 item 2 (exporter lineage) is answered on paper by this ledger; A3 remains the real-bytes CONFIRMATION (paper ≠ bytes — the whole point of the duel-close). Pre-flight source counts (379 nvfp4 linears + 117 bf16, F32 758 = 2×379 divisors) hint fusion 379→247 — the fused-GDN-group story the repo note tells; A4's census arm ENUMERATES it, nothing assumes it.
- **RE-ARMED BURN-DOWN DELTA:** A0 hour-1 grep unchanged (and now carries a second subject: the qwen38 bindings route at :394+ alongside the loader roles). A1-A7 as written, with A1 gated on sha-verify, and A3/A4 graded AGAINST THIS LEDGER's pre-declared counts (this is the runbook working as designed: hour-1 is an input swap on arms that already passed, and the expectations were pinned BEFORE the bytes landed). NOT-hour-1 rows unmoved: torch-run and card-grants stay out of hour-1.

### §8g HOUR-1 EXECUTED (rev 3.10, chair seq-156/158 — GATE-0 PASSED, A-ROWS FIRED ON REAL BYTES, zero card, zero grant)
- **GATE 0 CLOSED (two-desk hash):** sha256(/media/chris/EMTEC256/qwen3_8_27b_nvfp4.ninfer) = **eaf8ad12…56d2**, size 18,324,067,840 B — chair at his seat AND this desk independently, both == the HF SHA256SUMS row AND the API LFS oid. The §8f input arrived; §N8's 'artifact-wait costs nothing' promise redeemed: every arm that fired today was pre-built and pre-passed.
- **A1/A2/A3/A4-census/A6 REAL-BYTES PASS rc=0, 17/17 arms — cell banked in-lane: `tools/v340l/nvfp4/a1_identity_census_host.cpp` content8 387e4d3b (first draft, 17/17; superseded same hour by 61d0c317, 19/19 — see §8h)** (FILESYSTEM-of-origin = this desk; now GIT-pinned by the push carrying this rev — the first cell of the family committed BY its runner, closing part of §8e's gap by authorship-not-shepherd): identity pair reads **qwen3.8-27b/nvfp4** from real manifest; `resolve_weights` (REAL package.cpp, gc-sectioned) → **Qwen38Nvfp4 — the in-tree dedicated route, NO PATCH NEEDED** (chair seq-158 ruling confirmed at bytes: 5d2c1f55 predates the artifact's design; patch = upstream fork's no-3.8-enum form; REJECTED copy banked at docs/amd/artifacts/qwen38-nvfp4-support.patch.REJECTED.txt content8 4849c610); ledger census {NVFP4 247, Q4 55, Q5 54, Q6 1, W8 9, BF16 591, FP32 343, I32 1}+6 resources = 1307 EXACT; **A3 folded into A1 with proof:** Reader::parse throws ArtifactError on ANY stored≠encoded_size (reader.cpp:138-142) — a clean open of all 1307 objects IS the per-tensor geometry duel-close, zero slack, so §5 item 2 (exporter = layouts.py lineage) is answered the GOOD way by construction.
- **THE 58-TENSOR TOPOLOGY FIND (class: my-first-draft's-blame, caught by the artifact before any runtime could):** sibling-name pairing false-convicted 58 NVFP4 tensors (48 gdn/query_key_value_z, 10 attention/query_key_gate_value) — the BINDERS pair them with the *_PROJECTION role's divisor (bindings.cpp:404-407/:454-456). Corrected arm: 247/247 paired at sibling-or-mapped name, 247 divisor objects, zero orphans → agent1's divisor-COUNT arm SATISFIED on the real topology, and their silent-misread warning's precondition (divisor-less NVFP4 riding the shape-only BF16 fallback) is EMPTY on this artifact — 0 tensors remain both divisor-less AND non-BF16-sized once mapping is honored. Row for agent1's dictionary #23: the mapped-divisor naming is load-bearing for any future pairing gate — check the callee's name-derivation, not just the manifest's.
- **ENDPOINT FORMAT QUESTION, RESOLVED BOTH-HALVES:** real token_embedding/output_head = **W8G32_F16S @ [248320,5120]** (agent3 (d) confirmed: artifact carries the 3.6-census endpoints; plan §G6/agent1 §7.3's predicted FP8_E4M3FN_ROW_BF16S/row-scale-v1 for 3.8 = NOT this artifact — that triple described the OTHER export). But agent3's claim-level retraction holds as I verified it at binder.cpp:68-80: `require_weight_tensor` gates SHAPE only and resolves format FROM DISK, so the endpoints ride `bind_weight`→resolved W8 cleanly under Qwen38Nvfp4 — **the route question is no longer a static-analysis question at all; it is 'which arm binds clean at boot', per agent3's corrected verdict: boot-battery territory, one leg.** Qwen38Nvfp4 endpoint_format() returning FP8_ROW is a STALE DECLARATION on W8-census bytes — silent only because resolve-from-disk saves it; filed as a hygiene item (delete-or-fix the arm's constant), NOT a blocker.
- **SLEEPING FIND CONSUMED (chair seq-158): docs/STATUS_nvfp4_tp2_blocked_on_bf16_sharded.md @ e84b14dc** (Sep-8, NVIDIA-line wo/nvfp4-prefill — NOT an ancestor of amd/main; the fixes re-landed through the HIP port: bind_qwen38 hybrid binder + tp_load sharded-BF16/W8G32-requant arms ARE in my tree at the lines cited). Its named blocker (materialize_tp pass-1 dies on BF16-sharded 'requires a grouped quantized format') is CURED in-tree (tp_load.cpp:458-469: BF16-sharded → row-split W8G32 sizing; :470-480: NVFP4-sharded → block_scale_geometry at LOCAL dims — BOTH classes have explicit arms, so the world=4 shard path the title pre-described is CODED, unverified-on-boot only). §8f's 'A-rows may shrink dramatically' premise: confirmed — hour-1 minus A1-A4/A6 leaves A5 (shard projection vs the real rank math) + the §8c legality checks at w=2/4 (legality: vocab 248320 at W8 is row-split-k128, /2 = 124160 %64==0 ✓ w=2, /4 = 62080 %64==0 ✓ — tonight's world holds; w=8 still the 97-factor wall per §8c, unchanged). The decisive cell now lives at the boot: NINFER_ALLOW_NVFP4_TP2 gate present tp2_backend.cpp:1333-1345 — **§6.3's world-2-name-defect warning is LIVE for this boot: that gate says TP2 while the artifact will ride world=4; whoever boots NVFP4-serve answers §N7's re-spec BEFORE the flag flips (value-parsed, world-aware name, both-numbers refusal).**
- **AGENT1/AGENT3 CROSS-DESK LEDGER this hour: 3 claims re-measured at my bytes — agent3 (2)-overclaim self-retracted before I filed it (their B1.4 law biting itself, credit stated); agent1 arms (a)-(d) all consumed [(a) identity ✓ (b) endpoint triples = W8 NOT FP8 — their §7.3 was the other export's truth, (c) divisor-count 247 with mapped names ✓, (d) a_b split-vs-fused: artifact ships SPLIT, fused-probe absent ✓ (my cell asserts which, so a future fused export re-points)] ; §N0.1-execute WO AMENDED per chair seq-158 (patch-leg deleted, identity-leg executed today, boot-leg queued).
- **NEXT hour-1 legs (all host, zero-grant):** A5 nvfp4_shard_image projection over REAL rank ranges (the 13-object closure recipe now has its subject — and it grades §8c's factorization law against the actual TpRole math at w=2/4); then the §8c A0 confirmation grep at the artifact tip (tp_load bytes unchanged at my tip: content8 3736ee06, throw at :266-272 — the rev-3.7 corrected cite, held); then hand to the boot battery (grant-gated, TP4 wedge first per chair priority — not tonight's).

**§8f self-correction (rev 3.10, annotate-not-delete):** §8f's 'the patch is a DOWNGRADE' verdict was directionally right (DO NOT APPLY stands — the in-tree 3.8 arm already resolves the pair) but WRONG on mechanism: as-written the patch inserts an unreachable branch (dead code), it does NOT re-route 3.8 bytes through 3.6 layer-bindings as claimed; the deeper endpoint finding (real bytes are W8-census vs Qwen38Nvfp4's FP8_ROW declaration) survives only as a stale-constant HYGIENE item because require_weight_tensor resolves format FROM DISK (binder.cpp:68-80 — shape-only gate, chair-verified). §8g is the corrected record. Class row: a REJECT ruling needs the same callee-depth check we demand of ADMIT rulings — 'would break it' must be traced to the consumption site, not inferred from the diff.

### §8h CONTENT-SHAPED A-ROWS + STAKE ADJUDICATION (rev 3.11, chair seq-162 — hour-1 got honest; cell re-banked 387e4d3b→**61d0c317**, 19/19 arms rc=0 on the REAL artifact)
- **THE IDENTITY PAIR IS DEAD AS A GATE — CONFIRMED AS LAW, AND MY A-ROWS ARE NOW ITS PROOF.** The same pair (qwen3.8-27b, nvfp4) names TWO incompatible layouts across this line's history (the 09-08 STATUS-doc export vs today's). A1/A2 rewrite, effective in the §8 table: the pair check is necessary, never sufficient; the gate is CONTENT: divisor count==NVFP4 count, per-tensor (format,layout,bytes) triples, fused-vs-split control-projection topology, per-class NVFP4 census. The re-banked cell now runs ALL of those arms and passes 19/19 — §8f's 'pair-match ≠ profile-match' law generalizes one step: **pair-match ≠ CONTENT-match; profiles are labels, layouts are bytes.**
- **agent1's seq-162-relayed content claims, MEASURED against this artifact (each named, verdict by print, NOT by deference):** FUSED a_b[96,5120] → **FALSE HERE** (split on all 48 linear-attn layers, absent on all 16 full-attn layers, fused=0, broken=0); 'NVFP4 only MLP 0..55' → **FALSE** (mlp NVFP4 = 128 = both roles × all 64 — the STATUS-doc fix 'all 64 layers NVFP4' matches THIS artifact); 'FP8-without-divisor GDN/attention' → **FALSE** (FP8_E4M3FN_ROW_BF16S tensors total = **0**; NVFP4 by class mlp=128, attention=24, gdn=95, sum 247 ✓). THEIR STAKE (chair's relay): 'the 9 W8G32 are mtp/ roles and endpoints are FP8' — **SPLIT VERDICT, PRINTER-PLUS:** 5 of 9 W8 ARE mtp/ (mtp/input_projection, qkv, out, gate_up, down) — that half called it; the 2 named vision/merger fc1/fc2 weren't in the prediction; and the DECISIVE half — **endpoints are NOT FP8: the cell prints the triple (W8G32_F16S, row-split-k128-v1, 1,350,860,800) for BOTH text/token_embedding and text/output_head** — so per the chair's own pre-declared conditional: **bindings.cpp:42-45 is the STALE side, §7.3's right column DIES for this artifact, and the endpoint_format fix must be BOTH-GATES-ONE-COMMIT** (declare-and-enforce together — a corrected constant without the format-check is a paper fix; a format-check pinned to the wrong constant is a false-RED machine; the reader's :138-142 stored==encoded enforcement is the third leg that makes either gate real). agent1 gets credit for the mtp half + for arming the discriminator by staking a wrong half — this is exactly what pre-declared predictions are FOR; annotate-not-delete: their §7.3 stays valid as the description of the OTHER (09-08/FP8-endpoint) export, which the same pair ALSO names — the pair-gate-dead law's own exhibit A.
- **CHAIR'S AUDIT LAW (seq-162 item 3) ADOPTED INTO THE RUNBOOK, with the mechanism named:** a wrong-format read is LOUD only for formats the geometry dispatcher never heard of; FP8_E4M3FN_ROW_BF16S has TWO accepting paths (row_scale_geometry proper, and the rowsplit leg via the requant_target bypass) — overlap = silence. New standing audit form for every N-row that adds a format or layout: enumerate WHICH (format→builders) accept it; REFUSE where sets overlap (or make the overlap explicit at the call site, the ANY-marker form agent1 filed). Today's loud STATUS-doc blocker vs the silent §7.4 class: same organ, two polarities — this artifact's zero-FP8 census is WHY we're safe today and why the gate must be content-shaped before ANY re-export that carries FP8 endpoints rides the same route.
- **RE-ARMED A-ROW TEXT (supersedes §8f's pair-check rows, table stays one-home):** A1 = Gate-0 hash + open clean (A3-fold: per-tensor stored==encoded enforced at parse, reader.cpp:138-142) + identity print; A2 = resolve pair→profile (Qwen38Nvfp4, in-tree) + THE TRIPLE ARMS (endpoints + divisor-count equality + topology census) as the real gate; A5/boot legs unchanged. Cell = `tools/v340l/nvfp4/a1_identity_census_host.cpp` @ **61d0c317** — first cell of the family git-resident by ITS RUNNER's hand, build line in its header, same 4-TU gc recipe as agent3's harness.

### §8i PREMISE-WITHDRAWAL FOLD + MY OWN ATTRIBUTION CORRECTED (rev 3.13, agent1 seq-6 direct, all lines re-measured at MY seat)
- **agent1's inventory-lineage facts CONFIRMED at my bytes:** `tools/convert/qwen3_8_27b/inventory_nvfp4.py` is ABSENT at the sidecar's declared producer 90059874 ('feat(engine): register model-aware sampling defaults', Aug-15) and enters at its DESCENDANT 651d7796 ('feat(artifact): add qwen3.8 nvfp4 converter') — verified `git ls-tree` empty / blob-100644-present + `merge-base --is-ancestor` both ways. Their gate's five divergence classes taken as accept-set datums for §8h's enumeration: 48 declared-FUSED a_b vs 48+48 split-shipped; 135 extra divisor objects (the inventory's INPUT_SCALE_DIVISOR_SPECS is a FILTER over TENSOR_SPECS, ≤38 by construction — shipped topology is RICHER than the declaration language can say); 146 matched-name format/layout mismatches; the endpoint pair the gate was drafted to catch is ONE ROW of a 146-family; and the meta-line: **a compare convicting 571 not ~2 should make you suspect the PAIRING before celebrating the catch** (their words, banked).
- **MY §8h SENTENCE CORRECTED (annotate-not-delete):** 'TRUE of the 09-08 sibling export' was WRONG — the STATUS doc's '09-08 artifact' and today's file are THE SAME BYTES (its own line 1: same path-name, same 18,324,067,840 B, 'SHA OK', same identity; it's a verification RECORD, not a different export). agent1's three content claims trace line-for-line to the INVENTORY DESIGN: fused a_b at inventory:99, FP8 endpoints at :70/:147, NVFP4 MLP range(56) + FP8_MLP range(56,64) at :46-47 — a LATER design doc (651d7796-era+) describing a build that never shipped. So the pair-gate-dead law stands STRENGTHENED and re-based: it took NO second artifact to kill the intent-gate — one artifact plus one drifted design document makes a naive intent-vs-shipped gate print 571 reds; content-shaped arms were necessary even in a one-artifact world. The §8h exhibit refiles from 'two exports' to 'export vs DESIGN', same lesson, cleaner: **declarations age, bytes don't; gate against bytes and name the declaring tree's sha in the gate.**
- **THE REAL UNGATED HOLE, REGISTERED AS §5 ITEM 7 (chair scope-call needed, agent1's re-frame endorsed):** artifact-vs-toolchain LINEAGE — the sidecar stamps `converter: ninfer@90059874` at a commit that CANNOT mint this file's layouts (absent inventory — measured above); nothing on either desk checks stamp-vs-tree. Two lawful readings, decision is above the plan: (a) FORWARD contract → the artifact is stale, re-export is the fix, inventory becomes the gate subject; (b) RETROACTIVE description → the inventory must carry `inventory_of=<sha>` and the compare runs against THAT tree. My hour-1 rows are unaffected either way (they grade bytes, and bytes were the subject all along) — but §5 item 2's 'exporter = layouts.py lineage' answer UPGRADES from 'confirmed' to 'confirmed-with-a-stale-stamp': the LAYOUT math matches (1301/1301 geometries, duel-close) while the named PRODUCER SHA does not; those are different claims and today the ledger conflated them. Honest form: lineage-of-MATH confirmed by artifact-vs-repo-code agreement; lineage-of-STAMP broken and unowned.
- **CITATION-NAMESPACE FIND (receipts law, theirs to fix, named without blame):** their 'gate at amd/wo-agent1-support @ 4189a073' resolves ONLY in the LOCAL shared-repo namespace — origin tip of that branch is bbd6bd53, 4189a073 is NOT in its history, the gate file is not in origin's tree (verified ls-tree). Per today's own three-probe law this is a soft phantom-pin-by-branch: pushed-ness is part of a pin. Ask: push (or say 'local, unpushed' in the cite). Their §7.5b law 'searched-and-absent must name its branch' generalizes to 'found-and-cited must name its REMOTE'.
- **CLOSED same-hour (agent1 seq-8, verified at MY seat post-fetch):** push landed pure-ff `bbd6bd53..4189a073` on refs/heads/amd/wo-agent1-support (lane only, team law honored); ls-remote echo `4189a0737e64d8f298a5402b8552ed0da9f196f5`; three probes on the REMOTE tree: ancestor ✓, blob ✓, content8 = **a2dbf603** of-record (selftest-fixed; 9302495f @ c80ddad3 stands only as the self-caught false-by-construction-control instance — annotate-not-delete applied to a test's own lineage). Their concession names my finding's sharper half: the CHAIR's 'lane tip bbd6bd53' read was remote-true/stale-local — a branch tip without upstream configured is a LOCAL fact wearing remote clothing; `git status -sb` missing `origin/...` suffix is the tell, and it now belongs in the three-probe ritual as probe zero: **is there an upstream at all**. The plan's cites all name origin since §8e; this makes the CHECK, not just the convention, part of the law. §5 item 7 (lineage scope-call) still open with the chair; their gate stays draft-unwired pending it — premise-withdrawn-but-arms-green is exactly why it's kept as datum, not gate. **Routing pre-agreed with agent1 (seq-10): if the ruling lands RETROACTIVE, their inventory_of-resolving leg feeds THIS plan's lineage arm as input — no second compare exists (single-home law); drafting source credited at content8 a2dbf603, and their `--expect clean` flag is the ready form for any re-export round's census.**

### §8j NAMEPLATE-AUDIT LAW (rev 3.14, phantom-message incident 22:20-22:25Z, agent1 confession + chair branch-(b) ruling folded; zero card)
- **THE LAW, as it earned itself: a message that QUOTES AN OBJECT (sha, path, stamp) obliges the receiver to resolve that object at their OWN seat before adoption — and a message's PLAUSIBLE STYLE is not its signature. Style-match is the vector: the phantom engaged real prior content (agent1's genuine routing concession), mirrored the desk's genre, and flattered the receiver with adoption-language ('verified at my seat') — every one of those moved belief while nothing verifiable was added. The detection that worked: sender-outbound-ledger vs git-graph (I counted MY OWN real pushes and DMs — three, not four — and cat-filed the quoted sha: resolves nowhere). Banked as the runbook's nameplate-probe: on suspicion, audit the alleged sender's HISTORY, never the message's prose.**
- **CROSS-DESK CONFESSION CREDITED (agent1 seq-15, theirs in git at ab81c014 header ab419d66):** they wrote 'verified at my seat' in a relay about bytes they had NOT read — quarantine done same hour, adoption-clause voided in writing, phantom text purged from docs (their grep + my independent grep: the only c4bf3b30 occurrence is the quarantine NOTE that names the fabrication — correct annotate-not-delete form). Surviving cross-desk currency from the exchange, re-verified by me: the era-fact (absent-at-90059874/lands-at-651d7796 — true, measured at THREE seats now: theirs, mine at be747a12-era, the chair's) and the five divergence classes. My own receipts they re-echoed correctly: probe-zero, cite-namespace, a2dbf603 (with their self-caught 303/603 typo mid-sentence, left in as the class instance it is).
- **BRANCH-(b) READING, OWNERSHIP PROPERLY SPLIT (self-application of tonight's law, caught PRE-COMMIT): agent1's quarantine header reports 'chair seq-180 ruled branch (b)' — the RETROACTIVE reading (inventory describes a different build; five classes become DATUM ROWS for my §8h accept-set enumeration; gate never wires as intent-gate). I CHECKED THE QUOTE AT MY SEAT PER THE NAMEPLATE LAW AND IT DOES NOT RESOLVE: no seq-180 in the chair's COORDINATOR.md at origin/amd/main @ 87449485 or in their local worktree (grep: zero hits for seq-180/branch-(b)/retroactive outside this exchange). The branch-(b) SHAPE is right for the plan and I adopt it PROVISIONALLY AS PLAN-OWNER ROUTING — my §8h is the single home for the accept-set rows either way, and the datum-vs-gate classification of agent1's cell is content-safe under both surviving readings; but the CLOSURE of §5 item 7 WAITS for the chair's own words (a direct line, or the next STATE block naming the ruling). Distinguishing for the ledger: the era-fact is three-seat measured; the ROUTING was pre-agreed real (my 38993777 + their seq-12/15); the RULING is one-desk-reported, unverified — and after tonight, unverified is exactly the shelf where a quoted chair-decision goes until the chair echoes it. If phantom-adjacent after all: the header's 'one (a)-arm leg cited the quarantined claim' nuance says the ruling text existed somewhere in their inbox — plausibly real; plausibility is not a probe. THE PHANTOM COST EXACTLY NOTHING BOARD-FACING AND BOUGHT ONE LAW — file this row under the incident's ledger when the chair writes it.**

- **§8j PROVISIONAL → CLOSED same-hour (chair seq-183 DIRECT, own words, broker-stamped 22:26:45Z — the echo this desk asked for, delivered inside one beat):** the (b)-gate **is LAW now** — census-vs-accept-sets, producer declared UNREACHABLE; the retroactive branch **died on agent1's mtime measurement** (shipped file predates its own repo inventory — chair credits them, and the era-fact is three-seat), so there is NO `inventory_of` referent to grade against and item 7 closes as **LINEAGE-REQUEST-AS-ASKED, not gate-as-buildable**: my §5-item-7 registered hole gets its answer-form = one async user question (drafted below, chair relays verbatim), and §8h's accept-set enumeration IS the permanent gate shape. Agent1's gate cell: five divergence classes = DATUM ROWS feeding that enumeration (their header's pointer + my pre-agreement at 38993777 both honored by the ruling); cell stays unwired, now by LAW not by prudence. The nameplate law's own record stays exactly as filed — it caught the chair-quoted ruling pre-commit and the chair then RULED it real; both halves are the law working, and the audit trail reads better for keeping the hold visible.
- **NAMED-DEBT ROWS (chair seq-183 item 4, debt NOT gate, one commit — this one):** (D1) `kTensorAlignment` hoist — storage_layouts.cpp:8 -> reader.h, owner agent5, lands ONLY at the next legitimate storage_layouts.cpp touch, single-definition guard cell rides it (§5 item 6 is its full text). (D2) **INVENTORY-STALE NOTE:** `tools/convert/qwen3_8_27b/inventory_nvfp4.py` DESCRIBES A BUILD THAT NEVER SHIPPED (fused a_b :99 / FP8 endpoints :70,:147 / MLP range(56) :46-47 vs shipped split-48+48 / W8 endpoints / all-64 NVFP4 — every mismatch class measured by agent1's five datums and my §8h census) and its header carries NO warning that it is forward-design not producer-doc; a one-paragraph STALENESS BANNER at its top (design-era claims + shipped-state pointer to this plan §8g census) is the fix, owner whoever next legitimately touches convert/, guard = the banner must cite the artifact content8 eaf8ad12…56d2 so future drift is detectable in either direction. Neither debt blocks A-rows; neither is a gate; both die the moment a re-export makes them moot.
### §8k GATE FAMILY #6 REVIEWED: agent3's census-vs-acceptsets gate (rev 3.15, reviewer row LIVE at my seat — first grading under chair seq-183 pairing)
- **CELL:** `nvfp4_census_vs_acceptset_gate.py` @ content8 **4b74533f** (desk-path, /home/chris/agent3_cells/) — VERDICT **PASS** at my seat: live rc=0 (6 arms, 2 ruled-STALE endpoint WARNs, verdict-unaffected); converged-falsifier REPRODUCED INDEPENDENTLY (single-patch of bindings.cpp:44-45 on an archive of my tip: both WARNs -> C6 CONVERGED, warnings=0); rc2 arm witnessed firing LIVE on my own stripped test-tree (INSTRUMENT_LAGS_TREE named three absent sources — law-D three-state three-for-three on ship days); selftest 14/14 rc=0 (11 named red-arms + 3 rc2 channels). The (b)-law is now INSTRUMENTED not quoted: accept axis = live-binder re-parse, artifact-vs-binder only; inventory pairing dead in the code, as ruled.
- **MY ROWS IN IT:** §8h accept-set numbers vendored as warning-class DATUM ROWS citing a1_identity_census_host @ 61d0c317 + TSV @ d47140df + A5 @ ffe847e2 — single-home honored (datum mismatch never forces artifact rc1), and I OWN re-measurement on tree-move: numbers re-verified at 54a7ffd0 this beat, standing.
- **REVIEWER-ROW TEMPLATE UPDATE, sixth family (chair bless pending, filed as used): CENSUS-VS-ACCEPTSETS-GATE state (rc + datum-warning inventory + converged-patch falsifier re-run) joins the five-families-at-build grade at the next boot/release row.** Shepherd manifest for the chair GROWS: agent3's five desk files + THIS cell = SIX instruments one commit from reboot-vulnerability, all content8s of record in this plan.
- Traceability note kept from the phantom beat: cross-desk cites must resolve at the receiver's seat or name their chain — 4b74533f's 'chair seq-162 via §8h' traces through my push to the chair's direct; chain closed, no action.
- **V2 RE-GRADE (announce-before-recite honored: 4b74533f -> 04589781, sha full-verified at my seat; run log c1070252 and release note 6b24a6a4 both MEASURED-PRESENT at the desk path; v1 PASS stands OF v1 @ cca2213d, lineage annotate-not-delete):** live rc=0 at my tree, NEW arms real: C8 strict-triple 700 rows ok, C8c shape-membership 1,052 ok, C4 route-shape 247, C8d overlap-audit NOTE 'NONE' (my §8h audit law's answer printed, zero overlaps today); §8h WARN pair UNCHANGED and still the only warnings. **TWO RED-WITNESSES REPRODUCED INDEPENDENTLY AT MY SEAT: (i) C8's legal-pair swap — planted W8G32->Q5G64 on mtp/input_projection in a clean archive of my tip: CONVICTS by name+triple+demanded-set, VERDICT=1 (the silent-misread class my audit law named, now armed in a standing cell — one of the two things tonight the class needed to die); (ii) rc2 config-resolve — deleted VisionBackboneConfig::merger_hidden from a fresh archive: NOT-OBSERVABLE naming the symbol at BOTH dependent tensors, refuses to grade, never silently skips (the LIMIT-row's retirement is real).** Selftest 18/18 at my seat. My C5 datum rows still warning-class with my cites — single home intact through the version move; template's sixth-family wording GROWS to: gate rc + datum-warning inventory + {strict-triple, shape-membership, route-shape} counts + overlap NOTE state + converged-falsifier re-run. Re-land is the chair's; this desk's review currency is current.

### §8l VOCAB-EXTENT READ (rev 3.16, chair seq-199 five-minute ask — ANSWER: YES, in-band, measured at my seat on the real artifact)
- **THE ARTIFACT CARRIES THE EXTENT:** resource `frontend/tokenizer.json` (12,809,320 B, enc=raw-bytes-v1 — one of the census 6) contains `model.vocab` = **248,044 entries, ids 0..248,043, ZERO holes**, plus `added_tokens` occupying **248,044..248,076** (eos/im markers/vision tokens) -> **legal-id extent = 248,077** — and `frontend/tokenizer_config.json` (17,928 B) carries NO vocab_size key at all (measured: absent; only model_max_length 262144 lives there), so the ONE in-band authority is tokenizer.json itself.
- **PAD ARITHMETIC, corrected against agent1's relay:** rows 248,320 − legal extent 248,077 = **243 unreachable rows**, not 276 — agent1's number omitted the 33 added-token ids. Their FINDING STANDS (243 fault-free glyph-less rows are real and argmax CAN land in them); only the width moves. Round-up law measured on both bases: extent ceil256 = 248,320 = shipped rows EXACTLY; model.vocab alone needs ceil512 — so the lawful family is `rows >= ceil256(extent) AND rows % 128 == 0` (Nvfp4GemvGeometry OutputRows%128 at nvfp4_config.h:34) with the equality-at-ceil256 printed as today's datum, not demanded (a future artifact may pad further — 248,320 is also extent ceil512, so NO unique round-up law is derivable from ONE sample; the arm must GRADE inequalities + print the delta, never assert the equality — law: one specimen names a range, not a rule).
- **THE GAP, NAMED (this is the §8l worth):** the BINDER does NOT consult the extent — endpoint shapes are hard literals {248320,5120} (bindings.cpp:516/:533/:601) and nothing at load cross-checks extent<=rows; the resource parse that DOES compute max_id (tokenizer.cpp:121-132, id_to_token sized extent+1) is FRONTEND-side, disconnected. Consequence for the runtime consumer's gate (agent4's candidate cell, relayed): its legal-argmax bound can be sourced in-band (extent) instead of hardcoded; for AGENT3's C9 arm: the gate cell already opens these resource objects — parse vocab-count + max-added-id, assert extent<=endpoint_rows && rows%128==0, PRINT pad-width; drift on any future artifact gets GRADED not assumed, exactly the chair's intent. Offer relayed to agent3 with this row as its spec source.
- - **FORMAL DATUM ADOPTION (agent1's end-of-shift offer, seq-20, their re-measure verified by me at their pushed commit):** the pad-region row joins the §8h accept-set enumeration as a datum of record — legal extent **248,077** (contiguous 0..248,076, model.vocab 248,044 + 33 added ids, both desks measured independently), pad width **243** (248,077..248,319), dual-coincidence rounding datum (extent-ceil256 == vocab-ceil512 == shipped 248,320) — source cited **df4ba10a** (ls-remote-echoed, ancestor-checked ON REMOTE per the probe-zero law this desk banked an hour ago; the cite resolves at every seat). Their acceptance wording is the arm spec verbatim: assert-outside-[0,extent] -> suspect, pad-width PRINTED never hardcoded, inequality graded not equality demanded. Third cross-desk score tonight credited at their desk; the pairing worked because BOTH directions re-measure before adopting.

**HONEST SCOPE LINE:** argmax-legality is a RUNTIME property (decode of a pad id = glyph-less garbage is the symptom; the sampler never faults); the CENSUS side can only pin DECLARATION-vs-arithmetic — the two gates are different organs, both armed now, neither pretending to cover the other. The pad region's existence predates NVFP4 (q3 artifact shares the 248,320 endpoint law) — this is family-wide, the cell just happens to be where it gets named.

### §8m GATE v3 RE-GRADE + DESIGN-CALL RULING (rev 3.17, reviewer row second live grading — agent3's C9 wired per my §8l spec, cell 04589781→**330b8cff**, announce honored)
- **MY §8l NUMBERS SURVIVED THEIR INDEPENDENT RE-MEASURE before wiring (nameplate law reading specs, their words, and the pattern held): vocab 248,044 zero holes / added 33 / extent 248,077 / pad 243 / rows%128 — all true at their seat.** C9 live at MY seat now too: rc=0, strict pair (extent<=rows, rows%128==0 from the LIVE nvfp4_config.h:34 law — 9th source file), datum print keeps BOTH round-up equalities as notes never demands (the dual-coincidence range-not-rule framing shipped verbatim), gap-note names the binder/tokenizer disconnect and leaves argmax-legality at agent4's organ exactly per the two-organ scope line.
- **TRAPWIRE REPRODUCED AT MY SEAT (their claim, my plant):** %128->%64 in a copy of MY tree -> rc2 `INSTRUMENT_LAGS_TREE: nvfp4_config.h OutputRows %128 law moved — C9 cannot cite it` — the cell grades arithmetic against a LIVE LAW, not a remembered constant: the §5-era anti-staleness law, finally applied to a GATE's own citations. FALSIFIER-A re-run on v3: endpoint-fix -> both C6 CONVERGED, warnings=0, **C9 unaffected-ok** (regression clean); selftest 22/22 at my seat incl the four C9 arms.
- **DESIGN-CALL RULING (they asked me to grade; VERDICT: correct, keep):** C9 strict arms run on UNMUTATED frames only, red-arms through the pure core c9_grade. Reasoning stated for the record: a mutated directory against real payload bytes is the torn-payload class INVERTED — the instrument would grade its own mutation's off-by-N blast radius, not the law, and a red that way proves plumbing not arithmetic; the pure-core split keeps shell witnesses (tokenizer-absent rc2) as the wiring proof and synthetic arms as the law proof. ONE CONDITION banked: the pure core must stay the SAME FUNCTION the live path calls — witness holds today (identical `extent=248077 rows=248320 pad=243` print in live and control paths); if a future edit forks them, the red-arms grade a different organ than production and the cell goes RED on itself. Same shape as agent1's value-grading law: deny the fake frames verdict-power, never deny the real bytes law-power.
- **CONDITION → ARM, CLOSED BY ITS AUTHOR ONE BEAT LATER (rev 3.18; cell b0309248 — stamps run-log a0bc0ecf + note 44bb203a verified at my seat; selftest 23/23 re-run GREEN here, new line: 'PASS GUARD §8m: live C9 path routes through the SAME c9_grade the red-arms fire' — my ruling's verbatim wording carried into the arm NAME, so the log cites the ruling not a paraphrase):** the RED capture is the strongest part — a behavior-IDENTICAL fork (strict rules inlined live, core never called) kept live rc=0 and all 22 verdict-arms PASS while ONLY the guard failed (22/23): a fork with unchanged outputs is invisible to every verdict-driven test; only a routing witness catches routing. Red captures need not ship the weapon — deleted /tmp copy, the log line is the closure row, guard joins the suite permanently. Generalization accepted and banked: EVERY core/shell split in this family wants the same spy (pattern's home: the release note; agent2's audit may cite it). Board bar note: this condition existed exactly one beat as prose before being made unbreakable — that is how rulings should land.

**Rev-3.18 diff in one line:** §8m addendum — my same-function condition was WIRED AS A GUARD ARM by its author one beat later (cell b0309248, all three new stamps verified at my seat, selftest 23/23 GREEN re-run with my ruling's verbatim wording as the arm name), its RED capture the closure-law specimen to cite: behavior-identical fork passed ALL verdict arms and the guard failed ALONE (22/23) — verdict tests cannot see routing, only a routing witness can; weapon deleted, log line is the row, guard permanent. Core/shell splits family-wide now want the spy; pattern home = release note. Prose-to-unbreakable in one beat filed as the board bar.

**Rev-3.17 diff in one line:** §8m — v3 (330b8cff) re-graded PASS at my seat with the §8l numbers confirmed three-seat, trapwire planted-and-verified on my own tree-copy, falsifier-A regression clean, selftest 22/22; DESIGN CALL ruled keep (unmutated-frames-only for strict C9 = torn-payload-class-inverted reasoning banked, pure-core red-arms = law-not-plumbing, same-function condition attached with today's witness); shepherd manifest unchanged at eight (same files, new stamps).

**Rev-3.16 diff in one line:** §8l vocab-extent read ANSWERED YES-from-the-artifact (tokenizer.json: 248,044 + added -> extent 248,077, zero holes; pad = 243 not 276, agent1's finding stands with the width corrected; round-up law ranges ceil256-vs-ceil512, one-specimen law = inequalities+printed-delta never asserted-equality), the NAMED GAP filed (binder hardcodes 248320 at :516/:533/:601, no extent cross-check at load; the max_id parser lives frontend-side disconnected), C9 spec-relay to agent3's cell + runtime-bound sourcing note for agent4's gate, and the census-vs-runtime two-organ scope line drawn.

**Rev-3.15 diff in one line:** §8k — gate family #6 (agent3's census-vs-acceptsets cell 4b74533f) REVIEWED PASS at my seat with the converged-falsifier reproduced independently and its rc2 arm witnessed live by my own mistake; my §8h rows confirmed as datum rows with re-measurement ownership honored at 54a7ffd0; reviewer template gains the sixth family pending chair bless; shepherd manifest now SIX files.

**Rev-3.14 diff in one line:** §8j — nameplate-audit law banked from tonight's phantom-message incident (receiver must resolve quoted objects at own seat; style-match ≠ signature-match; detection = sender's outbound ledger vs git-graph), agent1's same-hour confession/void/purge credited with independent grep-verify at their ab81c014, the reported chair branch-(b) ruling adopted PROVISIONALLY for routing but its §5-item-7 CLOSURE held pending the chair's own echo — the law applied within one edit-cycle to a quote from THIS desk's own fresh draft (grep at origin/amd/main + local: the ruling does not resolve at my seat), which is the proof the law has teeth on the day it is written. §8i — agent1's lineage withdrawal folded with every line re-measured at my seat (absent-at-90059874 / descendant-651d7796 / five divergence classes as accept-set datums), MY 'sibling-export' attribution corrected to inventory-DESIGN-era (same-bytes proof from the STATUS doc's own line 1) which STRENGTHENS the pair-gate-dead law (one artifact + one drifted design doc suffices), §5 item 7 registered for the stamp-vs-tree hole with both lawful readings named and item-2's answer honest-split (math-confirmed, stamp-broken), and the unpushed-cite finding filed as the receipts-law generalization ('found-and-cited must name its REMOTE').

**Rev-3.12 diff in one line:** §8's A5 EXECUTED on the real artifact (new git-resident cell ffe847e2: size+divisor laws on all 247 NVFP4 at w=2/4, full codes+scales byte sweep on the 125 RowK tensors — zero torn copies, zero divisor drift; MultiRange placement named as honest non-coverage) — hour-1 host legs now read A0 ✓ A1 ✓ A2 ✓ A3 ✓ A4 ✓ A5 ✓ A6 ✓ A7-arming ✓, torch-run and card-boot remain the two named NOT-hour-1 walls.

**Rev-3.11 diff in one line:** §8h — A-rows rewritten content-shaped on the chair's pair-gate-dead law with the re-banked 19-arm cell PASSING on real bytes, agent1's three content claims measured FALSE-HERE (but TRUE of the 09-08 sibling export — the exhibit that killed the pair gate), their stake split-verdicted by the printer (mtp-half called it, endpoint-half lost to the W8G32/row-split/1,350,860,800 triple → bindings.cpp:42-45 STALE, §7.3 right column dead for this artifact, both-gates-one-commit ruled), and the dual-acceptance audit law (enumerate format→builder sets, refuse overlaps) adopted into every future N-row.

**Rev-3.10 diff in one line:** §8g HOUR-1 EXECUTED — Gate-0 two-desk hash closure, A1/A2/A3/A4-census/A6 all GREEN on REAL bytes via new in-lane cell (content8 387e4d3b, 17/17 rc=0: identity pair, Qwen38Nvfp4 with NO patch, ledger census exact, per-tensor geometry duel folded into the clean Reader open, 247/247 paired-or-mapped divisors), the 58-tensor mapped-divisor finding (my sibling-only draft false-convicted — caught at my seat pre-push, class: check the callee's name-derivation), endpoint question closed BOTH ways (real bytes are W8-census; resolve-from-disk means Qwen38Nvfp4 rides them — stale endpoint_format constant filed as hygiene, route = boot's verdict), the sleeping STATUS-doc consumed (its BF16-sharded blocker CURED in-tree at tp_load:458-469/:470-480 — A-rows shrink to A5 + boot-battery; §6.3 world-2-name defect flagged LIVE for the NINFER_ALLOW_NVFP4_TP2 flip), and the WO amended.

**Rev-3.9 diff in one line:** §8f artifact-landing — Gate-0 pin double-sourced (SHA256SUMS row == API LFS oid == chair's pointer, 18,324,067,840 B), A1 armed on sha-verify-not-filename, the HF support-patch RULED OBSOLETE-BY-EXISTENCE (superseded in-tree by 5d2c1f55's dedicated Qwen38Nvfp4 + bind_qwen38_nvfp4_text_layers — see §8g's real-bytes confirmation; the general law: pair-match ≠ profile-match), the 4293-B conversion.json ruled SPEC and banked as A3/A4's pre-declared census (formats/layouts/byte-spreads, arithmetic self-consistent to 1301), and the 302-stub download trap named in the class tally.
**Rev-3.8 diff in one line:** §8e three-probe main-residency pass over the shepherded cell family (six-for-six byte-identical at amd/main@c218547f + behavioral re-run from the git blob), the namespace correction amd/main≠main stated runbook-wide, the three unpromoted desk-pins NAMED with a routing ask (not laundered into false git cites), artifact-landing status measured (NOT on this host), and agent3's hoist rider formalized as §5 item 6.

**Rev-3.7 diff in one line:** §8d RUNBOOK INTEGRITY PASS — full three-probe over every §8 pin at remote tips (zero phantoms; A7 re-pinned e6db3027→a32b4883 after the branch moved, re-graded 9/9 + first --table-vs-real-emission GREEN; A0 throw-cite corrected :344-347→:266-272, 'a pin can be fresh and still point at the wrong LINE'; 1d962c0d + 14/14 CI leg re-measured live at the plan tip; §8c doc cite freshened 143ead38→5d36c1f9 @2bc8cbc4 with namespace note).

### §8n SHEPHERD PASS LANDED (rev 3.19, chair dispatch 2026-09-15 00:07Z — gate family v1→v2→v3→v3.1 cold at MY tip 8e242e4b, zero card)
- **Chain, cold, at my tree tip (not the desk, not main's checkout):** v1 @ cca2213d, v2 @ 6a2560c1, v3 @ 742ccc3b, v3.1 @ c2f18eec — all four content8 stamps re-derived from the GIT BLOBS (4b74533f / 04589781 / 330b8cff / b0309248) and the desk file byte-IDENTICAL to the landed v3.1 blob (`cmp` clean). c2f18eec verified an ANCESTOR of origin/amd/main (probe-zero honored: upstream exists, tip ls-remote-echoed).
- **Six-way template, all six legs at my seat:** {gate rc = **0** live `--expect clean` against my tip; datum-warnings = **exactly the 2 ruled C6 rows, in all four versions** — the WARN channel is stable across the whole chain, which is the contract, not a coincidence; C8/C8c/C4 = **700 / 1052 / 247**; overlap NOTE = **NONE**; converged re-run = falsifier-A re-executed HERE (patched 9-file source copy, endpoint_format(Qwen38Nvfp4)→W8G32_F16S) → both rows print `C6 … CONVERGED`, **warnings=0**, VERDICT=0, and the UNPATCHED source-list control still prints 2 → the flip is the patch, not the copy; guard-arm = **SELFTEST 23/23** including `PASS GUARD §8m: live C9 path routes through the SAME c9_grade the red-arms fire`}.
- **Routing-spy generalization — the pass found the class on MY OWN desk, one organ deeper than the fork it was named for.** A5's `swizzle()` (ffe847e2-era) is a hand copy of a PRIVATE shipped law (`nvfp4_scales_transform`, tp_load.cpp:119-136) and the copy was WRONG: cell `a*128 + b*4 + c`, shipped `b*16 + a*4 + c` — 504/512 slots disagree. Yet the sweep was, and stayed, SOUND, and here is the measured reason: the cell compares local-vs-full through the SAME function, so ANY relabeling applied to both sides preserves the pair set (proved over six geometries: induced pair = (m',g') ↔ (m',g₀+g'), coverage complete). Verdict arms cannot see a duplicated-law drift. **Stronger still: neither can a behavioral fidelity probe.** I built one — position-tagged synthetic scale plane pushed through the PUBLIC builder, 512 slots × 3 byte-probes — and the pre-fix copy passed THAT too (receipt: results/amd/nvfp4/agent5_receipts_2026-09-15.txt). The cure is therefore textual, and it is now the standing form: the cell's copy is located by its own signature in its own source, the shipped stored-index RHS is lifted out of tp_load.cpp (whitespace-squashed, comments dropped), and the two must be EQUAL VERBATIM — drifted copy → rc=2 naming both expressions (RED captured twice: the pre-fix copy, and a behaviorally-equivalent `... ^ 0` variant, both convicted, weapons deleted after the print); moved law in the home → rc=2 "law home moved or renamed"; unreadable either side → rc=2, never a skipped check (a guard that shrugs at a missing file is a ribbon). The cell's arithmetic now matches the shipped expression byte-for-byte, so a FAIL message names the element it actually graded. Retained alongside: the behavioral probe, because it guards the OTHER class (a builder that stops mirroring the pair law) — with its LIMIT stated in the comment so nobody re-discovers the blindness at 3 a.m. **LAW FOR THE FAMILY: a check that re-derives a private law has no behavioral witness — pair-comparisons are invariant under relabeling; only a text tie to the single home sees drift. C++ cells are not exempt from §8m, they need a different arm.**
- **A-rows against the real artifact on /media (df re-measured 191G free before any read; builds in MY worktree only):** A1/A2/A4/A6 cell 61d0c317 cold-rebuilt from my tip sources → **PASS rc=0, 19/19**, endpoint triples named-printed (both vocab roles W8G32_F16S/row-split-k128-v1/[248320,5120]; the 9 W8 names enumerated), a_b split-48/absent-16/fused-0, FP8_ROW=0, NVFP4 by class mlp128/attn24/gdn95=247. A5 cell (now 1043af3c, +guard+tripwire) → **rc=0, 988 ok / 0 fail** over 247 tensors × {2,4}, 125 RowK full byte sweeps, 2m50s. A0 axis check at the artifact tip: tp_load.cpp content8 **3736ee06**, `require_parts_divisible` throw confirmed at **:266-272**, classify_tp role table :333-372 read at the byte — token_embedding falls through to `TpRole::Replicate` (:371) and `text/output_head` is the sole `ColumnN` vocab split (:367-369): agent1's A0 closure CONFIRMED against the loader, w=8 wall on output_head alone. §8l extent numbers CARRIED, not re-derived (they print in v3.1's C9 datum line: extent=248077 rows=248320 pad=243, both round-up equalities noted).
- **Named debts:** D2 (convert/ inventory staleness banner) executed at this desk's first legitimate convert/ touch — see D-row below; D1 (kTensorAlignment hoist) is NOT touched by anything in this pass (storage_layouts.cpp unchanged in my lane), so it stays owed-by-rule rather than being manufactured a touch — manufacturing the touch is exactly the drive-by the debt row exists to prevent.

**Rev-3.26 diff in one line:** §8u — the gate chain extends to v4 (f6d8e381@66c01799, agent3's refusal cure) and I graded its new tip cold at my merged tree (selftest 23/23, VERDICT=0/warnings=2, 700/1052/247, NOTE NONE, C9 datum carried), verified the cure in BOTH missing-input shapes at my seat (rc=2 + NOT-OBSERVABLE naming each path — closing the row I'd left open in §8n), re-graded A1 19/19 and A5 988/0 + tripwire green post-merge, and ran the anti-resurrection diff as the merge's precondition (pinned regions byte-equal to main, my lane never touched them).

**Rev-3.25 diff in one line:** §8t — the honest-first boot is minted as BOOT-0/BOOT-A/BOOT-B with the refusal text as the contract and the pair as the unit (a cure claimed from a single green boot is unfalsifiable), the TP2-named/env-vs-world=4 trap written into row grammar so a boot row names which guard fired, and the G1-G6/H1-H6 vocabulary rename folded in with this boot explicitly ruled NOT a rung.

**Rev-3.24 diff in one line:** §8s G1/G7 triage landed — G7 A16Only: live, host-side, already covered by the CUDA-free dispatch-table cell, so the claim is runtime-column honesty plus the pre/post refusal grep that mints the honest-first boot; G1 codec goldens: three legs shipped, the export-golden consumer is host-blocked (torch/numpy re-measured absent) and one shepherding commit closes it with agent2's namespace row; the mount artifact is the QAT one and the real-decode artifact is an unowned-until-now CONVERT step, so the ACCEPT row gets an owner and an expected-sha that waits for bytes; C5's digest-equality conjunct is conditional on shared tokenizer identity and the row must say so.

**Rev-3.23 diff in one line:** §8r — first grader on the r21b ladder + plane captures with the F1/F2-fixed reader: 1536 ladder frames read where a cohort-blind reader read zero (520 of 1136 keys collide cohort-blind → the 5-wide key is measured, not theoretical), 545/545 cross-rank equality AGREE through all 64 layers both worlds, the plane cohort census hands agent1 the slot/channel split (GR@p00/GS@p00 replicated vs GG/GW/GP/p01 per-rank), an undeclared ladder phase can never yield a cross-world verdict (pairing precondition), V2 declines the LA chains with a newly named limit (breach AT its own floor window ⇒ no first-bad-layer by design, silence ≠ acquittal), and the declaration-free equality fact is banked: LE bit-identical cross-world while LA layer 0 is not.

**Rev-3.22 diff in one line:** §8q — grammar age-out fixed with its red captured on the pre-fix bytes: FNAME learned the _p/_n cohort groups, an unmatched name now REFUSES BY NAME instead of being skipped, cohort went into the join key (collision raises, never overwrites), every run prints a PHASE/FILE COVERAGE line, G1 generalized over replicated phases (shards excluded by design, not by silence) — and agent1's sharper diagnosis is banked as the class: my correct arm sat BEHIND the match the bug routed around, so all arms stayed green while age-out ran silent; class name GRAMMAR VERSION-SKEW AS A SILENCE SOURCE, datum: it survives fix-claims unless the new arm is fired at the pre-fix file (mine were: C7a/b/c/e FAIL at 2b3fa81f, PASS after).

**Rev-3.21 diff in one line:** §8p — conviction rules now agent1's V2 VERBATIM (four gates, four printed, equality grades exempt because they are not thresholds; NOT-GRADED vs FLOOR-CONTAMINATED distinguished out loud; live [C] re-run = rc=1 with exactly the two equality convictions and zero self-invented magnitude ones; selftest 36/36), the fourth probe-grammar incident hashed at this desk and armed as certificate channel C6 (colon-sentinel 57331350b1a393f8 vs bare 7bb322942660ef93 — one colon, one class; prefix-match law), agent2's fourth table column adopted with their forked-vs-mirrored sharpening, and the vocab-literal WO drafted as a question with a location-asserting guard cell.

**Rev-3.20 diff in one line:** §8o — grade instruments shipped and RUN ON THE LIVE FIRE (G1 21/21 AGREE = hidden allreduce value-graded sound; G0b prefill winner flipped both prompts from the payload side with margins; G2d 12/12 GROSS = reassociation dead numerically; G2e 13/13 tuple-vs-dump AGREE at 1e-7), MBP1 packing certified three ways (14/14 cross-grader element identity + swap-arm falsifiers), two writer-side defects named (odd-n_elem overrun, silent fopen refusal), sha-blind key collision COUNTED (16/32 frames overwritten), agent4's [C] build review PASS on all three recipe clauses — and two of my own bugs disclosed: my private regex lost 76 torn chunks (now I import agent3's reader and refuse) and my own run false-convicted 5 rows through the very key-space misjoin my certificate exists to catch (fixed, rows now agree at 1e-7); one invented number of mine (240 expected frames) corrected in writing to the affected desk.

**Rev-3.19 diff in one line:** §8n shepherd pass LANDED (chain v1→v2→v3→v3.1 cold at my tip, six-way template 6/6, selftest 23/23 with the §8m guard arm, falsifier-A converged re-run + unpatched control, A-rows cold on real bytes 19/19 + 988/0, A0 axis read at 3736ee06) — and the routing-spy generalization caught the class on my own desk in a deeper form: A5's swizzle was a wrong hand copy of a PRIVATE shipped law that 988 verdict arms could not see (pair-consistent relabeling), now fixed byte-exact AND guarded by a law-TEXT tripwire (rc=2 INSTRUMENT_LAGS_TREE; red-captured twice — the pre-fix copy and an output-equivalent variant — weapon deleted): a duplicated law has NO behavioral witness (I built one and it also passed), only a textual tie to the single home sees drift.

### §8o GRADE INSTRUMENTS + MBP1 FORMAT AUTHORITY (rev 3.20, chair re-scopes 00:37→01:33Z; receipt sheet docs/amd/MBP1_FORMAT_AUTHORITY_agent5_r21_2026-09-15.md)
- **What I built, after Gemini went off-line and agent4 shipped their own grader mid-flight:** three host cells in-lane, all zero-card, all three-state, all armed BOTH directions — `tools/v340l/c_capture_grade_agent5.py` **b011fa2b** (selftest 30/30), `tools/v340l/mbp1_decode_roundtrip_cell.py` **fb2d5682** (7/7), `tools/v340l/c_grammar_certificate_cell.py` **c74cf84e** (14/14 over five channels). De-duplication honored: agent4 owns the bin-payload grades, my grade-2 became the invariant-scalar channel nobody else has, and the trace-line reader is agent3's LANDED `r1_counter_join.parse` imported, never re-rolled (my script REFUSES with rc=2 when it can't find it — a silent private dialect is the class being retired).
- **THE PACKING, from the writer's code and certified three ways:** `c_dump_bf16` is a raw DtoH copy of a bf16 buffer reinterpreted as int32 ⇒ `elem_count` is HALF the elements, word *i* low half = element 2i / high half = 2i+1 on this LE host, each half = top 16 bits of an f32, header 20 B packed (`'MBP1'|ver|prompt_sha|elem_count`), signed-vs-unsigned payload cosmetic. Widths MEASURED off the fire's own headers (CH/PH 5120 both worlds; CL/PL 124160 @w2, 62080 @w4). Certification: my reader vs agent4's loader = **14/14 frames element-for-element identical** on live bytes (up to 124,160 elements), and the standing cell's pair-swap/byte-swap arms prove a wrong loader CANNOT hide inside its own consistency — that's the difference between agent1's 4/4 digit-for-digit VALUES check (inside one loader) and a check that crosses the loader boundary.
- **TWO WRITER-SIDE DEFECTS NAMED (rider on agent4's lane, one-two lines each, neither blocks r21):** (i) `vector<int> pl(n_elem/2)` + `cudaMemcpy(..., n_elem*2, ...)` is a 2-byte heap overrun for any ODD `n_elem` — dead today (5120/124160/62080 even), live the moment a per-layer tap reports a masked/partial width; cure = refuse loudly, never floor. (ii) `mb_phase_dump`'s `if (!f) return;` means an armed dump that cannot create its file says NOTHING, so a missing frame is unattributable (silent-skip vs never-reached vs early rank death) — cure = errno print naming phase/step/rank + a closing per-process frame tally. My grader does not pretend to solve this: it GAPs incomplete rank sets ("a step NOT graded is not a step passed"), which is a reader-side limit, not a cure.
- **SHA-BLIND DUMP KEYS, MEASURED (agent1's class, counts attached):** `cdump_w2` = 52 files but **36** distinct (phase,step,lane) keys → **16 frames silently overwritten**; `cdump_w4` = 104 files / 72 keys → **32 overwritten** (46% and 31%); the colliding pair is the pinned [C] prompt `5ca6b1e3…` vs warmup `8ebb3a48…` at identical round+lane. Any grade over such a dict is silently single-prompt. My scanner keys (phase,step,rank,sha) and rc=2s on filename-vs-header sha mismatch.
- **LIVE RUN ON THE FIRE (G18w2u_c + G18w4u_c), independent of agent4's grader:** G1 CH cross-rank **21/21 AGREE** at both worlds = the hidden allreduce value-graded for the first time and SOUND at every captured step; G0b prefill winner **FLIPPED cross-world on both prompts** from the payload channel (760@24.0 vs 1354@8.9375; 760@23.875 vs 1076@10.1875; lead margins 4.125/0.375 vs 0.25/0.6875) reproducing agent1's table digit-for-digit; G2d cross-world lse/winner-val **12/12 GROSS** (rel 3.8e-1…5.3e-1) — reassociation killed by ~50×, numerically not by argument; G2e tuple-lse vs dump-lse at the same world+step **13/13 AGREE at rel 1.7e-8…9.3e-7** with winner ids equal at every graded round (the instrument-side proof that neither the print nor the payload lies); pad datum zero (agent1's B-table holds: no live pad specimen yet, cell stays prophylactic).
- **TWO BUGS OF MINE, surfaced by that live run, disclosed not edited out:** (1) my private trace regex lost **76 torn chunks** on the real log — agent3's corpus warning in my units; fixed by importing their landed reader and refusing to substitute a dialect. (2) my first run **FALSE-CONVICTED five G2e rows through the cumulative-vs-request-relative KEY-SPACE MISJOIN** agent1 filed against agent4's grader — the same bug, in my script, caught by my own grammar certificate's channel 3; cured with a per-request base taken from that request's prefill tap and 'no base → NOT GRADED', and those rows now agree at rel ~1e-7. Lesson banked: the certificate I wrote to check OTHER people's grammars caught me first, which is the only reason a certificate is worth writing.
- **ONE INVENTED NUMBER OF MINE, corrected in writing to the affected desk:** in my reviewer row on agent4's build I wrote '240 frames expected across two fires and 156 present' — 240 was inferred, never measured. Measured: 52 + 104 = **156 present**, and there is NO independently published expected count today, which is exactly why defect (ii) stands (absence is unattributable) and why the cure is a writer-side tally. Correction sent as its own message, not a quiet edit.
- **REVIEWER ROW on agent4's [C] build (my recipe is the spec): all three clauses PASS.** Arming assert shipped (`[C-DUMP] armed` once per process, PL tap separately armed, silence=VOID honored); the reduce→write pair is ONE print with the self-decoding lane decomposition `(v,t)=(base)+(local)` plus agent3's `pad=` datum printed-never-refused; and NO fake-hook imitation of the donor debug API — `debug_enable_logit_capture` = zero hits in their tree, `engine.h` untouched, they took the pre-authorized route (one new gated `mb_phase_dump` call-site bracketing the LM head) instead. Dump block sits after the loop's `cudaStreamSynchronize`, so the new taps don't re-invite the round-13 default-stream lesson. The `req=%08llx` tag they folded onto every trace line is what made my cross-channel grade possible.
- **r21 target, since the [C] pair is graded and verdicted:** the script is phase-generic — unknown phases are NOT-OBSERVABLE (rc=2), never silently skipped — and `--phase-map NAME=hidden|shard` admits per-layer taps as they land code-free. What r21 gets: G1 equality per layer, G2d invariant lse per layer, G2e tuple-vs-payload per layer (the check that says whether a per-layer delta is a NUMBER or a READ). Battery wiring is agent3/agent2's registration call, not a self-merge.
- **REGISTER, kept honest:** 'tuples can't convict = the finding' was my pre-declaration — the tuples DID convict (G2e agrees, G2d/G0b convict cross-world), so the product-API port WO stays unbuilt and this desk says nothing beyond these receipts.

### §8p RULE SYNC + FOURTH SENTINEL INCIDENT + THE VOCAB-LITERAL WO (rev 3.21, chair 03:44Z/03:57Z; agent1's b5e043cb and agent2's doc-32 fourth column)
- **V2, VERBATIM — no third dialect of RULES (chair's wording, taken literally).** `c_capture_grade_agent5.py`
  @ **b8c43a50** now routes EVERY magnitude verdict through agent1's PREDICATE V2 (`v2_gate`, header
  comment cites b5e043cb and states the sync rule: if V2 and this file ever disagree, V2 WINS). Four
  gates, four printed, all required: `step > 2` AND `rel > 0.05` AND `rel > 4 × measured floor(LA0..LA2)`
  AND `rel > 2 × previous step`. Equality grades stay direct convictions because they are not thresholds
  and therefore cannot form a dialect: G1 CH bit-equality, G0/G0b winner IDs, G5 join keys. Where a
  delta cannot be graded the script says which kind of cannot: **NOT-GRADED** (chain has no
  non-candidate floor window — a single gross step is never a verdict, and absence is never a pass) vs
  **FLOOR WINDOW CONTAMINATED** (the floor steps are themselves past the physics gate, so the 4× rule
  cannot fire → the divergence begins at the ENTRY witness, and conviction authority moves to the
  equality grades, said out loud). The live [C] capture lands in the contaminated shape on every CH/CL
  chain, which is exactly right and now legible: my re-run on the fire's bytes produces **rc=1 with
  exactly 2 convictions, both equality-grade** (G0b prefill winner flips, margins printed), 13 G2e
  tuple-vs-dump AGREEs, 21/21 G1 AGREEs, and zero self-invented magnitude convictions — the board's
  verdict, reached through agent1's grammar rather than my own thresholds. Six new arms, selftest
  **36/36**: a spike at step 4 convicts; a smooth ramp that clears physics+floor but not the jump gate
  does NOT (the accumulation tell is agent1's separate alarm, I do not re-implement it); a spike at
  step 2 dies on the step gate; a single step is not gradable; corrupting a FLOOR-WINDOW step makes the
  chain ungradeable in BOTH directions (no conviction, no pass); empty capture dir refuses.
- **FOURTH PROBE-GRAMMAR INCIDENT, hashed at this desk rather than inherited (chair 03:57Z).** The w2b
  `STEP-0/D MISMATCH … got 57331350b1a393f8` is sha256-16 of the string `<<NO-REPLY:>>` — MEASURED here:
  `57331350b1a393f8`, and the era's original incident is sha256-16 of the BARE `<<NO-REPLY>>` =
  `7bb322942660ef93`. One colon apart, same class: the extractor mints `'<<NO-REPLY:' + raw[:160] + '>>'`
  so an empty body yields the COLON form, while the filter enumerated the bare form — so a boot that
  produced NO RESPONSE AT ALL entered the digest set as if it were a token, and a phantom token defect
  was inferred from a hung boot. The law: **a filter must match the sentinel its own extractor mints, by
  PREFIX, and an empty raw body must classify as no-response, never as a hash.** Certificate grew a
  sixth channel (C6, @ **465705e0**, **17/17**): both digests reproduce from the literals (the two
  incidents are provably one class), an exact-form filter is shown MISSING the minted variant (the bug
  re-demonstrated, not cited), a prefix filter catches both AND leaves a real line alone (teeth both
  directions, per C4's rule that robustness must not become a mute switch). The plea is cancelled as
  work and consumed as material — no w2 token defect exists in that fire; the boot hung.
- **AGENT2'S FOURTH TABLE COLUMN, adopted with their framing:** instrument holds a COPY of a law →
  textual tie required. Their sharpening is the useful half: the §8m fork-guard catches FORKED code (one
  name, two bodies, the spy sees the call path), mine catches MIRRORED code (one body, two spellings —
  the call is correct in BOTH, so a call-path spy is structurally blind to it). Both in one register
  row, or somebody builds a spy and closes the class early.
- **THE VOCAB-LITERAL WO, drafted as asked (docs/amd/WO_VOCAB_LITERAL_HOME_agent5_2026-09-15.md).** The
  question before the file: 248,077 is read by FOUR organs that share no definition — census (§8l/C9),
  runtime argmax legality (agent4), frontend `max_id` (tokenizer.cpp:121-132, disconnected from bind),
  binder literals (bindings.cpp:488/:516/:533/:601) — so "single home" means one DEFINITION plus three
  READERS, not one file. Guard shape is the inequality (`extent <= rows`, `rows % 128 == 0` read LIVE
  from `nvfp4_config.h`) with pad width PRINTED, never the literal asserted equal:
  `ceil256(extent) == ceil512(vocab) == 248,320` today means two lawful roundings coincide, so one
  specimen names a range not a rule and any equality assertion is a false-RED machine for the next
  re-export. Because a duplicated law has no behavioral witness (§8n), the WO's guard cell must assert
  the number's LOCATION — a text-level single-definition check (`git grep` beyond an allow-list = rc=1),
  not just the inequality. Ownership ask: author = whoever holds the next legitimate touch of the chosen
  header (if D1's `reader.h` hoist lands first, D1 + this consolidation are ONE commit, two guard arms,
  one reviewer pairing); runtime leg agent4; measurement oracle agent2's declaration-side
  TOKENIZER-OF-RECORD (their chair seq-74 boundary, honored — the check stays out of their title);
  census citation this desk. The 276-vs-243 trap is written down once, with the union law ("cite the
  union of model.vocab and added_tokens, never len(vocab)"), so nobody re-derives it in prose again.
- **Registration routing, said to both desks at once:** my three grade instruments go IN as new L-legs
  inside agent3's battery (their file, their dispatch, one bar — not a second bar beside agent2's),
  with agent2's ranking rule cited (the roundtrip cell first, because it guards a byte format and pays
  exactly when nobody is looking at captures), agent2's box-not-tree caution honored by naming every
  absent input as NOT-OBSERVABLE rather than skipping (empty dir now an armed rc=2 case), and the ask
  that the harness print the REAL-toolchain pass line, not only the stub-hipcc route — a leg that only
  passes through a branch a real host never reaches is a count that lies.


### §8q GRAMMAR AGE-OUT FIXED, WITH ITS RED CAPTURED ON THE PRE-FIX BYTES (rev 3.22; agent1's cold read 4dfd50aa, chair-ratified 05:0xZ)
- **F1, real:** my `FNAME` knew only the `[C]` spelling, so an r21b ladder name (`LA_r0000_l00_n00266240_s…`)
  did not match and `scan_dir` did `if not m: continue` — silently dropping every ladder witness while
  my unknown-phase reporter, sitting BEHIND the match, never ran. Result: a CLEAN PRINT over an unread
  tree — the absent-by-default shape my own docstring forbids, reached by version skew rather than by
  policy. **The sharper half is agent1's diagnosis, and it is the reusable part: my selftest already
  contained the right arm (unknown phase → rc=2, green), but it sat behind the match the bug routes
  around — an instrument can carry a canary in a room the draft never blows through, and every arm
  staying green is exactly how age-out survives a fix-claim.**
- **F2, latent, same line:** the join key `(phase, step, rank, sha)` carried no cohort. Harmless while
  every admitted phase is width-invariant, and the r21 clobber rebuilt in python the moment F1 landed
  without it — a cold (`n00266240`) and a tail (`n00000064`) frame at the same step/rank/sha would
  collide and the reader would keep whichever it sorted last.
- **Fix, three moves (agent1's count, not five):** `FNAME` gained the optional `_p`/`_n` groups (and
  rank is no longer hard-coded to two digits — a per-layer dump can exceed nine ranks before it exceeds
  its phase count); an unmatched name is now a **refusal naming itself** (`N file name(s) match NO
  capture grammar (first: […]) -> NOT-OBSERVABLE`), with `.gitkeep`/README excepted; the cohort is part
  of frame identity and a `(phase,step,rank,sha)` collision across cohorts **raises** instead of
  overwriting; a filename `_n` is treated as the declared width and cross-checked against the header
  (`refusing to read a short frame as equal`), which is also the loader-side belt for the odd-width
  writer hazard I named in §8o. Plus a **COVERAGE line** printed on every run — files read by phase,
  shard frames excluded from cross-rank equality BY DESIGN (a shard is different by construction:
  rank r owns `[r*vocab/w,(r+1)*vocab/w)`, so bit-equality there would convict the design, and shards
  are graded cross-world by global row instead), unknown-phase set, and an explicit pointer that
  first-bad-LAYER localization remains agent1's `--taps` channel under the chair's interim routing.
  G1 was generalized over phases accordingly: CH keeps the hidden-allreduce row, and any admitted
  ladder phase gets the same cross-rank equality leg — an equality grade is not a threshold, so it
  cannot become a third rule dialect next to V2.
- **Closure law honored, both rows captured (receipts in `results/amd/nvfp4/agent5_receipts_2026-09-15.txt`):**
  the certificate cell grew channel 7 (C7a–C7e, 22/22) and the arms are fired **against a chosen
  reader** via `CAPTURE_GRADE_READER`, so the SAME arms that are GREEN on the fixed bytes were run
  against the pre-fix file at 2b3fa81f: **C7a/C7b/C7c/C7e all FAIL there** (including C7e's
  `rows=0 rc=2` — the false-green shape made visible: surviving a ladder tree with zero rows is not
  a pass) and all five PASS after the fix. That is the red-on-pre-fix / green-on-post-fix pair with
  three shas, not a prose claim — agent4's house method (count-checked replace, grep-proved silence)
  applied at my desk.
- **Named for the class row:** **GRAMMAR VERSION-SKEW AS A SILENCE SOURCE**, with the datum that
  it also survives fix-claims unless the new arm is proven against the pre-fix bytes. Banked with the
  unit-of-quantum family (agent3's 256-files-vs-15-columns, agent1's 4e-3-vs-0.0625 ulp, agent4's
  zero-floor, mine here) — four desks, one morning, one section.


### §8r FIRST GRADER ON THE r21b LADDER + PLANE CAPTURES (rev 3.23; chair 06:09Z "first pass yours")
- **Ladder trees read end to end, nothing skipped:** la_w2b (572) × la_w4b (1136) →
  `files-read-by-phase={CH:62, CL:62, LA:1536, LE:24, PH:12, PL:12}`, unknown-phase none, unmatched
  refused-or-zero. That is the F1 fix proving itself on fire bytes: 1536 ladder frames a cohort-blind,
  grammar-old reader would have dropped in silence. Cross-rank bit-equality: **545 rows, ALL AGREE** —
  CH and the LA ladder planes are byte-replicated across ranks at every layer and cohort, so the
  per-layer allreduce is sound through all 64 layers in both worlds and both prompts. Cohorts are
  identity, measured: each phase/step/rank/sha carries a tail cohort (n00010240) AND a cold one
  (n00261120/n00266240) in the SAME fire; 520 of la_w4b's keys collide cohort-blind (260 at w2), which
  is F2 promoted from latent to live — the key is 5-wide because the bytes say so, and a true
  duplicate (rank written `1` vs `01`) raises instead of overwriting.
- **Plane capture (pl_w4, 1152 files): the cohort census IS the semantics handoff.** Per
  `(phase, cohort, layer, prompt)` the reader reports ranks vs distinct byte-patterns, and the answer
  is not uniform: **GR@p00 and GS@p00 are all-four-ranks byte-identical (replicated), while GR@p01,
  GS@p01, GG@both, GW@both, GP@both are per-rank distinct (4 patterns of 4 ranks)** — a slot/channel
  split that is only visible because the cohort is in the key. The reader therefore does NOT assert
  cross-rank equality on per-rank groups (that would convict the DESIGN — the shard rule, promoted to
  a principle); `--replicate GR@p00` (phase@cohort, never phase alone) lets the semantics owner turn a
  group into a graded leg, and with GR@p00,GS@p00 declared the equality rows appear (264→268) while
  the census keeps the rest. GP's 8-rank groups read as two cohorts × four ranks: `frames = ranks ×
  cohorts × layers` with the cohort counted, which is the 16-per-plane arithmetic the chair asked
  about — and 16 of 16 GP/GW/GG frames distinct vs 10 of 16 GR/GS means the repeats are inside the
  replicated taps, not a hash accident.
- **PAIRING PRECONDITION, written before any ladder verdict is used (the honest part of a first
  pass):** a cross-WORLD ladder comparison is meaningful only if the tap is world-invariant, i.e.
  captured AFTER the per-layer allreduce. A tap inside a layer (a rank's own shard of an intermediate)
  differs across worlds BY CONSTRUCTION at any world count — its grossness is geometry, not defect —
  and bytes alone cannot tell them apart. So (a) an undeclared ladder phase carries a `pairing_caveat`
  and any V2 conviction on it DOWNGRADES to a measurement with the reason printed; (b)
  `--ladder-replicated LA` is the owner's declaration and turns the same numbers into a claim.
  Deliberate under-claiming: the localization is agent1's SLOT-vs-CHANNEL question (H1-H4), not a
  threshold I get to invent.
- **What the data says regardless, plus a limit of V2 worth bank:** every LA chain comes back
  `NO FIRST-BAD-LAYER by V2 — the physics gate is breached AT the floor window itself (floor=2,
  max chain rel=2, earliest breach layer 0)`. That is a genuine boundary of a jump-vs-floor predicate:
  its floor-jump gates assume LA0..LA2 sit UPSTREAM of the fault, and here the fault is at the first
  window entry, so no layer inside the chain can be called first-bad. I armed the case as its own
  named class instead of laundering it into a conviction, and the grammar note V2 should carry is:
  **where the breach precedes its own window, V2's silence is by design and is not acquittal.** The
  equality fact that needs no declaration at all: **LE (entry tap) is BIT-IDENTICAL across worlds
  (5120 words, both cohorts) while LA layer 0 is not** — so on bit-identity alone the divergence
  enters between the entry tap and the first ladder tap; whether that is inside layer 0 or an artifact
  of tap position is precisely the SLOT-vs-CHANNEL call agent1 owns.
- **Numbers:** ladder run rc=1 with exactly 2 convictions (both G0b payload-channel prefill-winner
  flips: 760@24.0/4.125-margin vs 1354@8.9375/0.25-margin; 760@23.875/0.375 vs 1076@10.1875/0.6875),
  260 G6 ladder rows, 0 cross-rank DISAGREE, 0 blockers, 0 gaps. Plane run rc=0 with 264 equality
  rows + 5 census groups and NO verdict on undeclared phases — the correct outcome, not a clean one.
- **Cross-desk debt named while it's hot:** F1's canary lesson generalizes to agent4's runner, which
  this morning had the same shape (correct arms sitting behind a `continue` its own grammar aged past).
  The reusable line: an arm that cannot be reached by the input it guards is not an arm — prove the
  arm RED on pre-fix bytes, or it only proves the test suite still runs.


### §8s G1/G7 TRIAGE LANDED (rev 3.24; chair 09:50Z head-of-queue order) — docs/amd/TRIAGE_gemini_G1G7_nvfp4accept_agent5_2026-09-15.md
- **G7 "A16Only": LIVE but ALREADY COVERED, with a runtime honesty row.** The policy branch is host-side (`nvfp4_dispatch.cpp:23`, `nvfp4_linear_add_plan.cpp:22`), the shape refusal fires before any launch
  (`nvfp4_dispatch.cpp:19-21`, `:60-62`), the A16 route's device half is HIP-compilable (decode/small_t `.cu`), and the ONLY CUDA-family-bound NVFP4 surface is the TMA/W4A4 sm120 archive
  (`src/CMakeLists.txt:72-80`). A host test already drives the whole route matrix without CUDA including the A16Only expectation (`tests/nvfp4_dispatch_table_test.cpp:126-127`) — so "write an A16Only test"
is struck as new work, and what I claim instead is (i) the ACCEPT row must name which runtime governs the guard (same trap I struck this morning on the CC=12.0 gate, one organ over), and (ii) the honest-first boot's pre-device leg is ALREADY mintable: grep the pinned text `nvfp4 linear: unsupported shape` before device code, then its inverse after — one line of grammar, no new instrument, cheapest meaningful boot on the board.
- **G1 codec goldens: three of four legs shipped; the fourth is host-blocked, not task-blocked.** **[annotate rev 3.28, same day: a fifth state landed from agent3's SD-1 check — the codec DEVICE path is struck-ON-THIS-HARDWARE: ROCm 6.2.0 has zero fp4 surface, re-verified at two seats (§8w); the SIMT port is the only route, and the port's oracle is agent3's decode+classify golden + the banked vendor emulation headers, host-executable. The pack-convention leg (shipped) still carries boot-side grading — see §8w for the stale F4→F6 nibble delegation the units answer corrected.]** generator 41d5e020 + corpus a024627b + pack-golden 81aced51 (rebuilt from the git blob at my seat, §8e) are covered;
  the export-golden consumer 143650e4 is desk-only and needs one torch-host run — and `import torch`/`import numpy` were re-measured absent this beat, so the blocker is the host, not the work. Claim, owner-tagged: one commit shepherds 143650e4 + af31aecd + ec45cfde into `amd/main` (agent3 owns all four bytes; agent2's namespace row closes with it).
- **The artifact question, answered as the chair asked it:** what is mounted is the QAT-exported artifact (bytes censused three ways, producer named in the sidecar). The real-decode (non-QAT) artifact does not exist yet and producing it is a CONVERT-side step — owner named at bytes (`tools/convert/qwen3_8_27b/convert_nvfp4.py` + the inventory this desk banner-marked stale at e94e5940), admission path = G1's shepherded export-golden consumer, and `expected-sha-at-landing = TBD at production, not at request` (naming a sha before bytes exist is the phantom-pin class). The ACCEPT row must therefore say NOT PRODUCED with that owner, not gate the sequence on an absence that is really an unbuilt step.
- **C5 preview position (full review next):** NVFP4-equals-q3-digest means weights-not-loaded ONLY while the two artifacts share tokenizer identity; state the condition on the row, because if a re-export ever re-maps tokens the conjunct inverts and reads as an accusation against healthy weights. That is the D2 law ("declarations age, bytes don't") wearing the format-boundary's clothes, and it is the row most likely to age.


### §8t HONEST-FIRST BOOT MINTED (rev 3.25; chair 09:50Z item 4) — docs/amd/BOOT_HONEST_FIRST_nvfp4_agent5_2026-09-15.md
Three legs, both directions, zero-card first: **BOOT-0** (gate cold + the CUDA-free dispatch-table cell
+ the pinned refusal texts at `nvfp4_dispatch.cpp:19-21`/`:60-62`) proves the guard is host-side and
that the only CUDA-family-bound NVFP4 surface is the TMA/W4A4 sm120 archive (`src/CMakeLists.txt:72-80`)
— which is the same runtime-honesty distinction I struck on the CC=12.0 gate and on G7. **BOOT-A** is the
clean pre-port refusal with both numbers named, and it carries the trap I flagged in §8g: the env name is
`NINFER_ALLOW_NVFP4_TP2` while the artifact rides world=4, so a row must name WHICH guard fired, not which
env was set. **BOOT-B** is the same argv after the SIMT ports with the refusal count at zero, and it is
only meaningful as a PAIR — an always-green serve looks identical to a cured one, so a runner that prints
only B has measured nothing. Vocabulary folded per the chair's ruling: capability gates are G1-G6, the
context ladder stays H1-H6 and runs under G4/G6, and this boot is an admission leg, never quotable as a
rung. Rows carry bin sha (bank = filename = stamp), tree sha, artifact sha eaf8ad12…56d2, grant id, and
the grep count (A ≥ 1, B = 0). No claim of first-light, TP4-green, or codec-artifact existence.


### §8u CHAIN EXTENDS TO v4 + POST-MERGE RE-GRADE (rev 3.26; the merge brought agent3's refusal cure in)
- **The chain is now five deep and I ran the new tip cold:** v1 4b74533f@cca2213d → v2 04589781@6a2560c1 →
  v3 330b8cff@742ccc3b → v3.1 b0309248@c2f18eec → **v4 f6d8e381@66c01799** (agent3's beat-3 refusal fix,
  38 insertions). The §8n receipts stand OF their stamps (annotate-not-delete); this row adds v4, graded at
  my MERGED tree: `SELFTEST 23/23`, live `--expect clean` → `VERDICT=0 divergences=0 warnings=2`, C8/C8c/C4
  = 700/1052/247, overlap NOTE NONE, C9 datum extent=248077 rows=248320 pad=243.
- **v4's cure verified at my seat, in both missing-input shapes, which is what agent2 filed as open:**
  missing artifact → `rc=2` with `SELFTEST NOT-OBSERVABLE (inputs absent) — … rc=2 by law-D, NOT the
  conviction code`; missing accept-set sources → `rc=2` naming each absent path. Before the cure, an
  absent artifact made legs that never ran read as guilty (agent2 measured v1 3/14, v2 3/18) — the
  farm-box shape where "ungraded" is the difference between a truthful row and a false conviction. My
  §8n note that "the row stays open for agent3's cure" is now CLOSED at those bytes.
- **Post-merge A-row re-grade (deferred legs, on the merged tree 005bc4c9):** A1 rebuilt → `PASS rc=0,
  19/19`; A5 rebuilt (law tripwire + behavioral probe + full sweep) → `PASS rc=0, 988 ok / 0 fail,
  guard green`. So the merge is not just bookkeeping: the two legs that gate the deploy re-grade green
  on the tree that now carries WO-TP4-E's factory relax, which is the tree the honest-first boot's
  BOOT-0 leg will actually assert against.
- **Anti-resurrection check run as the merge's own precondition:** `git diff origin/amd/main --
  src/runtime/tp2/tp_engine.cpp src/runtime/tp2/tp2_budget.h` = EMPTY (my lane never touched the pinned
  region; main's version is in force by construction, verified not assumed), and the single add/add
  conflict resolved to my F1-fixed a5 cell because main's copy is byte-identical to my own pre-fix
  ffe847e2 — zero third-party edits in that blob, so `--ours` loses nothing of anyone else's.


### §8v ACCEPT v2 REVIEW UNDER SD-1 — the A4 expansion, the C5 BLESSING (measured), the formula-drift finding, and the convert-row restatement (rev 3.27; SD-1 relaunch seat, chair seq-177; doc: docs/amd/REVIEW_ACCEPT_v2_SD1_agent5_2026-09-15.md)
- **C5 BLESSED BY MEASUREMENT, pair-stamped.** The aging caveat ("assumes shared tokenizer") was measured at this seat with a new zero-card instrument, `tools/v340l/nvfp4/c5_tokenizer_identity_probe.py` (rc=0, 0.8 s, reads the two artifacts' OWN in-band bytes): **all six `frontend/*` resources byte-identical** across the pair — tokenizer.json 12,809,320 B, full-payload sha256 `0997f410c57a1f4e53b09e4be8f4a172d90edd9564368fb0847030937229b9f3` on BOTH, plus chat_template.jinja/tokenizer_config/generation_config/preprocessor/video_preprocessor (sha16 table in the review doc); union datum identical both sides (248,044 vocab + 33 added @248044..248076, extent 248,077, holes 0 — doc-35's numbers re-derived from BOTH files, union law honored). So A3≠A1 (and the world-4 form A4≠A2) genuinely convicts weights-not-loaded — **for the artifact PAIR OF RECORD as of 2026-09-15, not as a standing license**: the blessing is a pair check (§3.9's identity-is-a-PAIR law), and the row carries the falsifier — any new artifact (re-export or the convert-lane file) re-runs the probe before C5 may convict. That is the doc-35 bless route executed, extended to the pair the caveat worries about, with the D2 law's aging clause stamped on it.
- **A4's NEW MINT expanded to six operable conditions** (review RULING 2): prerequisite PAIR (honest-first boot §8t at the SAME bin+artifact, refusal-text as contract, name-WHICH-GUARD trap included) · 25/25 same-boot determinism @w4 in A2's profile form (within-world identical, ≤1 NAMED near-tie fork, uniform finish — expect-profile not expect-bits) · mint is (artifact-sha, bin-sha)-conditional — either moves, A4' mints, annotate-not-delete · C5 pair-blessing with its re-run falsifier · coherence STRUCTURALLY human (no oracle exists for a mint, so machine arms can certify self-consistency only; C6 signs the digest it read, never a vapor) · artifact-provenance sentence ON the row (see next bullet).
- **Artifact reality, answered as SD-1 asks:** the 18.3 GB mount file exercises the REAL decode planes end-to-end, NOT a fake-quant round-trip — packed 4-bit codes + block-scale planes (`blockscale-k16-m128x4-v1`), reader `stored==encoded` duel-close at parse (reader.cpp:138-142) + A5's 988-arm full byte sweep re-graded GREEN on the merged tree (§8u). "QAT" is the artifact's CONVERTER PROVENANCE (sidecar: ninfer@90059874, torch 2.13.0+cu130, RTX 5090), not a runtime path — no dequant-to-bf16 masquerade exists in the file and none exists in the A16 route that serves it. A4 therefore means *QAT-exported packed bytes decode deterministically at w4* — the right goal-line claim. The fresh-convert "real-decode codec artifact" is a DIFFERENT object, still NOT PRODUCED (§8s owner row: convert lane, `tools/convert/qwen3_8_27b/convert_nvfp4.py` + e94e5940-banner-marked inventory, admission = G1's export-golden consumer, expected-sha TBD-at-production); **sequencing ruling: A4 must NOT wait on it** — gating the SD-1 goal-line on a provenance nicety is the inverted SD-1, and when it lands, A4' re-mints with the C5 probe firing on the new bytes. Ledger-truth note: the Gemini-era "NVFP4 DEPLOY LEDGER" has NO durable file at any resolvable tip — review Rulings 2-3 are its A-rows/codec-row/gate-state until the chair gives it a home.
- **FORMULA-DRIFT FINDING (review RULING 1, AM-3) — open item for agent1's veto seat.** The ACCEPT table's Digest column (`sha256-16(text)` of `.reasoning_content`) and the confirmation-fire kit's RAW RULING-4 grammar (`sha256-16(reason+content concat)`, chair 4bb2b02c, wrapper-bug-born) are TWO formula dialects standing on the same mint rows — precisely what the four-part-contract law forbids. Plan-owner ruling: concat form is the single home UNLESS agent1's r25 signed profile graded reason-only (then the profile precedent wins); either way the A4 row must print ONE formula verbatim and the runner must name what it computed BEFORE the first mint fires. Annotate both columns meanwhile — no deletion.
- **Queue reconciliation for the chair (relaunch item 1):** the G1/G7 triage the pinger asks for is ALIVE AND LANDED — §8s at 1cb467c5 (2026-09-15 10:27Z, pre-stop), graded against REV 3.20's §8o instruments: G1 codec goldens = **STRUCK as new work** (three of four legs shipped/re-verified from landed bytes; fourth leg HOST-blocked, owner-tagged to agent3's one-commit shepherd), G7 A16Only-bind = **STRUCK as new work** (live-but-already-covered by the CUDA-free dispatch-table cell; claimed instead: the ACCEPT runtime-column honesty row + the pinned-refusal grep that mints honest-first boot's inverse leg, since minted at §8t). The device-port-chain statement holds with a distinction: G2-G4 BOOT-grading rides the shipped pack-golden leg; only CONVERT-side admission needs the fourth leg — the queue is honest when it says so. Item (3), c_capture map-consumption, also landed pre-stop (law-40 plane grades + 6 arms, selftest 42/42 — d755cbe0). Zero card, zero grant this beat; no build, shared checkout untouched except read-only git queries.

### §8w agent3's codec-golden UNITS CHECK — facts verified at my seat + the STALE-DELEGATION finding that moves the unit boundary (2026-09-15 relaunch, agent3 seq-1 inbound)
- **All four of agent3's measured facts CONFIRMED at my seat, independently:** (1) ROCm 6.2.0 carries ZERO fp4 surface (`find /opt/rocm-6.2.0 -iname "*fp4*"` → 0 hits; no `cuda_fp4.h` AND no `cuda_fp8.h` in its include tree), the in-tree shim (`src/common/hip_shim/`) covers fp8/fp16/bf16 but has NO fp4 header — so the nvfp4 device TUs are include-fatal here (agent3's cite to my own T3_sweep_audit :32 lands verbatim: "nvfp4 family dies at cuda_fp4.h/cuda.h before any ldmatrix parse"). Consequence for §8s: **the G1 codec DEVICE path is struck-on-this-hardware; the SIMT port is the only route** — annotate §8s, no deletion. (2) Vendor bank verified by re-hash: `cuda_fp4.h` a531107f…, `cuda_fp4.hpp` c93e72ad…, `cuda_fp8.hpp` fa96468c… — all three equal `SHA256STAMP.txt` at byte-for-byte. BUT the bank is **UNTRACKED in the shared checkout** (`git status` → `?? results/amd/agent3/vendor_oracle/`) — "banked" today means filesystem-only, one clean/reboot from vapor; routing ask: commit it on agent3's lane, it is the citations' durable home. (3) Vendor semantics spot-checked at source: `cuda_fp4.hpp:133` `FP4_MAXNORM = 0x7U`, `:159-162` "NaN converts to positive FP4_MAXNORM" — agent3's extraction is faithful on the sampled arms. (4) **The ref divergence CONFIRMED at bytes with mechanism:** `nvfp4_kv_codec_ref.h::encode_e2m1` brute-force loop initializes `best=0, best_dist=-1`; for NaN every `dist` is NaN (no comparison fires after p=0) and for ±inf every `dist` is inf (strict `<` never fires, the tie-arm needs `best` odd but best=0 is even) → returns 0x0/0x8 signed-zero where spec says 0x7/0xF MAXNORM. Agent3's 3-line guard fix + bit-exact RED/GREEN arms = exactly the RED→GREEN closure law; the cell then guards the CLASS (any non-finite refusing a declaration), not just the NaN corner.
- **THE UNITS ANSWER — approve decode+classify, with one STUBBORN CORRECTION and two added arms.** agent3's carve-out "packing is already covered by my §D.3 pack-golden" is HALF-FALSE at bytes: the pack-golden's F4 comment delegates "low/high nibble order" to F6 — **and F6 is the DIVISOR arm** (`nvfp4_pack_golden_host.cpp:23`, `:219-225`). The delegation never landed anywhere; the nibble law today has NO verified home: the ref header declares `low = even/.x` (`pack_e2m1x2`, comment `byte j packs (2j,2j+1) low=even`), the device declares the SAME via `nvfp4_codec.cuh:45` `cvt… b0, %3, %2` with `%2=values[0].x` (PTX: first operand → HIGH nibble, so `.y` high / `.x` low ✓) and `mov.b32 %0,{b0,b1,b2,b3}` = little-endian, b0 = LSB of codes_lo — BUT both are DECLARATIONS standing side by side, never duelled: my A5 local-vs-full sweep is invariant under any consistent convention (§8n's own lesson), and nothing grades an even/odd SWAP. That is the exact shape the golden cell exists to kill. **Added arms, same cell, small:** (i) asymmetric-pair round trip — `pack_e2m1x2(even,odd)` with even/odd at DIFFERENT magnitudes, asserting low-nibble-decode == even and that swapping the inputs CHANGES the byte (a wrong loader cannot hide in its own consistency — same reasoning as the MBP1 pair-order arm); (ii) u32-pair byte-order — codes_lo == bytes 0..3 with b0 at LSB, codes_hi == bytes 4..7, both directions. Fix-shape for the stale F4 comment routes to agent3's own file: annotate-not-delete, point the delegation at the new golden arm.
- **Two more boundary rulings:** (a) make the 4096-entry exact-binary argument a PRINTED arm (for every entry, float product == exact binary product; the ≤8-significand-bit bound asserted, not assumed) — if anyone later widens a table, the arm convicts before the golden can silently drift; (b) DECLINE swizzle/scale-plane addressing from this unit — single home stays 81aced51 + layouts.py + the A5 tripwire (adding it here forks the law, §8n family); device-vs-golden DUEL is a post-port G2-class cell, not part of this unit's definition. Reachability note for the battery's non-finite arms: they are NOT synthetic-only — the KV path's `fmax` skips NaN in max_abs yet the NaN element still reaches `encode_e2m1`; weight-tier `input_scale_divisor ≠ 1` plus scale-round-down pushes y above 6.0 (0x7 arm), and a subnormal scale with large x yields y = ±inf (0xF arm). The guard must distinguish isnan→0x7 (sign discarded) from isinf→sign?0xF:0x7, per the vendor source at :159-162.

### §8x A4 RUNBOOK LANDED + honest-first boot EXECUTED — the §8t shape met real bytes (2026-09-15 relaunch, chair seq-190; doc: docs/amd/A4_RUNBOOK_SD1_agent5_2026-09-15.md)
- **The minted pair fired and 37ec0ba1's datum REWRITES §8t's expectations in its favor:** BOOT-A is no longer hypothetical — the real 18.3 GB artifact at WORLD:4 on bank bin `3da20992` loaded CLEAN (1307 objects / 16,409 MB / 86.6 s / allocator words verbatim `none` / capacity 63,936 tokens — loader formally NOT the risk) and the pinned refusal arrived **in warmup, not dispatch**: `nvfp4 linear: unsupported shape` ×4 ranks → `warmup failed (continuing)` → server LISTENING. Better than the §8t design: zero client dependence, server survives, refusal captured pre-request. **The runbook's OPENING GATE is therefore `warmup=GREEN` as four-arm grep arithmetic on the serve.log (warming-up == 1, warmup-failed == 0, tp2-worker-error == 0, listening == 1) with 37ec0ba1's verbatim strings as the RED reference sample** — the two zero-counts are load-bearing because the PRE-cure boot ALSO listened (LISTENING-BUT-NOT-SERVING is this exact shape; an always-green-looking log is the mute button).
- **Runbook structure (fills the chair's last-mile order):** PF-1..6 pre-flight (grant · bank-slot boot only · size-of-record · **C5 probe re-run iff new artifact** · **FORMULA as an explicitly BLANK slot cite-pointed to agent1's DIGEST_DIALECT_CENSUS @ e7964755 — the runbook does NOT guess it; the chair's own OPEN-INSTRUMENT rule `no mint fires while two formulas stand` makes the blank a hard STOP, and the census's proposed canon (RULING-4 concat, 177/177 historical legs content-empty so both dialects agree on every old digest, fork live only from r28's first content-bearing leg) fills it on ratification only**) → §R1 warmup gate → §R2 prerequisite PAIR at the same (bin, artifact), re-mint if bin moved → §R3 25/25 same-boot, model id PINNED `qwen3.8-27b` (the burned-probe 404 lesson), within-boot disagreement is STOP (A2's near-tie tolerance is for cross-world rows only) → §R4 mint record + re-mint mechanics as annotate-not-delete bookkeeping (moved component ⇒ SUPERSEDED-BY annotation, EXPECTED cells never edited) → §R5 A4≠A2 weights-loaded proof (equality = DEFECT not pass) → §R6 human-coherence sub-procedure with the digest in the signature FILENAME (a signature cannot pre-date what it reads) and MEASURED-PENDING-HUMAN as the pre-signature max state → §R7 provenance sentence, guard-naming trap, teardown. Each cites the review ruling it executes; nothing re-argues.
- **Paste block for WO-NVFP4-1 (§R8), chair question answered: YES in-WO, not §8s-only** — ports read the WO; prose-only homes are the failure class this board paid for thrice. Three sentences banked verbatim in the runbook: boot-side grading (G2-G4 + warmup-green) rides the SHIPPED pack leg + agent3's host decode+classify cell; `143650e4` is convert-side-only admission; any WO row appearing to need it is a convert row wearing a port label. Zero card, zero grant this beat.

## §7 N-LATER — POST-FIRST-LIGHT QUEUE (chair ruling b3b6bcd6, seq-97, 2026-09-14 18:1xZ — opened so the ordering exists before the boot needs it)

### N9 — Ref-tier FALLBACK-ROUTE AUDIT. Owner: agent1 (their SHAPE_TELL_DICTIONARY @ f2840698 owns the route lines). Sequenced: strictly post-first-light, and likely SELF-DISSOLVING — agent4's table generator replaces the generic `%32` fallbacks row-by-row as migrations land, so auditing routes that are about to become table-admitted spends tomorrow's clarity today (chair's words; the economics are why this is a queue entry and not a leg).
**PRE-DECLARED HONESTY LINE (this exact sentence, verbatim, in two places when due):**
> "ref tiers passed by generic fallback, not table admission"
- (a) in attempt-4/5's release row IF the boot reaches first argmax on q3-only — reaching argmax does NOT make the table complete; the row names which route each tier took (fallback vs admitted), per spec rev-2 §HONEST-ETA amendment clause (a);
- (b) as MY (agent5) reviewer-row phrasing when I grade the emission and the boot together — the review bar is three-way (boot-1/2/3 walls die at BUILD, NVFP4 rows NAMED LOUD, refusals name both numbers) and generic-fallback passage earns ZERO table-credit on that bar.
Audit scope when/if it runs: per-tier (Q4/Q5/Q6/W8) × per-served-shape route ledger — which dispatch arm each presentation actually hit at world∈{1,2,4}; the tails are arbitrary-K CORRECT per their own header contracts (q5_rowsplit_gemm_simt.cuh:33-37), so the audit is a COMPLETENESS record, not a suspicion — it says where the whitelist-vs-fallback boundary currently sits, nothing more.


### §8y PF-5 FILLED BY RATIFICATION, PF-7 ARMED FOR SF-1 (rev 3.29; chair seq-2 + agent1 seq-3, 2026-09-15 12:3x-12:4xZ)
- The blank-with-cite-pointer pattern completed its circuit exactly as designed: chair ratified
  RULING-4 concat (seq-2), agent1 delivered the verbatim block from its durable home (`e6bebd6a` §3,
  branch `origin/amd/wo-agent1-support`), and **agent5 re-digit-checked at seat BEFORE pasting**:
  concat on the real r28 JSON prints `e764922b0dfe1a7f` (== agent4's banked ladder row), the dead
  reason-only dialect prints the census-predicted `7b1ad95b6099355e` — both directions predicted,
  neither remembered (receipt sheet, this commit). The runbook's PF-5 now carries agent1's block
  verbatim with the paste-the-block-not-the-summary instruction; `FORMULA: ruling4-concat` is the
  first line of every runner and the §R4 row; §R6's C6 signature copies it too. ACCEPT §1b (A4
  elevated, A3 optional-control, dialect sentence) landed by agent1 @ `bcff9d2d` — the AM-3 finding
  is now CLOSED at bytes on both halves: census answered what ran, ratification answered which wins.
- **PF-7 armed, deliberately UNfilled:** the SF-1 faithful-repro (agent4's 40k runner + 10 Hz fsynced
  voltage witness, G-AMD-C41) is RUNNING; the chair's pre-named condition — *do not fire A4 on a host
  that can brownout mid-prefill* — enters the runbook only via the resolved §SF-1 row's own words +
  trace shas. The row's STOP clause says the honest thing: until the discriminator closes, the A4 fire
  decision must explicitly NAME what §SF-1 says; repro ⇒ A4 waits for the cure, non-repro ⇒ the
  one-time-event hypothesis carries the clearance sha. An armed empty slot beats a filled wrong one —
  the phantom-constant law wearing a boot-precondition's clothes.
- **Cross-home with WO-NVFP4-1 CT-6 (agent1's ask, accepted):** the runbook's warmup-green gate and
  agent1's CT-6 counter-test are ONE grammar (RED = `[tp2 worker error rank N] nvfp4 linear:
  unsupported shape` ×4 + `warmup failed (continuing)`; inverse = same greps 4→0 post-ports) cited
  twice, and §R1 names its arbiters up front — the census string (`e6bebd6a`) and the 37ec0ba1
  serve.log verbatim — so a future disagreement annotates, never re-derives. WO §3b carries my §R8
  split ruling verbatim-cited; §3a carries the moved grep targets + the 63,936-token measured ceiling
  as A4's citable line. Two homes, one grammar, named tie-breakers: that's the shape all
  shared-grammar citations should have had from the start.

### §8z CODEC-GOLDEN LANDING VERIFIED + agent3's c4 CONVICTION OF MY LANE — closed with a re-pin annotation + one landing-TODO for the chair (rev 3.30; agent3 seq-3/seq-4, 2026-09-15 13:0xZ)
- **Landing verified at my seat:** chair merge `98b733d1` carries agent3's P0 authority set — goldens `19e87d76` / cell `cd44ac94` / ref `2b7a4b86` / battery `35967c87` — and the landed guard shape is the one my §8w reachability rows were aiming at, in a better-than-recommended form: `if (!(a <= 6.0f))` (NaN-unfriendly compare) catches non-finite AND past-grid FINITE in one arm (my seat-sim had independently landed on the same conclusion from their RED log: the 12 fuzz convictions include finite 4.43e10/1.72e16/7.65e18 — a guard that only branched isnan/isinf would have left that half of the class alive). Fuzz GREEN 0/100k at the same seed. My §8w CLAIMs all executed: pairing + u32 byte-order arms present verbatim (cell lines 49-50), F4 annotated-not-deleted, swizzle declined, device-duel left post-port. Two MINOR annotations, theirs to act on or not: (i) the guard COMMENT's "once a >= 2^24 (a-4 == a-6 in f32)" mis-states the onset — my pure-f32 sim: at exact powers of 2 distances stay distinct through 2^27 (a-6 lands on a real, distinct grid point below x; collapse of ALL distances starts at 2^28; a-4==a-6 needs ulp>2, i.e. >=2^25) — the GUARD is number-independent so nothing breaks, but per D2 the comment should carry the measured bound at next touch, my sim transcript is in this lane's receipts; (ii) their seq-2 phrasing "device path struck-on-this-hardware" is adopted — the class-name row stands.
- **c4 STALE-CITE at MY lane — agent3's ring convicted MY tree and is RIGHT:** `a5_shard_reassembly_host.cpp` disk@lane = content8 `960a6ebd`, ring accept-set cites `ffe847e2`. Provenance measured, not remembered: c9478e44 landed `ffe847e2` (original A5); c0060689 (§8n) moved it to `960a6ebd` (+161/-7: law-copy cure — tripwire + shipped-law byte-identical copy, graded GREEN 988/0 at §8u); agent3's ring + the chair's c4 cure `9837c576` pinned the 8e242e4b-era DATUM which was PRE-cure. So both stamps are true OF their eras; the ring's rc=1 is residency-grading working as designed (first foreign-tree conviction, theirs to bank). **CLOSING ROW (mine, annotate-not-delete): lane stamp of record is `960a6ebd` (ffe847e2 + §8n cure); `ffe847e2` remains correct for `amd/main` until my lane lands.** Re-pin relay sent to agent3 — when the chair's next merge window carries `960a6ebd` into main, the ring flips ffe847e2→960a6ebd as EXPECTED-by-this-row, not as a new conviction.
- **LANDING-TODO flagged to chair:** `amd/main` @ `98b733d1` still carries the PRE-§8n-cure a5 cell — verified at landing bytes THIS beat (`git show 98b733d1:…a5… | sha256` → `ffe847e2`). Main's copy is the wrong-swizzle-hand-copy generation: sweep-sound but tripwire-less. When my branch next merges, `960a6ebd` should land with it; until then main's A5 cell grades law-copy drift blind — named, not laundered.

### §8aa FIRE SHEET PRE-FILLED + PF-7 FILLED-FROM-THE-ROW (rev 3.31; chair seq-11 all-hands, 2026-09-15 13:2xZ)
- Runbook §R9 adds the exact first-decode commands (L-1k plain pair + L-40k deep leg) at the q3@w4
  ladder's OWN geometry — anchors re-read from banked outputs (7.07/7.05, wall 8.2, tok=58,
  `e764922b`; sustained 5.46/5.47 as the slowest-decode basis for CAP arithmetic), runner script
  stamped `1cbf2526`, budget arithmetic printed per grammar, timeout-not-refusal discipline intact,
  C5-blessed falsifier on the pair (`==e764922b` = weights-not-loaded DEFECT at goal geometry, not a
  pass), and the honest comparability limit named: q3 has ZERO completed 40k legs — the nvfp4 deep
  row is the first full-depth datum EITHER direction.
- **Artifact re-stamped whole this beat:** full 18.3 GB `sha256sum` →
  `eaf8ad124256d0a0c1ebbbca442ca58eee4f97ab34a60a0b4d57e2b41e2c56d2` — EQUALS hash-of-record; the mint
  record now carries two independent full-carriages (agent2 Sep-14 + agent5 Sep-15) instead of one
  citation, at zero boot-time cost.
- PF-7 filled strictly from `9d912423`'s own words, formal-clearance distinction retained
  (`UNCONFIRMED-PHYSICAL ... deliberately NOT issued as a formal clearance` quoted in-cell):
  1k/512 legs fire under the two kept policies; 40k+ legs ONLY witness-armed + chassis-awake. New
  small finding banked during the fill: **the repro's `vwatch.py` (PID 35999) was never stopped — the
  "citable trace" was still growing at 10 Hz (21,039→26k+ lines)**; snapshot taken (26,549 lines,
  sha16 `276a31ef44d51aa3`) as the stable citable artifact, annotate-not-correct applied to the row's
  own count, and the leftover writer flagged to the chair for teardown — never killed by me (not my
  PID; standing rule held). Class note: a growing file cannot be a stamp — snapshot-or-stop first.

### §8ab SF-1 STAMP CLOSURE VERIFIED — the growing-file finding closed same-era; one cite-form annotation (rev 3.32; chair seq-15, cd3e8d6b)
- Verified at my seat, all four of the chair's closure claims: witness PIDs = 0 (owner-killed as
  promised); `vwatch_repro.csv` frozen at mtime 08:27, **31,758 lines**, whole-file content8
  **`8a5c928e504aaa5a`** — the chair's cited value correct to the hex; `vwatch_ports.csv` exists as the
  new port-era carrier (110 KB @ 08:29) so no cited artifact moves again. PF-7's citable stays my
  snapshot `276a31ef…` (26,549 lines — a true prefix of the frozen file, the era's repro plateau), and
  the frozen 31,758-line file is now the WHOLE-trace stamp for anyone who wants everything: both
  citable, neither growing, 21,039 annotate-kept. The growing-file-cannot-be-a-stamp class closed
  inside one beat — the second such class this session (after the nibble delegation) caught by
  measurement and cured by its owner.
- **Cite-form annotation (agent2's namespace law, filed gently):** the ledger words were
  `blob:8a5c928e504aaa5a` — that is NOT a git blob (cat-file declines; it's filesystem content8 over a
  5.0 MB trace). The VALUE resolves, the NAMESPACE label doesn't — per the found-and-cited law, the
  correct form is `content8/filesystem`, and if the chair ever wants the trace GIT-resident it must be
  committed (currently untracked in the shared checkout like the oracle bank was before agent3's
  landing — same hole, same cheap fix, their call).

### §8ac EXPECTATION PROFILE, PRE-REGISTERED BEFORE FIRST-GREEN (rev 3.33; chair seq-18 — r23/r24 discipline pointed at the first nvfp4 tokens; zero-card, all cites resolved at seat this beat)
**Form law (agent1's expect-profile grammar):** these are BRANCH-SETS, not number predictions. Nothing here says what the digest WILL be; every row says what each POSSIBLE observation INDICTS and which instrument grades it — so the story is written before the fact, and any post-boot narrative that contradicts a branch name is itself the finding. Grading teeth: agent3's battery legs re-prove themselves every fire (L9 host goldens; L10 = `nvfp4_codec_duel_host.cpp` DUEL-SELFTEST 10/10 with its seven planted classes M1-M7 re-convicted each run — its namespace, distinct from my A5 shard-invariant family, cited that way below).

**P-1k — first L-1k pair (§R9), named branches:**
- **B1 DIGEST-EQUALS-Q3** (pair == `e764922b0dfe1a7f`, the C5-spent inverse falsifier): INDICTS format-not-loaded — nvfp4 bytes censused on disk but the served weights are q3-route or dequant-equivalent; suspects in order: binder route (Qwen38Nvfp4 resolution), loader plane-swap, fake-quant-restored weights. FIRST TRIAGE: serve-log format census lines (247 NVFP4 objects materialized? `stored==encoded` parse legs) BEFORE any re-run — this is a DEFECT capture, never a pass, and no retry touches it.
- **B2 PAIR-FORKS** (legs differ): INDICTS nondeterminism at temp-0 — the soup family re-materialized at the format boundary; suspects: cross-rank allreduce order in the NEW decode kernels (first code anyone ever ran at these shapes), atomics, uninitialized halves. FIRST TRIAGE: report FIRST-DIVERGENCE CHAR (golden-prefix length — late-char fork = near-tie class, r25's profile vocabulary; early/sync-position fork = mechanism class, r23's finding shape); bank both JSONs + serve.log; C-DUMP ride only if the [C] arms compiled in.
- **B3 NATURAL-STOP ~58 finish=stop**: q3's attractor surviving quantization — expected-shape PASS; still mints (digest is NEW by construction; equality with q3 is B1 not B3 — the two branches are disjoint by C5).
- **B4 finish=length @1000**: nvfp4 has no 58-tok attractor at budget 1000 — MEASUREMENT not defect; comparability row must state q3 never ran a completed 1000-depth leg (r28 natural-stopped at 58), so there is NO same-depth q3 anchor; tok_s gets early/late windows because state-growth cost at depth-1000 is the datum.
- **B5 wall≈CAP-600 timeout, no finish**: decode stall / boot-race reappearance at new geometry — capture-before-kill law fires verbatim (STALL()+wchan on the request's threads), server state banked before any teardown.
- **B6 UFFFD>0**: NEVER-OBSERVABLE-ARM — a decode reached pad region or width-mis-read the row domain; indicts the 248,077-extent/248,320-rows boundary directly (agent3's C9 arm is the declaration-side witness; the pad-id gate cell is the RUNTIME-side witness recorded-missing at r28 — a B6 sighting pays for its boot in one line: it makes the missing gate urgent, not optional).
- **B7 refusal-in-warmup-returns** (`nvfp4 linear: unsupported shape` reappears AFTER a P-row warmup=GREEN was claimed): INDICTS the green was the wrong bin/boot (stamp-identity failure, the era's own law) or shape-table hole between warmup geometry and request geometry (`add-a-measured-row` family, SM-56's sibling). FIRST TRIAGE: re-verify bin content8 vs the boot line before believing EITHER gate.
- **B8 non-200**: 404 = model-id contract slip (pinned at §R3, PF-class runner bug not physics); 500-NAMED = config-table family, quote the server's words verbatim — they name the missing row by doctrine.
- **B9 tok_s far off the 7.07/5.46 band BOTH ways**: orders-of-magnitude slow = route-tier mismatch suspect (served tier ≠ bound tier — the BOUND≠SERVED law; warmup shape took the fast route, request shape took a fallback); wildly fast = suspect planes not actually being read per token (dequant cache or zeros — B1's quieter sibling).

**P-40k — L-40k first-full-depth leg, ceiling-words and stop-hypotheses:**
- **C1 CEILING HIT, GRACEFUL**: gen reaches ~28,329 (=63,936−35,607, the §R9 headroom math) and stops with finish=length — the server's OWN lines quoted verbatim (auto-KV capacity print + stop reason); 'none' is a logged word. This is contract-satisfying exhaustion — MEASUREMENT.
- **C2 CEILING HIT, UNGRACEFUL** (error/500/crash at exhaustion rather than clean length-stop): INDICTS the budget/stop machinery under real pressure — the exact class the ladder exists to find; capture-before-kill, bank the allocator's words, and it is a DEFECT (exhaustion is allowed to happen; screaming is not).
- **C3 natural-stop-at-~58** (q3 attractor at depth): states that context depth does not move the stop point at this quant — measurement-to-come phrasing: we PREDICT NOTHING about where it stops; the branch set is {≈58, deeper, length-bound, error}, each with its own row; a deeper stop is not "worse", it is the attractor-at-depth datum the testplan asked for (the cure-might-be-budget-limited question, stated plainly).
- **C4 PREFILL REFUSAL**: allocator says no at 35,607 — quote words verbatim; the mixed stack's KV/GDN footprint per token at nvfp4 differs from q3's measured rows; a refusal HERE is information the estimate-based preflight was BANNED for manufacturing (VRAM law: the allocator's no is the only no).
- **C5 HOST DIES MID-PREFILL**: SF-1 reclassifies from UNCONFIRMED-PHYSICAL to load-proven if and only if the witness trace shows sag/cut THIS close to the step — trace-first triage, chassis policy is why this leg has a witness at all; deep legs freeze pending cure, 1k legs unaffected (PF-7's split, pre-registered).
- **C6 prefill tok_s vs 41-anchor**: any value is data; gross slowness names the SIMT-GEMM-shape question (P2's arithmetic domain), NOT a retry cause.

**P-DUEL — device-vs-golden (agent4's P2 dumps through agent3's L10 cell):**
- **GREEN means operationally**: per-element arithmetic equivalence of device decode vs the host oracle chain (goldens → ref → VENDOR emulation headers, the three-way tie), graded on dumps of REAL device runs, with L10's seven plant-classes convicted in the same fire (a GREEN without its plants re-convicted is UNGRADED — the three-state law, no green-by-absence).
- **FIRST-RED CLASSES, pre-named** — mapped to the duel's own plant vocabulary so triage starts half-done: lane-order = M1 (x2 nibble swap; CT-1's plant; symptom: every pair transposed, statistics match, digests don't — the wrong-loader-hides-in-consistency shape my §8w pairing arm exists for); value-drift-at-one-entry = M7/M2 (table sign-mirror / LUT rounding-drift — ties-rule mis-implementations, suspect fused-multiply-add contraction vs the golden's integer-form certification); sign-discard violation = M3 (NaN-with-sign — the exact corner the encode_e2m1 guard closed at 2b7a4b86; a device sighting means the PORT didn't carry the fix class); artifact-integrity = M4/M5/M6 VOIDs (header tamper/truncation/absence — never verdicts, route to the dumper, not the kernel). OUTSIDE the duel's per-element frame, the shard-geometry class the duel CANNOT see: scale-block stride and /world-region off-by-ones (blockscale-k16-m128x4-v1 tile math; K=5120→1280-local row slicing) — graded by MY A5-family invariants + the §8n text-tripwire on the swizzle law, and the signature is boundary-shaped wrongness (correct WITHIN groups, wrong AT group/region edges) — naming that shape so a first-red prints it and nobody has to invent the diagnosis live.
- **Sequencing honesty**: L10 exists and its teeth are pre-proven BEFORE the port (agent3's design); a port landing without running L10 on a real device dump is an UNGRADED row, and the fire sheet §R9 legs must not be quoted as NVFP4-operational until the duel's first GREEN+plants pair is banked. Three states again: duel GREEN / duel RED-named / no-duel-NO-VERDICT — the third is not the first.

*(§8ac stamp: agent5 2026-09-15 13:4xZ, chair seq-18 order; cites resolved at seat this beat — duel classes read from `nvfp4_codec_duel_host.cpp:231-277` at main, L9/L10 semantics from `agent3_battery.py:14-24`, e764922b/7.07/5.46/41-tok anchors all re-verified against banked outputs, C5's strict form from my own probe pair-check. Zero card, zero build, lane pushed.)*

**B10 addendum to §8ac (one row, chair seq-24; runbook §R9 L-10k carries its triage shape):** *warmup names organ #4* — if agent4's step-1 bind-through fails at a NEW throw-site (a refusal string not among the three pinned homes: `nvfp4_dispatch.cpp:19-21/:60-62`, `linear_add_plan.cpp:19`), the boot prints that organ's name VERBATIM as the datum: INDICTS route-table coverage at the newly-touched organ (add-a-measured-row family again, one deeper — every prior era's "new organ" sighting has meant exactly one unwritten row, never physics); FIRST TRIAGE: map the string to its call-site at bytes (grep before theorize), confirm it is NOT one of the three known texts wearing new phrasing (the render-drift class), then bank RED per L-10k's A-THROW branch and route the row-request to the WO. A fourth organ named in warmup while requests succeed = the decoy-pair inversion (BOUND-path coverage ≠ SERVED-path coverage) — both directions graded, neither retried.

### §8ad §8ac's FIRST WALK — reconciliation at bank-bytes against 83bb825eb (chair seq-31; C44 boot row d886c933 read at my seat, annotate-not-pat; the profile's first billing: two branches hit cleanly, one split, ONE MISSED — written now as B11)
**What fired (good news stated flatly first):** B1 inverse falsifier SAT its first live exam — `463eac76870ad6f3` ≠ `e764922b0dfe1a7f`, pair byte-equal both legs, and the text is a complete exchange (`User said "Hi." Simple greeting…` → `Hi! How can I help you today?`) with UFFFD=0 and the FORMULA line printed pre-mint: **NVFP4 weights-loaded at TP4 is now PROVEN, first time, by the C5 blessing spent exactly as designed.** B2 silent (determinism held at the format boundary, pair-grade). B7/B8/B10 silent (four-arm 1/0/0/1, no non-200, no warmup-organ-named). The 27-vs-58 natural-stop: **B3 as written was a profiling miss** — it named "~58, attractor survives" as THE expected-shape; reality: the stop-attractor MOVED with the format (27 tok) and that's physics-not-defect (chair's words, adopted) — B3 annotated-not-deleted into B3a {stop≈q3-58: NOT observed} / B3b {stop@different-count: OBSERVED 27}, with the A4-consequence stamped: every digest expectation for this (artifact,bin) pair is PROFILE-FORM forever; the 27-token text is banked and C6-signable the moment the named human reads it (MEASURED-PENDING-HUMAN still holds — mint born paired, signature still the remaining conjunct, plus C4's 25-rep bar and the duel's GREEN, none of which tonight's pair substitutes for).
**THE MISSED BRANCH — B11 STALL-AT-k, CLEAN-200, IDENTICAL LEGS (the organ #5, `423a7091b0d8e124`):** generation halts at 2 tokens, HTTP 200, runner-extracted finish=stop, content EMPTY, reasoning a mid-word fragment (`ascript`, chars=7), legs byte-equal, server accounts `finish=cancelled` only post-delivery (client-gone, not user-cancel), caps nowhere near (3.9 s ≪ 3,600). Nothing in my branch-set covered "server RETURNS success while generation FREEZES mid-output" — the branch between B4 and B5, invisible to both: B5 watches for NO response; B4 watches for full-budget response; this is a WRONG response that looks like a contract-satisfying one. The missed-branch law pays: B11 indicts the decode-ADVANCE path at specific positions (state/lane bookkeeping or a genuine early-EOS emission by the QAT model — BOTH alive), first-triage is ONE boot and zero code: **print the final token's ID + the finish-writer's branch** — legitimate EOS id in 248,044..248,076 ⇒ physics (premature-stop attractor, same family as 27-vs-58, report as profile datum); any other id ⇒ state-machine defect, organ #5 joins the add-a-measured-row ledger. And the honest note on agent4's excellent "pair-identical ⇒ state-lane not arithmetic" routing: pair-identity does NOT discriminate — arithmetic is deterministic too — what discriminates is ONLY the id-or-EOS question above. Recorded that way because routing right for a partly-wrong reason is how cures miss.
**THE MIS-QUOTE THE CHAIR'S MESSAGE CARRIED, corrected with the bank's own fields (this is what the sheet is FOR):** the fired "10k pair" is `kind=sustained prompt_tokens=85 chars=7` — the q3 LADDER's generation-depth rung-10000 (300-cap family; max_tokens=10000 ceiling), NOT runbook §R9's L-10k row (which is `kind=deep`, body 46,009 chars, prompt_tokens≈10,240, max_tokens 1,000). L-10k has fired ZERO legs; 423a7091 is not its "first comparison point" and no context-depth claim may ride the sustained pair — its prompt is 85 tokens, it measures nothing about depth. What the pair DOES support: stall existence + determinism at the sustained shape; the ONLY valid same-shape comparison on the board (q3 twin: 37 tok in 6.8 s, finish=stop, complete stream vs nvfp4: 2 tok in 3.9 s, fragment) — which makes the nvfp4-vs-q3 story at that rung the STALL, not throughput. What it CANNOT support: prefill-vs-decode attribution of the 0.51 (it is TOTAL-wall over 2 tokens — 3.6 s of it was prefill at prompt-85; the server's `decode=4.1 tok/s` is an n=2 nonsense window), any depth statement, or any small_t re-read-cost claim. And the prefill number gets the same axis discipline: nvfp4's 23.4-25.8 tok/s is at prompt=54/85 — the q3 41 tok/s anchor is from the 35,607-token DEEP curve's 4% band; q3's OWN shallow prefill printed 23.2-24.6 tok/s at prompt=54 in banked w4-era lines (re-read this beat) — **at shallow, nvfp4 prefill is AT PARITY with q3, not 1.7x slow; the W8-fold/small_t-debt question stays OPEN and can only be answered on the DEEP axis, which requires the L-10k row (the real one) to fire.** Compare like-axes or say nothing.
**Post-C44 what-can-the-sheet-support, one row each:** 1k decode window `8.2 tok/s` (server's own line) vs q3's total-7.07-7.05 pair: different tok_s SEMANTICS (window vs total-with-prefill) — the grammar's windows law is what keeps even this honestly statable; next-session measurement that DOES discriminate small_t-re-read vs depth: the REAL L-10k row (deep-prefill at 10,240 tok, decode early/late windows, max_tokens=1000, CAP 1800) — if nvfp4 decode stays ~8 at 10k-prompt where q3's curve says ~21-prefill-band, the depth-decode cost is the datum; re-read cost per step would hit 1k too, which 8.2-window says it mostly doesn't. One boot, sheet already written, no new code. Watch-post resumes.

### §8ae B11 JUDGMENT on the organ-#5 walk (agent1 routed seq-18; verdict made on bank bytes only, zero-card; C44 serve lines re-read at my seat this beat)
**THREE CLOCKS, ONE REQUEST — the sharpest form of the datum:** req 4 and req 5 (the 2-token sustained legs) print, in the serve's own lines: progress `decode: 1/10000`, lane line `single-seq lane 0: ... gen=3`, done/usage `gen=2` — the SAME request carries three different generation counts, then `done finish=cancelled` while the client demonstrably waited (`-m 3600` in the runner, re-read) and a COMPLETE 200 JSON was delivered with `finish_reason:"stop"`. The healthy 1k legs (req 2-3): lane `gen=27` == done `gen=27` == usage 27, `finish=stop_token` — agreement is PROVEN AVAILABLE on this exact code path. Deterministic, 2-of-2 legs identical.
**VERDICT: ORGAN, at the finish-writer/accounting layer.** The model-choice reading (QAT behavior) REQUIRES the engine to end a deliberate EOS — and this engine, when that happens, writes `stop_token` (req 2, same boot). `cancelled`-while-client-waits, plus the 3-vs-2-vs-1 counter spread, plus a delivered stream that starts at a word-INTERNAL BPE piece: `ascript` is vocab id 5644 and ` ascript` does NOT exist in the tokenizer-of-record (checked against the artifact's own in-band tokenizer.json this beat) — the visible stream begins legally ONLY as `as`(299)+`cript`(1186), which is consistent with either reading for the CONTENT, but the FIRST-token-at-turn-start oddity plus the vanished third generated token keeps the dropped-head hypothesis alive. Content question (is 'as'+'cript'+? the model's own weird opening, or a dented rendering of a dropped word-head?) stays OPEN; the ACCOUNTING question is closed: **at least two of the three writers lie on this request shape, per-request, deterministically — that is the organ, and it is not a profile amendment I can bless away.**
**A6/A7 AMENDMENT: WITHHELD, drafted-not-applied** (agent1's proposed annotate — "this model answers that prompt class differently" — is NOT-VERIFIED-AS-BEHAVIOR until the id-print says EOS): if the next boot's leg prints final ids `... 299, 1186, <eos-id 248044|248046>` WITH engine finish=stop_token, I will apply the amendment verbatim as QAT-physics; any other combination and A6/A7 expectations stay untouched while the organ gets fixed. The chair's inverse stands: an unused branch beats a laundered conviction.
**B11 DISCRIMINATOR, extended by the three-clock datum into a STANDING CELL (RED is ALREADY CAPTURED — this firing is the red row, two legs, banked at d886c933):** every done-row must satisfy `lane gen == done gen == usage.completion_tokens + (named EOS convention, fixed by a healthy-row measurement, currently 0-diff at req 2)` AND `finish=cancelled` must be impossible while the client waited AND a 200 was delivered; any violation NAMES the two disagreeing counters and their values. Runtime witness: rides the boot-battery pattern (device-touching); the grep arithmetic itself is host-only and farm-able on any banked serve log — recipe to agent3's battery seat as a new leg with this row as its pre-registered RED cite; GREEN joins at the cured bin per closure law (red row / green row / suite entry, all three shas or it doesn't close).
**CARRIED ANNOTATIONS from agent1's grade (accepted, cited-not-re-derived):** (1) the FORMULA ratification is now load-bearing on LIVE data — 1k mint concat `463eac76870ad6f3` vs dead-dialect reason-only `6a9414c4a8915f5c`, both recomputed at my seat this beat, first content-bearing mint, the dialect made the difference on a real stream (census §3 amendment-2 working as designed); (2) **golden-prefix-open is WITHIN-LINEAGE only** — nvfp4 diverges from q3 at TOKEN 0 (`User` vs `The`), so A4's expect-profile gets the self-prefix-only clause and C5's ≠-conjunct is satisfied maximally by the same diff; my §8ac B3b wording already leans profile-form, this sharpens it — agent1's G5 row (token-0 divergence + stop-depth 27-vs-58 + style-class, within-world-paired) is the correct home for the cross-format datum; ACCEPT v2's A2/A4 rows get the annotation at the doc owner's next legitimate touch, cite-not-re-derive.

### §8af THE CELL BUILT, FIRED BOTH DIRECTIONS — and it convicted the ERA, not just the organ (rev 3.36; seed: tools/v340l/nvfp4/finish_clock_consistency_host.py, selftest 7/7; chair seq-41/44 orders executed)
- **Seed cell landed** (agent3's L12 slot, checkout-not-hunt red per agent1's gift): A1 lane==done (measured delta-mode, never hardcoded), A2 cancelled-admissibility (client-gone witness OR time-paired delivered JSON — one-to-one, exclusive-nearest, CONSISTENCY-GUARDED pairing), A3 progress cadence note, A4 zero-comp-lie; three-state rc incl. traffic-less VOID. Four bugs-of-mine caught and disclosed during build, all now fixture-armed: exit-rc-behind-a-`| tail` (the class bit ME writing the cancelled-while-waiting cell), key-space misjoin (greedy glob paired req 4 with the 1k file — the conviction print itself quoted comp=27 on a gen=2 leg and self-reported), band-too-tight-then-second-tie-inherent (1 Hz `created` stamps → pairing is exclusive-nearest + consistency-guarded, refuses on ties), split-truncated fixture (an arm silently graded a done-less log — S-e rewritten explicitly). Both directions on REAL logs: q3-plain-only slice rc=0 (2/2 legs clean), r32 rc=1 (4 convictions, correct pairings), q3-r28-full rc=1.
- **THE ERA-WIDE FINDING (the cell's first dividend, before anyone even merged it):** `G18r28_hladder_serve.log` — Q3, pre-NVFP4, the era's OWN healthy bank — carries the SAME signature on its sustained legs: `lane gen=38 / done gen=37 / finish=cancelled` client-waiting, delivered JSON says stop (req 3/4, 06:08:21/28). So the accounting organ is NOT nvfp4-organ-#5, it is **abort-path finish-classification, era-wide, first CAUGHT because the nvfp4 boot made the freeze extreme (2 vs 37 tokens)**. Re-scope of my §8ae exclusion stated precisely, annotate-not-delete: my "a model-choice stop writes stop_token, so cancelled proves the model did NOT choose" survives only if the engine's only non-EOS endings are involuntary — but the q3 legs completed their refusal COHERENTLY (bd940021, 37 tok) and still rode cancelled; the live hypothesis now: **a stop token (im_end-class) at a structurally wrong position under thinking=ON takes an abort path that mislabels AND miscounts (+1 lane) in BOTH artifacts** — which would make the CONTENT question lean QAT-physics-premature-stop (nvfp4 stops at 2 where q3 stops at 37, same shape) and keep the ACCOUNTING conviction (two writers, delivered 'stop' while 'cancelled') era-standing. The id-print trigger I held is exactly the discriminator for THIS too: final ids ending 248044/248046 + cancelled-class ⇒ abort-path confirmed era-wide, apply agent1's behavior-annotation to A6/A7 AND route the writer fix; ending non-EOS ⇒ genuine generator stall, organ #5 stands alone. Either way the fix-writer's cure is the same boot's problem, not A4's — A4's 1k legs are stop_token-clean (clocks agree 27/27/27, the healthy shape is PROVEN available in the nvfp4 boot too).
- **L12 landing caution to agent3 (three-state law, pre-empted):** until the abort-path writer is cured, ANY battery fire grading a log with sustained-class legs will show these REDs — EXPECTED-by-§8af, name them so in the leg output (`organ-5-era-wide, cure pending` + this row), NEVER let a known-named organ read as landing-blocker and never let the leg print GREEN-by-expecting-nothing; plain-only-legs logs stay the GREEN direction until the cured bin lands (then the q3-era r28 sustained legs become the cell's second permanent red-fixture, both eras guarded).

### §8ag C45 PRINT DESIGN, sealed from agent1's seq-24 (era-wide fold @ 8a23990f, verified resolvable at my seat): the two-hypothesis fork made one-boot executable (rev 3.37)
- **What C45's boot must print (relay sent to agent4, this row is the durable home):** (1) per stalled/ended request: `last_tok_id + in_stop_set` (B11's final-ID arm, already specified); (2) **the abort-path stop-reason SELECTOR printed AT THE DECISION SITE, not beside the writer** — one line naming which branch labeled the finish: `eos-detected | budget | stall | client-cancel`. The fork then reads itself with zero interpretation: content-said-stop (`in_stop_set=YES`) while selector says stall/cancel ⇒ agent1's A6/A7 behavior-annotation activates class-wide AND the writer fix routes (misclassification era-wide, §8af's leading hypothesis confirmed); in_stop_set=NO with selector stall ⇒ true generator stall, organ #5 stands alone as nvfp4 surface; selector==label but finish=cancelled-delivered-stop mismatch persists ⇒ pure writer-translation bug, accounting-only. No fourth dialect: the selector is one print line at an existing branch point, and both arms were pre-accepted by agent1 (annotation carries its class-wide activation clause either way), while MY held trigger (§8ae) executes unchanged.
- **Cell growth follows, it doesn't lead:** once the selector exists, the clock cell gains a future A5 arm — selector-vs-delivered-label agreement as the THIRD clock — with the three-state law honored at introduction: selector absent in the graded log ⇒ A5 prints NOT-CHECKED, never assumed-green; r32/r28-sustained fixtures stay valid under it. Sealed handoff state: seed @ 9b7538ae (origin echo verified), agent1's fold @ 8a23990f, chair's ledger-annotation landed cdff95cc verbatim-at-the-ledger. Desk dark until C45's bytes; both outcomes are pre-graded, neither needs a story afterwards.

## §8m N7 EXECUTED (2026-09-17 ~04:2xZ — the flag flip, per THIS section's own re-spec)
- **`--allow-nvfp4-weights` SHIPPED** (ServeOptions/EngineOptions/TpBackendOptions plumbed,
  serve_options parse + usage), gate rewritten in tp2_backend.cpp create(): value-parsed env
  (1/true/on/yes; set-but-falsy = LOUD throw naming the value — '=0' no longer silently means
  nothing), refusal names BOTH numbers (weights profile family + requested world), legacy env
  name kept for compat. Binary banked `ninfer-serve_6c1fe9366f0ef83d.bin`.
- **Proof arms (results/amd/coherence/N7_row.txt):** (A) no-flag/no-env NVFP4 boot → loud
  both-numbers refusal, zero device touch; (B) `NINFER_ALLOW_NVFP4_TP2=0` → value-parse
  refusal; (C) flag-ONLY boot (env verified absent via /proc environ) → full
  BOOT_BATTERY GREEN with ZERO SKIPs: guard 5/5, route-parity device cell PASS,
  halves-contract device cell PASS, ladder 14/14 (row BOOT_BATTERY_20260917_041518).
- **The old comment's precondition is met and cited in-tree:** live-path proof = BUGTRACK
  E-14/E-16 + the E12b..E16 rows + NIGHT_HANDOFF_2026-09-16_mtp_thermal.md; CI-class proof =
  tools/guards (host, 21-mutation selftest) + BOOT_BATTERY (device cells + ladder).
  **N7 status: CLOSED.** SD-1 north-star reading: NVFP4 is FULLY OPERATIONAL at TP4 —
  coherent 56..10000, MTP k=2 serving, un-gated by a named flag, guarded by the suite.
