# SPRINT STATE — RESUME HERE (chair, 2026-09-15 11:10Z)

> ## ⚑ STRATEGY DECISION SD-1 (user, 2026-09-15 11:1xZ — THE NORTH STAR, BINDING, DO NOT DRIFT)
> **The project's goal is: NVFP4 FULLY OPERATIONAL ON THIS HARDWARE AT TP4 (world=4). Not q3. Not w2. TP4-NVFP4.**
> - Everything else — the q3 context ladder (H1-H6), w2 controls, soak runs — is **RISK-REDUCTION FOR THAT GOAL, not the goal itself.** The ladder is DEMOTED to opportunistic boot slots; it never fronts the queue again.
> - w2 is CONTROL ONLY for NVFP4 (ACCEPT: A4/@w4 is the row that matters; A3/w2 optional; four-part-contract law still binds every row).
> - Standing ordering rule born with this decision: **the cheap falsification before the expensive engineering** — the honest-first boot (load the REAL 18.3 GB NVFP4 artifact at w4, let dispatch refuse cleanly) precedes device-port work because the load path has NEVER carried that artifact and this board's entire bug family lives in load paths.
> - If any future sprint finds itself polishing throughput, parity, or breadth on q3 while NVFP4-at-TP4 waits: that is drift from SD-1. Re-read this box.

**READ ORDER FOR FRESH SESSIONS:** this doc → `docs/amd/TP4_HARDENING_TESTPLAN_2026-09-15.md` (the ladder + failure semantics) → `docs/amd/NVFP4_ACCEPT_v2_and_RUNG_GRAMMAR_2026-09-15.md` (G-gates/H-rungs, four-part contract law, mandatory rung fields incl WORLD) → newest STATE block in `docs/amd/COORDINATOR.md` → chair ledgers (git log amd/main). Then the desk rows below. Laws register: `docs/amd/LAWS_OF_0914_0915_agent1_compile.md` (43+ rows, laws 42/43 + family candidates in the ledgers).

## MISSION (unchanged, user-set)
TP4 = green+merged, now **HARDENING by climb**: H1-H6 context rungs 1k→200k (team-green bar: 59 tok/s @ int8-200k precedent in docs/59_roadmap). Then **NVFP4 full deploy** (sequence owned, below). Counsel external: RETIRED (answered-by-observation, append-only).

## STATE AT STOP
- **main = current tip at stop** (soup fix + kit + all cells + battery 59-leg + ACCEPT/grammar docs MERGED; gate PASS fully-certified; battery 79/79 at last count; falsifiers VERDICT=0 at merge)
- **lanes**: p3-serve `6d4d974a` (56-row device.cu MEASURED — full commit for doctrine: all four dies hipGetDeviceProperties==56, values = in-effect pre-table constants as baseline, grid-hint perf DELIBERATELY immature w/ named follow-up; falsifier pre-stated: 1k digest must stay e764922b, 10k must go 200); nvfp4 `8cb5187e` (v4 cold-run chain); a1/shim at closed commits.
- **IN FLIGHT AT STOP: group-3** (ladder session on the 56-row bin: 1k digest-invariance pair + **10k sustained pair = the user's number** + deep-prefill context-axis leg + w2 control). agent4 told: bank partials, release card cleanly, report.
- KFD: one serve boot resident at stop-order (dying or dead by the time you read — `rocm-smi --showpids` to confirm, kill by own-pgid rows only if residue). df ~18G; battery line 15.0 GiB honored everywhere; banks 26/26 untouched.

## NUMBERS LEDGER (decode, tok/s)
- w2 control (r28 g1): 7.06–7.09, 4/4 determinism, sha e764922b
- **w4 (r28 g2, pre-56-row): 7.07–7.10 @1k — first w4 numbers**; the 1k rung PASSED pre-fix, the 10k-cap rung was the first defect
- 10k-cap @w4 pre-fix: HTTP 500 'unknown SM count 56' — FIXED by 6d4d974a row, re-run = group-3 pending result
- traced price: 13 s/token (PROBE env only); green precedent 59 tok/s int8@200k (comparability limits stated in rows: quant/die/world/TTFT-inclusive)
- natural stop: 58 tok uniform (residue 59-completion CLOSED at measured)


> **ERA-WIDE FINISH-WRITER ANNOTATION (agent5's §8af block, chair-penned; applies to every sustained/enum-shaped row above with `kind=sustained` or prompt=85/54-rung-10000 legs):** client-visible-JSON facts STAND (digests, tok counts, tok_s computed from delivered bodies, reproducible at seat); determinism credit UNTOUCHED (pairs byte-equal under the same broken path — law-51: equal clocks lying together still lie together); **FINISH-SEMANTICS on this shape are NOT quotable as model-choice until the abort-path writer is cured — do not quote "stop" from these legs as evidence the model chose to stop.** Caught by finish_clock_consistency_host.py's first back-fire (rc=1/conv=4 on r32; q3-era bank convicted same signature; era-wide, not format-specific). id-print discriminator (C45) decides EOS-misclassification vs generator-stall; either way the cure is class-sized.

## OPEN THREADS BY DESK (resume assignments)
- **agent4**: group-3 rows (the user's 10k number + deep-prefill) → report vs pre-stated falsifiers (digest e764922b invariant, 10k-200); then H3-40k/80k/160k climb, soak; MTP = its own WO (3d5453f2 draft); near-tie audit opens w/ extended-CL budget rung.
- **agent1**: **sha-identity RECONCILIATION OPEN** — ladder w4==w2 byte-equal vs r25 signed fork@≤32: candidates = budget-conditioned fork (law-43-family gold; amend r25 profile + ACCEPT A2 annotate-not-delete), render drift, or world-label error. Then rung-row grading (eight-row grammar), G5 cross-format seat (Gemini re-home), near-tie audit.
- **agent2**: battery-seconds @ 56-row bin ref (count AT ref), cleanliness-cell re-run at gate-changing tips, re-sim tooling ready (merge_falsifiers.py --step1/--merged, resolver mutation-tested 7/7).
- **agent3**: ring-as-pre-fire leg once lane tip carries v4+ (leak-free de1224f8+), r24/r28 capture census arms on plane/ladder grammar (empty=VOID, per-tag len), pad id-gate cell at NEXT boot tip (recorded-missing in r28 = the honest gap).
- **agent5**: ACCEPT v2 review at e335edad (C5 tokenizer-shared assumption → doc-35 bless question), G1-codec-goldens + G7 triage verdict (live-or-struck), c_capture map-consumption rows on group-3 captures, NVFP4 port queue behind ladder windows.
- **NVFP4 sequence (locked order)**: triage → ACCEPT review → G-codec ports → honest-first boot (admission green + named-shape refusal, then inverse) → real-decode artifact owner (convert-side) → deploy per ACCEPT C/R conjuncts.

## STANDING LAWS THAT BIND THE CLIMB (full register in compile doc)
allocator-is-only-no (its words logged verbatim, 'none' is a logged word) · LITH measured-on-this-machine · capture-before-kill on any non-200 (STALL()+wchan) · determinism pair every rung same-boot · bank-RED-and-continue, never retry-away · WORLD+budget+field+formula on every row (four-part contract) · counted refusals (rc=2=NOT-OBSERVABLE≠fail≠skip) · cite-by-sha/date, annotate-not-delete, message-vs-stat verification on every landing (arm: verify_landing_stamps) · no third grader dialect, no channel posts, grants through chair serial.

## WARTS ON RECORD (so nobody rediscovers them as surprises)
unreproduced boot race (w2b pre-L0, one sighting, capture protocol armed) · pad-gate cell not in 56-row bin (recorded-missing) · grid-hint perf on SM 56 = immature baseline-by-precedent (named in commit) · runner sed-surgery splice caught by bash -n pre-fire (silent-edit class 3x at agent4 desk — named) · MTP = never-booted frontier, own WO, same organ class that made the soup.

## ⚑ SF-1 — LIVE STABILITY FINDING (user-classified 2026-09-15 ~12:15Z; this section's ABSENCE was itself cited by me before it landed — chair law-43 instance, annotated here, see ledger efa-era)
**Host died without a log during 4-die synchronized 40k prefill.** Evidence chain (re-derivable): last journal line 07:01:28 routine tailscaled chatter; last serve-log line 07:01:52 `prefill: 1408/35607 tok (4%) 41 tok/s` — ~26-35 s into first full-load 40k across 4 V340 dies; new kernel first line 07:02:45. **53-second window; ZERO shutdown-sequence lines; no wtmp close** (Sep 11 reboots recorded theirs — logging machinery proven working); no OOM; no amdgpu errors in final minutes (118 workqueue-hog warnings over 4 days, last 11 h pre-death = ambient, not cause); pstore empty/not-configured; clean software reboot RULED OUT (would write hundreds of stop lines). User at chassis: did not press → live finding: 4-die synchronized prefill can silently kill this host. Candidate classes: **power-delivery transient** (4 MI25-class dies idle→full-burn simultaneously = classic brownout; single-die calibration shows one die pins 111 W = its 110 W factory cap, so a 4-die sync step is ~445 W GPU + CPU rising in seconds) **vs kernel hard-lockup** (leaves no journal trace by design). Instrumentation built at `/home/chris/sf1_tools/`: `vwatch.py` = 10 Hz fsync-per-line logger of per-die `in0` (vddgfx mV), `power1` (uW), temp1/2/3 (edge/hotspot/mem) + CPU + 2 DIMM temps — survives host death up to the last line; `burn.cu` = 4-die barrier-synchronized GEMM load reproducer. Measurements so far: idle 4-6 W/die, vddgfx 762-787 mV; single-die burn → 111 W pinned, voltage RISES to 862-956 mV (no sag — VRs healthy at 1/4 load); 2-die burn → no sag; staggered 4-die → 64 W peak total, survived. **The faithful repro (agent4's own runner: same 3da20992 bin, same 160,110 B file-routed body, 4-die synchronized 40k prefill) under witness = the decisive test; if host dies again → electrical trace captures last 100 ms; if it walks both legs → non-reproduction, one-time-event hypothesis carries the clearance.** Consequence for SD-1, both directions: repro ⇒ A4 fires only after the cure; non-repro ⇒ A4 proceeds with the clearance named. Either way: 40k+ deep legs from now on ride ONLY under witness, and someone awake stays at the chassis. Zero data lost from the death itself: all commits wire-reachable; the prefill ACCEPT (35,607 tokens, prompt_tokens IN the falsifier band) is banked as agent4's datum at 09c8c6d7.
**RESOLUTION 13:1xZ (user):** chair ran the faithful repro (same bin 3da20992, same 160,110 B body, witness-armed): 1k pair green (boots 6-7), 40k leg 1 walked to >=43% (15,232/35,607), peak 528 W measured across the four dies, **zero voltage sags in 21,039 fsynced samples, host survived the whole deep-load plateau** — user then supplied the physical hypothesis: **"maybe my cat hit the power button."** Fits ALL evidence (front-panel power cut = zero journal trace, empty pstore, 53-s dead zone, wtmp unclosed — a cut is indistinguishable from the worst brownout class by design). Status: **UNCONFIRMED-PHYSICAL, not software, not load-proven** — deliberately NOT issued as a formal clearance (a cat is a contingent event; absence of electrical anomaly under witness is evidence against power-delivery-but-not-for-loads-we-never-repeated). Residual policy kept cheap: deep rungs (40k+) ride the witness as ROUTINE (it costs nothing: python, zero card), someone awake near the chassis for first-attempt geometries, PF-7 in the A4 runbook fills with THIS row's words + repro-log sha. Repro terminated mid-leg-2 at user order (card-time economics: NVFP4 ports opened; see §SD-1 fit-amendment). Full trace: /home/chris/sf1_logs/vwatch_repro.csv — STAMP RESOLVED (agent5 finding: a GROWING file cannot be a stamp; witness had never been stopped, file at 26,549 lines when she cited snapshot blob:276a31ef44d51aa3, my 21,039-count annotate-kept as true-at-stamp-time). WATCHER STOPPED BY OWNER (pid 35999, mine) and final trace frozen at fs-content8:8a5c928e504aaa5a (31,758 lines, frozen; agent5 cite-form correction 665879c7: not a git blob — filesystem content-hash; NOW COMMITTED at results/amd/sf1/vwatch_repro_frozen_8a5c928e.csv, durable past any clean, instruments beside it) — PF-7 and this row cross-cite the snapshot; the whole-trace stamp rides here. Port-era witness re-armed to a NEW file (vwatch_ports.csv) so agent4's boots get coverage WITHOUT moving any cited artifact.
