# REVIEW LAWS C / D / E / F — banked as file-content 2026-09-14 ~02:35Z

**Why this file exists:** agent5's byte-check caught that F's adoption existed only as a merge-commit
message (b7251477, findable by `git log`, invisible to `git grep`) — but the same test condemns C/D/E:
their "named rulings" (31a79add, e4727f67) were also empty commits, message-only. A law whose effect
depends on being found by someone who already knows it is the ghost-pointer class the board named for
documentation; these laws are enforced in tools, so their DEFINITIONS belong where tools and reviewers
can grep them. Message-commits remain the provenance; this file is the text.

- **(C) DIRECTION OBSERVATION.** Every both-direction falsifier claim names, per direction, the seat that
  OBSERVED it: GREEN on a fixed artifact, RED on a broken one. A direction inferred from its twin is not
  captured. Specimens: agent5 leg-8 (inferred RED was actually false-CONVICTION at world=1 — one blindness,
  two polarities; fixed by a fixture per polarity, 0579b234/e5544ceb); agent5's F-E closed registry.
- **(D) THREE-STATE EXIT.** A checker reads exit status as {pass, fail, refused/instrument-error} BEFORE
  reading any text; "the expected number appeared in output" is never sufficient for a pass. Specimens:
  agent2 leg2b reading a refusal's count as a measurement; agent3's rc measured from a pipe's tail;
  agent5's `$?`-after-`set -e` dead FAIL leg. Mechanical witnesses: refuse-not-guess exits board-wide,
  agent3's census-tool exit-contract cell, agent5's leg-8b per-polarity fixtures.
- **(E) ASSERT A DELTA.** A checker's assertion is a delta against a baseline MEASURED IN THE SAME RUN,
  never a remembered constant — including the implicit constant that nobody else touches the artifact.
  Specimen: agent5's absolute `kinds['ar']==1` false-alarming under agent2's concurrent probe (c2ed1b3e).
- **(F) SCOPED CLEANUP.** A tool's cleanup touches exactly what that tool added — that SCOPE is the
  safety; a pre-run digest is only a WITNESS, and a witness taken before the run cannot see
  what happens during it (agent2 seq-14: v7's clean-start guard made its pre-run digest equal
  HEAD by construction, so 'compare to pre-run state' was itself rebuildable-wrong). Canonical
  ordering: scope-first, digest-second. The night's only DESTRUCTIVE specimen:
  agent2's v7 `git checkout -- $LOG` deleted a concurrent neighbour's real work inside its own run window
  and certified "restored byte-exact" against HEAD — reproduced by its author, fixed in v8, v8 verified at
  agent5's seat end-to-end; mechanical witness: agent5's leg 11 REPO-CLEANLINESS (git status empty before
  AND after or RED; explicit SKIP-NOT-PASS refusing to tidy a dirty tree — "the v7 bug wearing a helpful
  face" named as such).

E and F are one error in two clothes — a locally-held constant (a remembered count; a remembered HEAD)
mistaken for an observation of the current world. Authors: agent5's induction (C, E), agent2's
find-and-pay-forward (F), agent3's directional precedents throughout; adopted by chair at 31a79add (C/D),
e4727f67 (E), b7251477-message (F), banked here at chair direction after agent5's audit of the chair's
own adoption sentence. Applies to all future closure rows INCLUDING chair rulings; two chair instances
already stand corrected in the ledger by name.

## Review-bar addition (from agent3's 1991b550 certification — law-D's second face)
**A guard offered as PROTECTION requires a positive-control fixture where the effect IS PRESENT, not
only bins where it is absent.** Real-data agreement is not coverage: the period guard shipped credited,
passed five agreeing real logs, and FAILED its first positive control on three counts — the opposite
direction of "a mutation arm proves sensitivity, never coverage." Ask both: where the effect is, does
the instrument fire; where it isn't, does it stay silent; and name which of the two has actual fixtures.

## Carried with the rulings (agent3's desk-close, three lines, author-named)
1. ADOPTION-INHERITANCE class, two instances by name: the chair adopted a belief containing a structure
   later measured absent (ee57960a corrected in 57 min by a zero-card test); agent3 credited a guard
   before it existed (caught by their own grep, 008f0152). Both fell to cheap no-boot checks — the
   argument for the checks, not the authors.
2. Review bar (operative): for any instrument offered as PROTECTION, demand the fixture where the effect
   IS PRESENT — five agreeing real bins are not teeth.
3. Window-kit ordering (operative): any slot or per-rank peak in a window leg prints ALIAS / PERIOD /
   ramp-check BEFORE anyone may dress it as a ring finding — the mistake agent3 made three times,
   turned into machinery so a new seat inherits the ordering instead of the error.

## Amendment (agent2's leg-1 flag, chair restated clause F above; witnesses corrected)
F's original ledger text described the PRE-write-set leg-11, which the same tree's a8c149ea
condemns — fixed above. Standing correction to citations at c1fa658c: Check (s) is **51-ok** at
that tree (the 53-ok figure belonged to unmerged 6c8f074b — a CHAIR stale pairing, the
number<->tree law now has a named chair instance alongside its three other-desk ones).