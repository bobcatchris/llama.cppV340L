# docs/amd/ INDEX — action-changing AMD-line docs (successor-facing)

**Why this file exists** (agent2 #758): tonight produced docs that CHANGE what a reader does, and a successor reading only PROGRESS/history would never meet them — `git grep -l 'v340l/2[0-2]'` over main returned 0 pointers. A doc whose effect depends on being found must be findable from the front door. Add a row here when you land one; cite-by-NAME per the ledger law.

| Doc | What it changes | Status |
|---|---|---|
| `BOOT_LAUNCH_RUNBOOK.md` | **How to boot**: dev0 is the DISPLAY card — never pair on it; all 4 dies measured topology-identical (weight-40/2-hop PCIE), clean pair = dev2,3; `HIP_VISIBLE_DEVICES=2,3` remaps to logical 0,1 so `--devices` stays `0,1`; `/home/chris/Desktop/…q3.ninfer` is TRUNCATED (6.6 GB, 740/1124 objects past EOF) — boot EMTEC256; kit `results/amd/p3/G3_bringup_kit.sh` v5 is the single-source minutes-path | CURRENT |
| `WO_VRAM_1_deconstant_preflight.md` (+ addendum `892891ee`) | Three banned-class refusal CONSTANTS on the auto-KV decision path (headroom 1024 MiB, throw-to-legacy 9059 fallback, kGateReliefBytes 1200 MiB); conformant order: basis-to-measured FIRST, then constants die with it; mandatory near-capacity LAUNCH cell | OPEN, owner agent4's seat |
| `v340l/20_ldmatrix_census_predicate_named.md` | Every site/file count must name its predicate ({128,140,152}×{40,42,44} dissolved — 128 retired) | SEALED |
| `v340l/21_shim_verifier_poison_check_is_textual.md` | Shim verifier's poison check WAS textual — chair-reproduced FUNC_MACRO mutation certified PASS on a poisoned header; CLOSED: main's tools/ops/verify_registered_exception.py now plants its own `#define __shfl_xor_sync…(val)` fixtures (:592/:621, arrived with the gate family, leg 2) | CLOSED, do not re-file |
| `v340l/22_merge_readiness_snapshot.md` | The eight registration rows are MAIN's (gemini never edited the array — its 17 IS the base); the one real leg-1 conflict was the add/add on agent5's spec; README stale-citation row (landed as lane fix `c2e6106b`) | mostly consumed by the merge wave; row added per agent5 #758's last open ask |
| `v340l/24_item7_decisive_cell_review.md` | Item-7 boot-design defect: an observation channel must be able to SEE the mechanism it is declared decisive for (A1TRACE-K reads flags; the tear theory lives in payload) — superseded-in-substance by the 5/5 adjudication, instrument-law stands | banked for the record |
| `v340l/25_lane_state_note.md` | Detail annex: verified loader facts (Binder::finish contract, zero dflash2 consumption sites, make_engine routing), gen-64 cell geometry + flatness rule, provenance settlements | ANNEX to v340l/26 |
| `v340l/26_session_debrief_at_relaunch.md` | **SUCCESSOR START HERE**: session close state — queue-(3) USER-TASK is the one live obligation (request template, validated flags, manifest/pgid/spawn-minute discipline, ceiling facts so no card time re-prices them); G-AMD-28 DO-NOT-REFIRE; disk 14 G and tightening; ~25 stale relays still arriving — byte-check before belief | CURRENT |
| `TRIPWIRE_GATE_CLASS_SPEC_agent5.md` | Contract-drift tripwire class spec, three-spelling final form (spelling 3 = declaration-site widening, UNCLOSEABLE by overload deletion; green-today leg; seal residual deferred) — merged branch-side-wholesale per the binding add/add instruction (agent5 #755, agent2 #758 re-verified at merge-tree); the only complete record of tonight's counting law | law-current |
| `TP4_DELTA_MAP_v1.md` | Phase-2 decided: **pure engine port** (artifact frozen); 81-tensor hazard inert by ROLE; four TRIVIAL-PARAMETERIZATION deltas named; transport deferred to real 4-card window | RULED |
| `ITEM6_decode_anomaly_agent5.md` | Perf item 6: the 17f decode cliff does NOT reproduce on merged tree (marginal 4.8 tok/s flat, 3-seat arithmetic); the two-row marginal test is standing law; §method pre-registers the 17g4/17g5 swap analysis | seat active (agent5) |
| `WO_TP4_all_lanes.md` | **The Phase-2/3 dispatch**: five work orders (A engine, B transport, C geometry, D gates, E budget), hard prereqs (TP2 consumed; item-7 adjudication BEFORE TP4 parity-gate authoring), one 4-card window shape | OPEN, all lanes |
| `T3_UNMERGED_OPEN_ITEMS_2026-09-13.md` | Historical: the boot-readiness sequence that merged — read for the laws, state is stale | HISTORICAL |
| `RESTART_2026-09-13/` | Per-lane cold-start kits — **kits are era snapshots; this index + COORDINATOR.md STATE are the current overlay** | as marked |
| `RESTART_2026-09-14/MORNING_HANDOFF.md` | **SUCCESSOR START HERE (all seven sessions incl. chair)**: morning queue items 1-5 with owners (A-4 enforce-flip, trace-OFF pair census settling the un-folded steady-lag skew, (b)-policy bin, K-ruling, 4-card window announcement); compressed standing laws; per-lane pointers; the honest open datum named separately from the closed carrier | CURRENT |
| `TP2_CLOSEOUT_PLAN.md` | The night's operational spine — bin queue (i)/(ii)/(iii) all resolved, reading table as code, warm-cache/renewed-harness instruments in use | CLOSED, read for lineage |
| `T3_I7_LMHEAD_REFUTAL_AND_CARRY_IN…md` + `T3_VOIDED_INFERENCE_INVENTORY…md` + `T3_I7_CONSUMER…` | Item-7 evidence chain: refutals (0-atomics sweep, split-k unreachable), the voided-inference inventory (absence claims name arming proof — the law that caught its own author twice), consumer read (caught-and-continued); all superseded-in-outcome by db09c924, all load-bearing as METHOD | HISTORICAL-law |
| `G18_WINDOW_RUNBOOK_agent1.md` + census reader `tools/census/read_census.sh` | Window pre-flight; the five-world reader, now **world-general** (universe census + presence-set semantics at world>2, per-log totality asserts) and era-aware on retry kinds (`CENSUS_STRICT_KINDS=1` refuses a kind-less line post-A-4, since `6b98623f` every emitter names its kind) | CURRENT-tools |
| `CLOSURE_BAR_AUDIT_agent5_2026-09-13.md` + `CLOSURE_BAR_AUDIT2_agent5_2026-09-13.md` | The bar itself applied to the board's own closures: both item-7 and transport triples CONFIRMED complete; three bugs found and closed (F-A class-blind retry split, F-C pair-shaped legs at world=4, F-D a gate leg calling 27% of a bin TOTAL — found by **agent2**, not by me), one filed (F-B: `tests/CMakeLists.txt:724` comment-swallow = a falsifier born unregistered), and a priced mechanism (absorbed retry ≈72-79 ms after in-bin drift control, inside the source's own 65-80 ms ceiling) | law-current |
| `AR_PARITY_ARM_design_agent5.md` + `CENSUS_RUNBOOK_bin_ii_agent5.md` (both ON main since `3a71ab4d`) | The arm's design + its pre-declared triggers (trigger-(i) evaluated-EMPTY at G18e; trigger-(ii) = first B-2-tier boot at world>2), and §7's closure bar the board quotes; the runbook's world-(b) sentence is RETRACTED in place (thread-local `rb n=` has no discriminating power — agent3/agent4) | CURRENT, arm unbuilt |
| ~~`AR_WRAP_TIMEOUT_design…`~~ | **NEVER LANDED anywhere** (`git grep -l AR_WRAP` on main = this file) — the row was written '(if landed)' and the hedge was honest; deleted as a pointer, kept as a record so a successor does not go looking. The work it named is live in `one_shot_allreduce.cu` bin (i)/(ii) + the AR-parity design's §5 boundary rows | GHOST, closed |

**The three laws a booting successor must not relearn the hard way**: VRAM LAW (allocator is the gate; measured-only; constant-refusals are defects to report, not walls — see WO-1); grant-before-device with ONE BOOT PER TRUTH (release rows for deaths included); re-derive-at-use (stamps are rentals; cite the object you can re-open).
