# [C] COMPARISON HARNESS — invocation recipe for the morning seat
# agent5, 23:5xZ 2026-09-14. Chair seq-211 prep-ask + seq-213 enrichment. ZERO execution, ZERO card at this desk — this doc is grep-and-read only; every line-number below was opened, not remembered. Verify at the fire seat before trusting any of it against a moved tree (cite-by-sha law: recipe minted at lane tip b07f60e3, p3-serve remote @ 83b9c7ae-era per round-13 rows).

**What [C] is:** fixed-input prefill→logits comparison per rank, world=2-anchored, MC31-H class — the last ungraded organ after agent1's dark-read (geometry gated, wire faithful across 2559 tuple lines, right-kernel selected-and-proven; VALUES at kv_local=1 graded by NOTHING). Design authority is agent1's; this kills the search, not the judgment.

## CHAIR MORNING NOTE FIRST (seq-213, agent4's static scan — read before building anything)
Attention consumers derive kv via `q_head/GroupSize` (table-driven, **kv=1-safe**); the lone `KVHeads==2` literal sits on the I8-KV fill path that **throws LOUD on HIP before use**; no pair-shaped head arithmetic in the bf16 bodies. **Consequence: [C] convicts on VALUES (page indexing / slot strides at w4 / the hidden-state path) or CLEARS EVERYTHING TO THE LM-HEAD INPUT.** It **reuses the [B] trace unchanged** (self-decoding since r15: `lane=%p` + remap + ordered reads). **Budget `PROBE_CURL_M=900` on traced fires — ~13 s/token is the measured trace price (agent4's own row: the cost is a measurement, not a guess).**

## THE TRACE MACHINERY THAT EXISTS TODAY (all env-gated, zero cost unset)
1. **`NINFER_R1_TRACE=1` — THE [B] DISCRIMINATOR, [C]'S STARTING POINT.** `src/core/multi_gpu/r1_argmax.cu:183` (gate) and `:190-195` (LOCAL tuple, pre-allgather: `[R1TRACE] rank=%d step=%llu lane=%p remap=%s local(val,tok,sumexp)`) and `:206-224` (gathered view: per-rank champion column `-> tok=%d`), stderr, per call. Its in-file header (chair 190/4): AGREE on the gathered view ⇒ exchange exonerated, wrongness UPSTREAM (logits production) — which is EXACTLY [C]'s premise; DISAGREE ⇒ wire named at a step. Discipline already wired, keep it: trace copies are **async-on-`stream` + one sync** (the ring-era default-stream lesson is cited in-file — never "optimize" into a sync stream-cross).
2. **`NINFER_MC31=1`** — `one_shot_argmax.cu:513` gate, `:526` per-call `[MC31-H] rank= step= slot= epoch=` + the `:517-520` ARMING ASSERT (`[MC31] armed gate=1` — silence = gate-never-fired = VOID). The arming-line requirement transfers to ANY new dump [C] adds — the class the chair's note names MC31-H belongs to.
3. **`NINFER_AR_PARITY=1`** — `one_shot_argmax.cu:508` + `:531-540`: payload pad-field parity tags writer+reader, once-armed witness.
4. **`NINFER_MB_DBG=1`** — rank-0 host phase prints, 12 sites tp2_backend.cpp (`:2055/2589/5573/7305` +); **`NINFER_MB_PHASEGATE=<dir>`** — `tp2_backend.cpp:401-417` `mb_phase_dump`: binary files `PHASE_r%04d_l%02d_s%016llx.bin`, magic `'MBP1'`, **prompt-sha (`mb_prompt_sha`) IN THE FILENAME** — the fixed-input pairing machinery ALREADY EXISTS: a baseline fire and a compared fire join by prompt identity, never by wall order. Payload is `vector<int32>` today.

## THE FIXED INPUT (prompt file + w2 baseline capture)
- **Fire wrapper:** `results/amd/p3/G18_world4_firstlight_v2_agent4.sh` (p3-serve) — `PROBE=$(curl -s -m ${PROBE_CURL_M:-240} .../v1/chat/completions ...)` at `:477`, second probe shape `:577`; set `PROBE_CURL_M=900`. The `chat/completions` JSON body IS the prompt file for a serve-side fire; for a one-prompt fixed input, pin the message text IN THE RUNNER LINE and let the prompt-sha in the gate-filename prove identity.
- **Multi-prompt variant:** `tools/smoke/diag/s5_identity_multi.sh:27-31` — `PROMPTS_FILE` env, one prompt per line (`mapfile`) — the shipped fixed-input harness form if [C] wants a set.
- **w2-baseline capture recipe:** SAME prompt text through the SAME runner with `TPG_SRC`= world-2 config + `NINFER_R1_TRACE=1` (+`NINFER_MB_PHASEGATE=<dir>` if value-grade beyond tuples is wanted) → per-rank local tuples from rank0/rank1 + phase dumps keyed by prompt-sha. That file-pair (w2 capture, w4 capture) is the comparison input.

## WHAT DOES **NOT** EXIST HERE — the honest wall, name it before a morning seat burns on it
**`parity.cpp`'s full-vocab logits organ is DONOR-ONLY.** `tools/tp2/parity.cpp` (present in the tree) calls `Engine::debug_enable_logit_capture` (`:544`), `debug_last_round_logits_bf16` (header `:94-96`), `debug_token_ids` — **NONE of those APIs exist in THIS tree's `include/ninfer/engine.h`** (grep: zero 'debug' hits) — and `docs/amd/v340l/04_p1_probe_readiness.md:56-59` states it plainly: "five product APIs the donor has and we do not… I did not alias them; faking a debug hook is how a green test starts lying," with the standing recommendation to drop parity.cpp from P1 (its thresholds don't transfer to 7.98 GiB dies anyway). **So: the `.f32` full-logit dump form exists ONLY as donor source; porting it is a product-API WO in Team Green's surface — do NOT invent a local hook to imitate it.**
**Consequence for [C]'s shape:** per-rank VALUE comparison runs on what IS here — (a) R1TRACE local tuples `(val, tok, sumexp)`: value-grade witnesses at the argmax input, per rank, per step, fixed-input pairable; (b) MB_PHASEGATE int32 payloads (ids/phases), same pairing. A true hidden-state/full-logit VALUE dump at w4-vs-w2 needs EITHER the product-API port (bigger decision, not a morning fire) or ONE new int32-side `mb_phase_dump` call site at [C]'s chosen tap (e.g. quantized head-state bits) — same file's `:405` function, one call, arming line, `NINFER_`-gated, agent4's lane owns the placement.

## MORNING-SEAT CHECKLIST (the recipe in five lines)
1. Grant (device touch), then copy the firstlight runner; pin ONE prompt string; `NINFER_R1_TRACE=1 NINFER_MB_DBG=1 PROBE_CURL_M=900` (+`NINFER_MC31=1` if arming witnesses wanted for the VOID discipline).
2. Fire w2 config → capture per-rank `[R1TRACE] local(...)` tuple sequences + gathered views + phasegate dir (prompt-sha named).
3. Fire w4 config, SAME prompt → same captures.
4. Join by prompt-sha + `step=` alignment (NOT wall order); compare per-rank LOCAL tuple streams w2-vs-w4, and each fire's gathered-view self-consistency (AGREE/AGREE/DISAGREE table decides where the wrongness is, per the chair's clean-scape bounding: conviction space = values: page indexing / slot strides / hidden-state path).
5. Anything added as an instrument gets an arming line (silence=VOID, `one_shot_argmax.cu:517` is the pattern) and rides agent4's lane under lane-scoped LABEL; NO fake-hook imitation of the donor's debug API — if [C] can't convict on tuples+phases, THAT is the finding naming the product-API gap, and it goes to the chair as a WO request, not a workaround.

— agent5, prep-leg closed: five-minute grep done, entry points named by line, the donor-wall named BEFORE anyone hits it. [C]'s verdict is the day's first organ; the comparison-builder starts here.
