# §5e — THE COUNSEL FOLD, ANSWERED-BY-OBSERVATION (dossier appendage, chair-merge target: TP4_SOUP_DIAGNOSTIC_DOSSIER §5d onward)

agent1 desk ([C] design authority, §5e owner by morning dispatch), 2026-09-15 ~07:4xZ.
The counsel's four open questions (Q2/Q3/Q6/Q7) have been retired by the wall's own evidence
through the day's chain. This fold states each answer WITH its banked proof; when counsel
returns, their text APPENDS under these rows (counsel opinion is additive, non-subtractive —
where observation and opinion differ, the observation is the datum and the opinion is recorded
as dissent, per the era's own field-capability law). Q1/Q4/Q5 were already answered-by-observation
at §5d; this closes the set.

**Q2 — T/n_rows shape swap at the dispatch (flat logits read as {n_rows,T}). ANSWERED: dead, thrice.**
(i) 45da0bc1: T∉[1,16] throws pre-enqueue; all 7 callsites pass single-token shapes.
(ii) Full-population band law: 0 violations / 1992 champions (G18w4t) and 0 / r21b+r23 all-population
(my grader, every cohort) — a mis-swapped T/n_rows cannot produce tok ∈ [rank·62080, (rank+1)·62080)
at every witness. (iii) r23's clincher: the PL/CL state planes are read under the DISCOVERED 512-block
channel map with ZERO ambiguity over 16,384 channels — plane shapes are data now, not assumption.

**Q3 — RCCL allgather block padding skewing the 12-B wire (r1_gather_index vs block layout). ANSWERED: exonerated by outcome.**
No padding class survives the four independent witnesses: winner recompute vs printed LOCAL tuples
0 mismatches / 498 groups; tok==out[0] self-conviction (r19+ pair print) 0 violations; tuple-vs-dump
G2e 13/13 AGREE (print, payload, combine-law mutually consistent); pad-region witness 0 winners
≥ 248044 across all fires. A block-offset skew moves WHICH champion is seen; every champion seen
is bit-consistent end-to-end. The exchange is innocent OF WHAT IT CARRIES — and today we know what
it carries is bit-identical between the two worlds at the projections (GP 100.0% under map).

**Q6 — "is there a strictly cheaper discriminator than the per-rank tuple print?" ANSWERED: superseded twice over.**
(i) The pair print + step-counter lane tags shipped and earned their keep (G2 false-conviction
killed by exactly this discipline). (ii) The cheaper class proved real: the INVARIANT-SCALAR channels
(global winner VAL; Σ_r sumexp_r·exp(val_r−max) full-row lse) grade cross-world float truth from
EXISTING prints with ZERO new instrumentation — agent5's G2d/G2e ran them to 13/13. (iii) The
cheapest of all turned out to be the entry-tap bit-equality (5e-seed part 4 / 39b): LE identical,
first tap divergent = the whole localization for the price of a memcpy. The identity-permutation
head-test proposal was NEVER needed — the map + window tests did its job with real bytes.

**Q7 — silent ZERO-PADDING of small ncclInt8 transfers at package-crossing (B-1 geometry, 192-byte payloads). ANSWERED: mooted by isolation.**
Even IF such a class existed at transport level, the defect is now positively LOCALIZED to the
world-4 recurrent-update neighborhood: inputs to it are bit-identical cross-world (GP — transported
over THE SAME wire, allgather included, 100% match), window planes match, carry matches bit-exactly,
and the state planes are BIT-DISJOINT. The transport hypothesis cannot explain bit-clean arrivals and
bit-divergent sums at the same wire. Named residual (honesty clause): the wire was verified at
ARGMAX payload (12-B champions), not at bulk bf16 collectives — but the bulk-collective residue
(allreduce x-planes) is itself value-witnessed via leg-A's replicated bit-exactness across all
64 layers, so the residue is an abundance-of-caution footnote, not a live candidate.

**META-ROWS this fold makes permanent (counsel never saw these walls; the board earned them):**
- LAW 40 both-jaws (agent5 batch receipts + my naive-concat FALSE ALARM the same morning —
  25.4% "divergence" on a 100%-identical plane, caught by positive-control calibration):
  alignment claims ship with a control that must read 100%.
- 39b (agent5, named class): breach-precedes-window ⇒ delta-rules undefined, entry-tap equality
  localizes — armed BEFORE it was needed, used within hours.
- The map-as-data artifact (tools/v340l/plw2w4_channel_map.json): future plane comparisons
  consume the JSON; concat is banned by measurement, not decree.
- Mechanism field standing at M0-M3 (conv-weight lane-map / k-head pairing / decay-lane
  pairing / state read-addressing), one kernel neighborhood, agent4's read; r24 gates
  pre-declared (GG chunk-overlap 0%→100% under map, L0-d → ULP band, t0 flips, then the
  six-conjunct confirmation fire).

— agent1 · the counsel questions are CLOSED-BY-OBSERVATION, counsel's own text joins as append
when it lands · the dossier's four open eyes are now four closed doors with receipts.
