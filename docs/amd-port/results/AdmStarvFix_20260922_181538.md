# Admission starvation fix (E-090 desk): served validation window

Date: 2026-09-22. Lane 8083, worktree wt-server-fixes, branch amd/server-fixes
(synced to campaign HEAD 4ab107998 via fast-forward merge of amd/v340-port-v2
at d98e8cdcb, fix on top). Build: build-hip rebuilt in the worktree from this
branch (llama-server version 213, d98e8cdcb + the E-090 fix). Boot lock
/tmp/campaign_gpu_boot.lock held per protocol (desk name + 150 s cadence),
released at the window's teardown; the t3-copyback desk took the lane after.

Boot line: campaign in-split default with 10k-class context (HIP_VISIBLE_
DEVICES=0,1,2, --device ROCm0,ROCm1,ROCm2, -ngl 999 -sm tensor, -c 10240,
-b 512 -ub 512, q4_0 KV, -fa on, --spec-type draft-mtp, port 8083, -t 8).
kv unified verified per boot: "initializing, n_slots = 4, n_ctx_slot = 10240,
kv_unified = 'true'". Runner: docs/amd-port/tests/admfix_window.sh.

Geometry: probe_kv_admission.py, two ~7.9k prompts fired concurrently
(p2 = p1 + a 5-token suffix), n_predict 16, cache_prompt false. Arrival order
this window: task 0 = p2 (7862 tokens) admitted on slot 3; task 2 = p1
(7857 tokens) deferred.

## 1. Fixed build (kv-admission on, default) - PASS

  request A: http 200 in 160.8s
  request B: http 200 in 80.1s
  log: defer-lines=1 exceeded-lines=0 http500~=0
  FIX PASS: both 200, zero exceeded, admission defer engaged

Engagement, verbatim (mm.ss.ms timestamps):

  0.23.794.077 W unified KV occupancy is too high, defer task 2 (n_need = 7857, n_used = 512, n_pending = 7350, n_ctx = 10240)
  1.40.151.733 I slot release: id  3 | task 0 | stop processing: n_tokens = 7877, truncated = 0
  1.40.151.745 I slot get_availabl: id  3 | task -1 | selected slot by LCP similarity, sim_best = 1.000 (> 0.100 thold), f_keep = 0.997
  1.40.151.763 I slot launch_slot_: id  3 | task 2 | processing task, is_child = 0

The E-088 starvation is gone: the deferred task re-admitted 30 ms after the
incumbent released (E-088: deferred forever, cancelled at the probe's 600 s
timeout). Deferral engaged exactly once while the incumbent was in flight
(the E-054 guarantee: n_pending = 7350 reserved), then the released slot's
retained cache (7877 cells) was selected by LCP similarity 1.000 and the
re-admission was decided on the INCREMENTAL need (credit: 7857 cached prefix
tokens of the 7857-token task -> incremental ~0 against 2363 free cells).
No purge, no eviction - the prefix was left intact for reuse.

Because the probe disables cache_prompt, the slot drops the retained prefix
at launch (keep_first(0)) and re-prefills the full 7857 (80 s at 99.5 t/s);
the freed cells cover the credit - the conservative case in the design.
Total: 1 defer line, zero "Context size has been exceeded", zero HTTP 500,
both requests 200 (80.1 s and 160.8 s - serialized exactly as expected;
160.8 = 80 s defer + 80 s own prefill).

## 2. Control arm (--no-kv-admission) - legacy signature is now a hard abort

Ran per brief ("run the --control arm if safe"). The legacy build did NOT
reproduce the E-037/E-038 textbook signature ("Context size has been
exceeded" retries / clean HTTP 500s). It aborted the whole server:

  2.15.552.279 W decode: failed to find free space in the KV cache, retrying with smaller batch size, off = 0, n_batch = 256, ret = 1
  ... halving 128, 64, 32, 16, 8, 4, then:
  2.15.553.468 W decode: failed to find free space in the KV cache, retrying with smaller batch size, off = 0, n_batch = 2, ret = 1
  tools/server/server-context.cpp:365: GGML_ASSERT(task) failed
  E send_error: task id = 0, error: got exception: speculative batch index 2 is not inside the current sub-batch [0, 2)
  ggml_abort <- server_context_impl::decode <- update_slots <- start_loop (backtrace in the log)

  request A: http 500 in 115.7s
  request B: http 0 in 117.1s (connection dropped - server dead)
  log: defer-lines=0 exceeded-lines=0 http500~=0
  CONTROL FAIL: unexpected signature (probe expects the 500/exceeded class)

Reading: with admission off, the overcommit drives the unified KV to zero
free cells during decode; the retry loop halves n_batch to 2, and the abort
fires in the slot bookkeeping (need_embd's GGML_ASSERT(task), line 365) with
a draft-mtp batch error on the way out. On today's HEAD the E-054 admission
gate is the only thing between this served geometry and a server-killing
abort - the legacy knob is not a safe escape hatch for concurrent ~8k
prompts. Root-causing the abort itself is OUT of this desk's scope
(default path never reaches it; recorded as evidence for the ledger).

The probe's CONTROL verdict line prints FAIL because the observed signature
(hard abort) is not the expected 500/exceeded class; the fixed-build gate of
this window is unaffected. A third boot (control without draft-mtp, hunting
the textbook signature) was NOT run: the t3-copyback desk holds the lane
from here (lock law).

## 3. Host tests (zero GPU, all green)

- test_server_exposures_host.cpp EXTENDED (docs/amd-port/tests/): new
  sections 9-11 mirror the served geometry at the exact cells - the E-088
  defect arithmetic (fits(7862, 10240, 7872, 0) == false, the starvation),
  the fix (kv_cells_needed(7857, 7862) == 5 -> admits with the prefix
  intact, no eviction), the E-054 counter-cases (in-flight incumbent: both
  the suffixed B and a NON-overlapping C must still defer, verbatim
  n_used = 512 / n_pending = 7345 numbers), and the last-relief eviction
  (mid-divergence task: credit alone cannot admit -> candidate cache evicted
  to the prompt cache only when the full task then fits; still defers, cache
  untouched, when even that is not enough). Existing sections updated to the
  unconditional prefix credit (the old cache_prompt = false -> no-credit
  check WAS the E-090 defect). ALL PASS.
- All existing host suites re-run green on this branch:
  async_input, mtp_sampling, packed_get, pinned_staging, prefix_catchup,
  t3_detect, verify_row_sampling, q81_cache, w6_isolation, w6_mtp_device,
  t3_alias.

## Verdicts of record

1. E-090 fix VALIDATED SERVED: deferred task re-admits immediately after the
   incumbent releases (30 ms), admission engages exactly once with the
   E-054 reservation intact, both requests 200, zero exceeded, zero 500s.
   Probe FIX PASS.
2. The credit is conservative by design: with cache_prompt = false the
   server re-prefills (80 s) - correct, just not faster; with cache_prompt
   = true the retained prefix is reused and the re-prefill collapses to the
   incremental cells (host-test level).
3. Control arm: legacy (admission off) now aborts the server (GGML_ASSERT
   + draft-mtp batch error after KV-full halving to n_batch = 2) instead of
   the historical 500 signature - captured verbatim, out of desk scope.

## Files

- Receipt: this file; boot + probe logs admfix_fixed_20260922_181538.log and
  admfix_control_20260922_181538.log (probe output appended to each).
- Runner: docs/amd-port/tests/admfix_window.sh (lock protocol: check-and-hold,
  desk name, 150 s cadence).
- Host test: docs/amd-port/tests/test_server_exposures_host.cpp (sections 9-11).
- Ledger: E-090 appended to docs/amd-port/OPTIMIZATION_PLAN_TP3_200K.md.
- Boot lock: held under "desk=ADMISSION STARVATION FIX", released at window
  teardown; lane 8083 handed to the t3-copyback desk.
