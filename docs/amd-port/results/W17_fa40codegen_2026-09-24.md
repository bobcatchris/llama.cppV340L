W17 FA40-CODEGEN DESK RECEIPT - 2026-09-24
branch amd/fa40-codegen (wt-fa40-cg), resumed at 4b94f907f (dispatch 4;
dispatch 3 crashed 06:45 after committing its inherit, measurements lost)

MISSION: land the q4_0-direct attention KV tile path by routing around the
hipcc/clang-18 -O3 miscompile. At 200k KV depth attention is ~386 ms of the
~503 ms round (W15); a correct q4_0-direct tile path moves 5.1x fewer DRAM
bytes per tile load.

BUILD (A2):
- build-bench was crash-orphaned: 52 ZERO-BYTE .o files dated 07:03 (crashed
  dispatch 3 build) made libggml-hip.so link "successfully" without
  ggml_backend_cuda_reg/init (make saw the empty objects as up-to-date;
  examples/simple link failed as the visible symptom). Deleted the 52 empty
  objects + stale .so, rebuilt ggml-hip target: BUILD-EXIT:0 (77 MB lib).
  LESSON: after a crashed build, check for -size 0 objects before trusting
  an incremental rebuild.
- bench_attn_real.cu compiled with the canonical hipcc flags (W11 recipe),
  two binaries: /tmp/fa40_bench (clean, timing) and /tmp/fa40_bench_dbg
  (-DGGML_FATTN_Q40_DBG, staged-oracle dumps). CC-EXIT:0 both.

ORACLE (A3), die 3 (PCI 0000:0d:00.0 = card0, HIP_VISIBLE_DEVICES=3),
lock-arbitrated, one short process per invocation, GGUF
Qwen3.8-27B-ASCII-P1M, dst memcmp vs the f16-KV base arm (6144 f32 els):

| variant  | shape                            | d=7168   | d=10240  | d=65536  | d=199936 |
|----------|----------------------------------|----------|----------|----------|----------|
| v1_ctl   | as-of-W16, 8B shared stores      | DUST     | DUST     | DUST     | DUST     |
| v2_c1c   | noinline dequant block           | DUST     | DUST     | DUST     | DUST     |
| v3_c1d   | volatile LDS reads               | DUST     | DUST     | DUST     | DUST     |
| v4_nlm   | noinline mad chain               | DUST     | DUST     | DUST     | DUST     |
| v6_hk    | half-width K chunks              | DUST     | DUST     | DUST     | DUST     |
| v7_lb    | launch bounds (256,1)            | DUST     | DUST     | DUST     | DUST     |
| v8_u1    | unroll-1 dot chain               | DUST     | DUST     | DUST     | DUST     |
| v9_optno | optnone diagnostic               | DUST     | DUST     | DUST     | DUST     |
| v10_st16 | 16B shared-tile stores           | DUST     | DUST     | DUST     | DUST     |
| v11_stsc | SCALAR half2 shared-tile stores  | BIT-EXACT| BIT-EXACT| BIT-EXACT| BIT-EXACT|

- DUST = 6144/6144 els differ (complete garbage, not numeric dust).
- STAGED (dbg build, GGML_FATTN_Q40_DBG=1, d=7168): every failing variant
  diverges at the FIRST stage (KT-X), i.e. the shared K tile content is
  already garbage right after the load, before the dot chain. v11 staged:
  KT=ok VT=ok KQ=ok (identical K tile, V tile and KQ accumulator vs base).
- basedup in-run determinism control passed every session.
- READING: the miscompile is localized to the ggml_cuda_memcpy_1<8>
  (8-byte) shared-tile STORE granule in flash_attn_tile_load_tile_q40;
  replacing it with scalar half2 stores (v11) fixes bit-exactness at every
  depth. Store width, not dequant math, not dot chain.

SERVED-ENTRY (library instance, rebuilt libggml-hip from this tree):
- GGML_CUDA_FATTN_TILE_Q40_DIRECT=1: gate INFO line appears, launch clean.
  The library instantiates flash_attn_tile<256,256,4,2,softcap,11> from the
  same fattn-tile.cuh / hipcc / -O3 the bench TU proved bit-exact above.
- env unset: zero gate-line occurrences (negative control).

TIMING (A4), --solo arms (base = f16-KV in-arm control, v11 = q4_0-direct,
basedup determinism control; niter=30, rep 0 discarded; one process per
depth; die 3 lock-arbitrated):

| depth  | base med us | v11 med us | delta   | windows | note                    |
|--------|-------------|------------|---------|---------|-------------------------|
|  7168  |   884.0     |   552.4    | -37.5%  | 8       | serve depth             |
| 10240  |  1085.8     |   661.0    | -39.1%  | 3       |                         |
| 10240  |  1245.4     |   740.5    | -40.6%  | 8       | rerun, die hotter       |
| 65536  |  6542.9     |  4021.8    | -38.5%  | 3       | spread 0.05-0.8%        |
| 199936 | 26904.9     | 16310.9    | -39.4%  | 3       | min-to-min -45.5%       |
| 199936 | 27893.5     | 18213.2    | -34.7%  | 8       | rerun                   |

GB/s of the direct arm's KV stream (2 x ne11 x 72 B / launch):
  7168: 1.87 GB/s   10240: 2.23   65536: 2.35   199936: 1.76
The instance is NOT DRAM-bound at this geometry (T=4, 1 KV head, few
warps): the fattn KQ loop is latency/serial-bound, so the modeled 5.1x
tile-load byte saving lands as ~35-40% wall time, roughly flat in depth,
NOT 5.1x time. Byte accounting per launch is by construction:
  f16-pool base: 2*72n read + 2*(512n write + 512n read) = 2192n B
  q4_0-direct v11: 2*72n = 144n B (15.2x fewer bytes through DRAM).

MEASUREMENT CAVEATS:
- Die 3 was thermally hot by the late sessions (junction 85 C, mem 88 C,
  rocm-smi right after the 200k rerun). Absolute medians drift +12-15%
  between sessions (10k base 1085.8 -> 1245.4 us); arm RATIOS are stable
  in every single window: every v11 window beat every base window at every
  depth. basedup matched base within +-1.5% throughout (determinism).
- At 200k the W11 1.05% spread law is unattainable in this thermal state
  (base 3.5-15.6% spread, v11 25-26% with a downward ramp across windows);
  medians still land the delta in the -35..-39% band, consistent with the
  tight 64k session.

VERDICT / RECOMMENDATION:
- v11 (scalar half2 shared-tile stores) is the workaround: bit-exact vs the
  f16-KV reference at 7168/10240/65536/199936, staged K tile + V tile +
  KQ_acc identical, and -35..-40% per-launch time vs the served f16-KV path
  at all depths. The library decode arm (env-gated
  GGML_CUDA_FATTN_TILE_Q40_DIRECT) already instantiates var 11 (commit
  4b94f907f); these measurements validate that choice.
- RECOMMENDED: keep var 11 as the served q4_0-direct decode arm for the
  served-window trial (env-gated as committed; serve-depth saving -37.5%,
  200k -35..-39% of the 386 ms attention class is the prize).
- Stop sweeping the miscompile: all 8B-store codegen shapes (noinline,
  volatile, launch bounds, unroll, half-K, optnone, 16B stores) fail
  identically at the K-tile stage; scalar stores are the only exact shape
  found and no further variant is warranted.
- OPEN for the integration desk: thermal-state spread at 200k needs the
  quiet-boot window (svm-hog counter) for round-level confirmation; the
  per-instance bit-exactness is settled.
