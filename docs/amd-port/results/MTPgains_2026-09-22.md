# MTP-gains SERVED desk: E-065 deferred validation protocol executed

Date: 2026-09-22. Lane 8083 exclusive, main tree (amd/v340-port-v2 @ d6a1ae66e),
post-reboot clean silicon, stock clocks, cooldown-gated before every boot
(hottest serving die junc < 60 C; waits at 150 s cadence). Measurement + docs
only - no code changes. Boot lock /tmp/campaign_gpu_boot.lock held across the
whole ladder. Campaign boot line throughout; no OD/cap writes.

Arms (exact boot logs under docs/amd-port/results/):

| arm | env (beyond campaign line)          | boot log                       |
|-----|-------------------------------------|--------------------------------|
| T1  | LLAMA_SPEC_TIMELINE=1 LLAMA_DECODE_TIMELINE=1 | mtpgain_T1_20260922_130900.log |
| F1  | T1 env + LLAMA_DECODE_TIMELINE=1, -lv 4 | mtpgain_F1_20260922_133446.log |
| F2  | F1 env + LLAMA_DRAFT_FAST_TOPK=1    | mtpgain_F2_20260922_134057.log |
| F3  | = F1 (unset rep 2)                  | mtpgain_F3_20260922_134819.log |
| F4  | = F2 (set rep 2)                    | mtpgain_F4_20260922_135525.log |
| G1  | LLAMA_TP_BACKEND_SAMPLING=1 + timelines, -lv 4 | mtpgain_G1_20260922_140332.log |
| V1  | + --spec-mtp-device ROCm3, HIP_VISIBLE_DEVICES=0,1,2,3 | mtpgain_V1_20260922_141027.log |

F1-F4 all carry LLAMA_SPEC_TIMELINE=1 (ladder requirement) plus
LLAMA_DECODE_TIMELINE=1 and -lv 4 so the per-ubatch/drain lines are captured
identically in every arm (library INFO maps to LOG_LEVEL_TRACE=4 in
common/log.cpp common_get_verbosity - suppressed at the default verbosity 3;
that is why T1 shows no [decode-timeline] lines). Interleave order: F1 (unset)
-> F2 (set) -> F3 (unset) -> F4 (set).

Launch errors kept for the record: mtpgain_F1_20260922_133154.log (-lv 4
single-token arg quote bug, relaunched), mtpgain_V1_20260922_140720.log
(--spec-mtp-device ROCm3 invalid while HIP_VISIBLE_DEVICES=0,1,2 - the draft
die must be visible: HVD=0,1,2,3, then ROCm3 resolves).

## 1. Timeline attribution (T1, the key cell)

5 decode-guard-class requests (7857-token probe, n_predict 128, temp 0):
1 battery cell + 4 manual probes = 210 draft rounds (42/request, steps = 3
every round, accept 0.66667 everywhere).

Battery cell: 14.90 t/s PASS. Manual probes: 14.878 / 14.929 / 14.472 /
14.636 t/s, accept 0.66667 each (mtpgain_T1_20260922_probes.jsonl).

Per-step numbers over 210 [spec-timeline] draft rounds (steps = 3 in all):

  decode_issue  ms/step : mean 2.553  med 2.509  p10 2.398  p90 2.639
  sample+batch  ms/step : mean 1.132  med 1.123  p10 1.045  p90 1.200
  round total   ms      : mean 11.053 med 10.918 p10 10.407 p90 11.581
  wall ms/round (line deltas): med 195.2 p10 191.1 p90 215.9

Deep attribution from F1 (same config, -lv 4, 84 rounds):

  target verify ubatch (n_tokens = 4), n=170:
    build 0.032 ms, inputs 0.027 ms, issue mean 85.3 MED 138.1 (p10 8.5, p90 166.9)
  draft ubatch (n_tokens = 1), n=254:
    build 0.009 ms, inputs 0.010 ms, issue mean 2.72 med 2.28
  [decode-timeline] drain (synchronize), n=11677: mean 0.041 ms - NOTHING
    pending at sync; the device wait sits INSIDE graph_compute (blocking).
  spec process (verify-class) issue: med 13.9 ms
  round wall anatomy (med 190 ms): verify-batch device ~85-138 + draft loop
    ~11 + host sampling/batch ~40-55.

VERDICT (E-065 rule): sample+batch 1.13 ms/step is far below the 5 ms
threshold => the CPU chain is minor; the overhead is device-side. STRONGER
revision: the old "37 ms/draft-step" model was misattributed - the whole
3-step draft loop costs 11 ms/round (draft graph ~2.3-2.7 ms/step device +
~1.1 ms/step CPU). The round is owned by the TARGET VERIFY decode: a
blocking ~85-138 ms device round trip for the 4-token batch (3-die pipeline
+ allreduce latency at 4 tokens), plus ~40-55 ms host per round. The
drain/relaunch attack must aim at the verify-batch device critical path and
the per-round host slice, not the draft steps. The 21.4-24.8 t/s class needs
round = 121-140 ms; from today's ~190 ms that is verify -> ~90 ms and host ->
~20 ms, draft already fine.

## 2. FAST_TOPK interleaved 2x2 (all cells decode-only guard + determinism)

| arm | LLAMA_DRAFT_FAST_TOPK | decode t/s | accept   | sample+batch ms/step | round total ms |
|-----|-----------------------|-----------|----------|----------------------|----------------|
| F1  | unset                 | 14.93     | 0.66667  | 1.146 (1.09-1.20)    | 11.036         |
| F2  | SET                   | 14.91     | 0.66667  | 0.960 (0.91-1.00)    | 10.278         |
| F3  | unset                 | 14.94     | 0.66667  | 1.116 (1.05-1.18)    | 10.735         |
| F4  | SET                   | 15.02     | 0.66667  | 0.972 (0.93-1.01)    | 10.269         |

- ENGAGEMENT GATE (a): PASS. sample+batch drops 1.131 -> 0.966 ms/step
  (unset mean -> set mean, -15%) in both set reps; round total 10.9 ->
  10.27 ms. The env provably engaged.
- ACCEPT GATE (b): 0.66667 / mean_len 3.0 / draft 126-84 in every cell
  (jsonl receipts mtpgain_F*_*_battery.jsonl).
- DETERMINISM (c): within-arm byte-identical 2-run greedy AND the same
  sha256 across all four arms (4beb1ba25219ee9b, 312 chars) - fast-topk is
  byte-exact vs the regular path on this model at temp 0.
- t/s delta: +0.2% mean (14.965 vs 14.935), inside run noise. The modeled
  +1-3% assumed a bigger CPU slice; the removed work is ~0.66 ms of an
  ~190 ms round (~0.35%). Verdict: fast-topk is a free, byte-exact CPU-chain
  reduction but is NOT a decode t/s lever at 200k; its value shows only if
  the round shrinks (host share grows relatively) or at smaller contexts.

## 3. Arm G: backend-sampling abort capture (not a perf arm)

Boot WARN signatures (G1 log lines 248-249):

  W set_sampler: experimental backend sampling under SPLIT_MODE_TENSOR enabled via LLAMA_TP_BACKEND_SAMPLING
  W set_sampler: sampler ops on vocab-sharded logits are not modeled by the meta split machinery; aborts are likely

One decode request aborts at the FIRST draft-round process():

  ggml/src/ggml-backend-meta.cpp:814: GGML_ASSERT(src_ss[i].axis != GGML_BACKEND_SPLIT_AXIS_UNKNOWN) failed

stack: ggml_abort <- ggml_backend_meta_get_split_state <- ... <-
common_speculative_impl_draft_mtp::process <- server update_slots. The first
host GET of a vocab-axis-sharded tensor (logits/h rows for sampling) walks
into the un-modeled split state and asserts - empirically confirming the
MTP_overhead desk's static map: on-device sampling needs head re-shard or a
per-shard top-k op (subsystem project). Controlled GGML_ABORT, not a driver
event: dmesg shows NO libamdhip64 segfault at the event and 4/4 dies
enumerated for the V1 boot immediately after.

## 4. v1 VRAM capture at 200k (completes the E-070 VRAM column)

V1: campaign line + --spec-mtp-device ROCm3, LLAMA_SPEC_MTP_STRICT unset,
mode line verified at boot: "draft-device mode: partial (v1)" (V1 log line 7).
One decode probe: 14.935 t/s, accept 0.66667 - perf parity with in-split
(matches E-069/E-070). VRAM via /sys/class/drm/card{0,1,3,4}/device/
mem_info_vram_used (map: HIP0=card1, HIP1=card3, HIP2=card0, HIP3=card4;
8176.0 MiB total per die). Receipts: mtpgain_vram_insplt_T1_* (in-split,
from T1) and mtpgain_vram_v1_V1_* (v1).

| die  | in-split boot | in-split post-probe | in-split free | v1 boot | v1 post-probe | v1 free   |
|------|---------------|---------------------|---------------|---------|---------------|-----------|
| HIP0 | 7809.7        | 8166.5              | 9.5           | 7074.8  | 7675.4        | 500.6     |
| HIP1 | 7591.8        | 8069.9              | 106.1         | 7052.9  | 7605.6        | 570.4     |
| HIP2 | 7643.9        | 8122.0              | 54.0          | 7105.0  | 7665.5        | 510.5     |
| HIP3 | 18.0          | 18.0                | 8158.0        | 1440.8  | 2320.8        | 5855.2    |

Exact values live in the json receipts; the table rounds nothing.

VRAM VERDICT: v1 saves ~456-491 MiB on EVERY serving die post-probe
(free 500-570 MiB/die vs 9.5-106 MiB/die in-split) for zero decode cost;
the draft die carries 2.32 GiB post-probe with 5.86 GiB still free.
In-split at 200k post-probe has 9.5 MiB free on HIP0 - one long prompt from
OOM. On VRAM grounds the in-split-vs-v1 operating point CLOSES in favor of
v1 (E-069 item 3 resolved): v1 is the safer serving default at 200k,
identical t/s and acceptance.

## Verdicts of record

1. ATTRIBUTION: device-side owns the overhead - but it is the TARGET VERIFY
   batch (~85-138 ms blocking device round trip per round), not the draft
   steps (11 ms/round total). CPU sample+batch is 1.1 ms/step. The
   drain-attack desk should attack verify-batch device critical path
   (pipeline/allreduce latency at 4 tokens) + the ~40-55 ms host slice.
2. FAST_TOPK: engaged (sample+batch -15%), byte-exact (same sha across arms),
   accept gate exact everywhere, t/s +0.2% (noise) - free but not a t/s
   lever at 200k today.
3. Arm G: meta split-state GGML_ASSERT at ggml-backend-meta.cpp:814 on the
   first sampling GET; closes the "where does it fail" question.
4. v1 vs in-split at 200k: v1 = +490 MiB/die class serving-die headroom at
   parity t/s; in-split is OOM-adjacent (9.5 MiB free). v1 wins on VRAM.

## Files

- Receipts: this file; mtpgain_{T1,F1,F2,F3,F4}_*_battery.jsonl (+ thermal
  logs), mtpgain_T1_20260922_probes.jsonl, mtpgain_V1_20260922_probes.jsonl,
  mtpgain_vram_{insplt_T1,v1_V1}_*.json, boot logs mtpgain_*_*.log.
- Boot lock: /tmp/campaign_gpu_boot.lock (held T1 -> V1, released after V1).
- Ledger: E-073 .. E-076 appended to docs/amd-port/OPTIMIZATION_PLAN_TP3_200K.md
  (renumbered keep-both after the drain desk's concurrent E-072).
