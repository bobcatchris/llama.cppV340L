W27 Q40-PREFILL DESK RECEIPT - 2026-09-24
branch amd/q40-prefill (wt-q40prefill, off amd/v340-port-v2 @ 9f0f35965),
ZERO-GPU desk behind the U1 depth window (lock held by coord-u1 all desk);
benches STAGED, not run

MISSION: implement the W24 design (W24_prefill_depth_2026-09-24.md
section 2) - extend the fa40 var-11 q4_0-direct tile path to the PREFILL
instance, env-gated separately from the validated decode arm, and stage
the oracle + timing bench so the die-3 run is one command when the lock
frees.

1. IMPLEMENTATION (diff summary, file:line)

ggml/src/ggml-cuda/fattn-tile.cuh (commit 0d973076a):
- :1627-1644 new prefill arm inside the existing `if constexpr (DKQ == 256
  && DV == 256)` HIP block of launch_fattn_tile_switch_ncols2, placed
  after the decode arm (:1608-1625):
  - :1632 gate env GGML_CUDA_FATTN_TILE_Q40_PREFILL (static const getenv,
    same pattern as the decode gate :1609; decode gate byte-identical)
  - :1633 conditions: use_gqa_opt && Q->ne[1] > 4 && K/V q4_0. The > 4
    split makes the two arms disjoint: T<=4 -> decode arm, T>4 -> prefill
    arm; with only the decode env set, prefill falls through to the f16
    cols_per_block=16 branch (:1458-1468) exactly as before
  - :1637-1638 instance of record: nthreads/nbatch_fa from the
    cols_per_block=16 config row (fattn-tile.cuh:217 = 256 nthreads,
    occ 2, nbatch_fa 32, nbatch_K 128), i.e. ncols1=8/ncols2=2 - the same
    launch geometry + parallel-block scan as the served f16 prefill arm
  - :1639 engagement line GGML_LOG_WARN naming the gate (E-117: served
    log drops ggml INFO, hence WARN)
  - :1641-1642 flash_attn_tile<256,256,8,2,softcap,11> (W17 scalar
    half2 stores), launch_fattn need_f16 = false,false: pool conversion
    and f16_extra scratch skipped at launch (same as decode arm; the
    graph-side reserve in fattn.cu get_alloc_size is untouched, exactly
    as the served decode arm ships today)
- Any non-conforming shape (no mask, max_bias != 0, K->ne[1] not a
  multiple of 256, non-q4_0 KV) still falls through to the f16 path
  unchanged - fail-safe.

docs/amd-port/tests/bench_attn_real.cu (commit c472fa1d2), per the W24
bench recipe:
- :70 T made runtime (g_T, default 4 = decode, behavior unchanged
  without flags); :379 --M N flag
- :74 :429-442 --used N: mask cols >= used filled with -inf half (f16),
  modeling the padded-n_kv causal boundary (W24 risk 3); default 0 keeps
  the all-zeros mask
- :83 :102-104 :142-152 :169-171 prefill arms: basep = f16-pool
  <256,256,8,2> (served today), v11p = <256,256,8,2,.,11> with
  need_f16=false (launch_arm_q40p), basedupp = basep determinism
  control; :377 :404-408 --prefill selects the 3-arm set
- staged-oracle DBG channel needed no changes: KT/VT/KQ dumps and the
  compare offsets are all template-generic (nbatch_fa/nbatch_K
  parameters; prefill instance = 32/128, same 256-thread shape as
  decode's 64/128, dump regions fit the existing layout)

2. BUILD (A3) - all from this tree, gfx900/ROCm 6.2.0 clang 18
- build-bench (cmake /home/chris/opt/cmake, GGML_HIP=ON Release
  GGML_NATIVE=ON CMAKE_HIP_ARCHITECTURES=gfx900 GGML_HIP_RCCL=ON
  LLAMA_CURL=OFF): BUILD-EXIT:0, libggml-hip.so 77 MB with the new
  <256,256,8,2,softcap,11> instantiation
- /tmp/fa40_bench (clean timing) + /tmp/fa40_bench_dbg
  (-DGGML_FATTN_Q40_DBG staged-oracle dumps), W17 canonical hipcc flags,
  rpath to THIS worktree's build-bench/bin: CC-EXIT:0 both

3. ORACLE + TIMING - NOT RUN (staged)

Lock /tmp/campaign_gpu_boot.lock held by coord-u1 (u1-depth window,
all 4 dies ~100% busy) for the whole desk. The runner is staged and
must be executed when the lock frees:

  bash /media/chris/ssd128/llamacpp/wt-q40prefill/docs/amd-port/scripts/run_q40prefill_bench.sh

(run_q40prefill_bench.sh, commit c472fa1d2; ~183 lines; aborts unless
/svm-hog is quiet and the lock is grabbable; single sleeps <= 150 s;
kill-by-PID law trivially satisfied - no children, one short bench
process per invocation, lock released via EXIT trap)

Runner structure:
- PCI resolution AT RUNTIME: rocminfo GPU-agent BDFID decode finds the
  HIP index whose bus = 0x0d, cross-checked against rocm-smi --showbus.
  NEVER hardcode the index: on this boot the mapping is 05:00->0,
  08:00->1, 0d:00->2, 10:00->3, i.e. PCI 0000:0d:00.0 = HIP index 2 -
  W17's "die 3 = HIP_VISIBLE_DEVICES=3" annotation does not match the
  enumeration order of this boot; the runner is immune to that class of
  error and stamps the resolution into the log
- PHASE 1 oracle (dbg build, GGML_FATTN_Q40_DBG=1, --oracle --prefill),
  9 cells, one process each:
    M=512  x depths 7168 10240 32768 65536 102400 199936
    M=268  x depth 7168                    (tail ubatch 33x8+4)
    M=268  x depth 10496 --used 10235      (padded n_kv boundary)
    M=512  x depth 10496 --used 10235      (same at ubatch M)
  adjudication per cell: basedupp bit-exact else hard fail (bench exits
  1); v11p staged KT=ok VT=ok KQ=ok required (any KT-X = the W17
  miscompile signature => GARBAGE => STOP, no variant sweep per W17
  law); dst class BIT-EXACT = pass, DUST with max_rel <= 1.3e-3 =
  DUST-PASS (owner sign-off required), anything else = GARBAGE => stop.
  Acceptance: EXACT-or-DUST at ALL cells
- PHASE 2 timing (clean build, --prefill --solo, reps 5, rep 0
  discarded in-bench): depths 10240 / 51200 / 102400 / 199936 at M=512,
  basep vs v11p, per-launch med us + % delta. niter and BENCH_WARMUP
  are sized per depth (a 199936 M=512 launch is ~seconds: the W24
  "~90 ms" estimate assumed the bandwidth bound, the latency-bound
  reality is 25 ns x 512 rows x 199936 ~ 2.6 s, so warmup is kept tiny)
- 199000 is not a multiple of 256 (instrument rejects it); 199936 is
  the bench cell that maps the served ~199k depth (W17 convention)

4. SERVED-WINDOW SPEC (U2, per W24 section 3)

Prereq: U1 window COMPLETE (baseline cells banked) AND phase 1 oracle
EXACT-or-DUST at all 9 cells AND v11p >= 30% faster per launch at 10k+
in phase 2 (W24 acceptance). Kill criteria (any one kills the arm):
- paired prompt_tps gain < +5% at the 10k cell
- any oracle GARBAGE / decode_guard breach (decode arm untouched =
  canary; E-137 ratchet must hold)

U2 command sequence for the coordinator (after the staged bench passes):

  # 0. sanity: staged runner verdicts
  cat /media/chris/ssd128/llamacpp/wt-q40prefill/docs/amd-port/results/W27_q40prefill_stamp_*.txt

  # 1. lock check-and-hold (desk stamp q40-prefill-u2), cool-die < 60 C
  # 2. boot BASEENV + GGML_CUDA_FATTN_TILE_Q40_PREFILL=1 (decode env
  #    GGML_CUDA_FATTN_TILE_Q40_DIRECT=1 stays on - candidate serve
  #    config, one boot, both arms live), canonical config
  #    -c 200000 -b 512 -ub 512 -ctk q4_0 -ctv q4_0 -fa on
  # 3. verify engagement lines in the served log (both WARN level):
  #    grep -c "GGML_CUDA_FATTN_TILE_Q40_PREFILL=1" server.log  # > 0, prefill arm live
  #    grep -c "GGML_CUDA_FATTN_TILE_Q40_DIRECT=1"  server.log  # > 0, decode arm live
  # 4. re-run U1 cells 10k/50k/100k minimum, same recipe,
  #    cache_prompt=false, void-gate adjudicated, arm-identity stamped
  # 5. paired delta vs U1 baseline per cell; apply the kill criteria
  #    above; model expectation (fresh-wall ratios): +17% @10k, +39%
  #    @50k, +51% @100k; served paired deltas may exceed at 50k+ (less
  #    wall time in the 96C state)
  # 6. teardown by PID only, release lock with evidence

5. DESK LOG
- 12:15 desk open, worktree wt-q40prefill off amd/v340-port-v2
  (9f0f35965), CHECKIN.log opened
- 12:22 A2 implemented per W24 2.2, committed 0d973076a (21 lines in
  fattn-tile.cuh, decode arm byte-identical)
- 12:26-12:29 A3: build-bench BUILD-EXIT:0; /tmp/fa40_bench{,_dbg}
  CC-EXIT:0; bench instrument committed c472fa1d2 with the staged
  runner
- 12:29 lock check: HELD by coord-u1 (u1-depth, ts 1790270573, all
  dies 99-100% busy) -> A4 stays STAGED per mission; svm-hog check
  quiet (no process) but moot while the lock is held
- 12:33 CHECKIN; receipt written
- OPEN for the bench desk: run section 3's one command when the lock
  frees; adjudication is mechanical (stamp file lines per cell); a
  GARBAGE anywhere stops the arm without further sweeps (W17 law)
