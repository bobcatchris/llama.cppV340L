W11 ATTENTION DESK - P0 DECOMPOSITION (zero-GPU) - 2026-09-23

Instrument: TP4 kernel census of record (589k dispatches,
/media/chris/ssd128/llamacpp/llama.cpp/docs/amd-port/results/TP4_kernel_census_2026-09-23_kernel_trace.csv,
agent_info maps Agent_Id 1-4 = the four gfx900 dies). Phase split: decode
rounds identified by clustering decode-class gated_delta_net launches
(<100 us) with >10 ms gaps: 31 clusters x 48 GDN at ~130.4 ms period
starting 41.68 s (server log: prompt 6964 tok, eval 3911 ms = 30 rounds).
Grid semantics decoded and cross-verified: rocprof Grid_Size_X records
gridDim.x * Workgroup_Size_X (verified on flash_attn_combine_results
where gridDim.x = Q->ne[1] is known, on dequantize_block_q4_0 where the
grid = nelements/256, and on the T=512 prefill flash where ntiles_x=32).

1. HYBRID SPLIT (src: src/models/qwen3next.cpp:17-22, is_recr_impl =
   (i+1) % full_attn_interval != 0, interval 4): 65 blocks = 16 full-attn
   + 49 gated-delta-net. Census: 16 verify flash calls/die/round,
   49 GDN launches/die/round (1488/30 = 49.6 with catch-up GDNs).

2. FLASH PATH SELECTION (built tree): gfx900 (cc 90000): no
   turing_mma (fattn.cu:457), no volta_mma (487), no WMMA (498), no
   AMD MFMA (506), no AMD WMMA (519) -> falls to fattn.cu:524-537:
   q4_0 KV is quantized -> VEC only if Q->ne[1] <= 2; served verify
   T=4 -> BEST_FATTN_KERNEL_TILE -> ggml_cuda_flash_attn_ext_tile.

3. THE EXACT INSTANCE (census name + geometry): flash_attn_tile<256,
   256, 4, 2, false> = DKQ 256, DV 256, ncols1 4, ncols2 2
   (launch_fattn_tile_switch_ncols2: gqa_ratio 6 % 2 == 0 ->
   ncols2=2 fattn-tile.cuh:1311-1315; ncols1: Q->ne[1]=4 > 4/2 ->
   cols_per_block 8, fattn-tile.cuh:1202-1210). Config (fattn-tile.cuh:216)
   (256,256,8): nthreads 256 (8 warps), occupancy 2, nbatch_fa 64,
   nbatch_K 128. LDS = Q_tmp 4096 + KV_tmp 17408 + KQ 1024 = 22528 B
   (census-exact). Grid (1, 37, 3): ntiles_x = ceil(4/4) = 1,
   parallel_blocks = 37 (fattn-common.cuh:1147-1181 scan: ntiles_dst 3,
   56 CUs x occ 2 = 112 blocks/wave, break at pb=74), z = 3 head pairs.
   Served tensor geometry: Q [256 x 4 x 6 x 1], K/V [256 x 7168 x 1 x 1]
   (ne11 = 7168 pinned by the dequant grid: 229376/32 = 7168 blocks
   x 256 el = 7168 x 256 x 1 KV head).

4. THE TILE-PATH KV TAX: need_f16_K = need_f16_V = true (fattn.cu:554-560)
   -> launch_fattn dequants the ENTIRE K and V cache q4_0 -> F16 into a
   pool buffer every flash call (fattn-common.cuh:1019-1081). Census:
   dequantize_block_q4_0 n=33/round (16 layers x K+V + catch-up),
   24.9 us each = 0.82 ms/round; writes 2 x 3.67 MB F16 per layer.
   The tile kernel then re-reads the F16 KV 3x (one full stream per
   z head-pair): 22.0 MB per call at 722 us = 30.5 GB/s effective
   (BW-bound class on this die; MMVQ 54.7 GB/s, ceiling 183.8).

5. ms/round table corrected for the hybrid (per die, decode, ~7.1k KV):
   verify flash   16 x 722.4 us (med, <256,256,4,2>)  = 11.56 ms
   catch-up flash  1 x ~722   us (draft-ctx KV ~2.4k)  = ~0.4 ms
   draft flash     3 x 61.8   us (<256,256,1,2>, gz3)  =  0.19 ms
   combine        20.6 x 21.3 us                       =  0.44 ms
   KV dequant     33 x 24.9  us                        =  0.82 ms
   KV set_rows    33 x 9.6   us                        =  0.31 ms
   FLASH CLASS TOTAL (census 390.7 ms/30 rounds + combine) = 13.0 ms/round
   gated_delta_net 49.6 x 16.4 us                      =  0.84 ms/round
   CORRECTIONS vs the round map/task brief: flash class is 13.0 not 16.1
   (the round map's own components sum to 13.4; 16.1 double-counted);
   gated-delta-net decode cost is 0.84 ms/round, NOT 3.8 ms - GDN is a
   small-kernel-class cost at TP4 decode (49 launches x 16 us); the
   flash pool is 100% in the 16 full-attn layers and scales ~linear
   with KV depth (UNBOUNDED item U1 stands).
